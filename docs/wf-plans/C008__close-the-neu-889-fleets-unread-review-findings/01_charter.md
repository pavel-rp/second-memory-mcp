# C008 — Close the NEU-889 fleet's unread review findings

**Status:** Published
**Created:** 2026-07-17  **Updated:** 2026-07-17
**Tracker:** NEU-945
**Original idea:** see 00_intake.md
**Written by:** claude-opus-4-8[1m]

## Problem & why now

The `/fleet NEU-889` run on 2026-07-16 shipped 13 sub-tasks (NEU-932…NEU-944) to merged PRs — 610, 612, 614, 615, 616, 617, 618, 624, 625, 628, 630, 631, 634 — producing the C005 DP knowledge-and-skill map under `B:/Projects/second-memory/docs/research/C005-*`. Every one of those PRs merged. An audit of the GitHub API across all 13 (2026-07-17, recorded in `00_intake.md`) found **25 inline Copilot findings, 23 of which have no reply at all**.

C008 closes two things the fleet left behind:

1. **The 16 findings its shippers never saw** — landed on the PR after the shipper had stopped looking, and adjudicated by nobody.
2. **One live, HIGH, unticketed defect in the shipped map** — `F-943-1`, which no other charter or ticket owns.

The first is the larger population and the reason the audit ran. The second is folded in because it is the only known-live defect in the C005 package with **no owner anywhere** — see OUT-7 and Assumption 4.

**C008 is four outcomes, each with a live, evidenced cause:** close the review findings nobody saw (**OUT-1**), get the three never-reviewed files actually reviewed (**OUT-3**), re-adjudicate the two severity misclassifications (**OUT-4**), and repair `F-943-1` (**OUT-7**). A fifth — a regression guard on the deliverable path's ignore rule — was cut on 2026-07-17 (OUT-6, retired): its cause is already fixed and nothing else in the charter rests on it.

The findings are not noise. Four were spot-checked against the merged tree during charter grounding:

- **PR 625** — `B:/Projects/second-memory/docs/research/C005-dp-map-coverage/03_residual-exclusion-consolidation.md:139-144`: the §8 bucket counts sum to **55** (19+21+10+3+2) while line 144 reports `Total consolidated: **52**`. The line carries a parenthetical ("some entries resolve to a shared verdict; `E3` splits into two"), so the arithmetic is not broken — it is **unreconciled**: a reader cannot get from 55 to 52. Line 146 then claims *"verified, not assumed."*
- **PR 610** — `B:/Projects/second-memory/docs/research/C005-dp-map-foundations/dry-run/00_representation-dry-run.md:36-41` vs `:87`: the specimen manifest lists `files: [mainstream.yaml, frontier.yaml]` and `owners: [SUB-6, SUB-13]` as two **parallel lists with no file→owner mapping**, yet line 87 scores probe **Q8** ("Which file do I write if I am SUB-13?") answerable "from the manifest's owner mapping." No such mapping exists in the specimen. The dry-run's headline "9/9 answerable from the specimen alone" rests on it.
- **PR 612** — **narrower than Copilot stated, and narrower than earlier drafts of this charter stated.** Copilot read `index/00_technique-index.md` as a three-way contradiction (header says `GENERATED, NEVER HAND-EDITED`; line 53 says the generator does not exist; line 59 says it is hand-maintained; `manifest.yaml:157-158` asserts `sole_writer: "generator only"`, `hand_edited: false`). **The file reconciles itself.** `index/00_technique-index.md:57-62` states the index is maintained by hand **by SUB-2 only** and that this is *"a deliberate, temporary, and logged exception to `hand_edited: false` — not a licence for mappers to edit it"*, logged as `INC-S2`. An agent reading the file therefore **can** determine it may not edit. The real residue is narrower: **the logged `INC-S2` exception is absent from `B:/Projects/second-memory/docs/research/C005-dp-map/manifest.yaml:155-161`**, which states the `hand_edited: false` rule with no reference to the exception the index records. That is *unreconciled between two files*, not broken — the same nuance this charter already applies to the 625 finding. (Current state must be re-checked before acting: NEU-944 later shipped a real generator.)
- **PR 631** — `B:/Projects/second-memory/docs/research/C005-dp-map/nodes/cl-4-optimization/frontier.yaml:507` and `:620` mark two nodes `idiom-shift` where each node's own rationale recites a test the severity vocabulary in `B:/Projects/second-memory/docs/research/C005-dp-js-materiality/00_method-and-scope.md:119-124` assigns to a higher severity. Line 511 states the accumulator "can exceed `Number.MAX_SAFE_INTEGER`" — the written test for `correctness-risk`. Lines 633-634 state the traversal "exceeds JavaScript's ~1e4 frame cap … and **must be an explicit stack**" — the written test for `blocking`. A node in the same file carrying `JS-E2` **alone** (`:792-793`) is marked `correctness-risk`, which establishes what that effect earns on its own. **This is not a mechanical mismatch** — see OUT-4 and Constraints. Both nodes under adjudication carry *multiple* effects and the vocabulary does not say which one governs; the `:793` precedent is single-effect, so it supplies the per-effect severity the aggregation rule needs rather than settling the aggregation itself.

**The reason this charter exists is not the findings — it is that nobody knew they were there.** The fleet's own reports contradict the API record:

| PR | Reported by shipper/orchestrator | Actual |
| --- | --- | --- |
| 610 | "Copilot review never attached despite two differently-shaped requests" | 5 findings posted |
| 612 | "Copilot never attached … requested_reviewers then cleared with no review" | 2 findings posted |
| 625 | no Copilot review mentioned in the final report | 5 findings posted |
| 631 | "review requested but never landed … no review posted within the capped polls" | 4 findings posted |
| 624 | "3 valid findings — all fixed in `4a93c14`" | 3 findings; thread never replied to |
| 630 | "4 findings, all valid, all fixed" | 4 findings; thread never replied to |

Four false "no review landed" reports is the timeliness argument. Those 16 findings are live in the merged tree today, behind merged PRs, with no trace on the record of anyone having seen them — that is what C008 closes, and it is worth doing whether or not anything upstream ever changes.

**Stopping the *recurrence* is a separate, owned job — not this charter's.** The behavioral causes are being fixed in the wf plugin as [WF-313](https://linear.app/neurasphere/issue/WF-313); C008 changes no skill behavior and does not wait on it.

### The 25 findings — census

| Population | Count | PRs | Treatment |
| --- | --- | --- | --- |
| **A — never seen** (shipper reported no review had landed) | **16** | 610 (5), 612 (2), 625 (5), 631 (4) | **In scope.** Adjudicate from scratch (OUT-1) |
| **B — claimed fixed, thread silent** (shipper claims fixed in code) | **7** | 624 (3), 630 (4) | **Cut — no work.** The user's answer of 2026-07-17 trusts the "already fixed in code" reports. See Assumption 1 and the accepted residue below |
| **C — already closed** (replied and fixed in `bb091e7`) | **2** | 634 (2) | None — baseline, no work |
| **Total** | **25** | | **16 in scope** |

**Accepted residue on population B, recorded rather than smoothed:** cutting B means **7 threads on 624 and 630 stay permanently unreplied**, and their "fixed" status rests on a shipper self-report — the exact claim class this charter's own first constraint distrusts. The user weighed that and chose to trust it (Assumption 1, confirmed). C008 does not reply "closed on the shipper's word" on those threads either: a reply recording trust in an unverified self-report would put a false audit trail on the record, which is worse than a silent one. The silence is the honest state. **This is a deliberate, user-authorized exception to the "a shipper's self-report is not evidence" constraint, and it is the only one.**

Separately, **3 PRs carry a false clean**: 615, 617 and 618 (617 posted it twice) attached a reviewer but reported "wasn't able to review any files" — they show 0 findings because Copilot resolved **zero files**, not because the content is clean. **Their content has never been reviewed by anything.** They are a *third* population, distinct from A and B, and they are in scope (OUT-3).

## Users & journeys

**Primary actor: the C005 package's downstream consumer** — the agent or human that reads the DP map and its audits as binding input. Entry state: they open `03_residual-exclusion-consolidation.md` §8, the dry-run's 9/9 result, or the map's `progression_stage` labels, and take them as verified fact. Path: they rely on a number they cannot reconstruct, a probe result the specimen does not support, or a stage ordering that is wrong on 6 dependencies. Value moment: after C008, every claim they read either survives an adversarial review or carries an honest, recorded caveat. Resolution: the package's own assertion — "verified, not assumed" — becomes true of the audits themselves.

**Primary actor: the creator auditing what the fleet actually did.** Entry state: they want to know whether the NEU-889 fleet's review findings were seen and addressed. Path today: the PR record cannot answer it — 23 of 25 threads are silent, so a fixed finding and an unread one look identical, and the fleet's own report is the claim class that already proved false four times. Value moment: after C008, every in-scope thread carries a verdict, so the record answers the question without trusting anyone's recollection. Resolution: the audit trail exists on the PRs themselves.

**Explicitly not a journey this charter serves: the creator running the *next* fleet.** Stopping the recurrence — a shipper that cannot merge while claiming unbacked that no review landed — is real work with a real owner, but it is [WF-313](https://linear.app/neurasphere/issue/WF-313)'s, not C008's (see Dependencies & impact). C008 cleans up after the failure; it does not change how any fleet behaves.

**Material failure case: OUT-3's re-review silently returns another false clean.** 615, 617 and 618 resolved zero files because — on this charter's leading inference — their single-file diffs (2,244 / 2,931 / 2,185 added lines) sat above a per-file review size cap. Re-presenting the *same* files at the *same* size reproduces exactly that outcome, and an agent reading "0 findings" as "clean" would re-run the original failure with C008's name on it. **OUT-3's content must therefore be presented below the cap, and a review that resolves zero files is a failure signal, never a pass.**

**Not served by this release:** `INC-C1` and `INC-C2` — already known, owned, and recorded with revision triggers in `B:/Projects/second-memory/docs/research/C005-dp-map-package/03_open-items-and-provisional-register.md`. `F-943-1` was in this list until 2026-07-17; the user folded it in (Assumption 4, reversed) because, unlike those two, it is HIGH, live, and has **no ticket anywhere**. See OUT-7.

## Outcomes

| ID | Outcome | Success measure | Verified by |
|----|---------|-----------------|-------------|
| OUT-1 | **Population A (16 never-seen findings) is adjudicated and closed.** Each is upheld or rejected on the merged tree's evidence; upheld ones are fixed; rejected ones carry a recorded reason. | 16/16 findings have a verdict; 16/16 threads carry a reply stating the verdict and, where fixed, the commit. | GitHub API: zero unreplied Copilot threads on 610, 612, 625, 631. Re-read of each cited file showing the defect gone or the rejection rationale recorded. |
| ~~OUT-2~~ | **Retired 2026-07-17 — population B cut by user answer, not deferred.** Was: "population B (7 claimed-fixed findings on 624, 630) is independently verified against the merged tree and anything still live is fixed." The user chose to trust NEU-624's and NEU-630's "already fixed in code" reports (Assumption 1, confirmed), which removes this outcome's entire population. The accepted residue — 7 threads stay unreplied on an unverified self-report — is recorded in the census above. Id retained, never reused. | n/a — not delivered | n/a |
| OUT-3 | **The 3 PRs whose review resolved zero files get the review they never got — presented below the size cap.** 615, 617, 618 content is reviewed by re-presenting it in units small enough to resolve, and the resulting findings are treated exactly like population A. **A review that again resolves zero files is a failure, not a pass.** | All 3 PRs' content obtains a review that **resolves files**; every resulting finding has a verdict and a reply per OUT-1's measure. The count of findings is discovered, not predicted. | The review record shows files reviewed (not "wasn't able to review any files"). |
| OUT-4 | **No shipped C005 verdict silently contradicts the package's own documented criteria — and where the criteria do not decide, C008 says so and states the rule it applied.** The two `frontier.yaml` severity nodes (`:506`, `:619`) are re-adjudicated against `00_method-and-scope.md:119-124` **plus the multi-effect aggregation rule stated under Constraints**, which C008 authors because the vocabulary lacks one. The re-adjudication must **engage** `frontier.yaml:636`'s recorded judgment ("idiom-shift rather than blocking because both obstacles are surmountable") and rule on whether surmountability may downgrade **at the per-effect level, where Constraints fixes that it operates** — upholding or overriding it explicitly. Silently overwriting it fails this outcome. **These are two of the four 631 findings already counted in OUT-1 — not additional findings.** | Each node's `severity` and `rationale` agree with each other, with the aggregation rule as stated, and with the documented test. The aggregation rule and the surmountability ruling are both **recorded** — as a package-level decision, not an unstated premise — and the ruling states, per effect, what each of `JS-E8`, `JS-E2` and `JS-E1` earns on its own test, so the maximum is reconstructable rather than asserted. Any change to a **NEU-889 OUT-5** verdict is recorded in the package's ledger per its own change rules. | Re-read of `frontier.yaml` against `00_method-and-scope.md:119-124`; the recorded rule is findable by a cold reader; and each single-effect precedent — `frontier.yaml:793` (`JS-E2`) and `cl-2-combinatorial.yaml:2481` (`JS-E1`) — reaches **one of three** recorded states: it **no longer conflicts**, it is **explicitly distinguished on the record**, or **the conflict it now stands in is recorded as a routed finding** naming the nodes left at odds with the rule and the owner the correction falls to. **The third state is the expected one on the uphold branch** — Constraints fix that upholding `:619` leaves the `JS-E1` `blocking` nodes conflicting and outside OUT-4's two-node bound, and that C008 does not correct them, so the first two disjuncts are unreachable there. A measure admitting only the first two would be satisfiable only by overriding, which is not a result this charter pre-decides. |
| ~~OUT-5~~ | **Retired 2026-07-17 — cut from this charter, not dropped as work.** Was: "a landed review finding can no longer go unseen, and no merge rests on an unverified 'no review landed'." The `/fleet` and `/ship` skills are being ported to the wf plugin, so this fix belongs in that codebase, where it has a branch, a PR, and CI. Filed as [WF-313](https://linear.app/neurasphere/issue/WF-313) (team WF Plugin, High). Id retained, never reused — see Dependencies & impact for the hand-off. | n/a — not delivered here | n/a |
| ~~OUT-6~~ | **Retired 2026-07-17 — cut by user answer (Q2.1), taking this charter's own "If wrong: cut OUT-6" clause.** Was: "a research deliverable cannot silently fail to be committed" — a guard against an ignore rule matching a deliverable path re-stranding untracked new files. Cut on four grounds: its original justification ("prevents the false clean recurring") **died with the retracted root cause** (Assumption 7); its real cause — the gitignore silently swallowing untracked new files — is **already fixed** by `2215786`, which removed the rule and committed the 11 stranded documents; the residual value is **speculative**, since nobody has proposed re-adding the rule; and the decomposition's guard had **no named execution trigger** (finding F2.9), so it could have shipped unwired, satisfied all three of its acceptance scenarios, and never fired on the regression it existed to catch. Unlike OUT-1, OUT-3, OUT-4 and OUT-7, it had no live cause. Id retained, never reused. | n/a — not delivered | n/a |
| OUT-7 | **`F-943-1` is closed: the map's stage, gate and depth annotations are recomputed over the edge-complete graph.** **26 of 179** depth values are wrong and **6** dependencies (across 5 nodes) order backwards by stage. The register's named fix — **re-run the depth/stage computation over the edge-complete (post-NEU-939) graph** — is the right one, but **it does not explain the whole 26, and C008 does not pretend it does:** `D-P2`'s own evidence records that the pre-NEU-939 counterfactual *"reproduces declared depth on **20/26**"*, so **the stated root cause accounts for 20; the remaining 6 — the `cl-4.matrix-exponentiation-dp` family, declared 4 / rubric 5 / pre-939 2 — match neither graph and are wrong for a further, unestablished reason.** (Not to be confused with the 6 *stage inversions*, a different set.) Recomputation corrects all 26 regardless, because it derives from the graph rather than from the diagnosis — but the 6 must be **surfaced, never absorbed**. Closing the defect also means closing `F-943-1`'s record, which is bound in **more places than any list written here can name** — so the record's closure is discharged by a **search**, not by working a list. | 0 of 179 depth values disagree with a recomputation over the edge-complete graph (from 26); 0 stage inversions across the 25 cross-cluster edges (from 6); **each of the 179 dimension-bearing nodes' `entry_gate` agrees with its recomputed stage** under `01_progression-stages.md:81-84`'s PS→gate table (`gate-a` iff PS-1, `gate-c` for PS-2/3/4) — the recomputation moves nodes across that boundary, so a gate left at its pre-recomputation value is a defect C008 itself introduced. **The rule is bounded to those 179 deliberately:** PS-0's 8 frozen roots carry no `difficulty_dimensions` block and therefore no `entry_gate` — they are the remaining 8 of the cross-reference view's 187 blocks, they sit at depth 0 by construction, and no recomputation moves them, so they are outside this check rather than swept into `gate-c` by it. **The discovery required below is performed and recorded as evidence; every hit it returns is classified and treated by its class** — live `F-943-1` sites report `F-943-1` closed, generated sites are regenerated rather than hand-edited, historical sites are untouched. The **6 unexplained values are surfaced with their residual cause stated as unestablished**, not folded silently into the corrected set. **`F-943-3` does not close with `F-943-1`.** SUB-14's re-derivation of `entry_gate` closes **`F-943-3`'s inheritance limb only** — its redundancy limb (`entry_gate` carries no independent information; Gates B and D instantiated by no node) is **preserved by the repair and its text stays true**, so **`F-943-3` remains open (Low)** and the surviving limb is routed on the record to its owner (NEU-940/NEU-888). **`F-943-3` still open after C008 is a PASS; `F-943-3` marked closed is a FAIL** — the false present-tense claim this epic exists to correct. | `B:/Projects/second-memory/docs/research/C005-dp-map-integrity/validator/audit-graph-integrity.mjs` reports zero inversions and zero depth disagreements; `B:/Projects/second-memory/docs/research/C005-dp-map-package/generator/package-completeness-gate.mjs` passes against the closed state; the recorded search output and its per-hit classification are re-runnable by a reader. |

### OUT-7's binding sites — discovered by search, not worked from a list

**The acceptance bar is the search, not the list below.** Three successive revisions of this charter each enumerated the sites `F-943-1` binds, and each enumeration was found incomplete by the next review — a second register and `D-P2`, then `PG-7a2` and `PG-7e`, then NEU-940's own `C005-dp-progression` package. The instances differed every time; **the class did not.** The lesson is not that the list needs one more pass: it is that **a hand-written binding-site list cannot be completed at charter time**, and this charter's previous escape hatch — *the list is the floor, not the ceiling; a missed site is recorded and closed, not skipped* — is precisely what let each miss through, because a floor with no obligation to look below it is just a list. **So the enumeration is demoted to a seed and the obligation is inverted: OUT-7 is met by performing a documented search and classifying everything it returns, not by working the rows below.**

**The required discovery.** Over `B:/Projects/second-memory/docs/research/`, search for — and classify every hit of — all three of:

1. **The finding ids** — `F-943-1` and `F-943-3`. (A grep at charter time returns **18 files**, materially more than any table here lists, which is the point.)
2. **The corrected values** — the 26 depth values, the 6 inverted stage orderings, and the `entry_gate` values the recomputation changes, wherever a document restates a node's dimension values rather than deriving them.
3. **The statistics derived from them** — the stage distribution, the PS-4 share, and the depth range. **This limb is not optional and is not covered by limb 1:** the `C005-dp-progression` site below states the distribution and range **without citing `F-943-1` at all**, so an id-only search misses it. That is exactly how it survived three enumerations.

**The search itself is evidence.** Its command and output are recorded so a reader can re-run it and get the same hit set; each hit is classified **live / generated / historical** on the record. A hit no row below anticipated is the search working, not an exception to it.

**The seed — a known minimum, not the bar.** The three classes decide treatment: **some sites must change, some must not.**

**1. Live bindings — assert present-tense state. For `F-943-1`'s sites, OUT-7 is not met until every one reports `F-943-1` closed. `F-943-3`'s own sites are the exception: `F-943-3` stays open (its redundancy limb survives the repair — see OUT-7's measure and Assumption 13), so their true post-C008 state is "open, inheritance limb discharged, surviving limb routed to NEU-940/NEU-888" — a site that flipped `F-943-3` to closed would be the defect, not the fix.**

| Site | What binds |
| --- | --- |
| `B:/Projects/second-memory/docs/research/C005-dp-map-package/03_open-items-and-provisional-register.md:35-49` (§1) | The package's primary register — `F-943-1` **unresolved**, owner NEU-940, revision trigger. **Flips to `F-943-1` closed.** |
| `B:/Projects/second-memory/docs/research/C005-dp-map-package/03_open-items-and-provisional-register.md:106` (§1.5) | The same register's `F-943-3` entry. **`F-943-3` stays open (Low)** — its redundancy limb survives the repair — with the inheritance limb recorded discharged and the surviving limb routed to NEU-940/NEU-888. **Do not flip this to closed.** |
| `B:/Projects/second-memory/docs/research/C005-dp-map-integrity/05_findings-register.md:12` | **A second register**, NEU-943's own, whose `Status` column reads **open** on `F-943-1`. A status field is a present-tense claim; leaving it "open" after closure is a false one. **Flips to closed.** |
| `B:/Projects/second-memory/docs/research/C005-dp-map-integrity/05_findings-register.md:14` | The same second register's `F-943-3` `Status`. **The true present-tense claim here is "open"** — `F-943-3` survives as an open Low; record the inheritance limb discharged and route the surviving limb to NEU-940/NEU-888. **Flipping it to closed would file the false claim, not the fix.** |
| `B:/Projects/second-memory/docs/research/C005-dp-map-schema/adjudication/01_schema-decision-ledger.md:109` (`D-P2`) | The ledger row binding `F-943-1` as an open defect, Status **unresolved**. **Its own revision trigger names precisely what OUT-7 does** — *"a re-run of NEU-940's depth-and-stage computation over the edge-complete graph. On that re-run `F-943-1` closes, `F-943-3` closes with it, and the 26 nodes' values are re-adjudicated here."* OUT-7 is that trigger firing, so **`D-P2` resolves by its own terms as to `F-943-1`** — the trigger it named has fired. **Its "`F-943-3` closes with it" clause is a forecast, not a finding, and does not decide `F-943-3`:** the superseding entry records `F-943-3`'s status against what the substance re-check actually established (an open Low, surviving limb routed to NEU-940/NEU-888), not against what `D-P2` predicted in advance. |
| `B:/Projects/second-memory/docs/research/C005-dp-map-package/generator/package-completeness-gate.mjs:155-184` (PG-7) | Executable. Actively asserts the item is surfaced as **unresolved**. It will fail loudly on the closed state — which is the intended behavior of a gate, not a problem to route around. |
| `B:/Projects/second-memory/docs/research/C005-dp-map-package/04_package-completeness-gate.md:76-82` (the `PG-7a`…`PG-7e` results table) and `:183` | **The gate's human-readable results doc — a distinct file from the executable above, and hand-written.** Its own header states *"The gate is a script, not this prose … Prose that disagrees with the script is wrong; the script is the artifact"* — so when the script's verdicts change, this table's `✅`s are stale prose by its own rule. `:183` likewise certifies "the **known** defect (`F-943-1`)" present-tense. **The results table is live; the authoring-history narrative at `:87-97` — how `PG-7a` was hardened during authoring — is historical and stays.** |
| `B:/Projects/second-memory/docs/research/C005-dp-map-package/README.md:39`, `:90`; `06_caps-and-incomplete-scope.md:65` (§4.2); `05_cold-context-dry-run.md:46`, `:191` | The package's present-tense self-description — "an open defect in the shipped map", "`F-943-1` ships open". |
| `B:/Projects/second-memory/docs/research/C005-dp-progression/01_progression-stages.md:108-110` | **NEU-940's own package — the statistics derived from the very values OUT-7 rewrites**, stated present-tense: *"Observed distribution: PS-1 20 · PS-2 32 · PS-3 36 · PS-4 91"*, *"**PS-4 holds 51% of the graph**"*, *"The depth range is 1–9"*. Every one changes on recomputation — correcting 26 under-reported depths drops PS-1 to **≤19** on the bitmask node alone (`cl-3-state-compression.yaml:274` is `PS-1`/depth 1 against a rubric depth of 4). **Live, and reached only by limb 3:** the passage names no finding id. Its cited source, `04_consistency-check.md`, **does not exist in the package** — so the distribution is restated here with nothing behind it to regenerate from, and the restatement is the binding. |

**2. Generated — regenerated, never hand-edited.** `B:/Projects/second-memory/docs/research/C005-dp-map-package/01_cross-reference-view.md` and the 26 per-node warning markers in it are **emitted by** `B:/Projects/second-memory/docs/research/C005-dp-map-package/generator/build-cross-reference.mjs` from a computed set. A correct recomputation drops them automatically; hand-removing them is the same category error `sole_writer` exists to prevent, and `D-P1`'s own revision trigger already requires regeneration when the map changes.

**3. Historical — records of what was true then, correctly left standing.** These must **not** be edited; rewriting them would falsify a true record of a past decision:
- `B:/Projects/second-memory/docs/research/C005-dp-map-package/06_caps-and-incomplete-scope.md:46` (§3, "What NEU-944 deliberately DECLINED to do"). NEU-944 **did** decline the repair, on a named ground, and that remains true forever. The row stays; what closes is the `D-P2` binding it created (a live site above).
- `B:/Projects/second-memory/docs/research/C005-dp-map-schema/adjudication/01_schema-decision-ledger.md:221` (`AC-6-P`) — *"`F-943-1` is bound as an explicit, prominent, unresolved element … **Pass**"*. This was a true pass record at NEU-944's ship. It becomes false as a present-tense claim on closure, and it is **mechanically re-checked by the gate**, so it cannot simply be ignored either. **It is superseded by the new ledger entry, never silently contradicted and never rewritten in place.** The ledger's own precedent governs: `AC-4-P` records that the ledger is **"UNIONED, never replaced … No row deleted, renamed, or rewritten"**, and `INC-S3` is the worked example — its text preserved verbatim with a discharge note appended. OUT-7 follows that form.

## Scope

**IN:**
- Adjudicating, fixing, and replying to the **16** population-A findings on 610, 612, 625 and 631 (OUT-1).
- Obtaining the review that never happened on 615, 617, 618 — by presenting the content below the size cap — and closing whatever it finds (OUT-3).
- Re-adjudicating the two `frontier.yaml` severity nodes against the package's written severity tests **plus the aggregation rule C008 states**, including any consequent correction to **NEU-889's** shipped OUT-5 verdicts and the ledger entry that correction requires (OUT-4).
- Repairing `F-943-1` — recomputing the 26 wrong depths, the 6 backwards stage orderings and the `entry_gate` values that follow from the recomputed stages over the edge-complete graph, and closing **`F-943-1`'s** record at **every live `F-943-1` site the discovery required under OUT-7 returns** (seeded by both registers, `D-P2`, the completeness gate, the package's self-description, and `C005-dp-progression`'s derived statistics — but bounded by the search, not by that seed), regenerating the generated view rather than hand-editing it, and superseding `AC-6-P` with a new ledger entry rather than rewriting it (OUT-7). **`F-943-3` is not closed with it: its inheritance limb is recorded discharged, but it survives as an open Low and its surviving limb is routed to NEU-940/NEU-888 (Assumption 13).**

**Every outcome above is repo-local work in `pavel-rp/second-memory-mcp`**, deliverable through the standard branch → PR → merge path — so each decomposed sub-task is a valid `/wf:ts`, `/wf:tc`, or `/ship` input. That is a hard contract on this charter, and it is why the review-gate fix was cut.

**OUT:**
- **Population B — the 7 findings on 624 and 630.** Cut by user answer 2026-07-17: their "already fixed in code" reports are trusted. Retired as OUT-2. The accepted residue is recorded in the census.
- **The review-gate / process fix — root causes 2, 3 and 4** (`--add-reviewer`'s silent false success; the capped-review rule that races the review; the absent reply requirement). Retired as OUT-5 and filed as [WF-313](https://linear.app/neurasphere/issue/WF-313) in the WF Plugin team, because the `/fleet` and `/ship` skills are being ported there — a separate codebase where the fix gets a real branch, PR and CI instead of a hand-edit from here. **C008 changes no skill behavior.** These causes survive in this charter only as the explanation of why the findings went unread.
- **`INC-C1` and `INC-C2`** — already known, owned, and carrying revision triggers in the package's open-items register. Absorbing them makes this epic unbounded and duplicates an existing decision trail. (**`F-943-1` is no longer in this list** — see OUT-7 and Assumption 4.)
- **A regression guard on the deliverable path's ignore rule.** Cut by user answer 2026-07-17 and retired as OUT-6. The failure it targeted — an ignore rule silently swallowing untracked new files — is **already remediated** by `2215786`, and no one has proposed re-adding the rule, so the guard defends a hypothetical. C008 touches `.gitignore` not at all.
- **The 2 findings on 634** — replied and fixed in `bb091e7`. Baseline, not work.
- **Any new audit of the C005 map's content** beyond what the in-scope findings and `F-943-1` raise. C008 closes findings that exist; it does not commission new review of unflagged material. OUT-3 is not an exception: it obtains the review that was already requested and silently produced nothing.
- **Revising the severity vocabulary itself.** OUT-4 states the one aggregation rule the vocabulary is missing and applies it; it does not reopen the four severity tests.
- **Re-litigating rejected findings' underlying design.** A finding rejected with a recorded reason is closed.

## Constraints & principles

- **A shipper's self-report is not evidence.** Established by this fleet: four "no review landed" reports were false against the API record. This applies to every outcome below. Verification reads the merged tree or the API, never the report. **One authorized exception exists** — population B's "already fixed" claims, trusted on the user's instruction (Assumption 1) and recorded as accepted residue in the census. That exception does not generalize: nothing else in C008 may be closed on a report.
- **Populations A, B and the false-clean three are distinct and must not be conflated.** They have different causes and different treatments: A is adjudication from scratch (in scope), B is trusted-closed (cut), and 615/617/618 have never been reviewed by anything (in scope, and needing a different method — see OUT-3). A decomposition that merges them re-runs the failure that produced them.
- **A review that resolves zero files is a failure signal, not a clean bill.** Reading that result as "clean" is what let 615, 617 and 618 merge as reviewed when nothing had been reviewed. The rule binds regardless of *why* the files failed to resolve — it does not depend on the size-cap inference being right, and it governs OUT-3 and any reading of a review result in C008.
- **The C005 package's own change rules bind any correction.** `manifest.yaml` defines `settled` as "binding … Change requires a ledger entry, never a local edit," and `sole_writer` ownership per file. OUT-4's severity corrections and OUT-7's dimension recomputation therefore go through the ledger, not a local edit.
- **The severity vocabulary does not decide OUT-4 on its own, and C008 says so rather than pretending otherwise.** The table at `00_method-and-scope.md:119-124` gives **one test per severity** but **no rule for which effect governs when a node carries several** — and both nodes under adjudication do: `frontier.yaml:506` carries `["JS-E8","JS-E2"]`, `:619` carries `["JS-E8","JS-E1"]`. `frontier.yaml:636` already resolves this by an unwritten criterion — *"idiom-shift rather than blocking because both obstacles are surmountable"* — which appears nowhere in the vocabulary. **The rule C008 applies, and must record: a node's severity is the most severe of the severities its individual effects earn under the written tests.** This is C008 **filling a documented gap in the vocabulary**, not reading a rule that was already there, and it must be recorded as a decision a cold reader can find and contest. What C008 does **not** pre-decide is whether `:636`'s surmountability rationale is a legitimate downgrade under the vocabulary or an unwritten criterion — that is the judgment OUT-4 commissions, and it must be ruled on explicitly, never silently overwritten.
- **The surmountability ruling operates when scoring an individual effect — never as an aggregate downgrade.** The aggregation rule above is stated as *most-severe-wins*, and most-severe-wins has **no downgrade step**: applying surmountability after the per-effect scores were assigned would subtract from a maximum the rule already fixed, contradicting the rule in the same breath it was recorded. So surmountability enters when an effect is scored against its written test, or it does not enter at all. **This settles the level, and deliberately not the outcome** — both remain live, and OUT-4 is decidable either way:
  - Rule that `JS-E1` here **earns `idiom-shift` on its own test** — because the JavaScript realization is feasible and correct once an explicit stack is written, and surmountability is what that test's "feasible and correct" already means (`00_method-and-scope.md:123`) — and the maximum over `["JS-E8","JS-E1"]` is `idiom-shift`. **`:619`'s current severity is upheld, on an explicit ruling rather than an unwritten one.**
  - Rule that it **earns `blocking`** — because that test (`00_method-and-scope.md:121`) asks whether a JavaScript-specific technique is *required, not preferred*, which is a question about **necessity, not difficulty** — and the maximum is `blocking`. **`:619` is overridden.**
- **One piece of internal evidence the adjudication must weigh, which does not settle it.** `frontier.yaml:636` downgrades on the ground that the explicit-stack rewrite is surmountable, and cites `cl-2.implement-tree-dp-post-order-dfs` as the node that owns that rewrite. **That node carries `JS-E1` alone and is marked `blocking`** (`B:/Projects/second-memory/docs/research/C005-dp-map/nodes/cl-2-combinatorial.yaml:2437`, severity at `:2481`), with a rationale stating the recursive form "does not run at all on the standard adversarial input." It is the `JS-E1` twin of the single-effect `JS-E2` → `correctness-risk` anchor at `frontier.yaml:793` that this charter already relies on, so it **offers** a per-effect severity for `JS-E1` the way `:793` does for `JS-E2`. **It is adjudication input, not a verdict** — and it does not foreclose upholding `:619`: the two nodes cite the *same* obstacle, so the honest path to `idiom-shift` runs through the surmountability ruling itself — that surmountability legitimately lowers `JS-E1`'s per-effect score — rather than through distinguishing their facts. **That path has a consequence the adjudication must state, not absorb:** it puts `cl-2-combinatorial.yaml:2481` and the other `JS-E1` `blocking` nodes at odds with the rule, and those nodes are **outside OUT-4's two-node bound**. C008 does not correct them; the adjudication **records the conflict as a routed finding** with the same honesty the rest of this charter demands. **Whichever way it rules, it may not leave the conflict unaddressed.**
- **Findings are hypotheses, not truth** — the project's existing `/wf:address-pr` principle. Every in-scope finding gets verified before it gets fixed; a wrong finding is rejected with a reason on the thread. Grounding already narrowed two of them (625 and 612 are *unreconciled*, not broken), which is what this principle looks like in practice.
- **Verification for documentation findings is re-audit and observation, not automated tests.** Most in-scope findings are in markdown and YAML under `docs/research/`. The exceptions are executable: the integrity validator `audit-graph-integrity.mjs` and the package completeness gate `package-completeness-gate.mjs`, both of which OUT-7 must satisfy.
- **C008 changes no skill behavior.** Every outcome is repo-local content in `pavel-rp/second-memory-mcp`. The behavioral fix is WF-313's.

### Why the findings went unread — context, not C008's work

**Fixing causes 2, 3 and 4 is [WF-313](https://linear.app/neurasphere/issue/WF-313)'s job in the wf plugin, not C008's** — C008 does not edit `fleet/SKILL.md`, `ship/SKILL.md`, or any other skill. Skill paths below are cited as evidence of the cause, never as files this charter touches.

1. **Why 3 reviews resolved zero files — a per-file diff size cap. `[INFERENCE — not proven]`**

   > **⚠ Retraction.** Earlier drafts of this charter, [WF-313](https://linear.app/neurasphere/issue/WF-313), and PR **#636**'s description all claimed the zero-file reviews were caused by `/docs/research/` being in `.gitignore`. **That claim is false and is retracted.** **PR 610 merged 2026-07-16T17:19:31Z with all 13 of its changed files under `docs/research/` and received a full Copilot review with 5 findings** — while the ignore rule was still in place. The rule was not removed until commit `2215786` (2026-07-17 00:09:45+03:00 = 2026-07-16 21:09Z), **about four hours later**. Mechanically: `git add -f` makes a file **tracked**, and `.gitignore` governs only **untracked** files — the deliverable was always tracked, always in the diff, always reviewable. **WF-313 and PR #636 have both been corrected.** PR #636's *action* still stands on its real merits (cause 1', below). This charter is precise here because the false root cause already propagated into two external artifacts before it was caught.

   The leading replacement explanation is a **per-file diff size cap**, and it is an **inference from the PR record, not an established fact**:

   | PR | Largest single-file diff | Copilot outcome |
   | --- | --- | --- |
   | 610 | 13 files, max **147** lines | reviewed, 5 findings |
   | 616 | **1,747** lines (`cl-3-state-compression.yaml`) | reviewed, 2/2 files |
   | 618 | **2,185** lines (`frontier.yaml`) | **zero files** |
   | 615 | **2,244** lines (`cl-1-foundational.yaml`) | **zero files** |
   | 617 | **2,931** lines (`cl-2-combinatorial.yaml`) | **zero files** |

   The boundary sits between **~1,750 and ~2,185** added lines in a single file. Corroborating: `frontier.yaml` reviewed fine on PR **631** (a small modification) but not on PR **618** (its 2,185-line creation) — **same file, different diff size, different outcome**, which no property of the file itself explains. It is a hypothesis with a clean threshold. **It is directly testable — a throwaway PR containing one ~2,500-line file — and that test is OUT-3's, which is what promotes this from inference to fact.** Until it runs, every reliance on this claim in this charter is labelled as inference. For C008 this is *why the false-clean population exists* (615, 617, 618 → OUT-3) and why re-presenting the same files unchanged would reproduce the same nothing.

   **1'. The gitignore's real (separate, smaller) failure — already remediated.** `/docs/research/` being ignored never blocked review. What it did do is **silently swallow untracked new files**: `git add` skips an ignored path without error, so 11 durable research documents sat uncommitted until `2215786` added them, and every one of the 13 fleet PRs needed `git add -f` to get its deliverable into the diff. That is a real failure with in-repo residue, and **it is already fixed** — `2215786` removed the rule and committed the 11 stranded documents. **C008 adds no guard against its return** (OUT-6, retired 2026-07-17): the cause is remediated, nobody has proposed re-adding the rule, and the guard would defend a hypothetical. Recorded here as the honest account of what the gitignore actually broke — which has nothing to do with review coverage — not as work.

2. **`gh pr edit --add-reviewer` reports success while silently not registering.** Observed first-hand by NEU-941; a direct `gh api` call did register. `ship/SKILL.md:45` treats the exit code as sufficient and only falls back on an *error*, so a silent non-registration is invisible. **→ WF-313.**
3. **The capped-review rule races the review, and it is written down.** `C:/Users/recky/.claude/skills/fleet/SKILL.md:114` instructs shippers: *"after requesting/re-requesting a code review, poll at most twice (~2 min apart); if nothing lands, note it on the PR and proceed to merge — never wait indefinitely."* This directly overrides `C:/Users/recky/.claude/skills/ship/SKILL.md:150-151`, which caps at ~20 min and states *"do not merge unreviewed."* Copilot typically takes 1-3 min; two polls at 2 min is a coin flip, and the loser reports "no review landed" as fact. **→ WF-313.** **For C008 this is why population A exists** — 610, 612, 625 and 631's findings landed after their shipper had stopped looking, so they were never adjudicated. It is the single cause C008 is cleaning up after.
4. **No reply was ever required**, so a landed finding left no trace of being seen. 23 of 25 threads are silent, which is precisely why the failure went unnoticed until the audit. **→ WF-313** for the requirement going forward. For C008 this is *why the audit trail must be restored by hand*: the reply is the only durable evidence a finding was adjudicated, so OUT-1's and OUT-3's measures are thread replies, not code changes alone.

## Dependencies & impact

- **Outbound hand-off — the review-gate fix goes to [WF-313](https://linear.app/neurasphere/issue/WF-313) (team WF Plugin, High):** *"Harden the review gate: a shipper must not merge while claiming no review landed."* Root causes 2, 3 and 4 live in the `/fleet` and `/ship` skills, which are being ported to the wf plugin — a separate codebase with its own team and pipeline. **This is a hand-off, not a dependency:** every C008 outcome completes whether or not WF-313 has shipped, and WF-313 stops the *recurrence* without closing a single one of the in-scope findings. **WF-313 carried the false gitignore root cause and has been corrected** — anyone reading an older revision of it should re-read cause 1 above. It also carries the related `/fleet` lessons from the same run (shipper sub-fan-out, Step 0 data-dependency blindness, the `TaskOutput` liveness check, `git status` blindness under a gitignored path — which is cause 1', not a review-coverage issue).
- **OUT-3's method depends on an unproven inference.** If the size-cap inference is wrong, presenting the content in smaller units may not change the outcome, and OUT-3's boundary probe is what tells the difference. A probe that reviews a ~2,500-line file successfully **falsifies the inference and re-opens the question of why three reviews resolved nothing** — that is a re-scoping trigger, not a failure of OUT-3.
- **OUT-4 impacts shipped verdicts.** Correcting either node changes NEU-889's shipped OUT-5 conclusions and requires a ledger entry under the package's own rules. Downstream consumers that already read those verdicts as settled are affected. The aggregation rule OUT-4 records is itself a new package-level decision, binding on any future multi-effect node.
- **OUT-7 crosses a deliberate ownership boundary and cascades into the package's own gates.** The register at `03_open-items-and-provisional-register.md:35-49` assigns `F-943-1` to **NEU-940's owner** and states the repair is explicitly **out of SUB-11's scope** — writing dimension values under an assembly spec is "the exact category error the charter's file-ownership rules exist to prevent." C008 takes ownership of the repair; the `sole_writer` model still governs *which files* the fix may touch (the CL-3/CL-4 dimension values). Closing the defect also **invalidates the package's current self-description**: `package-completeness-gate.mjs:155-184` (PG-7) actively asserts that `F-943-1` is surfaced as **unresolved**, that all **26** affected nodes carry a per-node warning marker, and that the register names an owner and a revision trigger. Repairing the map without closing the record turns a passing gate into a false assertion — so the record's closure is part of the outcome, not follow-up. **The record is wider than one register, and wider than this charter can enumerate:** a second register (`C005-dp-map-integrity/05_findings-register.md`), the ledger's `D-P2` row, the ledger's `AC-6-P` pass record, and NEU-940's own `C005-dp-progression` distribution claims all bind the item too — each found only after a review, which is why OUT-7 discharges the record's closure by a **required search** rather than by a list, and classifies every hit live / generated / historical: the treatment differs by class, and getting it wrong in either direction (a live `F-943-1` site left open, or a historical record rewritten) produces a new inconsistency of its own. (`F-943-3`'s own live sites are the deliberate exception — `F-943-3` survives as an open Low, so leaving them open is the correct state, not a miss; see OUT-7's measure and Assumption 13.) **`entry_gate` is part of this cascade** — it is a documented function of `progression_stage` (`C005-dp-progression/01_progression-stages.md:81-84`), so recomputing stages moves nodes across a gate boundary that nothing currently checks: the validator tests depth agreement and stage monotonicity only, and neither `.mjs` file asserts on `entry_gate` at all. Left out of the measure, OUT-7 would pass over a map C008 had just made newly inconsistent.
- **The merged tree has moved since the fleet ran.** `docs/research/` is now tracked (`2215786`), and NEU-944 shipped generators (`B:/Projects/second-memory/docs/research/C005-dp-map-package/generator/build-cross-reference.mjs`, `package-completeness-gate.mjs`). Note the artifact NEU-944 shipped is a **cross-reference builder and a completeness gate**, not necessarily the technique-index generator that 612's finding concerns — that must be checked, not assumed, because if a real index generator now exists, `INC-S2`'s exception has ended and 612's residue has changed shape. Every in-scope finding must be re-checked against the tree as it stands, not as the PR left it.
- **The C005 package's file-ownership model** (`sole_writer` per file, per `manifest.yaml`) constrains who may write what. It is what made the original fan-out safe and it applies to C008's fixes too.

## Risks

| Risk | Severity | Mitigation |
| --- | --- | --- |
| **A second false all-clear.** An agent reports findings closed without the threads actually being replied to, or reads OUT-3's re-review resolving zero files as "clean" — reproducing the exact failure being remediated. | **High** | Every outcome's success measure is an API or merged-tree observation, not a report. "Zero files resolved" is a constraint-level failure signal. The closing check is a fresh API sweep for unreplied Copilot threads **on 610, 612, 625, 631 and the three re-reviewed PRs — explicitly excluding 624 and 630**, whose silence is the accepted, user-authorized residue and must not be re-flagged as a defect. Run by something other than the agent that did the work. |
| **The size-cap inference is wrong**, so OUT-3's slicing does not produce a review and three PRs' content stays unreviewed. | **High** | The inference is labelled as such everywhere it is relied on, and OUT-3 carries the probe that tests it directly. A failed probe re-opens the cause with better evidence rather than leaving a false explanation in the record — which is the failure mode the retracted gitignore claim already demonstrated. |
| **OUT-7 breaks the package's own gate**, because `package-completeness-gate.mjs` asserts `F-943-1` is open and 26 nodes carry its warning marker. A repair that ignores the gate turns a passing check into a lie. | **High** | Closing the record is **part of OUT-7's measure** rather than a follow-up, discharged by the required discovery so the gate's bindings are reached whether or not this charter named them. The gate is executable and will fail loudly if skipped. |
| **OUT-7 closes the map but not the record**, leaving `F-943-1` repaired while some site still calls it open — the exact new inconsistency OUT-7's own text warns against. **Three enumerations of the binding sites have each been found incomplete by the next review**, so the risk is not that a list is short but that any hand-written list is. | **High** | **The measure is the discovery, not the list.** OUT-7 requires a recorded search over `docs/research/` on the finding ids, the corrected values, **and** the statistics derived from them — the third limb because the site missed longest cites no id — with every hit classified live / generated / historical. The seed table is a known minimum; a hit it did not anticipate is the search working. The check is re-running the search, not re-reading the list. |
| **OUT-7 introduces a new inconsistency it is not measured on** — `entry_gate` is a function of `progression_stage`, the recomputation moves nodes across the PS-1/PS-2 gate boundary, and **nothing in the package asserts gate/stage agreement**: the validator checks depth and monotonicity only, and neither `.mjs` asserts on `entry_gate`. A PS-4 node would ship carrying `gate-a` with every gate green — the "second false all-clear" this table's first row names, caused by C008 itself. | **High** | Gate/stage agreement is part of OUT-7's success measure: each of the 179 dimension-bearing nodes' `entry_gate` is **re-derived** from its recomputed stage per `01_progression-stages.md:81-84` (`gate-a` iff PS-1, `gate-c` for PS-2/3/4), never carried forward. PS-0's 8 roots carry no gate and no dimension block, so the rule does not reach them. |
| **OUT-7 over-corrects and falsifies a true historical record** — rewriting `AC-6-P` or NEU-944's "declined" row so the package claims it never bound the item open. | Medium | The historical class is named explicitly and its treatment is the ledger's own standing precedent (`AC-4-P`: unioned, never replaced; `INC-S3`'s appended discharge note). `AC-6-P` is **superseded by a new entry**, not edited. |
| **OUT-4 destabilizes shipped conclusions** — *if* the re-adjudication upholds Copilot's reading, changing two severities cascades into consumers that read NEU-889's old OUT-5 verdicts as settled. | Medium | Any change goes through the package's ledger, which exists for exactly this. The adjudication is bounded to two nodes; upholding the current severities — on an explicit ruling that surmountability legitimately downgrades — is an equally valid result. |
| **OUT-4's implementer treats the re-adjudication as mechanical** and silently overwrites `frontier.yaml:636`'s considered judgment, replacing a recorded rationale with an unrecorded rule. | Medium | The gap is named in Constraints, the aggregation rule is stated rather than implied, and OUT-4's measure requires the surmountability rationale to be ruled on and the rule to be recorded. |
| **Population B's cut is wrong** and 624/630 contain live defects nobody will now find. | Medium | Accepted, explicitly, on the user's instruction (Assumption 1) and recorded as residue in the census rather than hidden. Grounding's spot-check of 630's four findings against the merged tree found them addressed, which is corroboration for 4 of the 7 — **624's three were never opened.** If wrong: re-open OUT-2. |
| **OUT-3's fresh review floods scope** with findings on three previously-unreviewed PRs whose volume is unknown until the review runs. | Medium | Scope is stated as outcome-shaped (every resulting finding gets a verdict and a reply); the count is discovered, and a large result is a re-scoping trigger, not a silent overrun. |

## Assumptions & decisions

| # | Assumption / decision | Source | Status |
|---|----------------------|--------|--------|
| 1 | **Scope is only the ~16 findings the shippers never saw** (610 ×5, 612 ×2, 625 ×5, 631 ×4). The 7 "already fixed" findings on 624 and 630 are **trusted on the shippers' reports** and cut. The host's default was "verify all 25"; the user overruled it. This does **not** touch 615/617/618, a third population whose content has never been reviewed at all and which stays in scope via OUT-3. Consequence, accepted: 7 threads stay unreplied and their status rests on an unverified self-report. If wrong: re-open OUT-2 and verify 624/630 against the merged tree. | User answer 2026-07-17 (deferred Q1, answered) | **confirmed** |
| 2 | **The review-gate / process fix is NOT in scope — C008 is content-only.** The host's default was to include it; the user overruled that. The `/fleet` and `/ship` skills are being ported to the wf plugin, so root causes 2, 3 and 4 are fixed there, where they get a branch, PR and CI — filed as [WF-313](https://linear.app/neurasphere/issue/WF-313) (team WF Plugin, High). The fix is real and is being done, **just not in this charter**. OUT-5 is retired accordingly and C008 changes no skill behavior. | User answer 2026-07-17 (deferred Q2, answered) | **confirmed** |
| 3 | **PR 631's severity findings are re-adjudicated, not merely flagged** (OUT-4) — an adjudication that upholds Copilot's reading would change **NEU-889's** shipped OUT-5 verdicts (that charter's JS-materiality outcome, owned by SUB-8/NEU-941 — unrelated to C008's retired OUT-5). The user overruled the host's revised "flag only" recommendation. **The re-adjudication is not pure rule-application:** the vocabulary carries no multi-effect aggregation rule, so C008 states one (see Constraints) and the surmountability question is left genuinely open for the adjudication to rule on. The result is not presumed here. | User answer 2026-07-17 (deferred Q3, answered) | **confirmed** |
| 4 | **`F-943-1` is folded into C008** (OUT-7); `INC-C1` and `INC-C2` stay out. The host's default was "keep all three separate"; the user **reversed** it for `F-943-1` alone, because it is HIGH, live (26/179 depths wrong, 6 dependencies sequencing backwards), its fix is known, and — unlike the review-gate fix, now owned by WF-313 — it has **no ticket anywhere**. `INC-C1`/`INC-C2` remain owned and carry revision triggers, so they stay out. **Consequence: C008's spine is no longer strictly "findings nobody saw"** — it is "close the findings nobody saw, and repair the one live defect nobody owns." The Problem statement says so. | User answer 2026-07-17 (deferred Q4, answered) | **confirmed** |
| 5 | **The 3 zero-file-resolution PRs (615, 617, 618) get a real review** (OUT-3), because their "0 findings" is an artifact of the review resolving no files, not a clean bill. **The causal half of this assumption was corrected on 2026-07-17:** the earlier claim that the gitignore caused the zero-file resolution is **retracted and false** (PR 610 was fully reviewed under the same ignore rule). The re-review is still warranted — the content is demonstrably unreviewed either way — but the *method* now rests on the size-cap inference, which is unproven and carries its own probe. If wrong: OUT-3 is dropped and their content ships unreviewed by anything. | Codebase fact (the reviews resolved zero files) + inference (why); the deferred questions never asked | **confirmed** (that a review is needed); the **cause is `[INFERENCE]`** |
| 6 | **The four spot-checked findings (625, 610, 612, 631) are real defects, live in the merged tree today** — though two are narrower than Copilot stated: 625's arithmetic is *unreconciled*, not wrong, and 612's index *does* reconcile itself, leaving only the `INC-S2` exception's absence from `manifest.yaml`. Population A therefore does not need re-litigating at the charter level — only adjudication. | Codebase fact — files read at the paths cited in Problem & why now | confirmed |
| ~~7~~ | **RETRACTED 2026-07-17 (finding F1.1, CRITICAL).** Was: "root cause 1 (gitignored deliverable) is already remediated, so OUT-6 is regression prevention, not a fix" — with the causal claim that the gitignore blocked review. **The causal half is false:** PR 610 received a full 5-finding review with all 13 files under `docs/research/` while the ignore rule was live, four hours before `2215786` removed it. `git add -f` makes a file tracked, and `.gitignore` governs only untracked files. The factual half survives as Assumption 11. Id retained, never reused. | Falsified by the PR record; host-verified | **retracted** |
| 8 | **Population C (2 findings on 634) needs no work** — both replied and confirmed fixed in `bb091e7`. With population B cut, 25 = **16 in scope** + 7 trusted-closed + 2 baseline reconciles with zero remainder. | Intake evidence — GitHub API audit | confirmed |
| 9 | **The anomalous review-body shape on 614 and 624** (a "Pull request overview" narrative omitting the standard "reviewed N of M / generated K comments" summary line) is a reporting-format variance, not a distinct defect. With population B cut, this no longer affects any C008 outcome — it survives only as a pipeline concern ("don't read a missing summary as no review") for WF-313, recorded here so the hand-off carries it. | Inferred from intake evidence | **[unconfirmed]** — no longer material to C008 |
| 10 | **OUT-6 is CUT.** Revision 1 kept it on a re-justification (the ignore rule silently swallowing **untracked new files** — the 11 stranded documents and `git add -f` on all 13 fleet PRs) while conceding it was "the weakest outcome in the charter" and naming its own escape clause: *"If wrong: cut OUT-6."* **The user took that clause.** Four grounds: its original rationale ("prevents the false clean recurring") **died with Assumption 7's retraction**; its real cause is **already fixed** by `2215786`; the residual value is **speculative** — no one has proposed re-adding the rule, so the guard defends a hypothetical; and the decomposition's guard had **no named execution trigger** (F2.9), so it could have shipped unwired, satisfied all three of its acceptance scenarios, and never fired on the regression it existed to catch. **C008 is now four outcomes — OUT-1, OUT-3, OUT-4, OUT-7 — each with a live, evidenced cause.** | User answer 2026-07-17 (Q2.1, answered) — taking this charter's own escape clause | **confirmed — cut** |
| 11 | **The gitignore rule is gone from the merged tree.** `.gitignore` no longer matches `research` — removed by `2215786` ("chore: track docs/research and commit the durable research docs", 2026-07-17 00:09), which also committed the 11 stranded documents. This is the surviving factual half of retracted Assumption 7, with its false causal claim removed. | Codebase fact — `.gitignore` read; `2215786` inspected (12 files, `.gitignore` −1 line, 11 docs added) | confirmed |
| 12 | **C008 authors the multi-effect severity aggregation rule** — *a node's severity is the most severe of the severities its individual effects earn under the written tests* — because `00_method-and-scope.md:119-124` gives one test per severity and no rule for nodes carrying several, while both nodes under adjudication do (`frontier.yaml:506`, `:619`). This is a gap-filling decision, recorded as such, not a reading of an existing rule. **Corollary, added in Revision 2 (F2.1): because most-severe-wins has no downgrade step, the surmountability ruling operates when scoring an individual effect, never as an aggregate downgrade** — otherwise it would subtract from a maximum this same rule fixes. It still does not pre-decide OUT-4: whether `:636`'s "surmountable" rationale legitimately downgrades **`JS-E1`'s own per-effect score** is the open judgment the adjudication must rule on, and both severities remain reachable through it. If wrong: the adjudication records a different rule, but it must record *some* rule, at a *stated level* — leaving either unstated is what F1.11 and F2.1 identified. | Inferred — codebase fact (the gap is real, verified by reading the table and both nodes) + the user's answer to re-adjudicate | **[unconfirmed]** |
| 13 | **`F-943-3` survives C008 as an open Low; only its inheritance limb closes.** SUB-14 re-derives `entry_gate` as a deterministic function of `progression_stage`, which **discharges `F-943-3`'s inheritance limb** (the gate was untrustworthy only because the stage it derives from was wrong) but **preserves the determinism `F-943-3` reports** ("carries no independent information"; Gates B and D instantiated by no node) — so that limb's text stays true and `F-943-3` stays open. OUT-7's measure therefore admits `F-943-3` open as a **PASS** and treats "`F-943-3` marked closed" as a **FAIL**; the surviving limb is routed to its owner (NEU-940/NEU-888). Marking it closed would file the exact false present-tense claim this epic exists to correct. The decomposition already reflects this (SUB-15 closes only the limbs the evidence closes). | User answer 2026-07-17 (round-3 flag, answered) | **confirmed** |

### Clarification log

- 2026-07-17 Q: Scope — all 25 findings, or only the ~16 the shippers never saw? — A: **Answered — only the ~16 never seen.** The host's default (verify all 25) is overruled: NEU-624's and NEU-630's "already fixed in code" reports are trusted and population B's verification is cut. This does **not** touch 615/617/618, a third population whose content has never been reviewed at all. (affects: OUT-2 retired, census, Scope IN/OUT, Constraints — one authorized exception to the self-report rule, Risks — closing check excludes 624/630, Assumption 1 → confirmed)
- 2026-07-17 Q: Is the process fix in scope, or content only? — A: **Answered — content only.** The `/fleet` and `/ship` skills are being ported to the wf plugin, a separate codebase with its own Linear team (WF Plugin); the process fix is filed there as [WF-313](https://linear.app/neurasphere/issue/WF-313) "Harden the review gate: a shipper must not merge while claiming no review landed" (High). (affects: OUT-5 retired, Scope IN/OUT, Users & journeys, Constraints, Dependencies & impact, Risks, Assumption 2 → confirmed)
- 2026-07-17 Q: The decomposer flagged that OUT-5's fixes edit user-global skill files with no branch, PR or review pipeline — how is that work delivered and tracked? — A: **It is not delivered here.** WF-313 owns it in the wf plugin, where the normal pipeline applies. Every remaining C008 outcome is repo-local. (affects: the same sections; closes the open question on out-of-repo delivery)
- 2026-07-17 Q: PR 631's severity findings on NEU-889's OUT-5 verdicts — re-adjudicate or flag? — A: **Answered — re-adjudicate against the written test.** The host's revised "flag only" recommendation is overruled. **But the written test does not decide it alone** — the vocabulary has no multi-effect aggregation rule and both nodes carry multiple effects, so the charter states the rule it applies and leaves the surmountability judgment open. (affects: OUT-4, Constraints, Risks, Assumptions 3 → confirmed and 12 → new)
- 2026-07-17 Q: Relationship to F-943-1 / INC-C1 / INC-C2? — A: **Answered — fold `F-943-1` in; keep `INC-C1`/`INC-C2` out.** The host's "keep separate" default is reversed for `F-943-1` alone: it is HIGH, live, its fix is known, and it has no ticket anywhere. (affects: OUT-7 added, Problem & why now — the spine is restated, Scope IN/OUT, Users & journeys, Dependencies & impact, Risks, Assumption 4 → confirmed/reversed)
- 2026-07-17 Q: Keep or cut OUT-6 now that its original justification is retracted? — A: **Answered — cut it.** The user took this charter's own "If wrong: cut OUT-6" clause. Its rationale died with the retracted root cause; its real cause is already fixed by `2215786`; the residual value is speculative (nobody has proposed re-adding the rule); and its guard had no named execution trigger, so it could have shipped unwired and satisfied every acceptance scenario while never firing. **C008 is now four outcomes — OUT-1, OUT-3, OUT-4, OUT-7.** (affects: OUT-6 retired, Problem & why now — the spine restated to four, Scope IN/OUT, Constraints — cause 1', Assumption 10 → confirmed as "cut")
- 2026-07-17 **Correction (not a question) — the surmountability ruling's level is fixed (F2.1).** Revision 1 stated the aggregation rule (*most-severe-wins*) but never said at which level `frontier.yaml:636`'s surmountability rationale operates, and the two readings gave opposite results — leaving OUT-4 undecidable from the charter. **Fixed: surmountability operates when scoring an individual effect, never as an aggregate downgrade**, because most-severe-wins has no downgrade step and applying one after the fact would contradict the rule in the same breath it was recorded. **The level is settled; the outcome deliberately is not** — both `idiom-shift` (upheld) and `blocking` (overridden) remain reachable through a per-effect ruling, per Assumption 3. (affects: Constraints, OUT-4, Assumption 12)
- 2026-07-17 **Correction (not a question) — OUT-7's binding sites are enumerated (F2.2).** The measure named three sites ("the register, the ledger, and the package's completeness gate") while `F-943-1` is bound open in more: a **second register** (`C005-dp-map-integrity/05_findings-register.md:12`, `:14`), the ledger's **`D-P2`** row, and the ledger's **`AC-6-P`** pass record — so the measure permitted exactly the inconsistency OUT-7's own text warns against. The sites are now enumerated and split three ways — **live** (must flip), **generated** (regenerated, never hand-edited), **historical** (left standing) — because the treatment differs by class and erring in either direction creates a new inconsistency. (affects: OUT-7 + new "binding sites" subsection, Scope IN, Dependencies & impact, Risks — two rows added)
- 2026-07-17 **Correction (not a question) — root cause 1 is retracted.** The claim that a gitignored `/docs/research/` caused the zero-file reviews on 615/617/618 is **false**: PR 610 merged 2026-07-16T17:19:31Z with all 13 changed files under `docs/research/` and received a full 5-finding review while the rule was live; `2215786` removed it four hours later; and `git add -f` makes files tracked, which `.gitignore` does not govern. The replacement is a **per-file diff size cap**, held as a labelled **inference** with a boundary of ~1,750–2,185 added lines and a probe that tests it. The gitignore's real failure is separate and smaller (untracked new files silently skipped). The false cause had already propagated to WF-313 and PR #636; **both have been corrected**. (affects: Problem & why now, Users & journeys — material failure case, OUT-3, OUT-6, Scope OUT, Constraints — root causes 1 and 1', Dependencies & impact, Risks, Assumptions 5 corrected / 7 retracted / 10 re-justified / 11 added)

- 2026-07-17 **Correction (not a question) — OUT-7's enumeration becomes a discovery requirement (F3.5).** Round 3 found a **third** missed binding site — NEU-940's own `C005-dp-progression/01_progression-stages.md:108-110`, whose distribution ("PS-1 20 · PS-2 32 · PS-3 36 · PS-4 91", "PS-4 holds 51%") and depth range ("1–9") all change once the 26 under-reported depths are corrected — after Round 2 found `PG-7a2`/`PG-7e` and Round 1's fix found a second register, `D-P2` and `AC-6-P`. **The instances differed each round; the class did not, so appending three more rows would invite a fourth miss.** Fixed at the class: the enumeration is demoted to a **known-minimum seed** and the acceptance bar becomes a **required, recorded search** over `docs/research/` on (1) the finding ids, (2) the corrected values, and (3) the statistics derived from them — with every hit classified live / generated / historical. **Limb 3 is what the previous escape clause could not supply:** the progression site cites no finding id, so no id-grep reaches it. The clause *"the list is the floor, not the ceiling"* is retired — a floor carrying no obligation to look beneath it is just a list, which is what F2.2 already found insufficient. (affects: OUT-7 measure + "binding sites" subsection rewritten as seed + discovery, Scope IN, Dependencies & impact, Risks — one row rewritten)
- 2026-07-17 **Correction (not a question) — `entry_gate` joins OUT-7's measure (F3.1, CRITICAL — charter half).** `entry_gate` is a documented function of `progression_stage` (`01_progression-stages.md:81-84`: PS-1 → Gate A; PS-2/3/4 → Gate C) and the recomputation **moves nodes across that boundary** — `cl-3-state-compression.yaml:273-276` carries `PS-1`/`gate-a`/depth 1 against a rubric depth of 4 (→ PS-4 → Gate C). **Nothing catches an inconsistent gate:** the validator asserts on depth agreement and stage monotonicity only, and neither `.mjs` file asserts on `entry_gate` at all. Without this, OUT-7's measure would pass over a map C008 had just made newly inconsistent — the "second false all-clear" this charter's own High risk names. Gate re-derivation from the recomputed stage is now part of the measure; the write target is the decomposer's to assign. (affects: OUT-7 measure, Scope IN, Dependencies & impact, Risks — one row added)
- 2026-07-17 **Correction (not a question) — OUT-7's causality is split 20/6 (F3.8).** OUT-7 claimed NEU-940's values were computed against the pre-NEU-939 graph **"so"** 26 depths are wrong. **The package's own evidence contradicts the "so":** `D-P2` records that the counterfactual *"reproduces declared depth on **20/26**"*, and the validator agrees — 6 nodes (the `cl-4.matrix-exponentiation-dp` family: declared 4 | rubric 5 | pre-939 2) match **neither** graph. The stated root cause explains **20, not 26**; 6 are wrong for a further, unestablished reason. **In a charter convened over a total a reader cannot reconstruct from its parts (the 625 finding), shipping the same unreconciled shape is self-defeating.** The split is now stated, and surfacing the 6 — rather than absorbing them into the corrected set — is a measure requirement. (affects: OUT-7, Open questions)
- 2026-07-17 **Correction (not a question) — OUT-4's measure admits the routed-conflict outcome (F3.4).** The measure offered two disjuncts — the single-effect precedents (`frontier.yaml:793`, `cl-2-combinatorial.yaml:2481`) "either no longer conflict or are explicitly distinguished on the record" — and **both are unsatisfiable on the uphold branch this charter declares valid.** Constraints state that upholding `:619` leaves the `JS-E1` `blocking` nodes "at odds with the rule", that "C008 does not correct them", and that the honest path to `idiom-shift` runs through the surmountability ruling "rather than through distinguishing their facts". The real third outcome — **recording the conflict as a routed finding** — was missing, making OUT-4 verifiable only on the override branch and re-introducing the one-sidedness F2.1 was raised to remove. **The drift was the charter's measure alone; Constraints already stated the outcome correctly and are unchanged.** (affects: OUT-4 § Verified by)
- 2026-07-17 Q: OUT-7 asserted `F-943-3` "is resolved with it." The decomposer flagged that SUB-14's substance re-check can close only `F-943-3`'s **inheritance** limb — SUB-14 re-derives `entry_gate` **as** a deterministic function of stage, so the redundancy `F-943-3` reports ("carries no independent information"; Gates B and D instantiated by no node) is **unchanged and its text stays true**. Is OUT-7 met with `F-943-3` surviving as an **open Low**? — A: **Yes. Close the inheritance limb, record it, and leave `F-943-3` open (Low); route the surviving limb to its owner (NEU-940/NEU-888).** The repair *preserves* the determinism `F-943-3` describes, so its text remains accurate; marking it closed would file a false present-tense claim — precisely the failure this epic exists to correct. (affects: OUT-7's success measure now admits `F-943-3` open as a PASS; OUT-7's binding-sites subsection — the class-1 rule and rows are split so `F-943-3`'s own sites (`:14`, `:106` §1.5) are exempt from the flip-to-closed rule; `D-P2`'s "closes with it" clause de-endorsed as a forecast; Scope IN; Dependencies & impact; Assumption 13 → new)

## Open questions

None blocking. Three items are load-bearing and carry their own test or surfacing requirement rather than waiting on an answer:

- **The per-file size cap (root cause 1)** is an inference, not a fact. OUT-3 carries the probe that promotes or falsifies it. If it falsifies, OUT-3's method — not its necessity — is re-opened.
- **The multi-effect aggregation rule (Assumption 12)** is C008 filling a documented gap in the C005 severity vocabulary — *most-severe-wins*, with the surmountability ruling operating at **per-effect scoring**. Both the rule and its level are stated so they can be contested before OUT-4 is adjudicated, rather than discovered afterwards in a diff. **What the rule leaves open is deliberate:** whether surmountability legitimately downgrades `JS-E1`'s per-effect score is OUT-4's to rule on, and both severities remain reachable through it.
- **Why 6 of the 26 wrong depths are wrong is unestablished** — the `cl-4.matrix-exponentiation-dp` family reproduces under neither the pre-NEU-939 nor the edge-complete graph, so `F-943-1`'s stated root cause explains 20 of 26. **This does not block OUT-7:** the recomputation derives every value from the graph rather than from the diagnosis, so all 26 are corrected either way. It is carried here because the correct repair and the correct *account* of the defect are different things, and a charter whose subject is an unreconstructable total may not ship one of its own. OUT-7's measure therefore requires the 6 to be **surfaced with their residual cause named as unestablished**. If the recomputation turns out to explain them after all, that is a finding to record, not a silent reconciliation.

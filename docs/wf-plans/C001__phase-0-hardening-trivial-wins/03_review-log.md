# C001 — Review log

Reviews: 3 · Revisions used: 2 of 3

## Round 1 — 2026-07-07

**Audited by:** claude-fable-5

```
REVIEWER — Round 1: FINDINGS
Model: claude-fable-5
Coverage: 9/9 outcomes
Findings: 9 (1 critical, 1 high, 4 medium, 3 low)
- F1.1 | CRITICAL | confidence high | route decomposer | check 11 | 02_subtasks.md § SUB-8: Re-include decayed prerequisites by gating mastery on retrievability | quote: "Given a prerequisite with estimated retrievability ≥ 0.7, when the session partitions auto-added prerequisites, then the prerequisite is skipped." | issue: `classifyChunk` returns R = 1.0 for fresh chunks ("Fresh item or null interval → treat as fully retrievable", src/domain/algorithms/classify-chunk.ts:45–47), so this scenario as written mandates skipping never-reviewed prerequisites — inverting today's behavior where `repetitions === 0` prereqs are always included and taught. | fix: Define mastered as `repetitions > 0 AND R ≥ 0.7` (or explicitly exclude null/zero-interval chunks from the skip branch) and add an acceptance scenario "reps = 0 → included".
- F1.2 | HIGH | confidence high | route charter-writer | check 11 | 01_charter.md § Outcomes (OUT-6) | quote: "Unit tests: reps>0 & R<0.7 → included; R≥0.7 → skipped" | issue: The charter's own verification signal carries the same unconditional-skip defect — a fresh prerequisite scores R = 1.0 under the reused `classifyChunk` computation, so the pinned test would enshrine never teaching new prerequisites. | fix: Amend OUT-6's success measure and Verified-by to condition skipping on `repetitions > 0` (or an explicit fresh-chunk exclusion) alongside R ≥ 0.7.
- F1.3 | MEDIUM | confidence high | route charter-writer | check 6 | 01_charter.md § Dependencies & impact | quote: "`ALLOWED_HOSTS` governs Host/Origin acceptance" | issue: This conflates two differently-shaped allowlists — Host validation takes hostnames while Origin validation compares scheme+host+port values — and neither the charter nor SUB-2 says which env drives the new 403 Origin check, how it relates to the existing `CORS_ALLOWED_ORIGINS` (default `'*'`, normalized at src/config/resolve-auth-config.ts:83), or whether an unset `ALLOWED_HOSTS` fails startup or gets a default. | fix: Name the env(s) governing Origin rejection vs Host validation, their relation to `CORS_ALLOWED_ORIGINS`, and the unset-default behavior.
- F1.4 | MEDIUM | confidence medium | route charter-writer | check 12 | 01_charter.md § Constraints & principles (Rauthy reality) | quote: "OUT-1 therefore sets `AUTH_AUDIENCE` to the client_id Rauthy actually emits" | issue: `AUTH_AUDIENCE` is URL-validated today (`optionalUrl` throws on non-URL, src/config/resolve-auth-config.ts:33–42) and doubles as the PRM `resource` identifier (src/transport/prm-handler.ts:6) and the base URL for the WWW-Authenticate resource_metadata URL (src/transport/jwt-middleware.ts:56–58), so a non-URL client_id value breaks startup parsing and PRM URL construction — a coupling no SUB owns. | fix: Add the audience↔PRM coupling to Dependencies/Risks and put its resolution (decouple PRM base from audience, or confirm the deployed client_id is URL-shaped) in SUB-1's scope.
- F1.5 | MEDIUM | confidence medium | route charter-writer | check 5 | 01_charter.md § Outcomes | quote: "(applies to every outcome below per the 2026-07-07 rollout decision)" | issue: Read literally, "every outcome below" extends the no-new-env-knobs rule to OUT-2 and OUT-9, which explicitly introduce new env vars (`ALLOWED_HOSTS`, rate-limit settings), while the intake decision scoped no-new-config to items 0.4/0.5/0.6/0.8 only. | fix: Restrict the parenthetical to OUT-4/5/6/8 so the rollout constraint cannot be read against the transport outcomes.
- F1.6 | MEDIUM | confidence medium | route user | check 13 | 01_charter.md § Assumptions & decisions (#7) | quote: "Requests without an `Origin` header are not rejected; 403 applies only to present-and-not-allowlisted origins" | issue: This [unconfirmed] inferred default shapes the security boundary a compliance charter ships (OUT-2/SUB-2 both build on it) and is a genuine posture choice, not a defect an author can settle alone. | fix: Confirm the allow-absent-Origin policy with the user (see Q1.1) and mark assumption #7 confirmed.
- F1.7 | LOW | confidence medium | route decomposer | check 12 | 02_subtasks.md § SUB-3: Rate-limit POST /mcp per authenticated subject | quote: "Limiter keys on the JWT subject (the `sub`/`azp` identity the JWT middleware already extracts) — not the MCP session." | issue: Subject-keyed limiting only throttles post-authentication traffic, so on auth-enabled deployments an unauthenticated flood of `POST /mcp` (each request costing JWKS/JWT-verify work before its 401) is never rate-limited and no artifact owns or explicitly accepts that gap. | fix: State in SUB-3 whether pre-auth requests get a limiter (e.g. per-IP) or record the gap as an accepted risk.
- F1.8 | LOW | confidence medium | route decomposer | check 12 | 02_subtasks.md § SUB-3: Rate-limit POST /mcp per authenticated subject | quote: "compliance is self-contained in the app (no reverse-proxy dependency)" | issue: An in-app limiter's state store is unspecified — with per-instance in-memory counters, "any deployment" running multiple replicas silently multiplies the effective limit. | fix: Add the single-instance assumption (or the state-store decision) to SUB-3's spec-time decisions.
- F1.9 | LOW | confidence high | route charter-writer | check 6 | 01_charter.md § Problem & why now | quote: "The leech flag fires on 3 consecutive failures with no minimum evidence base (src/domain/algorithms/sr-calculator.ts:157)" | issue: The flag actually fires at `config.leechConsecutiveFailures` — default 3 but already env-tunable via `SM_LEECH_CONSEC_FAILS` (src/config/resolve-algorithm-config.ts:57–59) — so the charter and SUB-7 present a configurable threshold as hardcoded, which blurs SUB-7's "raise to 4" fallback (a default change, achievable today via env). | fix: State "default 3, env-configurable via SM_LEECH_CONSEC_FAILS" in the problem statements so the fallback option reads as a default change.
Questions for user:
- Q1.1 | Should requests without an Origin header be accepted on /mcp, with 403 reserved for present-but-not-allowlisted origins? | options: allow absent Origin (assumption #7 — keeps non-browser MCP clients working; DNS-rebinding attacks come from browsers, which send Origin) | reject absent Origin (stricter posture; breaks most non-browser clients) | context: This unconfirmed assumption defines the security boundary OUT-2 ships and both SUB-2's middleware and its acceptance tests are built on it.
```

**Round 1 resolution:** Q1.1 answered 2026-07-07 (allow absent Origin). Revision 1 spent: writer applied F1.2–F1.5, F1.9 + Q1.1 answer (Scope changed: yes); decomposer applied F1.1, F1.7, F1.8 and realigned with the revised charter.

## Round 2 — 2026-07-07

**Audited by:** claude-fable-5

```
REVIEWER — Round 2: FINDINGS
Model: claude-fable-5
Coverage: 9/9 outcomes
Findings: 7 (0 critical, 2 high, 2 medium, 3 low)
- F2.1 | HIGH | confidence medium | route user | check 13 | 01_charter.md § Assumptions & decisions (#12) | quote: "The 403 Origin check reuses the existing `CORS_ALLOWED_ORIGINS` env as its allowlist (origin-shaped values, default `*` = no rejection); no second origin-allowlist env is introduced" | issue: This [unconfirmed] assumption shapes the shipped security boundary — with the `*` default the spec-MUST Origin rejection ships inert on every default-config deployment (the exact B2 gap the charter says blocks directory submission), while the sibling Host allowlist was made required precisely because "a silent default would re-create the disengaged-check gap". | fix: Put the default-`*`-vs-required posture to the user (Q2.1) and mark assumption #12 confirmed with the chosen answer.
- F2.2 | HIGH | confidence medium | route user | check 13 | 01_charter.md § Assumptions & decisions (#13) | quote: "`ALLOWED_HOSTS` (hostname list for the SDK Host check) is required when `TRANSPORT=http`; unset fails startup via the existing fail-fast pattern rather than defaulting | Inferred (matches `AUTH_ISSUER` fail-fast; a silent default would re-create the disengaged-check gap)" | issue: This [unconfirmed] assumption is scope-shaping (breaks every existing HTTP deployment at startup) and its rationale is false for the default bind — the SDK auto-applies `localhostHostValidation()` when host is 127.0.0.1/localhost/::1 (node_modules/@modelcontextprotocol/sdk/dist/esm/server/express.js:36–39), so default-bound deployments are already protected without `ALLOWED_HOSTS`. | fix: Put unconditional-vs-non-localhost-only requirement to the user (Q2.2) and mark assumption #13 confirmed with the chosen answer.
- F2.3 | MEDIUM | confidence high | route charter-writer | check 6 | 01_charter.md § Problem & why now | quote: "the SDK's Host-header DNS-rebinding check silently disengages because `createMcpExpressApp` is called without `allowedHosts` (src/transport/http.ts:81)" | issue: For the default `HTTP_HOST` of 127.0.0.1 (src/config/resolve-transport-config.ts:20) the SDK auto-enables localhost Host validation without `allowedHosts`, and for 0.0.0.0/:: binds it emits a console warning — the check disengages only for non-localhost binds and not fully silently, so the charter (and SUB-2's problem slice, which repeats the sentence) overstates the gap that justifies the breaking new env var. | fix: Qualify the claim in both artifacts: protection is auto-active on the default localhost bind and disengaged only for non-localhost `HTTP_HOST` values.
- F2.4 | MEDIUM | confidence high | route charter-writer | check 13 | 01_charter.md § Assumptions & decisions (#9) | quote: "When auth is disabled (HTTP without `authConfig`), the rate limiter keys on a fallback identity (e.g. per-IP or global) since no JWT subject exists | Inferred from src/transport/http.ts (auth is optional)" | issue: The cited basis is wrong for production — `resolveAuthConfig` throws when `TRANSPORT=http` and `AUTH_ISSUER` is unset (src/config/resolve-auth-config.ts:74–85) and src/transport/main.ts:42–51 always passes the resulting non-null config, so "HTTP without authConfig" is reachable only by direct `startHttpTransport` invocation in tests, and SUB-3's auth-disabled acceptance scenario exercises a production-unreachable configuration. | fix: Restate #9's basis as a test-only/direct-invocation path and let SUB-3 fold the fallback-identity mechanism into the pre-auth (per-IP) limiter decision instead of a standalone auth-disabled scenario.
- F2.5 | LOW | confidence medium | route decomposer | check 10 | 02_subtasks.md § Dependency order | quote: "2. SUB-2 [P]" | issue: SUB-3 is serialized after SUB-2 solely because "both churn src/transport/http.ts", yet SUB-1 and SUB-2 are declared parallel while both plausibly churn src/config/resolve-auth-config.ts (SUB-1 makes `audience` required at line 82; SUB-2 builds on the same file's origin normalization at line 83 and needs a home for the `ALLOWED_HOSTS` fail-fast beside `requireEnv`), applying the file-churn ordering principle inconsistently. | fix: Either serialize SUB-2 after SUB-1 with the same rationale or state where `ALLOWED_HOSTS` resolution lives so the [P] claim holds.
- F2.6 | LOW | confidence high | route decomposer | check 6 | 02_subtasks.md § SUB-5: Map assessment answers to quality pass→4 / fail→2 | quote: "Update prose that documents 5/1: src/shared/instructions.ts:33 and the schema comment at src/infrastructure/db/schema.ts:208." | issue: The enumerated list misses two 5/1 prose occurrences at the change site itself — the JSDoc "Pass → quality 5, fail → quality 1 (no retry)" (src/orchestration/teaching-workflows.ts:1212) and the inline comment "override quality to 5/1 for SR" (line 1223) — which would go stale even though the desired outcome promises "every piece of prose" matches. | fix: Add teaching-workflows.ts:1212 and :1223 to SUB-5's in-scope prose list (and to the charter's "Existing behavior encoded elsewhere" enumeration).
- F2.7 | LOW | confidence low | route charter-writer | check 14 | 00_intake.md § Adopted context — Phase 0 as defined in the synthesis (item 0.6) | quote: "Prereq mastery: gate on existing retrievability R≥0.7 instead of `repetitions > 0`" | issue: The charter and SUB-8 gate on `repetitions > 0` AND R ≥ 0.7 — a necessary correction (fresh chunks score R = 1.0 under classifyChunk, so a literal "instead of" would skip never-reviewed prerequisites) — but no clarification-log entry records that the compound gate deliberately supersedes the intake's literal wording. | fix: Add a clarification-log line noting the compound gate supersedes item 0.6's literal "instead of" for the fresh-chunk reason documented in OUT-6.
Questions for user:
- Q2.1 | Should the Origin-403 allowlist keep `CORS_ALLOWED_ORIGINS`'s `*` default (no Origin ever rejected until an operator sets explicit origins), or become required/explicit whenever TRANSPORT=http? | options: keep `*` default (non-breaking, opt-in enforcement) | require explicit origins in HTTP mode (strict posture consistent with ALLOWED_HOSTS; breaking) | context: with the default, the spec-MUST Origin rejection that OUT-2 claims closes gap B2 ships inert on default-config deployments (assumption #12, unconfirmed).
- Q2.2 | Should `ALLOWED_HOSTS` be required whenever TRANSPORT=http, or only when `HTTP_HOST` is a non-localhost bind? | options: always required (uniform fail-fast; breaks localhost deployments the SDK already auto-protects) | required only for non-localhost binds (no break for the default 127.0.0.1 bind; conditional startup rule) | context: the SDK auto-applies localhost Host validation for 127.0.0.1/localhost/::1, so the unconditional requirement's stated rationale does not hold for the default bind (assumption #13, unconfirmed).
```

**Round 2 resolution:** Q2.1 answered 2026-07-07 (require explicit origins in HTTP mode); Q2.2 answered 2026-07-07 (ALLOWED_HOSTS required for non-localhost binds only). Revision 2 spent: writer applied answers + F2.3, F2.4, F2.7 (Scope changed: yes); decomposer applied F2.5, F2.6 and realigned (SUB-2 serialized after SUB-1).

## Round 3 — 2026-07-07

**Audited by:** claude-fable-5

```
REVIEWER — Round 3: FINDINGS
Model: claude-fable-5
Coverage: 9/9 outcomes
Findings: 2 (0 critical, 0 high, 1 medium, 1 low)
- F3.1 | MEDIUM | confidence medium | route charter-writer | check 4 | 01_charter.md § Outcomes (OUT-7) | quote: "The first ~1.5–2 KB (≤2,048 bytes) of `SERVER_INSTRUCTIONS` contain the critical rules" | issue: The charter's problem statement motivates OUT-7 with two clients — "truncated at 2 KB by Claude Code and to ~512 useful chars in ChatGPT, so critical workflow rules are invisible to exactly the clients most likely to connect" — but the outcome's success measure and tests bind only the 2,048-byte head, leaving the named ChatGPT case (~512 chars, a quarter of the tested budget) unfixed, and the only acknowledgment of that residual gap is SUB-9's assumption ("ChatGPT's ~512-char window gets the best-effort ordering of the most critical rule first, but 2 KB is the tested contract") — a scope-shaping deferral that lives in the decomposition with no charter backing in Outcomes, Risks, or Assumptions. | fix: State in the charter (OUT-7 success measure or Risks/Assumptions) that the ~512-char ChatGPT window gets best-effort most-critical-rule-first ordering only and 2 KB is the tested contract, so the charter no longer implies the ChatGPT invisibility problem is resolved.
- F3.2 | LOW | confidence low | route charter-writer | check 6 | 01_charter.md § Outcomes (OUT-7) | quote: "full depth remains available verbatim through `get_server_workflow`" | issue: Since `get_server_workflow` serves whatever `SERVER_INSTRUCTIONS` becomes (src/server/server-workflow-tools.ts:19 returns the same string), "verbatim" is trivially true of any restructured text and gives no measurable preservation guarantee — the enforceable requirement is the semantic completeness SUB-9 tests — while a literal reading ("original text preserved word-for-word") would contradict the restructuring itself. | fix: Replace "verbatim" with the measurable claim ("the full restructured text, semantically complete, is served unchanged by `get_server_workflow`") in OUT-7 and the AI-agent journey.
Questions for user: none
```

**Round 3 resolution:** warnings-only. User accepted both findings as-is on 2026-07-07 → Converged with warnings.

## Accepted warnings

- F3.1 — `charter-writer|check 4|01_charter.md § Outcomes (OUT-7)` (MEDIUM: 512-char ChatGPT window is best-effort only; 2 KB is the tested contract)
- F3.2 — `charter-writer|check 6|01_charter.md § Outcomes (OUT-7)` (LOW: "verbatim" wording untestable; semantic completeness is the enforceable claim)

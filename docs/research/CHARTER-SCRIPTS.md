# Charter scripts for Phases 1–3

Paste-ready `/charter` invocations for the remaining phases of the 2026-07-07 roadmap (`docs/research/SYNTHESIS.md`). Each block is fully self-contained: paste it verbatim as one message into a **fresh Claude Code session in this repo's root**. The session needs the `/charter` skill and the Linear MCP connection (tracker config is already in `wf.config.js`). You will be asked up to 5 interview questions per charter — stay available until it converges and publishes.

These charters are safe to run now and execute later: every script mandates a **drift-verification gate as its first outcome (AC1)** — a quick, blocking first sub-task that re-validates the facts the charter bakes in (code file:line refs, behavior claims, and where relevant external spec/directory facts) against reality at execution time, and routes invalidated facts back as amendments before the affected work starts.

Run order: Phase 1 → Phase 2 → Phase 3 is the roadmap order, but Phase 1 and Phase 2 are independent enough to charter in either order. Phase 3 should be chartered last (several of its items build on Phase 1's measurement and Phase 2's hosting).

Research artifacts every script relies on (all in-repo):

- `docs/research/SYNTHESIS.md` — roadmap, verdicts, monetization strategy, corrections
- `docs/research/results/01-mcp-spec-compliance-audit.md` — spec compliance matrix, gap list, directory-listing checklists
- `docs/research/results/02-mcp-new-features-survey.md` — protocol feature × client support matrix, top-5 opportunities, "not worth it" list
- `docs/research/results/03-pedagogy-evidence-audit.md` — per-question evidence verdicts, ranked top-5 pedagogy changes
- `docs/research/results/04-monetization-market-research.md` — market landscape, pricing comps, licensing precedents, model scoring
- `docs/wf-plans/C001__phase-0-hardening-trivial-wins/` — Phase 0 charter precedent (Linear umbrella NEU-832, sub-tasks NEU-833–NEU-841)

---

## Phase 1 — Measurement (paste everything inside the fence)

```
/charter Phase 1 of the 2026-07-07 roadmap: Measurement — build the closed feedback loop the pedagogy audit found missing.

CONTEXT FOR YOU (the chartering agent): This repo completed a four-report research pass on 2026-07-07. Before drafting anything, read `docs/research/SYNTHESIS.md` (the roadmap — this charter is its "Phase 1" table) and `docs/research/results/03-pedagogy-evidence-audit.md` sections Q4, Q12, Q13 (the evidence behind every item below). Phase 0 is already chartered and published: umbrella NEU-832 with sub-tasks NEU-833–NEU-841, artifacts in `docs/wf-plans/C001__phase-0-hardening-trivial-wins/` — follow that charter's artifact style and do not overlap its scope (isError, fuzz, assessment mapping 4/2, prereq gate, leech gate, instructions restructure are all Phase 0, not yours).

THE PROBLEM (from report 03, executive summary finding #3): the system collects rich attempt-level data (`session_question_attempts`, `time_spent_ms`) but computes none of the standard SRS health metrics from it. There is no true retention rate, no calibration check, no forgetting-curve fit — no way to tell whether the scheduler's assumptions match observed recall. This is the enabling phase: without it, no later scheduler change (including the Phase 3 FSRS migration) can be validated.

SCOPE ITEMS (from SYNTHESIS.md Phase 1 table):
| Item | What | Evidence |
| 1.1 | True retention rate: pass-rate at due time, overall + by tier/interval band. Anki ships this as a first-class "True Retention" table; this system has zero pass/fail-at-due-time aggregation | report 03 Q13 |
| 1.2 | Calibration check: bucket reviews by implicit predicted-recall probability vs observed pass rate (RMSE-bins style, per the FSRS benchmark methodology), or log-loss | report 03 Q13 |
| 1.3 | Correct-answer exposure after the second failed attempt: today the failure is silently recorded; Pashler et al. 2005 found supplying the correct answer after an incorrect response increased final retention by 494%. Light-touch — surface the correct answer/explanation, not a forced restudy | report 03 Q4 |
| 1.4 | Use `time_spent_ms`: per-session latency+quality trend as a real fatigue signal; replace the unvalidated hard 90-min cutoff with periodic break prompts (evidence favors micro-breaks over a single cutoff); make session/day thresholds configurable and documented as engineering defaults, not evidence-derived | report 03 Q12, Q13 |
| 1.5 | Resolve dead pedagogy config: wire the never-called cognitive-load model (`src/domain/services/cognitive-load.ts`) to real signals or delete it; same decision for the dead session-composition config (`sessionComposition` in `src/domain/config/algorithm-defaults.ts` — maxNew caps, interleaveStrategy). Note: do NOT implement interleaving here (Phase 3); this item is only wire-or-delete of dead surface | report 03 Q13; recon facts in SYNTHESIS.md |

DECISIONS ALREADY MADE (do not re-ask): hard-cutover rollout with no new rollout flags is the established Phase 0 precedent for behavior changes; hexagonal purity (src/domain/ zero-I/O, never throws) is non-negotiable; integration tests are a ship-gate for DB-mutating paths (project CLAUDE.md); no session-lifecycle investment (the 2026-07-28 MCP spec RC removes protocol sessions — see SYNTHESIS.md corrections).

STRATEGIC NOTE (from report 04 / SYNTHESIS monetization section): these metrics are the future paid product surface — the consumer "is this working for me" screen and the B2B dashboard. Design the data shapes with that reuse in mind, but UI/dashboard work is OUT of scope (Phase 2+).

MANDATORY FIRST OUTCOME (AC1 — DRIFT VERIFICATION): make this the charter's first outcome (OUT-1) and the decomposer's first sub-task in dependency order, blocking every other sub-task. This charter may be executed well after it is written, and Phase 0 (NEU-833–841) merges will have moved things. Before any other item is implemented, quickly re-verify every code fact this charter bakes in against the repo at that time: the submit_answer/teaching-workflows references (NEU-837 changes the same path item 1.3 touches), whether NEU-839's config audit already wired-or-deleted parts of item 1.5's dead config (sessionComposition, cognitive-load), and the analytics module shapes. Grep/read-level verification only — target under an hour of agent effort, no re-research. Output: a drift report at 04_drift-check.md in the charter folder listing each checked fact as HOLDS / SHIFTED (with new location) / INVALIDATED; any INVALIDATED fact routes back as an amendment to the affected sub-task brief (or the charter) before that item's implementation begins.

OPEN QUESTIONS THE INTERVIEW SHOULD SETTLE (pick the material ones): (a) where the new metrics surface — extend the existing analytics_daily/analytics_window MCP tools, add new dedicated tools, or both; (b) whether item 1.3 changes the submit_answer response schema (additive field) or adds a new step in the teaching flow; (c) break-prompt mechanics for 1.4 — server-injected advisory in teach_next responses vs. session_status recommendation change; (d) for 1.5, whether the default posture is delete (audit leaned "an uninvoked model is itself a measurement gap") or wire.
```

---

## Phase 2 — Tenancy, distribution, cloud (paste everything inside the fence)

```
/charter Phase 2 of the 2026-07-07 roadmap: multi-tenancy, distribution, and the hosted cloud — the monetization gate.

CONTEXT FOR YOU (the chartering agent): This repo completed a four-report research pass on 2026-07-07. Before drafting anything, read: `docs/research/SYNTHESIS.md` (roadmap + the "Monetization strategy" section — this charter executes its Phase 2 table), `docs/research/results/01-mcp-spec-compliance-audit.md` §4–5 (best-practice guidance + the three directory-listing readiness checklists — these are effectively this charter's acceptance criteria), `docs/research/results/04-monetization-market-research.md` (section D licensing precedents + the Implications scoring), and `docs/research/results/02-mcp-new-features-survey.md` §2 rows 2–3 (annotations client reality: runtime no-op in most clients but REQUIRED for directory listing; plus the Claude-Code-proprietary `_meta["anthropic/requiresUserInteraction"]` flag). Phase 0 is published as umbrella NEU-832 (artifacts: `docs/wf-plans/C001__phase-0-hardening-trivial-wins/`); its transport-hardening sub-tasks NEU-833 (aud enforcement) and NEU-834 (Origin/Host validation) are prerequisites for public hosting — note the dependency but do not duplicate their scope.

THE STRATEGY ALREADY DECIDED (SYNTHESIS "Monetization strategy" — do not re-litigate): the chosen model is open-core cloud — free MIT self-hostable core, paid hosted multi-tenant service (Plausible is the structural comp), with consumer freemium pricing ($8–15/mo band) on top. Rejected: learning-infra API, content marketplace, per-call micropayments (all fail the demand gate — report 04 Implications). B2B is deferred, not dead. There is NO MCP-native payment rail (SEP-2007 still draft) — billing is ordinary Stripe web checkout. The durable differentiator vs free first-party features: explicit SR scheduling + queryable knowledge graph + portability across clients (Claude Memory is claude.ai-app-only).

SCOPE ITEMS (from SYNTHESIS.md Phase 2 table):
| Item | What | Source |
| 2.1 | Multi-tenancy: user_id/owner column on all tables (`src/infrastructure/db/schema.ts` currently has NONE — report 01 row 25), thread the JWT `sub` (already extracted by `src/transport/jwt-middleware.ts`) through ports/adapters as row-level scope. THE blocker for everything else in this phase | 01 S4 |
| 2.2 | Scope enforcement: define app scopes (e.g. sr:read/sr:write), publish in the PRM `scopes_supported`, check the token's scope claim before mutating tools, 403 insufficient_scope per the spec's step-up flow | 01 S2 |
| 2.3 | Tool annotations: `title` + readOnlyHint/destructiveHint (+idempotent/openWorld where apt) on all 45 tools — currently 0/45; required by both Anthropic's Connector Directory and OpenAI's Apps SDK. Plus `_meta["anthropic/requiresUserInteraction"]: true` on destructive tools (delete_chunk etc.) for Claude Code | 01 S5, 02 §3.3 |
| 2.4 | Tool consolidation pass before directory review: 45 tools is above published guidance; merge the calculate_* trio, fetch/list overlaps (Anthropic's tool-writing guidance explicitly recommends consolidating overlapping tools) | 01 N2 |
| 2.5 | Publication prerequisites: privacy policy (missing = immediate directory rejection), flip `"private": true` in package.json, publish the package, MCP Registry listing (namespace verification via GitHub OAuth) | 01 §5 |
| 2.6 | Hosted deployment: public HTTPS instance behind the existing OAuth (Rauthy), Stripe checkout for subscriptions, free-tier limits (e.g. active-chunk cap). The cloud's tenant/billing/dashboard layer goes in a SEPARATE PRIVATE REPO (Plausible pattern without relicensing — report 04 D: MIT alone gives zero legal barrier to hosted-service clones; Cal.com/Locust/Sandstorm cautionary tales apply) | 04 A4, D |
| 2.7 | Directory submissions: Anthropic Connectors Directory (requires Team/Enterprise claude.ai org to submit — individual accounts cannot) and ChatGPT apps (consumer reach post-review; digital-goods monetization NOT yet approved there, so billing stays external) | 01 §5, 04 A2–A3 |

CONSTRAINTS CARRIED FROM PHASE 0 / PROJECT: hexagonal architecture (ports/adapters — tenancy threading must respect it); fail-fast env pattern for new required config; integration tests are a ship-gate for auth/blocking paths; no session-lifecycle investment (2026-07-28 spec RC removes protocol sessions).

MANDATORY FIRST OUTCOME (AC1 — DRIFT VERIFICATION): make this the charter's first outcome (OUT-1) and the decomposer's first sub-task in dependency order, blocking every other sub-task. Two halves, both quick: (1) code facts — re-verify against the repo at execution time: the schema-has-no-user_id claim, the 45-tool count and zero-annotations claim, package.json's `"private": true`, and the JWT middleware/auth-config shape (Phase 0's NEU-833/834 change exactly those files). (2) world facts — one bounded web-check refreshing report 01 §5 and report 04 A2–A3: has the 2026-07-28 MCP spec finalized (and did its ratified form change anything this charter relies on); current Anthropic Connectors Directory requirements and submission gates (Team/Enterprise org, privacy policy, annotations); MCP Registry status (still preview?); ChatGPT apps digital-goods monetization policy; Stripe/agentic-payment rail status. Output: a drift report at 04_drift-check.md in the charter folder listing each checked fact as HOLDS / SHIFTED (with new value/location) / INVALIDATED; any INVALIDATED fact routes back as an amendment to the affected sub-task brief (or the charter) before that item's implementation begins.

OPEN QUESTIONS THE INTERVIEW SHOULD SETTLE (pick the material ones): (a) the license/repo-split decision — recommended: repo stays MIT, cloud layer in a private repo; alternative: fair-source relicense (community friction, see Cal.com backlash in report 04 D) — this MUST be decided before any cloud code is written; (b) whether the user has/will create a Team or Enterprise claude.ai org for directory submission, or defers 2.7; (c) migration strategy for the existing single-user dataset when user_id lands (assign all rows to the owner's subject?); (d) free-tier limit shape (active chunks? topics? reviews/month?); (e) scope split — this phase may be >10 sub-tasks; the decomposer may flag overscope, and a sensible split is items 2.1–2.5 (make the OSS server tenant-ready and listable) as this charter with 2.6–2.7 (cloud + submissions) as a follow-up charter. Ask rather than assume.
```

---

## Phase 3 — Pedagogy depth & protocol adoption (paste everything inside the fence)

```
/charter Phase 3 of the 2026-07-07 roadmap: pedagogy depth (FSRS and evidence-backed scheduler fixes) plus the worthwhile MCP protocol adoptions.

CONTEXT FOR YOU (the chartering agent): This repo completed a four-report research pass on 2026-07-07. Before drafting anything, read: `docs/research/SYNTHESIS.md` (roadmap — this charter is its Phase 3 table), `docs/research/results/03-pedagogy-evidence-audit.md` (Q1 FSRS with a concrete 5-step migration path, Q3 grading trust, Q5 successive relearning, Q6 lapse handling, Q10 interleaving, Q11 tier smoothing), and `docs/research/results/02-mcp-new-features-survey.md` §3 (top-5 opportunities — form-mode elicitation, progress notifications, resource links) and §4 ("not worth it" list — sampling is DEPRECATED in the 2026-07-28 spec RC, structured output has no client consumer; do not resurrect them). Phase 0 (umbrella NEU-832, `docs/wf-plans/C001__phase-0-hardening-trivial-wins/`) already shipped the constant-scale pedagogy fixes (assessment 4/2 mapping, fuzz, compound prereq gate, leech evidence gate) — do not overlap. Phase 1 (measurement: true retention + calibration) and Phase 2 (tenancy/cloud) should be chartered/underway; note dependencies below.

SCOPE ITEMS (from SYNTHESIS.md Phase 3 table):
| Item | What | Evidence / dependency |
| 3.1 | FSRS migration — the highest-absolute-impact change in the whole audit (FSRS-6 beats SM-2 on 99.6% of 9,999 benchmarked Anki collections). Migration path from report 03 Q1: replay `session_question_attempts` into per-chunk review logs (quality 0–5 → FSRS 1–4), fit weights with an open-source optimizer (start from published defaults until enough history), warm-start per-chunk Stability/Difficulty, replace the EF/interval heuristic with FSRS next-interval-for-target-retention; the existing retrievability-tier selection then consumes a genuinely fitted R (net simplification — two parallel retrievability computations collapse into one). DEPENDS ON Phase 1 calibration metrics to validate the migration | 03 Q1 |
| 3.2 | Lapse partial retention: replace the unconditional interval→1d/reps→0 reset with post-lapse interval scaled by prior learning depth (FSRS post-lapse stability is a function of prior stability, never zeroed; Ebbinghaus savings literature). Subsumed by 3.1 if FSRS lands first — sequence accordingly | 03 Q6 |
| 3.3 | Roadblock → true successive relearning: recovery correct-recalls must occur in NEXT scheduled sessions, not massed same-session (Rawson & Dunlosky: 68% vs 26% one-week retention for spaced vs massed criterion); cap how much massed in-session success can move EF/interval | 03 Q5 |
| 3.4 | Interleaving — topic/category mixing in review sessions (Brunmair & Richter meta-analysis g=0.42), respecting the prerequisite DAG for first exposures. NOT the dead easy-medium-hard difficulty ordering — report 03 Q10 found that targets the wrong axis entirely | 03 Q10 |
| 3.5 | Grading trust: independent judge pass on a sampled fraction of gradings (self-grading-own-questions compounds self-preference bias; LLM tutors over-validate incorrect answers up to 71% in hard cases), anchoring to canonical answers where possible, per-session leniency-drift monitoring. In the hosted cloud this is an operator-paid LLM call — a paid-tier "verified grading" feature (ties to Phase 2's cloud) | 03 Q3, 02 §4 |
| 3.6 | Tier smoothing: add a hinted-recall step between cued_recall and the multiple-choice scaffold tier; re-promote within-session when the learner clears a lower tier easily (tracks the moving optimal-challenge point) | 03 Q11 |
| 3.7 | Protocol adoptions that survived the evidence check: form-mode elicitation for destructive-op confirmation and learner-facing questions (form mode ONLY — URL-mode elicitationId is removed in the 2026-07-28 RC); progress notifications on long/bulk tool calls (confirmed to keep Claude Code from killing calls idle >5 min); a resource-links spike on search results (rendering behavior unverified in clients — spike before committing UX) | 02 §3.2, §3.4–3.5 |
| 3.8 | SDK v2 / 2026-07-28 spec migration: the RC removes protocol sessions, the initialize handshake, SSE resumability, and deprecates Roots/Sampling/Logging + OAuth DCR. This server's HTTP transport is built on the removed session machinery. Trigger: schedule when the final spec ships AND a Tier-1 client adopts it (v1 SDK is supported ≥6 months post-v2). Likely a watch-item sub-task with explicit trigger criteria rather than immediate work | 02 §1, §2.14 |

CONSTRAINTS CARRIED FORWARD: hexagonal purity (FSRS state, RNG, and any judge calls enter the domain as injected inputs/ports); hard-cutover precedent for scheduler behavior (no new rollout flags) EXCEPT where a data migration is inherently needed (FSRS warm-start); integration tests ship-gate; quality scale stays 0–5 at the MCP boundary unless the interview decides otherwise for FSRS's 4-point input.

MANDATORY FIRST OUTCOME (AC1 — DRIFT VERIFICATION): make this the charter's first outcome (OUT-1) and the decomposer's first sub-task in dependency order, blocking every other sub-task. Two halves, both quick: (1) code facts — the scheduler/teaching internals this charter cites will have been reshaped by Phase 0 (fuzz, leech gate, prereq gate: NEU-838–840) and Phase 1 (correct-answer exposure, metrics): re-verify each item's premise and file references against the repo at execution time, and confirm Phase 1's true-retention/calibration metrics actually exist (item 3.1's validation dependency — if absent, 3.1 blocks). (2) protocol facts — one bounded web-check: the ratified 2026-07-28 spec (did sampling deprecation, MRTR-based elicitation, and protocol-session removal ship as the RC described); current client support for form-mode elicitation and progress notifications; SDK v2 GA status and v1 support window; state of the FSRS ecosystem libraries (ts-fsrs / fsrs optimizer). Output: a drift report at 04_drift-check.md in the charter folder listing each checked fact as HOLDS / SHIFTED (with new value/location) / INVALIDATED; any INVALIDATED fact routes back as an amendment to the affected sub-task brief (or the charter) before that item's implementation begins.

OPEN QUESTIONS THE INTERVIEW SHOULD SETTLE (pick the material ones): (a) FSRS scope — full migration in this charter vs. a spike/shadow-mode first (compute FSRS schedules alongside SM-2 and compare via Phase 1 calibration before cutover); (b) whether 3.7/3.8 protocol items belong in this charter or split into their own (this phase may exceed 10 sub-tasks — let the decomposer flag it and ask); (c) grading-trust economics — judge-pass sampling rate and whether it's cloud-only (operator LLM cost) or also available self-hosted via the existing optional classifier config; (d) whether FSRS keeps 0–5 agent-reported quality mapped down to 1–4, or moves the submit_answer contract to native 4-point (breaking change to instructions/agents).
```

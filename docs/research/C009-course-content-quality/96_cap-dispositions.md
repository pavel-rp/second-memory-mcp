# 96 — Cap dispositions on the independent re-verification

**Task:** NEU-970
**Model:** claude-opus-5
**Date:** 2026-08-12
**Evidence:** `94_independent-gate-reverification.md` · adjudicated at `95_reverification-adjudication.md`
**Authority:** recorded on the creator's instruction. The creator is `D-F5`'s default owner and the
party `OI-S7-1` names; `A4` bars a *producing task* from promoting its own artifact, and neither
`94_` nor this record is being promoted by the party that produced it.

## The eleven

Each closes on a re-run by a reader whose model id differs from the package's. `94_` is that run:
45 attribution lines across 39 files carry `claude-opus-5[1m]`; the report carries
`gpt-5.6-sol[1m]` and authored none of them.

| Cap | Closure condition, abbreviated | Disposition |
| --- | --- | --- |
| `CAP-S1-4` | Independent reader re-runs the rights checks and reviews the judgment calls | **Closed** — `94_` §6 re-ran all five scans and adjudicated every hit afresh |
| `CAP-S2-5` | Same, for the forms catalogue | **Closed** — `94_` §§1, 2, 8 |
| `CAP-S3-7` | Same, for the citation procedure | **Closed** — `94_` §§6, 8 item 8 (vacuous, denominator stated) |
| `CAP-S4-6` | Same, for the correctness standards | **Closed** — `94_` §8 items 6, 9, 10 |
| `CAP-S5-5` | Same, for the conceptual obligations | **Closed** — `94_` §8 items 4, 5 |
| `CAP-S6-4` | Same, for the assessment evidence mapping | **Closed** — `94_` §§3, 8 |
| `CAP-S7-5` | Same, for the difficulty calibration | **Closed** — `94_` §4 (179/179 verified independently) |
| `CAP-S10-5` | Same, for citation drift | **Closed** — `94_` §§1, 7 |
| `CAP-S11-2` | Same, for the exemplar proof | **Closed** — `94_` §A, row-level |
| `CAP-S9-4` | Probe executed by a model that authored nothing under review | **Closed** — the whole run satisfies it |
| `CAP-S12-1` | A party other than the producing task re-opens the 40 blocks and 25 occurrences | **Closed** — `94_` §6 read all 40 fenced blocks; §3 read all 28 class-7 occurrences |

`CAP-S12-1` names 25 occurrences; `94_` found 28. The delta is `93_` landing after `92_` counted
(`95_` §6). Reading 28 discharges an obligation over 25.

## What the run did not close, and is not claimed to

- **`OI-S7-1` and the 179 provisional values.** A cross-model pass is NEU-887 class 4
  `[ai-critique]`; the review requires class 3. `94_` §4 confirms 179/179 still
  `deferred-provisional`. See `../C005-dp-map-package/06_creator-review-triage.md` for the
  narrowed review set.
- **Anything gated on network access, a registered QA engine, a serve surface, or a real content
  unit.** Roughly 26 caps. None is affected by who reads the package.
- **`CH-F5-1`.** Left open deliberately: the four fields it governs matter only once verified
  citations exist, and there are zero. Deciding it now would close caps without changing anything.

## Two findings from the run that were not defects

`94_` reported two cross-reference failures. Both are refuted with quoted evidence at `95_` §4 —
one reads a path in a corrections table as a live reference, the other declines to apply the
report's own resolvability rule. **`92_…` §7's cross-reference pass stands.** They are recorded
here so that a later reader who finds them in `94_` does not re-open them as open items.

## `92_`'s two failures, now

1. **`C-3` contamination** — discharged by `94_`. The gate's own run remains contaminated; the
   check it could not perform has now been performed by someone else.
2. **`OI-S13-1`, the `11_…` §10.2 roll-up** — **repaired** in this change, under a bounded
   suspension of the append-only convention recorded at `11_…` §10.2 and in the commit message.

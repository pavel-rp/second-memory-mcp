# Signal → Gate Evidence Register (SUB-6)

**Task:** NEU-962 (SUB-6) · **Charter:** C009 (umbrella NEU-890) · **Compiled:** 2026-08-10 · **Verification cutoff:** 2026-08-10 · **Covers:** OUT-5 · **Status:** **deferred — this register SETS no status.** Status lives in a ledger: this package's `adjudication/`, or the owning package's ledger for an inherited decision (`A1`–`A5`: a producing task may not promote its own artifact)
**Model:** claude-opus-5[1m]

One row per **gate-bearing claim** made in `../06_assessment-evidence-out-of-band.md` and `../decision-records/DR-C09-02_dr-m08-routing.md`. A gate-bearing claim is any claim that a signal **may** or **may not** feed a mastery gate or threshold, plus the claims the mapping structurally depends on. Each row carries the house columns: **evidence class** (one of the seven), **evidence type**, **cutoff**, **provenance** (file + section), and **structural limitation**.

Ids are namespaced `RG-S6-nn`, so no sibling's rows can collide with these and none of theirs is renumbered.

---

## 1. Register

### 1.1 Rows `RG-S6-01` … `RG-S6-25`

| Id | Claim | Class | Evidence type | Cutoff | Provenance | Structural limitation |
| --- | --- | --- | --- | --- | --- | --- |
| **`RG-S6-01`** | Bare self-report (`self_report_outcome`) may feed **no** gate: A–E and `MM-T1`…`MM-T15` are all in its may-not-feed list. | — | Design rule derived from an upstream decision, not a measurement | 2026-08-10 | `../06_…md` §4.1, §1 R1; charter assumption 22 | It is a **rule**, not a finding. Nothing enforces it at runtime; enforcement is SUB-9's (`CAP-S6-1`). |
| **`RG-S6-02`** | The gate-bearing set is exactly `retrieval` items, `assessment` items, and the pasted-back solution. | — | Upstream product decision, consumed | 2026-08-10 | C009 charter assumption 22, **confirmed**; `../decision-records/DR-C09-02_dr-m08-routing.md` §Decision | Decided upstream; not re-derivable from any repo file. Cited as the charter, per the `DR-C09-01` precedent. |
| **`RG-S6-03`** | Each of the six enumerated signals carries **exactly one** of the seven reliability classes, and none is class 7. | 2 | `[code-evidence]` — the taxonomy read at its source | 2026-08-10 | `../06_…md` §3.1, §3.4; `../../C005-product-foundation/01_evidence-taxonomy.md` | The class assignment is a **judgement** about each signal, not a measurement of its reliability. No reliability was measured for any signal. |
| **`RG-S6-04`** | `pasted_solution` may feed Gate A (`MM-T9`), Gate B (`MM-T1`, `MM-T3`) and `MM-T13`/`MM-T14`; not Gate C/D/E, not `MM-T2` alone, not `MM-T4`–`MM-T7`, not `MM-T10`. | 2 | `[code-evidence]` — gate/threshold semantics read at source | 2026-08-10 | `../06_…md` §4.2; `../../C005-instructional-model/mastery-model/00_operational-mastery-model.md` §4, §5 | Gate A admission is conditional on an unaidedness caveat that **no signal establishes** for the external solve (`OI-S6-3`). |
| **`RG-S6-05`** | `retrieval_item_result` may feed Gates A, B and D (pool only) plus remediation; not Gate C directly, not Gate E, not the fidelity preconditions, not `MM-T10`. | 2 | `[code-evidence]` | 2026-08-10 | `../06_…md` §4.3; mastery model §4, §5 | Gate D admission presumes an interleaved pool and mixed-type presentation that **does not exist yet** in any implementation. |
| **`RG-S6-06`** | `assessment_item_result` may **not** feed `MM-T2`, because **`spacing_eligible` is a field of `retrieval`, not of `assessment`**. | 2 | `[code-evidence]` — a field-set consequence of the inherited form contract | 2026-08-10 | `../06_…md` §4.4; `../02_content-and-exercise-forms.md` §3.9, §3.10 | Depends on SUB-2's field allocation holding. If a future revision adds `spacing_eligible` to `assessment`, this row is superseded. |
| **`RG-S6-07`** | `post_hoc_reflection` feeds **remediation only** (`MM-T13`, `MM-T14`) and opens no gate, despite `reflection` being a discriminative form. | — | Design rule, argued from the composition invariant | 2026-08-10 | `../06_…md` §4.5 | A judgement about what narrative evidence may carry. No study supports the specific boundary drawn. |
| **`RG-S6-08`** | `return_timing` is **not** solve latency and may feed nothing, `MM-T15` emphatically. | 6 | `[operational-log]` — a statement about what our own timestamps do and do not bound | 2026-08-10 | `../06_…md` §3.2, §4.6, §8.1 | Subject to `P5`/`EX6`: aggregate-only handling. The claim is about the *absence* of a measurement, which cannot itself be measured. |
| **`RG-S6-09`** | `MM-T4`, `MM-T5`, `MM-T6`, `MM-T7` are **preconditions on the grading system**, fed by no learner signal. | 2 | `[code-evidence]` | 2026-08-10 | `../06_…md` §4.0, §8.4; mastery model §5 (fidelity precondition rows) | Restates the upstream model's own framing; adds no evidence of its own. |
| **`RG-S6-10`** | The residual clause defaults any unenumerated signal to class `unclassified` with an empty may-feed list. | — | Standing design rule, undischarged | 2026-08-10 | `../06_…md` §3.3; `../90_open-items-and-provisional-register.md` `OI-S6-1` | Unexercised. Its correctness has never been tested against an actual unenumerated signal. |
| **`RG-S6-11`** | A happy-path-only item may not carry a gate, judged by the four-condition mechanical check. | — | Design rule with a stated decision procedure | 2026-08-10 | `../06_…md` §5.1, §5.2 | The check is mechanical but **unautomated**; nothing runs it. Condition 4 requires a human to enumerate the item's inputs. |
| **`RG-S6-12`** | A learner holding the greedy-substitution misconception scores `derived_quality = 2` on the specimen item, below `MM-T3`'s q ≥ 3, so the misconception is detected rather than passed. | 2 | `[code-evidence]` — computed against the shipped mapper's actual weights and fail-closed span rule | 2026-08-10 | `../06_…md` §5.4; `src/domain/algorithms/grade-mapper.ts` (`RUBRIC_WEIGHTS`, `mapRubricToQuality`) | A **worked demonstration on one constructed item**, not an evaluation over a population. It shows the mechanism can detect the misconception, not that items in practice will. |
| **`RG-S6-13`** | The `{1, 3, 4}` / target `6` input separates greedy from the correct recurrence (3 coins vs. 2). | 1 | `[literature]` — a standard non-canonical coin-system counterexample | 2026-08-10 | `../06_…md` §5.4 | Arithmetically checkable and checked; it is one counterexample and does not generalise to every DP node. |
| **`RG-S6-14`** | The evidence record's identity is `node_id` + `skill_type` (+ learner/session/time scoping); the citation is **never** in the key. | — | Specification authored here | 2026-08-10 | `../06_…md` §6.1 | A specification, not an implementation. **No store implements it**, so nothing verifies that a built version keeps the citation out of the key (`CAP-S6-3`). |
| **`RG-S6-15`** | Swapping a retired citation changes exactly `citation.stable_id` and `citation.canonical_url`; all accumulated mastery evidence survives; no record is orphaned and no gate result is silently invalidated. | 3 | `[dogfooding]` — a constructed specimen exercised by the producing task, n = 1 | 2026-08-10 | `../dry-run/06_corpus-swap-verification.md` §3, §4 (4/4 PASS) | One run, by the producing task, at specification level. Inherits `CAP-S1-4`/`CAP-S2-5`. **This is the package's single execution of this check; SUB-10 cites it.** |
| **`RG-S6-16`** | The swap result is **identical under both `CH-F5-1` dispositions**, so SUB-6 carries no field-set cap on that axis. | 2 | `[code-evidence]` — derived from the key, plus the interim field set read at source | 2026-08-10 | `../06_…md` §6.2; `../dry-run/06_…md` §4.4; `../decision-records/DR-C09-01_permitted-field-set.md`; `CAP-S1-2` | The wider disposition was **re-inspected, not executed** — `CH-F5-1` is open, so the wider set does not exist to run against. |
| **`RG-S6-17`** | Each of `M03`, `M04`, `M06`, `M08`, `M09`, `M10` is satisfied **without weakening**, carried by a named artifact and SUB-2 field. | 2 | `[code-evidence]` — mechanisms and field allocation read at source | 2026-08-10 | `../decision-records/DR-C09-02_dr-m08-routing.md` §Consequences (control-by-control table); `../../C005-instructional-model/package/00_per-mechanism-index.md`; `../02_content-and-exercise-forms.md` | A **demonstration by argument**, control by control. No control was executed or measured; six of six are argued, not tested. |
| **`RG-S6-18`** | **No instructional-ledger challenge is filed for `DR-M08`** and no adjudication round is required. | — | Upstream decision, consumed | 2026-08-10 | C009 charter assumption 22, **confirmed**; `../decision-records/DR-C09-02_dr-m08-routing.md` §Decision item 3–4 | Verifiable negatively: no challenge id, challenge section, or `adjudication/` entry appears anywhere in this change. |
| **`RG-S6-19`** | Gate **E** (`MM-T15`) and Gate **D** (`MM-T11`, `MM-T12`) cannot be opened on out-of-band evidence **at all**; Gate **C** (`MM-T8`) cannot be opened directly by any signal. | 2 | `[code-evidence]` — threshold definitions read at source | 2026-08-10 | `../06_…md` §8.1, §8.2, §8.3, §8.6; mastery model §4, §5 | A claim about **absent** evidence. It cannot be falsified by finding the evidence exists; it can only be falsified by a new observable signal, which the residual clause already routes to `unclassified`. |
| **`RG-S6-20`** | The composition invariant holds under this mapping: a weak grade cannot be laundered into an unlock, and contest-speed evidence never lowers an unlock bar. | 2 | `[code-evidence]` — invariant reproduced verbatim from its source line | 2026-08-10 | `../06_…md` §9; `../../C005-instructional-model/mastery-model/00_operational-mastery-model.md:55` | Checked by **inspection of the mapping's own edges**, by the task that wrote the mapping. No independent check, and no implementation to check against. |
| **`RG-S6-21`** | No signal defined here exposes a raw learner payload; log-derived evidence is aggregate-only; and learner `response` text is **not** redacted today. | 2 | `[code-evidence]` — the live redaction set read at source | 2026-08-10 | `../06_…md` §10; `src/shared/logger.ts` (`LOG_REDACT`) | The exposure is **recorded, not fixed** — no `src/` change is in scope. Filed as `OI-S6-5`. |
| **`RG-S6-22`** | `RA5` is retained (AI grading is not the signal of record) and `MC-4`'s metric is `PROXY-BOUNDING`; the shipped mapper and ceiling check are honoured and unchanged. | 2 | `[code-evidence]` | 2026-08-10 | `../06_…md` §10; `src/domain/algorithms/grade-mapper.ts`; `src/domain/algorithms/over-validation-guard.ts`; measurement-contract register `v1.0` | Verifiable negatively: no `src/` path appears in this change's diff. |
| **`RG-S6-23`** | Every SUB-2 field name is used in its exact spelling, with no renamed, pluralised or camelCased variant, so SUB-9 merges the mapping without translation. | 2 | `[code-evidence]` — checked lexically against SUB-2's definitions | 2026-08-10 | `../06_…md` throughout; `../02_content-and-exercise-forms.md` §3.5, §3.8, §3.9, §3.10, §4 | A **lexical** check. It proves the exact spellings appear; it cannot prove no near-synonym was introduced elsewhere in prose (inherited `CAP-S2-6`). |
| **`RG-S6-24`** | Every residual exposure named in the three adversarial scenarios is filed as an open item or a cap with a named owner and a trigger. | — | Structural check over this change | 2026-08-10 | `../06_…md` §7.1–§7.3; `../90_…md` `OI-S6-2`, `OI-S6-3`, `OI-S6-4`; `../91_…md` `CAP-S6-2` | Run by the producing task (inherited `CAP-S1-4`/`CAP-S2-5`). |
| **`RG-S6-25`** | `assessment_item_result` **may** feed Gate A (`MM-T9`) and Gate B (`MM-T1`, `MM-T3`), plus remediation (`MM-T13`, `MM-T14`); it may **not** feed Gate C directly, Gate D, Gate E, `MM-T4`–`MM-T7` or `MM-T10`. | 2 | `[code-evidence]` — gate/threshold semantics and the form's field set read at source | 2026-08-10 | `../06_…md` §4.4; `../02_content-and-exercise-forms.md` §3.10; `../../C005-instructional-model/mastery-model/00_operational-mastery-model.md` §4, §5 | Admission presumes the item is **discriminating** per §5.2's four-condition check, which **nothing enforces** (`CAP-S6-1`). The same signal's `MM-T2` exclusion is registered separately as `RG-S6-06`. |

## 2. Orphan-and-completeness check

### 2.1 Checks and their results

| Check | Result |
| --- | --- |
| **Every gate-bearing claim in `../06_…md` is registered.** Sections §3.1–§3.4 → `RG-S6-03`, `RG-S6-08`; §3.3 → `RG-S6-10`; §4.0–§4.8 → `RG-S6-01`, `RG-S6-04`, `RG-S6-05`, `RG-S6-06`, `RG-S6-25`, `RG-S6-07`, `RG-S6-09`; §5 → `RG-S6-11`, `RG-S6-12`, `RG-S6-13`, `RG-S6-23`; §6 → `RG-S6-14`, `RG-S6-16`; §7 → `RG-S6-24`; §8 → `RG-S6-19`; §9 → `RG-S6-20`; §10 → `RG-S6-21`, `RG-S6-22`. | **No claim unregistered.** |
| **Every gate-bearing claim in `../decision-records/DR-C09-02_…md` is registered.** Decision → `RG-S6-02`, `RG-S6-18`; control-by-control table → `RG-S6-17`. | **No claim unregistered.** |
| **Every row points at a claim that exists.** Each of `RG-S6-01`…`RG-S6-25` names a file and section that is present in this change. | **No row orphaned.** |
| **Class discipline.** Every row carries exactly one of the seven classes, or an explicit `—` for a design rule, a specification, or a consumed upstream decision that is not an evidentiary claim at all. | **Deviation, stated rather than held.** 17 of 25 rows carry one of the seven classes; **eight carry `—`** (§3, first disclosure). That is a departure from `README.md`'s one-class-per-claim rule, taken deliberately — assigning a class to a non-empirical claim would dress a judgement as a finding. It is recorded here as a **deviation**, not as a satisfied check. |
| **No class-7 row.** | **Held — zero rows carry class 7.** |
| **Every row states a structural limitation.** | **Held — 25 of 25.** |

## 3. Disclosures

### 3.1 Standing disclosures on this register

- **`—` in the class column is deliberate and is not a blank.** Eight rows (`RG-S6-01`, `RG-S6-02`, `RG-S6-07`, `RG-S6-10`, `RG-S6-11`, `RG-S6-14`, `RG-S6-18`, `RG-S6-24`) record a **design rule**, a **specification**, or an **upstream decision consumed**. None of these is an empirical claim, and assigning one an evidence class would dress a judgement as a finding — the disclosure discipline `DR-C09-01` §Evidence follows.
- **These checks were run by the task that produced the artifacts.** Inherited from `CAP-S1-4` / `CAP-S2-5` and re-stated as `CAP-S6-4`. The mitigation available was to keep the checks mechanical — presence, spelling, arithmetic, and a fixed-in-advance pass condition — rather than judgemental.
- **No class-7 `[future-real-user]` evidence supports any row, and none could.** No learner has used any part of this design. Class 7 does not exist for this package, and no class 1–6 row here is relabelled as one.

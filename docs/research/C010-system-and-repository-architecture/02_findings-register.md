# 02 — Findings Register

**Task:** NEU-971 (SUB-1) opens it · **Charter:** C010 (umbrella NEU-895) · **Opened:** 2026-08-21
**Model:** claude-opus-5[1m]
**Entries owned by:** each appending sub-task. **Consumer:** **NEU-896** (convergence).

---

## What belongs here

A **finding** is a fact about the programme's own shape that a later reader must know and would otherwise have to rediscover: a circularity, an ownership collision, a decision made in the wrong order, a gap between what a charter assumed and what the evidence shows.

A finding is **not** an open item (which has a resolving event and an owner who closes it), **not** a cap (which is an admission of a scope limit), and **not** a decision (which belongs in `decision-records/`). It is a **first-class record of something that is true about how this work happened**, published so that NEU-896 converges over a stated situation instead of rediscovering a contested one.

**A finding is recorded in its own right, not as an aside.** The charter is explicit about this for the two it names: the NEU-893 circularity and the C003/NEU-850 decision-ownership collision are each "recorded as a finding in its own right", carrying an id, not mentioned in passing inside a document about something else.

## Why this register is numbered `02` and not `9x`

It sits in the `00`–`89` per-sub-task range because SUB-1 owns and seeds it, but it behaves like a `90`-range shared register: **append-only, `F-S<n>-<k>` namespaced, keep both sides.** Read these rules, not the filename, for how to write into it. The README states the same exception.

## Append convention

> Each sub-task appends its own `### SUB-<n>` section. No sub-task reflows, renumbers, or rewrites another sub-task's entries. On a merge conflict in this file, keep **both** sides.

**Do not tidy duplicates in flight.** Two sub-tasks independently finding the same thing is evidence that it is visible from two directions; collapsing it early throws that away.

## Id namespacing (there is no global counter in this file)

Ids are **`F-S<n>-<k>`**, where `<n>` is the sub-task number and `<k>` restarts at `1` inside that sub-task's own section. SUB-12 allocates `F-S12-1`, `F-S12-2`, … concurrently with SUB-4's `F-S4-1`, without coordination and without collision. Cite a finding in its full form (`F-S1-1`), never as a bare `F-1`.

## Stable field set (the same fields on every entry, in this order)

| Field | What it records |
| --- | --- |
| **Id** | `F-S<n>-<k>` |
| **Finding** | The fact, stated as a fact. |
| **Evidence** | What establishes it — a real path, an upstream package with its version or date, or a tracker record with its read date. |
| **Consequence** | What is weaker, riskier or differently-shaped because this is true. |
| **What is assumed rather than derived** | Where the finding is about an inverted dependency, the specific inputs that were assumed, each cited by id. |
| **Handed to** | Who must act on it, and what they receive. |

---

## Entries

### SUB-1

*One entry. `F-S1-1` is the circularity the charter required to be published as a finding in its own right. The C003/NEU-850 decision-ownership collision is the other named finding this charter requires; it is **SUB-12 (NEU-986)**'s to file, together with its settled disposition, and it is deliberately not pre-filed here.*

#### `F-S1-1` — This package ran ahead of NEU-893, and the dependency between them is circular

- **Id:** `F-S1-1`
- **Finding:** **C005's OUT-9** (NEU-893, safe production integration and learner isolation) presupposes an application architecture, and **C005's OUT-8** — this package — needs the ownership model that NEU-893 would settle. The dependency runs both ways, so one of the two had to proceed on assumptions, and **it is this one.** NEU-893 is Backlog and unbuilt at this package's cutoff.

  *(Both ids in that sentence are **program-level C005 outcomes**, not this package's own `OUT-8`/`OUT-9`. See `00_method-and-provenance.md` §2.5 — a bare `OUT-n` in this package always means an entry in `01_outcome-register.md`.)*

- **Evidence:** The C010 charter records the circularity as a confirmed decision of its intake, sourced to the intake's first question and to a host finding. NEU-891, NEU-892, NEU-893 and NEU-894 were all Backlog and unstarted at the 2026-08-19 tracker read. The choice to proceed rather than block was made deliberately and recorded, not defaulted into.

- **Consequence:** Every decision in this package that touches learner isolation, the production deployment or the data-store topology rests on assumptions about NEU-893's output rather than on NEU-893's output. Those decisions are **not** thereby wrong — they are bounded by a stated tolerance envelope and carry a named invalidating outcome — but they are **assumed, not derived**, and NEU-893 must be chartered knowing which ones.

  The affected outcomes are `OUT-3` (the authority matrix), `OUT-4` (the isolation invariant and the NEU-893 disjointness contract) and `OUT-8` (the deployment-shape and data-store-topology selections).

- **What is assumed rather than derived:**

  | Input | Id | Status |
  | --- | --- | --- |
  | Learner isolation will be enforced **server-side at or below the port boundary** | `A-28` | **assumed** — `[unconfirmed]` stand-in |
  | The **existing production deployment continues to back the product** | `A-28` | **assumed** — `[unconfirmed]` stand-in |
  | A **backward-compatible migration path for existing global rows** exists | `A-28` | **assumed** — `[unconfirmed]` stand-in |
  | The authenticated subject a production token yields is a **human learner** rather than an OAuth client | `OI-S1-2` | **unverified** — no live token has been inspected |

  **And, equally important, what is *not* assumed:** learner-ownership **placement** is **consumed** from **NEU-850's** OUT-2 (`user_id` NOT NULL on every core table, keyed to the JWT subject, threaded through the 9 row-owning repository ports) with its source cited — it is a decision to honour, not a guess, and NEU-850 is converged though unimplemented. Likewise the deployment facts (single self-hosted VPS, unversioned compose stack outside the repository, no Dockerfile, no IaC, no rollback, automatic migration on boot, process-local in-memory state) are **confirmed against the repository**, not assumed from NEU-893.

  **The circularity is therefore narrower than it first appears**, and stating its exact boundary is the point of this record: three clauses of one stand-in plus one unverified transport fact — not the whole isolation question.

- **Handed to:**
  - **NEU-893**, which is chartered knowing exactly which of this package's inputs were assumed rather than derived — the four rows above — and which receives `OUT-4`'s two-list disjointness contract naming what this package closed and what it handed on.
  - **NEU-896**, which receives this finding alongside `93_stand-in-assumption-register.md`, so convergence reconciles a **stated** inversion rather than rediscovering it. The second named finding NEU-896 expects — the C003/NEU-850 decision-ownership collision with its settled disposition and any routed amendment — arrives from **SUB-12 (NEU-986)**.

---

<!--
Later sub-tasks: append your own `### SUB-<n>` section BELOW this comment. Do not edit any
section above your own. On conflict, keep both sides.
-->

### SUB-2

*Two entries. Both are facts about how the programme's own evidence is shaped — one about a presupposition that was true of one path and false of another, one about a class label that mis-sizes a component by about five times. Neither is an open item: nobody closes them, and there is nothing to resolve. They are things a later reader must know.*

#### `F-S2-1` — The umbrella brief's execution-environment presupposition was path-specific, and reading it as a single claim is what made it look contradictory

- **Id:** `F-S2-1`
- **Finding:** The C010 umbrella brief presupposes *"execution-environment needs established by upstream packages"*. That presupposition is **false for the learner path and true for the authoring path**, and NEU-890 says both things plainly in the same package. `../C009-course-content-quality/decision-records/DR-C09-04_authoring-languages.md:92` states that NEU-890 *"selects no runtime, no compiler, no sandbox and no execution environment"*; `../C009-course-content-quality/09_enforceable-quality-system.md:70` states that the `automated` gate class costs *"An execution environment, and a re-run budget."* **The two statements never contradicted each other.** One scopes what happens to a solution a **learner** writes; the other scopes what happens to an approach a **creator** writes, before any learner sees the node. Read as one claim about one system, they look irreconcilable, and that reading is what produced the presupposition rather than a decision.
- **Evidence:** `../C009-course-content-quality/decision-records/DR-C09-04_authoring-languages.md:92`; `../C009-course-content-quality/09_enforceable-quality-system.md:70`, `:216`, `:315`, `:325`; `../C009-course-content-quality/06_assessment-evidence-out-of-band.md:6`, `:54`, `:409` (NEU-890, compiled 2026-08-10). All read on `origin/develop` at the 2026-08-21 cutoff.
- **Consequence:** Any later reader who resolves the tension by picking **one** of the two statements gets a wrong architecture in a specific, predictable way. Picking `DR-C09-04:92` deletes the component that runs `EQ-S4-6` — an `automated`, **`blocks`** gate — leaving a quality system that specifies a gate nothing can run. Picking `09_…:70` re-introduces a learner-facing runner into a system whose entire assessment design exists because there is none. **The failure mode is not "getting it wrong"; it is getting it half-right and not noticing**, because either half is fully citable.
- **What is assumed rather than derived:** Nothing. Both limbs are `consumed` from a **built and published** upstream package and are cited by file and line; neither rests on a stand-in, and this finding introduces no new assumption of its own.
- **Handed to:**
  - **Every sub-task SUB-3 … SUB-16**, which may now cite `03_execution-environment-and-citation-drift-component.md` §2 and §3 instead of re-deriving the split from NEU-890 — and which should treat any single-limb citation of the execution question as a defect.
  - **NEU-896**, which converges over a **stated** reconciliation rather than rediscovering an apparent contradiction inside a delivered package.

#### `F-S2-2` — The `automated` gate-class label over-sizes the execution component by about five times

- **Id:** `F-S2-2`
- **Finding:** NEU-890's mechanism distribution puts **11 of 89** requirement rows in the `automated` class. Only **2 of those 11 execute authored code**: `EQ-S4-6` (runs the creator's approach over the node's boundary inputs) and `EQ-S6-4` (substitutes a named misconception's method into an item and evaluates it). The other nine are a lexical scan over artifacts (`EQ-S1-2`, `EQ-S1-3`, `EQ-S1-6`, `EQ-S8-11`), a URL or id resolution at an external source (`EQ-S3-3`, `EQ-S3-4`, `EQ-S3-5`, `EQ-S3-6`), or an idempotence re-run (`EQ-S3-10`). **The class label is not wrong — all eleven require *running something* — but "running a grep" and "running a creator's algorithm over an adversarial input" are the same class and different components.**
- **Evidence:** `../C009-course-content-quality/09_enforceable-quality-system.md:302` (the distribution: `deterministic` 28 · `schema` 20 · `server-side` 15 · `automated` 11 · `AI` 15 = 89) and the eleven rows themselves at `:154`, `:155`, `:158`, `:190`, `:191`, `:192`, `:193`, `:197`, `:216`, `:244`, `:283` (NEU-890, compiled 2026-08-10). Counted row-by-row at the 2026-08-21 cutoff rather than inherited, per `CAP-S1-2`'s owner rule.
- **Consequence:** Two distinct sizing errors, both of which a reader makes silently. **First**, a reader who provisions the authoring-time execution component from the class label provisions it for eleven rows rather than two — roughly a five-fold over-provision of an isolation substrate that, per `DR-C10-S2-2`, needs no containment at all. **Second and worse**, the four `EQ-S3-*` rows are not gate-runner work: they are **egress to external source sites** and belong to the citation-drift producer of `03_execution-environment-and-citation-drift-component.md` §4.2, which carries a completely different trust boundary, a completely different retention rule, and a budget of zero. Folding them into the gate runner would put the system's only externally-facing egress inside a component specified to have none.
- **What is assumed rather than derived:** Nothing about the counts — they are read from the published tables. The *classification* of each row as "executes authored code" or not is this sub-task's reading of the row text, not a label NEU-890 assigns; a reader who disagrees with a particular row's placement can re-read it at the line cited.
- **Handed to:**
  - **`SUB-4 (NEU-974)`**, which places both components and must not merge them: the two-row gate runner and the four-row egress producer are separate components with contradictory egress fields.
  - **`SUB-10 (NEU-984)`**, which sizes the substrate and should size it from two rows, not eleven.

### SUB-3

*Three entries. Two are citation-hygiene hazards that a mechanical audit can find and a human reader reliably cannot — one a namespace collision between two packages that both number their sub-tasks, one a wrong tracker id repeated across six merged files. The third is an upstream requirement that is unmet in the codebase today. None is an open item in the sense of needing a decision; all three are things a later reader must know before trusting a grep.*

#### `F-S3-1` — `S3` denotes two different sub-tasks in two live packages, and the `EQ-S3-*` ids are C009's

- **Id:** `F-S3-1`
- **Finding:** `F-S2-2` names `EQ-S3-3`, `EQ-S3-4`, `EQ-S3-5`, `EQ-S3-6` and `EQ-S3-10` as egress rows. Those ids are **NEU-890's**, defined in `../C009-course-content-quality/09_enforceable-quality-system.md`, where the `S3` segment denotes **C009's own sub-task 3**. They are **not** ids in this package's namespace and have nothing to do with C010's SUB-3 (NEU-973). Two live packages both namespace ids by sub-task number, both are cited in the same documents, and the segments collide exactly. **A reader — or a script — that resolves `S3` against the wrong package gets a confident, wrong answer rather than a miss.**
- **Evidence:** `../C009-course-content-quality/09_enforceable-quality-system.md:190`–`:193` and `:197` (NEU-890, compiled 2026-08-10) define the five rows. `README.md` declares this package's id families as exactly five — `A-`, `OI-S<n>-<k>`, `CAP-S<n>-<k>`, `SPK-S<n>-<k>`, `F-S<n>-<k>` — and `EQ-` is not among them. `00_method-and-provenance.md` §2.5 requires a program-level id to be owner-attached. Read on `origin/develop` at the 2026-08-21 cutoff.
- **Consequence:** The five rows are **not** carried as `SC-S3-*` inventory entries. Doing so would mint a sixth id family in a package that declares five, and would bind a foreign package's requirement ids into this package's namespace at the exact point the two collide. `04_state-category-inventory.md` §5.5 carries a cross-walk instead, mapping each row to the entry that holds its state — and recording that `EQ-S3-6` maps to **no** entry by design, because NEU-890 specifies that nothing from the page is stored. The live hazard is that **any future citation of a bare `S3` id in either direction is ambiguous on its face**, and the ambiguity is invisible to a reader who happens to resolve it correctly.
- **What is assumed rather than derived:** Nothing. Both id sets are read from published files at cited lines, and the collision is a property of the two naming schemes, not an inference about intent.
- **Handed to:**
  - **`NEU-985 (SUB-11)`**, whose citation audit can check this mechanically: every `EQ-` id in this package must resolve to C009, and every `S<n>-` id in a C010 family must resolve to a C010 sub-task. Neither direction is currently enforced.
  - **Every sub-task SUB-4 … SUB-16**, which should attach the owner when citing a cross-package id (`NEU-890's EQ-S3-4`), never the bare form.

#### `F-S3-2` — SUB-13 is NEU-977, and a merged sibling cites it as NEU-987 in thirteen places

- **Id:** `F-S3-2`
- **Finding:** SUB-2's merged output refers to `SUB-13` as **NEU-987** in **13 places across 6 files**. The umbrella's actual child mapping puts **SUB-13 at NEU-977**; **NEU-987 is not a child of NEU-895 at all.** The charter's children are deliberately non-sequential — SUB-7…SUB-16 are NEU-980, 981, 983, 984, 985, 986, 977, 978, 982, 979 — so the number cannot be inferred from the sub-task index, and a transposition produces a plausible-looking id that resolves to nothing in this charter.
- **Evidence:** The child mapping is read from the umbrella NEU-895's own children at the 2026-08-21 cutoff. The 13 occurrences are `92_spike-register.md:152`; `03_execution-environment-and-citation-drift-component.md:150`, `:179`, `:192`, `:205`, `:223`, `:252`; `90_open-items-and-provisional-register.md:136`, `:137`; `traceability/S2_execution-environment-and-drift-coverage.md:20`; `decision-records/DR-C10-S2-3_out-of-band-citation-drift-component.md:47`, `:51`; `decision-records/DR-C10-S2-2_authoring-time-execution-boundary.md:57`.
- **Consequence:** **`SUB-13 (NEU-977)` grepping the package for its own id will not find any of the thirteen rows addressed to it** — including `OI-S2-2`, an open item filed with SUB-13 as its named owner, which will therefore appear ownerless to the sub-task that owns it. This is precisely the silent-omission failure the package's id discipline exists to prevent, and it is invisible to a reader who does not already know the mapping. **Nothing here is corrected in place:** the registers are append-only and no sub-task rewrites a sibling's merged entries, so the correction is this finding, not an edit.
- **What is assumed rather than derived:** Nothing about the occurrences — each is cited by file and line. The mapping itself is read from the tracker rather than from a file in this package, so a reader re-verifying it queries NEU-895's children rather than re-reading `docs/`.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which must read the thirteen cited locations directly rather than trusting a grep for `NEU-977`, and in particular must pick up `OI-S2-2`.
  - **`NEU-985 (SUB-11)`**, whose citation audit should resolve every `NEU-9xx` id appearing in this package against the umbrella's actual child set and report any that resolve outside it.
  - **`NEU-986 (SUB-12)`**, which reconciles the completeness gate over a package containing a known-wrong id it may not silently repair.

#### `F-S3-3` — Both operational log tables hold learner payload with no retention window, no deletion owner and no principal field

- **Id:** `F-S3-3`
- **Finding:** NEU-887's operational-log privacy gate classifies `mcp_request_log.response_body` and `mcp_request_log.params` as **learner payload** — learner-facing text and learner free-text answers, credential-redacted only — and `operation_event_log.data` as potentially payload. That gate requires every raw result and every minimized derivative to carry a **stated retention window** and a **named deletion owner**. **The codebase implements neither, for either table.** Compounding it, neither table carries any principal field, so the payload cannot be attributed to a learner even in principle.
- **Evidence:** `../C005-product-foundation/measurement-contracts/05_operational-log-privacy-gate.md:19`–`:23` (the per-column payload classification), `:37` (the retention-limit and deletion-owner requirement), `:54` (`PLA-retention`) — NEU-887, dated 2026-07-12. Against the codebase at the 2026-08-21 cutoff: `drizzle/0010_create_infrastructure_mcp_request_log.sql:1`–`:19` and `drizzle/0012_extend_mcp_request_log.sql:1`–`:6` create and extend the first table; `drizzle/0013_create_operation_event_log.sql:1`–`:16` creates the second; neither defines a retention or expiry column, and a search of `src/infrastructure/db/schema.ts` for `user_id`/`userId`/`learner_id`/`learnerId` returns zero matches. Writers are `src/transport/pg-audit-transport.ts:117` and `src/transport/pg-event-transport.ts:109`.
- **Consequence:** Two `existing` categories — `SC-S3-16` and `SC-S3-17` — hold learner payload that **cannot be deleted per learner, because there is nothing to key the deletion on**. Any ownership model `SUB-6` selects, and any authority `SUB-13` assigns, has to confront a store where the learner-scoping question is not merely unanswered but currently unanswerable. `SUB-14`'s per-row isolation test applied to these two rows has no column to test. **This finding does not fix the gap, design a retention column, or name the deletion owner** — all three are outside SUB-3's scope, which enumerates and classifies.
- **What is assumed rather than derived:** Nothing about the schema or the requirement — both are read at cited lines. What is **not** established here is whether any learner payload has in fact been written to either table in any deployment; the finding is about what the schema permits and what the gate requires, not about a measured data population.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which assigns authority over `SC-S3-16` and `SC-S3-17` and must record that the rows are learner-payload-bearing.
  - **`SUB-14 (NEU-978)`**, which applies `SUB-5`'s isolation invariant per row and will find no column to apply it to on either.
  - **`NEU-986 (SUB-12)`**, via `CAP-S3-3`, which carries the unmet requirement to the package completeness gate.

### SUB-4

*Six entries. Two are numbers a later sub-task would otherwise inherit stale — one re-verified against `src/` under `CAP-S1-2`'s own obligation, one an internal inconsistency inside a merged sibling's file that append-only discipline forbids repairing in place. Four come out of walking the model: two hops whose authority is genuinely undetermined, one trust boundary that meets the test and that nothing enforces, and one authority that is determined only under an ownership model nobody has selected. None is a decision this sub-task declined to take.*

#### `F-S4-1` — The registered tool count is 46 across 16 modules, not the 45 the charter consumed

- **Id:** `F-S4-1`
- **Finding:** The charter's consumed figure is **45 tools (42 gated / 3 exempt)**. Counted against `src/` at this sub-task's own cutoff, the figure is **46 registered tools across 16 non-empty modules**, which with the three exempt tools makes the current split **43 gated / 3 exempt**. The divergence is one tool, in the gated half. `CAP-S1-2` obliges the sub-task that depends on the number to re-verify it against `src/` at its own cutoff and cite its own command; this entry is that re-verification.
- **Evidence:** `grep -rc "server.registerTool(" src/server/` on `origin/develop` at the **2026-08-21** cutoff returns per-file counts summing to 46 over 20 scanned files, 16 of them non-zero: session-lifecycle 4, session 1, server-context 1, analytics 3, teaching 5, remediation 1, spaced-repetition 7, query 3, topic 3, server-workflow 1, search 1, session-progress 3, server-info 1, notes 3, content 3, chunk 6; `tools.ts`, `persistence-tools.ts`, `session-management-tools.ts` and `tool-helpers.ts` register none. The three exempt tools are `init_agent_context`, `get_server_info` and `get_server_workflow`, fixed at `src/transport/context-token-middleware.ts:5`–`:9`.
- **Consequence:** Any sub-task sizing a surface, an audit or a migration from the charter's 45 sizes it one tool short, and — more importantly — sizes it against a number that has already moved once and will move again. **The count is a moving target, not a constant**, and the right treatment downstream is to re-run the command rather than to cite either figure. `SUB-8 (NEU-981)`, whose work is keyed to the tool surface, is the sub-task most exposed to inheriting a stale value.
- **What is assumed rather than derived:** Nothing about the count — it is one command over one tree at one cutoff, reported per file so it can be re-run and disagreed with. What is **not** established is that the count is stable, or that the divergence from 45 reflects a deliberate addition rather than a miscount upstream; this entry does not adjudicate which figure was right when the charter was written.
- **Handed to:**
  - **`SUB-8 (NEU-981)`**, which must re-run the command at its own cutoff rather than citing 45 or 46.
  - **`NEU-986 (SUB-12)`**, which reconciles the package against a charter figure that no longer matches the tree.

#### `F-S4-2` — `04_state-category-inventory.md` announces 41 entries and counts 45

- **Id:** `F-S4-2`
- **Finding:** The inventory's §3 heading reads *"The inventory — **41 entries**, each appearing exactly once"*, while §8's own count table totals **45** (30 `existing` + 11 `required-by-upstream` + 4 `assumed`), and the entry ids run continuously from `SC-S3-1` to `SC-S3-45`. **45 is the correct figure**; the 41 in the heading is stale. The document is internally inconsistent in the one place a reader is most likely to stop reading.
- **Evidence:** `04_state-category-inventory.md:70` (the heading) against `:528`–`:534` (the count table) and `:194`–`:205` (`SC-S3-42` … `SC-S3-45`, the four `assumed` entries). Read on `origin/develop` at the 2026-08-21 cutoff. The §4.5 walk result and §8's per-class rows agree with 45; only the §3 heading does not.
- **Consequence:** A reader who takes the heading at face value concludes four entries are missing and either re-derives the inventory or files a spurious gap. **Nothing here is corrected in place:** `04_state-category-inventory.md` is a merged sibling's file, the registers and topic documents are append-only, and no sub-task rewrites another's entries — so the correction is this finding, not an edit. The practical rule for every downstream reader is: **§8's table is the count; §3's heading is not.**
- **What is assumed rather than derived:** Nothing. Both figures are read at cited lines and the id range is continuous and checkable. This entry does not infer *why* the heading is stale — a late-added §3.7 is the obvious explanation and is not asserted.
- **Handed to:**
  - **`SUB-13 (NEU-977)`** and **`SUB-14 (NEU-978)`**, which iterate the entry set and must iterate 45 rows.
  - **`NEU-985 (SUB-11)`**, whose cross-cutting audit can check announced-versus-actual counts mechanically across every package document.

#### `F-S4-3` — The gate-verdict write is the one flow in the model with no authoritative side

- **Id:** `F-S4-3`
- **Finding:** Of the 22 flows in `05_system-context-and-responsibility-boundaries.md` §5, exactly one — `FL-S4-16`, the gate verdict travelling from the authoring-time gate runner through the battery onto the unit's record — **has no named authoritative side.** It is also the one undetermined hop in the `JNY-F1` walkthrough (hop F1-5). The model records it as a finding rather than narrating past it or nominating a plausible owner.
- **Evidence:** `../05_…` §5 (`FL-S4-16`), §10.2 (hop F1-5), §10.4 (the walk result: 21 of 23 hops with a named authority). The underlying gap is `90_open-items-and-provisional-register.md` `OI-S2-2`, whose resolving event is **SUB-13** publishing the `OUT-3` authority matrix with exactly one authority for each of the gate-verdict record, the drift-verdict store and the drift-verdict cache. Note that `OI-S2-2` names its consumer and owner as `NEU-987` — the wrong id recorded by `F-S3-2`; the owner is **SUB-13 (NEU-977)**.
- **Consequence:** A component-and-boundary model with one unattributed write is exactly as useful as one with none *provided the gap is visible*, and invisible-and-plausible is the failure mode this entry exists to prevent. Concretely: an implementation charter reading §5 and finding no authority for `FL-S4-16` must go to `OI-S2-2`, not invent one. **This sub-task does not assign the authority**; assigning it here would put two owners on one state category, which is the precise thing `OUT-3` forbids.
- **What is assumed rather than derived:** Nothing. The absence is a property of the published flow table, checkable by reading it, and the underlying open item is cited by id.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which closes it by publishing the authority matrix, and which must reach `OI-S2-2` through `F-S3-2`'s cited locations rather than by grepping its own id.

#### `F-S4-4` — Three walked hops have an authority that is determined only under an unselected ownership model

- **Id:** `F-S4-4`
- **Finding:** Hops B1-1, B1-2 and F3-1 have a determined authoritative side **today**, under the reading in which the MCP core is the only writer of learning state. That reading is not a selected ownership model — the all-MCP-versus-hybrid selection belongs to `SUB-6 (NEU-976)` and has not been made. **If SUB-6 selects hybrid, those three hops acquire a second candidate authority** and `BND-S4-16` (web tier ↔ Postgres) turns from an undecided edge into a real boundary row.
- **Evidence:** `../05_…` §10.1 (hops B1-1, B1-2), §10.3 (hop F3-1), §4.4 (`BND-S4-16` and the constraint its resolution must satisfy), §10.4 (the rider). The state facts the today-reading rests on are `04_state-category-inventory.md` §4.1's fourteen tables, all written from `src/adapters/drizzle/`.
- **Consequence:** The three hops must not be cited as settled authority in a document that outlives SUB-6's selection. The model states the constraint instead of the answer: **whichever way SUB-6 decides, `OUT-3` requires exactly one authority per category, so a hybrid selection owes a demonstration that the categories the web tier writes are disjoint from those the MCP core writes.** Disjointness is a condition to be demonstrated, not assumed. This entry is not an open item — `OI-S3-1` and the `OUT-3` matrix already carry the resolving event — it is the placement-side consequence of that open item, recorded where a reader of the walkthrough will hit it.
- **What is assumed rather than derived:** That the MCP core is today the only writer of the durable learning-state tables. That is read from `src/` at the 2026-08-21 cutoff and is a statement about the code as it stands, not a commitment about what SUB-6 may select.
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, which selects the ownership model and, if it selects hybrid, owes the disjointness demonstration and a classified `BND-S4-16` row.
  - **`SUB-13 (NEU-977)`**, whose authority matrix is where the second candidate authority would have to be resolved.

#### `F-S4-5` — STDIO is a trust boundary nothing enforces, and all three benchmark journeys are walked across it

- **Id:** `F-S4-5`
- **Finding:** The STDIO transport edge meets the trust-boundary test — it is reachable by a principal other than the operator — and **no component enforces it.** `src/transport/main.ts:46` mounts origin checking, JWT verification, per-JWT-subject rate limiting, MCP-session-to-subject binding, the context-token gate and audit capture; the `else` branch at `:55`–`:59` connects a bare `StdioServerTransport` with none of them. The model publishes it as `BND-S4-17`, classified **trust — unenforced**, owner **nobody**. Compounding it: the three C005 benchmark journeys that have an Existing-MCP vehicle are dogfooded through a local MCP client, i.e. through exactly this edge — **so every observation made while running them is an observation of the unprotected transport.**
- **Evidence:** `src/transport/main.ts:46`, `:55`–`:59`; `src/transport/http.ts:99`–`:111` (origin allowlist), `:48`–`:65` (MCP-session-to-subject binding), `:167`–`:170` (per-JWT-subject rate limiting, mounted only when auth is enabled); `src/transport/context-token-middleware.ts` (the gate, mounted on the HTTP path). Vehicle assignment: `../C005-product-foundation/benchmark-suite/01_journey-vehicles-and-fidelity.md:62`, `:64`, `:66` (NEU-900, compiled 2026-07-11). Read at the 2026-08-21 cutoff.
- **Consequence:** Two things a later sub-task must not do. **First**, write "the tool surface is authenticated" without a transport qualifier — it is false for half the surface, which is why `DR-C10-S4-2` makes the qualifier part of the decision rather than a note. **Second**, read "the journey ran fine" as evidence about the gated path; the journeys' hypotheses are pedagogical and are untouched by this, but no protection claim can be supported by them. This is a **finding about the system as it stands**, not a design proposal: this sub-task neither hardens STDIO nor recommends removing it.
- **What is assumed rather than derived:** Nothing about the mounting — each middleware is cited at its line and the two branches are read directly. Not established: whether any deployment actually exposes the STDIO path to a principal other than the operator. The finding is about what the code permits, not about a measured exposure.
- **Handed to:**
  - **`SUB-7 (NEU-980)`**, whose web-API resource inventory and negative boundary must not assume a uniformly gated surface.
  - **`SUB-10 (NEU-984)`**, which selects the deployment shape and therefore decides whether the unenforced edge is reachable in practice.
  - **`NEU-985 (SUB-11)`**, whose audit can check mechanically that every protection claim in the package carries a transport qualifier.

#### `F-S4-6` — No component is authoritative for a per-DP-pattern mastery signal, and the journey that needs one has nowhere to read it

- **Id:** `F-S4-6`
- **Finding:** `JNY-B1`'s BM-8 half requires reading a per-DP-pattern mastery signal. Walking it across the model, that hop (B1-10) has **no authoritative side**: the durable multi-session mastery composite is `SC-S3-39`, a `required-by-upstream` category with **no store**, and no component in the twenty-component inventory holds it. C005 reached the same wall from the other direction and marked it `INC-2`, UNRESOLVED. **The gap in the state inventory and the gap in the journey are the same gap seen from two sides.**
- **Evidence:** `../05_…` §10.1 (hop B1-10), §10.4; `04_state-category-inventory.md:175` (`SC-S3-39`, store `none`, `required-by-upstream`), and its §5.2 provenance in NEU-888's operational mastery model; `../C005-product-foundation/benchmark-suite/01_journey-vehicles-and-fidelity.md:16` (the BM-8 half is *"capability inspection only"*, `INC-2` UNRESOLVED) and `:62`. Read at the 2026-08-21 cutoff.
- **Consequence:** `JNY-B1`'s BM-8 half cannot be walked to a named authority, and a sub-task that assumed it could would have to invent either a component or a store. It also means the mastery signal is one of the categories `SUB-13`'s authority matrix must assign to a component **that does not exist yet** — which is a different and harder case than assigning an existing category, and worth flagging before the matrix is written. This entry neither designs the component nor selects a store.
- **What is assumed rather than derived:** Nothing. Both the empty store and the journey's own incompleteness marker are read at cited lines. Not asserted: that `SC-S3-39` *should* be held by any particular component — that is precisely what is undetermined.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which must assign an authority for `SC-S3-39` and will be assigning it to a component that has to be created rather than located.
  - **`NEU-986 (SUB-12)`**, at the completeness gate, as an upstream-required category with neither a store nor a holder.

### SUB-5

*Four entries. Two are inherited facts that re-verification moved — one a location, one a set of counts — filed under `CAP-S1-2`'s own re-verify obligation rather than passed on stale. One is a structural consequence of applying the isolation invariant to the transports. One records what the invariant establishes about the system as a whole, which is stronger and more uncomfortable than any single category's verdict. None is a decision this sub-task declined to take.*

#### `F-S5-1` — One frozen `AppContext` is shared by every MCP session, and it carries no principal field

- **Id:** `F-S5-1`
- **Finding:** `AppContext` is **a single frozen instance**, built once at process start and reused for **every** MCP session for the life of the process. It carries **57 top-level properties, 53 of them arrow-function closures**, and **no** auth, principal, subject, tenant or identity field of any kind. The charter's inherited figure of "~60 closures" is close but not the number; more importantly, the number is not the fact that matters — **the sharing is.**
- **Evidence:** `src/composition-root.ts:136`–`:314` (the type: 57 properties, 53 closures; the 4 non-closures are `contextTokens`, `contextTokenTtlMs`, `mapChunkRowToLearningItem`, `applyBatchSessionChunkOperations`), built at `:518`–`:636` with `Object.freeze` at `:638`; created **once** at `src/transport/main.ts:41`; reused per session at `src/transport/http.ts:220`. Read on `origin/develop` at the **2026-08-21** cutoff.
- **Consequence:** A per-principal execution context **cannot be a field added to `AppContext`**, because there is one `AppContext` and it is built before any request exists. Any mechanism that threads an authenticated principal into the domain must either construct a per-request derivative of the frozen instance or pass the principal alongside it through the 53 closures — and those are materially different changes with materially different costs. A sub-task sizing the change from the closure **count** sizes the smaller of the two problems. This is the wide half of `SUB-8 (NEU-981)`'s blast radius, and it is wide because of the lifecycle, not the arity.
- **What is assumed rather than derived:** Nothing about the structure — the property and closure counts, the single `Object.freeze`, the single construction site and the per-session reuse are each read at a cited line. **Not** asserted: which of the two threading strategies is correct, or what either costs. That is `SUB-8`'s pricing and `NEU-893`'s mechanism.
- **Handed to:**
  - **`SUB-8 (NEU-981)`**, which must size the compatibility change against the sharing lifecycle rather than the closure count.
  - **NEU-893**, whose isolation mechanism has to put the principal somewhere, and for which `AppContext` is not that place as currently built.

#### `F-S5-2` — The single-learner guard is in orchestration, above the port boundary, and outside `A-28`'s tolerance envelope

- **Id:** `F-S5-2`
- **Finding:** The behaviour is as the charter records it — the system rejects a session start while any learning session is globally active — but **its location is not.** The rejection is **not** in the adapter's `createSession`; it is in the orchestration workflow, which reads the unscoped `getActiveSession()` and fails the request if it returns anything. So the one guard that encodes single-learner behaviour today sits **above the port boundary**, and `A-28`'s tolerance envelope assumes enforcement **at or below** it.
- **Evidence:** `src/orchestration/session-workflows.ts:39`–`:46` — `const activeSession = await deps.sessions.getActiveSession(); if (activeSession) { return serviceFail({ type: 'conflict', … }); }`. `src/adapters/drizzle/session-repository.ts:37`–`:66` (`createSession`) contains **no** such check; `:73`–`:80` (`getActiveSession`) carries **no scoping predicate**. Read on `origin/develop` at the **2026-08-21** cutoff. The envelope this lands on is `93_stand-in-assumption-register.md`'s `A-28`.
- **Consequence:** An isolation mechanism implemented **at the port boundary** — which is what `NEU-850's OUT-2` describes and what `A-28` tolerates — scopes the repository reads and **leaves this guard untouched**, so a scoped `getActiveSession()` returning one learner's session is still adjudicated by a rule that means "any". The failure is quiet: the ports look correct and the behaviour is still global. It is also the concrete demonstration behind `../06_…` §3.6 case 3, where `SC-S3-5` reaches `fails-confinement` with `NEU-850's OUT-2` implemented in full — **a column is necessary and is not sufficient.** This entry does not relocate the guard or propose where it should live.
- **What is assumed rather than derived:** Nothing about the location — both call sites are read at cited lines and the adapter's absence of a check is a property of the file. **Not** asserted: that the guard is *wrong* to be in orchestration. Placing session-conflict policy above the ports is a defensible layering choice; the finding is that `A-28`'s envelope does not reach it, not that the code is misplaced.
- **Handed to:**
  - **NEU-893**, which must decide whether its mechanism covers enforcement above the port boundary or whether such enforcement is prohibited — `H8` on `../06_…` §5.3's list.
  - **`SUB-8 (NEU-981)`**, for whose blast radius the orchestration guard is a distinct site from the adapter query bodies.
  - **`SUB-14 (NEU-978)`**, because any category whose confinement rests on an orchestration-level guard fails I3 as stated.

#### `F-S5-3` — All 43 gated tools already declare `context_token`; the charter's 45 / 42 / 40 are stale in all three positions

- **Id:** `F-S5-3`
- **Finding:** Re-counted at this sub-task's own cutoff: **46 registered tools across 16 registering modules, 43 gated / 3 exempt**, and **every one of the 43 gated tools already declares `context_token`** — **42** through a named `*InputShape` constant (41 imported from `src/domain/types/`, one declared module-locally) and exactly **one genuinely inline**. The charter consumed **45 / 42 / 40**; all three figures are stale, and the third is stale in a way the first two are not — the "40 named" figure omits three declarations, so a reader deriving "2 tools lack it" from the charter's arithmetic would be **wrong about which tools need work**, not merely off by two. The set of gated tools lacking `context_token` is **empty**.
- **Evidence:** `grep -rc "server.registerTool(" src/server/` over `origin/develop` at the **2026-08-21** cutoff (46 across 16 non-zero files; `tools.ts`, `persistence-tools.ts`, `session-management-tools.ts` and `tool-helpers.ts` register none — the middle two are pure aggregators). The 3 exempt tools are fixed at `src/transport/context-token-middleware.ts:5`–`:9` and are the only three `z.object({}).shape` in `src/server/*-tools.ts` (`server-info-tools.ts:13`, `server-context-tools.ts:21`, `server-workflow-tools.ts:15`). The one genuinely inline declaration is `teach_next` — `inputSchema: z.object({` at `src/server/teaching-tools.ts:34`, `context_token` at `:35`. The one **module-local named** constant is `GetHistoricalFeedbackInputShape` at `src/server/session-progress-tools.ts:128`–`:138` (`context_token` at `:131`, consumed at `:153`) — a named `*InputShape` that simply is not imported from `src/domain/types/`, which is why a search of the domain-types tree alone undercounts by one. This entry is the re-verification `CAP-S1-2` obliges, and it **corroborates `F-S4-1`'s 46** against the charter's 45.
- **Consequence:** The claim `SUB-8 (NEU-981)` actually needs — **zero gated tool input schemas would newly declare `context_token`, so no bulk schema migration is implied** — holds, and it must be carried **count-independently**, as *"the set of gated tools lacking `context_token` is empty"*. `F-S4-1` already establishes the count is a moving target; a claim pinned to 43 goes stale the next time a tool is registered, while the emptiness claim does not. The residual work on this surface is **semantic**, not migratory: what it means to reuse or widen an argument already required on every gated call. The 3 exempt tools remain a separate decision.

  **Where the stale figures live, and why they are not edited:** `01_outcome-register.md:75` — `OUT-6`'s own success measure — states *"45 registered tools across 16 `src/server/*-tools.ts` modules"* and *"all 42 gated tool schemas already declare `context_token` (40 through shapes in `src/domain/types/*.ts`, 2 inline in `src/server/`)"*. That is the text `SUB-8 (NEU-981)` reads when it opens its own outcome, and all four numbers are now wrong. **Nothing is corrected in place:** the outcome register is a merged sibling's file and no sub-task rewrites another's entries, so — exactly as `F-S4-2` did for the inventory's stale heading — **the correction is this finding, not an edit.** The practical rule for `OUT-6`'s reader: take the counts from here or, better, re-run the command; take only the *qualitative* claim from `OUT-6`, and note that even that claim's parenthetical breakdown is wrong while its conclusion ("no bulk schema migration is implied") is right.
- **What is assumed rather than derived:** Nothing about the counts — one command over one tree at one cutoff, reported per file so it can be re-run and disagreed with. **Not** adjudicated: whether the charter's 45 / 42 / 40 was correct when written or miscounted then. This entry supersedes those figures for downstream use without ruling on their provenance.
- **Handed to:**
  - **`SUB-8 (NEU-981)`**, which must re-run the count at its own cutoff and cite neither 45 nor 43, and which should carry the emptiness claim rather than any number.
  - **`NEU-986 (SUB-12)`**, reconciling the package against charter figures that have now moved twice.

#### `F-S5-4` — No state category can reach `holds`, and the binding constraint is the transport rather than the database schema

- **Id:** `F-S5-4`
- **Finding:** Applying the isolation invariant across the transports yields a result about the **system**, not about any one category: **at this cutoff no state category can satisfy the invariant, and completing `NEU-850's OUT-2` in full would not change that.** Every in-domain category fails at I4 (transport invariance) before its confinement is ever assessed, because the STDIO transport produces **no authenticated principal at all** — so the binding constraint is the transport, and **a column cannot supply a principal the transport never produced.**
- **Evidence:** `src/transport/main.ts:55`–`:59` connects a bare `StdioServerTransport`; every protection is HTTP-only and reached only from `main.ts:46`–`:54` — JWT `src/transport/http.ts:163`–`:165`, origin `:106`–`:120`, rate limit `:172`–`:174`, context-token gate `:185`–`:187`. The identity that exists on HTTP is an HTTP-response local (`src/transport/jwt-middleware.ts:133`–`:136`), which has no analogue on STDIO. `../05_…` §4.2 `BND-S4-17` classifies the edge as trust, **unenforced, owner `nobody`**; `F-S4-5` records that all three dogfooded benchmark journeys are walked across it. The derivation is `../06_…` §3.3–§3.4 (the ordered checks) applied in §4.2. Read at the **2026-08-21** cutoff.
- **Consequence:** Three, and they point in different directions. **(1)** Schema work is necessary and **not** sufficient; a plan that lands `user_id NOT NULL` across the core tables and declares learner isolation achieved is wrong, and the invariant says so mechanically rather than as a caution. **(2)** The STDIO gate is on the **critical path** for isolation, not beside it — which is a scheduling fact for NEU-893, whose four charter questions contain no transport item. **(3)** Because I4 precedes I5, the STDIO gap currently **masks** the `sub`-versus-`azp` defect; closing it does not make the system isolated, it makes the principal-kind problem visible (`../06_…` §4.3). The corresponding limit on this package — that the invariant therefore has **zero positive instances** and is shown well-formed rather than satisfiable — is capped as `CAP-S5-1`, not restated here.
- **What is assumed rather than derived:** The transport facts are read at cited lines. What is **derived** is the universality — that *every* in-domain category fails I4 — and it rests on one premise stated plainly: that no principal is produced on the STDIO path at all, so no per-category difference can rescue any of them. **Not** asserted: that the unenforced edge is reachable in the production deployment; that is `SUB-10 (NEU-984)`'s, and the verdict is unconditional on it.
- **Handed to:**
  - **NEU-893**, as `H7` on `../06_…` §5.3's list — the transport gate it must close, and the sequencing consequence that closing it surfaces `H6` rather than finishing the work.
  - **`SUB-14 (NEU-978)`**, which will find this verdict uniform across its per-row application and should report it as a system fact rather than 45 separate ones.
  - **`SUB-10 (NEU-984)`**, which determines the exposure this creates in practice without changing the verdict.
  - **`NEU-986 (SUB-12)`**, at the completeness gate, as the package's own answer to whether the isolation outcome is achievable within the consumed placement alone.

### SUB-6

#### `F-S6-1` — The codebase states its own concurrency premise as single-writer, and nothing backs that premise up

- **Id:** `F-S6-1`
- **Finding:** Every durable learner-state write in this system is **read-modify-write** — the row is read, the new values are computed in application code, and the computed object is written back by a blind `UPDATE … WHERE id = ?` — and the codebase **says in its own words** that this is safe because there is one user: *"Note: TOCTOU — getChunk → validate → persist has a small race window. Acceptable for single-user MCP; the rowCount === 0 guard below catches concurrent deletes."* Nothing enforces that premise. There is **no optimistic concurrency control anywhere in `src/`** — zero `FOR UPDATE`, zero `pg_advisory`, zero `xmin`, and no compare-and-set predicate; the one `version`-shaped column, `contentVersion`, is written unconditionally and never appears in a `WHERE` clause. **No transaction sets an isolation level**, so Postgres's default READ COMMITTED applies everywhere. The premise is therefore already false for any horizontally-scaled MCP deployment — **before any web tier exists at all.**
- **Evidence:** Read on `origin/develop` at the 2026-08-21 cutoff. The comment: `src/orchestration/review-workflows.ts:190`–`:191`. The read-modify-write path: `src/orchestration/review-workflows.ts:35` (read), `:60`–`:89` (compute), `:99` (persist) → `src/adapters/drizzle/review-persistence-adapter.ts:78`–`:83`. The absent OCC: `contentVersion` declared at `src/infrastructure/db/schema.ts:70`, written unconditionally, never in a `WHERE`; repository `update()` calls filter on `eq(table.id, id)` alone. The absent isolation level: `src/infrastructure/db/operations.ts:21`–`:24` (`db.transaction(async tx => fn(tx))`, no options object) and `src/adapters/drizzle/unit-of-work-adapter.ts:9`–`:24`. **The pattern is available and simply not applied to learner state:** `src/adapters/drizzle/chunk-repository.ts:149`–`:160` merges JSONB in SQL with a comment saying it exists to avoid exactly this race, `:347` increments in SQL, and `src/adapters/drizzle/linter-validation-repository.ts:41`–`:49`, `:84`–`:97` use `ON CONFLICT DO UPDATE` — all on corpus and ordering data, never on learner state.
- **Consequence:** Two, and they point in different directions. **For the ownership comparison:** this baseline is common to all three candidate models, so `07_state-ownership-model-selection.md` §1.3 rule 1 deliberately **excludes it from every `C1` score** — including it would inflate all three equally and change no ordering, while making the scores look like a judgement about the codebase rather than about the models. It is recorded here instead, which is the point of a findings register. **For the system:** the premise is a latent correctness assumption with no owner. Any deployment that runs two MCP core replicas already violates it, and no artifact in this package establishes that the production deployment runs exactly one.
- **What is assumed rather than derived:** Nothing about the code — every claim above is a read at a cited line, and the absences were established by named searches whose zero results are stated as zero results. **Not** adjudicated: whether the production deployment is in fact single-replica today. That is `SUB-10 (NEU-984)`'s, and the finding is deliberately written so it does not depend on the answer — a premise that nothing enforces is a finding whether or not it currently holds.
- **Handed to:**
  - **`NEU-896`**, the implementation charter, which inherits a learner-state write path whose safety argument is a code comment.
  - **`SUB-10 (NEU-984)`**, which determines whether the deployment already violates the premise, and which owns `CAP-S6-1` — the observation this finding was **not** able to make.
  - **`NEU-986 (SUB-12)`**, at the completeness gate, as context for why `C1` was scored on additional rather than absolute exposure.

#### `F-S6-2` — `SC-S3-3` has no transactional envelope under any ownership model, because `reviewPersistence` is not in the unit of work

- **Id:** `F-S6-2`
- **Finding:** `UnitOfWorkPort`'s `TransactionPorts` exposes exactly three ports — `chunks`, `topics` and `sessions`. **`reviewPersistence` is absent.** The SM-2 scheduling read-compute-write of `SC-S3-3` therefore cannot be wrapped in the unit of work at all, and is not: it runs as two independent round trips outside any transaction. This is orthogonal to the ownership question — it is true under all-MCP, under a hybrid, and under any model this or a later comparison might select.
- **Evidence:** `src/ports/unit-of-work-port.ts:26`–`:28` (the `TransactionPorts` shape); `src/adapters/drizzle/unit-of-work-adapter.ts:9`–`:24` and `src/infrastructure/db/operations.ts:21`–`:24` (`withTx`); the unwrapped path at `src/orchestration/review-workflows.ts:35`, `:60`–`:89`, `:99`. Several write paths also bypass `UnitOfWorkPort` entirely and open their own transaction directly — `src/adapters/drizzle/session-repository.ts:50`–`:65` and `src/adapters/drizzle/session-question-repository.ts:79`–`:84`, `:210`–`:221` — so the port is not the only transaction boundary in the system either. Read at the 2026-08-21 cutoff.
- **Consequence:** `07_state-ownership-model-selection.md` §3.3 scores `C3` (recovery) at **4 rather than 5** for both `M-A` and `M-C` because of this: the property holds only while the single-writer condition is maintained, which is anchor 4, not anchor 5. More broadly, any downstream plan that treats "wrap it in the unit of work" as an available mitigation for a scheduling-state race must first **add the port**, which is a change to a port interface and therefore inside `../06_…md` §6's blast radius rather than a local fix.
- **What is assumed rather than derived:** Nothing. The port's member list is read at a cited line and the absence is an absence in a three-element type. **Not** adjudicated: whether `reviewPersistence` was omitted deliberately or by oversight — the finding records the state, not the intent.
- **Handed to:**
  - **`SUB-8 (NEU-981)`**, whose backward-compatibility audit covers the port surface and which would price the addition.
  - **`NEU-896`**, which would make it.

#### `F-S6-3` — The one-active-learning-session invariant is enforced only in application code, across two round trips, with nothing behind it

- **Id:** `F-S6-3`
- **Finding:** The rule that a learner has at most one active **learning session** is enforced by an application-code check that reads the active session and returns a `'conflict'` error if one exists — then, on a **separate** round trip and inside a **different** transaction, inserts the new session. The check and the insert are not in the same transaction. The `learning_sessions` table carries **no partial unique index** on `status = 'active'` and no equivalent constraint; its only database-level constraints are two CHECKs on `mode` and `status`. A second writer process racing that window creates two concurrently active learning sessions with **nothing in the database rejecting it**.
- **Evidence:** The check at `src/orchestration/session-workflows.ts:39`–`:46`; the insert at `:68`–`:78` via `src/adapters/drizzle/session-repository.ts:50`–`:65`. The table definition at `src/infrastructure/db/schema.ts:99`–`:124`, with the two CHECK constraints at `:118`–`:122` and no unique index over `status`. For contrast, the database schema **does** carry a real write-once guard elsewhere — `uniqueIndex('uq_session_question_attempts_question_number')` on `(sessionQuestionId, attemptNumber)` plus `check('chk_attempt_number', … IN (1,2))` at `src/infrastructure/db/schema.ts:225`–`:229` — which is what makes the omission here a gap rather than a house style. Read at the 2026-08-21 cutoff.
- **Consequence:** This is the **concrete mechanism** behind `M-B`'s `C1` score of 1 in `07_state-ownership-model-selection.md` §3.3 — the model is not scored down on a general principle about two writers, but on a specific unguarded window in this codebase. It is also the sharpest illustration of `F-S6-1`'s premise: the invariant is real, it is relied on, and its only enforcement is that historically there has been one writer.
- **What is assumed rather than derived:** Nothing about the code. **Not** derived: whether a duplicate active learning session would actually corrupt downstream behaviour or merely produce a confusing state — that depends on consumers this finding did not trace, and `CAP-S6-1` records that no live observation was possible.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which assigns authority for `SC-S3-5` (the learning-session row) and should know that its uniqueness is not database-enforced.
  - **`NEU-896`**, for which a partial unique index is the obvious and cheap repair.

#### `F-S6-4` — The latency argument for a hybrid does not survive measurement

- **Id:** `F-S6-4`
- **Finding:** The standing intuitive case against an all-MCP model is that routing every learner-path read through an MCP tool call cannot meet `A-25`'s sub-second budget. Measured, the MCP tool boundary — JSON-RPC framing, transport dispatch, tool input schema validation and result-envelope construction — costs **p95 0.1892 ms** for a 714-byte tutoring-state read and **p95 0.0631 ms** for a 29,715-byte `LearnerContext` aggregate, against a direct in-process baseline in the same run. That is **at most 0.02% of a 1000 ms budget**, and the relative overhead *narrows* as payloads grow. Criterion `C5` consequently scores 4 for all three models and **discriminates between none of them**.
- **Evidence:** `92_spike-register.md` `### SUB-6`, `SPK-S6-1` — 2000 measured iterations per arm after 200 warm-up, two payload sizes, real MCP `Client`/`Server` pair over the SDK's `InMemoryTransport`, Node **v22.23.1**, `@modelcontextprotocol/sdk` **1.27.1**. The full numbers are stated in that record; they are not restated here because the record is the source of truth for them. The claim could not have been read: `93_stand-in-assumption-register.md`'s `A-25` states a requirement and names an invalidating outcome but records no figure, and the upstream SDK publishes no latency characteristic at that version.
- **Consequence:** An argument that has been used to justify a hybrid is removed from the comparison — not weakened, removed: it is worth **zero** points of differential. Downstream sub-tasks and charters should stop treating MCP-boundary latency as a reason to give the web tier write authority. Two residuals are **not** removed and are stated in `SPK-S6-1` rather than absorbed: the measurement excludes any network hop, so it is a floor rather than a prediction; and it is per call, so an N+1 access pattern across the boundary is unbounded by it.
- **What is assumed rather than derived:** That the harness's stand-in payloads are representative in **size** of `SC-S3-42` and `SC-S3-29` reads. Both categories have store `none` or are derived-on-read, so no real payload exists to sample — the sizes are constructed from the field lists `04_state-category-inventory.md` §3 gives for each. The conclusion is insensitive to the assumption: the larger payload showed *less* relative overhead, so a mis-sized stand-in in either direction leaves the three-orders-of-magnitude margin intact.
- **Handed to:**
  - **`SUB-7 (NEU-980)`**, which designs the web API's access patterns and owns the per-call residual — the boundary is cheap, but *k* crossings cost *k* times cheap.
  - **`SUB-10 (NEU-984)`**, which owns the network-hop residual as part of deployment topology.
  - **`NEU-986 (SUB-12)`**, at the completeness gate, as an instance of a spike that changed a criterion's weight in effect without changing it on paper.

#### `F-S6-5` — `92_spike-register.md` §10 announces that it holds no spike results while holding two

- **Id:** `F-S6-5`
- **Finding:** `92_spike-register.md` §10 opens with **"None. This register holds no spike results."** followed by *"SUB-1 set the rules above and ran no spike under them."* That was true when SUB-1 wrote it. It stopped being true when SUB-2 appended `SPK-S2-1` below the section's own append marker, and it is now wrong twice over with `SPK-S6-1` appended by this sub-task. A reader who stops at the section heading concludes the register is empty and does not scroll past the HTML append comment to the `### SUB-<n>` sections that follow.
- **Evidence:** `92_spike-register.md:134` (the "None" line) and `:136` (the SUB-1 sentence), against `:144` (`### SUB-2`) and the `### SUB-6` section this sub-task appended below it. Read on `origin/develop` at the 2026-08-21 cutoff, after this sub-task's own append.
- **Consequence:** **Nothing is corrected in place.** `92_spike-register.md`'s sections above `### SUB-6` are SUB-1's, the register is append-only, and no sub-task rewrites another's text — so the correction is this finding, exactly as `F-S4-2` corrected `04_state-category-inventory.md`'s stale "41 entries" heading and `F-S5-3` corrected `01_outcome-register.md`'s stale tool counts. **The practical rule for every reader of `92_…`: the `### SUB-<n>` sections are the records; §10's opening sentence is not.** The rule the section states about *appending* remains correct and is unaffected.
- **What is assumed rather than derived:** Nothing. Both the stale sentence and the records that falsify it are read at cited lines in one file. **Not** adjudicated: whether SUB-2 should have edited the line when it appended — the append convention at `../92_…md` §7 says it should not have, and this finding agrees with that convention rather than second-guessing it.
- **Handed to:**
  - **`NEU-985 (SUB-11)`**, whose cross-cutting audit can check announced-versus-actual register contents mechanically, and for which this is the third instance of the same class of defect in this package.
  - **`NEU-986 (SUB-12)`**, at the completeness gate, which reconciles the registers and is the only party that could correct the line without violating the append convention.

### SUB-13

#### `F-S13-1` — The assignment rule has no clause for a row whose sole writer is already fixed by a merged predecessor contract

- **Id:** `F-S13-1`
- **Finding:** `../07_…md` §6.1's six clauses key on the row's own inventory cells plus a `../05_…md` gate lookup. **None of them keys on "a merged predecessor already specifies this category's only writer."** For the two citation-drift categories that produces an authority the predecessor forbids exercising. `SC-S3-33` (the cached verdict) matches **clause 2** — `../05_…md` §7.3's four-row disposition reads the cache on every content serve — and clause 2's named authority on the request path is `CMP-S4-7`. `SC-S3-34` (the verdict store) matches no clause before **clause 6**, whose default is also `CMP-S4-7`. But `../03_…md` §4.2 specifies the store as written **only** by the producer; §4.3 specifies the cache as internal, keyed-read-only, computing nothing, and states it "never derives, refreshes or ages" a verdict; and `../05_…md` `FL-S4-13` names **`CMP-S4-17` the cache's only writer**. A rule output requiring `CMP-S4-7` to write either store contradicts three merged statements.
- **Evidence:** The rule at `07_state-ownership-model-selection.md` §6.1 (clauses 1–6 and tie-breaks (a)–(d)). The contradicted specifications at `03_execution-environment-and-citation-drift-component.md` §4.2 (the producer writes exactly one tuple; corpus walk prohibited; it is the system's only component with egress outside operator control) and §4.3 (the cache is internal, has no egress, computes nothing). The placement at `05_system-context-and-responsibility-boundaries.md` §5 `FL-S4-13` (authoritative side `CMP-S4-17`, "the cache's only writer"), §7.3 hop 3 (`CMP-S4-18` authoritative for the stored verdict and its date; `CMP-S4-16` for what to do about it), and §3.2 (`CMP-S4-18` holds `SC-S3-33`). Resolved in `08_per-state-authority-matrix.md` §8.6.
- **Consequence:** Both rows still carry **exactly one** authority — `CMP-S4-17` — reached through the rule's own **tie-break (c)**, "the component that produces the row wins". Reaching (c) required reading tie-break (b) — "out of domain but read on a gate path → core wins" — **narrowly**, as adjudicating core-versus-web candidacy rather than core-versus-anything: it is the companion to clause 2's "the web tier never holds gate authority", and `CMP-S4-17` is a `Z-CONT` producer, neither core nor web. That reading is disclosed at the row rather than assumed, precisely because it is the load-bearing step. **The rule was not patched locally.** If SUB-6 intends tie-break (b) to be read broadly, the rule as published assigns `CMP-S4-7` an unexercisable authority over two categories and needs a seventh clause or an amended (b) — which is a change to the rule, not to this matrix.
- **What is assumed rather than derived:** Nothing about the inputs — every clause, tie-break and specification above is read at a cited section in a merged artifact. **Assumed rather than derived:** the narrow reading of tie-break (b). SUB-6 did not state which reading it intended, and the finding exists so that the choice is visible and overturnable rather than buried in an assignment.
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, which owns the rule and is the only party that can say which reading of tie-break (b) it meant, and whether a clause keying on a fixed sole writer belongs in §6.1.
  - **`SUB-14 (NEU-978)`**, which validates the matrix and should attack these two rows first — they are the two whose reasoning is least mechanical.

#### `F-S13-2` — Clause 4 names `CMP-S4-2` as `Z-IDP`'s component; `../05_…md` places `CMP-S4-2` in `Z-EXT`

- **Id:** `F-S13-2`
- **Finding:** `../07_…md` §6.1 clause 4 reads that a `SC-S3-45`-class row is *"authored in `Z-IDP` (`CMP-S4-2`)"*. **The zone and the component id do not denote the same thing.** `../05_…md` §3.1 places **`CMP-S4-2` in `Z-EXT`** — it is the external MCP client, a third party outside the trust boundary — and names **`CMP-S4-10`** (the identity provider) as `Z-IDP`'s only component. Applied to the literal id, clause 4 assigns the learner-identity → owner mapping to a third party.
- **Evidence:** The clause at `07_state-ownership-model-selection.md` §6.1 cl. 4. The zone membership at `05_system-context-and-responsibility-boundaries.md` §3.1 (zones) and §3.2 (`CMP-S4-2` = external MCP client, `Z-EXT`; `CMP-S4-10` = identity provider, `Z-IDP`). The category at `04_state-category-inventory.md:205` (`SC-S3-45`, store `none`, `assumed` under `A-28`). Resolved in `08_per-state-authority-matrix.md` §8.7.
- **Consequence:** Read literally, clause 4 produces **`A-29`'s invalidating outcome arriving by a different door** — an external client holding write authority over a state category, which `A-29` states no isolation invariant under `OUT-4` can then enforce. The matrix records the authority for **the zone the clause names**, resolved to that zone's actual component, `CMP-S4-10`, and routes the id error here. Exactly one authority is recorded and the reasoning is on the page, so SUB-14 or SUB-16 can overturn it if SUB-6 meant something else. **Nothing in `../07_…md` is edited** — the registers and the merged chapters are append-only, and a sibling's clause is not this sub-task's to rewrite.
- **What is assumed rather than derived:** That SUB-6 meant the **zone** rather than the id — inferred from the clause's own construction (it names the zone first and parenthesises the component) and from the fact that the literal reading contradicts a stand-in the same package published. **Not** established: whether the id is a transposition of `CMP-S4-10` or a different error. The finding records the mismatch, not its cause.
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, which owns clause 4.
  - **`SUB-14 (NEU-978)`**, which validates `SC-S3-45`'s assignment.
  - **`NEU-985 (SUB-11)`**, whose cross-cutting audit can check mechanically that every `CMP-S4-*` id cited outside `../05_…md` resolves to the zone the citing sentence claims for it. This is the second id-mismatch class defect in the package after `F-S3-2`, and the first that is not a tracker id.

#### `F-S13-3` — `OI-S3-1`'s closure condition is unsatisfiable by any document that respects `../04_…md` §6

- **Id:** `F-S13-3`
- **Finding:** `OI-S3-1` closes only when a resolved learner-scoping value exists for every `SC-S3-*` row, and states that *"a matrix that carries the column forward still marked `question — open` does not close it."* But `../04_…md` §2 defines the `Learner-scoped` column as recording **the scoping question, never a schema fact**, and §6 establishes by search that **no ownership column exists on any table today** — `user_id`, `userId`, `learner_id` and `learnerId` return **zero matches** across all twelve Drizzle tables and both raw-SQL log tables. NEU-850's `OUT-2` is a **decision to honour, not an existing fact**. **A document that resolved the column would be asserting a schema fact the package forbids it to assert.** The condition therefore cannot be met by SUB-13, by SUB-14, or by the two together — not for want of effort, but because meeting it requires the schema change first.
- **Evidence:** `90_open-items-and-provisional-register.md` `OI-S3-1` (consumers SUB-13 and SUB-14; owner SUB-13; the quoted closure sentence). `04_state-category-inventory.md` §2 (the column's definition), §6 (the four-term search and its zero results; *"This document states the question and assigns nothing"*). `../06_…md` §3.3, which builds on the same reading: `question — open` is **in domain**, so the 18 open rows are "questions to answer, never exemptions". Read at the 2026-08-21 cutoff.
- **Consequence:** `OI-S3-1` is **half-discharged and stays open**, and the half that is discharged is the useful one: every row's **isolation-domain membership** and hence its clause is settled, and clause 5's "only an explicit `no` leaves the domain" means an unresolved question is treated conservatively rather than optimistically — 20 rows reach clause 5, reconciling exactly with `../06_…md` §3.3's independent census of 26 in-domain categories. What remains is a schema question that no C010 sub-task performs. **The risk this finding exists to prevent is a later reader treating the item's non-closure as SUB-13 having skipped it**, and then closing it by asserting the values — which would put a false schema fact into the package's most-consumed matrix.
- **What is assumed rather than derived:** Nothing. The closure condition, the column definition and the zero-result search are each read at a cited location. **Not** adjudicated: whether `OI-S3-1` was written before `../04_…md` §6's search was run, which would explain the condition without excusing it.
- **Handed to:**
  - **`NEU-986 (SUB-12)`**, at the completeness gate, which reconciles the registers and is the only party that can restate a closure condition without violating the append convention.
  - **`SUB-14 (NEU-978)`**, the item's co-consumer, which will hit the same wall and should not spend effort on it.

#### `F-S13-4` — The charter and tracker size this sub-task at 25–30 categories; the merged inventory publishes 45

- **Id:** `F-S13-4`
- **Finding:** The C010 charter and the NEU-977 tracker item size this work at *"roughly 25–30 state categories"* and *"250–300 authored cells"*. `../04_…md` publishes **45**, so the real deliverable is **45 rows × 10 cells = 450 authored cells** — **50–80% more than the estimate**. This is a stale *charter* sizing, and is **distinct from `F-S4-2`**, which records that `../04_…md`'s own §3 heading says "41 entries" while its body and its §8 counts table both carry 45. Two different documents are stale about two different numbers, and citing `F-S4-2` alone would leave the charter's estimate uncorrected.
- **Evidence:** `04_state-category-inventory.md` §8 (counts: 30 `existing` + 11 `required-by-upstream` + 4 `assumed` = 45) and §9 (*"receives **45 rows**"*), against the charter's and tracker's sizing. Verified mechanically at the 2026-08-21 cutoff by extracting every `SC-S3-<n>` token from `../04_…md` and filtering to the strictly numeric form: **45 distinct ids, minimum 1, maximum 45, no gaps**. Note for anyone re-running it: §2 contains the bare template token `SC-S3-<k>`, which a naive `SC-S3-` extraction picks up as a valueless match and which corrupts a numeric sort.
- **Consequence:** **The inventory was not trimmed to fit the estimate** — `../08_…md` authors all 45 rows. Recorded because a stale sizing that goes unrecorded invites exactly the wrong repair: a later reader comparing the matrix against the charter would see 45 rows where 25–30 were scoped and could read the surplus as scope creep rather than as the charter being behind its own inventory. The same estimate appears in downstream sizing for SUB-14 (which validates all 45) and SUB-16 (which republishes them), so the correction is worth carrying forward rather than absorbing here.
- **What is assumed rather than derived:** Nothing. Both numbers are read at cited locations and the 45 is independently re-derived by a mechanical extraction whose method is stated above so it can be repeated.
- **Handed to:**
  - **`NEU-986 (SUB-12)`**, at the completeness gate, which reconciles the package against the charter and is where a stale charter figure should be recorded.
  - **`SUB-14 (NEU-978)`** and **SUB-16**, both of which iterate the same 45 rows and inherit the same estimate.

### SUB-14

*Eleven findings from validating `08_per-state-authority-matrix.md` at its `pre-validation` revision. **Every one names an owning sub-task.** The headline is stated once, here, so it is not lost among them: **zero of the eleven is a defect in SUB-13's authority assignment.** The invariant's frontier is the transport, the absent principal-attribution mechanism and the absent access-path enumeration — exactly what `F-S5-4` recorded before this validation ran. No routable defect was invented to make the census look productive.*

#### `F-S14-1` — `I3`'s "at or below the port boundary" is unsatisfiable as written for the 15 categories that sit behind no port

- **Id:** `F-S14-1`
- **Finding:** `06_isolation-invariant-and-the-neu-893-split.md` §3 phrases `I3` as confinement enforced **"at or below the port boundary"**. **Fifteen categories have no port between the caller and the state** — the 13 clause-1 process-local rows (`SC-S3-18`…`SC-S3-30` less the two derived-in-core rows, plus `SC-S3-21`/`SC-S3-27`), and the two raw-SQL log tables `SC-S3-16`/`SC-S3-17` that `OI-S5-1` already records as sitting behind no port. For these, the check's locus does not exist, so `I3` is not *failed* — it is **unaskable as written**. This chapter read the clause **purposively**: §3.6 case 3 introduces it to exclude guards sitting *above* the ports that a port-scoping mechanism would leave untouched, and where no port mediates the category there is no port-scoping mechanism to miss it. **The ruling is disclosed here rather than made silently in a verdict cell.**
- **Evidence:** `06_…md` §3 (`I3`'s statement), §3.4.1 (the asymmetry rule), §3.6 case 3 (the worked case the clause is drawn from), and §3.5's ordering→owner table placing `I3` with SUB-8 (NEU-981). `OI-S5-1` in `90_open-items-and-provisional-register.md` (the two log tables sit behind no port, so `OUT-2`'s mechanism cannot reach them). The 15 portless categories are identified in `09_authority-matrix-validation.md` §4.3.
- **Consequence:** **None of the 15 changes disposition under either reading** — each fails at `I2` or `I3` regardless, in both censuses — so the ruling moves no verdict and inflates no count. It is recorded because a later reader applying `I3` literally to a portless category would either invent a sixth outcome or silently return `holds`, and §3.4.1 forbids the second absolutely. What the finding asks for is a **stated rule in the procedure**, not a repair to any row.
- **What is assumed rather than derived:** The purposive reading itself. SUB-5 did not state what `I3` means for a category with no port, because its five worked cases do not include one. The reading is inferred from the clause's own worked origin and is offered to be overturned, not relied upon.
- **Handed to:**
  - **`SUB-5 (NEU-975)` / `NEU-893`**, which own the procedure and are the only parties that can add the rule to `I3`.
  - **`SUB-16 (NEU-980)`**, to disposition the reading when it absorbs this validation.

#### `F-S14-2` — `fails-principal: 0` is a consequence of the adjudication order, not a clean bill on principal integrity

- **Id:** `F-S14-2`
- **Finding:** Both censuses return **zero** `fails-principal` verdicts. That is **not** evidence that principal integrity is sound. `06_…md` §3's adjudication rule is *"the first failing check names the verdict, stop there"*, and `I5` is **last**. Every in-domain row fails at `I2`, `I3` or `I4` first, so **`I5` is never reached and principal integrity is never actually tested by this census.** Meanwhile the defect `I5` exists to catch is real and merged: `06_…md` §3.6 case 5 records `jwt-middleware.ts:127`'s `const subject = (typeof payload.sub === 'string' && payload.sub) || azp || undefined;` — an `azp` fallback that lets a client-credentials token resolve to a principal. **A reader who takes `fails-principal: 0` at face value would conclude the opposite of the truth.**
- **Evidence:** `06_…md` §3.4 (the closed six-verdict set), §3's ordered checks and the first-failing-check rule, §3.6 case 5 (the `sub`-vs-`azp` case reaching `fails-principal` **only because** that worked case's earlier checks were assumed satisfied), §3.5 (`I5` → NEU-893 against `OI-S1-2`). The distributions in `09_authority-matrix-validation.md` §15.2.
- **Consequence:** Any downstream audit or gate that consumes the verdict distribution must read `fails-principal: 0` as **"unreached"**, never as **"passed"**. This is the single most misreadable number in the record, which is why it is a finding rather than a footnote. It also means the `I5` workload is **not** sized by this chapter: the count of rows that *would* fail `I5` is unknown and will stay unknown until `I2`–`I4` are satisfiable.
- **What is assumed rather than derived:** Nothing. The order, the rule and the census are each read or computed at a cited location.
- **Handed to:**
  - **`SUB-16 (NEU-980)`**, to carry the qualification into the republished matrix rather than the bare count.
  - **`NEU-983 (SUB-11)`**, whose audits consume the distribution and are the most likely to misread it.

#### `F-S14-3` — `SC-S3-45` is structurally incapable of reaching `holds` under any target state, because its authority is external

- **Id:** `F-S14-3`
- **Finding:** `08_…md` assigns `SC-S3-45` (learner-identity → owner mapping) to **`CMP-S4-10`**, the identity provider — a component in `Z-IDP`, outside this system. `I3` requires an **enumerated access-path set covering reads and writes** for the category (`06_…md` §3.4.1). **No such set can be enumerated for a store this system does not own**, in any target state, including one that assumes `OUT-2` fully implemented — `OUT-2` adds ownership keys to *this system's* tables. So `SC-S3-45` returns `fails-confinement` **permanently**, not pending a change. This is not an assignment defect: the assignment is correct and follows `07_…md` §6.1 clause 4 (as resolved by `F-S13-2`). It is a **consequence of the model** that the row's own verdict can never improve.
- **Evidence:** `08_…md` §9 row `SC-S3-45` (authority `CMP-S4-10`, clause 4, `assumed` under `A-28`). `07_…md` §6.1 clause 4. `F-S13-2` (the `Z-IDP`/`CMP-S4-2` id mismatch and its resolution to `CMP-S4-10`). `06_…md` §3.4.1. `A-28` in `93_stand-in-assumption-register.md` — *"isolation enforced server-side at or below the port boundary"* — whose **tolerance envelope** covers a server-side mechanism and whose **invalidating outcome** is enforcement that is not server-side; an external authority is outside that envelope in both directions, which is why the row cannot be rescued by assuming `A-28` holds.
- **Consequence:** The category's isolation guarantee is **inherited, not enforced** — this system can only fail closed on a projection failure, which is exactly what its Recovery cell requires. Recorded so that a later reader does not schedule work to "fix" `SC-S3-45`'s verdict; there is no local work that changes it. The row's **one** local obligation (fail closed rather than default to an unowned row) is separately verifiable and is not covered by this finding.
- **What is assumed rather than derived:** That clause 4's intended authority is `CMP-S4-10` — inherited from `F-S13-2`'s resolution, not re-derived here.
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, which owns clause 4 and is the only party that can decide whether a row with an external authority should be in the invariant's domain at all.
  - **`SUB-16 (NEU-980)`**, to disposition.

#### `F-S14-4` — `SC-S3-42`'s divergence outcome is undefined: the hint-usage/mastery rule exists in no merged input

- **Id:** `F-S14-4`
- **Finding:** The divergence walk (`09_…md` §8) reaches a **defined** outcome for 44 of 45 rows. `SC-S3-42` (tutoring / hint interaction state) does not. Its Consistency cell requires the hint state and its attempt (`SC-S3-9`) to be *"attributable to each other, or hint usage cannot be excluded from — or included in — a mastery judgement"*, and then states that **"which of those it is, is NEU-891's decision, not this matrix's."** The two candidate resolutions produce **opposite** mastery verdicts for the same divergence, and no merged artifact picks between them.
- **Evidence:** `08_…md` §8's `SC-S3-42` block (Consistency and Freshness cells, quoted above). `A-25` in `93_…md` — the sub-second read-latency stand-in for tutoring state — whose **tolerance envelope** explicitly admits *"any number of escalation levels"* and any granularity, so **no point inside the envelope resolves this**; and whose **invalidating outcome** (synchronous multi-turn AI orchestration inside a gate-bearing write path) would make it worse, by placing the undetermined interaction inside the mastery transaction. Cited here at the verdict it decided.
- **Consequence:** `OUT-3` requires a defined scenario outcome per row and this one is not defined **yet**. It is routed, not narrated. **This is not a matrix defect**: SUB-13 recorded the indeterminacy honestly and named the deciding party, which is the correct behaviour for a cell that cannot be answered from merged inputs. A matrix that guessed would be worse.
- **What is assumed rather than derived:** Nothing. The indeterminacy is quoted from the row.
- **Handed to:**
  - **`NEU-891`**, which owns the hint model and is the only party that can state the inclusion rule.
  - **`SUB-16 (NEU-980)`**, to carry the residual at the row when it republishes.

#### `F-S14-5` — `SC-S3-31`'s conflicting-write outcome is undefined: aggregate-vs-append is a store-shape decision no merged artifact makes

- **Id:** `F-S14-5`
- **Finding:** The conflicting-concurrent-write walk (`09_…md` §9) reaches a **defined** outcome for 44 of 45 rows. `SC-S3-31` (corpus-neutral assessment-evidence record) does not. Its Concurrency cell reads: *"Two assessments of the same `node_id` + `skill_type` must serialize **if the record is an aggregate**; append-per-event has **no race**. Which of the two shapes applies is not determined by any merged input — SUB-10 decides it with the store."* The two shapes give **opposite** outcomes: a mandatory serialization requirement versus no race at all.
- **Evidence:** `08_…md` §8's `SC-S3-31` block. `04_state-category-inventory.md` §3 row `SC-S3-31` (identity `node_id` + `skill_type`; store `none`; `required-by-upstream`). `CAP-S6-1`, which bounds what may be said about two-writer behaviour at all. `OI-S13-1`, the adjacent open store-shape question, same owner.
- **Consequence:** Material, because `SC-S3-31` is the load-bearing row of NEU-890's durability property. **Checked deliberately: the durability property survives either resolution** (`09_…md` §13.2), so the indeterminacy does not propagate into `SC-S3-38`/`SC-S3-39`. What it does block is a definite statement of the row's concurrency obligation, which SUB-8's confinement work and SUB-10's topology work both need.
- **What is assumed rather than derived:** Nothing. The indeterminacy is quoted from the row; the durability-property robustness is derived in `09_…md` §13.
- **Handed to:**
  - **`SUB-10 (NEU-984)`**, which selects the data-store topology under `OUT-8` and is named in the cell itself.
  - **`SUB-16 (NEU-980)`**, to disposition and carry the residual.

#### `F-S14-6` — `FL-S4-2` names `CMP-S4-3` authoritative; the matrix gives `CMP-S4-3` authority over zero of 45 rows

- **Id:** `F-S14-6`
- **Finding:** `05_system-context-and-responsibility-boundaries.md` §5 `FL-S4-2` names **`CMP-S4-3`** (the web tier) the authoritative side for *"learner-facing content and derived state for display"*. `08_…md`'s authority distribution gives `CMP-S4-3` **zero rows out of 45**. **Direction: the flow names a component the matrix holds empty.** This is not an oversight — it is the direct consequence of `07_…md` §6.3's presentation-exception list (clause 3's enumerated set) being **empty under the selected model `M-A`**, which is precisely the property distinguishing `M-A` from `M-B`.
- **Evidence:** `05_…md` §5 `FL-S4-2`. `08_…md` §11's authority distribution (`CMP-S4-9` 21, `CMP-S4-7` 9, `CMP-S4-4` 5, `CMP-S4-14` 4, `CMP-S4-17` 2, `CMP-S4-19` 2, `CMP-S4-8` 1, `CMP-S4-10` 1 — no `CMP-S4-3`) and §11's clause distribution showing **clause 3 → 0 rows**. `07_…md` §6.3 (the empty list) and §6.1 clause 3.
- **Consequence:** The matrix's answer is `M-A`'s answer and is correct. The flow's annotation describes a **display relationship**, not a state authority. Recorded because the reading is not self-evident: a consumer who assumes `CMP-S4-3` holds *something* would mis-scope any web-tier work, and the clause-3-zero property is exactly the kind of structural result that gets quietly re-litigated. **Expected disposition: record the reading, amend nothing.**
- **What is assumed rather than derived:** Nothing. Both counts are read at cited locations.
- **Handed to:** **`SUB-16 (NEU-980)`**.

#### `F-S14-7` — `FL-S4-4` and `SC-S3-26` disagree about JWKS, and the matrix answers it the opposite way from `SC-S3-45`

- **Id:** `F-S14-7`
- **Finding:** `FL-S4-4` names **`CMP-S4-10`** authoritative for the JWKS, *"and only for signature material under the issuer allowlist"*. `08_…md` assigns `SC-S3-26` (the JWKS remote key set) to **`CMP-S4-4`** on clause 1 (non-durable → the component whose process computes it). **Direction: the flow names the upstream issuer; the matrix names the downstream in-process cache holder.** The substance is not either assignment in isolation — it is that **the matrix answers the same structural question the opposite way at `SC-S3-45`**, where clause 4 *did* yield to the external authority `CMP-S4-10` for a category this system merely projects. `SC-S3-26` and `SC-S3-45` are the only two rows where the question arises, and they are answered inconsistently.
- **Evidence:** `05_…md` §5 `FL-S4-4`. `08_…md` §9 rows `SC-S3-26` (`CMP-S4-4`, clause 1) and `SC-S3-45` (`CMP-S4-10`, clause 4). `08_…md` §8's `SC-S3-26` block, whose Freshness cell states *"the issuer's set wins… the refresh policy is `Z-IDP`'s"* — the flow's answer, inside a row assigned to `CMP-S4-4`. `07_…md` §6.1 clauses 1 and 4.
- **Consequence:** A consumer reasoning about **projected external state** gets different guidance depending on which row it reads. The isolation consequence is nil either way — neither assignment changes `SC-S3-26`'s verdict — so this is a **modelling-consistency** finding, not a security one. The disposition SUB-16 must make is whether clause 1 should yield to an external authority when the cached thing is a projection of it.
- **What is assumed rather than derived:** That `FL-S4-4` and `SC-S3-26` are about the same value. They are: the flow's value is the key set, and the category is the key set as held in process.
- **Handed to:** **`SUB-16 (NEU-980)`**, with **`SUB-6 (NEU-976)`** named for the clause question.

#### `F-S14-8` — `FL-S4-8`/`FL-S4-9` and `FL-S4-20` contradict each other about the two log tables, inside one merged document

- **Id:** `F-S14-8`
- **Finding:** `FL-S4-8` and `FL-S4-9` name **`CMP-S4-19`** authoritative for what is stored in `SC-S3-16` and `SC-S3-17`. **`FL-S4-20`, in the same section of the same document, names `CMP-S4-9` authoritative "for the source rows"** — and the source rows *are* `SC-S3-16` and `SC-S3-17`. `08_…md` assigns both categories to **`CMP-S4-9`** with `CMP-S4-19` as the `W` annotation, **agreeing with `FL-S4-20`**. **Direction: `FL-S4-8`/`FL-S4-9` name the writer; the matrix and `FL-S4-20` name the store.** So the primary defect is **internal to `05_…md`**, and the matrix merely exposes it.
- **Evidence:** `05_…md` §5 `FL-S4-8`, `FL-S4-9`, `FL-S4-20`. `08_…md` §9 rows `SC-S3-16`, `SC-S3-17` (authority `CMP-S4-9`, `W` `CMP-S4-19`, clause 5). `05_…md` §3.2's own statement that `CMP-S4-9` is *"the only writer of the `public` and `infrastructure` database schemas **on the request path**"* — while these tables are written from **transport worker threads**, which is not the request path, so the matrix's assignment stretches `CMP-S4-9`'s stated charter and `FL-S4-8`/`FL-S4-9`'s does not. `OI-S5-1` (both tables sit behind no port).
- **Consequence:** Whichever component is named, **the isolation consequence is identical** — `OUT-2`'s mechanism cannot reach either table under either assignment (`OI-S5-1`) — so this is a modelling finding, not a security one. But it is the cross-check's strongest, because it is the only place where a merged artifact contradicts **itself** about a category, and because `SC-S3-17` is **read by the Tier-2 blocking gate**: a reader who has to guess which flow is authoritative is guessing about a gate input.
- **What is assumed rather than derived:** That `FL-S4-20`'s *"source rows"* denotes `SC-S3-16`/`SC-S3-17`. It does — the flow's own value column reads *"Batch read of `SC-S3-16`/`SC-S3-17` under an allowlist"*.
- **Handed to:**
  - **`SUB-16 (NEU-980)`**, which republishes and is the only party that can amend `05_…md` without violating the append convention. **This chapter amended neither artifact.**
  - **`NEU-983 (SUB-11)`**, whose cross-cutting audit can check flow-vs-flow consistency mechanically; this is the first intra-document authority contradiction found in the package.

#### `F-S14-9` — `FL-S4-14` names the cache's holder authoritative; `FL-S4-13` and the matrix name its producer

- **Id:** `F-S14-9`
- **Finding:** `FL-S4-14` names **`CMP-S4-18`** authoritative *"for the stored verdict and its date"*. `08_…md` assigns `SC-S3-33` (the cached citation-drift verdict) to **`CMP-S4-17`**, with `CMP-S4-18` demoted to *holder*. **Direction: the flow names the holder; the matrix names the producer.** And `FL-S4-13` — same document, same section — already calls **`CMP-S4-17` the cache's *only* writer**, so the matrix is the reading consistent with `05_…md`'s own §5 and `FL-S4-14` is the outlier.
- **Evidence:** `05_…md` §5 `FL-S4-13`, `FL-S4-14`, and §7.3 hop 3. `08_…md` §8.6 and §9 row `SC-S3-33` (`CMP-S4-17`, clause 2 → tie-break (c)). `03_execution-environment-and-citation-drift-component.md` §4.3 (the cache is internal, keyed-read-only, computes nothing, *"never derives, refreshes or ages"* a verdict). `F-S13-1`, which is the same tension seen from the assignment side.
- **Consequence:** Concrete if left unreconciled: a reader taking `FL-S4-14` at face value concludes the cache may be written by whoever holds it — **exactly the property `FL-S4-13`'s single-writer rule exists to deny**, and the property that keeps the drift verdict's provenance intact. Note this is the second finding (with `F-S14-8`) where `05_…md` §5 disagrees with itself; the class is worth a mechanical check rather than two point fixes.
- **What is assumed rather than derived:** Nothing.
- **Handed to:** **`SUB-16 (NEU-980)`**, with **`NEU-983 (SUB-11)`** for the mechanical class check.

#### `F-S14-10` — `FL-S4-16` still records "Undetermined" for a question the matrix has since resolved

- **Id:** `F-S14-10`
- **Finding:** `05_…md` §5 `FL-S4-16` records the gate-verdict authority as **"Undetermined"**, citing `OI-S2-2` as open and owned by SUB-13, and filing `F-S4-3`. **`08_…md` resolves it**: `SC-S3-35` → **`CMP-S4-14`**, clause 2, with `CMP-S4-15` as `W`; SUB-13's §12 records this as discharging `F-S4-3` and closing `OI-S2-2`. **Direction: matrix → flow.** `05_…md` is stale on this one row.
- **Evidence:** `05_…md` §5 `FL-S4-16` and §7.2 hop F1-5. `08_…md` §9 row `SC-S3-35` and §12's disposition of `F-S4-3`/`OI-S2-2`. `90_open-items-and-provisional-register.md`'s SUB-13 section.
- **Consequence:** Low severity and high nuisance: the flow document was **correct when written** — it named the party that would resolve it and refused to narrate past the gap, which is the behaviour the package wants. Recorded so the amendment is **owned and scheduled** rather than left as a silent inconsistency that a later reader resolves by assuming the older document is authoritative.
- **What is assumed rather than derived:** Nothing.
- **Handed to:** **`SUB-16 (NEU-980)`**, which republishes.

#### `F-S14-11` — Five flows name an enforcing, admitting or producing component where the matrix names the holding one; the two documents lack a stated vocabulary mapping

- **Id:** `F-S14-11`
- **Finding:** `FL-S4-5`, `FL-S4-17`, `FL-S4-19`, `FL-S4-20` (its extract half) and `FL-S4-21` each name a component the matrix records in its **`W`** annotation rather than as the authority. **These are not disagreements about fact.** `05_…md` defines "authoritative side" as *"if the two sides disagree about this value, whose value is the fact"* — on an enforcement or admission hop that is the enforcing component. `08_…md` defines "authority" as *"the single writer of the category"* and states that `W` *"is an annotation, never a second authority"*. Both are correct in their own document; **what is missing is a stated mapping between the two vocabularies.**
- **Evidence:** `05_…md` §5 `FL-S4-5` (`CMP-S4-4`), `FL-S4-17` (`CMP-S4-13`), `FL-S4-19` (`CMP-S4-4`), `FL-S4-20` (`CMP-S4-20`), `FL-S4-21` (`CMP-S4-13`), against `08_…md` §9 rows `SC-S3-13`, `SC-S3-1`/`2`/`32`, `SC-S3-44`, `SC-S3-41`, `SC-S3-37` — in every case the flow's component is the matrix's `W`, and in two cases (`SC-S3-13`, `SC-S3-44`) the matrix's own `W` cell literally reads *"enforced at `CMP-S4-4`"*. The full comparison is `09_authority-matrix-validation.md` §12.2.
- **Consequence:** **None of the five threatens the exactly-one-authority audit**, and none is a defect in either artifact. The risk is a mechanical cross-check by a later audit reporting five false positives, or a consumer treating `W` as a second authority. **Recommendation carried with the finding: publish the vocabulary mapping; amend neither artifact.** This chapter amended neither.
- **What is assumed rather than derived:** Nothing — each pairing is read at a cited location in both documents.
- **Handed to:** **`SUB-16 (NEU-980)`**, with **`NEU-983 (SUB-11)`** so its mechanical audits do not re-report these five.

### SUB-16

*Four findings from dispositioning SUB-14's routed-findings list and republishing the matrix at its `post-validation` revision (`10_republished-authority-matrix.md`). **Every one names an owning sub-task.** Stated once, here: **none of the four is a defect in SUB-13's authority assignment or in SUB-14's validation method.** Two (`F-S16-2`, `F-S16-3`) are model questions this sub-task is explicitly forbidden to decide and therefore re-routes to SUB-6; one (`F-S16-4`) is a residue in a merged sibling that the append-only discipline forbids repairing by edit; one (`F-S16-1`) is about identifiers, not architecture. **Eleven findings were received and eleven dispositioned** — the register is at `10_…md` §5, and no finding was closed by absorbing it silently into a row.*

#### `F-S16-1` — SUB-14 pairs three sub-task labels with the wrong tracker ids, in 24 places across 5 merged files

- **Id:** `F-S16-1`
- **Finding:** SUB-14 writes **`SUB-16 (NEU-980)`** in **16** places, **`NEU-983 (SUB-11)`** in **7**, and **`SUB-12 (NEU-985)`** in **1**. The tracker records **SUB-16 = `NEU-979`** (this sub-task), **`NEU-980` = SUB-7** (*"Define the general web API's scope, its negative boundary, and its resource-level inventory"*), **SUB-11 = `NEU-985`** (*"Run the package's mechanical audits…"*), **SUB-12 = `NEU-986`** (*"Close the package…"*) and **`NEU-983` = SUB-9** (*"Decide the repository topology…"*). All three pairings are inverted or displaced; **all 24 instances are handoffs**, which is the only place in an append-only package where an identifier is load-bearing.
- **Evidence:** `09_authority-matrix-validation.md` §17 (both the `SUB-11` and the `SUB-12` row); `02_findings-register.md` `### SUB-14` (11 × `SUB-16 (NEU-980)`, 4 × `NEU-983 (SUB-11)`); `90_open-items-and-provisional-register.md` `### SUB-14`; `91_caps-and-incomplete-scope.md` `### SUB-14`; `traceability/S14_authority-matrix-validation-coverage.md`. Counted mechanically over the merged tree at the 2026-08-22 cutoff. The **correct** pairings are already published elsewhere in the same package — `../04_…md` §7 and `../07_…md` §7 both write `SUB-11 (NEU-985)` and `SUB-12 (NEU-986)`, and `../05_…md` §7 correctly pairs `NEU-983` with SUB-9 — so this is drift introduced at SUB-14, not a package convention. Zero references to `NEU-979` exist anywhere in the package.
- **Consequence:** A consumer that routes by **tracker id** rather than by sub-task label mis-delivers **three of SUB-14's four handoffs**: work owed to this sub-task lands on SUB-7's web-API scope, work owed to SUB-11's mechanical audits lands on SUB-9's repository topology, and work owed to SUB-12's completeness gate lands on SUB-11. This is the **second sighting** of the class first recorded as **`F-S3-2`** (SUB-2 wrote `NEU-987` for `NEU-977` in 13 places across 6 merged files); a class with two independent sightings and no mechanical check is a package property, not an author's slip, which is why it is filed rather than corrected in passing.
- **What is assumed rather than derived:** Nothing. Every pairing is read from the tracker and every instance counted in the merged tree.
- **Handed to:**
  - **`SUB-12 (NEU-986)`**, at the completeness gate, which reconciles identifiers across the package and is the only party positioned to decide whether an erratum chapter or a standing convention is the right repair.
  - **`SUB-11 (NEU-985)`**, whose mechanical audits can check label↔id pairing across the package in one pass — the check that would have caught both sightings.
  - **Not repaired here.** The 5 files are merged and the discipline is append-only, so the finding is the correction. `10_…md` writes only correct ids.

#### `F-S16-2` — Should a category whose authority is external be in the isolation invariant's domain at all? (re-routed from `F-S14-3`)

- **Id:** `F-S16-2`
- **Finding:** `F-S14-3` establishes that **`SC-S3-45` cannot reach `holds` under any target state**, because `I3` requires an enumerated access-path set covering reads and writes and no such set can be enumerated for a store this system does not own. The assignment is **correct** — clause 4, as resolved by `F-S13-2` — so the obstruction is not repairable by re-running the rule, and this sub-task is explicitly forbidden to re-decide the model. **The open question is therefore a model question:** does `07_…md` §6.1's clause 4, which admits an external authority, interact correctly with `06_…md` §3's domain rule, which then subjects that row to an invariant it is structurally incapable of satisfying? Either the row is out of the invariant's domain, or the invariant admits a permanently-unsatisfiable member and says so.
- **Evidence:** `F-S14-3` and its evidence chain. `../07_…md` §6.1 clause 4 (identity mapping, authored in `Z-IDP`). `../06_…md` §3.4.1 (`I3` may not return `holds` by failing to find a counter-example) and §3.5. `../08_…md` §9 row `SC-S3-45`. `A-28` in `93_stand-in-assumption-register.md`, whose tolerance envelope covers a **server-side** mechanism and therefore cannot rescue the row in either direction. `10_republished-authority-matrix.md` §5.3.
- **Consequence:** Until it is answered, `SC-S3-45` is carried in both censuses with a **permanently** unimprovable verdict, and any downstream reader computing a "rows remaining to fix" figure will include one row that no work can fix. Carried as **`CAP-S16-1`** for exactly that reason: the disposition is complete, the answer is not.
- **What is assumed rather than derived:** Nothing new. The structural incapacity is `F-S14-3`'s, consumed as finished.
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, which owns the model, its criteria and clause 4 — **the only party that may answer this**. SUB-6 is merged and Done, which is itself recorded as **`OI-S16-1`**.
  - **`SUB-12 (NEU-986)`**, at the completeness gate, since an unanswered re-route to a closed sub-task is a gate condition.

#### `F-S16-3` — Is clause 1 preceding clause 4 intended for a process-local projection of external state? (re-routed from `F-S14-7`)

- **Id:** `F-S16-3`
- **Finding:** `F-S14-7` reports that `SC-S3-26` (JWKS key set) and `SC-S3-45` (identity→owner mapping) answer *"who owns a value this system merely projects from an external issuer?"* in opposite ways. **Re-running the rule shows this is not an inconsistency of judgement but a consequence of clause order**, which is first-match-wins: `SC-S3-26` is process-local and derived-on-read, so **clause 1 fires and clause 4 is never reached**; `SC-S3-45` is not process-local, so clause 1 falls through and **clause 4 fires**. Both authorities are therefore correct as published and **neither row is revised**. What remains open is whether that ordering is **intended** for the projection case — i.e. whether clause 1's "the component whose process computes it" should yield to an external authority when the computed thing is a projection of that authority's own state.
- **Evidence:** `../07_…md` §6.1 clauses 1 and 4 and the first-match-wins statement. `../08_…md` §9 rows `SC-S3-26` (`CMP-S4-4`, clause 1) and `SC-S3-45` (`CMP-S4-10`, clause 4), and §8's `SC-S3-26` block whose Freshness cell already records *"the issuer's set wins… the refresh policy is `Z-IDP`'s"* — the external answer, inside a row clause 1 assigns locally. `F-S14-7`. `10_republished-authority-matrix.md` §5.2, which shows the clause-order derivation as a table.
- **Consequence:** **Nil for isolation** — neither row's verdict moves under either answer, in either census, so nothing is blocked on it. It matters for **modelling consistency**: a future category that is a process-local projection of external state will be assigned by clause 1 by default, and if that default is wrong it will be wrong silently. Recorded rather than absorbed because `SC-S3-26` and `SC-S3-45` are the only two rows where the question currently arises, and two is enough to establish a pattern but not enough to notice one.
- **What is assumed rather than derived:** That `FL-S4-4`'s value and `SC-S3-26` denote the same thing — inherited from `F-S14-7`, which derives it.
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, which owns the clause order. Merged and Done — see **`OI-S16-1`**.
  - **`SUB-12 (NEU-986)`**, at the completeness gate.

#### `F-S16-4` — Three `05_…md` §5 annotations remain unreconciled against the `post-validation` revision; none is repaired by edit

- **Id:** `F-S16-4`
- **Finding:** Dispositioning `F-S14-8`, `F-S14-9` and `F-S14-10` settles the **matrix** half of each by re-running SUB-6's rule, and leaves the **flow** half standing in `05_system-context-and-responsibility-boundaries.md` §5. Three residues remain, each with its direction named: (i) **flow-vs-flow** — `FL-S4-8`/`FL-S4-9` name `CMP-S4-19` authoritative for `SC-S3-16`/`SC-S3-17` while `FL-S4-20`, in the same section of the same document, names `CMP-S4-9` *"for the source rows"*; (ii) **flow-vs-flow** — `FL-S4-14` names the drift cache's **holder** (`CMP-S4-18`) authoritative while `FL-S4-13`, same section, calls `CMP-S4-17` the cache's **only** writer; (iii) **matrix→flow** — `FL-S4-16` still reads *"Undetermined"* for the gate-verdict authority that `../08_…md` resolved to `CMP-S4-14` and that SUB-13 recorded as closing `OI-S2-2`. **`05_…md` was not edited, and neither was `08_…md` or `09_…md`.**
- **Evidence:** `../05_…md` §5 `FL-S4-8`, `FL-S4-9`, `FL-S4-13`, `FL-S4-14`, `FL-S4-16`, `FL-S4-20`, and §3.2's statement that `CMP-S4-9` is the only writer of the `public` and `infrastructure` schemas **on the request path**. `../08_…md` §9 rows `SC-S3-16`, `SC-S3-17`, `SC-S3-33`, `SC-S3-35`. `F-S14-8`, `F-S14-9`, `F-S14-10`. `10_republished-authority-matrix.md` §5.4, §5.5, §5.6 and §6.3 (the three published reasons for not reconciling by edit).
- **Consequence:** Two of the three are **intra-document** contradictions — the first of their kind found in this package — and (i) is the sharper of them, because `SC-S3-17` is read by the **Tier-2 blocking gate**, so a reader guessing which flow is authoritative is guessing about a gate input. **The isolation consequence of all three is nil** under either reading, which is why they are modelling findings and not security ones. Recorded with direction named on each so a later pass reconciles a **stated** discrepancy rather than rediscovering it, and so that nobody silently adopts whichever side they read first.
- **What is assumed rather than derived:** Nothing. Every annotation is quoted at a cited location, and the matrix half of each is re-derived from `../07_…md` §6.1 in `10_…md` §5.
- **Handed to:**
  - **`SUB-4 (NEU-974)`**, which owns `05_…md` and is the only party that may amend it. SUB-14 recorded this as *"SUB-16's to amend"*; **that assigns ownership of the outcome, not a licence to rewrite a merged sibling** — `09_…md` §12 states that reconciling the two sides *"would destroy the record of which one was written first and on what evidence"*, and `10_…md` §6.3 publishes the reasoning in full.
  - **`SUB-11 (NEU-985)`**, whose mechanical audits can check flow-vs-flow authority consistency as a **class** rather than as three point fixes.

### SUB-7

*Four entries. Two (`F-S7-1`, `F-S7-2`) are the **unmatched items** of the resource-inventory ↔ matrix cross-check that discharges `OUT-5` (`11_web-api-scope-and-resource-inventory.md` §11.2) — each carries the entry at issue, the expected value, an owner and a disposition, because a bare count discharges nothing. Two (`F-S7-3`, `F-S7-4`) arose from the cross-check without being unmatched items, and are filed in their own right rather than folded into the count.*

*Three of the four route to a sub-task that is **merged and closed**. That is the accepted charter-review warning **`F5.7`**, of which `SUB-13 → SUB-7` is one of the four named instances; these findings are its mirror image travelling the other way. Each therefore co-names **`NEU-896`** and **`SUB-12 (NEU-986)`**, so no finding here is addressed solely to a party that cannot act. **The residual is carried, not fixed.***

#### `F-S7-1` — No state category covers the web session's own identity binding, so the web API's most basic projection resolves to no authority

- **Id:** `F-S7-1`
- **Finding:** The learner-facing web surface requires a read-projection of **which authenticated learner the current web session belongs to** — every other projection in `11_…md` §9.1 is per-learner and is meaningless without it. **No category in the forty-five covers it.** `SC-S3-19` binds a JWT subject to an **MCP session** in `CMP-S4-4`'s process memory and is described in `04_state-category-inventory.md` §3.4 as *"the only server-side learner-identity binding that exists anywhere in the system"*; `SC-S3-45` binds an authenticated principal to **the rows it owns**, which is a different relation with an external authority (`CMP-S4-10`); `SC-S3-43` is browser-held UI interaction state, not a server-side binding. **The category does not exist**, so the matrix cannot assign it and the capability resolves to **zero** authorities.
- **Evidence:** `../04_…md` §3.4 (`SC-S3-19`, store `src/transport/http.ts:83`, `SessionIdentity` `:32`–`:35`, enforced `verifySessionBinding` `:52`–`:72`, written at `onsessioninitialized` `:204`–`:210`) and §3.7 (`SC-S3-43`, `SC-S3-45`). `../05_…md` `CMP-S4-3` (*"Terminates the learner's **web session**"*) against `../05_…md` §1's own `session` disambiguation — a **web session** and a **learning session** are different things, and `SC-S3-19` is keyed to neither. `../05_…md` `FL-S4-3`: *"Neither `CMP-S4-1` nor `CMP-S4-3` is authoritative for identity."* `10_republished-authority-matrix.md` §8, all 45 rows.
- **Consequence:** The web API cannot be **completely** scoped at this revision. `11_…md` publishes sixteen entries over a surface that needs seventeen, and the seventeenth is the one every other entry depends on. Practically, an implementation charter will invent the binding — in a cookie, a server-side session store, or by re-presenting the bearer token on every call — and **whichever it picks becomes an unassigned state category by default**, which is precisely the silent-second-writer shape the negative boundary exists to prevent. **This does not contradict `10_…md` §7.2's `UNMATCHED 0 / 0`**: that audit compares the matrix to `../04_…md`'s inventory and both agree, because the gap is in the category set they share.
- **What is assumed rather than derived:** That the surface `A-27` posits requires a server-side notion of *this* web session's learner at all. `A-27` is `[unconfirmed]`, and a purely token-forwarding web tier holding no session state is inside its envelope — which is exactly the provisional reading carried as **`OI-S7-1`**, and which would make the missing category unnecessary rather than merely unassigned.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which owns the assignment — with the root cause named one step upstream at **`SUB-3 (NEU-973)`**, whose category set has no *web session → principal* row. Both are merged and closed.
  - **`NEU-896`** at convergence and **`SUB-12 (NEU-986)`** at the completeness gate, per the `F5.7` residual above.
  - **`SUB-15 (NEU-982)`**, which cannot decide a protocol style without knowing whether the surface is session-bearing or token-forwarding.

#### `F-S7-2` — A learner-payload erasure intent resolves to no authority, because the two rows holding learner payload have no deletion owner

- **Id:** `F-S7-2`
- **Finding:** *Erase this learner's payload* is a write-intent a learner-facing surface is required to offer — `../04_…md` records that **NEU-887's privacy gate requires** a retention window and a deletion owner, and that neither is implemented (`F-S3-3`). The two categories holding learner payload are `SC-S3-16` (MCP request log — *"`response_body` and `params` carry learner-facing text and learner free-text answers"*) and `SC-S3-17` (operation event log). Both carry an **authority** (`CMP-S4-9`, write path `CMP-S4-19`) and **no deletion owner**, and `CAP-S4-1` establishes that **no component in `../05_…md`'s inventory can be one**. The intent therefore resolves to **zero** authorities and is not published as an inventory entry.
- **Evidence:** `../04_…md` §3.3 (`SC-S3-16`: created `drizzle/0010_create_infrastructure_mcp_request_log.sql:1`–`:19`, extended `drizzle/0012_extend_mcp_request_log.sql:1`–`:6`, written `src/transport/pg-audit-transport.ts:117`; `SC-S3-17`: created `drizzle/0013_create_operation_event_log.sql:1`–`:16`, written `src/transport/pg-event-transport.ts:109`) — both recorded with *"no retention window and no deletion owner are implemented"*. `91_caps-and-incomplete-scope.md` `CAP-S4-1`, at its **seventh** sighting here. `../05_…md` §9.2's unblocking condition. `10_…md` §8 rows `SC-S3-16`, `SC-S3-17`.
- **Consequence:** **The API's erasure capability cannot be scoped at all** — not postponed, not simplified. A downstream charter asked to implement "delete my data" over this architecture has no component to route the intent to, and the honest answer is that the request cannot be satisfied for these two categories by anyone. `SC-S3-17` sharpens it further: it is **read by the Tier-2 blocking gate** (`src/adapters/drizzle/tier2-blocking-stats-repository.ts:39`), so deleting from it would alter a gate input. Carried as **`CAP-S7-1`**, which is a cap on **this chapter's scope** and is deliberately **not** a re-filing of `CAP-S4-1`, which is a cap on the ownership model. **`CAP-S4-1` stays open and this sub-task does not close it.**
- **What is assumed rather than derived:** That the learner-facing surface is obliged to offer erasure. That obligation is read from NEU-887's privacy gate as `../04_…md` reports it, not from a direct reading of NEU-887, and not from any legal analysis — none is performed here or anywhere in this package.
- **Handed to:**
  - **`SUB-13 (NEU-977)`** on the matrix side, for the absent deletion owner on two assigned rows. Merged and closed.
  - **`NEU-893`**, which is chartered for safe production integration and learner isolation and is where a retention-and-deletion owner would be decided.
  - **`NEU-896`** at convergence and **`SUB-12 (NEU-986)`** at the completeness gate, which must weigh an architecture carrying a privacy obligation with no assignable owner at its seventh sighting.

#### `F-S7-3` — `SC-S3-43`'s assignment to `CMP-S4-9` is unenforceable for exactly the portion of UI state `A-27` permits to stay in the browser

- **Id:** `F-S7-3`
- **Finding:** `SC-S3-43` — the category literally named **"web-session and UI interaction state"** — is assigned to `CMP-S4-9`, the MCP core's durable store, with write path `CMP-S4-7`, under clause 5. That is correct under `M-A`, and `../07_…md` §6.3 is explicit that the clause-3 presentation exception which would have moved it is **empty**. But `A-27`'s tolerance envelope permits *"arbitrarily rich client-side interaction state"* — and interaction state that stays in the browser and is never round-tripped is **held nowhere `CMP-S4-9` can see**. **The assignment is not wrong; it is unenforceable over the browser-only portion**, because no mechanism requires that portion to reach the authority at all.
- **Evidence:** `10_…md` §8 row `SC-S3-43` (`CMP-S4-9`, `CMP-S4-7`, clause 5, `assumed` — `A-27`). `../07_…md` §6.3: *"`M-A` makes the MCP core the exclusive writer of all 45 categories, so clause 3 matches no row"*, and its naming of `SC-S3-43` as the single row a reversal would move. `../93_…md` `A-27` tolerance envelope. `../05_…md` `FL-S4-2` (*"A browser-side copy is a cache, never an authority"*) and `FL-S4-1` (*"The browser reports; it never decides"*).
- **Consequence:** **Nil for the Critical divergence risk, and saying so is part of the finding.** The row is not gate-bearing by `A-27`'s own terms, so browser-only interaction state cannot affect a mastery decision, and `FL-S4-2` already classes the browser copy as a cache. What is weaker is the **meaning of the assignment**: a reader who takes "assigned to `CMP-S4-9`" as "held by `CMP-S4-9`" would over-estimate what the matrix guarantees for this row. Inflating this into a divergence risk would be manufacturing one; suppressing it would leave the strongest-looking row in the negative boundary resting on an unstated qualification.
- **What is assumed rather than derived:** `A-27` in full — the row is `assumed`, not `existing`, and no store exists to observe. Nothing here is derived from a running surface.
- **Handed to:**
  - **`SUB-13 (NEU-977)`**, which owns the assignment. Merged and closed; **`NEU-896`** and **`SUB-12 (NEU-986)`** co-named per `F5.7`.
  - **`SUB-15 (NEU-982)`**, whose rendering-model decision determines how much interaction state stays in the browser and therefore how much of the assignment is enforceable in practice.

#### `F-S7-4` — A web-originated mutation reaches the tool surface gated but unattributed, because the context-token table carries no authenticated subject

- **Id:** `F-S7-4`
- **Finding:** Under `M-A` **every** web mutation is an MCP tool call across `CMP-S4-4` (`FL-S4-5`), and `CMP-S4-4` mounts a context-token gate on `tools/call` for all but three exempt tools. `../04_…md` §3.1 records of `SC-S3-13` that **the context-tokens table carries no authenticated subject**. The gate therefore establishes that *a bootstrap happened*, not *which learner is acting*: a web-originated mutation arrives **gated but not attributed**.
- **Evidence:** `../04_…md` §3.1 `SC-S3-13` — *"the table carries **no** authenticated subject"*, store `public.context_tokens` (`src/infrastructure/db/schema.ts:312`). `../05_…md` `FL-S4-5` and the context-token gate at `src/transport/context-token-middleware.ts:5`–`:9`. `10_…md` §8 row `SC-S3-13` (`CMP-S4-9`, W *"`CMP-S4-7`; enforced at `CMP-S4-4`"*, clause 5). `../04_…md` §6: **zero** matches for `user_id`/`userId`/`learner_id`/`learnerId` in `schema.ts`, so no ownership column exists to attribute against either.
- **Consequence:** Attribution for a web-originated write rests entirely on the **JWT subject** verified at `CMP-S4-4` (`src/transport/jwt-middleware.ts:114`) and on `SC-S3-19`'s in-memory, MCP-session-keyed binding — neither of which is durable and neither of which reaches the row that is written. This bears directly on the **confinement residue** `10_…md` §8.3 attributes to **`SUB-8 (NEU-981)`**: an enumerated access-path set covering reads and writes cannot distinguish a learner's own web-originated write from any other under the current mechanism. *(Transport: HTTP. Tier: core edge, `CMP-S4-4`, gating `CMP-S4-6`. No claim here holds on STDIO, where `BND-S4-17` records that the context-token gate is not mounted at all.)*
- **What is assumed rather than derived:** Nothing about the code. What is assumed is that a general web API exists to originate such calls — `A-27` and `../05_…md`'s `[unconfirmed]` `CMP-S4-3`.
- **Handed to:**
  - **`SUB-8 (NEU-981)`**, named by `10_…md` §8.3 as owner of the confinement residue this bears on.
  - **`NEU-850`'s `OUT-2`** and **`NEU-893`**, which between them own the attribution mechanism that would resolve it.
  - **`SUB-12 (NEU-986)`** at the completeness gate.

---

### SUB-8

*`NEU-981`, covering `OUT-6` — the application-versus-reusable-core rule and the backward-compatibility contract, published as `12_application-versus-core-rule-and-compatibility-contract.md`. Four entries. **Two publishable observations are deliberately not filed here**, because in each case a duplicate would have been **less precise than the original** rather than merely redundant: the tool-surface count (`F-S5-3` owns it — `F-S8-1` below is scoped strictly to refining its **diagnosis**) and the `sub`-or-`azp` principal collapse (`F-S5-4` and `../06_…md` §4.1 own it; `12_…md` §11 consumes it and adds only a pricing observation). `../91_…md` records that a duplicate entry is correct-by-convention until `SUB-12` reconciles, so neither omission is required — both are judgements, and they are stated so the reconciliation can disagree with them.*

#### `F-S8-1` — The charter's compatibility-surface figures are a miscount, not staleness: `F-S5-3`'s count is corroborated and its diagnosis refined

- **Id:** `F-S8-1`
- **Finding:** `F-S5-3` records that *"the charter's 45 / 42 / 40 are **stale** in all three positions"*. The count is right and is independently corroborated here; **the word "stale" is wrong**. Charter assumption 8 carries an explicit evidence note recording a re-verification *"by direct count"* on **2026-08-21**. `src/server/` last changed at **`6efd9fe`, 2026-08-04** — seventeen days earlier. The re-verification therefore ran against a tree identical in `src/server/` to the one measured here, and returned the wrong number. No elapsed time explains the difference: the figure was **produced by running the procedure**, not left behind by it.
- **Evidence:** `git log -1 -- src/server/` → `6efd9fe`, 2026-08-04. Charter C010 assumption 8's own evidence cell — *"Re-verified 2026-08-21 by direct count: 45 `inputSchema` registrations across 16 `src/server/*-tools.ts` modules"* — status `confirmed`. Re-derived at cutoff `ad5eebb`: **46** registrations across **16** registering modules (two further modules, `persistence-tools.ts` and `session-management-tools.ts`, register **zero** and are delegation aggregators); **3** exempt carrying `z.object({}).shape` (`server-context-tools.ts:11`, `server-info-tools.ts:8`, `server-workflow-tools.ts:8`); **43** gated; **43 of 43** declaring `context_token`, resolved per tool by brace-matching each registration's `inputSchema` to its defining shape. Full enumeration at `12_…md` §7.1–§7.4.
- **Consequence:** The two diagnoses imply **different remedies**, which is the whole of this entry. A stale figure is fixed by re-running the procedure. This figure survived the procedure. A `confirmed` assumption whose stated evidence method yields the wrong number when honestly executed is a defect in the **method's reliability**, and every downstream consumer that read "42 gated" inherited it from a note asserting it had been checked. **Nothing in `12_…md`'s conclusions moves**: its pricing depends only on *"the set of gated tools lacking `context_token` is empty"*, which holds at 42 and at 43 — which is precisely why `SUB-5` chose to hold the claim count-independently, and that judgement is vindicated twice over. *(Transport: not applicable — a count over registrations, not a runtime claim.)*
- **What is assumed rather than derived:** Nothing. Both the commit date and the counts are mechanical reads of the tree at a named cutoff. What is **not** claimed is *which* registration the charter's count missed — the partition is reproduced (`SUB-5`'s 42-named + 1-inline and this chapter's 41-in-`domain/types` + 2-in-`src/server` are the **same 43** cut on different axes), but no attempt is made to identify the specific omitted row, because the remedy does not depend on it.
- **Handed to:**
  - **`NEU-896`**, which consumes the charter's assumption register as the reconciliation list and owns assumption 8's status.
  - **`SUB-12 (NEU-986)`** at the completeness gate.
  - Recorded **against `F-S5-3`** rather than beside it: **`SUB-5 (NEU-975)`** is merged and closed — the accepted `F5.7` residual — so the co-routing above is what makes this actionable.

#### `F-S8-2` — Two of the thirteen ports are characterised as pure-compute; both wrap external network calls

- **Id:** `F-S8-2`
- **Finding:** The inputs to this sub-task describe two of the 13 interfaces in `src/ports/` as **pure-compute** — the residue after the 9 row-owning repositories, `SearchPort` and `UnitOfWorkPort`. They are `embedding-port.ts` and `content-classifier-port.ts`, and **neither is pure-compute**: both wrap **outbound network calls to an external provider**. They are accurately *no-DB-row, external-provider I/O* ports.
- **Evidence:** `src/ports/embedding-port.ts` and `src/ports/content-classifier-port.ts`, implemented at `src/adapters/langchain/embedding-adapter.ts` and `src/adapters/langchain/content-classifier-adapter.ts`. Consistent with `../05_…md` `CMP-S4-11` (AI provider — *"behind a port; authoritative for no verdict"*). Contrast `../05_…md` `CMP-S4-8` (domain core — *"pure computation, zero I/O"*), which is what "pure-compute" names in this package's vocabulary.
- **Consequence:** The mischaracterisation is material to **two** live decisions and is filed for that reason rather than for tidiness. (i) **Egress.** `../05_…md` places `CMP-S4-17` as *the only component with egress to a party outside the operator's control*; if these two ports carry provider egress, that statement needs qualifying — they reach a **contracted** provider rather than an arbitrary third party, which is a different trust class but not zero egress. (ii) **Topology.** `SUB-9 (NEU-983)` consumes `12_…md`'s rule as its criterion and will reason about what may sit on either side of the distribution line. A port that appears pure-compute looks freely placeable; a port that makes outbound calls under a provider credential does not. *(Transport: not applicable — the egress is server-side and occurs identically under HTTP and STDIO.)*
- **What is assumed rather than derived:** Nothing about the adapters, which are read directly. **Not** claimed: that any published chapter asserts the "pure-compute" label — the characterisation appears in this sub-task's brief. It is filed so the correct reading reaches the consumers above regardless of where the label originated.
- **Handed to:**
  - **`SUB-9 (NEU-983)`**, as the live consumer of the port inventory.
  - **`SUB-4 (NEU-974)`**, which owns the component model and `CMP-S4-11`'s scoping. Merged and closed — `F5.7`.
  - **`SUB-12 (NEU-986)`** at the completeness gate.

#### `F-S8-3` — The authority driver changes zero tool contracts, so `AC-5` is discharged vacuously — reported with its derivation rather than left blank

- **Id:** `F-S8-3`
- **Finding:** `12_…md`'s per-item audit resolves all 49 entries against two drivers. Against **Driver B — the per-category authority assignments** — the result is **0 contract-changed / 49 unchanged**. The acceptance criterion asking that a tool writing a state category **assigned to web authority** be recorded contract-changed is therefore satisfied **vacuously**: the set of such tools is empty because **the set of such categories is empty**.
- **Evidence:** All 45 rows of `../10_…md` §8 read for their Authority column at revision `post-validation` (SUB-16 / NEU-979). **`CMP-S4-3` appears zero times.** Corroborated from two independent directions: `../10_…md` §11 states it holds *by construction* under `M-A`, and `../11_…md` §9.3 reports 0 of 16 resource-inventory entries naming `CMP-S4-3`. The nearest row, `SC-S3-43` (*Web-session / UI interaction state*), is authored by `CMP-S4-9` with a write path through `CMP-S4-7`, clause 5, `assumed — A-27` — and **no MCP tool writes it today**, so it contributes zero entries under either driver.
- **Consequence:** A vacuous discharge is a **weaker result than a populated one** and must not be read as a passed check. It is filed so the zero is auditable as a derivation rather than appearing as an unfinished section. `12_…md` §10.4 records the conditional that keeps it useful: were `../07_…md` §5.3's conjunction `R1 ∧ R2 ∧ R3` satisfied and `M-C` selected, `SC-S3-43` is the **single** row that would move, and any tool then written to serve web-session state would be contract-changed with **`RD-S8-1`** (cross-principal replay) as its detection method — a confinement question, not a schema one. *(Transport: the conditional is HTTP-scoped; `SC-S3-43` presupposes a web surface, which STDIO has no analogue of.)*
- **What is assumed rather than derived:** The Authority column is **consumed** from `../10_…md`, not re-derived — this chapter originates no authority. The conditional statement inherits `../11_…md` §13's `R3` verdict, which rests on `A-27` and is `[unconfirmed]`; if `A-27` is invalidated the antecedent may begin to hold and the vacuous discharge re-opens.
- **Handed to:**
  - **`SUB-11 (NEU-985)`**, which owns the outcome-coverage audit rows and will read this count.
  - **`SUB-12 (NEU-986)`** at the completeness gate, as the party that must tell a vacuous pass from a real one.

#### `F-S8-4` — The two `DP` tokens in `src/` are Dynamic Programming, not the Diploma Programme: a collision a grep-based leakage audit will misreport

- **Id:** `F-S8-4`
- **Finding:** In the charter and in `12_…md`, `DP` denotes the **IB Diploma Programme** — the course application on the private side of the distribution line. In `src/`, the only two `DP` tokens denote **Dynamic Programming**, a computer-science teaching concept, and are **not** course leakage. A leakage audit run as a token search will report them as findings.
- **Evidence:** `src/domain/types/teaching.ts:285` and `src/domain/algorithms/grade-mapper.ts:10` — the surrounding schema (`correct_recurrence`, `correct_base_case`, `correct_iteration_order`, `complexity_stated`) fixes the reading. Corroborated in the opposite direction: **no** IB Diploma Programme or course-specific vocabulary appears in any tool name, schema field or prompt across the 46 registrations and 3 prompts (`12_…md` §13, 0 of 5 proposed core surfaces carrying a DP-specific concept, 0 findings).
- **Consequence:** Small, and filed only because the cost of not filing it is asymmetric. A future reviewer discharging the *no-DP-concept-in-core* obligation by grep gets **two false positives in the public core**, which is the most alarming possible place to get one — the plausible reaction is to treat published MIT code as leaking course structure. One sentence here prevents that. *(Transport: not applicable.)*
- **What is assumed rather than derived:** Nothing; both sites are read directly and the surrounding field names are quoted.
- **Handed to:**
  - **`SUB-11 (NEU-985)`**, which owns the audit rows and is the most likely party to run such a search.

---

### SUB-15

*Appended by `NEU-982` (SUB-15) on 2026-08-22. Verification cutoff `229e8f4`. Nothing above this heading was edited.*

#### `F-S15-1` — The "share the runtime so the web tier can reuse the core" argument is unsound on this codebase, and the runtime must be decided on contract-sharing instead

- **Id:** `F-S15-1`
- **Finding:** The most commonly offered reason to give a web tier the same runtime as its core — *so it can import and reuse the core's code* — does not hold here, for two independent reasons that point in opposite directions. **(i) The reusable part does not need the runtime shared.** `src/domain/` is runtime-portable: a static transitive import-closure walk over the built tree resolves all 65 domain entry points to **zero Node builtins**, with only `zod`, `compromise` and `markdown-it` crossing the package edge. Anything that can run `zod` can run the domain layer, so sharing the runtime buys the domain layer nothing it did not already have. **(ii) The part that needs the runtime shared is not reused.** `../12_…md` §6.1's **`CC-S8-5`** records that the web tier reaches state **only through tool calls**, and places *"no obligation on the MCP surface"* to be importable. The composition root's closure — 103 files, 6 external packages, 7 Node builtins — is precisely the part a web tier would have to share a runtime to import, and it is precisely the part `CC-S8-5` says it does not import. **The reuse argument therefore proves the wrong half in each direction: what is portable needs no sharing, and what needs sharing is not consumed.**
- **Evidence:** `SPK-S15-1` (`92_spike-register.md` § SUB-15), run against the built tree at `229e8f4` on Node v22.23.1 — arm D (all 65 `domain/` entries): 65 entries, external `zod`/`compromise`/`markdown-it`, **0 builtins**; arm E (`composition-root.js`): 103 files, 6 external, 7 builtins; arms A and B (single domain modules): 1 file each, 0 builtins. Corroborated statically in the source tree: `src/domain/` is 65 TypeScript files with **0** imports of `pg`, `drizzle-orm`, `node:*`, `fs` or `path`. Contract side: `../12_…md` §6.1 `CC-S8-5`; §7.1–§7.2 for the 46 tools / 43 gated / 3 exempt surface (`F-S8-1`).
- **Consequence:** `DR-C10-S15-2` could not be argued the usual way, and is not. Had the record rested on code reuse it would have been **defeated by a fact in its own package** — the reviewer who opened `CC-S8-5` would have found the premise contradicted. The decision instead rests on **compatibility-contract detection reach**: the 43 gated tool schemas are mechanically shareable as contract types only within a TypeScript consumer, and `RD-S8-4`'s golden-manifest method cannot see a foreign-language client at all. That is a *narrower* argument than reuse and it is the one the evidence supports. A second-order consequence: the same measurement **strengthens** the alternative it rejects — a foreign-language web tier is more viable here than folklore suggests, and `DR-C10-S15-2`'s rejected alternative 1 says so explicitly rather than dismissing it.
- **What is assumed rather than derived:** The closure walk is **static** — it resolves declared import specifiers transitively and would not see a runtime-only `require`, a dynamic `import()` behind a computed specifier, or a native binding pulled in by a dependency's own internals. No such construct was found, but their absence is not proven. The `compromise`/`markdown-it`/`zod` packages' *own* internal builtin use was not walked; the measurement is of this repository's closure, not of its dependencies'. One harness artifact is disclosed in `SPK-S15-1`'s Result field: a regex captured the string `" must be a valid calendar date"` as a package name, which is a defect of the extractor and not a dependency.
- **Handed to:**
  - **`SUB-9 (NEU-983)`**, which scores build tooling, testing strategy, local dev and release process, and now inherits a runtime decided **without** a reuse obligation to price — `DR-C10-S15-2`'s criterion-input table states this explicitly so SUB-9 does not re-derive a constraint that was never taken on.
  - **`SUB-10 (NEU-984)`**, which carries deployment shape forward: a domain layer with zero builtins is deployable to substrates a builtin-dependent one is not, and that is a live input to substrate selection rather than a curiosity.

#### `F-S15-2` — `SPK-S6-1`'s ≤0.02% latency result is an in-process transport floor with no network hop, and citing it as a deployment round-trip cost over-reads it

- **Id:** `F-S15-2`
- **Finding:** `SPK-S6-1` measured the cost of routing a learner-path state read through the MCP tool boundary and returned a figure at or below **0.02%** of the sub-second budget `A-25` predicates. That measurement was taken across an **in-process transport** — the harness pairs both ends in one process, so the number is a **serialization-and-dispatch floor** and contains **no network hop, no TLS handshake, no connection setup and no inter-host latency**. It is a correct answer to the question it was asked and a wrong answer to a question it was not: *what does a tool call cost when the web tier and the MCP server are separate deployed processes?* That figure is **unmeasured**, and nothing in the package supplies it.
- **Evidence:** `92_spike-register.md` § SUB-6, `SPK-S6-1` — its Method field names the in-process transport pairing and its Result field states the ≤0.02% figure; read at `229e8f4`. The gap matters because `../11_…md` §9.2 records that every one of the five write-intents is *"forwarded as an MCP tool call"*, so the per-call cost multiplies across the write surface rather than being paid once.
- **Consequence:** Two decisions in `13_…md` are affected, and both are written to survive it. `DR-C10-S15-4` (rendering model) commits every gate-bearing read to a server round trip, and `DR-C10-S15-3` (protocol style) commits the write surface to intent-forwarding — so if the real deployed round-trip cost turns out to be materially above the in-process floor, the *cost* of those decisions rises even though their *reasoning* does not change, because neither is argued from latency. Recorded as **`CAP-S15-1`** rather than left implicit, so the unmeasured quantity is visible to whoever prices deployment. The narrower risk is citation drift: `SPK-S6-1`'s number is quotable and its envelope is not, so a downstream reader could reasonably lift "0.02%" into a deployment argument it does not support.
- **What is assumed rather than derived:** Nothing about the original spike's correctness — this finding does not dispute `SPK-S6-1`'s result, its method or its use in `SUB-6`'s own comparison, where the in-process framing is the right one. It records only the **envelope**, which the original record states and which a later citation could drop. No estimate of the real round-trip cost is offered here; offering one would be exactly the over-reading this finding names.
- **Handed to:**
  - **`SUB-10 (NEU-984)`**, which owns deployment shape and is the first party with a topology concrete enough to measure against. It receives the envelope and `CAP-S15-1`.
  - **`SUB-9 (NEU-983)`**, for awareness only: if local development runs the two tiers as separate processes, the floor is not what developers will observe.

---

### SUB-9

*Appended by `NEU-983` (SUB-9) on 2026-08-22. Verification cutoff `0962279`. Nothing above this heading was edited.*

#### `F-S9-1` — The repository-history figures behind charter assumption 11 have moved, and the move reconciles exactly; the structural claim the criterion rests on is unchanged

- **Id:** `F-S9-1`
- **Finding:** Charter assumption 11 (**confirmed**) states the creator authored **431 of this checkout's 648 commits** under two git names on one address (415 + 16), with the remaining **217 automated** (215 `github-actions[bot]`, 2 `Copilot`). Re-measured at `0962279`, the totals are **720 commits: 468 human** — `Pavel Kalabin` **452** and `recky-ru` **16**, both resolving to `recky.ru@gmail.com` — and **252 automated**, being `github-actions[bot]` **250** and `Copilot` **2**. The difference reconciles exactly and leaves no unexplained commits: **+37 human** (415 → 452) and **+35 automated** (215 → 250) sum to the **+72** commits between the charter's checkout and `0962279` (648 → 720). **The structural claim the criterion input actually rests on is unchanged: every non-bot commit in the repository is the creator's, across two git names on one email address, with zero third-party human commits.** The charter's numbers are not wrong; they are a measurement at an earlier commit, and the drift is entirely this charter's own fleet activity plus the version-bump bot.
- **Evidence:** `git rev-list --count HEAD`, `git shortlog -sn HEAD` and `git log --format='%ae'` tallied at `0962279` in a dedicated worktree, 2026-08-22. Charter side: `01_charter.md` assumption 11, itself re-verified 2026-08-21 on `feature/847-surface-the-correct-answer-after-a` (415 + 16 + 215 + 2 = 648). The automated share is mechanically explained by `.github/workflows/ci.yml`'s `version-bump` job, which bumps the patch version on `develop`, opens its own pull request and auto-merges it.
- **Consequence:** The sole-maintainer criterion input (`14_…md` §3.1, weight **decisive**) is scored on the structural claim rather than on the arithmetic, so the selection in `DR-C10-S9-1` is unaffected. The finding matters for a different reason: **the automated share is growing faster in proportion than the human one** (215/648 = 33.2% → 250/720 = 34.7%), and every one of those commits is produced by a job that writes the **root** `package.json`. Under the selected split-visibility workspace that job must be retargeted at the core member manifest, so a figure that looks like trivia is in fact the mechanism behind migration step `M8`.
- **What is assumed rather than derived:** That author identity is a proxy for maintainership. The tally counts commits by git author on this branch's ancestry; it does not establish who reviewed, operated or decided anything, and it cannot see contributions that arrived without a commit. The two-names-one-address reading assumes the address is not shared, which is consistent with everything observed but not proven by the tally. Commits reachable only from unmerged branches are out of the count by construction.
- **Handed to:**
  - **`SUB-12 (NEU-986)`**, for reconciliation: charter assumption 11's numbers and this register's now differ by cutoff, and the completeness gate should read them as one claim measured twice rather than as a contradiction.
  - **`NEU-896`**, which inherits migration step `M8` and the `version-bump` retargeting it implies.

#### `F-S9-2` — The repository-scale figures behind charter assumption 16 have grown; the qualitative facts the migration path depends on are unchanged

- **Id:** `F-S9-2`
- **Finding:** Charter assumption 16 (**confirmed**) records **165 TypeScript source files, ~25,200 lines, 197 test files, 25 Drizzle migrations**, no workspace and no `apps/`/`packages/`/web directory. Re-measured at `0962279`: **169 source files** under `src/`, **26,816 lines**, **202 test files** under `tests/`, and **25** Drizzle migrations. Three of the four counts have grown; **the migration count is unchanged**. Every **qualitative** fact is unchanged and was re-verified rather than assumed: `pnpm-workspace.yaml` still declares **no `packages:` key** (it carries only `onlyBuiltDependencies: [esbuild]`), and there is still **no `apps/`, `packages/` or web directory** — the repository root holds `_local/`, `content/`, `docs/`, `drizzle/`, `scripts/`, `src/` and `tests/`.
- **Evidence:** `find`/`wc` tallies over `src/`, `tests/` and `drizzle/` at `0962279` in a dedicated worktree, 2026-08-22; `pnpm-workspace.yaml` and a root directory listing read at the same cutoff. Charter side: `01_charter.md` assumption 16, sourced to repository file counts taken 2026-08-19.
- **Consequence:** `14_…md` §8's migration path is stated against the **measured** values, not the charter's, so that a reader walking it does not find four counts that no longer match the tree. The direction of the drift is itself relevant: the core grew by 4 source files and ~1,600 lines in three days while the migration count held, which is consistent with the tree being actively developed and is a reason the migration path is written as a sequence of **target states** rather than a file-by-file inventory that would go stale on contact.
- **What is assumed rather than derived:** The line count is a raw `wc -l` over `src/**/*.ts` and includes blank and comment lines; it is not a logical-statement count and is comparable to the charter's figure only because both were taken the same way. "Test files" counts `*.test.ts` and `*.spec.ts` under `tests/` (202); a broader count of **all** TypeScript files under `tests/`, including helpers and fixtures, is 209, and the charter's 197 is assumed to have used the narrower definition. The migration count is of `.sql` files at `drizzle/`'s top level and does not verify that `drizzle/meta/_journal.json` holds 25 corresponding entries.
- **Handed to:**
  - **`SUB-12 (NEU-986)`**, for reconciliation against charter assumption 16 as a same-claim-different-cutoff pair.
  - **`NEU-896`**, which executes the migration path against whatever the counts are at execution time; the path's steps are count-independent by construction.

#### `F-S9-3` — The core is not consumable as a dependency today, and `"private": true` is not the reason

- **Id:** `F-S9-3`
- **Finding:** At `0962279` `package.json` declares **no `main`, no `exports`, no `types` and no `bin`**. A consumer that resolves `second-memory-learning` therefore **fails outright** — measured, not inferred: `SPK-S9-1` variant A returns **`TS2307` ("Cannot find module … or its corresponding type declarations")** on every import. **`"private": true` is not the cause.** Variants B, C and E resolved cleanly with the flag still set; `private` blocks **publication**, not **resolution**. The operative absence is the entry-point declaration. **Nothing in the repository detects this, because nothing consumes the package as a dependency** — the build emits to `dist/` and is launched by path (`node dist/transport/main.js`), never by package name.
- **Evidence:** `package.json` read at `0962279` (fields `name`, `version`, `private`, `license`, `type` present; `main`, `exports`, `types`, `bin`, `publishConfig`, `files` absent). `SPK-S9-1` (`92_spike-register.md` § SUB-9), variants A/B/C/E, TypeScript 5.9.3 under `module`/`moduleResolution` `Node16` with `verbatimModuleSyntax: true` — the repository's own compiler settings. Corroborating: a search for `publish`, `registry`, `NPM_TOKEN` and `npmjs` across all four files in `.github/workflows/` returns **zero matches**, so no pipeline would have caught it either.
- **Consequence:** NEU-850's `OUT-6` is **not a one-line change**. Removing `"private": true` would make the package publishable and still leave it unusable, because the published artifact would expose no entry point. Migration step `M5` therefore states the entry-point declaration as part of what `OUT-6`'s execution requires. The finding also supplies the evidence behind criterion `K3`'s score in `14_…md` §5.3: the selected split-visibility workspace does **not** force the maintainer to consume their own published artifact, so this exact failure mode can persist under it — which is why **`OI-S9-1`** exists as an obligation rather than an observation.
- **What is assumed rather than derived:** The spike used a **stand-in** core package reproducing this repository's resolution-relevant configuration (name, `type: module`, `private`, and the same `Node16`/`verbatimModuleSyntax` compiler settings), not the 169-file core itself. It therefore establishes how **Node16 resolution** behaves against a manifest of this shape, and assumes the real core would resolve the same way — which follows from resolution being a function of the manifest and file layout, not of module count, but was not executed against the real tree. Runtime resolution (`node`) was not tested separately from type resolution (`tsc`); the two agree on `exports` under Node16 but a runtime-only failure mode would not have been seen.
- **Handed to:**
  - **`NEU-850`**, whose `OUT-6` this changes the cost of — see also `OI-S9-3` on which manifest is meant.
  - **`NEU-896`**, which owns `OI-S9-1` and migration step `M5`.

#### `F-S9-4` — `package.json` declares no `files` field, so a publish would ship everything not gitignored

- **Id:** `F-S9-4`
- **Finding:** At `0962279` `package.json` declares **no `files` field** and there is no `.npmignore` at the repository root. npm's fallback in that case is to include **everything not excluded by `.gitignore`** (minus npm's own fixed exclusion list). Because the repository is not published today (`F-S9-3`; no publish workflow), this has never had an effect. Under any topology in which a public artifact is emitted from a tree that also contains private material, **it becomes the mechanism on which the distribution line depends.**
- **Evidence:** `package.json` parsed at `0962279` — its complete key set is `name`, `version`, `description`, `private`, `type`, `license`, `author`, `repository`, `keywords`, `scripts`, `lint-staged`, `engines`, `dependencies`, `devDependencies`; **`files` is absent**, as are `main`, `exports`, `types`, `bin` and `publishConfig` (the same read that evidences `F-S9-3`). A directory listing at the repository root shows **no `.npmignore`**. Both checks were run against the **real** manifest, not against `SPK-S9-1`'s stand-in core — the stand-in reproduces only the resolution-relevant fields and is not evidence about this repository's publish surface.
- **Consequence:** This is the measured basis for criterion `K2`'s `−` score against the selected topology in `14_…md` §5.2, and for the honest statement that `T2` holds the distribution line by **configuration** while the rejected `T1` holds it **structurally**. It is what `OI-S9-2` obligates: the publish filter must be an explicit **allowlist**, and it must exist **before** the first publish, not after — which is why migration step `M6` is written as a precondition of `M5`'s first publish rather than as a later step. Stated without over-claiming: an allowlist **reduces** the risk that `T1` **eliminates**; it does not convert a configured guarantee into a structural one, and `K2`'s score is not revised by the existence of the obligation.
- **What is assumed rather than derived:** npm's default-inclusion behaviour is taken from the package manager's documented semantics and was **not** executed here — no `pack` was run against the real repository, so the actual contents of a hypothetical tarball are not enumerated. The finding is about the **absence of a declared allowlist**, which is directly observed; the consequence of that absence is the documented default, which is not. pnpm's publish path is assumed to follow the same field, which it does by design but was not verified.
- **Handed to:**
  - **`NEU-896`**, which owns `OI-S9-2` and migration step `M6`.
  - **`SUB-12 (NEU-986)`**, for reconciliation: this finding and `F-S9-3` are two halves of one gap between the repository as it is and the repository `OUT-6` assumes.

---

### SUB-10

*Appended by `NEU-984` (SUB-10) on 2026-08-22. Verification cutoff `03efe1d`. Nothing above this heading was edited.*

#### `F-S10-1` — SUB-9's structural argument rests on a premise the deploy workflow contradicts; the conclusion survives on a weaker footing

- **Id:** `F-S10-1`
- **Finding:** `14_…md` §6.4 argues that no realistic deployment shape reverses the repository-topology selection, and it argues it **structurally**, from "push-based SSH deployment to a self-managed host **that never sees the repository**". At `03efe1d` the deployment target **does** see the repository: `.github/workflows/cd-prod.yml:62`–`:65` runs `cd "$REPO_DIR"`, `git fetch origin`, `git reset --hard "$SHA"` and `git clean -fd` on the host, so the host holds a clone and updates it on every deploy. The premise is false as written. **`OI-S9-4`'s answer is unchanged — still No** — but its basis moves: the shape does not avoid the public-repository question structurally, it answers it because the cloned repository is **private**. That is a weaker and differently-conditioned footing than the one published.
- **Evidence:** `.github/workflows/cd-prod.yml` read in full at `03efe1d` in a dedicated worktree, 2026-08-22 — trigger `workflow_run` on CI `completed` for branch `develop`, job gated on `conclusion == 'success' && event == 'push'`, then an SSH connection to a single `secrets.VPS_HOST` executing the four git commands above in `REPO_DIR` before `docker compose up -d --build` in a separate off-repo `COMPOSE_DIR`. Upstream side: `14_…md` §6.4 and `DR-C10-S9-1` consequence 5, both at cutoff `0962279`.
- **Consequence:** `DR-C10-S9-1` **stands** and is not re-decided — `15_…md` §6.2 answers `OI-S9-4` with No, and `DR-C10-S9-1`'s revision trigger 2 does not fire. What changes is the argument's robustness: a structural claim holds under every future change to repository visibility, whereas a visibility-conditioned one does not. If charter assumption 32 ever moves — the DP course application ceasing to be private/closed — SUB-9's own revision trigger 3 fires anyway, so the two failure modes now coincide where §6.4 presented them as independent. A reader assessing how much weight §6.4 can bear should read it as conditioned on assumption 32 rather than as independent of it.
- **What is assumed rather than derived:** That `REPO_DIR` on the deployment host is a clone of **this** repository rather than of some other tree is inferred from `git fetch origin` succeeding against a `$SHA` produced by this repository's CI; the host's git remote configuration is off-repo and was not read. Whether the clone is a full clone, a shallow one, or a bare one is not established and does not affect the finding, which turns only on the host holding a working copy at all. No claim is made that the private clone is *insecurely* held — the finding is about the accuracy of a published premise, not about the deployment's security posture.
- **Handed to:**
  - **`SUB-9 (NEU-983)`**, as the owner of the argument, which is **merged and closed** — routed under the accepted `F5.7` residual pattern rather than absorbed into residual uncertainty.
  - **`SUB-12 (NEU-986)`**, for the completeness gate: this is a second instance of the structure `OI-S16-1` names — a finding routed correctly to a recipient that has already shipped.

#### `F-S10-2` — The installed migrator has no serialization, so two boot migrators crash one process; the deploy path has no rollback to recover

- **Id:** `F-S10-2`
- **Finding:** `src/transport/main.ts:27` makes `await initializeDatabase();` the **first** statement of `bootstrap()`, with **no environment guard anywhere**, and `src/infrastructure/db/migrate.ts:38`–`:50` calls drizzle's `migrate()`. The installed migrator is `drizzle-orm` **0.45.1**, and `node_modules/drizzle-orm/pg-core/dialect.js:44`–`:72` shows it (i) creates `drizzle.__drizzle_migrations` with `id SERIAL PRIMARY KEY, hash text NOT NULL, created_at bigint` and **no unique constraint** on `hash` or `created_at`; (ii) decides pending-ness with a plain `select id, hash, created_at … order by created_at desc limit 1` issued **before** the transaction opens — **no `pg_advisory_lock`, no `FOR UPDATE`, no `LOCK TABLE`**; and (iii) then wraps the entire pending-migration loop in one transaction. That is a time-of-check-to-time-of-use race with **no serialization mechanism of any kind**. Against this repository's actual DDL, which is not idempotent (`drizzle/0000_strong_tarantula.sql:1` is a bare `CREATE TABLE`), two concurrent boots during a pending-migration window resolve as: Postgres catalog locking serializes the DDL, the first transaction commits, the second fails with `relation "…" already exists`, its whole transaction aborts, and `src/transport/main.ts:62`–`:65` logs and calls `process.exit(1)`.
- **Evidence:** Direct reading of installed source at `03efe1d` in a dedicated worktree, 2026-08-22: `node_modules/drizzle-orm/package.json` (version exactly `0.45.1`, against `package.json`'s declared `^0.45.1`); `node_modules/drizzle-orm/node-postgres/migrator.js:2`–`:5`; `node_modules/drizzle-orm/migrator.js:3`–`:30`; `node_modules/drizzle-orm/pg-core/dialect.js:44`–`:72`. Repository side: `src/transport/main.ts:27`, `:62`–`:65`; `src/infrastructure/db/migrate.ts:38`–`:50`; `drizzle/0000_strong_tarantula.sql:1`. Zero-match search for a rollback step across `.github/` and `scripts/` at the same cutoff.
- **Consequence:** This is the decisive criterion behind `DR-C10-S10-2` clause 3, which makes migration **exclusive to the MCP core process** and forbids a second replica of it during a pending-migration window. Without that clause the selected deployment shape would carry a known crash-on-boot, and because `.github/workflows/cd-prod.yml` has **no rollback step**, the health poll would fail and `exit 1` would leave the failed state in place. The failure mode is worth stating precisely because it is *not* the one usually feared: for this repository's DDL it is a **crash**, not a silent double-application. Silent double-application is reachable only if a migration's SQL were idempotent, in which case the missing unique constraint would permit a duplicate `__drizzle_migrations` row without error — so making migrations idempotent would trade a loud failure for a quiet one, which is the opposite of an improvement.
- **What is assumed rather than derived:** The failure sequence was **not executed** — no Postgres and no container runtime was reachable in this run (`docker` absent; `127.0.0.1:5432` `ECONNREFUSED`), so no reproduction transcript exists. What is directly observed is the **absence of any lock** in the migrator's source and the **non-idempotence** of the DDL; the resulting sequence follows from Postgres's documented DDL catalog-locking behaviour, which is assumed rather than measured here. That `drizzle-orm` will remain at 0.45.1 is not assumed — the finding is pinned to the installed version and the `^` range permits a future minor that adds locking, which is `DR-C10-S10-2` revision trigger 3.
- **Handed to:**
  - **`NEU-896`**, which owns the implementation and must either honour clause 3 or introduce an externally-locked migration step.
  - **`SUB-12 (NEU-986)`**, for the completeness gate: this finding is the sole support for a decision clause, and a gate reading the clause should be able to reach the evidence.

#### `F-S10-3` — Process-local session state makes a second replica reject in-flight sessions outright, with two distinct error shapes

- **Id:** `F-S10-3`
- **Finding:** `src/transport/http.ts:82`–`:83` declares `const transports = new Map<string, StreamableHTTPServerTransport>()` and `const sessionIdentity = new Map<string, SessionIdentity>()` inside the `startHttpTransport()` closure — pure in-process memory, with **no external or shared store anywhere in `src/`**. A process that did not create a given session therefore always misses on `transports.get(sessionId)` and takes one of two explicit rejection paths: **POST `/mcp`** returns **HTTP 404** with JSON-RPC error code **`-32000`**, message `Session not found` (`:226`–`:232`, whose own comment reads "Stale or unknown session ID — 404 lets clients know to re-initialize"); **GET/DELETE `/mcp`** returns **HTTP 400**, plain text `Invalid or missing session ID` (`:245`–`:250`). The same process-local pattern holds for three further pieces of state: rate-limit windows (`src/transport/rate-limit-middleware.ts:58`, whose comment states "State is per-instance and in-memory: the deployment assumption is a single server instance. Multiple replicas would each keep their own counters, multiplying the effective limit by the replica count"), the Tier-2 breaker's tripped set, cache and in-flight map (`src/orchestration/tier2-circuit-breaker.ts:68`–`:76`), and the audit and event transports' buffers and breakers (`src/transport/pg-audit-transport.ts:45`, `:48`–`:52`; `src/transport/pg-event-transport.ts:15`–`:38`).
- **Evidence:** Direct reading at `03efe1d` in a dedicated worktree, 2026-08-22, of each cited line. Corroborating: `.env.example:79`–`:81` states the single-instance assumption in prose; zero-match search for Redis or any shared state store across `src/` at the same cutoff.
- **Consequence:** This is the evidence behind `DR-C10-S10-2`'s rejection of horizontal replication (rejected alternative 3) and behind `15_…md` §5.5's resolution of three of `OI-S13-1`'s four named cross-category interactions — `SC-S3-20` (per-process counters multiplied by instance count), `SC-S3-21` (breaker state diverging across replicas) and `SC-S3-18` (MCP session affinity). `08_…md` §8.4 offered two options for each: a shared store, or accept the divergence. `DR-C10-S10-2` clause 2 takes a third — **one instance of each process** — which makes the divergence unreachable rather than mitigated. The resolution is therefore exactly as strong as clause 2's premise, and `CAP-S10-3` records that the actual instance count is not discoverable from the repository.
- **What is assumed rather than derived:** The two rejection paths are read directly from the handlers and are not inferred, but **no multi-replica deployment was stood up** to observe them — no container runtime was available. What a load balancer would do in front of two replicas (round-robin, sticky, or something else) is not established and is not a repository fact. The finding also does not establish that sticky routing would be *sufficient*: it would address `SC-S3-18` while leaving `SC-S3-20`'s counter multiplication and `SC-S3-21`'s breaker divergence untouched, since those do not follow a session.
- **Handed to:**
  - **`NEU-896`**, which owns the implementation and would have to introduce a shared store before any replication.
  - **`SUB-12 (NEU-986)`**, for the completeness gate, as the support for three of `OI-S13-1`'s four interaction resolutions.

#### `F-S10-4` — `F-S14-5`'s premise does not hold: selecting the store does not settle `SC-S3-31`'s record shape

- **Id:** `F-S10-4`
- **Finding:** `F-S14-5` records `SC-S3-31`'s Concurrency cell as reading: two assessments of the same `node_id` + `skill_type` "must serialize **if the record is an aggregate**; append-per-event has **no race**. Which of the two shapes applies is not determined by any merged input — **SUB-10 decides it with the store**." The premise in that last clause is false. **Both shapes are expressible in one Postgres instance**, and neither is excluded by the selected topology; choosing where the bytes live does not choose whether a row is an aggregate updated in place or an append-only event stream. That is **record shape**, which is schema design — explicitly out of `OUT-8`'s scope (`15_…md` §1, §12). Selecting a store therefore settles `SC-S3-31`'s **destination** and leaves its **shape** exactly where `F-S14-5` found it.
- **Evidence:** `F-S14-5` read at `03efe1d`; `10_…md` §8's `SC-S3-31` row and `08_…md` §8.6's migration-path cell for the same row, which independently records "Store shape is **`OI-S13-1`** (SUB-10)". `01_outcome-register.md` `OUT-8`, whose scope excludes schema design. `DR-C10-S10-1` § Decision, which selects a store and no record shape.
- **Consequence:** No shape is asserted. `15_…md` §5.4 records `SC-S3-31`'s destination as the shared store, `public` schema, and routes the shape question rather than answering it — which keeps the alternative that "asserting is not an available third option" out of this chapter. **SUB-14's walk counts are preserved unchanged: 180 walks, 178 defined, 2 undefined.** Had a shape been asserted here, `SC-S3-31`'s conflicting-write walk would have become defined on this sub-task's say-so rather than on evidence, which would have silently changed a merged sibling's published count.
- **What is assumed rather than derived:** That both shapes are expressible in one Postgres instance is a general property of relational stores rather than a measured fact about this schema; no aggregate and no append table was created to demonstrate it. The finding does **not** claim to know which shape is correct, nor that the question is unimportant — only that a store decision is the wrong instrument for it. Whether some *other* input this sub-task holds could settle it was checked against the seven atomicity rows and the authority matrix and found not to; that check is a reading, not a proof of exhaustion.
- **Handed to:**
  - **`SUB-14 (NEU-978)`**, as the finding's origin, which is **merged and closed** — a third instance of the `OI-S16-1` backwards-routing structure. Note that `F-S14-5` was itself handed to SUB-16 as **NEU-980**, which is a mis-pairing and an `F-S16-1` instance; this chapter neither repairs nor propagates it.
  - **`NEU-896`**, which will hold both the schema and the concurrency requirement at implementation time and is the first party able to settle the shape.
  - **`SUB-12 (NEU-986)`**, for the completeness gate, since `OI-S13-1` and `F-S14-5` both pointed at this sub-task and only one of them is dischargeable here.

#### `F-S10-5` — `CC-S8-2` designates token-bound identity reusable-core, but the implementation is welded to Express with no port to reuse

- **Id:** `F-S10-5`
- **Finding:** `CC-S8-2` places token-bound identity on the reusable-core, public-MIT side of the distribution line, and `15_…md` §8.1 answers the identity make-or-reuse record **reuse-from-core** on that basis. **The surface that answer names does not exist.** Identity is implemented entirely inside `src/transport/` and `src/config/` and terminates in an Express-specific mechanism: `src/transport/jwt-middleware.ts`, `src/transport/prm-handler.ts` and `src/config/resolve-auth-config.ts` produce `res.locals.auth`, consumed directly at `src/transport/http.ts:59`, `:206` and in `src/transport/rate-limit-middleware.ts`. There is **no `ports/auth-port.ts` or equivalent**, and **nothing under `src/ports/` or `src/domain/` references identity at all**. A consumer of the reusable core cannot obtain a verified principal without also taking Express and the HTTP transport.
- **Evidence:** Direct reading at `03efe1d` in a dedicated worktree, 2026-08-22, of each cited file and line, plus a zero-match search for identity or auth references across `src/ports/` and `src/domain/`. Verification mechanism read at `src/transport/jwt-middleware.ts:6`–`:51` (OIDC discovery against `${issuer}/.well-known/openid-configuration`, `jwks_uri` resolved from it) and `:114`–`:131` (`jwtVerify` against the discovered JWKS, subject resolved as `sub` falling back to `azp`). Config binding at `src/config/resolve-auth-config.ts:105`, `:108`.
- **Consequence:** The reuse answer stands — nothing else in evidence would justify a second verifier, and building one would create two places for `CC-S8-2`'s meaning-narrowing to diverge on a surface `13_…md` §6.3 records that **nothing** catches. What the answer acquires is a **disclosed precondition**: extraction to a transport-neutral form is an implementation obligation the answer creates, not a step that already exists. This compounds with `OI-S8-2`, which records that STDIO has no gate to extend and `CC-S8-3` is unowned — so the reuse answer inherits one obligation whose subject must be built and another whose subject does not exist. Both are stated at `15_…md` §8.1 as the price rather than discounted.
- **What is assumed rather than derived:** That extraction is *feasible* is not established — no refactor was attempted and none is specified here; the finding observes the current coupling, not its cost to remove. `resolve-auth-config.ts:105` returns `null` for STDIO, so the absence of a STDIO gate is directly observed rather than inferred, but whether a transport-neutral port would make one *possible* is a design question this sub-task does not answer. The `sub`-then-`azp` subject resolution is a deliberate design choice for client-credentials grants and is **not** reported here as a defect; `DR-C10-S8-2:178` already records the semantic consequence ("the principal is an OAuth client, not necessarily a person — `fails-principal`") and this finding neither restates nor extends it.
- **Handed to:**
  - **`SUB-8 (NEU-981)`**, as the owner of `CC-S8-2` and the distribution line, which is **merged and closed** — routed under the same residual pattern as `F-S10-1`.
  - **`NEU-896`**, which owns the extraction and the `OI-S8-1` / `OI-S8-2` implementation work this depends on.

#### `F-S10-6` — `DR-C10-S6-1`'s "all 45 categories" headline contradicts the republished matrix's assignment of `SC-S3-33`/`SC-S3-34` to `CMP-S4-17`

- **Id:** `F-S10-6`
- **Finding:** Two merged artifacts in this package state incompatible things about the same two rows. `DR-C10-S6-1` is titled "The MCP core is the exclusive writer of every state category (all-MCP)" and its decision reads, at `:12`–`:13`, "**the MCP core is the exclusive writer of all 45 categories** in `../04_…md` §3". The **republished** authority matrix assigns `SC-S3-33` (cached citation-drift verdict) and `SC-S3-34` (citation-drift verdict store) to **`CMP-S4-17`**, not the MCP core, at `10_…md:741`–`:742`; and `10_…md:227` — SUB-16's own disposition of `F-S14-9` — calls `CMP-S4-17` the cache's "**only writer**". If `CMP-S4-17` is the only writer of `SC-S3-33`, the MCP core is not the exclusive writer of all 45. The two readings cannot both hold, and **neither artifact acknowledges the other on this point**.
- **Evidence:** Direct reading at `03efe1d`, 2026-08-22: `decision-records/DR-C10-S6-1_state-ownership-model.md:1`, `:12`–`:13`; `10_republished-authority-matrix.md:227`, `:741`–`:742`. `08_…md:884` independently corroborates the matrix reading with its prohibition on co-locating `SC-S3-34` "under an authority that would give any other component a write path to it".
- **Consequence:** It is **not** a store-topology consequence — `DR-C10-S10-1` clause 5 places these rows under `CMP-S4-17` either way, because `08_…md` §8.6's prohibition is independent of which artifact names their authority. The consequence is to **one ground of one argument**: ground 3 of `DR-C10-S10-1`'s "Why the carve-out does not reopen the reversal" holds only under the matrix reading. If the headline reading governs instead, `07_…md` §5.2 **row 4** ("a separate store for MCP core state — not evaluated → raise a finding to SUB-6") fires for the carve-out and the store-reversal check must be re-run for it. **Grounds 1 and 2 are independent of the reading**, so the reversal *conclusion* — selection stable — survives either way; what changes is whether a routing obligation was owed. That contingency is stated in the record itself rather than left for a reader to discover.
- **What is assumed rather than derived:** Which reading is correct. This sub-task **does not decide it** and takes the matrix only as the operative reading for its own record, on three stated grounds — `10_…md` is the later artifact, it is the republished one, and it carries SUB-14's validation and SUB-16's disposition. That is a precedence argument, **not** a demonstration that `DR-C10-S6-1`'s headline is wrong; it is equally possible the headline is right and the matrix rows are the defect, in which case `F-S13-1` (already open against `SC-S3-33`'s tie-break, owner SUB-6) is the closer relative. No claim is made either way, and **no upstream artifact is amended here.**
- **Handed to:**
  - **`SUB-6 (NEU-976)`**, owner of `DR-C10-S6-1` and already the named owner of the unclosed `F-S13-1` on `SC-S3-33`'s tie-break — **merged and closed**, so routed under the same accepted `F5.7` residual pattern as `F-S10-1` and `F-S10-5`.
  - **`NEU-985 (SUB-11)`**, whose audit reads both artifacts and would otherwise have to rediscover the conflict.

### SUB-11

*Seven findings from running the package's four mechanical audits (`16_mechanical-audits.md`). **Every one names an owning sub-task, and every routing was verified against the tracker child map restated at `16_…md` §8.1** — never against the sub-task label written in the file that raised the item, because `F-S11-4` establishes that 38 such labels in this package name the wrong id. Stated once, here: **not one of the seven is repaired.** The registers are append-only; twelve of the thirteen topic chapters are merged; and — the reason that matters — a repair destroys the audit's own evidence. Had the 159 dangling paths been quietly fixed, the fact that they are **one mechanism across twelve sub-tasks** would have been invisible and the package would have shipped with the generating mechanism intact and no record it existed. **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended.** `F-S10-6` is left as the open routed finding SUB-10 filed it as; this audit does not adjudicate it.*

#### `F-S11-1` — 161 of the package's 1,420 path citations do not resolve, and 159 are one mechanism: a relative path wrong by exactly one directory level

- **Id:** `F-S11-1`
- **Finding:** `00_method-and-provenance.md` §2 states the rule and names the auditor: *"A path that does not resolve on the branch under review is a defect, not a typo — `NEU-985 (SUB-11)` checks this mechanically."* It was checked. **1,420 backticked path citations were extracted from all 61 package files and resolved relative to each citing file's own directory. 1,248 resolve. 172 do not. 11 of those are by-design non-claims** (itemised at `16_…md` §5.4 — filenames that must *not* exist, naming-convention templates, and bare forms named in order to say they must not be resolved). **161 are genuine defects, and 159 of them are the same error in two mirror-image directions.** **C1 — spurious `../` (129):** a **package-root** file writes `../04_…md` for a package-root sibling, resolving to `docs/research/04_…md`, which does not exist; the correct form is the bare name. **C2 — one `../` short (30):** a file in `decision-records/` or `traceability/` writes `../C009-…/…` for an upstream package, resolving to a sibling of this package's root; the correct form needs `../../`. **C3 (2):** `04_…md` cites two upstream filenames bare, with no directory at all. Every one of the 159 is off by **exactly one level**, and the two classes are off in opposite directions — root files adding a level they do not need, subfolder files omitting one they do.
- **Evidence:** Extraction and file-relative resolution over all 61 `.md` files at cutoff **`ecf88c5`**, with every non-resolving citation hand-classified. C1 citing files: `02_findings-register.md` ×47, `90_open-items-and-provisional-register.md` ×37, `91_caps-and-incomplete-scope.md` ×24, `92_spike-register.md` ×19, `13_architecture-material-rule-and-web-tier-decisions.md` ×2 — 21 distinct targets. C2 citing files: `traceability/S2_execution-environment-and-drift-coverage.md` ×11, `traceability/S3_state-category-inventory-coverage.md` ×6, `traceability/S4_component-and-boundary-coverage.md` ×1, `decision-records/DR-C10-S2-2_authoring-time-execution-boundary.md` ×5, `DR-C10-S2-1_no-learner-facing-execution-environment.md` ×4, `DR-C10-S2-3_out-of-band-citation-drift-component.md` ×3 — 11 distinct targets. C3: `04_state-category-inventory.md` ×2. **Owner tally**, attributed by file ownership or, in the shared registers, by the nearest preceding `### SUB-<n>` heading: SUB-2 **43**, SUB-6 **32**, SUB-3 **23**, SUB-13 **17**, SUB-4 **14**, SUB-5 **7**, SUB-15 **7**, SUB-7 **6**, SUB-9 **5**, SUB-14 **3**, SUB-16 **3**, SUB-10 **1**. Separately: **0** citations overrun a file's end, and **7 of 7** cited commit refs resolve. Full method and counts at `16_…md` §5.
- **Consequence:** Both classes degrade gracefully for a reader *inside* the package — `../04_state-category-inventory.md` is obviously the sibling, and a human recovers in one step. The cost lands squarely on the two consumers **`OUT-12` was written for**. A **link checker** reports 161 broken references and cannot separate them from genuinely dangling citations, so the mechanical check `OUT-12` invites is useless until they are fixed. And for the **cold reader working only from the published package**, C2 is materially worse than C1: its target is an *upstream package the reader may not know exists*, so a failed resolution reads as "the evidence is missing" rather than "the path is one level short". **Twelve of the sixteen sub-tasks are represented**, which is why this is filed as a package property with a class-level owner rather than as twelve independent slips: a defect this evenly distributed is a missing convention and a missing check, not twelve authors being careless.
- **What is assumed rather than derived:** Nothing about the counts — every citation is resolved mechanically and every non-resolving one is classified by hand with its file and line recorded. **Not** adjudicated: whether the correct repair is an erratum, a standing citation convention with a mechanical check, or both. That is a package-level decision this sub-task does not own.
- **Handed to:**
  - **`SUB-12 (NEU-986)`**, at the completeness gate, which is the only party positioned to decide erratum-versus-convention and the only one that can act across merged siblings. It now owes that decision on **two** mechanically-detectable classes at once — this one and the 38 id mis-pairings of `F-S11-4`.
  - **Each named owner in the tally above**, for the count attributed to it.
  - **Not repaired here.** The files are merged and the discipline is append-only, so the finding is the correction — and the *systematic* character of the defect, which is the useful part, is only visible because it was not quietly fixed.

#### `F-S11-2` — `04_…md` cites two upstream C009 documents by bare filename, with no directory at all

- **Id:** `F-S11-2`
- **Finding:** `04_state-category-inventory.md` cites `01_evidence-taxonomy.md` and `09_enforceable-quality-system.md` as bare filenames. Both are **C009 documents** living in `../C009-course-content-quality/`; neither resolves from this package, and neither has a full-path form anywhere in the citing file. This is separated from `F-S11-1`'s C1/C2 classes because it is a **different error**: those are off by one directory level, this one carries no directory information at all.
- **Evidence:** `04_state-category-inventory.md`, two occurrences, at cutoff `ecf88c5`. Both targets verified to exist at `docs/research/C009-course-content-quality/`.
- **Consequence:** A reader inside this package resolves a bare `09_enforceable-quality-system.md` against **this** folder first, where a `09_` document exists — `09_authority-matrix-validation.md`. The failure mode is therefore not a dangling link but a **plausible wrong resolution to a same-numbered sibling in the wrong package**, which is worse: it fails silently and confidently. This is the same hazard `14_…md:518` and `90_…md:504` each name explicitly when they warn that `03_review-log.md` must not be resolved against this folder — the hazard is already known to the package, and these two citations walk into it.
- **What is assumed rather than derived:** Nothing. Both bare names are read at their locations and both targets are verified present in the upstream package.
- **Handed to:**
  - **`SUB-3 (NEU-973)`**, as the author of `04_…md`.
  - **`SUB-12 (NEU-986)`**, folded into the same erratum-or-convention decision as `F-S11-1`.

#### `F-S11-3` — `OUT-11` and `OUT-12` have no traceability row, and both are substantively discharged

- **Id:** `F-S11-3`
- **Finding:** The traceability set holds **13 files and 259 rows**, and the union of the outcomes they declare is **`OUT-1` … `OUT-10`**. **`OUT-11`** (numbered, tolerance-bounded stand-in assumptions handed to NEU-896) and **`OUT-12`** (the C005 house style, standalone and cold-readable) have **no row in any file**. The cause is recorded by the folder itself, at `traceability/README.md:7`: *"**This folder is empty of registers.** SUB-1 declared the shape and wrote none — it discharged no outcome's content."* `OUT-11` and `OUT-12` are **SUB-1's own outcomes**; every other outcome was picked up by a sub-task that wrote a file for it, and these two were not because SUB-1 wrote none. **Unmatched in the reverse direction is 0** — all 13 topic chapters declare their outcomes correctly — so the gap is one-directional.
- **Evidence:** Enumeration of every `traceability/S*.md` file's declared outcomes and every table row at cutoff `ecf88c5`, matched against `01_outcome-register.md`'s twelve ids. **Both outcomes are substantively discharged, and this audit verified it rather than assuming it.** `OUT-11`: `93_stand-in-assumption-register.md` is **closed** with exactly five entries, four packages covered (NEU-891 ×2, NEU-892, NEU-893, NEU-894); audited against `OUT-11`'s own verification clause, all five carry a package reference, a tolerance envelope, an invalidating outcome, a named re-validation trigger and `[unconfirmed]` status — **5/5, zero missing fields** — and the clause's decision-level half also passes, each of `A-25`…`A-29` being cited outside the register in **22, 9, 39, 26 and 21** files respectively, so not one is appendix-only. `OUT-12`: its house-style and standalone halves are discharged by `00_…md` §3 and the package's shape; its citation-audit half is discharged by `16_…md` §5 and **fails** — `F-S11-1`.
- **Consequence:** **The finding is not "two outcomes nobody covered."** It is that two outcomes are discharged with **no row recording it**, and `94_package-completeness-gate.md:29` reads the traceability set to establish that every one of `OUT-1` … `OUT-12` is covered. Read mechanically, that gate item **fails on a package whose content does not** — the worst shape a gate result can take, because the honest answers ("covered" and "no row") point opposite ways and a gate that resolves it by inspection has stopped being mechanical. There is a second, quieter cost: `OUT-11`'s handoff to **NEU-896** is the one place the package promises a downstream charter an explicit reconciliation list, and the row that would have pointed NEU-896 at `93_…md` is exactly the row that is missing.
- **What is assumed rather than derived:** Nothing. The absence is established by enumeration; the substantive discharge is established by auditing each outcome's own verification clause against the artifact that answers it.
- **Handed to:**
  - **`SUB-1 (NEU-971)`**, author of both outcomes and of the empty folder — **merged and closed**, so routed as a residual rather than as work.
  - **`SUB-12 (NEU-986)`**, at the completeness gate, as the only party that can record how the twelve-of-twelve item is answered without renumbering or rewriting a merged sibling. Note that the answer is **not** "write the two missing rows": doing so would put SUB-1's coverage in a file SUB-1 did not write, which the folder's own allocation rule forbids.

#### `F-S11-4` — `F-S16-1` undercounts by one, and the missed mis-pairing sits on the row that warns against conflating two audits

- **Id:** `F-S11-4`
- **Finding:** `F-S3-2` and `F-S16-1` each hand this sub-task the **mechanical package-wide label↔id check**. It was run over **843 pairs**. **61 occurrences do not match the tracker child map** — but a raw mismatch count is not a drift count, and three separate classes had to be removed before the number meant anything: **19 corrective quotations** (a file quoting the wrong pairing *in order to name it as wrong* — `F-S16-1`'s own entry, `10_…md` §5.7's citation, `08_…md:1432`, `90_…md:269`, `traceability/S16_…md:46`), **3 correct cross-charter references** (*"**NEU-890's** SUB-10 (NEU-966)"*, explicitly qualified), and **1 ambiguity** (`F-S11-7`). **38 are genuine mis-pairings. 37 are already filed:** `F-S3-2`'s 13 and `F-S16-1`'s 24 both **reproduce exactly, location for location**. **The thirty-eighth is new.** `09_authority-matrix-validation.md:1342`, in SUB-14's §17 handoff table, writes **`SUB-7 (NEU-982)`** — SUB-7 is **NEU-980**; **NEU-982 is SUB-15**. It is a **fourth** mis-paired family, in the same table and the same file as the three `F-S16-1` records, and `F-S16-1` names three families where there are four and counts 24 where there are 25.
- **Evidence:** Pair extraction across all 61 files at cutoff `ecf88c5`, matched against NEU-895's child map — **SUB-1 → NEU-971, SUB-2 → NEU-972, SUB-3 → NEU-973, SUB-4 → NEU-974, SUB-5 → NEU-975, SUB-6 → NEU-976, SUB-7 → NEU-980, SUB-8 → NEU-981, SUB-9 → NEU-983, SUB-10 → NEU-984, SUB-11 → NEU-985, SUB-12 → NEU-986, SUB-13 → NEU-977, SUB-14 → NEU-978, SUB-15 → NEU-982, SUB-16 → NEU-979** — restated in full here and at `16_…md` §8.1 because it lives in the tracker rather than in a readable file. Every one of the 61 mismatches was read in context to classify it. The new occurrence is `09_authority-matrix-validation.md:1342`. The class is confined to **two authors across 10 files**; the other fourteen sub-tasks pair correctly throughout.
- **Consequence:** **The consequence of this one occurrence is the sharpest in the whole class**, which is why it is filed rather than folded into `F-S16-1` as a count correction. The row it sits on is *the* row where the package states the `OUT-3`/`OUT-5` distinction most explicitly — SUB-7's **resource**-inventory↔matrix cross-check is a different audit over a different inventory than SUB-13's **state**-inventory↔matrix audit, *"named distinctly here so the two are never conflated."* A consumer routing by tracker id delivers that notice to **SUB-15 (NEU-982)**, whose chapter is the architecture-material rule and the web tier, and **SUB-7 (NEU-980)** — the party whose `OUT-5` cross-check the notice is about — never receives it. **A warning against conflating two audits, mis-delivered by the same defect class the warning sits beside.** More generally: the mechanical check `F-S3-2` and `F-S16-1` both asked for has now been run, and it **confirms both exactly** while finding one family neither recorded. A class with three sightings, an exact reproduction, and a fourth family found only by the mechanical pass is settled as a package property.
- **What is assumed rather than derived:** Nothing. Every pair is read at its location and every mapping is read from the tracker. **Not** adjudicated: whether the three unqualified-versus-qualified `NEU-890's SUB-10` forms should be normalised — that is `F-S11-7`, filed separately because it is an ambiguity rather than a wrong id.
- **Handed to:**
  - **`SUB-14 (NEU-978)`**, as author of the row — **merged and closed**, routed as a residual.
  - **`SUB-16 (NEU-979)`**, whose `F-S16-1` undercounts by one and names three families where there are four. **Not an error in its method** — its evidence and its three families are exact; the fourth is an omission a targeted read would not catch and a mechanical pass does.
  - **`SUB-7 (NEU-980)`**, which is the **actual addressee** of `09_…md:1342` and is named here by sub-task rather than by the id written in the file, so the notice reaches it.
  - **`SUB-12 (NEU-986)`**, at the completeness gate, which now owes the erratum-or-convention decision on this class and on `F-S11-1`'s together.

#### `F-S11-5` — `01_outcome-register.md` publishes the charter's superseded tool and repository figures in 11 places with no forward pointer

- **Id:** `F-S11-5`
- **Finding:** `01_outcome-register.md` carries the charter's original figures inside two success measures. **`OUT-6`** (`:77`) reads *"each of the **45** tools and 3 prompts"*, with "42 gated" in the body; the package's frozen figure is **46 tools / 43 gated / 3 exempt / 49 audit entries** (`F-S8-1`, refining `F-S5-3`). **`OUT-7`** (`:85`) reads *"165 TypeScript source files, ~25,200 lines, 197 test files"*; SUB-9's re-measurement gives **169 / 26,816 / 202**, with 25 migrations. **11 occurrences across the file.** The two cases are **not the same kind of divergence**, and the register does not distinguish them: `OUT-7`'s figures are **stale** — the codebase grew — while `OUT-6`'s is a **miscount**, one tool that was always there and was never counted.
- **Evidence:** Figure scan across all 61 files at cutoff `ecf88c5`, each hit read for whether it **asserts** the figure or **restates** it. **Every live use elsewhere in the package is correct**: `11_`, `12_`, `13_` and `15_` all write 46 / 43 / 3 / 49 and cite `F-S8-1`; the only other 45/42 occurrences are in `02_findings-register.md`'s own finding recording the miscount. So the drift is confined to `01_…md`.
- **Consequence:** A reader resolving `OUT-6`'s success measure against `src/` today finds **46** registered tools and concludes either that the outcome is unmet or that the codebase has drifted from the register — and **nothing in `01_…md` points at `F-S8-1`**, where the discrepancy is resolved and explained. The same reader resolving `OUT-7` finds 169 files and 26,816 lines. The outcome register is the **first file a cold reader opens** and the one every traceability row resolves an outcome id into, so a discrepancy here is met before any of the chapters that would explain it. This is emphatically **not** a correction to make: the register is merged, and `00_…md` §3 grants SUB-1 exactly two restatement edits, both already made and both recorded in its own restatement note.
- **What is assumed rather than derived:** Nothing. Both frozen figures are consumed from `F-S8-1` and from SUB-9's re-measurement rather than re-derived here, per `16_…md` §2 — an audit that re-measured them would be contradicting the package it is auditing.
- **Handed to:**
  - **`SUB-1 (NEU-971)`**, author of the register — merged and closed, routed as a residual.
  - **`SUB-12 (NEU-986)`**, at the completeness gate, which reconciles figures across the package and is the party that can record the forward pointer without editing a merged outcome's wording.

#### `F-S11-6` — Four product-domain terms this package coined have no `docs/GLOSSARY.md` row, and one is cross-referenced from a row that does exist

- **Id:** `F-S11-6`
- **Finding:** The package added **17** rows to `docs/GLOSSARY.md` under `## Architecture components (C010 system & repository architecture)`, all verified present and well-formed. **Four coined product-domain terms have no row:** **`state category`** (`04_state-category-inventory.md`; 45 ids `SC-S3-1` … `SC-S3-45`, the package's most-used domain noun), **`read-projection`** and **`write-intent`** (`11_…md` §9; 10 files each), and **`split-visibility workspace`** (`14_…md`; the **selected** repository topology `T2`; 8 files). `state category` is the sharpest case: the glossary's own **`isolation invariant`** row lists `state category` in its Cross-refs cell, **so the glossary already points at a term it does not define.**
- **Evidence:** Row-by-row verification of the C010 glossary section at `docs/GLOSSARY.md:194` against every term the package coins, at cutoff `ecf88c5`, with per-term usage counts across the package. **Terms considered and correctly excluded are published at `16_…md` §9.2** so this check is distinguishable from one that was not run: `architecture-material`, `tolerance envelope`, `open item`, `cap`, `spike`, `stand-in assumption`, `package-completeness gate` and `invalidating outcome` are **governance vocabulary**, excluded by `00_…md` §4.1 and by the glossary section's own preamble; `trust boundary`, `process boundary`, `negative boundary` and `make-or-reuse` are charter and outcome vocabulary the package **consumes** rather than coins; and `authority` and `target state` **do** have rows, written with qualifiers (`` `authority` (of a state category) ``), so a naive exact-match check reports them missing when they are not.
- **Consequence:** `docs/GLOSSARY.md` is the repository's stated one-hop domain-term lookup, and `CLAUDE.md`'s standing rule is that introducing a domain term adds its row **in the same change**. A reader meeting `SC-S3-31` or a `write-intent` entry in an implementation charter has no one-hop resolution and must find the coining chapter by search. `split-visibility workspace` is the costliest of the four for a different reason — it names the **selected** topology, so it is the term a downstream implementation charter is most likely to encounter first and least likely to guess correctly, "split visibility" being a phrase with an obvious-but-wrong reading.
- **What is assumed rather than derived:** Whether each of the four is a **product-domain** term rather than governance vocabulary. The line is `00_…md` §4.1's and it is applied here rather than re-decided; §9.2's exclusion list is published so the application is checkable. `state category` and `split-visibility workspace` are unambiguous; `read-projection` and `write-intent` are closer to the line, being API-surface vocabulary, and are routed with that noted.
- **Handed to:**
  - **`SUB-3 (NEU-973)`** for `state category`; **`SUB-7 (NEU-980)`** for `read-projection` and `write-intent`; **`SUB-9 (NEU-983)`** for `split-visibility workspace` — each as the coining sub-task, all merged and closed, so routed as residuals.
  - **`SUB-12 (NEU-986)`**, which is the only remaining party that can add a row without rewriting a merged chapter, and which must weigh it against the fact that a glossary row is a change to a **tracked file outside the package**.

#### `F-S11-7` — `03_…md:189` writes `SUB-10 (NEU-966)` unqualified in a chapter that also uses `SUB-10` for this charter's SUB-10

- **Id:** `F-S11-7`
- **Finding:** `03_execution-environment-and-citation-drift-component.md:189` reads *"both consumed from **SUB-10 (NEU-966)** rather than re-derived"*. **NEU-966 is a child of NEU-890, not of this charter**, and the reference is **correct** — the window and budget are NEU-890's. But it is written **without the `NEU-890's` qualifier** that the same author uses at `03_…md:255` (*"**NEU-890's** SUB-10 (NEU-966)"*), `DR-C10-S2-3:30` and `:62`. The same chapter writes **`SUB-10 (NEU-984)`** for **this** charter's SUB-10 at `:252` and `:253`, 63 lines later. **This is not a wrong id.** It is a correct id whose charter is left implicit, in a chapter that uses the identical label for a different sub-task in a different charter on the same page.
- **Evidence:** All four `SUB-10 (NEU-966)` occurrences read in context at cutoff `ecf88c5`: three carry the `NEU-890's` qualifier and one does not. This audit's first pass counted all four as label↔id drift; inspection showed three were correct cross-charter references and the fourth merely unqualified. **Filing all four as drift would have been a manufactured finding**, and the correction is recorded here because the distinction is the whole content of this entry.
- **Consequence:** Small and bounded, and stated as such rather than inflated. A reader who has not yet reached `:255` reads `:189` as naming **this** charter's SUB-10 — **NEU-984**, whose chapter is the substrate decisions and make-or-reuse records, a plausible enough source for a declared window and budget that the misreading does not announce itself. The cost is one wrong hop, recoverable at `:255`. It is filed because the package has **three sightings** of a label↔id defect class (`F-S3-2`, `F-S16-1`, `F-S11-4`) and this is the near-miss version of the same hazard: cross-charter labels that collide in the same namespace with nothing but a qualifier separating them.
- **What is assumed rather than derived:** Nothing. All four occurrences are read at their locations, and both charters' SUB-10 assignments are read from the tracker.
- **Handed to:**
  - **`SUB-2 (NEU-972)`**, author of `03_…md` — merged and closed, routed as a residual.
  - **`SUB-12 (NEU-986)`**, folded into the same convention decision as `F-S11-4`: if the package adopts a standing rule that every cross-charter sub-task label carries its charter, this occurrence is what the rule is for.

### SUB-12

*Four findings, from closing the package (`17_package-closure-and-neu-896-handoff.md`) and answering the completeness gate (`94_package-completeness-gate.md`). `F-S12-1` is the C003/NEU-850 decision-ownership collision that `02_…md` § SUB-1's preamble deliberately left unfiled for this sub-task. `F-S12-3` is the one genuinely **unfiled** defect the independent cold read surfaced. **This sub-task lifts one cap — `CAP-S11-1`, by performing and recording the cold read — and lifts no other; it adds no sighting to `CAP-S4-1` and does not adjudicate its disputed sighting count.** **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended.***

#### `F-S12-1` — The C003/NEU-850 decision-ownership collision: three consumed constraints, three dispositions, one partial overlap

- **Id:** `F-S12-1`
- **Finding:** C003/NEU-850 converged three outcomes that overlap this charter's subject, so two authorities could hold the same decision. All three are **consumed constraints** under the charter's assumption 24, each honoured with its source named and none re-decided here. **`NEU-850`'s `OUT-2`** — learner ownership lives in the MCP core schema keyed to the JWT subject, `user_id` `NOT NULL` on every core table, threaded through the nine row-owning repository ports — is consumed at `06_…md` §1. **`NEU-850`'s `OUT-6`** — remove `"private": true` and publish the core under MIT — is consumed at `14_…md` §3.2 as criterion input (b) and made executable by migration step `M5`. **`NEU-850`'s `OUT-7`** — a separate private repository for the cloud tenant/billing/dashboard layer, "zero cloud-business code" in the MIT repo — is consumed unchanged at `14_…md` §7. **The `OUT-7` overlap is partial, and that is the load-bearing part of this record:** `NEU-850`'s `OUT-7` binds the **cloud business layer** and does not name the **DP course application**, whose placement is this charter's own `OUT-7` and is decided by `DR-C10-S9-1`. The two ask the same-shaped question and diverge in subject.
- **Evidence:** `06_…md` §1–§2; `14_…md` §7, §7.1, §7.2; the 2026-08-19 tracker read recording NEU-850 as converged-but-unimplemented; `src/infrastructure/db/schema.ts`, which carries **no ownership column on any table**, re-verified at `03efe1d`. Restated in full and gitignore-independently at `17_…md` §5, because NEU-850 has no package under `docs/research/` — its charter lives in the gitignored `docs/wf-plans/` tree, so a reader of this package cannot open it.
- **Consequence:** NEU-896 converges over a **stated** ownership split rather than a contested one. Three "no amendment routed" dispositions are on record, each falsifiable: `06_…md` §2 names the two amendment candidates that were examined and rejected — Candidate A, the two port-less operational log tables (`SC-S3-16`, `SC-S3-17`), rejected as a *scope* question and filed instead as `OI-S5-1`; Candidate B, that placement is insufficient for isolation, rejected because "necessary but not sufficient" is not a contradiction — and states what would have cleared the bar (evidence that the JWT subject is not a viable ownership key), which was not found. `14_…md` §7.2 does the same for `OUT-6`/`OUT-7`, attaching one mechanical note as `OI-S9-3` rather than as an amendment. **`NEU-850` is converged but unimplemented**, so all three are decisions to honour and **never existing schema facts** — a distinction `04_…md` §2 and §6 enforce and this record preserves. `NEU-35` is noted as the pre-existing tracker duplicate of the C003 topology decision, so the backlog keeps one source of truth; this package does not touch it.
- **What is assumed rather than derived:** Nothing about the constraints themselves — each is read from the tracker with its date. What is **not** established here is whether `NEU-850`'s `OUT-2` ranges over the two port-less log tables (`OI-S5-1`, owner `NEU-850`) and which `package.json` its `OUT-6` means under a workspace (`OI-S9-3`, owner `NEU-850`). Both are handed back rather than answered.
- **Handed to:** **`NEU-896`** as the convergence surface, with `OI-S5-1` and `OI-S9-3` handed to **`NEU-850`** through its own `OUT-1` drift check. **Zero `NEU-850` constraints are re-decided by this package.**

#### `F-S12-2` — The independent cold read was performed and returned a negative verdict: seven questions block implementation, and five name merged-and-closed owners

- **Id:** `F-S12-2`
- **Finding:** The cold read `OUT-1` and `OUT-12` both require — and which `CAP-S11-1` recorded as unperformable by any party inside the package — **was performed**. A freshly-spawned reader inheriting none of this run's context, pointed at a filesystem copy holding only the 63 published package files and barred from the repository, `_local/`, `docs/wf-plans/`, the run scoreboard, git, the tracker and the web, answered the six questions put to it. Its verdict, verbatim: **"No. I could not start implementing from this package without asking a question."** Its reason, verbatim: *"Not because of the deliberate stop in Q5 — that is honest and correctly scoped. I would be blocked by contradictions about who writes state, which is the one thing an architecture package exists to settle, and which this package's own `OUT-3` promises to settle."* **Seven blocking questions** are recorded verbatim at `17_…md` §2.6: `SC-S3-17`'s two writers (`F-S14-8`); `SC-S3-33`/`SC-S3-34`'s writer (`F-S10-6`); `SC-S3-41`'s status (unfiled — `F-S12-3`); the deletion owner for `SC-S3-16`/`SC-S3-17` (`CAP-S4-1`); clause 4's zone for `CMP-S4-2` (`F-S13-2`); what `F5.5` says (a durability gap, answered at `17_…md` §7.6); and whether `04_…md:70`'s count is 41 or 45 (`F-S4-2`).
- **Evidence:** `17_…md` §2 in full — §2.2 states the isolation actually enforced, §2.3 quotes verbatim the reader's own unsolicited disclosure that the harness injected material it did not seek and could not decline (a SessionStart hook, two task-completion notices and a tool listing) together with its own judgement that *"I consider the exercise intact, not void — but the injections happened and you should know"*, §2.4 records what it **could** reach, §2.5 the verdict, §2.6 the seven questions, §2.7 the non-blocking gaps. Its closing confirmation: *"No files were read outside the package. No file was edited, created, or modified. The package is byte-identical."*
- **Consequence:** **`CAP-S11-1` is lifted, because the record it asks for now exists — and lifting it does not mean the package passed the test.** Three gate items fail on this result: `G-3` (naming each boundary's owner without asking — 15 of 17), `G-41` (the cold-read review itself) and `G-52` (the standalone property). The cap's own text anticipated exactly this: *"a cold read that stalls does not merely fail to lift it: it identifies which specific hop is unreachable, which is a more useful result than the cap."* Five of the seven questions name an owning sub-task that is **merged and closed**, and the registers are append-only, so no remaining pass in C010 can answer them — that is `CAP-S12-1`, owner `NEU-896`.
- **What is assumed rather than derived:** The isolation is **strong but not hermetic**, and this is recorded rather than smoothed over. The injected material carried two tracker ids and a process constitution; it carried no package content, no finding, no figure and no answer to any of the six questions, and the isolation that matters — that the reader had not read the package, its charter or this run's reasoning — held. **The cold read is a proxy signal** under `00_…md` §1.1, never external-user or expert validation.
- **Handed to:** **`NEU-896`**, as `CAP-S12-1`. Each of the seven questions is additionally routed to its owning sub-task as a residual at `17_…md` §2.6, so none is routed to a closed owner alone.

#### `F-S12-3` — `04_…md:452` assigns `SC-S3-41` to stand-in `A-28`, where `A-28`'s row is `SC-S3-45`; the same file classifies `SC-S3-41` as `required-by-upstream` in three places

- **Id:** `F-S12-3`
- **Finding:** `04_state-category-inventory.md:452` attributes the `assumed` status and stand-in **`A-28`** to **`SC-S3-41`**. Three other places in the same file classify `SC-S3-41` as **`required-by-upstream`** — `:177`, `:316` and `:533` — and `:205` shows that the row carrying `assumed — A-28` is **`SC-S3-45`**, whose subject (a server-side-enforceable identity mapping) is what `:452`'s sentence is actually about. This is a **transposition of two ids one row apart in a status table**, and it is **filed nowhere in the package**: it appears in no finding, no open item, no cap, and neither SUB-14's validation nor SUB-16's disposition pass nor SUB-11's four audits reach it.
- **Evidence:** The independent cold reader surfaced it as its third blocking question — *"§4 says `SC-S3-41` is `assumed` under `A-28`, but §3.6 lists it as `required-by-upstream` and shows `SC-S3-45` as the `A-28` row. Which id is wrong — and does `SC-S3-41` have an assumption backing it or not?"* — and it was then independently verified against all five locations at cutoff `3352c00`, and searched for across `02_…md`, `90_…md`, `91_…md` and every chapter, with no prior filing found. `04_…md` §3.6's published status distribution (**30 `existing` / 11 `required-by-upstream` / 4 `assumed`**) is consistent with `SC-S3-41` being `required-by-upstream`; the four `assumed` rows are `SC-S3-43` (`A-27`), `SC-S3-44` (`A-29`), `SC-S3-45` (`A-28`) and one further row, none of them `SC-S3-41`.
- **Consequence:** Bounded but real, and it lands on the reader least able to absorb it. A downstream implementer reading `:452` alone concludes `SC-S3-41` rests on an unconfirmed stand-in and is therefore provisional, when it is in fact required by an upstream package that has landed. `SC-S3-41` is additionally one of the three rows `15_…md` §5.4 records as arriving with an **unmet precondition** — a named deletion owner, adjacent to `CAP-S4-1` — so a reader chasing its status is already in the part of the package with the least redundancy. It is one wrong hop, recoverable at `:177`, `:316` or `:533`; it is filed because **nothing else files it**, and an unfiled defect is invisible to every downstream consumer of `02_…md`.
- **What is assumed rather than derived:** Nothing about the occurrence — all five locations are read. **Which id `:452`'s author intended is not decided here**, and deliberately so: correcting it means editing a merged chapter, which this sub-task is barred from doing, and the evidence points at `SC-S3-45` without proving intent.
- **Handed to:** **`SUB-3 (NEU-973)`**, author of `04_…md` — merged and closed, routed as a residual — and **`NEU-896`**, live, as part of `CAP-S12-1`.

#### `F-S12-4` — The package is audited but not edited: sixteen sub-tasks recorded their conflicts faithfully and no pass reconciled them

- **Id:** `F-S12-4`
- **Finding:** The independent cold reader's overall assessment, quoted because it is the most accurate one-paragraph summary of this package's state that exists: *"The reasoning quality here is high and unusually self-aware … The failure is not analytical, it is **integrative**: sixteen sub-tasks each did careful work, recorded their conflicts faithfully in `02_findings-register.md`, and **no pass reconciled them**. The package knows almost everything that is wrong with it … and repairs almost none of it. It is a package that has been audited but not edited."*
- **Evidence:** Every one of the reader's seven blocking questions except one was **already filed** in this package before the cold read ran — `F-S14-8`, `F-S10-6`, `CAP-S4-1`, `F-S13-2` and `F-S4-2`, plus the `F5.5` durability gap — and none had been repaired. `F-S11-1`'s 161 non-resolving citations, `F-S11-3`'s two uncovered outcomes, `F-S11-4`'s 38 label↔id mis-pairings, `F-S11-5`'s 11 superseded figures and `F-S11-6`'s four missing glossary rows were all measured, filed, and left in place. `91_…md` holds 28 caps at close, every one with an owner and not one lifted by the sub-task that filed the finding behind it. `90_…md`'s § SUB-10 records four further backwards routes (`F-S10-1`, `F-S10-4`, `F-S10-5`, `F-S10-6`) beyond the charter's accepted warning `F5.7`, which named four.
- **Consequence:** This is a **property of the append-only discipline**, not a failure by any sub-task, and the trade runs in both directions — the same rule that leaves the package unreconciled is the rule that made `F-S11-1`'s single-mechanism character visible at all, and that preserved the audit evidence a repair pass would have destroyed. What it costs is that **every backwards-routed finding in this package is unactioned**, because every named owner merged before the finding was filed; that fact is recorded at `17_…md` §7.4 as one of `OI-S6-2`'s two resolving events, and `OI-S6-2` closes on it. What the reader asked for is small and specific and is quoted so NEU-896 can act on it directly: *"a single reconciliation pass over the seven questions above, and one correction to `01_outcome-register.md` adding a forward pointer to `F-S8-1` … That is a small amount of work relative to the package's size."*
- **What is assumed rather than derived:** Nothing measured. The judgement that the defects are integrative rather than analytical is the cold reader's, quoted as its judgement and not restated as this sub-task's measurement.
- **Handed to:** **`NEU-896`**, as the reconciliation pass. Named alone: no remaining pass in C010 can reconcile findings whose owners have merged, and a sixteenth sub-task editing fifteen merged chapters is precisely what the append-only rule exists to prevent.

### NEU-988 — owner amendment (not a sub-task)

**Model:** claude-opus-5[1m] · **Appended:** 2026-08-23 · **Task:** NEU-988

*Four findings, from repairing the package's owner attributions after close (`91_caps-and-incomplete-scope.md` § NEU-988; `decision-records/DR-C10-N988-1`). **NEU-988 is not a C010 sub-task**, so it allocates in an `F-N988-<k>` namespace rather than minting a seventeenth `S<n>`; no global counter is introduced. **This section lifts no cap, closes no open item, files no `CAP-`/`OI-` id, and re-files no existing finding.** **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended.***

#### `F-N988-1` — Two of this package's named owners were closed by a sibling's PR merge two milliseconds behind it, and the register's live-owner invariant failed silently on all 28 caps

- **Id:** `F-N988-1`
- **Finding:** `NEU-896` and `NEU-893` — the two parties this package routes the most residual to — were **not completed**. Each was marked `Done` **two milliseconds after** a *different* item's completion, by an automated sweep triggered by that item's PR merge. `NEU-896` `completedAt` `2026-08-22T09:11:58.531Z` against `NEU-986`'s `…58.529Z`; `NEU-893` `completedAt` `2026-08-21T21:36:00.717Z` against `NEU-975`'s `…00.715Z`. Neither swept item has an attachment of its own: `NEU-896` carries only PR `#762` (`feat(NEU-986): …`) and `NEU-893` only PR `#740` (`feat(NEU-975): …`). **The register never noticed**, because a cap's owner is a name in prose and nothing reads the tracker: `91_…md:428` asserts of `NEU-896` that *"it is live"*, and `90_…md:600` that every backwards route is *"co-named to `NEU-896`, **which is live**"* — both written on 2026-08-22, the day it was closed.
- **Evidence:** Tracker read 2026-08-23 for `NEU-896`, `NEU-893`, `NEU-986`, `NEU-975`, `NEU-895`, `NEU-891`, `NEU-892`, `NEU-894`, `NEU-850` and all sixteen sub-tasks `NEU-971`–`NEU-986`. `NEU-896` held `Backlog` from `2026-07-11T10:47:46.178Z` until `09:04:26.474Z` on 2026-08-22 and read `Done` 7m32s later; **`NEU-895`, one of its own nine declared dependencies, closed at `09:16:40.532Z` — 4m42s *after* it** — while `NEU-891`, `NEU-892` and `NEU-894` remain `Backlog`, so the nine-package gate was unreachable. `NEU-893` has no package under `docs/research/` at all, and `93_stand-in-assumption-register.md` (`A-28`, and three further entries) describes it as **unbuilt**, standing in for it. `origin/develop` at `9514e9a`.
- **Consequence:** For the twenty-four hours between the package closing and this repair, **every one of the 28 caps and all 14 open items named an owner in a terminal state** — the precise failure `91_…md:20` exists to prevent (*"a cap with no owner is an orphaned gap wearing a label"*) and `OI-S16-1` names (*"recording an owner that cannot act"*). The defect is **not** in the routing: every sub-task named the correct party. It is that a tracker state changed underneath a published prose assertion, and no mechanism connects the two. Restoring both states repairs 17 of the 28 caps and **all 14** open items without editing a line; the remaining eleven are co-named at `91_…md` § NEU-988.
- **What is assumed rather than derived:** That the two closures were *automated sweeps* is inferred from the two-millisecond intervals, the sibling-owned attachments and the absence of any deliverable — the sweep mechanism itself is not observable from the tracker and is **not** named here. What is derived, and sufficient on its own, is that **neither item produced the work its `Done` state asserted**.
- **Handed to:** **`NEU-896`**, restored and live, which converges over a package whose closure record it must now read as accurate-but-not-complete. Additionally to whoever operates the closure automation: this record makes no claim about that mechanism and installs no guard against a recurrence — `DR-C10-N988-1` states the residual cost as a standing manual check rather than solving it.

#### `F-N988-2` — The ownerless surface is wider than any single artifact records: twelve of the 28 caps never named `NEU-896` at all

- **Id:** `F-N988-2`
- **Finding:** The package is read — including by the item that commissioned this repair — as routing its caps to `NEU-896`. **It does not.** Exactly **16** of the 28 caps name `NEU-896` as an owner. The other **twelve** name only merged C010 sub-tasks (`CAP-S1-1`, `CAP-S1-3`, `CAP-S2-1`, `CAP-S2-2`, `CAP-S3-1`, `CAP-S3-2`, `CAP-S3-3`, `CAP-S4-1`, `CAP-S5-1`, `CAP-S11-1`, `CAP-S16-1`) or, in one case, **no tracker party whatsoever** — `CAP-S1-2`'s owner is *"the sub-task that depends on the number"*, a rule rather than a party, and correct only while an unmerged sub-task exists.
- **Evidence:** Every `**Owner:**` line in `91_caps-and-incomplete-scope.md` enumerated mechanically at `origin/develop` `9514e9a`: 28 cap ids, 16 naming `NEU-896`, 12 not. `CAP-S1-2` at `:82`; `CAP-S11-1` at `:415`, which names `NEU-896` explicitly as **consumer** and not as co-owner. Cross-checked against `94_package-completeness-gate.md`'s close-at-28 count and `91_…md`'s own § SUB-12 preamble (*"The register closes at 28 caps, every one with exactly one named owner"*).
- **Consequence:** A repair scoped to `NEU-896` alone would have satisfied **16 of 28** and reported success. The distribution also shows *where* the residual actually sits: eleven of the twelve are about **this package's own conventions, gates, audits and unreconciled contradictions** rather than about any sibling charter's subject — which is why redistributing them across the four unbuilt sibling packages was rejected in `DR-C10-N988-1`, and why `CAP-S1-2`'s rule-shaped owner is a **latent** defect rather than a drafting slip: it was correct when written and became ownerless the moment the last sub-task merged.
- **What is assumed rather than derived:** Nothing. The count is mechanical and every id is listed at `91_…md` § NEU-988.
- **Handed to:** **`NEU-896`**, as the eleven co-named caps. `CAP-S5-1` goes to **`NEU-893`**, restored, which its original entry already names as *"the party positioned to produce the first positive instance"*.

#### `F-N988-3` — Re-sighting: the label↔id drift class reaches the open-items register's `**Owner:**` lines, where an identifier is load-bearing

- **Id:** `F-N988-3`
- **Finding:** Two owner lines in `90_open-items-and-provisional-register.md` pair a sub-task label with the wrong tracker id: `OI-S2-2` at `:136`–`:137` records consumer and owner as **"SUB-13 (NEU-987)"** where SUB-13 is `NEU-977`, and `OI-S14-1` at `:341` records **"SUB-16 (NEU-980)"** where SUB-16 is `NEU-979`. **Both are already filed** — the first as `F-S3-2` (13 instances, reproduced at `16_…md:541`, corrected at `05_…md` §12 and restated at `90_…md:269`), the second as `F-S16-1` (24 instances across 5 merged files) and inside `F-S11-4`'s 38 label↔id mis-pairings. **This is a re-sighting, filed as signal and not as a new defect**, per the precedent SUB-5 and SUB-6 set at `91_…md:180` and `:196`.
- **Evidence:** `90_…md:136`, `:137`, `:341`; already-filed at `F-S3-2`, `F-S16-1`, `F-S11-4`; corrections published at `90_…md:269` (*"**SUB-13 is NEU-977**"*) and `10_…md:957`. Tracker read 2026-08-23: **`NEU-987` is not a child of this charter at all** — it is a live fleet item scheduled to amend `05_`/`08_`/`10_` after this merges; `NEU-980` is C010's SUB-7.
- **Consequence:** Bounded, and **it does not bear on this task's acceptance**: both `OI-S2-2` and `OI-S14-1` are **closed** (`90_…md:271`, `:349`), so neither is among the 14 open items whose owner must be live. What the re-sighting adds is *where* the class lands — on an `**Owner:**` line, which `F-S16-1` itself calls *"the only place in an append-only package where an identifier is load-bearing"*. The `NEU-987` instance is the sharper one: it now names a **live, unrelated** item, so a reader routing `OI-S2-2` by its id would route work to a party that never held it. **This task does not repair it** — ids are outside its amend scope, the item is closed, and editing a merged sub-task's entry is what the append convention forbids.
- **What is assumed rather than derived:** Nothing. Both pairings and both corrections are read directly, and the tracker identities of `NEU-977`, `NEU-979`, `NEU-980` and `NEU-987` are read.
- **Handed to:** **`NEU-896`**, as part of the reconciliation pass `F-S12-4` already scopes. Not routed to `SUB-2 (NEU-972)` or `SUB-14 (NEU-978)` as a fresh residual: both are merged, both already carry the original findings, and re-routing a re-sighting to a closed owner would manufacture the very failure `F-N988-1` records.

#### `F-N988-4` — The amend authority granted for this repair was deliberately not exercised; the package's own additive precedent was followed instead

- **Id:** `F-N988-4`
- **Finding:** This task was granted narrow authority to **amend owner attributions in place**, including inside `93_stand-in-assumption-register.md`. **It was not used.** Every `**Owner:**` line in all four registers is byte-identical to what its sub-task wrote; `90_…md` and `93_…md` are **not modified at all**; and the entire in-repo change is two appended sections and one new decision record. The mechanism used instead is the package's own — additive co-naming, as SUB-12 published it at `90_…md:614`: *"The original owner is not overwritten; the co-naming is additive."*
- **Evidence:** `git diff origin/develop -- docs/research/C010-system-and-repository-architecture/` reports **zero deleted lines** across `02_…md`, `90_…md`, `91_…md` and `93_…md`; `93_…md` still holds exactly five stand-ins, `A-25`–`A-29`; `README.md` is unmodified. The reasoning, its weighted criteria and the four rejected alternatives are at `decision-records/DR-C10-N988-1`.
- **Consequence:** Three further items amend these same files after this one merges — `NEU-987` (`05_`/`08_`/`10_`), `NEU-990` (`94_`, possibly filing into `91_`) and `NEU-989` (citation paths). An in-place rewrite of eleven owner lines authored by seven different sub-tasks would have reached the same acceptance while destroying the audit evidence of who originally owned what, and while moving line anchors under three pending amendments. **The append form costs one section and risks nothing.** Recorded because the absence of an exercised authority is invisible in a diff: a later reader finding the authority granted and unused would otherwise have to reconstruct why.
- **What is assumed rather than derived:** That the three follower items amend these files is read from their briefs, not from anything published in this package. If any of them is cancelled, the argument from structural stability weakens — but the argument from **evidence preservation** (an original owner is a fact about who was accountable, not a routing slot) stands on its own and is the one `DR-C10-N988-1` weights as decisive.
- **Handed to:** **`NEU-896`**, which inherits a register whose original attributions are intact and whose live owners are stated additively alongside them.

### NEU-987 — state-writer adjudication (not a sub-task)

*Three **existing** findings resolved, plus one new finding about why they could not be resolved earlier. **NEU-987 is not a C010 sub-task**, so its one new finding allocates in an `F-N987-<k>` namespace rather than minting a seventeenth `S<n>`; no global counter is introduced. **`F-S10-6`, `F-S14-8` and `F-S16-4` are resolved in place under their own ids — no replacement finding is minted for any of them, and none of their original entries is edited, reflowed or renumbered.** The entries below are appended records of their disposition, not rewrites of them. Reasoning: `decision-records/DR-C10-N987-1_state-writer-adjudication.md`. **Zero lines were deleted from this register.***

**The ruling, in one table.** Each category now resolves to exactly one writing component across `05_…md`, `08_…md`, `10_…md` and the decision records:

| Category | Single writing component | The other named component |
| --- | --- | --- |
| `SC-S3-17` (and `SC-S3-16`) | **`CMP-S4-9`** | `CMP-S4-19` — issuing write path, a `W` annotation, never an authority |
| `SC-S3-33` | **`CMP-S4-17`** | `CMP-S4-18` — holder, never a writer |
| `SC-S3-34` | **`CMP-S4-17`** | — |

#### `F-S10-6` — resolved: the exclusive-writer claim and the matrix were never in conflict of fact, only of quantifier scope

- **Id:** `F-S10-6` — **existing id, resolved in place.** Filed by SUB-10 (NEU-984), nominally owned by SUB-6 (NEU-976). Not re-filed and not superseded.
- **Finding:** `DR-C10-S6-1`'s headline claim — *"the MCP core is the exclusive writer of all 45 categories"* — quantifies over **deployment tiers**, not over components inside the core. Its own Decision paragraph continues *"The web tier (`CMP-S4-3`) holds no write authority over any category and no database credential"*, and the tier it excludes is `Z-WEB`. The matrix assigning `SC-S3-33`/`SC-S3-34` to `CMP-S4-17` therefore sits **inside** that decision, because `05_…md` §3.1 places `CMP-S4-17` in `Z-CONT`, one of the three operator-side zones behind the MCP tool surface. Both statements were true as intended and irreconcilable as written. **`SC-S3-34` → `CMP-S4-17` and `SC-S3-33` → `CMP-S4-17`; no authority moved.**
- **Evidence:** `decision-records/DR-C10-S6-1_state-ownership-model.md` Decision paragraph; `05_…md` §3.1 zone table (`Z-WEB` = `CMP-S4-3`; `Z-CONT` = `CMP-S4-13` … `CMP-S4-19`); `08_…md` §8.6 (`SC-S3-33`, `SC-S3-34`); `10_…md`'s republished table. `07_…md`'s own restatements already carry the qualifier in situ, which is why the bare headline was the only surface that lost it and why `07_…md` needed no amendment.
- **Consequence:** The finding is **resolved, not overturned** — SUB-10 was right to route it and right that its substrate selection survived either reading. What changes is only that `DR-C10-S6-1` now states its quantifier, so a reader with only this package in hand can no longer take it component-wise. A reader who previously reconciled the two by assuming the matrix was wrong would have concluded that `CMP-S4-17` must not write its own store — contradicting `03_…md` §4.2's *"written **only** by the producer"*.
- **What is assumed rather than derived:** Nothing. Every input is published in the package; this entry re-reads them rather than adding evidence.
- **Handed to:** **Closed.** No successor obligation. `F-S13-1` — SUB-13's disclosure of the narrow tie-break-(b) reading behind `SC-S3-33`'s clause-2 assignment — is **not** closed by this and remains SUB-6's: this entry ratifies the *outcome* on independent grounds, not the tie-break reading.

#### `F-S14-8` — resolved: `SC-S3-17`'s authority is `CMP-S4-9`, and the `05_…md` residue it identified is now repaired

- **Id:** `F-S14-8` — **existing id, resolved in place.** Filed by SUB-14 (NEU-978), matrix half dispositioned by SUB-16 (NEU-979). Not re-filed.
- **Finding:** `CMP-S4-9` is the **single** writing authority for `SC-S3-16` and `SC-S3-17`; `CMP-S4-19` is the issuing write path — a `W` annotation and never a second authority. SUB-14's re-run of `07_…md` §6.1 clause 5 and SUB-16's republication both reached this, and the assignment rule conditions authority on the row's own `Learner-scoped` cell, never on which thread issues the write. The `05_…md` residue is repaired: `FL-S4-8` and `FL-S4-9` now name `CMP-S4-9`, matching `FL-S4-20`.
- **Evidence:** `08_…md` §8.3 (*"Authority: `CMP-S4-9`, written through `CMP-S4-19`"*); `10_…md` §6.2 (*"`CMP-S4-19` remains a `W` annotation and **never a second authority**"*, *"the deviation is in the write path, never in the authority"*); amended `05_…md` §3.2 (`CMP-S4-9`, `CMP-S4-19`), §5 (`FL-S4-8`, `FL-S4-9`), §7 (`B1-8`, `F3-5`); `10_…md` §9's residual row for these two categories, annotated closed.
- **Consequence:** The proximate defect was **a scoping clause, not an authority**: `05_…md` §3.2 scoped `CMP-S4-9` to *"the only writer … **on the request path**"*, which failed to cover an off-request-path write everyone already agreed was `CMP-S4-9`'s. Widening that clause dissolves the second writer without moving any assignment. **`CAP-S4-1` is unaffected** — deletion ownership for these two rows stays structurally unassignable for want of a principal field, and `05_…md` §9.2 is untouched; write authority and deletion ownership remain different questions.
- **What is assumed rather than derived:** Nothing. The direction was already named by SUB-14 and SUB-16; this entry records that it was acted on.
- **Handed to:** **Closed.** SUB-4 (NEU-974) and consumer SUB-11 (NEU-985) carry no residual obligation from this finding.

#### `F-S16-4` — resolved: both `05_…md` flow residues repaired; the `FL-S4-16` half stays open on its own merits

- **Id:** `F-S16-4` — **existing id, resolved in part in place.** Filed by SUB-16 (NEU-979), nominally owned by SUB-4 (NEU-974). Not re-filed.
- **Finding:** `F-S16-4` carried three `05_…md` residues. **Two are repaired.** (i) `FL-S4-8`/`FL-S4-9` vs `FL-S4-20` — resolved to `CMP-S4-9`, per `F-S14-8` above. (ii) `FL-S4-14` vs `FL-S4-13` — resolved to `CMP-S4-17`: `FL-S4-13` in the same table already called it the cache's *only* writer, and `CMP-S4-18`'s own responsibility statement says it *"never derives, refreshes or ages"* a verdict, so it is the **holder**, not the writer. `FL-S4-14` named the holder in a column that asks for the authority. (iii) `FL-S4-16`'s staleness is **not** resolved and is not this item's to resolve: it turns on the still-open `OI-S2-2`, which is an unanswered question rather than a contradiction.
- **Evidence:** `10_…md` §5.5 (*"`FL-S4-14` is the outlier"*; the holder-versus-authority distinction); `10_…md` §6.3 and its "What is edited — **Nothing**" row, now annotated with the repair; `08_…md` §8.6; amended `05_…md` §5 (`FL-S4-14`), §7.3 hop 3 — which named `CMP-S4-18` in the same Authority column and was the third instance of the same defect — and its §5 amendment note; `10_…md` §9's `SC-S3-33` residual row, whose outlier half is annotated closed while `F-S13-1` stays open; `03_…md` §4.3.
- **Consequence:** The reader-facing hazard `10_…md` §5.5 named — *"a reader taking `FL-S4-14` at face value would conclude the cache may be written by whoever holds it, exactly the property `FL-S4-13`'s single-writer rule exists to deny"* — no longer exists. `10_…md` §6.3's "`05_…md` is byte-identical after this pass" no longer holds from this commit forward, and is annotated in place rather than rewritten; `08_…md` and `09_…md` remain byte-identical.
- **What is assumed rather than derived:** Nothing.
- **Handed to:** **Partly closed.** The two flow-vs-flow residues are closed. The `FL-S4-16` half remains with `OI-S2-2`'s owner and is untouched here.

#### `F-N987-1` — A routed cross-chapter contradiction had no closing mechanism: every sub-task could find one and none could fix one

- **Id:** `F-N987-1`
- **Finding:** All three findings above were **correctly** routed by the sub-tasks that found them, and all three then sat unresolved — not because anyone judged them low priority, but because the package's own discipline gave no sub-task the authority to amend a sibling's merged chapter. `10_…md` §6.3 states the constraint exactly: *"SUB-14's phrase '`05_…md`'s half is SUB-16's to amend' assigns ownership of the outcome, not a licence."* Three independent sub-tasks (SUB-10, SUB-14, SUB-16) each reached the boundary and each stopped at it correctly. **The routing worked; the closing did not exist.** The package therefore reached 16/16 merged while carrying the one class of defect an architecture package exists to prevent — an ambiguous state writer.
- **Evidence:** `10_…md` §6.3 points 1–3 and its "What is edited — **Nothing**" disposition row; `10_…md` §5.5's "Neither artifact is amended"; `91_…md`'s residual sections; and the cold-read verdict recorded in NEU-987's brief — *"I would be blocked by contradictions about who writes state, which is the one thing an architecture package exists to settle."* NEU-987 exists solely to supply the missing licence, which is itself the evidence that no earlier item had it.
- **Consequence:** The append-only discipline is **correct and worth keeping** — `10_…md` §6.3 point 1's objection, that silent reconciliation *"would destroy the record of which one was written first and on what evidence"*, is right, and this pass accepted it rather than overriding it by disclosing every amendment in place. But append-only with **no** terminal amend step is not self-closing: a contradiction between two merged chapters can be named indefinitely and never repaired. A package using this discipline needs an adjudication item budgeted from the start, not discovered at cold-read time.
- **What is assumed rather than derived:** That no earlier item held the authority is read from the sub-tasks' own statements that they lacked it, not from an audit of every sub-task brief.
- **Handed to:** **`NEU-896`**, which converges over this package and inherits the pattern; and to whoever scopes the next research package under the same append-only convention — the mitigation is a planned adjudication pass, not a change to the append-only rule.

### NEU-990 — independent re-grade of the self-graded gate rows (not a sub-task)

**Model:** claude-opus-5[1m] · **Appended:** 2026-08-23 · **Task:** NEU-990

*Four findings, from independently re-grading the ten `†` self-graded rows of `94_package-completeness-gate.md` (`94_…md` §7; `decision-records/DR-C10-N990-1_independent-regrade-of-the-self-graded-rows.md`). **NEU-990 is not a C010 sub-task**, so it allocates in an `F-N990-<k>` namespace rather than minting a seventeenth `S<n>`; no global counter is introduced. **This section lifts no cap, closes no open item, files no `CAP-`/`OI-` id, and re-files no existing finding.** **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended.***

#### `F-N990-1` — The gate was not generous to itself: eight of the ten self-graded rows survive an independent re-grade, and the one other divergence runs the harsher way

- **Id:** `F-N990-1`
- **Finding:** `94_…md` §1.2 flagged its ten `†` rows as *"the weakest evidence class in this package … one party grading its own artifact"*, and `CAP-S11-1`'s history gave real grounds to expect self-serving grades: the one item where self-grading was categorically barred — the cold read, `G-41` — **failed**. Re-graded against the same package by a reader that authored none of it, **eight of the ten hold at the disposition SUB-12 gave them.** Exactly **one** row is downgraded (`G-38`, `pass` → `pass-with-qualification`). The only other divergence runs in the **opposite** direction: on `G-60` the independent reader graded `pass` where SUB-12 had graded itself `pass-with-qualification`. **The `†` disclosure was proportionate to a risk that was real but small, and the discipline SUB-12 showed on the cold read extended to the rows it did grade itself.**
- **Evidence:** `94_…md` §7.2 (the ten-row side-by-side table), §7.3 (both divergences), §7.5 (what the independent count reproduced from source: 61 rows with no gaps or duplicates; Group A re-derived from `01_outcome-register.md`'s twelve "Verified by" clauses as 4+3+3+4+3+5+4+3+3+3+4+3 = 42; dispositions counted at 40 / 15 / 5 / 1; **rows marked passing with no citation: 0**, matching §5's claim). Isolation contract and its three disclosed leaks at §7.1.
- **Consequence:** The gate's calibration is now measured rather than asserted, and `94_…md` §1.2's instruction that *"a reader converging this package should check the ten `†` rows directly rather than take them"* is **discharged** — NEU-896 no longer has to. What it does **not** establish is that the package is sound: the re-grade touched only the ten `†` rows, changed none of §5.1's six non-passing items, and leaves all five `CAP-S12-*` caps standing. The one downgrade moves a row between two *answered* classes, so the count of items answered with a citation is unchanged at 55.
- **What is assumed rather than derived:** That the re-grading reader was genuinely independent. It inherited none of the shipping run's context and was barred from the repository tree, `_local/`, `docs/wf-plans/`, git, the tracker and the web — but the bars were **honoured, not enforced**, and three leaks are recorded at `94_…md` §7.1, one of which (a self-disclosed staging slip that exposed `G-61`'s original disposition) means **`G-61`'s confirmation is contaminated and is not independent evidence.** The finding rests on the other nine.
- **Handed to:** **`NEU-896`**, which inherits ten rows carrying two verdicts each and may treat the `†` class as checked. `G-61` is handed over as *unconfirmed by an independent reader*, not as confirmed.

#### `F-N990-2` — `17_…md` §3 narrows two of the five stand-in tolerance envelopes it restates, on the exact surface NEU-896 reconciles against

- **Id:** `F-N990-2`
- **Finding:** `G-38` asks for *"the NEU-896 handoff list reviewed for coverage against the assumptions table."* Performing that diff — which is the check the item names and which no prior artifact ran — shows **two of five envelopes do not survive the restatement**, both in the **narrowing** direction. `A-25`'s envelope at `93_…md:66` tolerates the AI call *"**synchronous on a read path**, asynchronous anywhere, or batched ahead of time"*; `17_…md` §3 restates it as *"asynchronously or once per escalation"*, **dropping two of the three tolerated modes**. `A-26`'s envelope and invalidating outcome are restated **circularly** — *"Any budget the stated envelope contains"* and *"A budget the AI-placement decision's envelope cannot contain"* — where `93_…md:81` and `:83` state three concrete limbs and a concrete invalidating outcome. Separately, `traceability/S11_outcome-coverage-audit.md:65` — authored by SUB-11, not SUB-12 — records verbatim that this limb *"is **not** run here; it is a review, not a mechanical check, and **no C010 sub-task performs it**"*, and the tally `94_…md`'s `G-38` row leans on (`16_…md` §4.1) audited `93_…md`'s **own fields**, not the handoff list against the register.
- **Evidence:** `93_…md:59`–`:70` (`A-25`) and `:74`–`:86` (`A-26`) diffed against `17_…md` §3's table; `traceability/S11_outcome-coverage-audit.md:65`; `16_…md` §4.1; the re-disposition and its reasoning at `94_…md` §7.3.
- **Consequence:** **Bounded, and the authoritative text is unharmed** — `93_…md` is closed at five entries, is untouched by this task and by SUB-12's, and remains the register of record. What is defective is a **restatement** on `17_…md` §3, which that chapter presents as *"the reconciliation surface"* NEU-896 works from. A reader reconciling `A-25` from §3 alone would reject a hint model that `93_…md` actually tolerates, and would find `A-26`'s envelope carries no testable content at all. **No cap is filed:** the row is answered with a citation, so the gate's own rule makes it a qualified pass and an owned admission would be the wrong instrument. `94_…md`'s `G-38` row is re-dispositioned to `pass-with-qualification` at §7.2.
- **What is assumed rather than derived:** Nothing. Both restatements and both source entries are read directly and quoted.
- **Handed to:** **`NEU-896`**, as one item in the reconciliation pass `F-S12-4` already scopes: read `93_…md` for the envelopes, not `17_…md` §3. **Not repaired here** — `17_…md` is a merged chapter and this task holds no more licence to amend one than SUB-10, SUB-14 and SUB-16 held, which is precisely what `F-N987-1` records.

#### `F-N990-3` — This package cannot be used to blind-grade itself: a traceability file republishes the gate's aggregate tally and names four of the ten self-graded rows

- **Id:** `F-N990-3`
- **Finding:** A genuine re-grade requires the grader not to know the answer. Barring the reader from `94_package-completeness-gate.md` is **not sufficient**, because `traceability/S12_package-closure-coverage.md` — a legitimate, non-excludable corpus file — republishes the gate's headline result: **61 derived items, 40 pass / 15 pass-with-qualification / 5 fail / 1 capped**, *"Items marked passing without a citation: 0"*, `G-3` / `G-41` / `G-52` named as failing, and four of the ten rows marked as carrying the self-graded `†`. Any reader handed the published package therefore learns the shape of the answer before grading a single row. The reader disclosed this unprompted as anchoring pressure the task design had not anticipated.
- **Evidence:** `traceability/S12_package-closure-coverage.md`; the disclosure and its handling at `94_…md` §7.1, leak 2. Distinct from the separately-recorded staging slip (leak 1) and the harness injection (leak 3).
- **Consequence:** **It weakens every `†` confirmation in `94_…md` §7 by a knowable amount and it cannot be engineered away** while the traceability set stays part of the published package — which it must, since `OUT-12` requires it. It does **not** invalidate the re-grade: the reader still located each row's evidence itself, still counted independently from source, and still reached a disposition that differs from the published one on two rows, which a purely anchored reader would not have done. The practical lesson is for whoever re-grades a published gate next: the corpus is the leak, and the honest response is disclosure and independent recounting, not a claim of blindness the corpus makes impossible.
- **What is assumed rather than derived:** Nothing about the leak itself, which is read directly. What is **not** measurable is how much the leak moved the verdicts; no counterfactual exists and none is claimed.
- **Handed to:** **`NEU-896`**, as a caveat on how much weight the `†` confirmations carry; and to any later party repeating this method on this or a sibling package.

#### `F-N990-4` — `17_…md` §8's risk rollup does not reconcile with its own stated three-value scale

- **Id:** `F-N990-4`
- **Finding:** §8 defines its status scale explicitly and in three values — **mitigated**, **partially mitigated**, **open** — and then reports totals of *"Mitigated 5; partially mitigated 3; open 1; **split 1**"*. **`split` is a fourth value the stated scale does not contain**, carried by `R6`, whose cell reads *"Split result, and this is the one risk where the calibration returned two different answers."* Independently, the rollup reconciles with the rows' own headline labels **only** if `R3` — labelled *"**Mitigated**, with one named residual"* — is counted as *partially* mitigated, while `R4` (*"Residual: nothing revalidates until…"*) and `R5` (*"Residual: two mechanical notes…"*) keep the **mitigated** label despite each naming a residual, which is the section's own definition of *partially mitigated*. Counting the headline labels as written gives **6 / 2 / 1 / 1**, not the stated 5 / 3 / 1 / 1.
- **Evidence:** `17_…md` §8 preamble (the three-value scale), the `R3`, `R4`, `R5` and `R6` rows, and the register totals line following the table. Surfaced while re-grading `G-49` (`94_…md` §7.2).
- **Consequence:** **Presentational, and it changes no risk and no mitigation.** Every row states its own severity and its own residual in full, so a reader working row-by-row is never misled; only the rollup line and the scale disagree. `G-49` is **confirmed at `pass`** on the independent re-grade regardless — the item asks for a risk register carrying severity and mitigation status, and one exists with all ten rows populated. Recorded because a later reader recomputing the totals will not reproduce them and would otherwise have to work out why.
- **What is assumed rather than derived:** Nothing. The scale, the four labels and the totals line are all read directly.
- **Handed to:** **`NEU-896`**, as a one-line correction inside the reconciliation pass `F-S12-4` scopes — either the scale gains its fourth value or `R3`'s label moves. **Not repaired here:** `17_…md` is merged, and this task amends no sibling chapter.

### NEU-989 — citation-path repair (not a sub-task)

**Model:** claude-opus-5[1m] · **Appended:** 2026-08-23 · **Task:** NEU-989

*Two closures and two findings, from repairing the non-resolving relative citation paths `F-S11-1` and `F-S11-2` measured (`decision-records/DR-C10-N989-1_citation-path-repair.md`). **NEU-989 is not a C010 sub-task**, so it allocates in an `F-N989-<k>` namespace rather than minting a seventeenth `S<n>`; no global counter is introduced. **This section lifts no cap, files no `CAP-`/`OI-` id, and re-files no existing finding.** **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended** — the repair rewrote path tokens in place, so every changed file's diff is equal insertions and deletions.*

#### `F-S11-1` — CLOSED. 189 citation paths repaired; the package's in-scope citations now resolve from their own file's directory

- **Id:** `F-S11-1` (closure record; the original entry in `### SUB-11` stands unamended)
- **Finding:** `F-S11-1` measured **161 genuine non-resolvers** at cutoff `ecf88c5`, of which **159 were one mechanism** — a relative path off by exactly one directory level, in two mirror-image directions. Re-measured mechanically against the current tree, the same defect stands at **207 non-resolving citations**, of which **18 are by-design non-claims** and **189 are genuine**. The growth from 161 is not drift: four PRs landed into the package after SUB-11's cutoff (`NEU-988` #764, `NEU-987` #768, `NEU-991` #766 — which added a whole chapter, `18_neu-982-ceremony-reverification.md` — and `NEU-990` #770), and the re-measurement uses a slightly wider extractor that also resolves bare folder citations and comma-separated line-ref suffixes such as `:6,54,330,409`. **All 189 are repaired.** The convention `DR-C10-S12-2` published is now satisfied throughout: a package-root file cites a package-root sibling bare; a `decision-records/` or `traceability/` file uses one `../` for a package-root sibling and two for another package.
- **Evidence:** Repair at commit `f91f747`, 30 files, **189 tokens**, **146 insertions / 146 deletions** — equal on every file, so no line was added, removed, reflowed or renumbered. Class split: **C1 spurious `../` from a package-root file — 130** (`02_findings-register.md` ×47, `90_…md` ×37, `91_…md` ×25, `92_…md` ×19, `13_…md` ×2, and the rest across the decision records); **C2 one `../` short from a subfolder — 52**; **C3 upstream cited without its package directory — 7**. The per-file C1 tallies for `02_`, `90_`, `92_` and `13_` and the C2 tallies for `traceability/S2` (×11), `S4` (×1), `DR-C10-S2-2` (×5) and `DR-C10-S2-3` (×3) **reproduce `F-S11-1`'s own figures exactly**, which is what establishes that the two measurements are measuring the same thing rather than two different populations. Mechanical re-run after the repair reports **0** in-scope non-resolving citations, with `resolve from own dir` rising from **920 to 1109** — exactly +189.
- **Consequence:** `OUT-12`'s mechanical link check is now useful: it no longer reports 161 known-broken references it cannot distinguish from genuinely dangling ones. The cold reader `F-S11-1` named as the second injured consumer — the one working only from the published package, for whom a C2 failure reads as *"the evidence is missing"* — can now follow every in-scope citation. **The single-mechanism evidence is not destroyed by the repair**, which was `DR-C10-S12-2` Rationale (ii)'s objection: the class counts, the two directions and the per-owner distribution are all restated above and in `DR-C10-N989-1`, and `F-S11-1`'s original entry is untouched.
- **What is assumed rather than derived:** Nothing about the counts — every citation is extracted, resolved against its own file's directory, and re-resolved mechanically after the repair. **Not** claimed: that every repaired path points at the document the citing sentence intends. Resolvability is necessary, not sufficient; a sample across all three classes was checked semantically (recorded in `DR-C10-N989-1` § Evidence), but the guarantee this entry makes is resolvability.
- **Handed to:** **Closed.** No residual owner. `F-N989-1` and `F-N989-2` below carry the two things this repair found and did **not** fix.

#### `F-S11-2` — CLOSED. Both bare upstream filenames now carry their package directory

- **Id:** `F-S11-2` (closure record; the original entry in `### SUB-11` stands unamended)
- **Finding:** `04_state-category-inventory.md` cited two upstream documents — `../C005-product-foundation/01_evidence-taxonomy.md` and `../C009-course-content-quality/09_enforceable-quality-system.md` — **as bare filenames, with no directory at all** — the class `F-S11-2` correctly identified as **worse** than C1/C2, because a bare `09_…` resolves *plausibly and wrongly* against this package's own `09_authority-matrix-validation.md`. Both now carry their package directory, and the third clause of `DR-C10-S12-2`'s convention is satisfied in the file that motivated it.
- **Evidence:** `04_…md:318` now reads `../C005-product-foundation/01_evidence-taxonomy.md:12`–`:18`; `04_…md:421` now reads `../C009-course-content-quality/09_enforceable-quality-system.md`. Both targets verified present. The same two bare forms are quoted five further times in `16_…md` §5.2/§5.4 and `02_…md`'s `### SUB-11` **in order to report them as defective**, and those quotations are deliberately left unrepaired — see the non-claim list in `DR-C10-N989-1`.
- **Consequence:** The one genuinely dangerous citation class in the package — the one that fails silently and confidently rather than loudly — is gone from the file that carried it.
- **What is assumed rather than derived:** Nothing. Both targets were opened and checked against the claim the citing sentence makes.
- **Handed to:** **Closed**, with one correction carried forward as `F-N989-1`.

#### `F-N989-1` — `F-S11-2` attributes both bare filenames to C009; one of them is a C005 document

- **Id:** `F-N989-1`
- **Finding:** `F-S11-2` states that the two bare-filename targets are *"**C009 documents** living in `../C009-course-content-quality/`"*. That holds for the `09_` target, which is indeed `../C009-course-content-quality/09_enforceable-quality-system.md`. It does **not** hold for the `01_` target, which exists only at **`../C005-product-foundation/01_evidence-taxonomy.md`** — `../C009-course-content-quality/` carries no evidence-taxonomy document at any slot. Repairing the citation against `F-S11-2`'s stated location would have produced a path that resolves to nothing, or — had a same-numbered C009 sibling existed — the exact plausible-wrong-resolution failure `F-S11-2` exists to warn about.
- **Evidence:** `../C009-course-content-quality/01_provenance-and-rights.md` occupies that package's `01_` slot, and the package holds no evidence-taxonomy document at all. `../C005-product-foundation/01_evidence-taxonomy.md` §1 is titled *"Evidence Taxonomy & Claim Discipline"* and its lines `:12`–`:18` carry the seven classes `04_…md:318` cites by name (`[literature]`, `[code-evidence]`, `[dogfooding]`, `[ai-critique]`, `[automated-eval]`, `[operational-log]`, `[future-real-user]`) — the citing sentence's claim and the target's content match exactly. The repair targets the file that exists and that the sentence describes, not the location `F-S11-2` named.
- **Consequence:** Small and now closed for the citation itself. Recorded because it is a worked example of the failure mode this whole task had to guard against: a repair that satisfies the letter of a finding can still point at the wrong file, and the only defence is checking the target against what the citing sentence actually claims. It also means `F-S11-2`'s **Evidence** field — *"Both targets verified to exist at `docs/research/C009-course-content-quality/`"* — is wrong for one of the two.
- **What is assumed rather than derived:** Nothing. Both directories were listed and the target's content read.
- **Handed to:** **`NEU-896`**, as a one-line correction to `F-S11-2`'s Evidence field inside the reconciliation pass `F-S12-4` scopes. **Not repaired here:** amending a merged sub-task's finding text is a claim change, and this task changes path tokens only.

#### `F-N989-2` — 199 abbreviated shorthand references carry the same spurious `../`, and are deliberately not repaired

- **Id:** `F-N989-2`
- **Finding:** The package uses an abbreviated shorthand for sibling documents — `` `04_…md` ``, `` `../90_…md` ``, `` `../05_…` `` — and **199 occurrences of it in package-root files carry the same spurious `../` that C1 describes**. They are **not** counted among the 189 repaired and **not** repaired, for a reason that is decidable rather than stylistic: the `…` is not a filename, so the token resolves to nothing whether or not the `../` is removed. Stripping the prefix would change 199 lines of prose without making one citation resolvable.
- **Evidence:** Mechanical count over the package after the repair: 199 backticked tokens matching a leading `../` followed by an ellipsis, in files at the package root. The count is reported by the checker as a standing informational line, so it does not silently disappear.
- **Consequence:** A reader who expands a shorthand mentally will expand `../90_…md` into a `../`-prefixed `90_open-items-and-provisional-register.md`, which does not resolve, and must then drop the `../` themselves. The cost is one step and it fails **loudly**, which is `F-S11-1` §5.3's own criterion for the recoverable class. The package's resolvability gate is unaffected, because no abbreviated form is a resolvable citation in either direction.
- **What is assumed rather than derived:** That the shorthand is intended as prose shorthand rather than as a citation. This is derived, not assumed: `00_method-and-provenance.md` and every register use the form inline in sentences, and no `…`-bearing token anywhere in the package resolves.
- **Handed to:** **`NEU-896`**, at its discretion and as a **presentational** item only — either the shorthand drops its `../` package-wide or it is left as prose. This task states the count rather than acting on it, because a 199-line edit that repairs no link is the kind of high-count low-attention change `DR-C10-S12-2` Rationale (iii) warns produces new defects.

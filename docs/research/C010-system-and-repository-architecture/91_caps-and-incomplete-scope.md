# 91 — Caps and Incomplete Scope

**Task:** NEU-971 (SUB-1) opens it · **Charter:** C010 (umbrella NEU-895) · **Opened:** 2026-08-21
**Model:** claude-opus-5[1m]
**Entries owned by:** each appending sub-task. **Reconciled by a single named owner:** **NEU-986 (SUB-12)**.

---

## What belongs here

Every **cap** — a place where this package's scope stops short, stated honestly — and every statement of incomplete scope.

**A cap is an admission that there is no resolving event.** That is what separates it from an open item:

| | Has a resolving event | Named owner | Register |
| --- | --- | --- | --- |
| **Open item** | yes — an observable event closes it | yes | `90_open-items-and-provisional-register.md` |
| **Cap** | **no** — this package will not close it | **yes, always** | this file |

**Every cap carries a named owner.** "The package", "a later charter" and "whoever picks this up" are not owners. A cap with no owner is an orphaned gap wearing a label, and it is the failure mode this register exists to prevent.

**A cap is available only where a spike is infeasible.** If a bounded experiment would settle the claim, run the spike (`92_spike-register.md` §2) — a cap is not the cheap way out of an experiment.

**Recording a cap is not a way to mark a completeness-gate item as passing.** `94_package-completeness-gate.md` answers each item with cited evidence; an item that cannot be so answered is **recorded as a cap here rather than marked passing**. The two are alternatives, not a formality and its escape hatch.

## The single reconciling owner

**NEU-986 (SUB-12) is the declared single owner that reconciles this register at the end.** It merges duplicate entries, resolves cross-references, and publishes the reconciled register alongside `94_package-completeness-gate.md`.

Until SUB-12 runs, **a duplicate entry is correct-by-convention**, and any other sub-task that "tidies" one is destroying evidence rather than helping. Two sub-tasks independently capping the same gap is *signal* — it says the gap is visible from two directions — and collapsing it early throws that away.

Naming one owner is deliberate: a register that everyone reconciles is a register nobody reconciles.

## Append convention

> Each sub-task appends its own `### SUB-<n>` section. No sub-task reflows, renumbers, or rewrites another sub-task's entries. On a merge conflict in this file, keep **both** sides.

**Keep both sides is not a suggestion.** The default conflict resolution — pick one side — silently deletes a sibling's cap, and a silently deleted cap is the worst artifact this package can produce: the completeness gate then reads as passing over a gap that nobody owns and nobody can see. A visible duplicate is the intended failure mode, and SUB-12 is the declared owner that cleans it up once, at the end.

## Id namespacing (there is no global counter in this file)

Ids are **`CAP-S<n>-<k>`**, where `<n>` is the sub-task number and `<k>` restarts at `1` inside that sub-task's own section. SUB-3 allocates `CAP-S3-1`, `CAP-S3-2`, …; SUB-12 allocates `CAP-S12-1`, … — concurrently, without coordination, and without collision.

**No global counter exists here, and SUB-12's reconciliation does not introduce one.** Reconciling means merging duplicates and resolving cross-references; it does **not** mean renumbering into a single sequence. Renumbering would break every citation already written against the namespaced ids, which is precisely what the namespacing was adopted to prevent. Cite a cap in its full form (`CAP-S12-1`), never as a bare `CAP-1`.

## Stable field set (the same fields on every entry, in this order)

Entries are written as **blocks rather than table rows** — a table row carrying long cells is a single line, so two sub-tasks appending rows conflict on adjacent lines, while two sub-tasks appending whole sections do not overlap at all.

| Field | What it records |
| --- | --- |
| **Id** | `CAP-S<n>-<k>` |
| **Cap** | What this package does **not** do, stated as a limit rather than a topic. |
| **Why it is capped** | Why no experiment, no reading and no available party settles it here. |
| **What it leaves unsupported** | The decision, outcome or claim that is weaker because of this cap — named, so the cost is visible. |
| **Owner** | The named party accountable. Never "the package". |
| **What would lift it** | What would have to be true or available for the cap to be removed. Not a promise, and not a resolving event — a cap has none, by definition. |

---

## Entries

### SUB-1

*Three entries. SUB-1's caps are all about the limits of opening a package: it shipped conventions it cannot itself exercise, carried facts it did not itself re-verify, and had no QA engine to run.*

#### `CAP-S1-1` — The package conventions are unproven until siblings actually append

- **Id:** `CAP-S1-1`
- **Cap:** SUB-1 published the append convention, the `*-S<n>-<k>` id namespacing, the `00`–`89` / `90`–`99` numbering split and the keep-both-sides conflict rule, but **it did not and cannot demonstrate that they survive fifteen concurrent appends.** The registers are exercised by exactly one sub-task — the one that wrote them.
- **Why it is capped:** No experiment available to SUB-1 settles it. Simulating concurrent appends against files no sibling has written yet would test SUB-1's guess at what siblings will write, not the convention. The only evidence that counts is the real appends, and they arrive after this sub-task ships. It is not a spike (nothing to run), and it is not an open item (no single observable event closes it — it is discharged gradually, by fifteen of them).
- **What it leaves unsupported:** `OUT-12`'s house-style claim rests, for now, on the conventions being *mirrored from a package that did survive concurrent appends* (`../C009-course-content-quality/`, compiled 2026-08-10) rather than on this package's own evidence. That is a **proxy signal** under `00_method-and-provenance.md` §1.1 — precedent, not validation.
- **Owner:** **NEU-985 (SUB-11)** for the mechanical audit — id-namespace collisions, renumbered entries, entries missing a required field; **NEU-986 (SUB-12)** for the completeness gate's answer.
- **What would lift it:** SUB-11's audit reporting zero id collisions, zero renumbered entries and zero lost entries across every sub-task's appends — at which point the conventions have been exercised for real and the cap is answered by evidence rather than by precedent.

#### `CAP-S1-2` — SUB-1 re-verified none of the charter's codebase facts

- **Id:** `CAP-S1-2`
- **Cap:** The charter's numbered assumptions carry counted codebase facts — 45 registered tools across 16 modules, 42 gated and 3 exempt, 40 + 2 `context_token` declarations, 165 source files, ~25,200 lines, 197 test files, 25 Drizzle migrations, 431 non-bot commits of 648, 10 `public` tables and 2 Drizzle-defined `infrastructure` tables. **SUB-1 carried every one of them as `consumed` and re-counted none.**
- **Why it is capped:** Re-counting them was not SUB-1's scope, and doing it anyway would have produced a second set of numbers at a second cutoff with no mechanism to keep the two in step — a worse outcome than one clearly-attributed set. The charter records its own verification method and date for each fact.
- **What it leaves unsupported:** Any decision that depends on one of those counts depends on a number this package has not itself verified. `OUT-6`'s regression boundary (the 45/42/3 split) and `OUT-7`'s topology criteria (the maintainer tally, the single-package facts) are the two most exposed.
- **Owner:** **the sub-task that depends on the number.** A sub-task resting a decision on one of these counts **re-verifies it against `src/` at its own cutoff and cites its own command** — it does not inherit SUB-1's word for it. This is stated in `00_method-and-provenance.md` §6.
- **What would lift it:** Each dependent sub-task publishing its own re-verified count with its own cutoff and command. The cap is discharged piecewise, by the sub-tasks that actually need the numbers, not centrally.

#### `CAP-S1-3` — `qa-execution:engine` is unconfigured, so no QA pass exists

- **Id:** `CAP-S1-3`
- **Cap:** This repository's capability registry resolves to **`git, linear`** only; no `qa-execution:engine` provider is registered. The automated QA-execution phase is a genuine **no-op** for this package.
- **Why it is capped:** There is no engine to run and, for a documentation deliverable, nothing for one to exercise — this package contains no runtime behaviour. Registering a QA engine to satisfy a phase that has no subject would be theatre.
- **What it leaves unsupported:** **Nothing. No QA pass is claimed, fabricated, or implied anywhere in this package.** The phase is recorded as a no-op rather than as a skipped step, and verification here is by file inspection and `git diff` against the named success criteria — see `00_method-and-provenance.md` §5 and `94_package-completeness-gate.md`.
- **Owner:** **NEU-986 (SUB-12)**, which answers the completeness gate and records the no-op there rather than leaving the item blank.
- **What would lift it:** A registered `qa-execution:engine` provider **and** a deliverable with runtime behaviour for it to exercise. Neither exists for this package, and the first without the second would not lift it.

---

<!--
Later sub-tasks: append your own `### SUB-<n>` section BELOW this comment. Do not edit any
section above your own. On conflict, keep both sides — NEU-986 (SUB-12) reconciles duplicates
once, at the end.
-->

### SUB-2

*Two entries. Both are limits on **quantifying** what this sub-task's conclusions establish qualitatively — a budget with no corpus to measure against, and a failure frequency with no population to measure over. Neither is a spike in disguise: no experiment available to this package produces either number, because both need artifacts that do not exist.*

**Deliberately not filed here:** the `qa-execution:engine` no-op. `00_method-and-provenance.md` §5.1 states that a sub-task whose QA phase produced no execution *"records that fact and moves on; it does not report a pass it did not obtain, **and it does not file a cap for a check that was never applicable**."* The package-level statement is already `CAP-S1-3`, and this sub-task records the no-op in `03_execution-environment-and-citation-drift-component.md` §9 instead. A second cap for the same never-applicable check would inflate the register's count without adding a gap.

#### `CAP-S2-1` — The re-run budget the `automated` gate class requires cannot be quantified by this package

- **Id:** `CAP-S2-1`
- **Cap:** `../C009-course-content-quality/09_enforceable-quality-system.md:70` states the `automated` class's cost as *"An execution environment, and a re-run budget."* This sub-task decides the **execution environment** half — `DR-C10-S2-2`, with an isolation boundary and a wall-clock bound. **It puts no number on the re-run budget:** not the per-unit wall-clock bound, not the per-revision re-run allowance, not the concurrency the authoring pipeline may spend on gate execution. The specification in `03_execution-environment-and-citation-drift-component.md` §3.5 names the bound as **required** and leaves its value unset.
- **Why it is capped:** There is nothing to measure. NEU-890 specified 59 gates and implemented **none**; there are **0 content units**, so there are no nodes, no `test` instances and no authored approaches to run a battery over. A number produced here would be a guess with a unit attached, and it would then be inherited as though it were derived — the exact laundering `00_method-and-provenance.md` §1.2 exists to prevent. It is **not** a spike, because no bounded experiment available to this package produces the number: `SPK-S2-1` could settle whether a bound is *needed* using a stand-in unit, and did, but it cannot settle what the bound should *be* without real authored approaches and real boundary inputs. It is **not** an open item, because no observable event available to this package closes it.
- **What it leaves unsupported:** Any downstream decision that depends on the **cost** of the gate battery rather than on its **existence**. Concretely: `OUT-8`'s deployment-shape decision, if it were argued on the authoring pipeline's compute footprint; and any claim that the authoring-time battery is affordable at a given corpus size. Neither claim may cite this package as its basis. The **qualitative** conclusion — that a host-enforced bound is required — is unaffected and rests on `SPK-S2-1`.
- **Owner:** **NEU-986 (SUB-12)**, which answers the package-completeness gate and records this as an unanswerable item rather than marking it passing — the alternative `91_…`'s own admission rule names.
- **What would lift it:** A corpus with placed content units and a running gate battery to measure against — that is, the state after NEU-890's gates are implemented and a real node carries real `test` instances. The first without the second would not lift it: an implemented battery with nothing to run over produces no budget either.

#### `CAP-S2-2` — How often the failure mode `SPK-S2-1` demonstrates actually occurs is unmeasurable here

- **Id:** `CAP-S2-2`
- **Cap:** `SPK-S2-1` establishes the **consequence** of a non-terminating authored unit: an in-process same-thread guard cannot fire, the host is unrecoverable, and every gate queued behind it dies. **It establishes nothing about how often a creator writes one.** This package therefore states that the boundary is required, and does **not** state that the failure is common, rare, or worth a given cost.
- **Why it is capped:** Frequency is a property of a population, and this programme's population is one creator and zero authored units. `00_method-and-provenance.md` §1.1 is explicit that creator dogfooding is *"`n=1`, and the `1` is the person who designed it"*, and that a proxy signal may not be promoted to fill an external-validation gap. No experiment available to this package produces a rate: running the harness more times measures the harness, not the creators. It is **not** an open item — no observable event within this package's reach closes it — and it is **not** a spike, for the reason just given.
- **What it leaves unsupported:** Any cost-benefit argument about the isolation boundary — *"the boundary is worth its cost because runaway units are common"* — and any priority claim about implementing the bound early. `DR-C10-S2-2` deliberately makes **neither**: it argues from the consequence being unrecoverable and unobservable, which holds at any non-zero frequency, rather than from the frequency being high. A reader who needs the frequency argument does not have it.
- **Owner:** **NEU-986 (SUB-12)**, at the completeness gate, alongside `CAP-S1-1`'s and `CAP-S1-2`'s treatment of the package's other unmeasured quantities.
- **What would lift it:** Recorded gate-runner timeouts over a corpus authored by more than one creator — a production measurement over a population greater than one, which is `00_method-and-provenance.md` §1.1's definition of external validation and which this programme does not have. Not a promise, and no event in this package produces it.

### SUB-3

*Three entries. One is a field set this package is forbidden to widen, one is a permanent maintenance cost this sub-task's own decision record creates and names, and one is an upstream requirement the codebase does not meet and this package cannot meet on its behalf.*

#### `CAP-S3-1` — The problem-citation record's field set is unresolved upstream, and this package may not widen it

- **Id:** `CAP-S3-1`
- **Cap:** `SC-S3-32` — the problem-citation record — carries **`stable_id` + `canonical_url` and nothing else**. `title`, numeric `constraints`, difficulty signal and curriculum placement are **not admitted**, and are routed upstream to ledger challenge **`CH-F5-1`**, which is unresolved at NEU-890's cutoff. The inventory therefore specifies a record that is **known to be narrower than a downstream consumer may need**, and states so at the entry rather than quietly carrying the wider set.
- **Why it is capped:** `../C009-course-content-quality/decision-records/DR-C09-01_permitted-field-set.md:17` names the **only** permitted route for a sub-task that needs a wider set: cite `CH-F5-1` by id and carry the unresolved field set as a cap with a named owner. It does not permit local widening, and `00_method-and-provenance.md` §2.5's owner-attachment rule means this package cannot resolve another package's ledger challenge by asserting a preferred answer. It is **not** an open item: no observable event **within this package's reach** closes it — the resolving event belongs to NEU-890's ledger, not to any C010 sub-task. It is **not** a spike: no bounded experiment here decides what another package's decision ledger admits.
- **What it leaves unsupported:** Any downstream claim that a stored problem reference is sufficient to render, rank, place, or difficulty-sort a problem without a live fetch. Concretely: any component `SUB-4` places that would read `title` or `constraints` from local state; and any sizing argument that assumes citation records are self-describing. `SUB-13`'s authority assignment over `SC-S3-32` is unaffected — the authority question does not depend on the field count.
- **Owner:** **NEU-986 (SUB-12)**, which records it at the package-completeness gate as an item this package cannot answer, alongside the resolving owner named upstream in `CH-F5-1` itself.
- **What would lift it:** `CH-F5-1` resolving in NEU-890's decision ledger, either admitting the four contested fields or confirming the two-field set as final. Until then `SC-S3-32`'s field set is provisional-by-inheritance, and a later C010 sub-task re-reads the challenge rather than this entry.

#### `CAP-S3-2` — The inventory cannot be regenerated from the schema, permanently

- **Id:** `CAP-S3-2`
- **Cap:** There is **no script that reproduces `04_state-category-inventory.md` from the database schema, and there will not be one.** The inventory is maintained by hand, and a schema change does not mechanically propagate into it.
- **Why it is capped:** The individuation rule `DR-C10-S3-1` selects has four discriminators — the writing component, the lifecycle/retention rule, the volatility class, and the store. **Two of the four are not present in the schema**: the writing component, and the prospective authority under an ownership model `SUB-6` has not yet selected. A generator could reproduce §4.1's fourteen tables; it could not reproduce the three splits, any of the ten process-local entries, any of the three derived entries, or any of the fifteen entries with no store — that is, 31 of 45 rows. It is **not** an open item: no event closes it, because the cause is the rule's design and the rule was chosen deliberately over the table-keyed alternative that *would* be generatable (`DR-C10-S3-1`, rejected alternative 1, which cannot represent two thirds of the problem). It is **not** a spike: nothing is uncertain here.
- **What it leaves unsupported:** Any claim that the inventory is automatically current with `src/`. Its freshness is exactly its stated verification cutoff, **2026-08-21**, and no later. A reader at a later cutoff re-runs §7.3's falsifiers rather than trusting the row set — which is why those falsifiers are published in advance and are each mechanically runnable.
- **Owner:** **NEU-986 (SUB-12)**, at the package-completeness gate; the recurring maintenance obligation itself falls on whichever sub-task or contributor changes the schema, per `DR-C10-S3-1`'s "one judgement per schema change".
- **What would lift it:** Nothing available to this programme. It would require the ownership model to be selected **and** the writing component of every category to be derivable from the schema itself — the second of which is a property the codebase does not have and that no C010 outcome proposes to give it. Recorded as permanent rather than deferred.

#### `CAP-S3-3` — The operational logs' retention window and deletion owner are required upstream and set nowhere

- **Id:** `CAP-S3-3`
- **Cap:** NEU-887's operational-log privacy gate requires a **stated retention window** and a **named deletion owner** for the learner payload held in `mcp_request_log` and `operation_event_log`. Neither is implemented, and **this sub-task sets neither.** `04_state-category-inventory.md` §3.3 records the two entries with the gap named; `F-S3-3` states the finding in full.
- **Why it is capped:** Setting a retention window is a product and compliance decision about how long learner text may be held, and naming a deletion owner assigns an operational responsibility to a person or team. **Neither is a state-category question, and neither is any C010 outcome.** `OUT-2` enumerates and classifies; `OUT-3` assigns authority over categories, which is not the same as setting a retention policy over one. It is **not** an open item: no observable event within this package's reach closes it — `SUB-13` assigning authority over `SC-S3-16` does not produce a number of days, and `SUB-14` applying the isolation invariant does not name a deletion owner. It is **not** a spike: nothing here is settled by a bounded experiment.
- **What it leaves unsupported:** Any claim that this system's operational logging is compliant with NEU-887's privacy gate, and any claim that learner data held by this system is deletable on request — the second of which is currently false in a stronger sense than the cap alone conveys, because neither table carries a principal field to key a deletion on. Any `PLA-*` derived extract (`SC-S3-41`) inherits the same gap, since the gate requires the retention statement on the derivative as well as on the raw result.
- **Owner:** **NEU-986 (SUB-12)**, which records it at the package-completeness gate as an unmet upstream requirement rather than marking the gate passing — the treatment `91_…`'s own admission rule names.
- **What would lift it:** A retention window and a named deletion owner recorded against both tables, plus a principal field to key per-learner deletion on. The third is a schema change no C010 sub-task makes; the first two are decisions outside this charter's outcomes entirely.

### SUB-4

*One entry — a deliberate **second sighting** of a gap `CAP-S3-3` already carries, filed from the placement direction because it establishes something the first sighting does not: that the gap is not merely unassigned but currently **unassignable**. Two sub-tasks independently capping the same gap is signal, and duplicates in this register are recorded rather than tidied.*

**Deliberately not filed here: the automated QA no-op.** `qa-execution:engine` and `qa-execution:host` are unconfigured in this repository's capability registry, so the automated QA phase is a genuine Core Article 8 no-op for this sub-task. It is **not** a cap, because `00_method-and-provenance.md` §5.1 is explicit that a sub-task *"does not file a cap for a check that was never applicable"*. `CAP-S1-3` already carries the package-level statement; this is the same no-op, not a second one. The disposition is recorded in `05_system-context-and-responsibility-boundaries.md` §13's verification note, which is where a verification fact belongs.

**Also deliberately not filed here: `OI-S3-2`.** The item SUB-3 filed with this sub-task as its named owner offered a cap as one of two permitted outcomes. It is **discharged by publication instead** — `../05_…` §8 names the reading component and the copy disposition for both `SC-S3-37` and `SC-S3-40` — and the disposition is recorded in `90_open-items-and-provisional-register.md`'s `### SUB-4` section. Filing a cap for a question this sub-task actually answered would misreport the package's completeness in the direction that matters most.

#### `CAP-S4-1` — No component in the published model can be the operational logs' deletion owner, and the obstruction is structural

- **Id:** `CAP-S4-1`
- **Cap:** `05_system-context-and-responsibility-boundaries.md` places twenty components, and **none of them can be named the deletion owner for `SC-S3-16` (`infrastructure.mcp_request_log`) or `SC-S3-17` (`infrastructure.operation_event_log`).** This sub-task names none, and states why naming one would be worse than naming none: **a deletion owner must be able to enumerate one learner's rows in both tables, and no component can do that while neither table carries a principal field.** Naming an owner today would name a component that provably cannot perform the duty.
- **Why it is capped:** `CAP-S3-3` capped the missing retention window and deletion owner as a product-and-compliance decision outside every C010 outcome. This entry caps a different and narrower thing — the **placement** consequence — and it is capped rather than opened because **no observable event within this package's reach closes it.** `SUB-13 (NEU-977)` assigning authority over `SC-S3-16` does not create a principal column; `SUB-14 (NEU-978)` applying the isolation invariant finds no column to apply it to; and the mapping that would make enumeration possible, `SC-S3-45`, is itself `assumed` under **`A-28`** with no store. It is **not** a spike: nothing here is settled by a bounded experiment, and it is **not** an open item for the reason just given. It is filed **knowing `CAP-S3-3` exists**: the register's own rule is that two independent sightings of the same gap are signal, and duplicates are not tidied away.
- **What it leaves unsupported:** Any claim that this system's component model is complete with respect to NEU-887's operational-log privacy gate; any claim that a per-learner deletion request is expressible against the placed components; and any downstream sizing of `CMP-S4-20` (the operational-log derived-extract producer) that assumes its inputs are deletable — `SC-S3-41` is the only category in the entire inventory that carries "a named deletion owner" as part of its own definition, and it inherits the gap from the two categories it derives from. It also leaves `../05_…` §9.1's disposition table with a uniform **"inherits and does not fix"** against NEU-890's `OI-S6-5`, which this cap does not improve.
- **Owner:** **NEU-986 (SUB-12)**, at the package-completeness gate, recorded alongside `CAP-S3-3` as the same gap sighted twice — once from the state inventory, once from the component placement — rather than merged into it.
- **What would lift it:** A principal field on both log tables, or `SC-S3-45` given a store, either of which turns the deletion owner from **unassignable** into merely **unassigned** — at which point `SUB-13 (NEU-977)` can assign it and this entry collapses into `CAP-S3-3`. Neither is a change any C010 sub-task makes; both are schema changes outside this charter's outcomes. Recorded as a precondition, not as a promise.

### SUB-5

*One entry — the limit on what publishing an invariant can establish when nothing in the package can satisfy it. It is filed because the gap between "well-formed" and "satisfiable" is invisible in the deliverable itself: a procedure that returns a verdict on every category it is given looks complete whether or not any category can ever pass.*

**Deliberately not filed here: the automated QA no-op.** `qa-execution:engine` and `qa-execution:host` are unconfigured in this repository's capability registry, so the automated QA phase is a genuine Core Article 8 no-op for this sub-task too. It is **not** a cap — `00_method-and-provenance.md` §5.1 is explicit that a sub-task *"does not file a cap for a check that was never applicable"* — and `CAP-S1-3` already carries the package-level statement. This is the same no-op, sighted a fourth time, not a new one. The disposition is recorded in `06_isolation-invariant-and-the-neu-893-split.md` §8's verification note, following the placement SUB-2 and SUB-4 established. **No QA pass is claimed and none was fabricated.**

**Deliberately not filed here: a third sighting of the deletion-owner gap.** `CAP-S3-3` caps the missing retention window and deletion owner; `CAP-S4-1` caps its structural unassignability from the placement direction. This sub-task reaches the same two categories from a **third** direction — `SC-S3-16` and `SC-S3-17` return verdict `not-evaluable` under the isolation invariant, so no question about their isolation can be answered in either direction (`../06_…` §3.6 case 2). That is a genuinely distinct consequence and it is recorded, but **as a property of the invariant's verdict set rather than as a third cap**: the underlying gap, its owner and its lifting condition are identical to `CAP-S4-1`'s, and a third entry would multiply the register without adding a party, a precondition or an unsupported claim. The register's duplicate-tolerance rule admits second sightings as signal; it does not oblige a sighting per direction.

#### `CAP-S5-1` — The isolation invariant is published with **zero** positive instances: shown well-formed, never shown satisfiable

- **Id:** `CAP-S5-1`
- **Cap:** `06_isolation-invariant-and-the-neu-893-split.md` §3 publishes the isolation invariant as a decision procedure and demonstrates that it terminates, returning five different verdicts over five state categories. **Not one of them — and, as §4.2 establishes, not one of the 45 — reaches verdict `holds`.** This package therefore establishes that the invariant is **well-formed**: it has a named domain, ordered checks answerable from cited artifacts, and a closed verdict set in which every category lands exactly once. It does **not** establish that the invariant is **satisfiable** — that any state category, in any reachable state of this system, can pass all five checks. No positive instance exists anywhere in the package, and the demonstrations are all failures or exemptions.
- **Why it is capped:** Producing one positive instance requires three things to be simultaneously true, and **no C010 sub-task makes any of them**: an ownership key on the store (`NEU-850's OUT-2`, converged but unimplemented), the reaching query bodies scoped at or below the port boundary (`SUB-8 (NEU-981)`'s blast radius, which SUB-8 sizes and does not build), and an identity gate on the STDIO transport, which **does not exist as a component at all** (`src/transport/main.ts:55`–`:59`; `BND-S4-17`, owner `nobody`). No reading settles it, because the thing to be established is a property of running code rather than of a document. No bounded experiment settles it, so it is **not a spike**. And no observable event within this package's reach closes it, so it is **not an open item** — which is exactly why `OI-S5-3`, the *unexercised-against-a-real-matrix* item, is filed separately: that one has a resolving event (SUB-14 publishes) and this one has none.
- **What it leaves unsupported:** Any claim that the isolation outcome `OUT-4` describes is **achievable** as specified — as opposed to well-specified. Any downstream plan that treats "apply the invariant" as a step with a reachable success state, rather than as a measurement whose current answer is uniformly negative. Any reading of `SUB-14 (NEU-978)`'s forthcoming per-row application as a *test the system can pass*: at this cutoff every in-domain row returns `fails-transport` or worse, and a uniform negative result is a fact about the system rather than a defect in SUB-14's application. It also leaves unsupported the converse — nothing here shows the invariant is **un**satisfiable either; the honest statement is that satisfiability is untested in both directions, and this package could not have tested it.
- **Owner:** **`NEU-986 (SUB-12)`**, at the package-completeness gate, which must weigh an outcome whose deliverable is complete and whose subject matter has no demonstrated positive case. Named alongside **NEU-893**, which is the party positioned to produce the first positive instance and for which this cap is the standing definition of done.
- **What would lift it:** **One state category evaluating to `holds`** — which requires all three preconditions above to land together, since the invariant's ordered checks mean any one of them missing returns a failure verdict before the others are reached. The most likely first candidate is a durable learner-scoped category on the HTTP path once `NEU-850's OUT-2`, port-level scoping and a STDIO gate are all in place; nothing here reserves that outcome or predicts it. Recorded as a precondition, not as a promise.

### SUB-6

**Deliberately not filed here.** Two limits surfaced during this sub-task and neither is filed as a new cap.

- **The `qa-execution:engine` no-op.** The automated QA phase is a genuine Core Article 8 no-op for this sub-task as for every other in the package — the capability registry resolves to `git, linear` and no QA execution provider is registered, so no QA pass exists and none is claimed. **`CAP-S1-3` already carries this at package level**; a per-sub-task duplicate would make a single package-wide condition look like six independent ones.
- **The deletion-owner gap on `SC-S3-16` / `SC-S3-17`.** This is the **fourth** sighting of the gap `F-S3-3` opened and `CAP-S4-1` capped, and it is load-bearing here: criterion `C6` in `07_state-ownership-model-selection.md` §3.3 is scored against it. Following the precedent SUB-4 and SUB-5 set, the new **consequence** — that under the selected model the gap is frozen exactly as `CAP-S4-1` describes it, because no new log producer is introduced — is recorded in `decision-records/DR-C10-S6-1_state-ownership-model.md` § Consequences rather than as a fourth register entry. A structural gap does not become more owned by being filed again.

#### `CAP-S6-1` — The two-writer divergence this comparison rejects `M-B` for was never observed against a live database

- **Id:** `CAP-S6-1`
- **Cap:** The claim that the read-modify-write pattern this codebase uses for learner state **actually loses an update** when two independent writer processes race it against a live Postgres under the default isolation level was **not observed**. It is derived from reading the code and the database schema, not from watching it happen. No two-process harness was run, and no `M-B`-shaped deployment was stood up and stressed.
- **Why it is capped:** No Postgres instance was reachable at this sub-task's cutoff. Five connection probes were attempted — `127.0.0.1:5432`, `127.0.0.1:5433`, `localhost:5432`, `postgres:5432` and `db:5432` — and all five returned `ECONNREFUSED` or timed out; no `DATABASE_URL` is present in the environment and the repository carries only `.env.example`. `92_spike-register.md` §2's three-way rule admits a spike, a cap with a named owner, or a read; **asserting is not an available third option**, and the observation this claim needs cannot be made without a database. The *readable* half of the claim was read rather than capped and is recorded in full at `07_state-ownership-model-selection.md` §3.1 — eight facts, each cited to a path and line range: the scheduling write is read-modify-write (`src/orchestration/review-workflows.ts:35`, `:60`–`:89`, `:99`), the codebase's own comment states its single-writer premise (`:190`–`:191`), no optimistic concurrency control exists anywhere in `src/`, no transaction sets an isolation level (`src/infrastructure/db/operations.ts:21`–`:24`), and `learning_sessions` has no partial unique index behind its one-active-**learning session** invariant (`src/infrastructure/db/schema.ts:99`–`:124`).
- **What it leaves unsupported:** Any claim that divergence under two writers has been **demonstrated** in this system, as opposed to being unprevented by anything in the code or the database schema. Any quantitative claim about how *often* a race would be lost, or how wide the window is — the source comment calls it "small" (`src/orchestration/review-workflows.ts:190`) and nothing here measures it. Any claim that a *carefully drawn* `M-B` partition would fail in practice. It does **not** leave `M-B`'s rejection unsupported: that rejection rests on the readable facts — nothing prevents divergence, and the codebase states a premise `M-B` falsifies — plus the independent durability-property disqualification at `../07_…md` §7, neither of which needs the unobserved half. **The cap is on the strength of the evidence, not on the conclusion.**
- **Owner:** **`SUB-10 (NEU-984)`**, which decides data-store topology under `OUT-8` and must therefore stand up a store to reason about one; it is the first party in this charter positioned to make the observation, and it is already the consumer of this chapter's reversal check (`OI-S6-1`). Named alongside **NEU-896**, the implementation charter that would have to fix the pattern if the observation confirms it, and for which this cap is the standing reason to treat the single-writer premise as load-bearing rather than incidental.
- **What would lift it:** **A two-process harness run against a reachable Postgres**, under the default isolation level, exercising the `getChunk → compute → persistReviewUpdate` sequence concurrently from two processes and recording whether the second write silently discards the first. Filed under `92_spike-register.md`'s template, quarantined to `_local/scratch/`, with its result stated in full and an expiry. That is a spike this sub-task would have run had a database been reachable, and its absence is a property of the environment at this cutoff — not of the question's tractability.

### SUB-13

**No new cap.** Stated explicitly rather than by omission, because an empty section and a missing section read the same way at the completeness gate, and this register's reconciling owner (`NEU-986 (SUB-12)`) needs to be able to tell them apart.

Every limit this sub-task encountered was either **assignable** (and was assigned), **routable** (and was routed as a finding to a named owner), or **already capped** by an entry above. There was no limit left over that no party can act on — which is this register's admission test, and this sub-task did not meet it.

Recorded for the reconciliation, the four existing caps that bound `08_per-state-authority-matrix.md`, each **cited at the point of use and none re-filed**:

- **`CAP-S4-1`** — no component in `../05_…md`'s inventory can be the deletion owner for `SC-S3-16` or `SC-S3-17`, and the obstruction is **structural**. **It stays open and this sub-task does not close it.** The matrix assigns both rows a write authority (`CMP-S4-9`, written through `CMP-S4-19`), because **deletion ownership is not write authority** — two different questions, and the rule answers only the second. `../05_…md` §9.2's unblocking condition is unchanged: a principal field on both tables, or `SC-S3-45` given a store, turns the deletion owner from *unassignable* into merely *unassigned*, and only then can it be assigned. `SC-S3-41`'s row is the constructive counterpart — an extract carrying **its own** retention window and named deletion owner — and its migration-path cell records that shipping it without both would reproduce this cap one layer up rather than resolve it.
- **`CAP-S3-1`** — the problem-citation field set is frozen at `stable_id` + `canonical_url` under `DR-C09-01`, with the wider set carried against ledger challenge `CH-F5-1`. `SC-S3-32`'s row **does not widen the set** and cites the cap at the row rather than re-filing it.
- **`CAP-S1-3`** — no QA pass exists for this package. Cited, not duplicated. It applies unchanged to `../08_…md`: the chapter's two audits are mechanical checks over its own row set, and a mechanical self-audit is not a QA pass.
- **`CAP-S6-1`** — two-writer divergence could not be observed, so the cap is on evidence strength rather than on the conclusion. It bears on `SC-S3-21`'s row, whose migration-path cell states that per-process trip state lets *n* processes hold *n* opinions about whether Tier-2 blocking is tripped, and that the **cost** of that divergence is capped evidence rather than a measured quantity.

Two limits that might look like caps and are deliberately not filed as such, so the reconciliation is not left guessing:

- **The two rows the assignment rule under-determined** (`SC-S3-33`/`SC-S3-34`, and `SC-S3-45`) are **findings, not caps** — `F-S13-1` and `F-S13-2`, both routed to **`SUB-6 (NEU-976)`**, which owns the rule and can act. A cap is for a limit no available party settles; these have an available party.
- **The migration destination for the eighteen store-`none` categories** is an **open item, not a cap** — `OI-S13-1`, owner **`SUB-10 (NEU-984)`**, with an observable resolving event inside this charter's reach. That is precisely the distinction this register draws against `../90_…md`.

### SUB-14

**No new cap.** Stated explicitly rather than by omission, following SUB-13's precedent, because an empty section and a missing section read identically at the completeness gate and this register's reconciling owner (`NEU-986 (SUB-12)`) needs to tell them apart.

Every limit this sub-task encountered was **routable** (and was routed as one of eleven `F-S14-*` findings, each naming an owning sub-task), **already capped** by an entry above, or **already answered by a merged record** (the worker-terminability claim, settled by `SPK-S2-1` arrangement C — see `92_spike-register.md`'s `### SUB-14` note, which discloses that this sub-task measured it before re-reading the register and reports the run as a **replication, not a new spike**). Nothing was left over that no party can act on, which is this register's admission test.

**Three uncertain-and-material claims were deliberately *not* spiked, because a cap with a named owner already covers them.** Asserting them was not an available third option and none is asserted:

- **`CAP-S6-1`** — two-writer divergence **could not be observed**; five Postgres probes refused or timed out at SUB-6's cutoff, and no `DATABASE_URL` exists in this environment either. The cap is on **evidence strength, not on the conclusion**, and its named owner is **`SUB-10 (NEU-984)`**, alongside **NEU-896**. It bounds `09_authority-matrix-validation.md` §9 throughout: that section states what the matrix **requires** and what the code **fails to prevent** — nine rows carry a serialization requirement, no optimistic concurrency control exists in `src/`, no transaction sets an isolation level (`src/infrastructure/db/operations.ts:21`–`:24`), and `F-S6-2` records `reviewPersistence` absent from `UnitOfWorkPort`'s `TransactionPorts` — and asserts **nothing** about what was observed. **Re-running a probe already recorded as unrunnable would add no evidence, so it was not re-run and no duplicate cap is filed.** The lifting condition is unchanged.
- **`CAP-S5-1`** — the invariant's **satisfiability** is untested. `09_…md` §6 finds **zero `holds` across 90 row-evaluations**, which is consistent with unsatisfiability **and** with a merely-unimplemented mechanism; `06_…md` §3.4.1's asymmetry means a census **cannot** distinguish them. The cap therefore stands unchanged and this sub-task does not claim to have narrowed it. Owner: **SUB-5 / NEU-893**.
- **`CAP-S4-1`** — no component in `05_…md`'s inventory can be the deletion owner for `SC-S3-16` or `SC-S3-17`, and the obstruction is **structural**. **It stays open and this sub-task does not close it.** This is its **fifth sighting** (SUB-3, SUB-4, SUB-6, SUB-13, here), and this chapter records a **new consequence** rather than a new cap: §11's recovery walk classifies both categories as **R5 — not recoverable by construction**, because the only source that could reconstruct either is itself, and **`SC-S3-17` is read by the Tier-2 blocking gate**. A record that cannot be completed, cannot be deleted by anyone in particular, and is read by a blocking gate is the sharpest form the cap has taken. The unblocking condition in `05_…md` §9.2 is unchanged.

Recorded for the reconciliation, the two further existing caps that bound this chapter, **cited at the point of use and neither re-filed**:

- **`CAP-S1-3`** — **no QA pass exists for this package.** It applies unchanged here: the `qa-execution:engine` surface is unconfigured, so scenario execution is a genuine **Core Article 8 no-op**, the scenarios authored for NEU-978 are marked **`NOT RUN`**, and **no QA pass is claimed anywhere in `09_…md`**. A mechanical parse of the matrix is not a QA pass, and neither is a documentation gate.
- **`CAP-S3-1`** — the problem-citation field set is frozen at `stable_id` + `canonical_url` under `DR-C09-01`, with the wider set carried against ledger challenge `CH-F5-1`. §13's durability walk **relies on that narrowness**: because `SC-S3-32`'s whole content is those two fields, a retirement cannot carry collateral state into the evidence chain. The cap is cited at that verdict and the set is not widened.

Two limits that might look like caps and are deliberately **not** filed as such, so the reconciliation is not left guessing:

- **The two undefined scenario outcomes** (`SC-S3-42`'s divergence, `SC-S3-31`'s conflicting write) are **findings, not caps** — `F-S14-4` routed to **NEU-891**, `F-S14-5` routed to **`SUB-10 (NEU-984)`**. A cap is for a limit no available party settles; both of these name an available party, and the matrix named it first.
- **The re-run obligation against SUB-16's post-absorption revision** is an **open item, not a cap** — `OI-S14-1`, owner **`SUB-16 (NEU-980)`**, with an observable resolving event inside this charter's reach.

### SUB-16

**One new cap.** Stated explicitly rather than by omission, following SUB-13's and SUB-14's precedent, because an empty section and a missing section read identically at the completeness gate and this register's reconciling owner (`NEU-986 (SUB-12)`) needs to tell them apart.

#### `CAP-S16-1` — `SC-S3-45`'s isolation verdict is permanently unimprovable, and this package cannot change that

- **Id:** `CAP-S16-1`
- **Cap:** `SC-S3-45` (learner-identity → owner mapping) is assigned to **`CMP-S4-10`**, the identity provider, correctly and by rule (`../07_…md` §6.1 clause 4, as resolved by `F-S13-2`). `../06_…md` §3.4.1 requires an **enumerated access-path set covering reads and writes** before `I3` may return `holds`. **No such set can be enumerated for a store this system does not own** — in *any* target state, including the composed state that assumes `NEU-850`'s `OUT-2` fully implemented, because `OUT-2` adds ownership keys to **this system's** tables and this store is not one of them. The row therefore returns `fails-confinement` in Census B and `not-evaluable` in Census A **permanently**, not pending a change. `F-S14-3` established this; `10_republished-authority-matrix.md` §5.3 dispositions it; **this cap carries what the disposition cannot resolve.**
- **Why it is capped:** The disposition available to SUB-16 is exhausted. **Re-running the rule cannot help** — the assignment is already what the rule produces, and the obstruction is downstream of the assignment. **Re-deciding the model is forbidden** to this sub-task by its own charter, which is why the model question is re-routed as **`F-S16-2`** rather than answered. And the party that could answer it — **`SUB-6 (NEU-976)`**, which owns clause 4 and the domain rule — is **merged and `Done`**, so no scheduled pass will produce an answer; that routing defect is filed separately as **`OI-S16-1`**, because it is a *different* problem from this one. No reading settles this cap, because the obstruction is a property of an external system's storage rather than of a document. No bounded experiment settles it, so it is **not a spike**. And no observable event inside this package's reach closes it, so it is **not an open item** — which is exactly the distinction this register draws against `../90_…md`.
- **What it leaves unsupported:** Any "rows remaining to fix" figure computed from either census — **one of them is not fixable by any work**, and a figure that includes it overstates the remaining effort while understating the structural obstruction. Any claim that `SC-S3-45`'s isolation guarantee is **enforced**; it is **inherited**, and this system's only local obligation is to fail closed on a projection failure rather than default to an unowned row. Any downstream plan that schedules work against this row's verdict. It leaves the converse unsupported too: nothing here shows the row's guarantee is **weak** — an external identity provider may well confine it perfectly — only that **this package cannot see whether it does**, which is precisely why the verdict cannot improve.
- **Owner:** **`SUB-6 (NEU-976)`** — the party that owns clause 4 and the invariant's domain rule, and the only one that can decide whether a row with an external authority belongs in that domain at all. Named alongside **`SUB-12 (NEU-986)`**, at the package-completeness gate, which must weigh a matrix in which one row's verdict is structurally frozen and whose named owner is closed.
- **What would lift it:** **Either** SUB-6 (or `NEU-896` at convergence) rules that a category whose authority is external is **outside** the invariant's domain — at which point `SC-S3-45` becomes `not-applicable` by rule rather than `fails-confinement` by obstruction, and the census loses a permanently-negative member honestly; **or** the identity provider publishes an enumerable access-path set over the mapping, which this package has no means to require and does not predict. Recorded as a precondition, not as a promise.

Recorded for the reconciliation, the four existing caps that bound `10_republished-authority-matrix.md`, each **cited at the point of use and none re-filed**:

- **`CAP-S4-1`** — no component can be the deletion owner for `SC-S3-16` or `SC-S3-17`, and the obstruction is **structural**. **It stays open and this sub-task does not close it.** This is its **sixth sighting** (SUB-3, SUB-4, SUB-6, SUB-13, SUB-14, here). Both rows are `revised` in the republication, and the revision is confined to the **write path**: it records that the durable append is issued from a transport worker thread rather than the request path. **Write path is not write authority and neither is deletion ownership** — three different questions, and this revision answers only the first. `../05_…md` §9.2's unblocking condition is unchanged.
- **`CAP-S5-1`** — the invariant is published with zero positive instances and is **not proved satisfiable**. It applies unchanged: the republished revision reproduces **zero `holds` across 90 row-evaluations**, and `../06_…md` §3.4.1's asymmetry means neither census can distinguish unsatisfiability from an unimplemented mechanism. Cited at `10_…md` §9; not narrowed, not re-filed.
- **`CAP-S6-1`** — two-writer divergence was never observed, so the cap is on **evidence strength** rather than on the conclusion. It bears on the republication because `SC-S3-16`/`SC-S3-17`'s revised write path makes the multi-writer question concrete — pino transport worker threads are additional processes issuing durable appends — and **what may be said about their divergence is bounded by this cap, not extended by the revision**. Owner unchanged: **`SUB-10 (NEU-984)` / NEU-896**.
- **`CAP-S1-3`** — **no QA pass exists for this package.** `qa-execution:engine` and `qa-execution:host` are both unconfigured, so scenario execution is a genuine **Core Article 8 no-op**, the scenarios authored for `NEU-979` are marked **`NOT RUN`**, and **no QA pass is claimed anywhere in `10_…md`**. A mechanical parse of the republished table is not a QA pass, and neither is a documentation gate.

Three limits that might look like caps and are deliberately **not** filed as such, so the reconciliation is not left guessing:

- **The two model questions** (`F-S16-2`'s domain rule, `F-S16-3`'s clause order) are **findings, not caps** — both routed to **`SUB-6 (NEU-976)`**, which owns the rule. The fact that SUB-6 is closed is a routing defect and is filed as **`OI-S16-1`**; it does not convert either finding into a cap, because a cap is for a limit **no party** settles and the owning party here exists, it is merely unscheduled.
- **The three unreconciled `05_…md` annotations** are a **finding, not a cap** — `F-S16-4`, routed to **`SUB-4 (NEU-974)`**, which owns the document and can amend it. What SUB-16 may not do is amend a merged sibling's chapter; that is a *discipline*, not an incapacity.
- **The tracker-id drift** is a **finding, not a cap** — `F-S16-1`, routed to **`SUB-12 (NEU-986)`**, which reconciles identifiers at the gate, with **`SUB-11 (NEU-985)`** for the mechanical class check. Twenty-four wrong identifiers across five merged files is a defect with an available owner, not a limit of scope.

### SUB-7

**Two new caps.** Stated explicitly rather than by omission, following SUB-13's, SUB-14's and SUB-16's precedent, because an empty section and a missing section read identically at the completeness gate and this register's reconciling owner (`NEU-986 (SUB-12)`) needs to tell them apart.

#### `CAP-S7-1` — The web API's erasure capability cannot be scoped at all, because no row holding learner payload has a deletion owner

- **Id:** `CAP-S7-1`
- **Cap:** `11_web-api-scope-and-resource-inventory.md` does **not** scope an erasure capability for the general web API — not a deferred one, not a simplified one, **none**. The two categories holding learner payload, `SC-S3-16` (MCP request log) and `SC-S3-17` (operation event log), each carry an authority (`CMP-S4-9`, write path `CMP-S4-19`) and **no deletion owner**, so a *"erase this learner's payload"* write-intent resolves to **zero** authorities. It is published as unmatched item `U-2` and finding `F-S7-2` (`11_…md` §11.2), and it is **not** published as an inventory entry, because an entry naming no authority is exactly what `OUT-5`'s exactly-one audit exists to reject.
- **Why it is capped:** **No reading settles it.** `05_system-context-and-responsibility-boundaries.md` establishes that no component in its inventory can be the deletion owner, and the obstruction is **structural** rather than an assignment error — which is why re-running SUB-6's rule produces the same result and why routing it to SUB-13 (which is done, as `F-S7-2`) cannot by itself yield an owner. **No bounded experiment settles it**, so it is not a spike: the question is whose obligation a deletion is, not what a system does under a probe. And **no observable event inside this charter's reach closes it** — the parties who could create a deletion owner (`NEU-893`, `NEU-887`'s privacy gate) are outside it — so it is not an open item, which is precisely the distinction this register draws against `../90_…`.
- **What it leaves unsupported:** Any claim that the web API this package scopes can satisfy a privacy-driven erasure request for learner payload. Any downstream charter's plan to implement one over this architecture — there is no component to route the intent to, and the honest answer to the request is that it cannot be satisfied for these two categories by anyone. Any figure of "capabilities remaining to specify" computed from `11_…md` §9's sixteen entries — **one required capability is not specifiable by any work available here**, and a figure that omits it understates the gap. It leaves the converse unsupported too: nothing here shows the payload is *unerasable in principle*, only that **this architecture names nobody who may erase it**.
- **Owner:** **`NEU-893`** — chartered for safe production integration and learner isolation, and the only party positioned to assign a retention-and-deletion owner. Named alongside **`SUB-12 (NEU-986)`** at the package-completeness gate, which must weigh an architecture carrying a stated privacy obligation with no assignable owner, and **`NEU-896`** at convergence.
- **What would lift it:** A **named deletion owner** on `SC-S3-16` and `SC-S3-17`, with a retention window, published where a matrix row can cite it — at which point an erasure write-intent becomes scopable and `11_…md` §9 gains its first entry not present at this revision. Recorded as a precondition, not a promise. **`SC-S3-17` carries an extra condition:** it is read by the Tier-2 blocking gate (`src/adapters/drizzle/tier2-blocking-stats-repository.ts:39`), so a deletion policy over it must also state what happens to a gate input, which no party has yet done.

#### `CAP-S7-2` — The resource inventory is bounded to categories that have a store, so eight learner-facing categories carry a non-exposure instead of a capability

- **Id:** `CAP-S7-2`
- **Cap:** `11_…md` §9's inventory covers **only** categories whose `Status` is `existing`. Eight learner-facing categories are `required-by-upstream` or `assumed` — `SC-S3-32`, `SC-S3-37`, `SC-S3-38`, `SC-S3-39`, `SC-S3-42`, `SC-S3-43`, `SC-S3-44`, `SC-S3-45` — and **none has a store in any component**. Each carries a **stated deliberate non-exposure with a named lifting condition** (§10.3) instead of an inventory entry. The inventory is therefore a count over **what can be served today**, not over the eventual surface, and it is published as such.
- **Why it is capped:** **No reading settles it**, because the missing stores are the outputs of packages that do not exist — NEU-887, NEU-888, NEU-889, NEU-890 for the four `required-by-upstream` rows, and the stand-ins `A-25`, `A-27`, `A-28`, `A-29` for the four `assumed` ones. **No bounded experiment settles it**: there is nothing to probe. And it is **not an open item**, because the events that would close it — four upstream packages landing and four stand-ins confirming — are not inside this charter's reach and are not one event but eight. Publishing capabilities over them instead was considered and **rejected in `DR-C10-S7-1`** (rejected alternative 4): the exactly-one-authority audit would then have passed on entries resolving to an authority for a category **nothing holds**, which is a green audit over resources that cannot be served.
- **What it leaves unsupported:** Any reading of `11_…md` §9's **sixteen** entries as the web API's eventual surface — it is a **lower bound**, and a plan sized from it under-counts by at least eight read-projections. Any claim that the negative boundary's shape is stable: eight of its forty-five rows are not-owned **and** not-exposed today for two different reasons at once (no authority for the API *and* no store for anyone), and only the first of those reasons is permanent under `M-A`. Any inference that a category's absence from §9 means the surface will never carry it — §10.3 states the lifting condition for each precisely so that inference is not available.
- **Owner:** **`SUB-15 (NEU-982)`**, which decides the protocol style and rendering model against an inventory it now knows is a lower bound, and **`SUB-12 (NEU-986)`** at the completeness gate. Named alongside **`NEU-896`**, which converges over a package in which four upstream dependencies are unbuilt.
- **What would lift it:** For the four `required-by-upstream` rows, the owning upstream package landing **and** the category acquiring a store in this system — publication upstream alone is not enough, as `../04_…md` §3.6 says of `SC-S3-37` and `SC-S3-40`. For the four `assumed` rows, the corresponding stand-in's re-validation trigger firing (`A-27`'s is **NEU-892 lands**). `SC-S3-39` carries an extra condition even then: `A-27`'s envelope requires the server to re-evaluate every gate from server-held state, so it would lift to **read-only** and never to a write-intent.

Recorded for the reconciliation, the existing caps that bound `11_…md`, each **cited at the point of use and none re-filed**:

- **`CAP-S4-1`** — no component can be the deletion owner for `SC-S3-16` or `SC-S3-17`, and the obstruction is **structural**. **It stays open and this sub-task does not close it.** This is its **seventh sighting** (SUB-3, SUB-4, SUB-6, SUB-13, SUB-14, SUB-16, here). What this pass adds is a **new consequence** rather than a new sighting of the old one: the cap now blocks a **learner-facing capability**, not only a retention policy — the surface must offer erasure and has nowhere to send the intent. `CAP-S7-1` carries that consequence; `../05_…md` §9.2's unblocking condition is unchanged.
- **`CAP-S16-1`** — `SC-S3-45`'s isolation verdict is permanently unimprovable, its authority being external (`CMP-S4-10`). Cited at `11_…md` §8.2 claim 2 and §10.3, where the row's non-exposure as a **writable** resource is recorded as structural rather than deliberate. Not narrowed, not re-filed; owner unchanged, **`SUB-6 (NEU-976)`**.
- **`CAP-S3-1`** — the problem-citation field set is frozen at `stable_id` + `canonical_url` under `DR-C09-01`, with the wider set carried against ledger challenge `CH-F5-1`. Cited at `11_…md` §10.1 and §10.3 for `SC-S3-32`; **the set is not widened here**, and a downstream surface needing a wider one cites `CH-F5-1`, not this chapter.
- **`CAP-S5-1`** — the isolation invariant is published with zero positive instances and is not proved satisfiable. It bounds what §8's security claims may assert: **none of them claims confinement holds**, only which component enforces what, on which transport, at which tier.
- **`CAP-S1-3`** — **no QA pass exists for this package.** It applies unchanged: `qa-execution:engine` and `qa-execution:host` are both unconfigured, so scenario execution is a genuine **Core Article 8 no-op**, the scenarios authored for `NEU-980` are marked **`NOT RUN`**, and **no QA pass is claimed anywhere in `11_…md`**. A mechanical enumeration of forty-five rows is not a QA pass, and neither is a documentation gate.

Three limits that might look like caps and are deliberately **not** filed as such, so the reconciliation is not left guessing:

- **The missing *web session → principal* category** is a **finding plus an open item, not a cap** — `F-S7-1` routed to **`SUB-13 (NEU-977)`** with root cause at **`SUB-3 (NEU-973)`**, and `OI-S7-1` carrying the provisional reading with **`SUB-15 (NEU-982)`** as owner and an observable resolving event. A cap is for a limit no party settles; here a party exists and the event is inside this charter's reach. That the routed owner is **closed** is the `F5.7` residual, which does not convert a finding into a cap — `10_…md`'s `OI-S16-1` makes the same distinction from the other side.
- **`SC-S3-43`'s unenforceable assignment** is a **finding, not a cap** — `F-S7-3`, routed to `SUB-13 (NEU-977)` and `SUB-15 (NEU-982)`. It names an available party and its isolation consequence is nil; it is a statement about what the assignment *means*, which a rendering-model decision resolves.
- **The absent wire contract** is **neither a cap nor an open item** — it is a **deliberate stop**, audited as three zero counts and stated in prose at `11_…md` §12, with **`SUB-15 (NEU-982)`** named as the owner of what comes next. Filing a cap over a scope line this sub-task drew on purpose would misreport a decision as a limitation.

---

### SUB-8

*`NEU-981`, covering `OUT-6`. **One** cap. `12_…md` publishes a classification rule, a compatibility contract, five regression-detection methods and an obligated identity option — and can verify **none** of them by execution. That single limit is stated once rather than split across the four artifacts it bounds.*

#### `CAP-S8-1` — The compatibility contract's detection methods are specified, never executed; the contract is published unverified

- **Id:** `CAP-S8-1`
- **Cap:** `12_…md` §8.2 defines five regression-detection methods — `RD-S8-1` (cross-principal replay), `RD-S8-2` (issue-time binding probe), `RD-S8-3` (transport-parity probe), `RD-S8-4` (golden manifest snapshot), `RD-S8-5` (error-envelope and findings-routing probe). **Not one is run.** No method is demonstrated to detect the break it names, and no expected-zero it asserts is mechanically confirmed. The contract is published as a **specification of how a break would be caught**, never as evidence that one would be.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. (i) **There is nothing to run them against.** Every obligation they police is on unwritten code: `context_tokens` has no principal column (`OI-S8-1`), no STDIO gate exists (`OI-S8-2`), and `NEU-850`'s `user_id` commitment is converged but unimplemented. `RD-S8-1` and `RD-S8-2` would test a mechanism with no implementation. (ii) **There is no host for them.** Test authoring, CI configuration and the regression suite are explicitly out of this sub-task's scope, and `12_…md` writes no file under `src/`, `tests/` or `drizzle/`. (iii) **`RD-S8-4` is the one method runnable today, and running it here would prove nothing that matters** — it would snapshot the current manifest, which is the baseline by definition; its value is entirely in the *second* run, after a change this package does not make. (iv) **`CAP-S1-3` applies unchanged**: `qa-execution:engine` and `qa-execution:host` are both unconfigured, so scenario execution is a genuine Core Article 8 no-op, `NEU-981`'s QA is recorded **`NOT RUN`**, and **no QA pass is claimed anywhere in `12_…md`**. A 49-entry enumeration is not a QA pass, and neither is a documentation gate.
- **What it leaves unsupported:** Named, so the cost is visible rather than implied. (i) **The audit's headline result is unverified in the direction that matters.** Driver A returns 44 contract-changed of 49, and **not one is a schema change** — `RD-S8-4` is *specified* to return zero delta across all 49. That expected zero is itself the assertion, and it is unchecked. A reader who runs only the mechanical check sees nothing, which is precisely the failure `RD-S8-1` exists to prevent and precisely what is not demonstrated. (ii) **`DR-C10-S8-2`'s rejected alternative 2 is undetected.** Reusing the token argument to carry a *caller-encoded* principal is indistinguishable in a diff from the chosen option and opposite in threat model. `RD-S8-1` is the only thing that would tell them apart, and it is unrun — so the decision's single most likely misimplementation has no active guard. (iii) **The transport asymmetry is documented, not policed.** `RD-S8-3` is written to *record* today's HTTP-refuses/STDIO-succeeds result rather than fail on it, so nothing detects the gap widening before `CC-S8-3` lands.
- **Owner:** **`SUB-10 (NEU-984)`**, which carries the implied core changes forward and is the first party with an implementation to run them against. Co-named: **`NEU-896`**, since the regression suite that would host them is a programme-level asset no sub-task of this charter owns.
- **What would lift it:** An implementation of `CC-S8-1` or `CC-S8-2` on `origin/develop` **together with** a test host able to run `RD-S8-1` against it — that is, a two-principal HTTP harness with a reachable database. Lifting requires **both**: a harness with nothing to test, or a mechanism with no harness, leaves the cap exactly where it is. Note this is a strictly harder condition than `CAP-S6-1`'s (a reachable Postgres alone), and `CAP-S6-1`'s lifting condition is a **prerequisite** of this one rather than a substitute for it.

---

### SUB-15

*Appended by `NEU-982` (SUB-15) on 2026-08-22. Verification cutoff `229e8f4`. Nothing above this heading was edited; in particular `CAP-S4-1` is left exactly as SUB-4 published it.*

#### `CAP-S15-1` — The real deployed round-trip cost of a web-tier tool call is unmeasured, so all three decisions are priced against an in-process floor

- **Id:** `CAP-S15-1`
- **Cap:** This package **does not know what a web-tier-to-MCP tool call costs in a real deployment**, and all three of `13_…md`'s decisions are taken without that number. `DR-C10-S15-4` commits every gate-bearing read to a server round trip and `DR-C10-S15-3` commits all five write-intents to forwarded tool calls, so the per-call cost is multiplied across the entire surface — and the only measurement the package holds, `SPK-S6-1`'s ≤0.02% of the `A-25` budget, is an **in-process transport floor with no network hop, no TLS handshake, no connection setup and no inter-host latency** (`F-S15-2`). The cap is the gap between that floor and the deployed cost, and it is not a small or a formal gap: the floor excludes every term that dominates a real distributed call.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) There is no deployment to measure.** Deployment shape, hosting topology and process boundaries are `SUB-10 (NEU-984)`'s to select and are unselected — `OI-S1-3` records that hosting region, provider and TLS termination are not even discoverable in the repository. A round-trip measurement requires two hosts and a network path between them, and this package can name neither. **(ii) Measuring it against a synthetic topology would produce a number with no referent.** Standing up a loopback HTTP pair would yield a figure that is neither the in-process floor nor the deployed cost, and `92_spike-register.md` §3's justification test rejects a measurement whose result answers no question anyone will ask — worse, a plausible-looking number is more dangerous than a stated gap, because it invites exactly the citation drift `F-S15-2` exists to prevent. **(iii) Re-running `SPK-S6-1` differently is not available to this sub-task**, whose scope is the classification rule and three web-tier decisions; re-opening a merged sibling's spike to change its transport is outside it. **(iv) `CAP-S6-1` applies unchanged and compounds** — the two-writer divergence was never observed against a live database for the same structural reason: this charter has no running system.
- **What it leaves unsupported:** Named, so the cost is visible rather than implied. **(i) `DR-C10-S15-4`'s consequence set is unpriced.** The record's decisive criterion is `DR-C10-S4-3`'s *"stale-or-absent is the serve path's ordinary operating mode"* plus `R-4` — a **correctness** argument that stands whatever the latency turns out to be — but the *user-visible cost* of routing every gate-bearing read through the server is asserted acceptable rather than shown. If the deployed round trip is materially worse than the floor, the decision does not become wrong; it becomes **expensive**, and nothing here says how expensive. **(ii) `DR-C10-S15-3`'s write path multiplies an unknown.** Five intents, each a forwarded tool call, and `../11_…md` §9.2 confirms the API performs none of the writes itself — so every write pays the unknown at least once, and a composite learner action may pay it repeatedly. **(iii) `A-25`'s sub-second budget is unaudited against the chosen shape.** The budget is a premise these decisions inherit and none of them re-checks it, because re-checking requires the number this cap says is absent. **(iv) The comparison that would justify a client-side cache is unavailable** — `DR-C10-S15-4`'s rejected alternative 1 is rejected on the trust and staleness argument, and it would be *reinforced or complicated* by real latency data. Rejecting it without that data is correct on the evidence held and leaves the strongest counter-argument to it untested.
- **Owner:** **`SUB-10 (NEU-984)`**, which selects deployment shape and data-store topology and is the first party with a topology concrete enough to measure against. Co-named: **`NEU-896`**, since a performance harness spanning two deployed tiers is a programme-level asset no sub-task of this charter owns, and `CAP-S8-1` has already named the same absent host for the same structural reason.
- **What would lift it:** A **selected deployment topology** — process boundaries, host placement and network path all named — **together with** a harness able to issue a real web-tier-to-MCP call across it and report a distribution rather than a single sample. Lifting requires both: a topology with no harness yields an estimate, and a harness with no topology yields another floor. Note this is a strictly harder condition than `SPK-S6-1`'s, whose in-process pairing needed neither, and that `SPK-S6-1`'s result is not superseded by lifting this cap — it remains the correct floor, and the lifted measurement would sit above it.

#### `CAP-S15-2` — All three decisions rest on `A-27`, which is `[unconfirmed]`, and they go stale together rather than independently

- **Id:** `CAP-S15-2`
- **Cap:** This package **cannot confirm `A-27`**, the assumption that fixes what the browser is permitted to hold, and **all three** of `13_…md`'s decisions depend on it. The dependency is not incidental to one of them: `DR-C10-S15-4` cites `A-27` in its decision sentence and rejects the offline-capable alternative on `A-27`'s own named invalidating outcome; `DR-C10-S15-3`'s read surface is shaped by what the server must compose, which is what `A-27` bounds; and `DR-C10-S15-2`'s contract-sharing argument presumes a web tier that holds no durable authoritative state of its own. The cap is that a **single unconfirmed assumption is load-bearing for the whole chapter**, so the three decisions do **not** fail independently — they are correlated, and a reader assessing risk by counting how many decisions rest on confirmed inputs will over-count this chapter's robustness.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) `A-27` is a product and privacy commitment, not a technical fact.** What a learner's browser may durably hold is a decision about the product's posture toward learner data; no measurement of this repository can return it, and no file in the tree encodes it. **(ii) The party that can confirm it is above this charter.** `A-27` is an umbrella-level assumption owned at `NEU-896`; a sub-task cannot promote its own dependency's status, and doing so would convert an `[unconfirmed]` input into a confirmed one by fiat — the precise failure the `[unconfirmed]` tagging convention exists to prevent. **(iii) Deciding without it was not optional.** The alternative to depending on `A-27` was to defer all three decisions, which `../11_…md` §5.3 and §15 forbid by naming SUB-15 the owner of exactly these three, and which `DR-C10-S15-3`'s rejected alternative 5 rejects on ordering grounds. So the dependency is **taken deliberately and recorded**, rather than avoided. **(iv) `F-S7-3` shows the assumption is already doing contested work** — `SC-S3-43`'s assignment to `CMP-S4-9` is unenforceable for exactly the portion of UI state `A-27` permits to stay in the browser — so this is not a dormant assumption whose confirmation is a formality.
- **What it leaves unsupported:** Named, so the cost is visible. **(i) The correlation is recorded in every record, but one record's dependency is indirect.** `DR-C10-S15-2`'s revision trigger 5 and `DR-C10-S15-4`'s consequence 5 each state that all three decisions go stale together, and `13_…md` §8.3 carries the same statement, so the correlation is not confined to this entry. **`DR-C10-S15-3`'s dependency is the one that is inherited rather than direct:** the protocol style is argued from the inventory's shape, and its `A-27` dependency arrives through the read surface's server-composed shape rather than from the assumption itself. It is carried at that record's revision trigger 5, added for exactly that reason. The residual is that an **indirect** dependency is easier to lose in a future revision of either record than a direct one — not that it is unrecorded. **(ii) `DR-C10-S15-4`'s rejected alternative 2 inverts if `A-27` is relaxed.** Offline-capable/local-first is rejected on `A-27`'s named invalidating outcome; if the assumption is confirmed *in a weaker form* than assumed, that rejection weakens and the record's alternative set must be re-scored rather than merely re-read. **(iii) The chapter's residual risk is understated by construction.** Three decisions, one shared unconfirmed premise, and the package's own registers list them as three entries — the aggregation is not visible from the register shape, which is why it is filed as a cap rather than left to inference. **(iv) No fallback shape is specified.** If `A-27` is invalidated, this chapter says the three decisions re-open; it does **not** say what they would become, and constructing that fallback would require deciding against an assumption that does not yet exist.
- **Owner:** **`NEU-896`** (the umbrella), which owns `A-27` and is the only party that can confirm, weaken or withdraw it. Named alone rather than co-named with a sub-task: no sibling can discharge this, and co-naming one would spread accountability for a decision that has exactly one home.
- **What would lift it:** **`A-27` moving off `[unconfirmed]`** — confirmed, weakened to a stated envelope, or withdrawn — published at the umbrella with its scope stated in terms a decision record can cite. Confirmation lifts the cap and leaves all three decisions standing unchanged. Weakening or withdrawal does **not** lift it: it converts the cap into a **re-open obligation** on all three records at once, which is the outcome this entry exists to make legible in advance. Note that lifting requires the assumption's *status* to change, not merely for time to pass or for a downstream charter to proceed as if it were confirmed — proceeding is not confirming, and this cap is not discharged by anyone's reliance on it.

---

### SUB-9

*Appended by `NEU-983` (SUB-9) on 2026-08-22. Verification cutoff `0962279`. Nothing above this heading was edited.*

#### `CAP-S9-1` — Whether one maintainer can operate a split-visibility release pipeline on the current CI is not settled, and `K9`'s margin rests on it

- **Id:** `CAP-S9-1`
- **Cap:** `14_…md` §5.9 scores criterion `K9` (release) in the selected split-visibility workspace's favour on the ground that it needs **one** pipeline where the rejected separate-repository alternative needs **two**, plus a cross-repository release ordering. That comparison is about **pipeline count**, which is countable and is not capped. What **is** capped is whether the resulting single pipeline — one that publishes a public core subtree to a registry while never publishing the private application subtree, and that retargets a `version-bump` job which today writes the **root** `package.json` — is **operable by one person**. The package **does not know**, and `14_…md` does not assert it. Note precisely what is capped: not whether such a pipeline can exist, but whether the **sole maintainer and operator** (criterion input (a), charter assumption 11, **confirmed**) can build and run it without it becoming the kind of apparatus that decays untended.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) Settling it requires designing the pipeline, which this chapter is scoped out of.** `14_…md` §1 excludes build tooling, package-manager configuration and CI pipeline design, and `13_…md` §4.7 derives that exclusion from the architecture-material rule rather than asserting it — those choices are reversible without a boundary, an authority or a compatibility contract moving. A spike that designed the pipeline in order to test it would have performed the excluded work under a different name. **(ii) There is nothing to run it against.** The workspace does not exist: at `0962279` `pnpm-workspace.yaml` declares **no `packages:` key** and there is **no `apps/`, `packages/` or web directory** (`F-S9-2`). A pipeline validated against a stand-in workspace would demonstrate that pnpm can publish one member and not another — which is not in doubt — rather than that this maintainer can operate this pipeline. **(iii) The question is partly about a person, not only about a system.** Operability by a sole maintainer is a claim about sustained attention over time; no single run returns it, and a green pipeline on day one is weak evidence for it. **(iv) The readable half was read and is recorded rather than capped.** `.github/workflows/ci.yml` at `0962279` shows one `build-test-lint` job, a single-entry Node matrix, `actions/checkout@v4` invoked with **no `repository:` and no cross-repository token**, **no registry authentication** and **no publish step** anywhere; a public MIT package additionally needs no registry credential to be *consumed*. Those facts are stated in `14_…md` §5.9 as evidence. The cap covers only the residue that reading could not reach.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) `K9`'s margin, not its direction.** The separate-repository alternative unambiguously requires two pipelines, two version-bump loops and a publish-then-consume ordering enforced by one person's memory; that comparison holds however this cap resolves. If the cap resolved badly — a single split-visibility pipeline proving unworkable for one maintainer — `K9` would narrow toward level. **It would not invert**, because the alternative's costs do not disappear when the selection's costs rise. **(ii) The selection is not exposed.** `K9` carries weight `high`; the decisive criterion is `K4` (compatibility detection reach), which `14_…md` §5.4 argues from `SPK-S9-1`'s measured resolution behaviour and which is untouched by pipeline operability. `DR-C10-S9-1` therefore does not rest on this cap. **(iii) Migration step `M8` is stated and not costed.** `14_…md` §8 names what must become true of CI and stops; the effort and the failure modes of getting there are not estimated anywhere in this package. **(iv) The `version-bump` retargeting is the concrete unknown.** That job is the mechanism behind **250 of the 720** commits at `0962279` (`F-S9-1`), it writes the root manifest today, and under the selected topology the root manifest is the one that must **stay** private (`OI-S9-3`). Whether retargeting it is a one-line change or a rework is not established here.
- **Owner:** **`NEU-896`**, which owns the implementation and is the first party with a workspace concrete enough to build a pipeline against. Named alone rather than co-named with `SUB-10 (NEU-984)`: SUB-10 owns **deployment shape**, and this cap is about **release and publication**, which the selected topology fixes independently of where processes run — `14_…md` §6.4 establishes that deployment shape has no purchase on the topology selection, and it has none on this cap either.
- **What would lift it:** A workspace on `origin/develop` with **both** member trees present, **together with** a pipeline that publishes the core member while withholding the application member, **observed over at least one real release**. Lifting requires all three: a workspace with no pipeline yields a directory layout, a pipeline with no release yields a configuration that has never been exercised, and a single release yields a green run rather than evidence about sustained operability — which is why the condition names a real release rather than a successful dry run. Note this is a strictly harder condition than `OI-S9-2`'s resolving event, which inspects **one** packed artifact: that item asks whether the filter is correct, and this cap asks whether the whole apparatus is operable. `OI-S9-2` closing does **not** lift this cap.

---

### SUB-10

*Appended by `NEU-984` (SUB-10) on 2026-08-22. Verification cutoff `03efe1d`. Nothing above this heading was edited; in particular `CAP-S4-1` is left exactly as SUB-4 published it, and `CAP-S6-1`, `CAP-S8-1` and `CAP-S15-1` — all three of which name this sub-task as owner — are left unedited and unlifted. Their status after this sub-task is recorded at `15_…md` §10.3, not by amending them here.*

#### `CAP-S10-1` — Hosting, TLS termination, backup and monitoring are not discoverable in the repository, and no operator answer was available

- **Id:** `CAP-S10-1`
- **Cap:** This package **does not know** where the system runs, who terminates TLS, whether anything is backed up, or whether anything is monitored. `OI-S1-3` (charter assumption 31) records these as not discoverable in the repository and names `SUB-10 (NEU-984)` as both consumer and owner, with a resolving event of "each fact recorded with the operator's answer cited, **or converted to a `CAP-S10-<k>` entry** stating which fact remains unknown and what decision it leaves unsupported." **This entry is that conversion.** The unknown facts, enumerated: hosting region; hosting provider; TLS termination point; backup existence, scope and restore procedure; monitoring, alerting and on-call arrangement. Four further repository absences sit in the same class and are covered here rather than separately — **no Dockerfile** anywhere in the tree despite the deploy running `--build`, **no IaC** of any kind, **no reverse-proxy configuration**, and **no metrics exporter or tracing SDK**.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) The facts are off-repo by construction.** The production compose stack lives in a `COMPOSE_DIR` on the host that no file in this repository describes; `.github/workflows/cd-prod.yml` names the directory and never its contents. Reading harder does not reach them — this is the same class of absence `OI-S1-3` already established, re-verified rather than assumed. **(ii) The one party who could answer is not reachable in this run.** `OI-S1-3`'s primary route is an operator's answer, cited. This sub-task ran with no interactive channel, so the primary route was unavailable and the item's own alternate route was taken. **(iii) Discovering them by probing would be a deployment action**, which `OUT-8` places out of scope alongside hosting, TLS, backup and monitoring themselves — a spike that connected to production to enumerate its topology would have performed excluded work under a different name. **(iv) Selecting any of them is out of scope regardless.** Even a complete answer would be recorded, not decided: `OUT-8` excludes hosting region, provider, TLS, backup and monitoring from this sub-task's decisions.
- **What it leaves unsupported:** Named, so the cost is visible rather than implied. **(i) The two-process shape has no described front door.** `DR-C10-S10-2` places two processes on one host and says nothing about what routes to which, because no reverse-proxy configuration exists to read and selecting one is excluded. **(ii) Three facts of the production-compatibility assessment return *cannot be determined* on this cap's account** — `15_…md` §9 facts 3, 5 and 6 — and a fourth, fact 9, returns *not operable* partly because there is no monitoring against which a better readiness signal could be judged. **(iii) `/health` cannot be improved without knowing what consumes it.** It returns a static `{status:'ok'}` (`src/transport/http.ts:91`) and the deploy gate polls `docker inspect`; whether anything else watches it is unknown, so the cost of changing it is unknown. **(iv) No restore path is known for the failure `F-S10-2` describes.** A bad migration is not revertible by the deploy path, and whether a backup exists to restore from is exactly what this cap does not know — so the severity of `15_…md` §9 fact 7 is itself uncertain, in the unsafe direction.
- **Owner:** **The operator** — the only party holding the answers — reconciled at **`NEU-896`**, which owns the umbrella-level reconciliation surface and is the first place a recorded operator answer would land. Not co-named with an implementation sub-task: none of this charter's remaining passes can produce the facts, and naming one would record an owner that cannot act, which is the failure `OI-S16-1` names.
- **What would lift it:** **The operator answering each named fact, with the answer cited**, per `OI-S1-3`'s primary route — or the equivalent becoming readable in the repository, which would require the production compose stack, a Dockerfile and any proxy configuration to be brought in-tree. Lifting requires the **facts**, not a decision: this cap is not discharged by someone selecting a hosting provider, because the cap is about what is currently true and unknown, not about what should be chosen. Partial lifting is meaningful and expected — each fact is independently answerable, and the cap narrows as they arrive rather than resolving all at once.

#### `CAP-S10-2` — The citation-drift producer's scheduling and dequeue mechanism is not selected

- **Id:** `CAP-S10-2`
- **Cap:** `DR-C10-S2-3` specifies an out-of-band citation-drift verdict producer whose trigger "enqueues per served citation and **never** executes inline", and `03_…md` §4.2 leaves **what performs the dequeue unselected**. `OI-S2-1` routes that selection to this sub-task. **This sub-task does not select it.** What it does decide is narrower and evidence-backed: `DR-C10-S10-2` consequence 6 places the producer **outside the serve path's process**, and `15_…md` §5.4 places its state (`SC-S3-33`, `SC-S3-34`) in the drift component's own store under `CMP-S4-17`. A **process placement** and a **store** are not a scheduler. `OI-S2-1`'s resolving event permits exactly this outcome — "or filing a `CAP-S10-<k>` stating that it does not select one and what that leaves unsupported… whichever way it goes" — while forbidding the alternative it would otherwise invite: "**A plausible guess at a mechanism is not a resolution.**" No guess is offered here.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) There is nothing in the repository to adopt.** A search across `src/` at `03efe1d` for cron, scheduling, queue, job-runner or worker infrastructure returns **only false positives**: fixed-interval flush timers in `src/transport/pg-event-transport.ts:42`, `:138` and `src/transport/pg-audit-transport.ts:46`, `:151`, which batch an in-memory buffer to Postgres and are not a scheduled-job system, and local BFS queues in `src/orchestration/session-workflows.ts:236` and `src/domain/algorithms/dependency-resolver.ts:227`, which are `const queue: string[]` variables. `package.json` declares no job or queue library. **(ii) The candidates differ in what the substrate must provide, and the substrate facts are `CAP-S10-1`.** `03_…md` §4.2 names four consistent candidates — a cron-style periodic pass, a queue worker, a job runner co-located with the MCP core process, and an operator-triggered batch. An external scheduler's availability on the host is precisely the class of fact `CAP-S10-1` records as unknown, and the co-located option's viability depends on `CAP-S10-3`'s unverified single-instance premise. **(iii) Selecting the mechanism by naming a product would be a library pick**, which `OUT-8` excludes. **(iv) A spike could not settle it** — there is no deployment to run a scheduler on, and the question is a selection rather than a measurement.
- **What it leaves unsupported:** Named, so the cost is visible. **(i) The producer has a placement and a store but nothing that starts it.** `DR-C10-S2-3`'s "the serve path reads a verdict; it never computes one" holds only if something computes one, and this cap is the gap between the specification and anything running. **(ii) `SC-S3-33`'s cache has no defined population path.** `15_…md` §5.4 gives it a destination; a destination with no writer means a serve path that reads a verdict which is always absent, which `DR-C10-S2-3` handles by quarantining with `reason: retracted-input` — so the failure is graceful and **permanent** until this cap lifts. **(iii) The interaction `15_…md` §5.4 records between `SC-S3-36` and `SC-S3-33` is unexercised**, since quarantine reacts to a verdict that is never produced. **(iv) No latency or freshness property of the drift pipeline can be stated**, because nothing determines how long a stale verdict persists.
- **Owner:** **`NEU-896`**, which owns the implementation and is the first party with a deployment concrete enough to host a scheduler. Named alone rather than co-named with a sibling: no remaining pass in this charter selects mechanisms, and `SUB-11` audits and `SUB-12` gates without re-deciding anything left open.
- **What would lift it:** **A scheduling mechanism named and published**, against a substrate known to provide it. Lifting therefore requires `CAP-S10-1` to have narrowed at least to the point of establishing what the host can run — an in-tree Dockerfile or compose stack, or an operator's answer about process supervision. A mechanism named against unknown substrate facts would be the plausible guess `OI-S2-1` forbids, so this cap is **not** lifted by a selection made in the abstract. Note that `CAP-S10-4` shares this cap's root cause and would be lifted by the same substrate knowledge, but not automatically by the same selection: a drift scheduler is not a DP-map importer.

#### `CAP-S10-3` — The production instance count and process supervisor are not discoverable; the single-instance premise is self-documented, not verified

- **Id:** `CAP-S10-3`
- **Cap:** `DR-C10-S10-2` clause 2 fixes **one instance of each process**, and that clause carries three of `OI-S13-1`'s four cross-category interaction resolutions plus the rejection of horizontal replication. The repository **documents** the single-instance assumption in three places — `.env.example:79`–`:81` states it in prose, `.github/workflows/cd-prod.yml` names a single `secrets.VPS_HOST`, and `docker compose ps -q app` expects one container — but **documents is not verifies**. The actual number of running instances, and the process supervisor that would keep it at that number, are **off-repo facts** in the production compose stack. The cap is the gap between a self-documented assumption and an observed one, and the direction matters: every conclusion resting on clause 2 fails **silently** if the premise is false, because a second replica produces wrong counters and rejected sessions rather than an error anyone would attribute to instance count.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) The production compose stack is off-repo**, so the `replicas`, `deploy` and restart-policy directives that would answer it are not readable here — the same structural absence as `CAP-S10-1`, from which this cap is separated because it bears on different conclusions and could be lifted by a narrower answer. **(ii) Observing it would require connecting to production**, which is a deployment action `OUT-8` excludes. **(iii) The three in-repo indicators are consistent with the assumption but do not establish it.** A single `VPS_HOST` bounds the *host* count at one and says nothing about processes on it; `docker compose ps -q app` returning one id is what the deploy script *expects*, not what it enforces; and `.env.example`'s comment is the codebase describing its own assumption, which is exactly the claim in question rather than evidence for it. **(iv) No spike could settle it** — the question is about a running system this charter cannot reach.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) Three interaction resolutions hold conditionally.** `15_…md` §5.5 resolves `SC-S3-20` (rate-limit counters multiplied by instance count), `SC-S3-21` (breaker state diverging across replicas) and `SC-S3-18` (MCP session affinity) by making divergence *unreachable* at one instance. If the premise is false, all three revert to the two options `08_…md` §8.4 offered, and `F-S10-3` describes the observable damage: rejected in-flight sessions, `404`/`-32000` on POST and `400` on GET/DELETE. **(ii) `DR-C10-S10-2`'s rejection of horizontal replication is a rejection of the *option*, not evidence that it is not the *reality*.** **(iii) `F-S10-2`'s migration race becomes live rather than hypothetical** if a second MCP core replica exists, since clause 3's exclusivity would already be violated. **(iv) The rate limit's effective value is unknown** — `RATE_LIMIT_MAX` times an unknown replica count is not a number this package can state. What it does **not** leave unsupported: `DR-C10-S10-1`'s store topology, whose decisive criterion is the atomicity requirement and is independent of instance count.
- **Owner:** **The operator**, reconciled at **`NEU-896`** — the same pairing and for the same reason as `CAP-S10-1`, from which this is separated because a single narrow answer ("how many instances run, and what supervises them?") lifts it without touching hosting region, TLS, backup or monitoring.
- **What would lift it:** **An observed instance count and a named process supervisor**, either from the operator with the answer cited, or by the production compose stack coming in-tree where its `replicas` and restart policy could be read. Lifting requires an **observation or a configuration**, not an intention: a statement that the system is *meant* to run single-instance is what `.env.example:79`–`:81` already provides, and it is what this cap exists to distinguish from evidence. Note the asymmetry — an answer of "one" **lifts** the cap and leaves `DR-C10-S10-2` standing unchanged, whereas an answer greater than one does **not** merely lift it: it fires `DR-C10-S10-2` revision trigger 4, and the record is re-decided rather than patched.

#### `CAP-S10-4` — The `SC-S3-37` DP-map import mechanism is not selected, and the sub-task that shared ownership of it has shipped

- **Id:** `CAP-S10-4`
- **Cap:** `08_…md` §8.6 records `SC-S3-37` (DP-map node and prerequisite-edge records) as arriving "as an imported copy under `CMP-S4-7`, written through `CMP-S4-13`", and states that "**the import mechanism is SUB-10's and SUB-8's**". `15_…md` §5.4 supplies the **destination** — the shared store, as the one in-system copy `05_…md` §8 established when it discharged `OI-S3-2`. **Nothing is selected that performs the import.** `SUB-8 (NEU-981)` is merged and closed, so the shared ownership this cell names has one live holder, and that holder does not select it here.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) It has the same root cause as `CAP-S10-2`** — no scheduler, batch runner or job infrastructure exists anywhere in `src/` to adopt, and none is selected. An import is periodic or triggered work by nature, and this repository has neither mechanism. **(ii) The re-import path is the part that matters, and it is a schema question this sub-task is scoped out of.** `08_…md` §8.6 records the interaction with `SC-S3-38` — "a re-import that removes a node leaves progression rows pointing at nothing" — and `15_…md` §5.5 resolves that to *expressible*, since both rows land in one store. Whether a referential constraint is **declared**, and what a re-import does when it would violate one, is endpoint and schema design, which `OUT-8` excludes. **(iii) Half the named ownership is unreachable.** SUB-8 has shipped; a mechanism this sub-task selected alone would silently take a decision the cell allocated to two parties. **(iv) Selecting an import tool would be a library pick**, excluded by `OUT-8`.
- **What it leaves unsupported:** Named, so the cost is visible. **(i) `SC-S3-37` has a destination and no writer**, so the imported copy `05_…md` §8 relies on does not come into existence. **(ii) Two components read a copy that nothing populates** — `CMP-S4-13` to place a content unit on a node, and `CMP-S4-16` on the serve path. **(iii) `SC-S3-38`'s referential safety is expressible but unexercised**, because there is no re-import event to test it against; `15_…md` §5.5's resolution is therefore correct and inert. **(iv) `OI-S4-1`'s re-import staleness question has nothing to attach to** until a mechanism exists. What it does **not** leave unsupported: `OI-S3-2` itself, which SUB-4 discharged by publication and which this cap neither reopens nor weakens — the placement question is answered; only the mechanism is absent.
- **Owner:** **`NEU-896`**, which owns the implementation and inherits both halves of the shared ownership now that SUB-8 has shipped. Named alone: no remaining pass in this charter selects mechanisms.
- **What would lift it:** **An import mechanism published on `origin/develop`** — whatever performs the import, named — **together with** its behaviour on re-import stated against `SC-S3-38`. Lifting requires both: a mechanism that imports but leaves the removal case undefined re-creates the exact interaction `OI-S13-1` flagged, one layer down. As with `CAP-S10-2`, this cap depends on `CAP-S10-1` having narrowed enough to establish what the host can run; unlike `CAP-S10-2`, it would additionally be lifted by an import performed as a one-off operator action, since a DP-map import need not be recurring to be correct.

### SUB-11

*One cap, from running the package's four mechanical audits (`16_mechanical-audits.md`). **This sub-task lifts no existing cap and adds no sighting to one.** In particular it files **no** further sighting of the deletion-owner gap (`F-S3-3` → `CAP-S3-3` → `CAP-S4-1`, at its seventh sighting at `:297`): the four audits do not reach it, and — following the precedent SUB-5 and SUB-6 each set at `:180` and `:196` — the register admits second sightings as signal without obliging one per direction. `CAP-S4-1`, `CAP-S6-1` and `CAP-S15-1` remain open and unlifted at this cutoff. **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended.***

#### `CAP-S11-1` — The independent cold read `OUT-1` and `OUT-12` both require is performed by no sub-task, and the auditing sub-task is disqualified from performing it

- **Id:** `CAP-S11-1`
- **Cap:** **Two outcomes name the same verification, and nothing in the package performs it.** `01_outcome-register.md` `OUT-1` requires *"a cold-read by an independent implementation agent who must name each boundary's owner without asking"*; `OUT-12` requires *"a cold-read review by an independent implementation agent working only from the published package, who must reach the boundaries, authorities and topology without asking a question"*. `94_package-completeness-gate.md:42` makes the independence binding — *"A producing task does not grade its own artifact; the cold-read review `OUT-12` requires is by an independent reader working only from the published package."* The gap is **visible and unregistered**: `05_system-context-and-responsibility-boundaries.md:632` states plainly that *"a cold read is the acceptance test this document cannot run on itself"*, and `traceability/S4_component-and-boundary-coverage.md:21` records in its Residual column that *"the cold read itself is not run here"*. **Neither files it.** Until this entry, the only surface on which the package's largest unperformed verification appeared was one sentence of prose and one residual cell.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) The auditing sub-task is structurally disqualified.** `NEU-985 (SUB-11)` read the gitignored brief and every register in the package before writing a line, so it fails the *"working only from the published package"* condition by construction — and a cold read performed by a reader who already knows the answers measures nothing. **(ii) No producing sub-task can run it on its own artifact**, which is `94_…md:42`'s rule and the reason SUB-4 declined at `05_…md:632` rather than self-certifying. **(iii) `SUB-12 (NEU-986)` cannot run it either**: the completeness gate is itself a producing artifact over the same package, and by the time SUB-12 reads it the independence is already spent. **(iv) It is not a spike** — nothing is settled by a bounded experiment; the thing to be established is whether *a person who has not read this package* can reconstruct it, which is a property of a reader this charter does not have. **(v) It is not an open item**: an open item needs an observable resolving event within the package's reach, and no C010 sub-task remains that could produce one.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) `OUT-1`'s boundary-ownership claim is verified structurally and not experientially** — `16_…md` §4.1 confirms **17 of 17** boundary rows carry a populated "Forced by" cell and every row an Owner, so the *rule and the column* are demonstrably present; whether a reader who does not already know the model can apply the rule and name each owner unaided is untested. **(ii) `OUT-12`'s standalone claim is verified negatively and not positively.** The audits establish that **zero** traceability rows and **zero** package lines require following a path into `_local/` or `docs/wf-plans/` — the standalone rule is not *violated*. That a reader can actually reconstruct every decision, its evidence and its rejected alternatives from the package alone is a different and stronger claim, and it is the one the cold read tests. **(iii) The two verifications a cold read would most likely fail are already known to be degraded**: `F-S11-1`'s 161 non-resolving citations are precisely a cold reader's problem rather than an insider's, and `F-S11-3` leaves `OUT-11` and `OUT-12` without traceability rows to navigate by. A cold read run today would therefore report findings the package has already filed, which is an argument for running it **after** those are dispositioned, not for treating it as discharged. What it does **not** leave unsupported: every mechanical clause of `OUT-1` and `OUT-12` that does not require an independent reader — the citation audit, the standalone scan, the structural completeness of the decision records and the boundary table — all of which are run and reported with counts at `16_…md`.
- **Owner:** **`SUB-12 (NEU-986)`**, at the package-completeness gate, which is the only remaining party in this charter and the one that must decide how a gate item is answered when the verification it names cannot be performed by anyone inside the package. Named alone rather than jointly with **NEU-896**, because the decision owed is a *gate disposition* — cap, defer with a named reader, or record as unperformable — and only SUB-12 makes it. NEU-896 is the party that would eventually **supply** the independent reader, and is named here as the consumer rather than as a co-owner.
- **What would lift it:** **A cold read performed and recorded by a reader who has opened nothing but this repository's tracked `docs/research/` tree**, reporting whether the boundaries, authorities and topology were reached without a question and naming every place a question was necessary. Lifting requires the **record**, not the intention: a statement that the package is *designed* to be cold-readable is what `00_method-and-provenance.md` §3 already provides, and it is exactly what this cap exists to distinguish from evidence. Note the asymmetry — a clean cold read **lifts** the cap and leaves `OUT-1` and `OUT-12` standing, whereas a cold read that stalls does not merely fail to lift it: it identifies which specific hop is unreachable, which is a more useful result than the cap and is the reason the cap is worth lifting at all rather than being written off.

### SUB-12

*Five caps, from answering the package-completeness gate (`94_package-completeness-gate.md`). **This sub-task lifts exactly one existing cap — `CAP-S11-1`, by performing and recording the independent cold read (`F-S12-2`) — and lifts no other.** It adds **no** sighting to the deletion-owner gap and **does not adjudicate its disputed sighting count**: `16_…md` §6.2 places `CAP-S4-1` at its **seventh** (`:297`) and deliberately adds no eighth, while `15_…md` §5.4 and `90_…md` § SUB-10 each call it an **eighth**; the two are reading different file sets, the cap is **not lifted on either reading**, and adjudicating would mean editing a merged file's claim. `CAP-S4-1`, `CAP-S6-1`, `CAP-S15-1` and `CAP-S16-1` remain open and unlifted at this cutoff. **The register closes at 28 caps, every one with exactly one named owner. Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered or amended.***

#### `CAP-S12-1` — The seven questions that block an independent reader cannot be answered inside this charter

- **Id:** `CAP-S12-1`
- **Cap:** The independent cold read was **performed**, and it **failed**: *"No. I could not start implementing from this package without asking a question."* It recorded **seven** blocking questions, verbatim at `17_…md` §2.6 — `SC-S3-17`'s two writers (`F-S14-8`); whether the MCP core or `CMP-S4-17` writes `SC-S3-33`/`SC-S3-34` (`F-S10-6`); `SC-S3-41`'s `assumed`-versus-`required-by-upstream` status (**`F-S12-3`**, filed here and previously unfiled); the deletion owner for `SC-S3-16`/`SC-S3-17` (`CAP-S4-1`); clause 4's zone for `CMP-S4-2` (`F-S13-2`); what `F5.5` says; and whether `04_…md:70`'s count is 41 or 45 (`F-S4-2`). **Six of the seven remain unanswered at close.** The seventh — `F5.5` — is a durability gap rather than a package defect and is **closed** at `17_…md` §7.6, which restates all ten of the charter's review warnings in full.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) Five of the seven name an owning sub-task that is merged and `Done`** — `SUB-3 (NEU-973)`, `SUB-4 (NEU-974)`, `SUB-6 (NEU-976)` twice, and the structural obstruction behind `CAP-S4-1` — so there is no live party inside C010 to route them to. **(ii) Each is an architecture decision**, and this sub-task is barred from making or revising one; answering "who writes `SC-S3-17`" *is* the decision, not a report of it. **(iii) The registers are append-only**, so the contradictions cannot be resolved by amending the files that carry them, and repairing them in place would destroy the audit evidence that located them. **(iv) It is not an open item**: an open item needs an observable resolving event within the package's reach, and every one of these needs a decision by a party outside it. **(v) It is not a spike** — nothing is settled by a bounded experiment; each is a question about intent in a merged document.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) `OUT-1`'s and `OUT-12`'s cold-read clauses are answered negatively** — gate items `G-3`, `G-41` and `G-52` fail on this result. **(ii) A downstream implementer cannot begin on the state-ownership surface** without choosing between two merged artifacts that assert opposite absolutes, which is the one thing `OUT-3` exists to settle. **(iii) `SC-S3-17` is a Tier-2 gate input**, so the contradiction sits on a gate-bearing path rather than a peripheral one. What it does **not** leave unsupported, and this matters for calibration: the reader reached the **topology** cleanly (*"the cleanest answer in the package"*), reached **15 of 17** boundaries' owners without inference, reached six of eight sampled authorities unambiguously, confirmed the deliberate stop independently (**zero** endpoint paths, payload schemas or error catalogues), and checked `93_stand-in-assumption-register.md` unprompted and called it *"exemplary"*. **The failures are integrative, not analytical** (`F-S12-4`).
- **Owner:** **`NEU-896`**. Named alone: it is the convergence pass, it is live, and it is the only party that can reconcile findings whose owning sub-tasks have merged. Each of the seven is additionally routed to its owning sub-task as a residual at `17_…md` §2.6, so none is routed to a closed owner alone.
- **What would lift it:** **A reconciliation pass on `origin/develop` that answers each of the six outstanding questions, naming for each which artifact is authoritative and why.** Lifting requires the answers, not a re-read: a second cold read performed against the same unreconciled package would reproduce the same seven questions, which is a re-measurement rather than a repair. The reader named the remedy itself and it is quoted so the bar is its own rather than this sub-task's: *"a single reconciliation pass over the seven questions above, and one correction to `01_outcome-register.md` adding a forward pointer to `F-S8-1` … That is a small amount of work relative to the package's size."*

#### `CAP-S12-2` — `OUT-8`'s make-or-reuse record for the authoring-time execution environment is undischarged, and no party in this charter owns it

- **Id:** `CAP-S12-2`
- **Cap:** `OUT-8` requires *"one build-versus-reuse-versus-adopt record per architecture-material capability"*. The published make-or-reuse list stays **closed at four**, while `DR-C10-S2-2` keeps a **fifth** architecture-material capability: *"An authoring-time execution environment is an architectural component of the selected system"*, carrying an **isolation boundary the host can terminate**, one isolate per executed unit, a wall-clock resource bound, and a trust boundary of `first-party, creator-authored code`. An isolation boundary the host can terminate is a boundary, so by `13_…md`'s own architecture-material rule the capability is architecture-material and a record is owed. This is the charter's accepted review warning **`F5.9`**, whose text reads: *"The make-or-reuse set is a closed four; `OUT-9`'s authoring-time execution component may add a fifth whose build/reuse/adopt decision no sub-task owns if SUB-2 keeps it."* **SUB-2 kept it.** `15_…md` §8.6 states the disposition verbatim: *"the list stays closed at four, as published. `OUT-8`'s make-or-reuse requirement is left undischarged for the authoring-time execution environment. This is declared, not absorbed."*
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) No sub-task owns the decision** — that is the literal content of the warning the charter's own review **accepted** rather than assigned, and an accepted warning with no assignee has no holder to chase. **(ii) `SUB-2 (NEU-972)` scoped out the runtime, sandbox and scheduling mechanism** when it made the placement decision, so the record's subject was deliberately left unselected by the party that created the obligation. **(iii) `SUB-10 (NEU-984)` selected no isolation primitive** and is merged, so the sub-task that would naturally hold it has shipped without it. **(iv) Producing the record here would be making an architecture decision**, which this sub-task is barred from doing — a build-versus-reuse-versus-adopt record is a decision, not a summary of one. **(v) It is not an open item**: no observable event inside this package's reach closes it, because what is missing is a decision rather than a fact. **(vi) It is not a spike** — the question is which of three options to take, not what is true.
- **What it leaves unsupported:** Named, so the cost is visible. **(i) Gate item `G-27` fails**, and `OUT-8`'s verification clause is answered negatively rather than qualified. **(ii) `DR-C10-S2-2`'s component has a stated trust boundary and no stated substrate**, so a downstream implementer knows what the isolate must guarantee and nothing about what provides it. **(iii) The one place the gap is most likely to be missed is the quality-gate battery**: NEU-890's `automated` gate class is defined as requiring *"an execution environment, and a re-run budget"*, and gate `EQ-S4-6` runs an authored approach over authored fixtures, so the capability is on a path that is otherwise fully specified. What it does **not** leave unsupported: `DR-C10-S2-1`'s elimination of the **learner-facing** execution environment, which is a separate and settled decision, and which this cap must never be read as reopening — the two must not be conflated.
- **Owner:** **`NEU-896`**. Named alone: the decision belongs to whichever implementation charter builds the authoring pipeline, and no remaining pass in C010 selects substrates.
- **What would lift it:** **A build-versus-reuse-versus-adopt record for the authoring-time execution environment published on `origin/develop`**, carrying all six required sections including rejected alternatives, and stating how the selected substrate delivers the two properties `DR-C10-S2-2` already fixed — an isolation boundary the host can terminate, and a wall-clock resource bound. Lifting requires the record, not a note that the capability exists: the capability's existence is already published, and it is precisely the thing that created the obligation.

#### `CAP-S12-3` — `OUT-10`'s per-marker matched/unmatched count of uncertain-and-material claims does not exist and cannot be produced here

- **Id:** `CAP-S12-3`
- **Cap:** `OUT-10` requires *"an audit that every uncertain-and-material claim resolves to a spike record or a caps-register entry, with the count of each reported"*. The evidence published is **qualitative**: **121** `[unconfirmed]` markers across the package, **4** spike records, **136** spike citations with **0** dangling, and **28** owned caps at close. **No artifact carries a per-marker matched/unmatched count**, and `NEU-985 (SUB-11)` self-disclosed this as one of its three stated limits rather than letting the qualitative evidence stand as the count.
- **Why it is capped:** No experiment, reading or available party settles it inside this charter. **(i) Producing the count means running a new audit over 121 markers in sixteen merged chapters**, and this sub-task's scope explicitly excludes re-running or extending SUB-11's mechanical audits — they are consumed as input. **(ii) A count produced by the closing sub-task would be unreviewable**: SUB-11 is merged, no further pass follows, and a self-graded audit answering `OUT-10`'s own verification clause is exactly what `94_…md`'s reservation forbids. **(iii) The judgement is not mechanical.** "Uncertain-and-material" is not a string: a `[unconfirmed]` marker on a stand-in assumption is expected and matched by construction, while the same marker on a decision input is a gap — and separating the two requires re-reading each marker in the context of the decision it supports, which is the merged authors' judgement, not this sub-task's. **(iv) It is not an open item**: the resolving event would be an audit this charter has no remaining party to run. **(v) It is not a spike** — no bounded experiment settles a counting question.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) Gate item `G-35` cannot be answered with cited evidence**, and is the one item in the gate disposed as *capped* rather than passed, qualified or failed. **(ii) `OUT-10`'s third verification clause is the only one of its three not discharged** — the quarantine audit (**zero escapes**) and the read-test justification review (**4/4**, all 13 fields, 0 expired) both pass independently at `16_…md` §6.1 and §6.3. **(iii) The residual risk is bounded in one direction only**: an unmatched marker would be an uncertain claim with no spike and no cap behind it, which is precisely the failure `OUT-10` exists to prevent, and nothing here rules one out. What it does **not** leave unsupported: the spike register's own integrity, which is measured and passing, and the caps register's, which closes at 28 with a named owner on every entry.
- **Owner:** **`NEU-896`**.
- **What would lift it:** **A per-marker audit published on `origin/develop`** that walks each `[unconfirmed]` marker in `docs/research/C010-system-and-repository-architecture/`, classifies it matched or unmatched against a spike record or a caps entry, and **reports both counts**. Lifting requires the unmatched count specifically: a report of matches alone is the qualitative evidence that already exists.

#### `CAP-S12-4` — Whether the 30 undated upstream references rely on the provenance table or are simply undated is undeterminable, and no author can be named

- **Id:** `CAP-S12-4`
- **Cap:** `OUT-12` requires that *"every upstream claim carries a version or date"*. `16_…md` §5.7 measured **30 upstream references with no version and no date**, and §10 item 7 records the limit precisely: *"Could not determine: whether the 30 undated upstream references were intended to rely on `00_…md` §7's provenance table or were simply undated. The distinction changes whether it is a convention or an omission, and nothing in the package states which. Reported as an observation rather than filed against an author."*
- **Why it is capped:** No experiment, reading or available party settles it. **(i) The distinction is one of authorial intent**, and intent is not readable from a citation that carries no marker either way — the two hypotheses produce byte-identical text. **(ii) `00_…md` §7 does publish a provenance table** with the upstream packages' compilation dates, so the "convention" reading is genuinely available and not a charitable invention; but §7 never states that references may omit dates in reliance on it, so the "omission" reading is equally available. **(iii) Twelve sub-tasks wrote the 30 references between them and all sixteen are merged**, so there is no author to ask and no live party who could state which was meant. **(iv) Filing it against an author would be manufacturing a finding** — SUB-11 declined to do so for exactly this reason, and this gate does not re-inflate it. **(v) It is not an open item**: no observable event distinguishes the two readings after the fact.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) Gate item `G-42`'s second half cannot be answered**, and `G-42` therefore carries a cap alongside its fail. **(ii) A reader cannot tell, for any one of the 30, whether the upstream claim is anchored to a dated compilation or is simply floating** — which is the staleness-detectability property `OUT-12` names, so the property is unverified rather than absent. What it does **not** leave unsupported: the provenance table itself, which exists, is dated and covers the upstream packages; and the **other** half of `G-42`, which is measured, failing, decided in `DR-C10-S12-2` and routed as `OI-S12-1` — this cap is about the date half only, and must not be read as widening the citation defect.
- **Owner:** **`NEU-896`**.
- **What would lift it:** **A stated convention on `origin/develop`** — either that an upstream reference may omit its date when `00_…md` §7 carries it (making the 30 correct by rule), or that it may not (making them 30 omissions with a named repair). Lifting requires the **rule**, not a re-count: recounting the 30 reproduces the observation without resolving it. This is the same erratum-versus-convention shape `DR-C10-S12-2` decides for the path citations, and it is capped rather than decided here because the path convention is derivable from the tree while this one is not derivable from anything published.

#### `CAP-S12-5` — `NEU-983`'s unidentified verification `PARTIAL` is unrecoverable from the published package

- **Id:** `CAP-S12-5`
- **Cap:** `SUB-9 (NEU-983)` merged with **one `PARTIAL`** in its own verification record, and **the criterion it attaches to is not identified anywhere in the published package**. `16_…md` §10 item 6 records the fact and correctly declines to chase it: *"SUB-9 landed with one unidentified PARTIAL. A chapter that landed with open findings is audited as published; its open findings go to SUB-12's open-items register and do not block anything here."* This gate cannot chase it either.
- **Why it is capped:** No experiment, reading or available party settles it. **(i) Verification records live in the gitignored `_local/` tree**, which is not published, is not a permitted dependency of a reader, and — for this sub-task specifically — is not present in the working tree for `NEU-983` at all. **(ii) Inventing a criterion for it would be a fabrication of exactly the kind this package forbids**, and a plausible guess is worse than an admission because it would be indistinguishable from a real record. **(iii) `SUB-9 (NEU-983)` is merged and `Done`**, so no party can restate it. **(iv) It is not an open item**: there is no observable event inside this package's reach that reveals what a gitignored artifact said.
- **What it leaves unsupported:** Named, so the cost is visible and is not larger than it is. **(i) Gate item `G-60` is qualified rather than plainly passing** — SUB-11's four audit verdicts are consumed intact, but one predecessor's own verdict cannot be consumed at all. **(ii) A reader converging this package cannot tell which of `OUT-7`'s or `OUT-8`'s criteria SUB-9 considered only partly met.** What it does **not** leave unsupported, and this is the calibration that keeps the cap honest: **the substantive content of SUB-9's chapter is complete and independently checkable.** `14_…md` §11.1 records what it closes and §11.2 records what it explicitly does not; `DR-C10-S9-1` carries all six required sections including the eliminated fully-public monorepo; `traceability/S9_repository-topology.md` is present and its rows resolve; and `OI-S9-1` through `OI-S9-4` are all filed with owners, with `OI-S9-4` subsequently **discharged** by `SUB-10 (NEU-984)`. The cap is over a missing verification *record*, not over missing work.
- **Owner:** **`NEU-896`**.
- **What would lift it:** **The `PARTIAL`'s criterion identified and its residual stated on `origin/develop`** — either by publishing the residual as a finding or open item in the package, or by NEU-983's verification record being re-derived and its partial criterion named. Lifting requires the identification: a statement that a `PARTIAL` exists is what this cap already records.

### NEU-988 — owner amendment (not a sub-task; files no cap)

**Model:** claude-opus-5[1m] · **Appended:** 2026-08-23 · **Task:** NEU-988

*This section **files no `CAP-` id, lifts no cap, and changes no cap's text**. It is an owner amendment and nothing else. It exists because the register's own invariant — "**Every cap carries a named owner**", and "'whoever picks this up' is not an owner" (§What belongs here) — had silently stopped holding: **every one of the 28 caps named an owner that is in a terminal tracker state**, so the register that exists to prevent orphaned gaps was carrying twenty-eight of them. **Zero lines were deleted from this register and no sibling's entry was reflowed, renumbered, re-sequenced or amended.** NEU-988 is not a sub-task, so it allocates no `S<n>` namespace and introduces no global counter; it heads its section by its own tracker id.*

#### What was found

Read from the tracker on **2026-08-23**: all sixteen C010 sub-tasks (`NEU-971` … `NEU-986`) read `Done`, as did `NEU-893` and `NEU-896`. **Sixteen** of the 28 caps name `NEU-896` as an owner; the other **twelve** name only closed C010 sub-tasks, an unnamed party, or `NEU-893`. So no cap in this register had a live owner, in either group.

Two of those terminal states are **collateral auto-closures rather than records of completed work**, and both are established by timestamp:

- **`NEU-896`** — `completedAt` `2026-08-22T09:11:58.531Z`; **`NEU-986` (SUB-12)** — `…58.529Z`. **2 ms apart.** `NEU-896`'s only attachment is NEU-986's PR #762. It held `Backlog` from 2026-07-11 until `09:04:26` that morning and read `Done` 7m32s later. **`NEU-895` — one of its own nine declared dependencies — closed at `09:16:40.532Z`, 4m42s *after* it**, and `NEU-891`, `NEU-892` and `NEU-894` are still `Backlog`. Its nine-package convergence gate was unreachable and provably did not run.
- **`NEU-893`** — `completedAt` `2026-08-21T21:36:00.717Z`; **`NEU-975` (SUB-5)** — `…00.715Z`. **2 ms apart**, and NEU-975 is the sub-task that *contracts the split with* NEU-893 rather than performing its work. **No NEU-893 package exists under `docs/research/`**, and `93_stand-in-assumption-register.md` independently describes it as **unbuilt** (`A-28`), standing in for it.

#### What was done outside this file

**Both items were restored to `Backlog`** — the state each held before the sweep. That corrects a false record rather than routing around it, and it is the whole of the repair for **sixteen** caps (`CAP-S6-1`, `CAP-S7-1`, `CAP-S7-2`, `CAP-S8-1`, `CAP-S9-1`, `CAP-S10-1` … `CAP-S10-4`, `CAP-S12-1` … `CAP-S12-5`, `CAP-S15-1`, `CAP-S15-2`) and for **`CAP-S5-1`**, whose co-owner `NEU-893` is now live. `90_open-items-and-provisional-register.md` needs **no** amendment at all for the same reason, and `93_…md` stays closed at its five stand-ins. The decision, its two rejected alternatives and its revision trigger are recorded at `decision-records/DR-C10-N988-1_c010-residual-owner-restoration.md`.

#### The amendment: eleven caps are co-named to `NEU-896`, additively

The eleven caps below name **only** parties that are merged and closed, or name no tracker party at all. Each is **co-named to `NEU-896`**, the restored convergence pass, following the precedent this package already set at `90_…md:614` — *"The original owner is not overwritten; the co-naming is additive."*

| Cap | Owner as originally written (**unchanged**) | Co-named owner | Why NEU-896 is the correct live party |
| --- | --- | --- | --- |
| `CAP-S1-1` | `NEU-985 (SUB-11)` for the mechanical audit; `NEU-986 (SUB-12)` for the gate's answer | `NEU-896` | Both audits ran and both parties merged; whether the conventions survived is now a convergence-level observation over the finished package. |
| `CAP-S1-2` | *"the sub-task that depends on the number"* — **no tracker party at all** | `NEU-896` | The rule stands and is correct for a live sub-task; with all sixteen merged there is no such sub-task left, so the obligation falls to the party that consumes the counts next. |
| `CAP-S1-3` | `NEU-986 (SUB-12)` | `NEU-896` | The QA no-op is recorded at the gate; whether a QA engine is ever configured is an implementation-charter question. |
| `CAP-S2-1` | `NEU-986 (SUB-12)` | `NEU-896` | The re-run budget is quantifiable only against a running gate battery, which no C010 pass builds. |
| `CAP-S2-2` | `NEU-986 (SUB-12)` | `NEU-896` | Measuring the failure mode's frequency needs a deployed system. |
| `CAP-S3-1` | `NEU-986 (SUB-12)` | `NEU-896` | The field set is unresolved **upstream**; convergence is where upstream resolutions land. |
| `CAP-S3-2` | `NEU-986 (SUB-12)` | `NEU-896` | The recurring obligation attaches to whoever changes the schema, and that is downstream of this charter. |
| `CAP-S3-3` | `NEU-986 (SUB-12)` | `NEU-896` | The retention window and deletion owner are an unmet upstream requirement; `NEU-893`, also restored, supplies the mechanism. |
| `CAP-S4-1` | `NEU-986 (SUB-12)` | `NEU-896` | Same gap as `CAP-S3-3`, sighted from component placement. The structural obstruction is unchanged by any C010 pass. |
| `CAP-S11-1` | `SUB-12 (NEU-986)` | `NEU-896` | The entry already names `NEU-896` as **consumer** — *"the party that would eventually supply the independent reader"*. With SUB-12 merged, the consumer is the only live party; the gate disposition it was named alone for was made and is recorded at `94_…md`. |
| `CAP-S16-1` | `SUB-6 (NEU-976)` and `SUB-12 (NEU-986)` | `NEU-896` | The entry's own text records that the named owner is closed; `OI-S16-1` was already disposed via route (c) — *"both questions are routed to `NEU-896` at convergence"* (`90_…md:590`) — so the cap now follows its questions. |

**`CAP-S5-1` is deliberately absent from that table.** It names `NEU-986` **alongside `NEU-893`**, and `NEU-893` is restored, so it already has a live owner and needs no co-naming. It is listed here so that a reader checking twelve-versus-eleven can see which one moved and why.

#### What this amendment does not do

- It does **not** overwrite, weaken or reinterpret any original owner. Every `**Owner:**` line in this file is byte-identical to what its sub-task wrote.
- It does **not** renumber, reflow, restructure or re-sequence any entry, and it adds no `CAP-` id — the register still closes at **28 caps**.
- It does **not** lift a cap. `CAP-S4-1`, `CAP-S6-1`, `CAP-S15-1` and `CAP-S16-1` remain open and unlifted, exactly as SUB-12 left them, and no sighting is added to the deletion-owner gap.
- It does **not** adjudicate any disputed count, claim, id or finding, and it repairs no id. Two owner-id slips sit in `../90_…md` — `OI-S2-2` names "SUB-13 (**NEU-987**)" where SUB-13 is `NEU-977`, and `OI-S14-1` names "SUB-16 (**NEU-980**)" where SUB-16 is `NEU-979` — and **both are already filed** by this package (`F-S3-2` and `F-S16-1` respectively, the latter inside `F-S11-4`'s 38 label↔id mis-pairings), with the correction stated at `../90_…md:269` and `../05_…md` §12. **Both items are closed** (`../90_…md:271`, `:349`), so neither bears on the live-owner condition over the 14 open items. This task re-files nothing and repairs nothing there; the observation is carried at `02_findings-register.md` § NEU-988 as a **re-sighting**, not a new finding.
- It does **not** claim the residual is now resolved. Every cap here is still capped; what changed is that each one now names a party that can act on it.

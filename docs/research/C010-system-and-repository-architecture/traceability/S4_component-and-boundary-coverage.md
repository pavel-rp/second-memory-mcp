# `S4` — Component and boundary coverage

**Task:** NEU-974 (SUB-4) · **Charter:** C010 (umbrella NEU-895) · **Compiled:** 2026-08-21
**Model:** claude-opus-5[1m]
**Covers:** `OUT-1` (in full) and `OUT-9`'s **placement** half — SUB-2 specified the drift components, this sub-task places them.
**Audit rows are not here.** `NEU-985 (SUB-11)` owns the cross-cutting audit in its own file; this register documents SUB-4's own coverage only.

**Every row below resolves inside `docs/research/`.** Zero rows resolve into `_local/` or `docs/wf-plans/` — the binding constraint stated in `README.md` of this folder. This sub-task produced no gitignored working material that carries substance; nothing needed carrying over.

**Counts, both directions:** boundary → forcing input **17 / 17**; component → demanding requirement **20 / 20**. Flows with a named authoritative side **21 / 22**; journey hops with a named authoritative side **21 / 23**. The four exceptions are the same two findings (`F-S4-3`, `F-S4-6`), each counted once per direction.

---

## `OUT-1` — A published component-and-boundary model with no unclaimed hop

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-1` | **Every component is named once with a single responsibility statement and an owner** — twenty components, zero duplicated, zero appearing without a requirement that demands them. | `../05_system-context-and-responsibility-boundaries.md` §3.2; `../decision-records/DR-C10-S4-1_component-individuation-rule.md` | Structural — verifiable row by row by file inspection against `../00_method-and-provenance.md` §5; the existing components additionally cited to `src/` at the 2026-08-21 cutoff | **confirmed** for twelve rows (three of them "in part") · **consumed** for the four taken from SUB-2/SUB-3 · **`[unconfirmed]`** for the four that exist only under `A-27`, `A-29` or `SC-S3-41` | The inventory is complete against *this* cutoff and against the requirements published *so far*. Twelve of sixteen sub-tasks have not published yet; a later one that introduces a component owes it a row and a demanding requirement. |
| `OUT-1` | **Every interacting pair is classified `trust` / `process` / `neither`, each citing the upstream requirement or codebase fact that forced it**, and each naming the transport the claim holds for. | `../05_…` §4.2; `../decision-records/DR-C10-S4-2_trust-versus-process-boundary-classification.md` | Structural, plus **citation of `src/`** at the 2026-08-21 cutoff (`src/transport/main.ts:46`, `:55`–`:59`; `http.ts:99`–`:111`, `:48`–`:65`, `:167`–`:170`; `jwt-middleware.ts:90`, `:114`, `:127`) | **confirmed** | Pair-set completeness is a **structural** argument (§4.3), not an enumeration of the 20 × 20 grid: pairs are absent because no flow crosses them. A new flow creates an unclassified boundary. |
| `OUT-1` | **Every flow states what crosses, in which direction, and which side is authoritative** — twenty-two flows, twenty-one with a named authoritative side. | `../05_…` §5 | Structural, verifiable by file inspection | **confirmed** | `FL-S4-16` (the gate-verdict write) has **no** named authority. Filed as `F-S4-3`; the resolving open item is `OI-S2-2`, owner `SUB-13 (NEU-977)`. Not narrated past. |
| `OUT-1` | **Boundary ownership is derivable from a single published rule** — the boundary is owned by the side that must still be correct when the other side misbehaves — so a cold reader names every owner without asking. | `../05_…` §4.1, §4.2 "Owner" column | Published rule plus a per-row owner; **mechanically checkable** (every row's Owner cell is non-empty or explicitly `—` for a `neither` pair) | **confirmed** that the rule and the column are published · the **cold read itself is not run here** | The independent cold read `OUT-1` names is not this sub-task's to run on its own artifact. `BND-S4-16`'s owner is conditional by construction (§4.4) and `BND-S4-17`'s owner is **nobody** by finding (`F-S4-5`) — both stated, neither hidden. |
| `OUT-1` | **The browser holds nothing gate-bearing, and the property is stated as holding under every rendering model** — with no statement of where any surface renders. | `../05_…` §6.1–§6.2 | Structural, resting on three non-rendering facts: `BND-S4-1`'s trust classification, `SC-S3-43` being the only browser-held category and explicitly not gate-bearing (`../04_state-category-inventory.md:203`), and every gate evaluator sitting behind `BND-S4-2` | **confirmed** the argument · **`[unconfirmed]`** the surface it is about (`A-27`) | The property is only as durable as `A-27`. Its stated invalidating outcomes — offline-capable or client-authoritative learning state — are the events that reopen this row. |
| `OUT-1` | **No rendering-model selection leaks in; a constraint the trust property forces is handed to SUB-15 as an input instead** — five named constraints, one of which forbids citing the trust property as an argument for any rendering model. | `../05_…` §6.3 (R-1 … R-5); `../05_…` §12 | Structural; **mechanically checkable** by grepping the document for rendering vocabulary and confirming every hit sits in §6.2's satisfied-by-all argument or §6.3's handed-to list | **confirmed** | `SUB-15 (NEU-982)` may find R-2 or R-3 too tight. They are `A-27`'s stated envelope, not this model's invention; widening them is an amendment to `A-27`, routed to its owner. |
| `OUT-1` | **Content-orchestration placement is explicit, not implicit** — the authoring pipeline, the quality-gate battery and the content serve path each appear as a named, placed component with an owner, a responsibility statement and a boundary classification. | `../05_…` §3.2 (`CMP-S4-13`, `CMP-S4-14`, `CMP-S4-16`), §7.1 | Structural — the presence check is a table read, and a component that were only implied by the generic layering would have no row | **confirmed** that all three are named and placed · **partly `confirmed`** that they exist in code (Tier-1 at `src/orchestration/audit-pipeline.ts:94`, Tier-2 at `:174`, serve at `teaching-workflows.ts`) | Each is `confirmed` only in part. The full NEU-890 gate set, the isolate runner and the serve-time drift read do not exist at this cutoff, and the rows say so rather than implying a built system. |
| `OUT-1` | **The serve path is thin and the authoring path is thick**: exactly one gate at serve time — the citation-drift check reading SUB-2's cached out-of-band verdict — with no reviewer, no model call and no execution, quarantining on stale or absent; the full battery at authoring time. | `../05_…` §7.2–§7.3; `../decision-records/DR-C10-S4-3_thin-serve-path-thick-authoring-path.md` | Citation of a **consumed** upstream specification (`../03_execution-environment-and-citation-drift-component.md` §4.2–§4.4), carried unchanged | **consumed** | At a per-source revalidation budget of zero, stale-or-absent is the **ordinary** path. A serve path built for the fresh-verdict case is mis-built; §7.3 states it rather than engineering around it. |
| `OUT-1` | **Three benchmark journeys are walked hop by hop with a named authoritative side at each hop**, and the "three of five" referent is resolved against a real source rather than assumed. | `../05_…` §10.0–§10.4 | **File inspection** of `../../C005-product-foundation/benchmark-suite/01_journey-vehicles-and-fidelity.md:60`–`:66` for the referent; structural for the walks; `src/` citation for each hop's mechanism (`src/server/teaching-tools.ts:20`, `:112`, `:159`; `topic-tools.ts:32`; `src/domain/algorithms/grade-mapper.ts:71`) | **confirmed** | 21 of 23 hops have a named authority; the two exceptions are `F-S4-3` and `F-S4-6`. A walk is file inspection: it yields **no** evidence about any journey's hypothesis and upgrades no C005 status (§10.4). |
| `OUT-1` | **Every component on a learner-content path states its disposition against the existing unredacted-logging exposure**, citing NEU-890's `OI-S6-5` where the disposition is "inherits and does not fix". | `../05_…` §9.1 | **Citation of `src/`** at the 2026-08-21 cutoff (`src/shared/redact-params.ts:1`, `:13`; `src/transport/audit-middleware.ts:88`, `:105`, `:109`; `pg-audit-transport.ts:117`, `:142`–`:143`) plus citation of a **consumed** upstream open item (NEU-890 `OI-S6-5`) | **confirmed** the code facts · **consumed** the open item | The disposition is uniform: **inherit, do not fix.** A component-and-boundary model is not the artifact that closes `OI-S6-5`, and this row does not claim it is. |
| `OUT-1` | **`OI-S3-2` is discharged, not capped** — for `SC-S3-37` and `SC-S3-40` the model names the reading component and states whether it holds a copy. | `../05_…` §8.1–§8.2; `../90_open-items-and-provisional-register.md` `### SUB-4` | Structural, reasoned from `../04_state-category-inventory.md:173`, `:176`, `:177` and the serve path's no-egress constraint | **confirmed** | The **authority** over the imported `SC-S3-37` copy is `SUB-13 (NEU-977)`'s, not this row's. Import staleness is filed as `OI-S4-1`. |
| `OUT-1` | **The `F-S3-3` deletion-owner gap is addressed from the placement direction**: no component in this inventory *can* be the deletion owner, and the obstruction is structural rather than a matter of assignment. | `../05_…` §9.2; `../91_caps-and-incomplete-scope.md` `### SUB-4` (`CAP-S4-1`) | Structural, resting on `../04_state-category-inventory.md:115`, `:116` (no principal field on either table), `:448`–`:452` (the only identity-bearing state is process-local; `SC-S3-45` is `assumed` under `A-28`) | **confirmed** | `CAP-S4-1` is a **deliberate second sighting** of the gap `CAP-S3-3` already carries, from a different direction — recorded rather than tidied, per the register's own rule. It states the precondition that turns the deletion owner from unassignable into merely unassigned. |
| `OUT-1` | **The one boundary this model cannot classify unconditionally is stated as such, with the constraint its resolution must satisfy** rather than resolved by assumption. | `../05_…` §4.4 (`BND-S4-16`) | Structural; the constraint is `OUT-3`'s exactly-one-authority-per-category rule applied to a hypothetical second writer | **confirmed** as a stated constraint · the selection is **not made** | Owner `SUB-6 (NEU-976)`. If it selects hybrid, disjointness of the written categories is a condition it must **demonstrate**, not assume. |
| `OUT-1` | **A trust boundary that no component enforces is recorded rather than omitted**: the STDIO transport meets the trust-boundary test and mounts none of the protections. | `../05_…` §4.2 (`BND-S4-17`), §10.4 (`F-S4-5`); `../02_findings-register.md` `### SUB-4` | **Citation of `src/`** at the 2026-08-21 cutoff (`src/transport/main.ts:55`–`:59`; `context-token-middleware.ts` mounted only on the HTTP path) | **confirmed** | This is a **finding about the system**, not a design proposal. Closing it is nobody's in this package; a sub-task that ships a second transport inherits it. |

## `OUT-9` — The drift components, placed

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-9` | **SUB-2's three components are placed in the component model with their egress and isolation boundaries**, and they remain three components rather than being merged. | `../05_…` §3.2 (`CMP-S4-15`, `CMP-S4-17`, `CMP-S4-18`), §3.3 | Citation of a **consumed** upstream specification (`../03_execution-environment-and-citation-drift-component.md` §3.5, §4.2, §4.3), placed here | **consumed** (the specifications) · **confirmed** (the placement, by file inspection) | `F-S2-2` explicitly warned against merging the gate runner with the egress producer; §3.3 keeps the contradictory egress fields on separate rows. The **state authority** over `SC-S3-33`/`SC-S3-34`/`SC-S3-35` is `SUB-13 (NEU-977)`'s. |
| `OUT-9` | **The citation-drift verdict producer is the only component in the model with egress to a party outside the operator's control**, and its three inherited egress constraints are restated at the boundary it owns. | `../05_…` §3.3, §4.2 (`BND-S4-3`), §5 (`FL-S4-11`, `FL-S4-12`) | Citation of **consumed** upstream rules (`../03_…` §4.2, §5) | **consumed** | Stored fields stay limited to `stable_id` + `canonical_url` while `CH-F5-1` is open. A sub-task needing a third field routes an amendment to NEU-890's owner. |
| `OUT-9` | **The drift-verdict cache is placed on the learner's latency path with no egress, keyed read only**, and the boundary it is read across is classified `neither` — a miss is a defined outcome, not a failure. | `../05_…` §3.3, §4.2 (`BND-S4-10`, `BND-S4-11`), §7.3 | Citation of a **consumed** upstream specification (`../03_…` §4.3–§4.4) | **consumed** | The **scheduling mechanism** behind `BND-S4-10` is unselected — `OI-S2-1`. The process classification is stated without naming a mechanism precisely so that `OI-S2-1`'s resolution does not invalidate it. |

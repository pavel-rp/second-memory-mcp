# `S12` — Package closure coverage: `OUT-11` and `OUT-12`

**Written by:** NEU-986 (SUB-12) · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-11` (stand-in reconciliation and the two decision-ownership records) · `OUT-12` (closure, the completeness gate, the cold read)

---

## Why this file exists

`F-S11-3` established that **`OUT-11` and `OUT-12` were the only two outcomes with no traceability row
anywhere**, though both are substantively discharged. Its structural cause is `README.md:7` — SUB-1
declared the folder's shape and wrote no file, because it discharged no outcome's content. The finding is
explicit that *"the answer is **not** 'write the two missing rows'"*: a sub-task documents **its own**
coverage, and writing SUB-1's into a file SUB-1 did not write is what the convention forbids.

`OUT-11` and `OUT-12` are **this** sub-task's outcomes. This file therefore closes the gap in the only
permitted direction, and **there is still no `S1` file** — correctly so.

**Outcome coverage across the set moves from 10 / 12 to 12 / 12.** Zero rows below resolve into `_local/`
or `docs/wf-plans/`.

---

## `OUT-11` — Numbered, tolerance-bounded stand-in assumptions handed to NEU-896

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-11` | Every assumption standing in for NEU-891, NEU-892, NEU-893 or NEU-894 is its own numbered entry, `[unconfirmed]`, naming the package it stands in for and a re-validation obligation that fires when that package lands. | `../93_stand-in-assumption-register.md` — five entries `A-25` … `A-29`, **closed**; audited at `../16_mechanical-audits.md` §4.1 as **5 / 5**, four packages, zero entries missing a required field. | Mechanical audit by a sibling sub-task (proxy signal), over the register's own fields. | `confirmed` | Nothing revalidates until the four packages land. `A-25` … `A-29` are all `[unconfirmed]` by construction and remain so. |
| `OUT-11` | A decision resting on a stand-in **says so at the decision**, not only in an appendix. | Each stand-in cited outside the register in **22 / 9 / 39 / 26 / 21** package files; `SC-S3-43`, `SC-S3-44`, `SC-S3-45` carry `assumed — A-27 / A-29 / A-28` in `../10_republished-authority-matrix.md` itself. | Citation-count sweep over the package (proxy signal). | `confirmed` | `CAP-S15-2` — all three of SUB-15's decisions rest on `A-27` alone. |
| `OUT-11` | Where a decision would swing on an unbuilt package's outcome, the package states the **envelope it tolerates** and names the **outcome that would invalidate it**, rather than guessing. | `../93_stand-in-assumption-register.md`, tolerance-envelope and invalidating-outcome fields, 5 / 5; restated in full at `../17_package-closure-and-neu-896-handoff.md` §3. | Register fields, verified per entry. | `confirmed` | `A-26` is the AI latency/privacy/cost case C005 routes to the unbuilt NEU-891; no budget exists to check the envelope against. |
| `OUT-11` | NEU-896 receives an **explicit reconciliation list**, so convergence does not rediscover the assumptions. | `../17_package-closure-and-neu-896-handoff.md` §3 — exactly five entries, `A-25` … `A-29`, each with its re-validation trigger; **zero non-stand-in items**. | Published handoff list (self-graded — this sub-task wrote it). | `confirmed` | Self-graded: `../94_package-completeness-gate.md` `G-38` carries the `†` marker for this row. |
| `OUT-11` | Non-stand-in open items are handed over **separately and clearly marked**, so a reader does not reconcile them against a package landing. | `../17_package-closure-and-neu-896-handoff.md` §4 — a separately headed list carrying the charter's assumptions 19, 30 and 31 (restated in full, since the charter is gitignored) and all 14 still-open `OI-*` entries with owners. | Published handoff list (self-graded). | `confirmed` | Five of the 14 name an owning sub-task that is merged and closed; each is co-named to `NEU-896` rather than left pointing at a party that cannot act. |
| `OUT-11` | The **NEU-893 circularity** is recorded as a finding in its own right, naming which inputs were **assumed rather than derived**. | `F-S1-1` in `../02_findings-register.md` (filed by SUB-1); updated at closure in `../17_package-closure-and-neu-896-handoff.md` §6.1 — three clauses of `A-28` **assumed**, and the human-`sub` question `OI-S1-2` **unverified**. | Finding record + closure update. | `confirmed` | `OI-S1-2` is still unverified: no sub-task inspected a live production token, and none could. Owner moves to `NEU-893` (`H5`). |
| `OUT-11` | The **C003/NEU-850 decision-ownership collision** is recorded with its settled disposition and any routed amendment. | **`F-S12-1`** in `../02_findings-register.md`; carried at `../17_package-closure-and-neu-896-handoff.md` §5. `NEU-850`'s `OUT-2` consumed at `../06_isolation-invariant-and-the-neu-893-split.md` §1–§2; its `OUT-6` and `OUT-7` at `../14_repository-topology-decision.md` §7. **Three "no amendment routed."** `OUT-7`'s overlap stated as **partial** (§7.1). `NEU-35` noted as the pre-existing duplicate. | Consumed constraint with cited source + published disposition (self-graded on the filing). | `consumed` | `OI-S5-1` (does `OUT-2` range over the two port-less log tables) and `OI-S9-3` (which manifest `OUT-6` means) remain open, both owned by `NEU-850` through its own `OUT-1` drift check. `NEU-850` is converged but **unimplemented**, so all three are decisions to honour, never schema facts. |
| `OUT-11` | SUB-5's **isolation residue list** is handed to NEU-893. | `../06_isolation-invariant-and-the-neu-893-split.md` §5.3 List B — eight questions `H1` … `H8`; restated at `../17_package-closure-and-neu-896-handoff.md` §6.2. Disjointness audit §5.5: universe 18, A 10, B 8, both 0, neither 0. | Disjointness audit (proxy signal). | `confirmed` | The "0 on neither" is **definitional, not empirical** — the chapter's own caveat, carried forward. `H6` and `H7` are this package's additions beyond the charter's four. |

---

## `OUT-12` — The package ships in the C005 house style, standalone and cold-readable

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-12` | The package is published under tracked `docs/research/` in the C005 house shape: README, per-topic documents, decision records with rejected alternatives, a traceability set, an open-items register, a caps register with a single named owner, and a completeness gate. | `../README.md`; `../03_…md` … `../17_…md` (15 topic chapters); `decision-records/` (**26**, all six sections, **24 / 24** audited at `../16_mechanical-audits.md` §4.1); this folder (**15** files); `../90_open-items-and-provisional-register.md`; `../91_caps-and-incomplete-scope.md`; `../94_package-completeness-gate.md`. | Structural audit by a sibling sub-task + file presence. | `confirmed` | No `S1` traceability file exists, and `F-S11-3` forbids writing one. `../92_spike-register.md:7` and `:134` claim the register is empty of results while it holds four spikes (`F-S6-5`), unrepaired. |
| `OUT-12` | The completeness gate is **answered item by item with cited evidence**, any item that cannot be so answered recorded as a cap with a named owner rather than marked passing. | `../94_package-completeness-gate.md` — **61 derived items**, 40 pass / 15 pass-with-qualification / 5 fail / 1 capped. **Items marked passing without a citation: 0.** Method in `../decision-records/DR-C10-S12-1_package-closure-gate-method.md`. | Published gate (self-graded — no other party can grade a gate this sub-task solely owns). | `confirmed` | Six items do not pass. Five caps filed, `CAP-S12-1` … `CAP-S12-5`, each with one named owner, all `NEU-896`. Ten of the 61 rows are self-graded and marked `†`. |
| `OUT-12` | A **cold-read review by an independent implementation agent** working only from the published package, reaching the boundaries, authorities and topology **without asking a question**. | Performed and recorded at `../17_package-closure-and-neu-896-handoff.md` §2, with the isolation enforced stated (§2.2), the reader's verbatim self-disclosure of that isolation's limit (§2.3), the verbatim verdict (§2.5) and seven blocking questions (§2.6). Filed as **`F-S12-2`**. `CAP-S11-1` **lifted** — the test was run. | **Proxy signal** (`../00_method-and-provenance.md` §1.1) — never external-user or expert validation. | `confirmed` **as performed**; the criterion is **not met** | **The verdict is negative:** *"No. I could not start implementing from this package without asking a question."* Gate items `G-3`, `G-41` and `G-52` fail. → **`CAP-S12-1`**, owner `NEU-896`. Isolation was **strong but not hermetic**; the reader disclosed harness injections carrying no package content, and the disclosure is published rather than smoothed. |
| `OUT-12` | Every claim about the codebase cites a **real file path**. | `../16_mechanical-audits.md` §5.1 — 1,420 citations extracted, 1,248 resolve, 11 by-design non-claims, **161 genuine non-resolvers**, of which **159 are one mechanism** in two directions. | Mechanical citation audit by a sibling sub-task. | `[unconfirmed]` — audit verdict **FAIL** | Decided rather than repaired, in `../decision-records/DR-C10-S12-2_citation-and-label-erratum-versus-convention.md`: a standing convention, no erratum pass, no repair of merged files. Enforcement is **`OI-S12-1`** (owner `NEU-896`), published as success measure `SM-7` at **161 failing**. Severity reported as measured: one mechanical defect, not 161 independent ones. |
| `OUT-12` | Every claim inherited from an **upstream package** cites that package's **version or compilation date**, so staleness is detectable. | `../00_method-and-provenance.md` §7's provenance table; `../16_mechanical-audits.md` §5.7 — **30 undated upstream references**. | Mechanical citation audit; §10 item 7 records the limit. | `[unconfirmed]` | **Undeterminable:** whether the 30 rely on the provenance table (a convention) or are simply undated (an omission) is not stated anywhere, the two hypotheses produce identical text, and all twelve authoring sub-tasks are merged. → **`CAP-S12-4`**, owner `NEU-896`. |
| `OUT-12` | **Standalone:** a reader with access to nothing but this repository's tracked tree can reconstruct every decision, its evidence and its rejected alternatives from the package alone. | Tested rather than asserted, by the cold read above. Negatively verified at `../16_mechanical-audits.md` §4.1: **0** of 259 traceability rows resolve outside `docs/research/`, and all **54** package lines naming a gitignored tree are assertions *about* its unreadability, which `../00_method-and-provenance.md` §3 expressly permits. | Cold read (proxy signal) + mechanical scan. | **not met** | The negative check passes — the rule is not violated. The positive claim **fails**: the reader was blocked on who writes state. Three durability defects named — 161 non-resolving citations, ~60 outward `../C005-*` / `../C009-*` references indistinguishable from them, and citations into the gitignored review log. **The third is closed here**: `../17_package-closure-and-neu-896-handoff.md` §7.6 restates all ten of the charter's review warnings in full, including `F5.5`, which was cited twice and never restated. → `CAP-S12-1`, `OI-S12-1`. |
| `OUT-12` | The package carries a **risk register with severity and mitigation status**, **success measures**, and **evolution paths** for the selected architecture. | `../17_package-closure-and-neu-896-handoff.md` §8 (10 risks — 2 Critical, 3 High, 4 Medium, 1 Low; mitigated 5, partially mitigated 3, open 1, split 1), §9 (8 measures `SM-1` … `SM-8`, each with its value today), §10 (6 paths `EP-1` … `EP-6`, each with its trigger). | Published closure chapter (self-graded). | `confirmed` | Self-graded: gate items `G-49`, `G-50`, `G-51` carry the `†` marker. Every non-mitigated risk status names its residual and that residual's owner; `R2` is **open by design** — nothing in this package proves isolation in running code. |
| `OUT-12` | `NEU-985 (SUB-11)`'s mechanical audit results are **consumed rather than re-run**. | `../17_package-closure-and-neu-896-handoff.md` §13 — traceability **PARTIAL**, citations **FAIL**, spike register and quarantine **PASS**, no-in-app-judge sweep **PASS**; the 38-pair label sub-audit consumed with its per-owner tally intact. None re-run, none re-measured. | Consumption discipline (self-graded). | `consumed` | `NEU-983 (SUB-9)` merged with one **unidentified** verification `PARTIAL`, and verification records are gitignored, so **the criterion it attaches to is unrecoverable from the published package**. No criterion is invented. → **`CAP-S12-5`**, owner `NEU-896`. |
| `OUT-12` | The QA no-op is recorded honestly and **no QA pass is claimed**. | `../94_package-completeness-gate.md` `G-61` and §6; `CAP-S1-3`. **`qa-execution:engine` is unconfigured** — the active capability registry is `git, linear`, and no registered capability owns the surface. | Resolver state, recorded as a genuine no-op under Core Article 8. | `confirmed` | No QA report artifact exists, because none could honestly be produced. **No QA pass is claimed by this gate, by the closure chapter, or anywhere in this package.** |

# `S14` — Traceability: validating the per-state authority matrix

**Written by:** NEU-978 (SUB-14), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-4`, `OUT-3`'s scenario-evidence half, and `OUT-10` — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-14's own**
coverage and nobody else's. Every row below resolves into `docs/research/` or a real `src/` path with a
line. **Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of
§1.2 (`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence
about this package's content** and appears in no row below — this sub-task touches zero source files, so
the build gates say nothing whatever about it.

**This file documents a validation of the `pre-validation` revision.** Several residuals name **SUB-16**
as the party that dispositions a finding, and one (`OI-S14-1`) names it as the party that decides whether
this record must be re-run. That is the intended shape: `OUT-3` splits assignment (SUB-13), validation
(SUB-14) and absorption (SUB-16) across three sub-tasks, and this file documents the second only.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-4` | The row domain is **45 rows / 450 authored cells**, not the charter's 25–30 — re-verified independently rather than inherited | `../09_authority-matrix-validation.md` §3; mechanical parse of `../08_per-state-authority-matrix.md` §8 (45 `####` blocks; 45 distinct ids, min 1, max 45, no gaps; 0 blocks missing any of the nine `OUT-3` attributes); reconciled against `../04_state-category-inventory.md` §8 (30 + 11 + 4) | Automated check | `confirmed` | The charter's own sizing is still stale; `F-S13-4` routes it to `NEU-986 (SUB-12)`. Not this file's to fix. |
| `OUT-4` | `I1`'s input — the `Learner-scoped` column — yields **19** explicit `no`, **18** `question — open`, **8** explicit `yes` → **26 in domain**, re-derived independently and agreeing with `../06_…md` §3.3 | `../09_…md` §4.2, §5.1; `../04_state-category-inventory.md` §3 | Automated check | `confirmed` | The column records the scoping **question**, never a schema fact (`../04_…md` §2). `OI-S3-1` stays open; the wall is `F-S13-3`'s. |
| `OUT-4` | The invariant is applied to **all 45 rows** under **two named target states**, producing **90 row-evaluations**, every verdict drawn from `../06_…md` §3.4's **closed six-verdict set** and **none stated without its target state** | `../09_…md` §5 (Census A, form (b)), §6 (Census B, form (c) with its assumed set enumerated), §15.1 (the consolidated 45-row table) | Structured AI review | `confirmed` | Census B's assumed set is `NEU-850`'s `OUT-2` **as scoped today**; if it ships differently, Census B is re-derived — `DR-C10-S14-1` revision trigger 1. |
| `OUT-4` | **Census A distribution:** 19 `not-applicable` · 24 `not-evaluable` · 0 `fails-confinement` · 2 `fails-transport` · 0 `fails-principal` · 0 `holds` | `../09_…md` §5.4, §15.2 | Structured AI review | `confirmed` | — |
| `OUT-4` | **Census B distribution:** 19 `not-applicable` · 9 `not-evaluable` · 15 `fails-confinement` · 2 `fails-transport` · 0 `fails-principal` · 0 `holds` | `../09_…md` §6.2, §15.2 | Structured AI review | `confirmed` | Bound to the enumerated assumed set; not a statement about any shipped system. |
| `OUT-4` | **Every failure names a cause from a vocabulary that distinguishes the residue SUB-5 already recorded from an assignment or model defect**, and **SUB-13's assignment causes zero failures under either target state** | `../09_…md` §6.3, §15.2 (the cause tally); `F-S5-4` in `../02_findings-register.md` | Structured AI review | `confirmed` | The confinement residue (14 rows) is **SUB-8 (NEU-981)**'s workload; the transport residue (2 rows) is **NEU-893**'s. |
| `OUT-4` | **No row returns `holds` by failing to find a counter-example**, and no verdict rests on an ownership column being present today | `../09_…md` §4.1 (quoting `../06_…md` §3.4.1 verbatim), §15.4; `../04_state-category-inventory.md` §6 (four-term search, zero matches) | Structured AI review | `confirmed` | `CAP-S5-1` stands: zero `holds` across 90 evaluations does **not** show the invariant unsatisfiable, and §3.4.1's asymmetry means this census cannot distinguish the two. |
| `OUT-4` | `fails-principal: 0` is a consequence of the **adjudication order** — `I5` is never reached — and is **not** a clean bill on principal integrity | `../09_…md` §6.2, §16; `F-S14-2`; `../06_…md` §3.6 case 5 (`jwt-middleware.ts:127`'s `azp` fallback) | Structured AI review | `confirmed` | The `I5` workload is **unsized** and stays unsized until `I2`–`I4` are satisfiable. Owner **NEU-893** against `OI-S1-2`. |
| `OUT-4` | `I3`'s port clause is **unaskable** for the 15 portless categories; read purposively, **disclosed and routed** rather than applied silently, and **no row's disposition changes** under either reading | `../09_…md` §4.3; `F-S14-1`; `DR-C10-S14-1` Decision 2 and Rejected alternatives 5–6 | Structured AI review | `[unconfirmed]` — the reading is a judgement, filed as `F-S14-1` | **SUB-5 (NEU-975) / NEU-893** own the procedure and may overturn the reading; **SUB-16** dispositions. |
| `OUT-4` | Any verdict holding only under one transport is recorded transport-qualified, never unqualified; the two `fails-transport` rows are `SC-S3-19` and `SC-S3-20` | `../09_…md` §5.3, §15.4; `../05_system-context-and-responsibility-boundaries.md` `BND-S4-17` (STDIO: a trust boundary nothing enforces, owner `nobody`) | Structured AI review | `confirmed` | `F-S4-5`: all three benchmark journeys are dogfooded across `BND-S4-17`, so **"the journey ran fine" is not evidence about a gated path** — and no verdict here rests on one. |
| `OUT-3` | **Four scenario walks over all 45 rows — 180 row-walks, 178 defined outcomes, 2 undefined and both routed** | `../09_…md` §8 (divergence), §9 (conflicting concurrent writes), §10 (mid-operation interruption), §11 (recovery), §11.1 (the totals) | Structured AI review | `confirmed` | The 2 undefined are `F-S14-4` (→ **NEU-891**) and `F-S14-5` (→ **SUB-10 (NEU-984)**). Neither is a matrix defect: the cell named the deciding party. |
| `OUT-3` | The conflicting-write walk states what the matrix **requires** and what the code **fails to prevent**, and asserts **nothing** about observed two-writer behaviour | `../09_…md` §9.1; `src/ports/unit-of-work-port.ts:26`–`:28` (`F-S6-2`); `src/infrastructure/db/operations.ts:21`–`:24` (no isolation level set); `src/orchestration/review-workflows.ts:190`–`:191` (the single-writer comment) | Automated check (code read) | `confirmed` for the code; **`[unconfirmed]`** for the divergence behaviour | **`CAP-S6-1`**, owner **SUB-10 (NEU-984)** alongside **NEU-896** — a cap on evidence strength, not on the conclusion. Not re-run and not re-filed. |
| `OUT-3` | The recovery walk classifies all 45 rows into five classes (17 / 7 / 10 / 6 / 5), and identifies **`SC-S3-16` and `SC-S3-17` as unrecoverable by construction while `SC-S3-17` is read by a blocking gate** | `../09_…md` §11 | Structured AI review | `confirmed` | Conjoined with **`CAP-S4-1`** (structurally unassignable deletion owner, **fifth sighting**): recorded as a consequence, **no new cap filed**, and the cap **must not be closed**. |
| `OUT-3` | The **flow cross-check covers all 22** `FL-S4-*` annotations, naming the **direction** of every discrepancy; **neither artifact amended** | `../09_…md` §12 (7 agree · 4 not comparable · **11 discrepancies**: 4 class A, 6 class B, 1 class C); `../05_system-context-and-responsibility-boundaries.md` §5 | Structured AI review | `confirmed` | Routed as `F-S14-6`…`F-S14-11`, all to **SUB-16 (NEU-980)**, which republishes and is the only party that may amend `../05_…md` without violating the append convention. |
| `OUT-3` | `05_…md` §5 **contradicts itself** about `SC-S3-16`/`SC-S3-17` (`FL-S4-8`/`FL-S4-9` vs `FL-S4-20`) and about the drift cache (`FL-S4-14` vs `FL-S4-13`) | `../09_…md` §12.1; `F-S14-8`, `F-S14-9` | Structured AI review | `confirmed` | First intra-document authority contradictions found in the package; `NEU-983 (SUB-11)` is named for a mechanical class check. |
| `OUT-3` | NEU-890's **durability property holds on all four rows** it runs over — a retired citation degrades a placement without stranding mastery history | `../09_…md` §13; `../07_state-ownership-model-selection.md` §7; `../04_state-category-inventory.md` §3 (`SC-S3-31` identity = `node_id` + `skill_type`, citation a non-key replaceable attribute) | Structured AI review | `confirmed` as a property of the **specification** — all four rows are `required-by-upstream` | Robust to `F-S14-5`'s undetermined store shape (checked deliberately). Its **retention preconditions are unowned** — `F-S3-3`, `CAP-S4-1`. |
| `OUT-10` | The **"could this have been read instead?" test** was applied before any spike, and six claims were settled by reading | `../09_…md` §14.1 (five claims, each with the artifact that settled it) and §14.2 (the sixth) | Automated check (code and register reads) | `confirmed` | — |
| `OUT-10` | **No new spike record is filed**, and the one candidate claim — worker-isolate terminability under `SC-S3-35` — was **already measured by `SPK-S2-1` arrangement C** (`terminate()` resolved at 1007 ms, host survived, Node v22.23.1) | `../09_…md` §14.2; `../92_spike-register.md` §10 `SPK-S2-1` and its `### SUB-14` no-new-spike note | Automated check | `confirmed` | **Disclosed:** SUB-14 ran the probe **before** re-reading the register, replicating a merged result 5/5 at 4–9 ms. Reported as a **replication, not a spike**; citations inherit `SPK-S2-1`'s **2027-04-30** expiry. |
| `OUT-10` | Three uncertain-and-material claims are covered by **existing caps with named owners**, and **none is asserted** | `../91_caps-and-incomplete-scope.md` `### SUB-14`: `CAP-S6-1` (→ SUB-10/NEU-896), `CAP-S5-1` (→ SUB-5/NEU-893), `CAP-S4-1` (→ SUB-4, structural) | Structured AI review | `consumed` | Each cited at the point of use; **no cap re-filed and no new cap opened**. |
| `OUT-4` | `A-25`, `A-27` and `A-28` are cited **in the sentence of the verdict each decided**, with tolerance envelope and invalidating outcome named there | `../09_…md` §5.2 (`A-25` at `SC-S3-42`, `A-27` at `SC-S3-43`, `A-28` at `SC-S3-45`), §8.1, §11; `../93_stand-in-assumption-register.md` | Structured AI review | `[unconfirmed]` — each stand-in is itself `[unconfirmed]` by construction | `A-25`'s envelope **cannot** resolve `F-S14-4`; `A-28`'s **cannot** rescue `SC-S3-45` (`F-S14-3`). Both limits are stated at the verdict, not in an appendix. |
| `OUT-4` | `OI-S5-3` — *"the procedure is unexercised against a real matrix"* — is **resolved by this publication** | `../90_open-items-and-provisional-register.md` `### SUB-14`; `../09_…md` §16 | Structured AI review | `confirmed` | Resolution establishes the procedure **runs**, not that it is **satisfiable**. `CAP-S5-1` stands. |
| `OUT-3` | This record is bound to the **`pre-validation`** revision; SUB-7, SUB-8 and SUB-10 consume SUB-16's post-absorption revision instead | `../90_…md` `OI-S14-1`; `../09_…md` §16, §17 | Structured AI review | `[unconfirmed]` — the dependency is certain, its extent is not | **`OI-S14-1`**, owner **SUB-16 (NEU-980)**, consumers **SUB-11** and **SUB-12**. Resolves when SUB-16 republishes and states, per finding, whether the disposition altered an authority. |
| — | **No QA pass is claimed for this sub-task.** The `qa-execution:engine` surface is unconfigured, so scenario execution is a genuine Core Article 8 no-op and the authored scenarios are marked **`NOT RUN`** | `../91_caps-and-incomplete-scope.md` `CAP-S1-3`; `../09_…md` §18 | — | `consumed` | `CAP-S1-3` is the package-level cap and is **cited, not duplicated**. |
| — | **Scope discipline:** no authority was edited, re-assigned or reconciled; no file under `src/`, `tests/` or `drizzle/` changed; `../94_package-completeness-gate.md` untouched; no `README.md` row added; **zero deletions on all four append-only registers** | `git diff --numstat origin/develop` over `../02_findings-register.md`, `../90_…md`, `../91_…md`, `../92_…md` — all `n 0`; `git diff --name-only origin/develop` | Automated check | `confirmed` | A green type-check or lint line is **not** evidence about this package's content (`../00_method-and-provenance.md` §1.1) and is claimed as none. |

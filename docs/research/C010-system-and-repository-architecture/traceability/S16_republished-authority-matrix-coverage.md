# `S16` — Traceability: republishing the authority matrix with every validation finding dispositioned

**Written by:** NEU-979 (SUB-16), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-3`'s republication obligation and `OUT-4` — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-16's own**
coverage and nobody else's. Every row below resolves into `docs/research/`. **Zero rows resolve into
`_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of
§1.2 (`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence
about this package's content** and appears in no row below — this sub-task touches zero source files, so
the build gates say nothing whatever about it.

**This file documents the `post-validation` revision.** SUB-13 assigned, SUB-14 validated, and this
sub-task dispositions and republishes; `OUT-3` splits those three across three sub-tasks and this file
documents the third only. **No authority in the matrix is originated, changed or re-scored here.**

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-4` | **Every finding SUB-14 routed is dispositioned with its cause as SUB-14 named it, its disposition, its owner and the row it affected — `received = dispositioned = 11`, `undispositioned = 0`, `owners = 11 of 11`** | `../10_republished-authority-matrix.md` §4.1, §5; counts taken independently at `../09_authority-matrix-validation.md` §15.3 and `../02_findings-register.md` `### SUB-14`, which agree at 11 | Automated check | `confirmed` | Nine of the eleven are *"no change here; owned there"* — the owners are named per row in §5 and re-stated in §9. A disposition is not a resolution. |
| `OUT-4` | **Every `no change` disposition carries its reason**, and no finding is closed by silent absorption into a revised cell | `../10_…md` §5 (the register's Disposition column), §5.1–§5.7, §5.12 | Structured AI review | `confirmed` | — |
| `OUT-4` | **A model-cause finding is re-routed, never re-decided here.** `F-S14-3` is re-routed as **`F-S16-2`** to `SUB-6 (NEU-976)` and carried as **`CAP-S16-1`**; `F-S14-7`'s clause question is re-routed as **`F-S16-3`** | `../10_…md` §5.2, §5.3; `../02_findings-register.md` `### SUB-16`; `../91_caps-and-incomplete-scope.md` `CAP-S16-1`; `DR-C10-S16-1` Decision 2 and Rejected alternative 2 | Structured AI review | `confirmed` | **`OI-S16-1`**: `NEU-976` is merged and `Done`, so **no scheduled pass will answer either**. Owner **`SUB-12 (NEU-986)`** at the gate. |
| `OUT-3` | **Two rows are revised, and both by re-running SUB-6's rule rather than by fresh judgement.** `SC-S3-16` and `SC-S3-17` re-derive to `CMP-S4-9` on **clause 5** (clause 1 no — durable; clause 2 no — `../05_…md` places `SC-S3-21`, not `SC-S3-17`, on the Tier-2 gate path; clause 3 empty under `M-A`; clause 4 no; **clause 5 yes — `Learner-scoped: question — open`**) | `../10_…md` §6.1, §6.2 (the clause-by-clause derivation table per row); `../07_state-ownership-model-selection.md` §6.1 | Structured AI review | `confirmed` | **Authority unchanged on both.** The revision is confined to *write path*, *consistency* and *observability* — `CMP-S4-9`'s charter is request-path-scoped and these tables are written from pino transport worker threads. |
| `OUT-3` | **Every revised row repopulates all nine `OUT-3` attributes and cites the clause as its basis** | `../10_…md` §6.1, §6.2 | Automated check | `confirmed` | — |
| `OUT-3` | **No revised row describes an ownership column as present.** `NEU-850`'s `OUT-2` is carried as a decision to honour, never as an existing schema fact; where an ownership key is assumed it is assumed **only** inside Census B's enumerated composed state | `../10_…md` §6.1, §6.2, §8.4; `../04_state-category-inventory.md` §6 (four-term search, zero matches) | Automated check | `confirmed` | — |
| `OUT-3` | **The exactly-one-authority audit is re-proved over the republished revision, not inherited:** **45 rows · 0 with none · 0 with two or more · 45 with exactly one · 45 distinct ids · 0 duplicates · 0 non-`CMP-S4-*`** | `../10_…md` §7.1, §8 — a mechanical parse of §8's own table (rows matching `` \| `SC-S3-<n>` \| `` with **exactly nine columns**, counting `CMP-S4-<n>` in the `Authority` cell only) | Automated check | `confirmed` | The nine-column filter is load-bearing: §5's and §6.4's tables also open with `SC-S3-<n>` and over-match by six rows. Stated so `SUB-11 (NEU-985)` can repeat it. |
| `OUT-3` | **The state-inventory↔matrix audit is re-proved over the republished revision: 0 unmatched inventory→matrix, 0 unmatched matrix→inventory**, over an independent re-extraction of `../04_…md` §3 yielding **45 distinct numeric ids, min 1, max 45, no gaps** | `../10_…md` §7.2; `../04_state-category-inventory.md` §3 | Automated check | `confirmed` | The routing rule and named owner for **each direction** are published at §7.2 against the (empty) unmatched sets, so a future non-zero result is already owned. |
| `OUT-3` | **The two audits are named distinctly from SUB-7's.** The state-inventory↔matrix audit here is **not** `SUB-7 (NEU-980)`'s `OUT-5` resource-inventory↔matrix cross-check — a different audit over a different inventory | `../10_…md` §3.1, §7.3, §11 | Structured AI review | `confirmed` | `OUT-5` is **not** discharged here and is claimed nowhere in `../10_…md`. |
| `OUT-3` | **`W` is excluded from the exactly-one audit** and is carried as an annotation, never a second authority — 13 non-durable rows carry `n/a — non-durable`, 32 durable rows carry a write path | `../10_…md` §3, §7.1, §8; `../08_per-state-authority-matrix.md` §7 | Automated check | `confirmed` | `F-S14-11`'s five pairings all fall on the **granularity** side of the published mapping rule; none threatens the audit. |
| `OUT-3` | **The republished revision carries an identifiable `post-validation` marker and a consumption statement naming `SUB-7 (NEU-980)`, `SUB-8 (NEU-981)` and `SUB-10 (NEU-984)`** as the parties that resolve against it and cite it by marker | `../10_…md` front matter, §2 | Structured AI review | `confirmed` | A downstream audit citing SUB-13's **`pre-validation`** revision is stated to be **a finding against that audit**. `SUB-11 (NEU-985)` and `SUB-12 (NEU-986)` are named alongside. |
| `OUT-3` | **Both censuses are carried forward as two columns and are never collapsed or summed.** Census A (target state (b)) — 24 `not-evaluable` · 19 `not-applicable` · 2 `fails-transport`. Census B (composed state (c)) — 15 `fails-confinement` · 19 `not-applicable` · 9 `not-evaluable` · 2 `fails-transport` | `../10_…md` §3, §8, §8.2; `../06_isolation-invariant-and-the-neu-893-split.md` §3.2 | Automated check | `confirmed` | Both reproduce `../09_…md` §15.2 **exactly**. Census B's assumed set is `NEU-850`'s `OUT-2` **and nothing else**; it is not a statement about any shipped system. |
| `OUT-3` | **`fails-principal: 0` is annotated `UNREACHED — not passed` in the cell itself**, in both censuses, so the number cannot be read bare | `../10_…md` §5.1 (`F-S14-2`'s disposition), §8.2 | Structured AI review | `confirmed` | The `I5` workload stays **unsized**. Owner **NEU-893** for the `sub`/`azp` defect; `SUB-11 (NEU-985)` for the reading constraint. |
| `OUT-3` | **A verdict holding only under one transport is recorded transport-qualified, never unqualified.** `SC-S3-19` and `SC-S3-20` are `fails-transport` in **both** censuses; no repaired verdict anywhere in the chapter is stated unqualified | `../10_…md` §8, §8.4, §9; `../05_system-context-and-responsibility-boundaries.md` `BND-S4-17` (STDIO: a trust boundary nothing enforces, owner `nobody`) | Structured AI review | `confirmed` | `F-S5-4`: **the binding constraint is the transport, not the schema.** `F-S4-5`: all three benchmark journeys are dogfooded across `BND-S4-17`, so *"the journey ran fine"* is not evidence about a gated path, and none is offered as such. |
| `OUT-3` | **SUB-3's `existing` / `required-by-upstream` / `assumed` marking survives the revision unchanged: 30 / 11 / 4** | `../10_…md` §7.1, §8; `../04_state-category-inventory.md` §8 | Automated check | `confirmed` | No row was split, merged, added or removed; the row count stays **45**. |
| `OUT-4` | **`F-S14-8`'s intra-`05_…md` contradiction is dispositioned without editing `05_…md`** — split into three claims, direction named on each, neither side silently reconciled, and the flow half re-routed as **`F-S16-4`** to `SUB-4 (NEU-974)` | `../10_…md` §5.4, §6.3 (the three published reasons); `../09_…md` §12 (*"would destroy the record of which one was written first and on what evidence"*); `DR-C10-S16-1` Decision 4 and Rejected alternative 1 | Structured AI review | `confirmed` | **`../05_…md`, `../08_…md` and `../09_…md` are byte-identical after this pass.** Three residues stay unrepaired **by design**; `SUB-11 (NEU-985)` is named for the mechanical class check. |
| `OUT-3` | **`OI-S14-1` is resolved by publication**, on a mechanical check that no authority, clause, `Learner-scoped` value or status marking changed and that **the write path a durable append is issued from is an input to none of `I1`–`I5`** | `../10_…md` §5.12, §8.1; `../90_open-items-and-provisional-register.md` `### SUB-16` | Automated check | `confirmed` | `../09_…md` §15's verdicts **stand over this revision and need not be re-run**, in whole or in part. |
| `OUT-3` | **Every row still carrying an unresolved finding at publication is named with its owner**, and the 30 rows carrying none are named as such | `../10_…md` §9 | Structured AI review | `confirmed` | Includes `SC-S3-42` (**NEU-891**), `SC-S3-31` (**SUB-10 / NEU-984**), `SC-S3-45` (**SUB-6 / NEU-976**), `SC-S3-16`/`SC-S3-17` (**SUB-4 / NEU-974**), `SC-S3-19`/`SC-S3-20` (**NEU-893**), `SC-S3-33` (**SUB-6**), `SC-S3-35` (**SUB-4**), and the 14 Census-B confinement-residue rows (**SUB-8 / NEU-981**). `F-S3-3`'s retention gap is **unowned** and routed to the gate to assign. |
| `OUT-4` | **Four existing caps are cited at the point of use, none duplicated and none closed** — `CAP-S4-1` (**sixth sighting; structural; must not be closed**), `CAP-S5-1`, `CAP-S6-1`, `CAP-S1-3` | `../91_caps-and-incomplete-scope.md` `### SUB-16`; `../10_…md` §9, §10 | Structured AI review | `consumed` | Assigning a **write path** to `SC-S3-16`/`SC-S3-17` does not touch `CAP-S4-1`: write path, write authority and deletion ownership are three different questions and this revision answers only the first. |
| `OUT-4` | **`93_stand-in-assumption-register.md` is closed and was cited, never appended to** — `A-25`, `A-27`, `A-28`, `A-29` each cited at the row whose verdict it decided | `../10_…md` §6.4; `../93_stand-in-assumption-register.md` | Structured AI review | `[unconfirmed]` — each stand-in is itself `[unconfirmed]` by construction | `A-25`'s tolerance envelope **cannot** resolve `F-S14-4`; `A-28`'s **cannot** rescue `SC-S3-45`. Both limits are stated at the row, not in an appendix. |
| `OUT-3` | **Vocabulary is disambiguated at first use** — `session` = **a learning run**, never an HTTP or auth session — and the `05_…md`↔`08_…md` vocabulary mapping `F-S14-11` asks for is published with its rule and all five pairings | `../10_…md` §3, §3.2 | Structured AI review | `confirmed` | Published so `SUB-11 (NEU-985)`'s mechanical audit does not re-report the five as defects. **Neither artifact amended**, which is the recommendation SUB-14 carried. |
| `OUT-4` | **A finding this pass raises about its own input:** SUB-14 pairs three sub-task labels with the wrong tracker ids in **24 places across 5 merged files** — `SUB-16 (NEU-980)` ×16, `NEU-983 (SUB-11)` ×7, `SUB-12 (NEU-985)` ×1 | `../02_findings-register.md` `### SUB-16` `F-S16-1`; `../10_…md` §5.7; counted mechanically over the merged tree | Automated check | `confirmed` | Second sighting of the `F-S3-2` class. **Not repaired by editing merged files**; `../10_…md` writes only correct ids. Owner **`SUB-12 (NEU-986)`**, with **`SUB-11 (NEU-985)`** for the mechanical label↔id check. |
| — | **No QA pass is claimed for this sub-task.** `qa-execution:engine` and `qa-execution:host` are both unconfigured, so scenario execution is a genuine Core Article 8 no-op and the authored scenarios are marked **`NOT RUN`** | `../91_caps-and-incomplete-scope.md` `CAP-S1-3`; `../10_…md` §12 | — | `consumed` | `CAP-S1-3` is the package-level cap and is **cited, not duplicated**. A mechanical parse of the republished table is not a QA pass. |
| — | **Scope discipline:** no authority originated or re-scored; no merged sibling artifact edited; no file under `src/`, `tests/` or `drizzle/` changed; `94_package-completeness-gate.md` untouched; no `README.md` row added; **zero deletions on all three append-only registers**; **no spike filed** | `git diff --numstat origin/develop` over `../02_findings-register.md`, `../90_…md`, `../91_…md` — all `n 0`; `git diff --name-only origin/develop` | Automated check | `confirmed` | `README.md` row 7's generic `03_…`–`89_…` range row, owned *"SUB-2 … SUB-16"*, already covers `10_…md`. `../92_spike-register.md` is unchanged. |

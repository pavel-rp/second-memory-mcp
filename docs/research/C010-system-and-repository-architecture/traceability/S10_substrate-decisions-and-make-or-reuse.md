# `S10` — Traceability: the substrate decisions and the make-or-reuse records

**Written by:** NEU-984 (SUB-10), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-8`'s substrate half, and `OUT-10`'s spike-execution share for this sub-task — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-10's own**
coverage and nobody else's. Every row below resolves into `docs/research/` or a real repository path
with a command or a line. **Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of
§1.2 (`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence
about this package's content** and appears in no row below.

Three counting conventions, stated once. **(i)** Every repository fact in the `Evidence` column was
read at **`03efe1d`** in a dedicated worktree on 2026-08-22; where a read value contradicts an
upstream chapter's stated premise, the row cites the finding that records the contradiction rather
than silently preferring either. **(ii)** A row marked `consumed` asserts only that this sub-task
adopted the upstream decision without re-deciding it; it makes no claim about that decision's own
support. **(iii)** A row whose claim is that something was **left open** is a real coverage row, not
an absence of one — the residual column carries what remains unsupported and who owns it.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-8` | Each of the three substrate choices is classified in scope by **SUB-15's published architecture-material rule**, applied through its own three-step test rather than restated, demonstrated or amended. | `../15_…md` §3 screening table; `DR-C10-S10-1`, `-2`, `-3`, each § "In-scope basis under SUB-15's rule" | Citation of `../13_…md` §4.1–§4.2 at its published cutoff, applied leg by leg | `consumed` | The rule is consumed, never re-derived. **No finding is routed to SUB-15** — no choice was found misclassified. The screening also returns a **negative**: sync-to-async movement within one process moves no boundary and is therefore not architecture-material, which is why `DR-C10-S10-3` clause 4 leaves it open. |
| `OUT-8` | The data-store topology is decided on evidence, with rejected alternatives recorded and a decisive criterion that is not preference. | `DR-C10-S10-1` § Decision, § Rationale, § Rejected alternatives (five rows); `../15_…md` §5.2 | Published artifact — the seven cross-category atomicity rows read from `../10_…md` §8 and `../08_…md` §8, against `src/ports/unit-of-work-port.ts:26`–`:28` | `confirmed` | The criterion rests on `../09_…md` §7's termination premise. **No score is justified by latency** — `F-S15-2` establishes the only available figure is an in-process floor, and `CAP-S15-1` that the deployed cost is unmeasured. |
| `OUT-8` | The store-reversal check SUB-6 handed over was **run against the selected topology and its result recorded**, not left implicit. | `../15_…md` §5.3; `DR-C10-S10-1` § "The store-reversal check SUB-6 handed over" | Citation — `../07_…md` §5.1, §5.2 lookup table, §5.3 conjunction, quoted verbatim and evaluated conjunct by conjunct | `confirmed` | Result: **selection stable, no finding routed to SUB-6.** The selected topology matches §5.2 row 1, whose prescribed action is "None. Record the match." `OI-S6-1` closes on the check being run and recorded, which this row is. |
| `OUT-8` | `R3`'s disposition is carried forward exactly as SUB-7 left it, neither strengthened nor weakened. | `../15_…md` §5.3 | Citation — `../11_…md` §13, quoted verbatim | `consumed` | `R3` = **NOT ESTABLISHED**, inheriting `A-27`'s `[unconfirmed]` status and re-openable only if `A-27` is invalidated. `CAP-S15-2` records that `A-27` moves all three SUB-15 decisions together. |
| `OUT-8` | The `SC-S3-33` / `SC-S3-34` carve-out honours an existing merged migration-path constraint rather than overriding it, and is shown not to satisfy any of the three reversal-adjacent conditions. | `DR-C10-S10-1` clause 5 and § "Why the carve-out does not reopen the reversal"; `../15_…md` §5.4 | Published artifact — `../08_…md` §8.6's prohibition quoted verbatim and evaluated against `R1`, §5.2 row 2 and §5.2 row 4 | `confirmed` | Had the decision instead pulled both rows into the shared store, it would have contradicted a merged migration-path cell and required a finding to the matrix's owner. Neither row appears in any of the seven atomicity pairs, so the carve-out costs the decisive criterion nothing. |
| `OUT-8` | All **eighteen** store-`none` categories are reconciled against the selected topology, each resolved to a destination or **explicitly recorded as still unplaced**. | `../15_…md` §5.4 — an eighteen-row table, `SC-S3-28` through `SC-S3-45` | Published artifact — read from `../08_…md` §8.5, §8.6, §8.7, cross-checked against `../10_…md` §8 and `../04_…md` §3.5–§3.7 | `confirmed` | `OI-S13-1`'s resolving event is **conjunctive** and this row discharges the second half. Totals: 12 shared store, 2 drift store, 3 no destination **by construction**, 1 no destination **by decision**. Record *shape* remains out of scope for all eighteen. Three rows carry unmet preconditions, stated in-table. |
| `OUT-8` | The four cross-category interactions `OI-S13-1` names are each dispositioned. | `../15_…md` §5.5 | Published artifact against direct reading — `src/transport/rate-limit-middleware.ts:58`, `src/orchestration/tier2-circuit-breaker.ts:68`–`:76`, `src/transport/http.ts:226`–`:232`, `:245`–`:250` | `confirmed` | Three of the four (`SC-S3-20`, `SC-S3-21`, `SC-S3-18`) are resolved by **one instance of each process**, a third route `../08_…md` §8.4 did not enumerate — and hold **only while `CAP-S10-3`'s premise holds**. The fourth (`SC-S3-38` against `SC-S3-37`) is resolved to *expressible*, not *declared*. |
| `OUT-8` | The deployment shape is decided with rejected alternatives and a decisive criterion measured rather than assumed. | `DR-C10-S10-2` § Decision (five clauses), § Rejected alternatives (six rows), §6.3 of `../15_…md` | Direct reading of installed source — `node_modules/drizzle-orm/pg-core/dialect.js:44`–`:72` (drizzle-orm 0.45.1), `drizzle/0000_strong_tarantula.sql:1`, `src/transport/main.ts:27`, `:62`–`:65` | `confirmed` | The migration-exclusivity clause is forced by a **read-derived TOCTOU**, filed as `F-S10-2`. `drizzle-orm` is read as an existing fact, never selected — see the scope row below. |
| `OUT-8` | The `OI-S9-4` check is **answered explicitly**, and the accepted warning `F5.8` is thereby answered. | `../15_…md` §6.2; `DR-C10-S10-2` § "`OI-S9-4` — the check SUB-9 handed over, answered explicitly" | Published artifact against direct reading of `.github/workflows/cd-prod.yml` at `03efe1d` | `confirmed` | Answer: **No** — no public repository is required. `DR-C10-S9-1` stands. **But SUB-9's stated structural reason is contradicted**: `cd-prod.yml:62`–`:65` shows the host holds a clone and fetches into it, against `../14_…md` §6.4's "never sees the repository". Filed as `F-S10-1`, routed to SUB-9 (merged) under the accepted `F5.7` residual pattern. |
| `OUT-8` | The AI-orchestration placement cites `A-26` with its tolerated envelope and its invalidating outcome, and rests on the one envelope clause no tolerated budget can change. | `DR-C10-S10-3` § "The decisive criterion"; `../15_…md` §7 | Citation — `../93_…md` `A-26`, `[unconfirmed]`, standing in for NEU-891, quoted with all three envelope clauses | `[unconfirmed]` | The decision settles **ownership only**. Where on the timeline the call happens is left open by clause 4, because envelope clause (b) states ownership and timeline are independent. **No latency, privacy or cost claim is made anywhere** — none could be supported. |
| `OUT-8` | The fail-open, optional-and-degrades-to-disabled posture is retained as the stated baseline rather than re-derived. | `DR-C10-S10-3` § "Why the fail-open posture is retained rather than re-decided" | Direct reading — `src/composition-root.ts:387`–`:398`; `src/ports/content-classifier-port.ts:19`–`:21`; `src/orchestration/topic-workflows.ts:297`–`:322`; `src/orchestration/chunk-workflows.ts:425`–`:450`, `:1161`–`:1182`; `src/orchestration/tier2-circuit-breaker.ts:1`–`:16`, `:91`–`:112` | `confirmed` | Nothing in evidence displaces the baseline, so retaining it is the null hypothesis rather than a choice needing its own justification. |
| `OUT-8` | **Four** make-or-reuse records are answered `build here` / `reuse-from-core` / `adopt-external` against one shared criteria set fixed before any capability was answered, each with rejected alternatives. | `../15_…md` §8, §8.1–§8.5 | Published artifact against direct reading — `src/transport/jwt-middleware.ts:6`–`:51`, `:114`–`:131`; `src/ports/embedding-port.ts`; `src/shared/prompts/classifier-prompts.ts`; `src/shared/logger.ts`; and zero-match searches for citation-drift, scheduler, metrics and tracing code at `03efe1d` | `confirmed` | The list stayed **closed at four as published**. `F-S15-1` is honoured: **no answer is justified by the application reusing core code** — each rests on vocabulary and inherited obligation. |
| `OUT-8` | Every reuse-from-core answer states the SUB-8 compatibility obligation it inherits, **including STDIO coverage**, and the distribution-line side it lands on. | `../15_…md` §8.1, §8.2, §8.4, and the §8.5 summary table | Citation — `../12_…md` `CC-S8-2`, `CC-S8-3`; `F-S8-1` (46 tools / 43 gated / 3 exempt / 49 audit entries); `../13_…md` §6.6's pricing | `confirmed` | **Two of the reuse answers inherit an obligation whose subject does not exist**: `OI-S8-2` records STDIO has no gate to extend and `CC-S8-3` is unowned. Stated as the price rather than discounted; `OI-S8-2` is thereby sighted twice more. |
| `OUT-8` | The identity reuse answer discloses that the reuse has **no surface to reuse today**. | `../15_…md` §8.1; `F-S10-5` | Direct reading — `src/transport/jwt-middleware.ts`, `src/config/resolve-auth-config.ts`, `src/transport/http.ts:59`, `:206`; zero `ports/auth-port.ts`, zero identity references under `src/ports/` or `src/domain/` at `03efe1d` | `confirmed` | `CC-S8-2` designates token-bound identity reusable-core, but the implementation is welded to Express `res.locals`. Extraction is an implementation obligation this answer creates, filed as `F-S10-5` and routed to SUB-8 (merged). |
| `OUT-8` | Accepted warning **`F5.9` fired**, and the resulting gap is declared rather than absorbed or silently repaired. | `../15_…md` §8.6 | Citation — `DR-C10-S2-2`'s decision quoted verbatim; SUB-2 §7's deferral of the isolation primitive to SUB-10 | `confirmed` | **SUB-2 kept the authoring-time execution environment**, its isolation boundary makes it architecture-material by SUB-15's own rule, and **no sub-task owns its build/reuse/adopt decision**. The list stays closed at four; `OUT-8`'s requirement is left **undischarged for a fifth capability**, for `NEU-986 (SUB-12)`'s completeness gate to dispose of. No claim is made that the gap is harmless. |
| `OUT-8` | The production-compatibility assessment names **all ten** facts and returns operable / not operable / cannot be determined for each, covering the four the charter names. | `../15_…md` §9 — a ten-row table | Direct reading at `03efe1d` — `src/transport/main.ts:27`; `.github/workflows/cd-prod.yml`; `docker-compose.yml`; `src/transport/http.ts:82`–`:83`, `:91`; `src/transport/rate-limit-middleware.ts:58`; `.env.example:79`–`:81`; and worktree-wide zero-match searches for Dockerfile, IaC, reverse proxy, rollback, Redis, metrics and tracing | `confirmed` | **3 operable (facts 1, 2, 4), 2 not operable today (facts 7, 9), 5 cannot be determined (facts 3, 5, 6, 8, 10).** Fact 8 (process-local in-memory state) resolves to *cannot be determined* because its operability turns on the running instance count, which fact 10 records as not discoverable (`CAP-S10-3`); its conditional sub-result — operable at one instance, not under replication, per `F-S10-3` — is stated inside that verdict, not as a fourth label. The two *not operable* verdicts — no rollback path, non-probing `/health` — are real incompatibilities, **left unrepaired** because repairing either is a CI or source change this sub-task scopes out. Recorded so an implementation charter meets them as stated work. |
| `OUT-8` | Every deployment-shape conclusion resting on a platform capability not verifiable in the repository is a **cap with a named owner or an open-items citation — never an assertion**. | `../15_…md` §10.2 — `CAP-S10-1`, `CAP-S10-2`, `CAP-S10-3`, `CAP-S10-4`, each in `../91_…md` § SUB-10 with its full field set | Register entries with named owners | `confirmed` | `CAP-S10-1` **discharges `OI-S1-3` (charter assumption 31) by the conversion route that item itself names**. `CAP-S10-2` **discharges `OI-S2-1` by its own permitted cap route** — and no mechanism is guessed, which that item forbids. `CAP-S10-4` shares `CAP-S10-2`'s root cause: no scheduler exists in the repository. |
| `OUT-8` | Caps inherited from siblings are carried forward with their status stated, and none is closed on this sub-task's authority. | `../15_…md` §10.3 | Register citation — `CAP-S6-1`, `CAP-S15-1`, `CAP-S15-2`, `CAP-S8-1`, `CAP-S4-1`, `CAP-S5-1`, `CAP-S16-1` | `consumed` | **`CAP-S6-1` is not lifted** — no store could be stood up (no container runtime; `127.0.0.1:5432` `ECONNREFUSED`), and nothing is asserted in its place. **`CAP-S15-1` is not lifted** — the topology precondition is now supplied, the harness precondition cannot be. **`CAP-S4-1` is untouched and not closed**; §5.4 records `SC-S3-41`'s adjacent precondition as an **eighth sighting** without claiming the cap covers it. |
| `OUT-8` | `F-S14-5`'s premise is tested rather than accepted, and the shape it names is routed rather than asserted. | `../15_…md` §11; `DR-C10-S10-1` consequence 3; `F-S10-4` | Published artifact — `F-S14-5` quoted verbatim and evaluated against the selected topology | `confirmed` | The cell says "SUB-10 decides it with the store"; **selecting one store does not decide it** — both shapes are expressible in one store, and record shape is schema design, out of scope. **SUB-14's walk counts are preserved unchanged: 180 walks, 178 defined, 2 undefined.** `F-S14-5`'s own mis-pairing to NEU-980 is an `F-S16-1` instance, neither repaired nor propagated. |
| `OUT-8` | The scope check confirms **no framework or library pick**, and no Dockerfile, compose file, IaC or CI change. | `../15_…md` §12 — a nine-row table against `OUT-8`'s own out-of-scope list | Mechanical — `git diff --numstat` for this change touches only `docs/research/C010-system-and-repository-architecture/**` | `confirmed` | `drizzle-orm` 0.45.1 and Pino appear as **read facts about the existing system**, never as selections. Nothing was created, deployed or configured. |
| `OUT-10` | **No spike was filed**, and every candidate evaluated is disclosed with its disposition rather than silently dropped. | `../15_…md` §10.1 — four candidates in a table; `../92_…md` § SUB-10 | Published artifact naming, per candidate, what was read and what it answered | `confirmed` | Coverage stated as: **zero spikes run, three candidates withdrawn under the read test, one capped, none omitted.** Follows `../13_…md` §11.4's withdrawn-spike precedent. A zero-spike result is a real outcome for `OUT-10`, not an absence of coverage — the test SUB-1 prescribes is "could this have been read instead?", and three times it could. |
| `OUT-10` | The three withdrawn candidates were each settled by reading, with the read named. | `../15_…md` §10.1 rows 1–3; `F-S10-2`, `F-S10-3` | Direct reading of installed source and repository source — `node_modules/drizzle-orm/pg-core/dialect.js:44`–`:72`; `src/transport/http.ts:82`–`:83`, `:226`–`:232`, `:245`–`:250`; `../10_…md` §8 against `src/ports/unit-of-work-port.ts:26`–`:28` | `confirmed` | Two produced findings (`F-S10-2`, `F-S10-3`) rather than spike records, which is the correct artifact for a read-derived result. **No harness was built and nothing was written under `src/`, `tests/` or `drizzle/`**; `_local/scratch/` holds no `SPK-S10-*` artifact because none was created. |
| `OUT-10` | The one candidate no spike could settle is recorded as a **cap**, not asserted. | `../15_…md` §10.1 row 4; `A-26` cited at `DR-C10-S10-3` | Register citation plus the absence of a reachable provider credential in this run | `confirmed` | The AI latency/cost envelope is infeasible on two independent grounds: no provider credential was reachable, **and `A-26` records that no budget exists to test against** — a measurement with no acceptance threshold answers nothing. The second ground is the stronger one and would hold even with a credential. |

---

## What this file does not establish

- **It is not the outcome-coverage audit.** `NEU-985 (SUB-11)` owns those rows in its own file;
  nothing here edits a sibling's traceability file, and this file makes no claim about any outcome
  other than `OUT-8`'s substrate half and `OUT-10`'s spike share for this sub-task.
- **It is not a completeness judgement.** `94_package-completeness-gate.md` is `NEU-986 (SUB-12)`'s
  alone and is untouched by this sub-task. The `F5.9` gap declared at `../15_…md` §8.6 is stated
  **for** that gate, not disposed of here.
- **It does not claim the open items it discharges are now answered.** `OI-S1-3` and `OI-S2-1` are
  discharged **by cap**, which is the route each item names — the underlying facts remain unknown
  and the mechanism remains unselected. Discharging an item and answering its question are
  different things, and this file does not conflate them.
- **No row is evidenced by a build, type-check or lint result.** Per `../00_method-and-provenance.md`
  §5, this is a documentation deliverable and those are no-regression checks, not evidence about
  content. **This sub-task filed no spike, so it has no execution-evidence exception** — unlike
  `S9`'s `SPK-S9-1` rows, every row above rests on published artifacts, citations or direct reading.
- **`qa-execution:engine` is unconfigured** in this project, so **no QA pass is claimed** in any row
  above or in any artifact this sub-task produced.

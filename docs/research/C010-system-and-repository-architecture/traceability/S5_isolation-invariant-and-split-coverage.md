# `S5` — Traceability: the isolation invariant and the NEU-893 split contract

**Written by:** NEU-975 (SUB-5), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-21
**Model:** claude-opus-5[1m]
**Covers:** `OUT-4` — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-5's own**
coverage and nobody else's. Every row below resolves into `docs/research/` or a real `src/` /
`drizzle/` path with a line. **Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three
labels of §1.2 (`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line
is not evidence about this package's content** and appears in no row below.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-4` | Learner ownership lives in the MCP core database schema, keyed to the JWT subject; the answer is `NEU-850's OUT-2`'s, cited as its source, and is **not** re-derived here. No placement trade study was run. | `../06_…md` §1 | Upstream decision consumed with its owner-attached id and its tracker-read date (2026-08-19); carried in full because no NEU-850 package exists under `docs/research/` (standalone rule, `../00_method-and-provenance.md` §3) | `consumed` | NEU-850 is **converged but unimplemented** — OUT-2 is a decision to honour, never an existing schema fact. Its scope over the two port-less log tables is unresolved: `OI-S5-1`. |
| `OUT-4` | The amendment right was exercised — as **"no amendment routed"** — with both examined candidates and the bar for clearing it recorded, rather than left open. | `../06_…md` §2 | Reasoned disposition against two named candidates, each with its rejection ground stated | `confirmed` | Candidate A is filed as `OI-S5-1` and routed to NEU-850's `OUT-1` drift check. A later sub-task finding evidence that the JWT subject is not a viable ownership key would clear the bar this one did not. |
| `OUT-4` | The isolation invariant is stated as a **testable property**: one named evaluation domain, five ordered checks each answerable from a cited artifact, a closed six-verdict set, and first-failure-wins adjudication — so it returns exactly one verdict per state category. | `../06_…md` §3.1–§3.4; `DR-C10-S5-1` | Constructed decision procedure over `../04_state-category-inventory.md` §3's 45 rows; five worked applications reaching five distinct verdicts (`../06_…md` §3.6) | `confirmed` | The procedure is **unexercised against a real matrix** — SUB-13's authority assignment does not exist, so cases 3–5 name their own target state (form (c), enumerated per §3.2): `OI-S5-3`. It has **zero positive instances**: `CAP-S5-1`. And **I3 is not symmetrically decidable** (§3.4.1) — soundly failable from one unscoped path, but not passable without an enumerated access-path set, which no artifact yet supplies. |
| `OUT-4` | `question — open` in the `Learner-scoped` column places a category **in** domain; only an explicit `no` exempts one. A category cannot be exempted by leaving its scoping question unanswered. | `../06_…md` §3.3; `DR-C10-S5-1` (I1's sub-rule) | Rule stated against `../04_…md` §3's column and §6's scoping discipline (a question per entry, never a schema fact) | `confirmed` | The 18 rows currently reading `question — open` are `SUB-13 (NEU-977)`'s to answer (`OI-S3-1`); this row establishes only how the invariant treats them meanwhile. The column census (19 `no` / 18 open / 8 `yes` = 45; in-domain 26) is re-counted at this sub-task's cutoff, not inherited. |
| `OUT-4` | The `sub`-versus-`azp` threat case is walked to a stated verdict — `fails-principal` at I5 — and the invariant states what it *means* when the authenticated subject is an OAuth client rather than assuming a human learner. | `../06_…md` §4.1; §3.6 case 5 | Direct file inspection: `src/transport/jwt-middleware.ts:127`, `:133`–`:136`; `src/transport/rate-limit-middleware.ts:76`–`:77`; `src/transport/http.ts:32`–`:35`, `:52`–`:72`, `:83`, `:206`–`:209`; zero `sub` occurrences in `src/orchestration/`, `src/ports/`, `src/adapters/` — all at the 2026-08-21 cutoff | `confirmed` for the mechanism; `[unconfirmed]` for the production case | No live production token was inspected; **`OI-S1-2` is carried forward, not closed**, and is named as an input to NEU-893 (`H5`). Whether the resolved identity will carry its `sub`/`azp` provenance is `OI-S5-2` (`H6`). |
| `OUT-4` | The unauthenticated STDIO case is walked to a stated verdict — `fails-transport` at I4 — and the invariant's behaviour under that transport is stated explicitly rather than assumed away. | `../06_…md` §4.2; §3.6 case 4 | Direct file inspection: `src/transport/main.ts:55`–`:59` against `src/transport/http.ts:163`–`:165`, `:106`–`:120`, `:172`–`:174`, `:185`–`:187`, reached only from `main.ts:46`–`:54`; plus `../05_…md` §4.2 `BND-S4-17` (unenforced, owner `nobody`) and `F-S4-5` | `confirmed` | Whether the unenforced edge is **reachable** in the production deployment is `SUB-10 (NEU-984)`'s; the verdict is unconditional on it. Closing the gap is NEU-893's (`H7`). |
| `OUT-4` | **At this cutoff no state category reaches `holds`, and the binding constraint is the transport rather than the database schema** — the full OUT-2 change still leaves every in-domain category at `fails-transport`. | `../06_…md` §4.2, §4.3; `F-S5-4` | Consequence derived from the ordered checks over the transport facts above | `confirmed` | Recorded as a cap on this package's own reach, not on the invariant: `CAP-S5-1`. The sequencing consequence — closing STDIO surfaces the `sub`/`azp` defect rather than resolving it — is handed to NEU-893. |
| `OUT-4` | The split with NEU-893 is stated as a contract whose universe is **derived from the invariant's five checks**, with the disjointness audit reported as counts: universe 18, List A 10, List B 8, **on both 0**, **on neither 0**, routed elsewhere 7 (each named with its owner). | `../06_…md` §5.1–§5.5; `DR-C10-S5-2` | Derivation from `DR-C10-S5-1`'s checks, re-walked as the audit; 10 + 8 = 18 is arithmetic over the published tables | `confirmed` | The universe is only as complete as the five checks — if the check set is missing a failure mode, the contract inherits the hole and the audit still reports 0 and 0. Stated as a cost in `DR-C10-S5-2`'s Consequences. |
| `OUT-4` | NEU-893's four charter questions — identity mapping to the production Rauthy IdP, migration of existing global rows, staged rollout, rollback — each appear on the handed-on list **exactly once** and on no other list. | `../06_…md` §5.3 (`H1`–`H4`), §5.5 | Enumeration against the charter's own four, checked against List A and §5.4's routed-elsewhere set | `confirmed` | List B carries **8**, not 4: `H5`–`H8` fall out of the derivation and were not in the brief. Over-assignment is a real risk and is argued in `DR-C10-S5-2`'s rejected alternative 4. |
| `OUT-4` | The core-schema change's backward-compatibility **obligation** and **blast radius** are stated — narrow (the tool surface) and wide (`AppContext`, the 9 row-owning ports and adapters, `SearchPort`'s read paths, `UnitOfWorkPort`'s tx-scoped instances, the single-learner query bodies) — plus the separate decision on the 3 exempt tools and the absent STDIO gate. | `../06_…md` §6.1–§6.3 | Direct file inspection at the 2026-08-21 cutoff: `src/composition-root.ts:136`–`:314`, `:518`–`:636`, `:638`; `src/transport/main.ts:41`; `src/transport/http.ts:220`; `src/adapters/drizzle/session-repository.ts:73`–`:80`; `src/orchestration/session-workflows.ts:39`–`:46`; `src/transport/context-token-middleware.ts:5`–`:9`; `src/ports/*` | `confirmed` | The per-item regression audit and the pricing of the semantic change are **`SUB-8 (NEU-981)`'s** and are explicitly not run here. This row hands over the obligation and the radius only. |
| `OUT-4` | **Zero** gated tool input schemas would newly declare `context_token`, so no bulk schema migration is implied — held **count-independently**, as "the set of gated tools lacking `context_token` is empty". | `../06_…md` §6.2, §7 row 8; `F-S5-3` | Re-count at this sub-task's own cutoff under `CAP-S1-2`, command cited: 46 registered / 43 gated / 3 exempt across 16 modules; 42 via a named `*InputShape` constant (41 imported from `src/domain/types/`, 1 module-local at `src/server/session-progress-tools.ts:128`–`:138`) plus 1 genuinely inline (`src/server/teaching-tools.ts:34`–`:35`) | `confirmed` | The charter's 45/42/40 are stale in all three positions; `F-S4-1`'s 46 is corroborated. **The count is a moving target** — SUB-8 must re-run the command rather than cite any figure in this document. |
| `OUT-4` | The codebase facts the invariant must survive are recorded, each cited by path and line and re-counted at this sub-task's own cutoff rather than inherited from the charter. | `../06_…md` §7 (12 rows) | Direct file inspection plus two stated grep commands over one tree at one cutoff, reported per file so they can be re-run | `confirmed` | Two inherited facts are **corrected**: the global-active-session guard is in orchestration, not the adapter's `createSession` (`F-S5-2`); `AppContext` has 53 closures across 57 properties and is **one instance shared by every MCP session** (`F-S5-1`). |
| `OUT-4` | Applying the invariant to **every row** of the `OUT-3` matrix — the first clause of OUT-4's own "Verified by". | **Not discharged here.** `SUB-14 (NEU-978)`, against `SUB-13 (NEU-977)`'s matrix | — | — | Stated as a gap deliberately, not covered: the matrix does not exist yet. `../06_…md` §3.6 works five hand-picked categories and §3.7 states that this is a termination demonstration and **not** a census. Tracked as `OI-S5-3`. |

---

## Coverage note

`OUT-4`'s "Verified by" clause has four parts. Three are discharged above — the disjointness
audit, the NEU-850 reconciliation record with its amendment disposition, and the two-case
threat walk. **The fourth — application to every matrix row — is not**, and its row says so
rather than being omitted. An outcome with a partially-covered verification clause is
`NEU-986 (SUB-12)`'s to weigh at the completeness gate; hiding the fourth part would have made
`OUT-4` read as fully verified by this sub-task alone, which it is not.

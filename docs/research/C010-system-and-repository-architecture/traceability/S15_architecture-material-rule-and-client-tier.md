# `S15` — Traceability: the architecture-material rule and the web tier's runtime, protocol style and rendering model

**Written by:** NEU-982 (SUB-15), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-8` (the rule-and-client-tier half) and `OUT-10` (spike execution) — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-15's own** coverage
and nobody else's. Every row below resolves into `docs/research/` or a real `src/` path with a line.
**Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of §1.2
(`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence about this
package's content** and appears in no row below.

**Three scoping statements are made before any row is read.** (1) **`OUT-8` is covered in half.** This
sub-task owns the classification rule and the **client-tier** technology selections; the **substrate**
selections — data-store topology, deployment shape, AI-orchestration placement — are `SUB-10 (NEU-984)`'s,
and no row below claims them. A reader checking `OUT-8`'s completeness must read `S10`'s register beside
this one. (2) **`OUT-10` is covered for this sub-task's spikes only** — one run, one withdrawn with its
reason — not for the package's spike discipline, which is SUB-1's (`../92_spike-register.md` §1–§9).
(3) **All three decisions inherit `A-27`, which is `[unconfirmed]`**, so no decision row below is labelled
`confirmed` on its dependency status, however well-evidenced its reasoning. `CAP-S15-2` records that the
three go stale **together**, which no single row can show.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-8` | A **rule**, not a list, states what makes a technology choice architecture-material, and yields in-scope/out-of-scope **without further interpretation**: a choice is material when changing it, **on its own**, would move a **boundary**, reassign an **authority**, or alter a **compatibility contract**; all three no → not material, and this package does not make it. | `../13_…md` §4.1; `DR-C10-S15-1` Decision | The rule as published plus the §4.2 procedure — state the system with the choice and with its most plausible alternative, then check the three tables. Checkable by applying it, not by trusting it | `confirmed` | The rule **prioritises nothing, names no owner and settles no contested cell**; stated as a limit at `../13_…md` §4.8 so it is not read as more than it is. It has been applied by no reader outside this chapter — reproducibility is a property of its form, not yet an observation. |
| `OUT-8` | The rule's three terms are **bound to artifacts a reader can open**, so applying it needs no new judgement: **boundary** = a `BND-S4-*` row's existence, pair or class; **authority** = an Authority cell among `SC-S3-1`…`SC-S3-45`; **compatibility contract** = a `CC-S8-*` clause or the tool surface it bounds (46 / 43 / 3). | `../13_…md` §4.1 (binding table); `DR-C10-S15-1` Decision | `../05_…md` §4.2; `../10_…md` §8 at revision `post-validation` (SUB-16 / NEU-979); `../12_…md` §6.1, §7.1–§7.2 | `confirmed` | The bindings are to **tables that can be republished**. `DR-C10-S15-1` revision trigger 1 fires if `../10_…md` §8 is republished past `post-validation` or `../05_…md` §4.2 gains or loses a row — the rule's *form* survives, what it tests against does not. |
| `OUT-8` | The rule is demonstrated on a choice **the package never anticipated** and returns a determinate answer: *does the web tier keep a server-side web-session store, or re-derive from the bearer token?* → **architecture-material** on two limbs (it moves `BND-S4-16`'s existence; it touches `SC-S3-43`'s authority) — **and not this chapter's**, so it is **routed, not decided**. | `../13_…md` §4.4; `DR-C10-S15-1` Rationale ("Why a yes is separated from ownership") | Worked limb by limb against the bound tables, with the firing limbs named rather than a bare verdict; filed as **`OI-S15-2`** (`../90_…md` § SUB-15) and routed to `BND-S4-16`'s owner `SUB-6 (NEU-976)`, co-named with `SUB-13 (NEU-977)` for the authority limb | `confirmed` | This is the criterion's hardest case and it lands on a cell that is **`undecided`** (`../05_…md` §4.4). The rule reaches `BND-S4-16` **without disturbing it** — that is `C5` — but a reader wanting the *answer* to the session-store question will not find it here, by design. |
| `OUT-8` | The rule is demonstrated **in both directions**: an unanticipated **out-of-scope** choice — server-side versus client-side localisation — returns **not material** on all three limbs, so a "no" is shown as reproducible as a "yes". | `../13_…md` §4.6 | The same three-limb procedure applied and each limb answered `no` with its table named | `confirmed` | A single negative demonstration. The rule's discrimination is shown on four worked cases in total (§4.3, §4.4, §4.6, §4.7); four is four, and a fifth could disagree. `DR-C10-S15-1` revision trigger 4 names exactly what would refute it: a choice uncontroversially material that moves none of the three. |
| `OUT-8` | **Framework and library selection is excluded by derivation, not by naming.** Router, ORM, component kit, test runner and styling approach are each run through the test individually and each returns not-material. | `../13_…md` §4.7; `DR-C10-S15-1` Consequences 2 | Per-candidate evaluation against the three bound tables, reproducible by a reader | `confirmed` | Being a **criterion input** is not the same as being architecture-material — the test runner is not material *and* is a criterion `SUB-9 (NEU-983)` scores. Conflating them would pull tooling into this package through SUB-9's dependency; `../13_…md` §4.7 says so rather than leaving it to inference. |
| `OUT-8` | The **"on its own"** clause is load-bearing and shown to be, on the ORM case: the bundled question splits into *does the web tier reach durable storage at all* (**material** — `BND-S4-16`, and **SUB-6's**) and *given that edge, which ORM* (**not material**). | `../13_…md` §4.7; `DR-C10-S15-1` Rationale ("Why 'on its own' earns its place") | The split performed and both halves classified, rather than the bundle returning "it depends" | `confirmed` | The material half routes to a cell recorded **`undecided`**. Two of the four demonstrations end at `BND-S4-16`, which is the rule working correctly and also a concentration: if `BND-S4-16`'s status is misread, two demonstrations are misread together. |
| `OUT-8` | The rule is **distinct from SUB-8's `R8-1`…`R8-5`** and the two compose rather than compete — `R8-*` classifies *where a capability lives* (public core / private application) and returns a distribution-line consequence; this rule answers *whether the package should decide the choice at all*. One choice is worked through both. | `../13_…md` §4.5; `DR-C10-S15-1` Rejected alternative 4 | `../12_…md` §5 (`R8-1`…`R8-5`), **consumed and not restated**; the worked comparison returns different, composable answers | `consumed` | Rejecting reuse of `R8-*` is a **category** judgement, not a quality one, and is recorded as such. If a later reader finds a choice where the two rules give *contradictory* rather than composable answers, that is a defect in this record and routes here as a finding. |
| `OUT-8` | **Decision 1 — runtime and language: TypeScript on Node, shared with the core.** Decided against a weighted criteria set fixed **before** scoring, with the decisive criterion **C1**: mechanical contract-sharing over the 43 gated tool schemas, which a foreign-language client cannot reach because `RD-S8-4`'s golden-manifest method does not see it. | `../13_…md` §6; `DR-C10-S15-2` Decision, Rationale | Criteria table with weights fixed in advance; four rejected alternatives each carrying the consequence that decided it; the contract surface read from `../12_…md` §7.1–§7.2 (`F-S8-1`) | `[unconfirmed]` | **The obvious argument for this decision is unsound and is not used.** `F-S15-1` establishes that `src/domain/` is runtime-portable (65 entries, **0** Node builtins) and that `CC-S8-5` has the web tier reaching state **only** through tool calls — so code reuse proves nothing. The decision rests on the narrower contract-sharing ground, which is weaker than the folklore argument it replaces, and says so. |
| `OUT-8` | Decision 1's **rejected alternatives are argued, not dismissed** — including the one the same chapter's evidence **strengthens**: a non-Node JS runtime (Deno/Bun/edge) *"would probably work… buys nothing this package can name while costing all four of SUB-9's criteria"*; and **no separate web-tier runtime** is **disqualified by this chapter's own rule**, because it collapses `BND-S4-2`. | `DR-C10-S15-2` Rejected alternatives 1–4 | The chapter's own rule applied to its own alternative (§4.3), so the disqualification is derived rather than asserted | `confirmed` | Alternative 1 (a different language entirely) is **more viable here than folklore suggests**, precisely because of `F-S15-1`'s portability result, and the record states that rather than hiding it. The rejection therefore rests on a **cost** argument about SUB-9's criteria, which `CAP-S15-1`'s unmeasured deployment cost does not price. |
| `OUT-8` | The runtime is **argued, not inherited**, and what `SUB-9 (NEU-983)` receives is stated explicitly: a criterion-input table over build tooling, testing strategy, local development and release process, **with no reuse obligation to price**, because none was taken on. | `../13_…md` §6; `DR-C10-S15-2` Consequences | The criterion-input table published in the decision record itself, so SUB-9 does not re-derive a constraint that never existed | `confirmed` | SUB-9 has not consumed it. A criterion-input table written by its producer is a hand-off, not an agreement, and if SUB-9 scores against a different input set the two records diverge with nothing detecting it. |
| `OUT-8` | **Decision 2 — protocol style: a resource-oriented read surface over the 11 read-projections, plus named-intent writes over the 5 write-intents.** A write is never a verb-mutation on a read resource. Decided against SUB-7's **published** sixteen-entry inventory, not a hypothetical one. | `../13_…md` §7; `DR-C10-S15-3` Decision, Rationale | `../11_…md` §9, §9.2, §10.1, §6; criteria `P1`–`P5` with weights fixed before scoring; four properties of the inventory read off and cited individually | `[unconfirmed]` | The **asymmetry is the decision** and it is only as good as the inventory's shape: `DR-C10-S15-3` revision trigger 2 fires if §9 is republished with a different entry count or access-mode split. |
| `OUT-8` | Decision 2 **specifies no wire contract** — 0 endpoint paths, 0 payload schemas, 0 error catalogues, 0 versioning schemes, 0 pagination models — honouring `../11_…md` §12.2's stop, so a reader finding no endpoint path concludes the contract is **open by design, not missing**. | `../13_…md` §7; `DR-C10-S15-3` Decision clause 2 | `../11_…md` §12.1, §12.2 (the stop as published), consumed unchanged | `consumed` | The inherited constraint is carried, not relaxed: **no resource may be authoritative for any of the 45 categories**, and every resource must resolve to exactly one of the authorities §9 names. Nothing in this package enforces that on the contract nobody has written yet. |
| `OUT-8` | Decision 2's rejected alternatives include the **tempting** one argued on its merits — GraphQL/client-composed query, rejected on **authority visibility** (`P2`) and surface expansion (`P3`), explicitly **not** on the trust property, since no gate moves — and one rejected as a **category error**: tRPC-style typed RPC is *"not a protocol style"*, a framework pick out of scope by this chapter's own rule. | `DR-C10-S15-3` Rejected alternatives 1–5 | Each rejection carries the specific counted fact that decided it (no CRUD symmetry; 3 of 11 reads have no store; `W5` has no update path) | `confirmed` | Recording the framework pick as **rejected-at-this-level** rather than silently omitting it is what keeps the exclusion derived. A downstream charter may adopt it *underneath* the style chosen here, and this record does not forbid that. |
| `OUT-8` | **Decision 3 — rendering model: server-rendered/server-composed on every gate-bearing read; client enhancement confined to non-gate-bearing state.** Decisive criterion **D1**: `DR-C10-S4-3`'s *"stale-or-absent is the serve path's ordinary operating mode"* together with `R-4`. | `../13_…md` §8; `DR-C10-S15-4` Decision, Rationale | `DR-C10-S4-3`; `../05_…md` §6.3 `R-4`; criteria `D1`–`D5` with weights fixed before scoring | `[unconfirmed]` | The decision is **correctness-argued and cost-unpriced**: `CAP-S15-1` records that the real deployed round-trip cost is unmeasured, and every gate-bearing read now pays it. If the cost is high the decision does not become wrong — it becomes expensive, and nothing here says how expensive. |
| `OUT-8` | Decision 3 is argued **without** the obvious argument, because `../05_…md` §6.3 **`R-5` forbids citing the trust property for or against any rendering model**. The constraint is recorded as criterion **D2** and honoured rather than worked around. | `../13_…md` §8; `DR-C10-S15-4` criterion `D2` | `../05_…md` §6.3 `R-5`, read and applied as a binding constraint on this chapter's own reasoning | `confirmed` | This is the chapter's sharpest self-limit: the argument a reader will *expect* is unavailable, and its absence could be misread as an oversight. `D2` exists so the omission is legible as compliance. A reader who has not read `R-5` will find §8 under-argued. |
| `OUT-8` | Decision 3's rejected alternatives are argued **including one this record declines to claim**: *browser reads the drift cache* is marked **"Not this record's rejection to claim"** and consumed from `DR-C10-S4-3`/`R-4` instead of re-decided. | `DR-C10-S15-4` Rejected alternatives 1–5 | The prior decision cited as the deciding party; offline-capable/local-first rejected on `A-27`'s **named invalidating outcome** rather than on preference | `consumed` | Declining a rejection is unusual and is stated rather than implied. The cost is that a reader auditing this record's alternative set finds one entry whose reasoning lives in another document. |
| `OUT-8` | **Assumption `A-27` is cited in Decision 3's decision sentence with its tolerance envelope and its invalidating outcome**, per `../00_method-and-provenance.md`'s requirement that a stand-in be cited in the claim it supports, not only in an evidence table. | `../13_…md` §8; `DR-C10-S15-4` Decision | `../93_stand-in-assumption-register.md` `A-27`, quoted with envelope and invalidating outcome | `consumed` | `A-27` is `[unconfirmed]` and owned at **NEU-896**. It is load-bearing for **all three** decisions, not just this one. The correlation is recorded at `CAP-S15-2`, at `../13_…md` §8.3, at `DR-C10-S15-2`'s revision trigger 5 and at `DR-C10-S15-4`'s consequence 5 — but `DR-C10-S15-3`'s dependency is **inherited rather than direct**, and is carried only by its revision trigger 5, added for exactly that reason. |
| `OUT-8` | The three decisions' **interactions** are recorded as a directed index rather than left implicit: runtime → protocol style (the shared runtime lets the 43 gated schemas serve as contract types, so the style stays thin — `P5`); rendering model → protocol style (server composition forces whole-projection reads, not client-selected fields). | `../13_…md` §9; `DR-C10-S15-3` §"The two dependencies this decision carries" | Each interaction recorded **at the decision it constrains**, with the direction stated, so neither is a symmetric hand-wave | `confirmed` | The index is **not exhaustive by construction** — it records the interactions found, and an interaction nobody noticed is invisible to it. Notably, had the runtime gone to a foreign language, GraphQL gains its strongest argument; that counterfactual is stated, and it means the two decisions are **less independent than their separate records suggest**. |
| `OUT-8` | The chapter **takes no substrate decision and originates no authority**: 0 state categories added or reassigned, 0 boundaries moved, 0 merged chapters amended, 0 authorities originated. Register writes are append-only with **deletions 0** on all four. | `../13_…md` §10; `git diff --numstat` deletions column = 0 for `../02_…md`, `../90_…md`, `../91_…md`, `../92_…md` | The package's append convention (`../README.md`); `../93_…md` (closed, **no sixth entry**) and `../94_…md` untouched | `confirmed` | The scope audit is a **self-report**. `NEU-985 (SUB-11)` owns the independent check, and `NEU-986 (SUB-12)`'s gate at `../94_…md` reads it. The strongest external corroboration available today is the numstat count, which shows deletions but not over-reach. |
| `OUT-8` | Findings are **routed with named owners rather than absorbed**: **2 findings filed** (`F-S15-1`, `F-S15-2`), **2 open items** (`OI-S15-1`, `OI-S15-2`), **2 caps** (`CAP-S15-1`, `CAP-S15-2`), **1 spike** (`SPK-S15-1`), **1 spike candidate withdrawn** with its reason. | `../02_…md` `### SUB-15`; `../90_…md`; `../91_…md`; `../92_…md` | Per-entry records carrying the stable field set each register requires | `confirmed` | `OI-S15-1` routes to **`SUB-13 (NEU-977)`**, `OI-S15-2` to **`SUB-6 (NEU-976)`** co-named with SUB-13, and `F-S15-2` and `CAP-S15-1` route to **`SUB-10 (NEU-984)`** — the accepted **F5.7** back-routing warning shared with SUB-6, SUB-7 and SUB-8. Each is co-routed to `NEU-896` or `SUB-12` so none is addressed solely to a party that may have shipped. The residual — a back-routed finding going unactioned — is **carried, not fixed**. |
| `OUT-10` | A spike was run **because reading could not settle it**, and §3's justification test is discharged **by naming what was read and what each failed to answer**: `src/domain/`'s own imports (direct, not transitive), `CLAUDE.md`'s purity claim (design intent, the thing under test), `tsconfig.json`'s `"types": ["node"]` (project-wide ambient types, not a runtime fact), and `CC-S8-5` (settles the contract half, silent on portability). | `../92_…md` `SPK-S15-1` §"Why reading could not settle it" | Four named sources with a per-source reason, not a blanket "nothing answered it" | `confirmed` | The test is applied by the spike's own author. `../92_…md` §3 provides the standard; whether it was applied strictly is a judgement `NEU-985 (SUB-11)` can re-check against the four named sources. |
| `OUT-10` | The spike's **exit condition is an observable event stated before it ran** — the transitive closure of all 65 `src/domain/` entry points enumerated with its builtin count reported, **beside a control arm known to contain builtins** — and it ends whichever way the number comes out. | `../92_…md` `SPK-S15-1` §"Exit condition" | The condition as published; **not** a date and **not** anyone's satisfaction, per §5 | `confirmed` | A closure that could not be enumerated would have ended the spike as **failed**, not as a zero; the record says so, which is what stops a null result being read as a negative one. |
| `OUT-10` | The spike's **result is stated in full in the register**, because the quarantine tree is gitignored and no later reader can open it: all 65 `domain/` entry points reach **0 Node builtins** (external: `zod`, `compromise`, `markdown-it`); the control arm `infrastructure/db/client.js` reaches **6** builtins and `composition-root.js` reaches **7** across 103 files. **The control discriminated.** | `../92_…md` `SPK-S15-1` §"Result"; `F-S15-1` | Five arms reported with files reached, external packages and builtins each; run at `229e8f4` on Node **v22.23.1**; corroborated statically — `src/domain/` is 65 files with 0 imports of `pg`, `drizzle-orm`, `node:*`, `fs` or `path` | `confirmed` | The walk is **static**: a runtime-only `require`, a dynamic `import()` behind a computed specifier, a conditional export, or a native binding inside `zod`/`compromise`/`markdown-it`'s **own** internals would be invisible to it. None was found; their absence is not proven. |
| `OUT-10` | The spike's **failed half is disclosed rather than dropped**: part 1's runtime IO detector returned 0 events on **all** arms **including the control**, because the db client connects lazily — so **the detector did not discriminate and part 1 supports no conclusion in either direction**. Part 2 exists because part 1 failed this way. | `../92_…md` `SPK-S15-1` §"Result" (part 1), §"Confidence" | The control's null result reported as the reason the method was abandoned, not omitted from the record | `confirmed` | Confidence is recorded as **high for part 2, none for part 1**. A reader who reads only the headline number will not know one of two methods failed; the record's structure is the only thing that carries it. |
| `OUT-10` | **One harness artifact is disclosed**: part 2's specifier regex captured the string `" must be a valid calendar date"` as an external package name — a defect of the extractor, not a dependency — and it is excluded from every count reported. | `../92_…md` `SPK-S15-1` §"Result" | The artifact named, explained and excluded, rather than silently filtered | `confirmed` | One known extractor defect implies the extractor is imperfect, which is why the Confidence field names an independent re-walk (a bundler's dependency graph) as what would raise confidence. |
| `OUT-10` | **Quarantine held**: everything ran under `_local/scratch/NEU-982/`; **nothing was written under `src/`, `tests/` or `drizzle/`**, nothing was merged as product code, no dependency was added to `package.json`, and `pnpm-workspace.yaml` is unmodified. | `../92_…md` `SPK-S15-1` §"Quarantine path" | `_local/` is gitignored (`../../../.gitignore:100`); the scripts import and parse and write no file | `confirmed` | Because the quarantine is gitignored, **the scripts do not survive this task** — the method is reproducible from the record's Method field, not from the artifacts. That is the intended trade and it is why the Result field is stated in full. |
| `OUT-10` | The spike carries a **mandatory expiry** — **2027-04-30** — with a rationale tied to an event rather than a habit: **Node 22.x reaching end-of-line**, since a runtime migration is the likeliest trigger for a dependency sweep, and a sweep is what silently changes a transitive closure. | `../92_…md` `SPK-S15-1` §"Expiry", §"Expiry rationale" | The date and its rationale published together, per §6; expiry is **not** an exit condition and the record does not close on it | `confirmed` | The expiry bounds the *measurement*, not the *decision*: `DR-C10-S15-2` rests on contract-sharing reach, so an expired closure result weakens `F-S15-1`'s refutation of the reuse argument without unseating the runtime choice. |
| `OUT-10` | **One spike candidate was considered and withdrawn under §3, with its reason disclosed** rather than omitted: *can a rendering model satisfy the trust property without a server round trip on a gate-bearing read?* — withdrawn because `../05_…md` §6.2 point 3 and `R-1`/`R-4` answer it **by construction**, so a prototype would demonstrate the published rule rather than test it. | `../92_…md` `### SUB-15`, closing paragraph | §3's justification test applied to a candidate the author wanted to run; follows **SUB-14's precedent** for disclosing withdrawals | `confirmed` | Disclosure makes `OUT-10`'s coverage legible — **one run, one withdrawn, none omitted** — but a withdrawal is an author's judgement that reading sufficed. If a later reader finds `R-1`/`R-4` do **not** settle it by construction, the withdrawal was wrong and routes here as a finding. |

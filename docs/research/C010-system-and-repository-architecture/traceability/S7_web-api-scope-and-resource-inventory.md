# `S7` — Traceability: the web API's scope, negative boundary and resource inventory

**Written by:** NEU-980 (SUB-7), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-5` — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-7's own**
coverage and nobody else's. Every row below resolves into `docs/research/` or a real `src/` path with a
line. **Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of
§1.2 (`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence
about this package's content** and appears in no row below.

**`OUT-5` is discharged by this file's rows and by no row in `../traceability/S13_…md` or
`../traceability/S16_…md`.** Those cover `OUT-3`'s state-inventory ↔ matrix audit, which is a different
audit over a different inventory (`../11_…md` §3). Neither outcome is credited from the other's rows.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-5` | The web API's **responsibility, its relationship to MCP, and its relationship to production learning state** are published: it terminates the learner's web session, serves the learner-facing surface, reaches state only by calling the MCP core, and **holds zero of the forty-five categories**. | `../11_…md` §5.1 | `../05_…md`'s scoping of `CMP-S4-3` quoted verbatim; `../10_…md` §8's authority distribution showing `CMP-S4-3` absent from the Authority column; `../10_…md` §11's by-construction statement | `consumed` | The scoping itself is `[unconfirmed]` in `../05_…md` under `A-27`. This row inherits that, and the chapter labels it at every use rather than promoting it. |
| `OUT-5` | The **negative boundary** is published for **all forty-five** categories, each with the authority that displaces the API and a **row-specific reason**. Categories left silent: **0**. | `../11_…md` §10.1 (45-row table); count restated at §16 | Enumeration of `../10_…md` §8's 45 rows, each joined to its `Status` marking and its `../04_…md` §3 entry; reasons cited to the entry that establishes each | `consumed` | The reasons are derived from merged evidence, not from observation of a running web tier — none exists. A reason that turns out wrong is a finding against this chapter, and §10.1's per-row form is what makes such a finding locatable. |
| `OUT-5` | The **resource inventory** names every capability the API exposes with **exactly one** SUB-13 authority behind each: **16 entries, 16 exactly-one, 0 zero-authority, 0 two-or-more, 0 authorities originated here.** | `../11_…md` §9.1–§9.3 | Per-entry join to `../10_…md` §8; `W`-column annotations excluded per that chapter's own rule (*"an annotation, never a second authority"*) | `consumed` | The inventory is bounded to `Status: existing` categories — `CAP-S7-2`. Eight learner-facing categories with no store carry a stated non-exposure instead of an entry, so "16" is a count over what can be served today, not over the eventual surface. |
| `OUT-5` | The **resource-inventory ↔ matrix cross-check** is run and reported as **unmatched counts in both directions**: **inventory→matrix 2**, **matrix→inventory 0**. | `../11_…md` §11.2–§11.4 | Bidirectional join between §9's entries and `../10_…md` §8's rows, under the individuation rule published at `../11_…md` §6 | `consumed` | The `matrix→inventory` zero is **trivially zero by construction** — §10.1 enumerates all forty-five, so no row can be missing — and `../11_…md` §11.3 says so rather than presenting it as a discovery. |
| `OUT-5` | The cross-check **names the revision it resolved against**, and that revision is **SUB-16's post-absorption one**: `` `08_…md` + `10_…md`, revision `post-validation` (SUB-16 / NEU-979) ``. | `../11_…md` §2 | The marker string reproduced exactly from `../10_…md` §2, which also names SUB-7 (NEU-980) in its consumption statement | `consumed` | None on the citation itself. A revision published after `post-validation` obliges a re-run, not a carry-forward — recorded as `DR-C10-S7-1` revision trigger 3. |
| `OUT-5` | **Every** unmatched item is published as a finding with a **named owner** and a **recorded disposition** — not as a bare count. Two of two. | `../11_…md` §11.2; `F-S7-1`, `F-S7-2` in `../02_…md`; `OI-S7-1` in `../90_…md`; `CAP-S7-1` in `../91_…md` | Per-item record carrying the inventory item, the expected value, what was found, the owner and the disposition | `confirmed` | **Both route to SUB-13 (NEU-977), which is merged and closed** — the accepted `F5.7` warning. Co-routed to NEU-896 and SUB-12 (NEU-986) so the finding is not addressed solely to a closed party. The residual — that a back-routed finding goes unactioned — is carried, not fixed. |
| `OUT-5` | The **routing rule is published before the counts** and applies to an empty set as readily as a populated one, so the audit is reproducible by a third party. | `../11_…md` §11.1; the reproducible filter at §6 | Rule stated in advance: matrix-side → SUB-13 (NEU-977); inventory-side → held by SUB-7 (NEU-980) with the matrix-side counterpart routed | `confirmed` | The filter's step 3 (the learner-facing test) is a **judgement applied by a stated rule**, not a mechanical column read. `../11_…md` §7.1 publishes the test verbatim so a re-run can disagree with a specific row rather than with the count. |
| `OUT-5` | The **negative-boundary review** asserts, for every state category the API does not own, that the inventory **says so** — reported as **45 of 45**, with the seven access-mode non-exposures on exposed categories recorded separately and classed deliberate or structural. | `../11_…md` §10.1, §10.2 | 45-row enumeration; 7-row access-mode table splitting 3 deliberate + 1 deliberate-partial + 3 structural | `consumed` | The deliberate/structural split is load-bearing: a structural non-exposure has **no policy owner**, and a downstream charter that reads all seven as policy would go looking for one who does not exist. Stated in §10.2 rather than left to inference. |
| `OUT-5` | Every **learner-facing authority the API does not expose** appears as a **stated deliberate non-exposure with a named lifting condition** — never as an omission. **19** learner-facing rows: **11** exposed, **8** stated non-exposures, **0** silently absent. | `../11_…md` §7.2, §10.3 | Learner-facing test published at §7.1 and applied to all 45 rows; the eight non-exposures each carrying `Status`, the reason, and what would lift it | `[unconfirmed]` | The test is applied against *"the surface `A-27` posits"*, which is unbuilt. A different but defensible reading of "learner-facing" would move rows between the 19 and the 26; the test is published verbatim so the disagreement lands on a rule rather than on a number. |
| `OUT-5` | The **scope audit** confirms **zero endpoint paths, zero payload schemas, zero error catalogues** (and zero versioning schemes, zero pagination models), with the stop stated **in prose**. | `../11_…md` §12.1 (counts), §12.2 (prose) | Direct property of the published chapter, checkable by reading it; the prose states the stop is deliberate and names SUB-15 (NEU-982) as the owner of what comes next | `confirmed` | The stop leaves a real gap: nobody yet owns the wire contract. `../11_…md` §12.2 converts that into a **binding constraint** on whoever writes it rather than leaving it merely open. |
| `OUT-5` | The **auth and trust boundary** is placed against SUB-4's model **by boundary id**, and **every** security claim names the transport and the deployment tier it holds for. Seven claims, seven qualified, zero unqualified. | `../11_…md` §8.1 (`BND-S4-1`, `BND-S4-2`, `BND-S4-16`, `BND-S4-17`), §8.2 (7 claims) | `../05_…md`'s boundary rows with their own `Transport` column; `src/transport/jwt-middleware.ts:114`, `http.ts:99`–`:111`, `main.ts:46`, `src/shared/redact-params.ts:1`, `:13`, `audit-middleware.ts:88`, `:109` | `confirmed` | `BND-S4-16` is still recorded **undecided** in `../05_…md`; `M-A` resolves it and this chapter states the resolution, but **may not amend a merged sibling**. The annotation is left to SUB-4 (NEU-974) — a closed owner, so the same `F5.7` residual applies. |
| `OUT-5` | Assumption **`A-27`** is cited **in the sentence of the scope decision itself**, with its tolerance envelope and its invalidating outcome stated at that point rather than in a footnote. | `../11_…md` §5.2; `DR-C10-S7-1` Decision | `../93_…md` `A-27` quoted for envelope, invalidating outcome and re-validation trigger; cross-checked against `../05_…md` §6.3 constraint `R-3` | `[unconfirmed]` | `A-27` is `[unconfirmed]` and its re-validation trigger — **NEU-892 lands** — is outside this charter's reach. Everything downstream of the scope decision inherits that status, and `../11_…md` §16 labels which sections do. |
| `OUT-5` | The **`R3`** conjunct of `../07_…md` §5.3's store-reversal conjunction is answered explicitly — **NOT ESTABLISHED** — with its reasoning, its `A-27` dependence and its re-opening condition, and is **routed to SUB-10 (NEU-984)**. | `../11_…md` §13.1–§13.3 | Three-step argument over §9's sixteen entries and §11.2's two unmatched items, each tested against `R3`'s two clauses separately | `[unconfirmed]` | A negative result reported as a result. It settles `S6_…md`'s open question — whether `M-A`'s `C7 = 2` is *"merely awkward or actually blocking"* — as **awkward**, and inherits `A-27` entirely: invalidation re-opens `R3` over the same inventory with no new work. This chapter does **not** run §5.3's conjunction check and claims no re-confirmation of `M-A`. |
| `OUT-5` | The two audits are **named apart before either number is reported**, and neither is credited for the other. | `../11_…md` §3, restated at §11.4 | Side-by-side table of left-hand set, owner, publication point, result and outcome; `../10_…md` §7.2's own *"This audit is not SUB-7's"* cited from the other side | `confirmed` | None. This is the failure mode the chapter was most exposed to, and the separation is stated twice — once before the audit and once with its result. |
| `OUT-5` | The chapter **originates no authority and amends no merged artifact**: 45 rows consumed, 0 rows added/split/merged, 0 authorities originated, 0 merged chapters modified, register writes append-only. | `../11_…md` §16; `git diff --numstat` deletions column = 0 for `../02_…md`, `../90_…md`, `../91_…md` | The package's own append convention (`../README.md`); `../92_…md`, `../93_…md`, `../94_…md` untouched | `confirmed` | `CAP-S4-1` is sighted a **seventh** time and **left open**; `F-S16-1`'s tracker-id drift is neither re-filed nor propagated, and the 24 mis-paired instances in merged files are **not repaired here** — append-only is a discipline this chapter obeys rather than an incapacity. |

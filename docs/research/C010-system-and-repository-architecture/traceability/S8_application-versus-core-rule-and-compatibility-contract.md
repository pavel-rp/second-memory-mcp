# `S8` — Traceability: the application-versus-reusable-core rule and the backward-compatibility contract

**Written by:** NEU-981 (SUB-8), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-22
**Model:** claude-opus-5[1m]
**Covers:** `OUT-6` — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-8's own** coverage
and nobody else's. Every row below resolves into `docs/research/` or a real `src/` path with a line.
**Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of §1.2
(`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence about this
package's content** and appears in no row below.

**Two counting conventions are stated before any number is used.** (1) The per-item audit covers **49**
entries — 46 registered tools plus 3 registered prompts — not 48. The verified tool count is 46
(`../12_…md` §7.1), corroborating `F-S5-3`; auditing 45 would leave one registered tool unaudited to match
a figure the same evidence procedure disproves. (2) The `EQ-S*` ids cited below belong to **C009's**
sub-tasks, not to C010's, per `F-S3-1` — they are cited as C009 ids throughout.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-6` | A **rule**, not a list, classifies a proposed capability the package never enumerated as application-specific or reusable core, and yields the classification **without further interpretation**: five ordered, first-match-wins clauses (`R8-1`…`R8-5`), of which `R8-5` always fires, so the procedure terminates on every input. | `../12_…md` §5; `DR-C10-S8-1` Decision | The procedure as published, checkable by applying it; the inputs it needs enumerated at `../12_…md` §4 so a reader can confirm they hold only published artifacts | `confirmed` | The rule has classified no capability outside this chapter. Reproducibility is a property of its form, demonstrated on six implied changes and three C009 rows — not yet observed across independent readers. |
| `OUT-6` | **Every classification records which side of the distribution line the capability falls on**, and the licensing consequence is carried **inside** each clause rather than appended to the rule. | `../12_…md` §5 (`R8-1`–`R8-5`, each with a "Distribution line" line); `DR-C10-S8-1` Rationale | Charter assumption **32** (`confirmed`, Intake Q7) cited in the clauses themselves per `../00_…md`'s requirement, not only in the Evidence table | `consumed` | Assumption 32 is a commercial commitment outside this charter's reach. If it changes, every clause's distribution half is wrong at once while the classifications survive — `DR-C10-S8-1` revision trigger 1. |
| `OUT-6` | The rule is demonstrated against **every core change this architecture implies** — six, `CC-S8-1`…`CC-S8-6` — each put through the clauses in order with the firing clause named. | `../12_…md` §6.1 | Per-change clause-by-clause evaluation, not a bare verdict; each carried into §8.3's contract with an obligation and a detection method | `confirmed` | `CC-S8-6`'s set is **empty** (0 of 45 rows name a web authority). A demonstration over an empty set demonstrates nothing about that driver, and §10.4 says so rather than presenting the zero as a pass. |
| `OUT-6` | NEU-890's server-side enforcement points are sent **both ways**: **≥1 application-specific** and **≥1 reusable core**. `EQ-S3-12` (`G-NODE-EXISTS`) → application via `R8-1`; `EQ-S1-10` (`G-CITE-RESOLVE`) → reusable core via `R8-4`; `EQ-S3-2`+`EQ-S3-15` (`G-PRESELECT`) → split via `R8-3`. | `../12_…md` §6.2; `DR-C10-S8-1` Evidence | NEU-890 established as the adopted umbrella for **charter C009** (Linear NEU-890, scope naming `server-side` gates); rows read from `docs/research/C009-course-content-quality/09_enforceable-quality-system.md`'s published `server-side` class (15 of 89 rows) | `confirmed` | The three rows are drawn from one published table under one enforcement class, so the differentiation comes from the rule rather than from row selection — but three rows are three rows. A fourth could disagree. |
| `OUT-6` | The **regression boundary is fixed** as the existing public surface, enumerated as seven properties `B-1`…`B-7` covering tool names, input schemas, the snake_case convention, the `toolData`/`toolError` envelope, `content_quality` as the sole carrier of structured `findings`, prompt names and `argsSchema`, and the exempt-tool list. | `../12_…md` §8.1 | `src/server/tool-helpers.ts:80`, `:53`, `:76`; `src/server/topic-tools.ts:85`–`:96`; `src/transport/context-token-middleware.ts:5`–`:9`; `CLAUDE.md` → Naming Conventions | `confirmed` | `B-5` and `B-7` are **routing** properties, invisible to any structural diff of the tool manifest. They are on the list because of that, and nothing in this package enforces them mechanically. |
| `OUT-6` | The **per-item audit** records each of the **49** entries as contract-changed or unchanged against **both** drivers, with the detection method named where it changes. Driver A (identity): **44 changed / 5 unchanged**. Driver B (authority): **0 changed / 49 unchanged**. **Conditional entries: 0.** | `../12_…md` §10.1–§10.3 | Per-entry resolution of all 46 registrations to their declaring shapes by brace-matched enumeration (§7.3); all 45 authority rows read from `../10_…md` §8 | `confirmed` | **Not one of the 44 is a schema change.** `RD-S8-4` is specified to return zero delta across all 49, so the audit's own headline number is invisible to the mechanical check — which is why `RD-S8-1` is mandatory beside it. |
| `OUT-6` | The audit **names the revision it resolved against**, and that revision is SUB-16's post-absorption one: `` `08_…md` + `10_…md`, revision `post-validation` (SUB-16 / NEU-979) ``. | `../12_…md` §2 | The marker string reproduced verbatim from `../10_…md` §2 | `consumed` | None on the citation. A revision published after `post-validation` obliges a re-run of §10.3, not a carry-forward — `DR-C10-S8-1` revision trigger 4. |
| `OUT-6` | A tool writing a state category assigned to **web authority** is recorded contract-changed with its detection method named. **The set is empty: 0 of 45 rows name `CMP-S4-3`**, so the criterion is discharged **vacuously** — reported with its count and its derivation, with the conditional statement recorded so the zero is still useful. | `../12_…md` §10.4; `F-S8-3` | All 45 rows of `../10_…md` §8 read for their Authority column; corroborated by `../10_…md` §11 (by construction under `M-A`) and `../11_…md` §9.3 (0 of 16 inventory entries) | `consumed` | A vacuous discharge is a weaker result than a populated one and is labelled as such. The conditional — under `M-C`, `SC-S3-43` is the single row that would move, with `RD-S8-1` as its method — rests on `../11_…md` §13's `R3` verdict, which inherits `A-27` and is `[unconfirmed]`. |
| `OUT-6` | The **cost of a per-call identity argument** is priced against the **verified** schema state: gated **43**, exempt **3** recorded separately, **schemas newly declaring the argument 0**, **no bulk schema migration**, the priced cost being the **semantics** of reusing an already-declared argument, and **`RD-S8-1` named as a detection method a schema diff would miss**. | `../12_…md` §9.1, §8.2 | §7.3's per-tool resolution (43 of 43 declaring, 41 + 2); `src/infrastructure/db/schema.ts:312`–`:321` | `confirmed` | The pricing's substance is the last three rows, not the first. The zero-migration figure is the intuitive cost and it is zero; the real cost is a meaning change no client's generated types will see. |
| `OUT-6` | The **empty-schema exempt tools are a separately recorded decision**, not folded into the gated count — and the decision is **not uniform**: `init_agent_context` changes contract as the mint point; `get_server_info` and `get_server_workflow` change under neither option. **1 of 3.** | `../12_…md` §9.1, §10.2; `DR-C10-S8-2` Decision clause 5 | `src/server/server-context-tools.ts:11` (`ctx.createContextToken()`, empty input schema); `server-info-tools.ts:8`; `server-workflow-tools.ts:8` | `confirmed` | Folding the three into the gated count would have hidden the one that matters. That it is 1-of-3 rather than 0-of-3 or 3-of-3 is the reason the criterion demands they be recorded apart. |
| `OUT-6` | **STDIO has no gate to extend**, stated as such rather than assumed away. | `../12_…md` §9.1, §11 | `src/transport/main.ts` and `create-server.ts` contain zero references to the middleware or to `context_token`; `main.ts:55`–`:59` | `confirmed` | Consumes `../05_…md` `BND-S4-17`, whose owner is **nobody**. Unchanged by this chapter, and `CC-S8-3` remains unowned (`OI-S8-2`). |
| `OUT-6` | The per-call and token-bound options are **assessed against a weighted criteria set stated before the scoring**, and the contract **names which it obligates**: **token-bound (`CC-S8-2`)**, with per-call (`CC-S8-4`) rejected on **forgeability**. | `../12_…md` §9.2–§9.3; `DR-C10-S8-2` Decision, Rationale, Rejected alternatives (5) | The criteria table with weights fixed in advance; five rejected alternatives each carrying the specific consequence that decided it | `[unconfirmed]` | The choice is an obligation on unwritten code. `CAP-S8-1` records that no implementation exists and the detection methods are specified rather than run. Rejected alternative 2 — reuse the token argument to carry a caller-encoded principal — is *indistinguishable in a diff* from the chosen option and opposite in threat model; that is the residual most likely to bite. |
| `OUT-6` | The learner-ownership core change SUB-5 hands over (`user_id NOT NULL` keyed to the authenticated principal, consumed from NEU-850's `OUT-2`) is shown **reusable, backward-compatible and non-DP-specific**, with its regression boundary named. | `../12_…md` §6.1 (`CC-S8-1`, clause-by-clause), §8.3 | `R8-4` fires on both halves; `authenticated principal` is published vocabulary in `docs/GLOSSARY.md`; obligation and detection at §8.3 | `consumed` | **NEU-850 is converged but unimplemented** — its `user_id` commitment is a decision to honour, never an existing schema fact (`../04_…md` §6: zero matches for `user_id`/`userId` in `schema.ts`). The backfill has no correct value for rows written before any principal existed, and §8.3 names that cohort rather than defaulting it. |
| `OUT-6` | **No DP-specific concept appears in any proposed core surface.** Five surfaces reviewed (`CC-S8-1`…`CC-S8-5`) plus the rule's vocabulary and the five detection methods: **0 carrying one, 0 findings.** | `../12_…md` §13 | Per-surface enumeration rather than a blanket assertion; corroborated against the codebase — no course vocabulary in any tool name, schema field or prompt | `confirmed` | The two `DP` tokens in `src/` are **Dynamic Programming** (`src/domain/types/teaching.ts:285`, `src/domain/algorithms/grade-mapper.ts:10`), a naming collision filed as `F-S8-4` so a grep-based leakage audit does not misreport them. |
| `OUT-6` | **Every security and compatibility claim names the transport it holds for**; none is stated unqualified. Five claim rows, five qualified. | `../12_…md` §11 | `src/transport/http.ts:186`; `jwt-middleware.ts`; `rate-limit-middleware.ts`; `main.ts:55`–`:59`; `../05_…md` `CMP-S4-4` vs `CMP-S4-5` | `confirmed` | The asymmetry is total: **every** security claim this chapter makes is HTTP-qualified, and the chosen option does not change that. Stated as a consequence rather than a caveat. |
| `OUT-6` | The chapter **originates no authority, amends no merged artifact, and re-files no published finding**: 45 rows consumed, 0 added/split/merged, 0 authorities originated, 0 merged chapters modified, register writes append-only, deletions **0**. | `../12_…md` §16; `git diff --numstat` deletions column = 0 for `../02_…md`, `../90_…md`, `../91_…md`, `../92_…md` | The package's append convention (`../README.md`); `../93_…md` (closed) and `../94_…md` untouched | `confirmed` | Two publishable observations were deliberately **not** filed because a duplicate would have been *less precise than the original*: the tool-surface count (`F-S5-3` owns it; `F-S8-1` is scoped to refining its diagnosis from "stale" to "miscount") and the `sub`-or-`azp` principal collapse (`F-S5-4` and `../06_…md` §4.1 own it). |
| `OUT-6` | Findings are routed with named owners rather than absorbed: **4 filed** (`F-S8-1`…`F-S8-4`), **2 open items** (`OI-S8-1`, `OI-S8-2`), **1 cap** (`CAP-S8-1`), **0 spikes**. | `../02_…md` `### SUB-8`; `../90_…md`; `../91_…md`; `../92_…md` (explicit no-new-spike disclosure) | Per-entry record carrying the finding, its evidence, its consequence and its owner | `confirmed` | **Two of the four route to a closed owner** — the accepted **F5.7** warning shared with SUB-6, SUB-7 and SUB-15. Each is co-routed to NEU-896 or SUB-12 so none is addressed solely to a closed party. The residual — a back-routed finding going unactioned — is **carried, not fixed**. |
| `OUT-6` | `F-S7-4` — a web-originated mutation arriving **gated but not attributed** — is received by this sub-task as `../10_…md` §8.3's named owner of the confinement residue, and answered. | `../12_…md` §9.3; `DR-C10-S8-2` Decision clauses 1–2 | `F-S7-4`'s hand-off naming `SUB-8 (NEU-981)`; the answer is the issue-time binding that gives the token a principal to attribute | `[unconfirmed]` | Answered as an **obligation**, not as a fix: nothing implements it. `F-S7-4`'s own residual — that it rests on a web tier assumed under `A-27` — is inherited unchanged. |

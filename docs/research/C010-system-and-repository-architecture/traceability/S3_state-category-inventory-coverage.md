# `S3` — State-category inventory coverage

**Task:** NEU-973 (SUB-3) · **Charter:** C010 (umbrella NEU-895) · **Compiled:** 2026-08-21
**Model:** claude-opus-5[1m]
**Covers:** `OUT-2` (in full).
**Audit rows are not here.** `NEU-985 (SUB-11)` owns the cross-cutting audit in its own file; this register documents SUB-3's own coverage only.

**Every row below resolves inside `docs/research/`.** Zero rows resolve into `_local/` or `docs/wf-plans/` — the binding constraint stated in `README.md` of this folder. The schema-and-code walk that backs most of these rows was worked in a gitignored tree; its substance is carried **in full** into `../04_state-category-inventory.md` §4 and the pointer is dropped.

---

## `OUT-2` — Every category of state appears exactly once, with an argued completeness method

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-2` | **The inventory exists as a single published document** in which every category of state the system holds today or will hold appears exactly once, each entry carrying store, lifecycle, volatility, derived flag, learner-scoped question and status. **45 entries: 30 `existing`, 11 `required-by-upstream`, 4 `assumed`.** | `../04_state-category-inventory.md` §3, §8 | Structural — verifiable by counting the entry table against `../04_state-category-inventory.md` §8 | **confirmed** | A count is not a proof of exhaustiveness. The argument for exhaustiveness is §7 and is falsifiable by §7.3; the count only proves internal arithmetic consistency. |
| `OUT-2` | **"Appears exactly once" is a checkable property, not a claim of taste**, because what makes two pieces of state one category is decided by a published rule with five rejected alternatives rather than asserted inline. | `../decision-records/DR-C10-S3-1_state-category-individuation.md` | Structural — a decision record with all six required sections, verifiable against `../decision-records/README.md` | **confirmed** | The rule has a wrong answer and names it: `SUB-13 (NEU-977)`'s exactly-one audit reporting a row that needs two authorities is a recorded under-split. That test has not run — it cannot, until `SUB-13` exists. |
| `OUT-2` | **Every persisted table resolves to exactly one entry — zero missing, zero duplicated.** 14 tables (10 `public`, 2 Drizzle-defined `infrastructure`, 2 raw-SQL `infrastructure`) → 17 entries, the three splits each justified. | `../04_state-category-inventory.md` §4.1 | **Automated check** over `src/` and `drizzle/` at the 2026-08-21 cutoff (`../00_method-and-provenance.md` §1.1), re-counted at this sub-task's own cutoff per §6 and printed as a per-item disposition table | **confirmed** (2026-08-21) | The check proves what the schema contained at one moment. A table added later is falsifier 1 in `../04_state-category-inventory.md` §7.3. |
| `OUT-2` | **All process-local in-memory state is a first-class entry, not a footnote** — eleven structures across ten entries, with all three merges stated so the exactly-once property stays auditable. | `../04_state-category-inventory.md` §3.4, §4.3 | **Automated check** over `src/` at the 2026-08-21 cutoff | **confirmed** (2026-08-21) | Process-local state is what makes the current deployment single-instance. Whether that is acceptable is **not** decided here — `SUB-10 (NEU-984)` and `SUB-4 (NEU-974)`. |
| `OUT-2` | **The charter's own enumeration of process-local state was incomplete, and the omission probe caught it.** The charter names five structures; the walk found **eleven**. The six extras are `SC-S3-23`…`SC-S3-27`. | `../04_state-category-inventory.md` §7.2 | **Automated check** — an independent walk run from the charter's list alone, then diffed against the source tree | **confirmed** (2026-08-21) | An inventory built from the charter's list would have been ~45 % short on process-local state **while appearing complete**. The completeness method is revised in place to forbid the sweep that produced the miss. |
| `OUT-2` | **Derived-never-persisted state is inventoried as state** — `masteryLevel`, `LearnerContext` and the analytics KPIs — each with the file that computes it cited and the absence of a backing column asserted against the schema walk. | `../04_state-category-inventory.md` §3.5, §4.4 | **Automated check** — each checked against the §4.1 column list for a backing column of the same name; none exists | **confirmed** (2026-08-21) | These have no store, so no store-keyed audit will ever see them. They are carried because `SUB-13` must still say which component may compute and serve them. |
| `OUT-2` | **Four upstream packages are cross-checked, each cited by version or compilation date**, and every state category they name resolves to an entry. | `../04_state-category-inventory.md` §5.1–§5.4 | Citation of **consumed** upstream decisions — `../C005-product-foundation/README.md:3` (NEU-887, dated 2026-07-12); `../C005-instructional-model/README.md:3` (NEU-888, cutoff 2026-07-13); `../C005-dp-map-package/README.md:3` (NEU-889, **v1.0.0**, compiled 2026-07-16); `../C009-course-content-quality/README.md:3` (NEU-890, compiled 2026-08-10) | **consumed** | Each entry is only as fresh as the package cited. A later revision of any of the four that names a new category is falsifier 4 in `../04_state-category-inventory.md` §7.3. |
| `OUT-2` | **Two upstream cross-check results are negative, and the negatives are recorded rather than left to inference**: NEU-887's seven-class evidence taxonomy introduces **no** runtime state category, and NEU-888 names **no** misconception / **learner schema** state (zero matches for either term across its package). | `../04_state-category-inventory.md` §5.1, §5.2 | **Automated check** — term searches over `../C005-product-foundation/` and `../C005-instructional-model/` at the 2026-08-21 cutoff | **confirmed** (2026-08-21) | A negative result is the easiest thing for a later reader to mistake for an omission. Both are stated in the cross-check tables so that re-deriving them is unnecessary. |
| `OUT-2` | **The `EQ-S3-*` ids a merged sibling referenced are reconciled with their true owner named**, and are **not** carried as entry ids. They are `NEU-890`'s requirement rows, where the `S3` segment denotes **C009's** sub-task 3 — not this charter's SUB-3. A cross-walk maps each to the entry holding its state. | `../04_state-category-inventory.md` §5.5; `../02_findings-register.md` `### SUB-3` (`F-S3-1`) | Citation of `../C009-course-content-quality/09_enforceable-quality-system.md:190`–`:193`, `:197` (NEU-890, compiled 2026-08-10), against this package's five declared id families in `../README.md` | **confirmed** | `EQ-S3-6` maps to **no** entry by design — NEU-890 specifies that nothing from the page is stored. The namespace collision itself is live and is `F-S3-1`'s hazard, handed to `NEU-985 (SUB-11)`. |
| `OUT-2` | **No entry describes a learner-ownership column as present.** No `user_id`/`userId`/`learner_id`/`learnerId` column exists on any of the 12 Drizzle-defined tables or either raw-SQL log table; learner scoping is recorded as a **question per entry**. | `../04_state-category-inventory.md` §6 | **Automated check** over `src/infrastructure/db/schema.ts` at the 2026-08-21 cutoff — **zero matches** | **confirmed** (2026-08-21) | `NEU-850's OUT-2` is **consumed**, not re-decided here: learner ownership is committed to the MCP core database schema. The only identity-bearing state that exists today is process-local (`SC-S3-19`, `SC-S3-20`), and `SC-S3-45` carries the mapping as `assumed` under `A-28`. |
| `OUT-2` | **Every `assumed` entry names its stand-in in the entry itself**, and no assumed entry is marked `existing` or `required-by-upstream`. `A-26` introduces no category, and that one-to-none mapping is stated rather than left as an apparent gap. | `../04_state-category-inventory.md` §3.7 | Structural, against `../93_stand-in-assumption-register.md` `A-25`…`A-29` and `../00_method-and-provenance.md` §2.3 | **confirmed** | All five stand-ins are `[unconfirmed]` and inherit their register's re-validation triggers. Four entries rest on them; when a stand-in's package lands, its entry is re-derived, not merely re-labelled. |
| `OUT-2` | **Completeness is argued and falsifiable.** The method states the three sweeps that claim exhaustiveness, the revision the omission probe forced, and **six named falsifiers** — plus one named non-falsifier. | `../04_state-category-inventory.md` §7.1, §7.3 | Structural — the falsifiers are published in advance and each is mechanically runnable | **confirmed** | Falsifier 6 (`SUB-13`'s matrix corresponding row-for-row) cannot be run until `SUB-13` publishes. `NEU-985 (SUB-11)` owns running it and reporting counts. |
| `OUT-2` | **An unmet upstream requirement was found by the cross-check and is filed rather than absorbed**: both operational log tables hold learner payload with no retention window, no named deletion owner and no principal field, against NEU-887's operational-log privacy gate. | `../04_state-category-inventory.md` §5.1, §3.3; `../02_findings-register.md` `### SUB-3` (`F-S3-3`) | Citation of `../C005-product-foundation/measurement-contracts/05_operational-log-privacy-gate.md:19`–`:23`, `:37`, `:54` (NEU-887, dated 2026-07-12), plus an **automated check** over `drizzle/` and `src/transport/` | **confirmed** (2026-08-21) | This document **files** the gap; it does not fix it, design a retention column, or name the deletion owner. Owners are `SUB-13 (NEU-977)` for authority and `SUB-14 (NEU-978)` for the isolation consequence. |
| `OUT-2` | **The append-only register discipline held.** This sub-task added a `### SUB-3` section to three shared registers and modified no sibling entry; `93_…` and `94_…` were not opened. | `../02_findings-register.md`, `../90_open-items-and-provisional-register.md`, `../91_caps-and-incomplete-scope.md`, each `### SUB-3` | **Automated check** — `git diff --numstat` against `origin/develop`, **zero deletions on every touched file** | **confirmed** (2026-08-21) | The check proves no line was removed in *this* change. Whether the discipline survives all sixteen sub-tasks is `NEU-985 (SUB-11)`'s count, not this row's. |
| `OUT-2` | **No spike was run and none is filed**, because every claim in this inventory could have been read instead — `92_…` §3's own justification test. | `../04_state-category-inventory.md` §10 | Structural, against `../92_spike-register.md` §2–§3 | **confirmed** | `92_…` carries no `### SUB-3` section. That is the three-way rule resolving to "read it", not an omission. |

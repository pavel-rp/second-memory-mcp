# `S13` — Traceability: the per-state authority matrix and its two audits

**Written by:** NEU-977 (SUB-13), for its own coverage · **Charter:** C010 (umbrella NEU-895)
**Written:** 2026-08-21
**Model:** claude-opus-5[1m]
**Covers:** `OUT-3`, and `OUT-9`'s second half — see `../01_outcome-register.md`.

The audit rows over every outcome are `NEU-985 (SUB-11)`'s; this file documents **SUB-13's own**
coverage and nobody else's. Every row below resolves into `docs/research/` or a real `src/` path with a
line. **Zero rows resolve into `_local/` or `docs/wf-plans/`.**

Evidence classes are those of `../00_method-and-provenance.md` §1.1; statuses are the three labels of
§1.2 (`confirmed` / `[unconfirmed]` / `consumed`). **A green type-check or lint line is not evidence
about this package's content** and appears in no row below — this sub-task touches zero source files, so
the build gates say nothing whatever about it.

**This is the pre-validation revision.** Several residuals below name SUB-14 as the party that would
convert an `[unconfirmed]` into something stronger. That is the intended shape, not a gap in coverage:
`OUT-3` splits the assignment from its validation across two sub-tasks, and this file documents the first
half only.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| `OUT-3` | **Every one of the 45 state categories carries exactly one authority**, and the authority is a `CMP-S4-*` component id — never a role name, never a zone alone. | `../08_…md` §8 (45 `####` blocks), §9 (the summary table), §10.1 | Mechanical parse of the published chapter — each block's id, its `**Authority: …**` marker and its clause citation — reported as counts, not as a reading | `confirmed` | The audit establishes that there is exactly one authority per row and that each is a real component id. **It does not establish that the authority is the *right* one** — §10.3 states this explicitly. That is `SUB-14 (NEU-978)`'s. |
| `OUT-3` | **Every `OUT-2` row appears in the matrix and every matrix row appears in `OUT-2`**, checked in **both** directions with both counts published. | `../08_…md` §10.2 | Set comparison between `../04_…md` §3's id set and the chapter's block ids, run in both directions | `confirmed` | Zero unmatched in either direction, so **no item was routed** — the routing rules are published anyway so the audit is reproducible rather than merely reported. A one-directional check would have passed on a matrix that invented a category, which is why both are run. |
| `OUT-3` | **All nine `OUT-3` attributes are populated for every category** — reads, writes, consistency, freshness, concurrency, conflict handling, recovery, migration path, observability — for **450 authored cells**. | `../08_…md` §7 (the attribute definitions), §8 (the per-row tables) | Structural, verifiable by file inspection: one nine-row attribute table per `####` block | `confirmed` | A cell that cannot be answered from merged inputs **says so and cites the finding or item that owns the gap**; none is blank and none is guessed. The proportion of such cells is highest in §8.6 and §8.7, where the categories do not exist yet — which is a property of the inventory, not of the coverage. |
| `OUT-3` | **Every row publishes the clause of `../07_…md` §6.1 that produced it**, so the assignment is reproducible rather than curated. | `../08_…md` §5 (the rule restated), §8 (per-row clause citations), §11 (the clause distribution) | Structural; the clause distribution sums to 45 and is derived by the same parse as §10.1 | `confirmed` | Reproducibility is claimed for the *derivation*, not for the *rule*. Where the rule under-determined a row, the tie-break used and the reading it rests on are disclosed at the row — `F-S13-1`, `F-S13-2`. |
| `OUT-3` | **Clause 2's `../05_…md` lookup is written out as a by-id membership test**, so the one clause whose input is not readable off the row's own cells is not a judgement call. | `../08_…md` §6 | Enumeration of the four places `../05_…md` names state categories by `SC-S3-*` id (§3.2's "Demanded by", §5's flows, §7.3's disposition, §8's placements), with the resulting three-row match set stated | `confirmed` | Two boundary readings are recorded rather than assumed: **indirect consumption is not membership** (`SC-S3-17` via `SC-S3-21`), and **a row a gate writes is not thereby gate-bearing** (`SC-S3-4`). Both are disclosed so SUB-14 can reject them; neither changes an authority. |
| `OUT-3` | **The rule's application is cross-checked against an independently-taken census.** `../06_…md` §3.3 counts 26 in-domain categories; subtracting the 5 taken first by clause 1 and the 1 by clause 4 leaves exactly the 20 rows that reach clause 5. | `../08_…md` §11.1; `../06_isolation-invariant-and-the-neu-893-split.md` §3.3 | Arithmetic reconciliation between two counts produced by different sub-tasks from different directions | `confirmed` | This is the strongest single piece of evidence that the rule was applied consistently across all 45 rows — a per-row misapplication of clause 5 would break the reconciliation. It does **not** check clauses 2, 3 or 6 against anything external. |
| `OUT-3` · `OUT-9` | **The gate-verdict record, the drift-verdict store and the drift-verdict cache each have their own row with exactly one authority**, plus a freshness requirement and quarantine-on-stale behaviour. `OI-S2-2` closes. | `../08_…md` §8.6 (`SC-S3-33`, `SC-S3-34`, `SC-S3-35`), §12.1; `../90_…md` `### SUB-13` (Disposition of `OI-S2-2`) | Citation of **consumed** upstream specifications (`../03_…md` §3.5, §4.2–§4.4) placed against `../05_…md` §5 `FL-S4-13`, §7.3 | **consumed** (the specifications) · **confirmed** (the assignment, by file inspection) | Two of the three required a disclosed tie-break — `F-S13-1`, routed to `SUB-6 (NEU-976)`. The item's closure condition is met either way, since it asks for exactly one authority per category and not for an undisputed derivation. |
| `OUT-3` | **The `SC-S3-37` re-import is attributed**: it is `CMP-S4-7`'s write, executed through `CMP-S4-13`, and a refresh by anything else is a second writer. `OI-S4-1` closes. | `../08_…md` §8.6 (`SC-S3-37`), §12.1; `../90_…md` `### SUB-13` (Disposition of `OI-S4-1`) | Reasoned from `../05_…md` §8.1 (imported by `CMP-S4-13`, one in-system copy held, authority explicitly left to SUB-13) and `../07_…md` §6.2's own worked demonstration | `confirmed` | Upstream **NEU-889 remains authoritative for the graph itself** (`FL-S4-21`); this row governs the imported copy only. The import *mechanism*, *store shape* and *refresh schedule* are SUB-10's and SUB-8's and are not decided here. |
| `OUT-3` | **`A-25`, `A-27`, `A-28` and `A-29` are each cited at the assignment that rests on them**, with tolerance envelope and invalidating outcome named in the sentence rather than in an appendix. | `../08_…md` §8.7 (`SC-S3-42`, `SC-S3-43`, `SC-S3-44`, `SC-S3-45`); `DR-C10-S13-1` Rationale | Citation of `../93_stand-in-assumption-register.md`, whose labels are literal and load-bearing | `[unconfirmed]` (the stand-ins themselves, per the register) · `confirmed` (the citation discipline, by file inspection) | All four stand-ins are `[unconfirmed]` by the register's own contract, so the four assignments resting on them inherit that status. `93_…md` is **closed at five entries** and this sub-task adds no sixth. `A-26` introduces no category. |
| `OUT-3` | **No category was left without an authority, split between two, or deferred.** All 45 took one; the two under-determined rows were resolved by the rule's own tie-breaks and routed as findings. | `../08_…md` §10.1, §11.3, §12.4 | Mechanical (the audit counts) plus structural (the findings register's `### SUB-13`) | `confirmed` | The findings **accompany** assignments rather than substituting for them. `F-S13-1`'s narrow reading of tie-break (b) is the one step in this chapter that is assumed rather than derived, and it is stated as such at the row, in the finding, and in `DR-C10-S13-1`. |
| `OUT-3` | **The matrix carries a pre-validation revision marker** naming who validates it, who supersedes it, and which sub-tasks must wait for the superseding revision. | `../08_…md` §2, §13 | Structural, verifiable by file inspection | `confirmed` | A consumer reading this revision early is not forbidden — it is the only assignment that exists — but owes itself a re-check when SUB-16 lands. Nothing enforces that; the marker is the whole mechanism. |
| `OUT-3` | **`CAP-S4-1` is not closed**, and the matrix distinguishes deletion ownership from write authority rather than conflating them. | `../08_…md` §8.3 (the section preamble, `SC-S3-16`, `SC-S3-17`), §8.6 (`SC-S3-41`), §12.3; `../91_…md` `### SUB-13` | Citation of `../05_…md` §9.2's structural-obstruction finding and `../91_…md`'s existing entry | `confirmed` | Both log rows take a write authority (`CMP-S4-9`); neither takes a deletion owner, and neither can until a principal field lands or `SC-S3-45` gets a store. `F-S3-3` is cited at both rows and not re-filed. |
| `OUT-3` | **No row describes an ownership column as present.** The `Learner-scoped` value is carried forward from `../04_…md` unchanged, as the question that document recorded. | `../08_…md` §1 (the closing bullet), §8.7 (`SC-S3-45`), §12.2 | Reasoned from `../04_…md` §2 (the column's definition) and §6 (the four-term search returning zero matches across all twelve Drizzle tables and both raw-SQL log tables) | `confirmed` | `OI-S3-1` is therefore **half-discharged and stays open**, and its closure condition is contested as unsatisfiable — `F-S13-3`, routed to `NEU-986 (SUB-12)`. Asserting the values would have closed the item by writing a false schema fact. |
| `OUT-3` | **The sub-task is written as `NEU-977` throughout.** The one occurrence of `NEU-987` names it as a known merged typo. | `../08_…md` §12.1, §14 | Grep across every file this sub-task adds or appends to, for **both** ids | `confirmed` | `F-S3-2` records that a merged sibling cites SUB-13 as `NEU-987` in 13 places across 6 files, including `OI-S2-2`'s own owner field. Those are **not** corrected — the registers are append-only and a sibling's merged entries are not this sub-task's to rewrite. The correction is the citation, not an edit. |

---

## Coverage note — what this file does not claim

**`OUT-3` is not fully discharged by this sub-task, and this file does not pretend otherwise.**
`OUT-3` names both the assignment and its validation; `../08_…md` is the assignment. The following are
stated as gaps deliberately rather than omitted:

| Not discharged here | Owner | Why not here |
| --- | --- | --- |
| Applying the isolation invariant row-by-row over the matrix | `SUB-14 (NEU-978)`, per `OI-S5-3` | `../06_…md` §3.6 works five hand-picked categories and §3.7 states that this is a termination demonstration, not a census. The census needs the matrix, which now exists. |
| The four scenario walks and the `../05_…md` flow cross-check against the matrix | `SUB-14 (NEU-978)` | Validation, by the charter's own split. |
| Dispositioning SUB-14's findings and republishing | SUB-16 | `../08_…md` §2. That revision supersedes this one. |
| The resource-inventory ↔ matrix cross-check under `OUT-5` | `SUB-7 (NEU-980)` | A **different** audit against a **different** inventory, which does not exist yet. Named distinctly in `../08_…md` §10.2 so the two are not conflated. |
| The data-store destination for the 18 store-`none` categories | `SUB-10 (NEU-984)`, per `OI-S13-1` | `OUT-8`'s, not `OUT-3`'s. The matrix records the shape and the constraints, never the destination. |
| Whether `OI-S6-1`'s store reversal fires | `SUB-6 (NEU-976)`, evaluated by `SUB-10` | This matrix is written against `M-A` as selected. `../08_…md` §11 publishes the blast radius precisely: one row, `SC-S3-43`. |

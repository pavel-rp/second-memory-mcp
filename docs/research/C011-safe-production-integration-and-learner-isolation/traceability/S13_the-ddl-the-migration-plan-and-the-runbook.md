# `S13` — The DDL, the migration plan, and the runbook

**Task:** NEU-1006 (SUB-13) · **Charter:** C011 (umbrella NEU-893) · **Covers:** OUT-19
**Written:** 2026-08-26 · **Verification cutoff:** `fd05ca1`, 2026-08-26
**Model:** claude-opus-5[1m]

Every row resolves into `docs/research/`. **A green type-check or lint line is not evidence about
this package's content**, and none is cited as such below. In particular, the four repository gates
this sub-task ran are evidence that the repository still builds — they are **not** evidence that any
SQL statement in the chapter is correct, and no row below cites them.

---

## OUT-19 — The DDL, the migration plan and the runbook, at executable fidelity, applied nowhere

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| OUT-19 | The **schema DDL** is complete `CREATE`/`ALTER` text for every object the package requires: the ownership key on ten tables, both attribution carriers, the partial unique index, the consent record and an RLS appendix. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2 | Authored artifact against cited upstream decisions | confirmed | **Not one statement has been executed.** No production credential exists (`F-S1-2`), so the DDL is reviewed text. `R-S13-3`. |
| OUT-19 | `NEU-850`'s `OUT-2` is **cited by the DDL** at a resolving path, and the column is `user_id` verbatim from it. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.1 | Consumed upstream decision, quoted at `../../C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md:50`–`:53` | consumed | It is *"converged but unimplemented"* and *"never an existing schema fact"* (`:65`–`:66`); realizing it is a decision to honour, not a fact to report. |
| OUT-19 | The **value** the key carries is SUB-5's widening of the literal "JWT subject" to *"the resolved principal identifier"*, so the key is well-defined on STDIO. Carried as SUB-5's, not introduced here. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.1; `../05_the-enforcement-point-that-confines-every-read-and-write.md` §8.1 `C1` | Consumed upstream widening | consumed | The STDIO principal itself has no configuration surface anywhere (`OI-S4-1`), so the value is well-defined and unsupplied. |
| OUT-19 | SUB-5's `holds` derivation for `SC-S3-12` is **re-verified against the DDL as written**, requirement by requirement. **No divergence on `notes`.** | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.6 | Row-by-row check of `DR-C11-S5-2`'s `C1` and `C5` against §2.1, §3.5 and §3.6 | confirmed | The check is against the DDL **as text**; it establishes that the artifact matches the derivation, not that either works. `CAP-S5-1` is unchanged and remains **not lifted**. |
| OUT-19 | **Four divergences are found and routed, not absorbed.** SUB-5 says `session_chunks` and the three `session_questions` children carry no key of their own; SUB-6's disposition and SUB-7's two exit conditions say it does. The DDL follows SUB-6/SUB-7. | `F-S13-1`; `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.6; `DR-C11-S13-1` rejected alternative 6 | Direct contradiction between two merged siblings, both quoted | confirmed | **Unresolved by design.** SUB-5 (NEU-997) owns the resolution; this chapter states which side it built on and why. |
| OUT-19 | All **three principal states** are representable, and the `iff` rule is a database `CHECK` on both log tables. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.2; `DR-C11-S13-1` clauses 1–2 | Authored constraint against `DR-C11-S16-1` decisions 1 and 4 | confirmed | The biconditional is total only because `principal_kind` is `NOT NULL`; if a later change makes it nullable the constraint silently admits a third case. Stated in `DR-C11-S13-1`. |
| OUT-19 | `F-S5-6`'s two `principal_kind` domains are **reconciled rather than collapsed** — two different `CHECK`s, consistent by construction, on SUB-5's own stated reconciliation. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.2; `DR-C11-S13-1` clause 1 | Consumed reconciliation at `../05_the-enforcement-point-that-confines-every-read-and-write.md:266`–`:276` | consumed | — |
| OUT-19 | `R-S4-1` is closed **structurally on the token row** by a generated `learner_key` column, which is what SUB-5 handed forward. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §2.2; `DR-C11-S13-1` clause 3 | Authored schema object against `../05_the-enforcement-point-that-confines-every-read-and-write.md:1436` | confirmed | **Not closed on the ten owned tables**, where no `principal_kind` sits beside `user_id` and no constraint can check the writer's kind. `R-S13-4`. Also requires PostgreSQL ≥ 12 (`SPK-S13-1`). |
| OUT-19 | The **migration plan** carries per-sweep SQL for `S1`–`S5`, batched, idempotent and resumable, with the four-wave intra-`S4` order made **self-enforcing** by an `IS NOT NULL` guard on each parent. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §3.4, §3.5, §3.6; `DR-C11-S13-2` | Authored artifact against `../06_the-disposition-of-every-unowned-row.md` §3, §9.2, §9.3 | confirmed | Nothing executed. The self-enforcement is a property of the SQL as written; it has not been run against any dataset, synthetic or real. |
| OUT-19 | `R-S6-2`'s batched-idempotent-resumable obligation is discharged **as a design**, with the resume cursor being the sweep's own target predicate and no separate progress ledger. | `DR-C11-S13-2` clauses 1–2 | Authored contract against `../92_risk-register.md` § `R-S6-2` and `DR-C11-S7-2` clause 5 | confirmed | The residual `R-S6-2` names — several short breaches are still breaches — is **unchanged** and still escalates to `NEU-896`. |
| OUT-19 | The batch is bounded by a **wall clock**, not a row count, so the per-boot cost is bounded without the counts `OI-S6-1` records as never taken. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §3.3; `DR-C11-S13-2` clauses 3–5 | Derivation about the form of the bound | confirmed | **`CAP-S7-1` is not lifted.** Total completion still scales with the unknown row count; the unknown moves from outage length to migration length. `R-S13-1`. |
| OUT-19 | The two slice defaults are **registered stand-ins**, not derivations, with an owner and a re-validation trigger. | `A-S13-1`; `../13_the-ddl-the-migration-plan-and-the-runbook.md` §3.3 | Stand-in, argued for shape and not for value | `[unconfirmed]` | Re-validated only by `SPK-S6-2` (the counts) or `SPK-S15-1` (the restart duration). Neither has run. |
| OUT-19 | **Every pre-flight predicate limb is independently re-verified** against the codebase at this cutoff, with `file:line` evidence, and the predicate is forwarded **unchanged**. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §3.7 | Codebase read: `src/domain/types/spaced-repetition-tools.ts:63`, `:65`, `:67`–`:70`, `:102`; `src/domain/types/session.ts:147`; `src/domain/types/recommendations.ts:78`; `src/shared/constants/validation.ts:6`–`:7`; `src/domain/algorithms/sr-calculator.ts:191`; `src/config/resolve-algorithm-config.ts:12`–`:14`; `src/domain/config/algorithm-defaults.ts:7`; `src/domain/config/algorithm.ts:72`–`:76`; `src/infrastructure/db/schema.ts:58`, `:60`, `:61`, `:62`, `:65` | confirmed | The probe detects the pathologies it names and nothing else; `F-S6-2` records that mis-ownership is undetectable by any aggregate, and that is unchanged. |
| OUT-19 | The `operation_event_log` probe `F-S6-6` names as missing is **written**. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §3.7 (`P-ENC-3`) | Authored query against `drizzle/0013_create_operation_event_log.sql:1` | confirmed | **Six of `F-S6-6`'s seven unprobed tables remain unprobed** and are enumerated by name so the residual is visible. |
| OUT-19 | The **runbook** carries one section per stage `T0`–`T9`, each with entry, apply, verify, contain and reverse. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §4.1 | Authored artifact against `DR-C11-S7-1` and `../07_the-rollout-sequence-and-what-each-stage-cannot-undo.md` §6, §8, §9 | confirmed | Not walked. No stage has been executed and no reversal exercised. |
| OUT-19 | Every stage's containment is **separately executable from its reversal**, as OUT-4 requires, or carries SUB-7's named exception with its owner. **Zero blanks.** | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §4.1, §5 | Enumeration against `../07_the-rollout-sequence-and-what-each-stage-cannot-undo.md:445`–`:459` | confirmed | **None of the six controls exists.** Every "off" position is a specification until the implementation charter builds it. `R4`'s residual, unchanged. |
| OUT-19 | The **control surface is published** — six variables, their values, defaults, per-control safe position and precedence — firing `DR-C11-S7-2`'s own revision trigger. | `DR-C11-S13-3`; `../13_the-ddl-the-migration-plan-and-the-runbook.md` §5 | Authored specification against `DR-C11-S7-2` Revision trigger | confirmed | Specified, never demonstrated. Applying a toggle over SSH to the off-repo compose stack is **unprecedented in this repository** and needs a capability one person has (`R-S13-2`). |
| OUT-19 | The `CLASSIFIER_ENABLE` precedent supplies the **toggle shape only**; its own emergency path requires a deploy. Re-read at this cutoff rather than inherited. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §5; `DR-C11-S13-3` Rationale | Codebase read: `src/config/resolve-classifier-config.ts:22`–`:62`; `src/composition-root.ts:379`; `docs/runbooks/classifier-blocking-activation.md:261`–`:262` | confirmed | **No disable path in this chapter is restart-free**, and the restart re-runs the migrator (`F-S7-2`). |
| OUT-19 | Under STDIO **no audit and no event row exists**, so every verification step carries a STDIO limb or says *"not observable on this transport"*. Re-verified by enumerating every call site. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §4.0, §4.1 | Codebase read: `src/transport/http.ts:179`–`:181` is the sole construction site; `src/shared/logger.ts:170`, `:193`, `:220` define the three functions; `src/transport/main.ts:55`–`:59` is the STDIO limb and calls none | confirmed | This is `F-S7-9`, confirmed independently, not restated on SUB-7's authority. The STDIO substitute is stderr, which is unretained and needs the same SSH capability. |
| OUT-19 | The **repository audit** proves zero changes to `src/`, `drizzle/` and every deployment configuration file. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §0, §6; `G-S13-6` | `git diff --name-only origin/develop`, read path by path | confirmed | Establishes only that nothing was changed; it says nothing about whether the artifacts are correct. |
| OUT-19 | The **citation check** covers the shorthand blind spot and the `MISSING-target` bucket, not just the summary line. | `../13_the-ddl-the-migration-plan-and-the-runbook.md` §6 disclosure 2; `G-S13-7` | Checker run by hand plus a direct grep for `…`/`...` targets and an entry-by-entry read of the excluded bucket | confirmed | C011 is **not** in the gated list (`scripts/check-citation-paths.ts:21`) — `CAP-S1-2`, SUB-14's — so CI does not catch a C011 citation break, and the gate applies retroactively once SUB-14 lands it. |
| OUT-19 | OUT-19's **outcome-register row**, with its resolving evidence and its **success measure**, is authored here. | `../90_outcome-register.md` § OUT-19 | Authored content | confirmed | SUB-14 aggregates it and authors none of it. **No charter § Risks row names OUT-19** (charter assumption 48), so no `R<n>` entry is authored — four `R-S13-*` entries are. |

---

## What this file does not establish

- **That anything in the chapter works.** Every row above is about an authored artifact. No SQL was
  executed, no migration applied, no stage walked, no reversal exercised and no control built. The
  distinction between *"the artifact says X"* and *"X is true of the running system"* is load-bearing
  in every row and is why the Evidence class column never reads `observed-in-production`.
- **That any stage fits `OBJ-8`.** `CAP-S7-1` is unchanged; §3.3 re-shapes the residual and does not
  remove it.
- **That the RLS appendix is adoptable.** It is published with two unmet preconditions —
  `OI-S5-1`'s unpriced transaction cost, and the role question `F-S13-2` raises and `OI-S13-2`
  carries.
- **That `F-S9-6` is resolved.** The chapter writes no retention statement precisely so it cannot
  resolve it by accident.
- **That the consent table has a stage.** `F-S13-3` states the gap; `OI-S13-1` carries the placement,
  owned by SUB-7's owner.
- **That `CAP-S5-1` is lifted.** SUB-5 discharged it and did not lift it; realizing the DDL as text
  does not lift it either, because the cap's lifting condition is a *category evaluating to `holds`*
  against an applied schema.

# S5 — The enforcement point that confines every read and write

**Sub-task:** SUB-5 (NEU-997) · **Covers:** OUT-8
**Written:** 2026-08-25 · **Model:** claude-opus-5[1m]
**Codebase cutoff:** `origin/develop` @ `cc38cc9`

Every row's **Discharged by** column resolves into `docs/research/`, never into `_local/`. Evidence
columns additionally cite source and test paths, written bare from any depth. `src/…` and
`drizzle/…` are the excluded class `../README.md` § "Citations" names explicitly; `tests/…` and
`scripts/…` are cited on the same repo-root-relative footing, which the checker treats as the same
out-of-scope class (`scripts/citation-paths/checker.ts:245`) though the package README does not yet
enumerate them. That README extension is routed to SUB-14 (NEU-1007) rather than assumed here.
Chapter references are to `../05_the-enforcement-point-that-confines-every-read-and-write.md`.

---

## OUT-8 — Where the confinement is mechanically implemented, at or below the port boundary

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| OUT-8 | The enforcement point is the Drizzle adapter, with the principal bound at construction as an indivisible `(principal_id, principal_kind)` pair, adapters constructed per request, and the database as an independent second layer | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §2; `../decision-records/DR-C11-S5-1_the-enforcement-point.md` | Design derivation over the consumed rules (`DR-C10-S5-1` `I3`/`I5`; `A-28`; `DR-C11-S2-2`; `DR-C11-S4-2`) | `confirmed` | Nothing is implemented; the RLS second layer's transaction cost is `OI-S5-1` |
| OUT-8 | The enforcement point is placed at the adapter rather than the transport gate, because the gate is HTTP-only, fails open on internal error, and sits above the port boundary | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §2 | Repository read — `src/transport/http.ts:184`–`:187`; `src/transport/main.ts:55`–`:59`; `src/transport/context-token-middleware.ts:83`–`:86` | `confirmed` | — |
| OUT-8 | All 13 ports in `src/ports/` carry an enforcement point or a justified exclusion; zero unaddressed | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §3 | Repository read — all 13 files in `src/ports/`; adapters in `src/adapters/drizzle/` | `confirmed` | One port is named as not confinable by this mechanism and routed (`Tier2BlockingStatsRepository`, `F-S5-9`); two are excluded as owning no rows and named as an egress surface (`F-S5-2`) |
| OUT-8 | Only **7** of the 13 ports are row-owning, not the 9 the charter states: `Tier2BlockingStatsRepository` owns no rows and `ReviewPersistencePort` owns no table, writing instead into `learning_chunks` | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §1.1; `F-S5-1` | Repository read — `src/adapters/drizzle/tier2-blocking-stats-repository.ts:32`–`:47`; `src/ports/tier2-blocking-stats-repository.ts:10`–`:11`; `src/adapters/drizzle/review-persistence-adapter.ts:78`–`:82` | `confirmed` | Charter assumption 23 diverges; routed to SUB-14 for reconciliation |
| OUT-8 | `EmbeddingPort` and `ContentClassifierPort` are excluded because they own no persistent state — **not** because they are pure-compute; they are outbound-network adapters and therefore a data-egress path | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §1.1, §6.1; `F-S5-2` | Repository read — `src/composition-root.ts:130`–`:131`; `src/adapters/langchain/` | `confirmed` | The egress is a data-protection surface routed to SUB-8 (OUT-11) and `OI-S3-1`, not an isolation failure |
| OUT-8 | `AppContext` carries **57** subject-less members at `src/composition-root.ts:518`–`:636`, split 53 closures + 2 shorthand references + 1 port handle + 1 scalar — not the charter's 56 / 52 | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §1.2, §5; `F-S5-3` | Repository read — `src/composition-root.ts:518`–`:636`, member by member; `git log` shows the file last changed 2026-08-04 at `aa56c05`, before the charter was written | `confirmed` | Charter assumption 13 diverges; a miscount rather than drift; routed to SUB-14 |
| OUT-8 | Each of the 53 closures obtains a principal by inheriting one from the principal-scoped port instances its `deps` object closes over; the change set touching them is empty | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §5.1 | Repository read — `src/composition-root.ts:480`–`:516` (`deps` assembly), `:520`, `:575` (representative bindings) | `confirmed` | — |
| OUT-8 | All four non-closures are classified by name and none is asked a closure question: the `contextTokens` port handle (`:603`), the `contextTokenTtlMs` scalar (`:604`), and the two shorthand references to pure imported functions `mapChunkRowToLearningItem` (`:608`) and `applyBatchSessionChunkOperations` (`:627`) | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §5.2 | Repository read — `src/composition-root.ts:603`, `:604`, `:608`, `:627`; imports at `:92`, `:102`; interface declarations at `:285`–`:286` | `confirmed` | — |
| OUT-8 | Clause 4's request-scoped construction resolves C010's `F-S5-1` — one frozen `AppContext` shared by every MCP session, carrying no principal field | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §2 | Consumed — `../../C010-system-and-repository-architecture/02_findings-register.md:226`; repository read `src/composition-root.ts:638` | `consumed` | Resolved in design only; nothing is applied |
| OUT-8 | `getActiveSession()` is removed rather than shadowed — the owner predicate is conjoined inside the adapter method, so no SQL statement in `src/` selects `learning_sessions` without an owner predicate | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §4.1 | Repository read — `src/adapters/drizzle/session-repository.ts:73`–`:80`; seven call sites at `src/orchestration/session-workflows.ts:39`, `:185`, `src/orchestration/teaching-workflows.ts:157`, `:860`, `:930`, `:1901`, `src/orchestration/learner-context-workflows.ts:99`; two further indirect sites at `src/orchestration/teaching-workflows.ts:1741` and `src/server/session-lifecycle-tools.ts:178` | `confirmed` | Nothing is applied |
| OUT-8 | `createSession`'s global-conflict guard is **deleted** from orchestration and the rule re-expressed as a partial unique index, because scoping the repository read alone would leave the guard adjudicating "any" from above the port boundary | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §4.2 | Consumed — `../../C010-system-and-repository-architecture/02_findings-register.md:237` (`F-S5-2`); repository read `src/orchestration/session-workflows.ts:39`–`:46` | `confirmed` | The index is a DDL requirement stated here and authored by SUB-13 (OUT-19) |
| OUT-8 | The current guard is a time-of-check-to-time-of-use race: two concurrent `createSession` calls both read `null` and both insert | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §4.2; `F-S5-8` | Repository read — `src/orchestration/session-workflows.ts:39`–`:46`, with no atomicity between the read and the insert | `confirmed` | A concurrent integration test is named as required and bound to SUB-13's DDL |
| OUT-8 | A **third** unscoped session path exists that the charter does not name — `listSessions()`, whose sole production caller passes `{ status: 'completed', limit: 1 }` and receives another learner's most recent completed session | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §4.3; `F-S5-4` | Repository read — `src/adapters/drizzle/session-repository.ts:105`–`:118`, predicate applied only at `:111`; sole production call site `src/orchestration/learner-context-workflows.ts:100` | `confirmed` | Charter `R1`'s wording names only `getActiveSession()`; routed to SUB-13 and SUB-7 |
| OUT-8 | The deployed transport contradicts the settled identity rule today: the subject is derived as `sub \|\| azp` | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §1.4; `F-S5-5` | Repository read — `src/transport/jwt-middleware.ts:127`, populated into `res.locals.auth` at `:133`–`:136`; consumed at `src/transport/rate-limit-middleware.ts:76`–`:79` and `src/transport/http.ts:52`–`:72` | `confirmed` | Removal is assumed as **C4** of the composed target state; it is not applied |
| OUT-8 | `principal_kind` has two unreconciled domains in the package — two-valued on `context_tokens`, three-valued on the log tables — and the enforcement point reads the three-valued one | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §2; `F-S5-6` | Consumed — `../04_the-stdio-identity-gate-and-the-bound-context-token.md:248`; `../decision-records/DR-C11-S16-1_the-attribution-carrier.md:14` | `confirmed` | The two `CHECK` constraints are SUB-13's to write consistently |
| OUT-8 | An integration-test design proves subject A cannot read, mutate or delete subject B's rows through any tool path, via a two-principal fixture and a T1–T7 matrix | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §7 | Design derivation over the existing harness — `tests/helpers/db-setup.ts`; `tests/integration/server/persistence-tools.test.ts:12`–`:18`; `src/infrastructure/db/client.ts:78`; `vitest.setup.ts:6`–`:11` | `confirmed` | No test file is written. Four paths are named as uncovered: STDIO, operator/maintenance, the egress pair, and concurrency |
| OUT-8 | `SC-S3-12` (Notes) reaches verdict **`holds`** against a composed target state with five enumerated changes — the invariant's first published positive instance | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §8; `../decision-records/DR-C11-S5-2_the-first-holds-derivation.md` | Design derivation over `DR-C10-S5-1`'s five checks, applied in order | `confirmed` | A verdict against the *proposed mechanism*, not the deployment. Under target state (a) the category is `not-evaluable` |
| OUT-8 | `I3` is answered from a published **enumerated access-path set** of four SQL statements, closed by the module boundary | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §8.3 | Repository read — `src/adapters/drizzle/notes-repository.ts:4`, `:15`–`:25`, `:38`–`:40`, `:54`–`:56`, `:61`; `src/adapters/drizzle/unit-of-work-adapter.ts:17`–`:21`; `src/infrastructure/db/client.ts:78` | `confirmed` | The closure argument is bound to cutoff `cc38cc9`; a new import of the table object breaks it |
| OUT-8 | C010's `I3` clause and its enumerated-access-path-set obligation — which `../../C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md:242`–`:246` records that nobody owed — are settled here | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §8.3, §8.4 | Consumed and discharged — `../../C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md:237`–`:246` | `confirmed` | Settled for one category; the method is reusable, the artifact is not |
| OUT-8 | `CAP-S5-1`'s three preconditions each trace to a settled decision or an already-supplied outcome, with **zero** traced to an artifact that does not yet exist | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §9.1 | Consumed — `../../C010-system-and-repository-architecture/91_caps-and-incomplete-scope.md:186`; `../../C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md:50`–`:53`; `../decision-records/DR-C11-S4-1_the-stdio-identity-gate.md` | `confirmed` | SUB-13's OUT-19 DDL is named only as the later realization that re-verifies |
| OUT-8 | `CAP-S5-1` is **discharged, not lifted**, and its lifting condition is stated as a four-part landing condition on applied work against target state (a) | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §9.2; `../94_caps-and-incomplete-scope.md` § SUB-5 | Consumed — `../../C010-system-and-repository-architecture/91_caps-and-incomplete-scope.md:188`, `:189` | `confirmed` | Co-ownership with `NEU-986` unchanged; the cap is not closed on its owner's behalf |
| OUT-8 | The design sits **inside** `A-28`'s tolerance envelope, under two of the three forms it names, and the invalidating outcome did not fire | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §10 | Consumed — `../../C010-system-and-repository-architecture/93_stand-in-assumption-register.md:111`, `:113` | `confirmed` | No amendment routed to `NEU-895` or `NEU-896`. The finding OUT-8 would require on a breach is checked and **not filed**, because the condition did not arise |
| OUT-8 | The design **moves** the single-learner guard from outside `A-28`'s envelope to inside it, which is why "removed, not shadowed" is an envelope requirement rather than a style preference | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §10 | Consumed — `../../C010-system-and-repository-architecture/02_findings-register.md:237` | `confirmed` | — |
| OUT-8 | Confinement does not cover the sixth copy class `LD-S3-31`: it lives at `_local/scratch/`, outside the database and behind no port, and its confinement is by SUB-1's recorded terms | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §6.2 | Consumed — `../03_learner-data-inventory-and-classification.md:431`–`:455` | `consumed` | Zero members today; the terms exist regardless, and SUB-9 (OUT-12) routes a data right through the class |
| OUT-8 | An aggregate is confined **iff** the predicate applies to the row set before aggregation; returning no rows is not confinement | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §6.3 | Repository read — `src/adapters/drizzle/chunk-repository.ts:216`, `:241`, `:334`; `src/adapters/drizzle/review-persistence-adapter.ts:90`, `:114`–`:115`, `:208`–`:213`, `:283`–`:285`; `src/adapters/drizzle/session-question-repository.ts:251` | `confirmed` | `LD-S3-32` proper does not exist at this position; the rule is stated so SUB-6 inherits it |
| OUT-8 | One aggregate cannot take the rule — `getWeeklyBlockingCounts()` over `infrastructure.operation_event_log`, which has no ownership key — and is named as escaping | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §6.3; `F-S5-9` | Repository read — `src/adapters/drizzle/tier2-blocking-stats-repository.ts:32`–`:47`; consumed `../../C010-system-and-repository-architecture/90_open-items-and-provisional-register.md:202` | `confirmed` | Routed to `CAP-S3-3` / `CAP-S4-1`, owner `NEU-986`, co-named `NEU-896` |
| OUT-8 | Confinement over a mixed population **hides** pre-cutover rows from every principal — data loss by predicate, the inverse of `R-S16-1`'s erasure failure — and the rule does not assume a backfill | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §6.4; `F-S5-10` | Consumed — `../16_attribution-and-detection.md:279`–`:285`; `../92_risk-register.md:326` | `confirmed` | The disposition of the unowned population is SUB-6's (OUT-2); the ordering constraint is SUB-7's (OUT-3) |
| OUT-8 | The enforcement point is a **second, independent** cause of the deploy pipeline's smoke-run break; unmounting or deferring the transport gate does not unbreak it | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §11; `F-S5-12` | Consumed — `../92_risk-register.md:269`–`:278` (`R-S4-2`); `../91_findings-register.md:260` (`F-S4-3`) | `confirmed` | The fix is the creator's; the sequencing obligation is SUB-7's (OUT-3). This package may change no file under `.github/`, `src/` or `tests/` |
| OUT-8 | Enforcement clauses 1–4 add zero DB round-trips and zero connection acquisitions per request, so they are consistent with `OBJ-1` and do not move the 2–200 learner band | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §12 | Design derivation over `../15_operational-objectives-for-the-real-platform.md:248`, `:131`, `:161`–`:163`; repository read `src/infrastructure/db/client.ts:42`; `src/adapters/drizzle/context-token-repository.ts:39`–`:55` | `confirmed` | Clause 5's RLS layer is the one element with a potential `OBJ-1` interaction; registered as `OI-S5-1`, not argued away |
| OUT-8 | `R-S4-1`'s named residual is settled: the enforcement point takes `(principal_id, principal_kind)` as an indivisible pair at the port boundary | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §2 clause 2, §8.4 `I5`; `DR-C11-S5-1` clause 2 | Consumed and discharged — `../92_risk-register.md:264`, `:267` | `confirmed` | Structural enforcement in DDL is SUB-13's; nothing in the schema can express the rule today |
| OUT-8 | OUT-8's outcome-register row carries its resolving evidence and its success measure, and the Critical charter § Risks row `R1` is authored here | `../90_outcome-register.md` § SUB-5; `../92_risk-register.md` § SUB-5 | Authored content per charter assumptions 46, 47 and 48 | `confirmed` | Aggregated by SUB-14, which authors neither |
| OUT-8 | Every C010 decision consumed is cited with its source and no contradiction was found; the check ran and returned empty | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §13 | Twelve-row consistency table over `DR-C10-S5-1`, §3.4.1, §3.2, C010's `F-S5-2` and `F-S5-4`, `A-28`, `CAP-S5-1`, `DR-C10-S6-1`, `NEU-850`'s `OUT-2`, `DR-C10-S8-2`, the authority matrix, and `../../C010-system-and-repository-architecture/09_authority-matrix-validation.md` §4.3 | `confirmed` | **No amendment routed to `NEU-895`.** An addition to a C010 pricing is not a contradiction |
| OUT-8 | No file under `src/` or `drizzle/` is modified | `../05_the-enforcement-point-that-confines-every-read-and-write.md` §14 | `git diff --name-only origin/develop` lists files only under this package directory and `docs/GLOSSARY.md` | `confirmed` | — |

---

## What this file does not establish

- **That any category `holds` on the deployment.** Every `holds` row above is scoped to the composed
  target state of `../05_the-enforcement-point-that-confines-every-read-and-write.md` §8.1. Under
  target state (a), `SC-S3-12` is `not-evaluable`. C010's `F-S5-4` is unchanged.
- **That `CAP-S5-1` is lifted.** Discharged and not lifted; owner `NEU-986`, co-named `NEU-893`.
- **That anything is implemented.** Every `confirmed` status above is confirmed *as a design
  statement or as a repository read*, never as a deployed property. No status in this file means
  "observed in production", and no production quantity appears in it.
- **That the integration-test suite passes.** §7 is a design; no test file exists.
  `CAP-S1-3`'s QA no-op applies — the `qa-execution:engine` surface is unconfigured, so no QA pass
  exists and none is claimed.
- **That the fourteen other Census-B `fails-confinement` categories reach `holds`.** Each needs its
  own enumerated access-path set.
- **That a green citation-checker run covers this package.** C011 is not in the checker's gated list
  (`CAP-S1-2`, owner SUB-14), so the local run is the only gate, and the `…md` ellipsis shorthand is
  invisible to it by construction (`scripts/citation-paths/checker.ts:121`) — see `F-S5-7`.

# `S11` — Traceability: what an existing MCP client is guaranteed

**Task:** NEU-1004 (SUB-11) · **Charter:** C011 (umbrella NEU-893) · **Verification cutoff:** `35f92ba`, 2026-08-25
**Model:** claude-opus-5[1m]

**Outcomes covered:** OUT-16.

Every row resolves into `docs/research/`, never into `_local/`.

---

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| OUT-16 | The registered tool surface is **46**, across 16 registering modules, at `35f92ba` on a branch containing C010 — of the 20 files in `src/server/`, **four** register nothing (`tools.ts`, `persistence-tools.ts`, `session-management-tools.ts`, `tool-helpers.ts`; the middle two are pure aggregators), and no second registration idiom exists in the tree. | `../11_the-client-compatibility-contract.md` §1.1 | `repository-constant` (`grep -rc "server.registerTool(" src/` → 16 non-zero files; `ls -1 src/server/` → 20; `src/server/tools.ts:17`–`:30`) | `confirmed` | A count is a property of a moving tree; nothing in the repository detects it changing. `OI-S11-1`, `R11`'s named residual. |
| OUT-16 | The gate-exempt set is **exactly 3** — `get_server_info`, `init_agent_context`, `get_server_workflow` — and this is established **twice independently**, by empty input schema and by the middleware's own exclusion literal, with the two derivations agreeing. | `../11_the-client-compatibility-contract.md` §1.2 | `repository-constant` (`src/server/server-info-tools.ts:13`; `src/server/server-context-tools.ts:21`; `src/server/server-workflow-tools.ts:15`; `src/transport/context-token-middleware.ts:5`–`:9`) | `confirmed` | **Nothing structurally couples the two derivations.** Their agreement is a fact about this cutoff, not an invariant; either direction of divergence has a distinct failure mode. `OI-S11-1`, owner SUB-13 (`NEU-1006`). |
| OUT-16 | The gated surface is **43**, established as a **module-by-module mapping** of 43 `context_token` schema declarations onto the non-exempt registrations that balances in both directions — not as `46 − 3`. | `../11_the-client-compatibility-contract.md` §1.3 | `repository-constant` (`grep -rn "context_token:" src/`; 41 in `src/domain/types/`, `src/server/session-progress-tools.ts:131`, `src/server/teaching-tools.ts:35`) | `confirmed` | The durable claim is *the set of gated tools lacking `context_token` is empty*; `43` is expected to go stale and is recorded with its cutoff. |
| OUT-16 | The re-count **agrees with C010's `F-S5-3`** — at the 41 / 1 / 1 granularity of where the declarations live, not merely on the three headline figures — so **no finding is routed to `NEU-895`**. | `../11_the-client-compatibility-contract.md` §1.4; `../92_risk-register.md` § `R11` | `upstream-decision` (`../../C010-system-and-repository-architecture/02_findings-register.md:249`–`:254`, `:604`–`:609`) + independent `repository-constant` re-derivation at `35f92ba` | `confirmed` | `R11`'s escalation route to `NEU-895` (co-named `NEU-896`) stays **live for any later re-count**; it did not fire here. |
| OUT-16 | **`42` appears nowhere in the chapter as a codebase fact.** Two near-misses are disclosed rather than asserted away: one citation resolves to a line 42 (`src/infrastructure/db/client.ts:42`, the pool's `max: 4`, in §4 and §8), and one re-derived quantity *totals* 42 (the named `*InputShape` declarations), so §1.4 writes it as **`41 + 1`** and never states the total. | `../11_the-client-compatibility-contract.md` §1.6, §1.4 | `repository-constant` (`src/infrastructure/db/client.ts:42`) + `derivation` (`grep -n "42"` over the chapter, re-run after the fix) | `confirmed` | **The first draft failed this and certified it met** — a bare `42` sat in the reconciliation table's *"Re-derived here"* column. Caught by an adversarial pass before the PR opened; recorded in `../97_package-completeness-gate.md` § `G-S11-6` and `../90_outcome-register.md` limb 2 rather than repaired silently. The disclose-don't-assert precedent is `../08_consent-and-what-a-learner-can-export-and-erase.md` **§11**. |
| OUT-16 | The registered MCP entry-point surface is **49 — 46 tools plus 3 prompts** — and the gate's method predicate (`tools/call` only) does not reach the prompts on either transport. The prompt figure is published **separately** and never folded into the tool count. | `../11_the-client-compatibility-contract.md` §1.5; `../91_findings-register.md` § `F-S11-3` | `repository-constant` (`src/transport/create-server.ts:25`, `:45`, `:80`, `:32`–`:42`, `:56`–`:77`, `:94`–`:118`; `src/transport/context-token-middleware.ts:51`; `src/transport/http.ts:164`) | `confirmed` | **Not an exposure finding.** No prompt handler receives `ctx`, reaches a port, or can return learner content; the finding is about the completeness of the counted surface. |
| OUT-16 | This package's mechanism implies **seven** core changes (`CH-1` … `CH-7`), and each carries a compatibility obligation, a breaking / non-breaking verdict and a stated detection method — 7/7, none carrying two of three. | `../11_the-client-compatibility-contract.md` §3, §4 | `derivation` over `DR-C11-S4-1`, `DR-C11-S4-2`, `DR-C11-S4-3`, `DR-C11-S5-1` | `confirmed` | The detection methods are **specified, not implemented**. None of the four probes is written; `tests/` is out of scope by constraint. |
| OUT-16 | Three of the seven changes alter **semantics with zero schema-shape delta**, so a tool-manifest diff returns **empty** — all 46 names and all 43 gated schemas unchanged, because `context_token` was already required on every gated call — and four behavioural probes are specified in its place. | `../11_the-client-compatibility-contract.md` §4.1 | `derivation` over §1.3's mapping + `upstream-decision` (`../91_findings-register.md` § `F-S4-5`) | `confirmed` | `P2`'s positive limb and `P3`'s negative form are load-bearing because each catches a failure that **presents as success**; a suite omitting them is green and wrong. |
| OUT-16 | Every implied core change carries a **two-transport verdict**. Six of seven hold on both unconditionally; `CH-1` converges in outcome and diverges in cost; **one genuine divergence — audit parity — is named with its owner**. | `../11_the-client-compatibility-contract.md` §5 | `derivation` over `repository-constant` (`src/transport/http.ts:180`, `:186`; `src/transport/main.ts:55`–`:59`; `src/transport/audit-middleware.ts:23`) | `confirmed` | Audit parity is **not** one of the seven changes and is **not** required for `I4`, so descoping it must be argued as an observability decision. Owner SUB-16 (`NEU-999`); carried as `R-S4-4`, cited not restated. |
| OUT-16 | The DP-specificity review returns **clean on this package's seven changes** — zero course-specific concepts introduced — and **breached on the pre-existing surface**: the same four dynamic-programming criterion keys reach 3 of the 46 registered tools via `GradingPayloadShape` **and** the prompt surface via `formatQualityRubric()`, so the breach ranges over the 49 registered entry points rather than the 46 tools alone. | `../11_the-client-compatibility-contract.md` §9; `../91_findings-register.md` § `F-S11-2` | `repository-constant` (`src/domain/types/teaching.ts:275`–`:287`, `:285`–`:286`, `:330`, `:472`; `src/server/teaching-tools.ts:62`, `:88`, `:111`, `:198`; `src/shared/prompts/prompt-pack.ts:837`, `:855`, `:857`) | `confirmed` | A **reusability** defect, not an isolation or privacy one. Pre-existing; the remedy is a `src/` change out of scope here. `R-S11-1`, escalating to `NEU-896`. **No amendment routed to `NEU-895`.** **The standard itself is unverifiable from the repository** — the charter cites it as `C005 :61`, which resolves to no file; `OI-S11-3`. The observation about `src/` does not depend on it. **The prompt-surface reach was added after an adversarial pass** noted the review had been scoped to the 46 tools while this same chapter had widened the counted surface to 49. |
| OUT-16 | `F-S4-4`'s unpriced cost is priced: the seam must carry five of the seven `/mcp` pipeline layers, ranges over four Express-typed factories and three inline blocks, and turns on an **option A / option B** fork that determines whether the blast radius is `src/transport/` or all 46 registration sites. | `../11_the-client-compatibility-contract.md` §6.1, §6.2 | `repository-constant` (`src/transport/http.ts:108`–`:186`; `src/transport/jwt-middleware.ts:87`; `src/transport/rate-limit-middleware.ts:70`; `src/transport/audit-middleware.ts:23`; `src/transport/context-token-middleware.ts:46`; `src/transport/create-server.ts:17`–`:23`) | `confirmed` | **No effort estimate is asserted** and none is derivable from the repository. The fork itself is unresolved — `OI-S11-2`, owner `SUB-10 of C010 (NEU-984)`, co-named `NEU-896`. |
| OUT-16 | An **unpaid extraction does not preserve SUB-4's seven paths — it re-classifies three of them.** Under a cut extraction the adapter clauses still ship, so a STDIO caller resolves to kind `none` and is refused at the adapter **with no gate present to name the reason**. | `../11_the-client-compatibility-contract.md` §6.3; `../91_findings-register.md` § `F-S11-4` | `derivation` over `DR-C11-S5-1` clause 3 + `../91_findings-register.md` § `F-S5-12` + `repository-constant` (`src/config/resolve-transport-config.ts:35`) | `confirmed` | Rests on the rollout shape in which `CH-5`…`CH-7` ship while `CH-1` is cut — the de-risking move `F-S5-12` identifies as available and mistaken. Which tier ships is **SUB-7's** (`NEU-1001`); carried as `R-S11-2`. |
| OUT-16 | **Conditional on `A-S11-2`:** *if* the production `client_credentials` token carries no `sub`, the deploy-pipeline smoke run breaks in a **bounded** way — **six of eight scenarios pass, two fail**, both row-owning — and the remedy is two re-scoped scenarios or one re-provisioned principal, not a rewritten suite. | `../11_the-client-compatibility-contract.md` §7.1, §7.2 | `repository-constant` (`.github/workflows/cd-prod.yml:110`–`:174`, `:158`; `tests/smoke/smoke.test.ts:104`, `:111`, `:128`, `:152`, `:163`, `:200`, `:231`, `:263`) + `derivation` over `DR-C11-S5-1` clause 3 | **`[unconfirmed]`** — the walk is a sound derivation, but its premise is not observed | **The premise is `A-S11-2`**, citing `OI-S1-1` / `SPK-S1-1` (**not executed**) and `R-S2-2`. **On the other branch the walk inverts to 8 of 8 passing** and `CH-6` breaks no known client. `SPK-S1-1`, or §4.1's `P3` probe, settles it in one action. **Read, not observed** — the suite has never been run from here. Scenario 7 fails in the way most likely to be misread, since it already expects an error. Owner unchanged: the creator (`R-S4-2`), sequencing SUB-7 (`F-S5-12`). |
| OUT-16 | `init_agent_context` is **row-owning** despite being gate-exempt — it fires six port queries — and passes only because the call is wrapped in a **fail-open** that returns `null`. Gate exemption is a transport property and does not reach the enforcement point. | `../11_the-client-compatibility-contract.md` §7.3, §4.2 | `repository-constant` (`src/server/server-context-tools.ts:27`, `:28`–`:31`; `src/orchestration/learner-context-workflows.ts:95`–`:103`; `tests/smoke/smoke.test.ts:191`–`:194`) | `confirmed` | **The first draft classified this scenario *not row-owning*** and attributed its pass to gate exemption. Corrected after an adversarial pass. The residual is a real one: the smoke suite would go green while silently losing its entire learner-context payload, a degradation no assertion in it detects — the same safe-direction blindness `R-S5-1` names. Not routed as a new finding; it is a property of `CH-6` and of the suite, both already owned. |
| OUT-16 | A client's guarantee is **bounded by what the enforcement point confines**, and **four path escapes** are published in the contract itself alongside the two-directional attribution limit and the `OBJ-1` concurrency ceiling. | `../11_the-client-compatibility-contract.md` §8 | `consumed` from `../05_the-enforcement-point-that-confines-every-read-and-write.md` §6.1, §6.2, §6.3 and §7.4; `../15_operational-objectives-for-the-real-platform.md:248`, `:131`, `:161`–`:163` | `consumed` | **These are not SUB-5's §6 four re-stated, and §8 now says so explicitly.** SUB-5 enumerates four escapes at `../05_the-enforcement-point-that-confines-every-read-and-write.md:547` as §6.1–§6.4; this table carries §6.1–§6.3 and substitutes **§7.4** (operator and `psql` paths — a genuine path escape SUB-5 reaches while enumerating test coverage). SUB-5's **§6.4**, the non-retroactive boundary, is a *population* limit that cuts both ways and is carried below the table with its complement (`F-S8-2` under-reaching, `R-S5-1` over-reaching) rather than flattened into a one-directional row. The substitution was undisclosed in the first draft and is disclosed after an adversarial pass. Each escape keeps its own owner; none is re-decided here. **No concurrency improvement is implied** — `CH-7` adds zero round-trips and zero connection acquisitions, so it does not move the 2–200 band and does not raise it. |
| OUT-16 | OUT-16's outcome-register row carries its resolving evidence **and** its success measure, and the charter § Risks row the charter owns to OUT-16 is authored here as **`R11`** with severity, mitigation, named owner and escalation route. | `../90_outcome-register.md` § SUB-11; `../92_risk-register.md` § SUB-11 | `derivation` from the charter's § Risks table, cross-checked against six already-authored ids | `confirmed` | The id rests on the charter's § Risks table not having been reordered since SUB-3's read — untestable, because `_local/` is gitignored and unversioned. Registered as `A-S11-1`. **`F-S3-3` is cited, not resolved**; SUB-14 owns it. |
| OUT-16 | No file under `src/` or `drizzle/` is modified, and no test file is written. | `../11_the-client-compatibility-contract.md` §13 | `derivation` (`git diff --name-only origin/develop`) | `confirmed` | None. |

---

## What this file does not establish

- **That any existing MCP client exists**, how many do, or what any of them does today. No production
  credential is available in this environment; **no client count is asserted in either direction**,
  and path 4 is called *"the largest class"* strictly as a property of the default at
  `src/config/resolve-transport-config.ts:35`, never as a census. `CAP-S11-1`; `SPK-S11-1`,
  **designed and not executed**.
- **That the contract has been exercised.** No probe of §4.1 is implemented, no smoke scenario was
  run, and no gate was applied to a real client. Every verdict in the contract is a derivation from
  a repository fact.
- **That the gate extraction is affordable.** §6 prices what the cost is a cost *of* and which fork
  sizes it. **No hours, no line estimate, no band.**
- **That the surface figures will hold.** They are true at `35f92ba` and nothing in the repository
  detects them changing — no lint rule, no CI check, no test. `OI-S11-1`; `R11`'s named residual.
- **That the DP rubric has ever inconvenienced anyone.** `F-S11-2` observes a schema, not a victim.
- **A remedy for anything it finds.** `F-S11-2` needs a `src/` change; `F-S11-1` and `F-S11-5` need
  edits to other sub-tasks' entries, which the append-only rule reserves to SUB-14; `F-S3-3` needs a
  diff of two readings of a gitignored file. Each is routed with an owner and none is repaired here.
- **That any state category reaches `holds`, or that any isolation invariant is satisfied.** SUB-5's
  and C010's; nothing in this chapter moves `F-S5-4`'s census.
- **A QA pass.** The `qa-execution:engine` surface is unconfigured and the capability registry
  resolves to `git, linear`, so the phase is a genuine Core Article 8 no-op. **None is claimed** —
  and the record four places in this package cite as carrying that fact, `CAP-S1-3`, is not filed
  (`F-S11-5`).
- **That the 67 DB-backed integration tests pass.** They cannot run here — `vitest.setup.ts:6`–`:11`
  throws without `DATABASE_URL` — and are recorded as **not run locally**, which is neither a claim
  that they pass nor a claim that they are unverifiable; they run in CI against a Postgres container,
  and this sub-task changes no file they cover.

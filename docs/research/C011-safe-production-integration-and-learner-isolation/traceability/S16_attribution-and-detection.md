# `S16` — Traceability: attribution, and the detection of an isolation or privacy failure

**Task:** NEU-999 (SUB-16) · **Charter:** C011 (umbrella NEU-893) · **Covers:** OUT-15 · **Written:** 2026-08-25 · **Verification cutoff:** `5111841`, 2026-08-25
**Model:** claude-opus-5[1m]

Every row resolves into `docs/research/`, never into `_local/`.

**Evidence classes used below.** `repository-constant` — a value or control-flow fact read from
shipped code at the cutoff, cited `file:line`. `repository-history` — measured from this repository's
own git history. `upstream-decision` — a decision published by an earlier sub-task or by C010, cited
by id and consumed unchanged. `derivation` — reasoning over the preceding classes, shown in the
chapter. `authored` — content this sub-task originates (a success measure, a risk, a contract).
`absence` — a checked and reported non-existence. **A green type-check or lint line is not evidence
about this package's content**, and none is cited as such below. **`observed-in-production` does not
appear, because nothing here was observed in production.**

## OUT-15

| Outcome | Claim | Discharged by | Evidence class | Status | Residual |
| --- | --- | --- | --- | --- | --- |
| OUT-15 | On HTTP, an audit row is written in which **0 of 11 columns** carry a server-derived principal, and the two identifier-shaped columns that exist are both **caller-asserted**. | `../16_attribution-and-detection.md` §1; `../91_findings-register.md` § `F-S16-1` | `repository-constant` (`src/transport/audit-middleware.ts:94`–`:99`; `src/transport/http.ts:154`–`:157`; `drizzle/0010_create_infrastructure_mcp_request_log.sql`; `drizzle/0012_extend_mcp_request_log.sql`) | `confirmed` | None on the reading. What the columns actually contain **in production** is unobserved — `OI-S1-5`, SUB-1's; cited, not restated. |
| OUT-15 | On STDIO, **no audit record is written at all**, and no principal is resolved to write — a strictly worse state than HTTP's, reported as such rather than averaged with it. | `../16_attribution-and-detection.md` §1 | `repository-constant` (`src/transport/main.ts:46`–`:54` HTTP branch, `:55`–`:58` STDIO branch; `src/config/resolve-auth-config.ts:105`) | `confirmed` | None. |
| OUT-15 | The attributability audit reports **HTTP and STDIO separately, before and after**, and **STDIO reports no improvement** under the proposed change, with its residual named. | `../16_attribution-and-detection.md` §1 | `derivation` over the two rows above + `DR-C11-S16-1` decision 7 | `confirmed` | **STDIO remains unattributable.** Two residual owners, because there are two missing things: **SUB-7** (NEU-1001) for the record; C010's `OI-S8-2`, owner **`SUB-10 of C010` (NEU-984)**, co-named `NEU-896`, for the gate that would give it a principal. |
| OUT-15 | The gap on HTTP is one of **carriage, not derivation** — the verified subject exists in the process at the moment the audit row is written and is discarded at the persistence boundary. | `../16_attribution-and-detection.md` §1 | `repository-constant` (`src/transport/jwt-middleware.ts:127`; `src/transport/http.ts:204`–`:210`) | `confirmed` | None. This is why a column pair closes it rather than a new identity mechanism. |
| OUT-15 | Attribution is carried by a **new server-derived column pair** — `principal_kind` (`NOT NULL`, three-valued) plus `learner_key` (the `sub` verbatim, non-null iff kind is `user`) — and **neither existing column may be reused**. | `../decision-records/DR-C11-S16-1_the-attribution-carrier.md` decisions 1–3 (seven rejected alternatives) | `upstream-decision` (`DR-C11-S2-1`, `DR-C11-S2-2`, `DR-C10-S8-2`) + `repository-constant` | `confirmed` (design) | **Not implemented.** Zero files under `src/` or `drizzle/` change here. DDL is **SUB-13**'s (NEU-1006); enforcement is **SUB-5**'s (NEU-997); sequencing is **SUB-7**'s (NEU-1001). |
| OUT-15 | The carrier represents the **third principal state** — a `client`-kind service principal holding no learner state, with learner access **refused, not empty-scoped** — distinctly from both a learner row and an unattributed row. | `../16_attribution-and-detection.md` §2; `../decision-records/DR-C11-S16-1_the-attribution-carrier.md` decision 4 and rejected alternative 3 | `upstream-decision` (`DR-C11-S2-2` decision 3 and rejected alternative 5) | `confirmed` | The **refusal event itself does not exist** — `ME-S16-3`, owner **SUB-5** (NEU-997), per `DR-C11-S2-2` consequence 5. |
| OUT-15 | The model is designed against the **static-client (`claude-web`) shape, not DCR**, and applies `F-S2-1` by making the carrier **indifferent to the audience** rather than by hard-coding a client id. | `../16_attribution-and-detection.md` §2; `../decision-records/DR-C11-S16-1_the-attribution-carrier.md` decision 5 and rejected alternative 6 | `upstream-decision` (`../91_findings-register.md` § `F-S2-1`; `docs/adr/0001-oidc-issuer-and-dedicated-as-audience-binding.md:65`, `:67`) + `absence` (the literal `claude-web` appears nowhere in `src/`; the value is supplied at `src/config/resolve-auth-config.ts:94`–`:99`) | `confirmed` | The static client's **claim set** — and therefore whether its tokens carry a human `sub` — is unobserved: `OI-S2-2`, SUB-2's; cited, not restated. |
| OUT-15 | **Four failure modes map to a signal, a threshold, an alert route and an owner. Zero failure modes without a signal; zero blank cells.** | `../16_attribution-and-detection.md` §3 | `derivation` over the matrix | `confirmed` | **Every alert route is `[unconfirmed]`** against `OI-S1-9`, SUB-1's single record; cited, not restated. Carried as `R-S16-2` and stood over by `A-S16-1`. |
| OUT-15 | `SIG-S16-1`'s zero-tolerance threshold is arithmetically grounded: the only known `client` principal calls only `init_agent_context`, which is **one of exactly three exempt tools**. | `../16_attribution-and-detection.md` §3.1 | `repository-constant` (`src/transport/context-token-middleware.ts:5`–`:9`) + charter assumption 20 | `confirmed` (arithmetic) · `[unconfirmed]` (population) | **`n = 0`.** The claim that a non-exempt `client` call indicates a leak has never been checked against a real population — `R13` cited; capped by `CAP-S16-1`. |
| OUT-15 | `SIG-S16-2` is grounded in **three real fail-open sites**, not a hypothetical: a missing session binding is admitted, an internal gate error admits the call, and the gate may be unmounted. | `../16_attribution-and-detection.md` §3.2 | `repository-constant` (`src/transport/http.ts:57`–`:58`, `:184`–`:187`; `src/transport/context-token-middleware.ts:83`–`:86`) | `confirmed` | On **STDIO the signal is unsettable**, not zero — no record exists to count. Reported as undefined rather than as a passing count. |
| OUT-15 | `SIG-S16-4` limb (b) — the refusal rate — is **the only signal in the matrix computable from data the deployment emits today**, needing no schema change and no new emission. | `../16_attribution-and-detection.md` §3.3 | `repository-constant` (`drizzle/0010_create_infrastructure_mcp_request_log.sql:10`) | `confirmed` | It says nothing about *who* was refused; that is what attribution adds. Its comparison window is hours at the measured deploy cadence, and whether that is enough traffic is unknown — `OI-S15-3`, SUB-15's; cited. |
| OUT-15 | **Seven missing emissions are named, each with an owner.** Zero signals assume an emission the deployment does not make. | `../16_attribution-and-detection.md` §4 | `absence`, routed, seven times | `confirmed` | Owners: this chapter + **SUB-13** (`ME-S16-1`); **SUB-7** + C010's `OI-S8-2` (`ME-S16-2`); **SUB-5** (`ME-S16-3`, `ME-S16-4`); the creator as sole operator (`ME-S16-5` via `OI-S1-9`, `ME-S16-7` via `OI-S16-1`); **SUB-9** (`ME-S16-6`). |
| OUT-15 | **Exactly one privacy reading is determined, per table**: both log tables are **`learner-linked` personal data** under the proposed attribution, with the adoption condition stated rather than assumed. | `../decision-records/DR-C11-S16-2_the-audit-log-privacy-determination.md` decisions 1–3 (six rejected alternatives); `../16_attribution-and-detection.md` §5 | `derivation` over `DR-C11-S16-1` + `upstream-decision` (`DR-C11-S3-2`) | `confirmed` | Whether either table holds learner-derived content **in production** is unobserved — `OI-S1-5`, `OI-S1-6`, SUB-1's; **cited and not resolved by assumption**. The determination is sound under either answer. |
| OUT-15 | The consequence for **export, erasure and retention** is stated per table, including that erasure over either table is **today reachable by no per-learner predicate at all**. | `../decision-records/DR-C11-S16-2_the-audit-log-privacy-determination.md` decision 4; `../16_attribution-and-detection.md` §5.1 | `repository-constant` (`src/transport/audit-middleware.ts:88`, `:105`, `:109`; `src/shared/redact-params.ts:1`; `drizzle/0013_create_operation_event_log.sql:9`; the 256 cap at `src/orchestration/topic-workflows.ts:591` and `src/orchestration/chunk-workflows.ts:168`; `scripts/retention-cleanup.sql`) | `confirmed` | **No retention period is set** for `operation_event_log`, because that rests on a lawful basis this package does not hold — `OI-S3-1`, SUB-3's; cited, not duplicated. Carried as `R-S16-4`. |
| OUT-15 | **Zero revisions are raised against SUB-3's inventory**, and none is produced, requested or owed. | `../decision-records/DR-C11-S16-2_the-audit-log-privacy-determination.md` decision 6; `../16_attribution-and-detection.md` §5.3 | `derivation` — search over this sub-task's own output returns zero revision requests against `../03_learner-data-inventory-and-classification.md`; `git diff --numstat` shows that file **unmodified** | `confirmed` | `DR-C11-S3-2`'s first revision trigger fires with this determination, and firing it is the designed path. |
| OUT-15 | **Attribution is not retroactive**: every pre-cutover row is permanently unattributable, so a per-learner erasure over the log tables reports success while provably missing them. | `../91_findings-register.md` § `F-S16-5`; `../16_attribution-and-detection.md` §5.2 | `repository-constant` (`src/transport/http.ts:83`, `:57`–`:58`) + `upstream-decision` (`F-S15-3`) + `repository-history` (deploy cadence, `C-17`) | `confirmed` | The **disposition** of the pre-cutover population is **SUB-9**'s (NEU-1003) under OUT-12; its **size** is unobserved (`OI-S1-5`, `OI-S1-6`, `OI-S16-1`). **No row count is asserted.** Carried as `R-S16-1`. |
| OUT-15 | The **stalled-propagation signal contract** states **9 required fields, 3 location properties, 1 fire condition and 6 negative clauses** — conformable to without reference to any artifact that does not yet exist. | `../decision-records/DR-C11-S16-3_the-stalled-propagation-signal-contract.md` decisions 1–4 (seven rejected alternatives); `../16_attribution-and-detection.md` §6 | `authored` + `repository-constant` for each negative clause's ground | `confirmed` | **`deadline_at` has no value**, and none is invented. Stating it is **SUB-8**'s under OUT-11; the signal is *fully specified and not yet evaluable*, recorded that way in §3's threshold column. |
| OUT-15 | The contract's direction is **forward-only**: this sub-task asserts only its own conformability and makes **no claim about SUB-9's design**, which does not exist at position 7. | `../decision-records/DR-C11-S16-3_the-stalled-propagation-signal-contract.md` header and § Decision 5; `../16_attribution-and-detection.md` §6, §9 item 5 | `derivation` — no row of this matrix and no acceptance clause names a SUB-9 artifact | `confirmed` | **SUB-9's acceptance asserts the match**, at position 11. |
| OUT-15 | `OBJ-11` (65 536-byte body cap) is **consistent**, and is applied twice by two separate constants of the same value. | `../16_attribution-and-detection.md` §7 | `repository-constant` (`src/transport/audit-middleware.ts:14`; `src/transport/pg-audit-transport.ts:36`) | `confirmed` | A body of exactly 65 536 bytes must be read as **possibly truncated**, never as complete — applied in §5.1's export row. |
| OUT-15 | `OBJ-10` is **consistent and a lower bound**: a second loss path runs before the breaker opens, which it does not bound. Registered as an **extension, not a contradiction**. | `../91_findings-register.md` § `F-S16-2`; `../16_attribution-and-detection.md` §7 | `repository-constant` (`src/transport/pg-audit-transport.ts:30`–`:36`, `:83`–`:90`, `:92`–`:93`; `src/transport/pg-event-transport.ts`) | `confirmed` | **No revision is routed to SUB-15 and none is owed** — `OBJ-10`'s statement, provenance and citations are all accurate for what they bound, and `../15_operational-objectives-for-the-real-platform.md` §6 item 7 anticipated this reader. Magnitude needs `OI-S15-3`. Carried as `R-S16-3`. |
| OUT-15 | The tool surface holds at **46 registered / 43 gated / 3 exempt**, re-counted at this cutoff, with two disclosures: "gated" is **HTTP-only**, and the gate **fails open on internal error**. | `../16_attribution-and-detection.md` §8; `../91_findings-register.md` § `F-S16-3` | `repository-constant` (46 `registerTool` sites across `src/server/*.ts` reached via `src/server/tools.ts:17-31`; `src/transport/context-token-middleware.ts:5`–`:9`, `:82`–`:86`; `src/transport/http.ts:184`–`:187`) | `confirmed` | Agrees with C010 `F-S5-3` / `F-S8-1` as consumed through OUT-16. **No amendment routed to `NEU-895`.** **`42` appears nowhere as a codebase fact, and no citation here resolves to a line 42.** |
| OUT-15 | OUT-15's outcome-register row is **authored here** with its resolving evidence and its five-part success measure. | `../90_outcome-register.md` § SUB-16 | `authored` (charter assumptions 46, 47) | `confirmed` | SUB-14 aggregates it and authors none of it. |
| OUT-15 | Every residual exposure carries a risk entry with severity, mitigation, named owner and escalation route; **zero charter § Risks rows are claimed**, correctly. | `../92_risk-register.md` § SUB-16 | `authored` (charter assumption 48) | `confirmed` | Four entries; three open, one partially mitigated. **No § Risks row names OUT-15.** `R13` and the charter's `R2` are cited, not restated. |
| OUT-15 | **No file under `src/`, `drizzle/` or deployment configuration is modified.** | `../16_attribution-and-detection.md` §0 | `derivation` — `git diff --name-only origin/develop` returns zero paths under `src/` or `drizzle/` | `confirmed` | None. |

## What this file does not establish

1. **That any signal in the matrix has ever fired, or been run.** Every threshold is derived from a
   repository constant at **`n = 0`** observed production events. `CAP-S16-1` caps what a threshold
   here may be read to mean; `R13` carries the evidence position and is cited, not restated. **The
   `observed-in-production` label appears nowhere in this sub-task's output.**
2. **That a signal which fires would reach anyone.** All four alert routes are `[unconfirmed]`
   against `OI-S1-9`. Detection here is *specifiable and undeliverable* until that record closes —
   `R-S16-2`, stood over by `A-S16-1`.
3. **That STDIO is made attributable by anything here.** It is not, and §1 reports no improvement
   rather than a partial one.
4. **That the deployment is in the state the privacy determination classifies.** The determination is
   **conditional on adoption** and asserts nothing about the deployment as it stands. Asserting it
   would be the overstatement `R10` names.
5. **That SUB-9's completion proof conforms to the contract.** That design does not exist at position
   7. **SUB-9's acceptance asserts the match**, at position 11 — never this one's.
6. **That SUB-8, SUB-9, SUB-12 or SUB-13 in fact cite these ids.** The direction is forward-only:
   this sub-task publishes them; citing them is each consumer's own acceptance, at positions 10, 11,
   13 and 14.
7. **Anything legal.** Every duty named in §5 is an engineering consequence of a classification. The
   determination that would make it a legal obligation is `OI-S3-1`, owned elsewhere, and this
   package states positions rather than determinations.
8. **That the id shapes `F-S16-<k>`, `R-S16-<k>`, `A-S16-<k>`, `ME-S16-<k>` and `SIG-S16-<k>` are the
   package's settled convention.** They are computed from the charter alone under
   `DR-C11-S1-3`'s `S<n>`-is-the-sub-task-number rule and `DR-C11-S15-3`'s non-charter id scheme;
   **SUB-14 (NEU-1007) adjudicates** and may renumber, in which case every id in this matrix moves
   with it.

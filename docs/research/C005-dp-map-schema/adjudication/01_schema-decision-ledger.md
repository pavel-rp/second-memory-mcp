# Schema Decision Ledger

**Task:** NEU-933 · **Compiled:** 2026-07-16 · **Extends (references, never rebuilds):** `../../C005-product-foundation/adjudication/` (NEU-887 adjudication method and status discipline), following NEU-932's `../../C005-dp-map-foundations/adjudication/01_selection-decision-ledger.md`

**This file is the sole source of truth for the status of every NEU-933 decision — and for the status of every node in the map.** No other file sets a status. Not the README, not `manifest.yaml` (which carries the legend but adjudicates nothing), not a node's YAML.

**A downstream sub-task that wants a status changed files a challenge here. It never re-decides locally.**

---

## 1. Status values (inherited from NEU-887 — not redefined)

| Status | Meaning for a downstream consumer |
| --- | --- |
| **settled** | Binding. Adjudicated on correctly-classed evidence. Consume it; do not re-derive it. Change requires a ledger entry. |
| **provisional** | Recorded and usable but **not binding**. Carries a named revision trigger. A consumer relying on it must surface that reliance. |
| **unresolved** | Known open, with a named owner. A consumer **must not invent a value**. |

## 2. The ledger

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **D-S1** | **The node/edge schema** — `node_kind` (knowledge/skill), the eight-value closed `skill_type` vocabulary, the ordered first-match-wins typing cascade S1→S8, prerequisite-edge semantics free across knowledge/skill, and the four prerequisite fields (three drawn, one declared). **Resolves NEU-932's `D-F3a`.** | **settled** | `../01_node-and-edge-schema.md`; demonstrated by the 8 worked roots; desk-checked by `../dry-run/00_schema-usability-dry-run.md` (7/8 pass, 1 deferred as `INC-D1`, inherited) | ✅ 4 recorded — multi-valued `skill_type`; an open/extensible skill vocabulary; an unordered set of type definitions with no cascade; edges constrained by node kind | **>10 `D-S1a` entries accrue** at the coverage audit — signals the cascade needs revision rather than `conceptual` absorbing drift (mirrors `D-F4`'s `>10 U2` trigger). Or a mapper cannot express a real node in the schema. | This task; challenges via any mapper |
| **D-S1a** | Skill-type assignment for **specific indeterminate skill nodes** (Convention S). **1 entry — see §3.3** (filed by NEU-942 on NEU-934's behalf; `EXC-11` was warranted-but-blocked by file ownership). *Superseded the original "no entries yet" note, which was written before any technique node existed.* | **provisional** | §3.3; `cl-1.derive-recurrence-routine` `notes` | — | Filed by a mapper when a skill node's type cannot be confidently determined. **See §3 for the filing route.** **`D-S1` cascade-revision threshold (`>10`): NOT fired — count is 1.** | SUB-3/4/5/6/13 |
| **D-S2** | **The 8 DP first-principle root nodes** — the charter's four principles, each as a knowledge node + the skill node it licenses, typed, frozen, in `nodes/cl-1-foundational.yaml`. | **settled** | `../02_terminal-floor.md` §2, self-check FC-1…FC-3, FC-10; class 1 `[literature]` | ✅ 3 recorded — a single "DP fundamentals" node; one node per principle (no knowledge/skill split); a separate `nodes/roots.yaml` outside CL-1's file | A mapper demonstrates a DP technique whose prerequisite chain **cannot** terminate on any of the 8 roots and is not a boundary-anchor case. Or the charter's principle set changes. | This task; challenges via any mapper |
| **D-S3** | **The assumed-knowledge boundary register** `1.0.0` — 5 sanctioned non-DP anchors (segment tree, Li Chao tree, convex-hull/envelope geometry, modular arithmetic, linear algebra), named and versioned, **none decomposed**. | **settled** | `../02_terminal-floor.md` §3, self-check FC-4…FC-8, FC-11…FC-12; class 1 `[literature]` | ✅ 2 recorded — folding Li Chao into `anchor.segment-tree`; pre-emptively registering anchors the spec does not name (would invent scope) | **An AR-1 request is filed** (§3) — the register is **not asserted complete** (`INC-S1`). Or an anchor's scope needs changing (a MAJOR `register_version` bump, because every dependent's terminal shifts meaning). | This task; requests via any mapper |
| **D-S4** | **Root edges are DRAWN directly by every mapper**, via `prerequisites.roots`, not declared for SUB-12 — even though roots carry `cl-1.` ids and therefore span clusters by a naive endpoint test. | **settled** | `../01_node-and-edge-schema.md` §4.2 — a four-part argument from what NEU-932's rule 4 is *for* (a visibility problem roots do not have) | ✅ recorded — routing root edges through SUB-12 as cross-cluster declarations (**rejected: the floor would not exist until integration, so the floor audit could not run on the mappers' own output**) | NEU-932's author, or an audit, rejects the refinement. **Carried as `X-S1`** rather than treated as settled-by-silence. | This task; SUB-12 and the dependency audit are affected parties |
| **D-S5** | **The register extension** — NEU-887's taxonomy, materiality rule, status values, relation vocabulary, and marker discipline extended by reference to the DP map; only `-S`-namespaced ids added. | **settled** | `../04_register-extension.md`, self-check RE-1…RE-12; `../traceability/01_schema-evidence-register.md` | ✅ 2 recorded — a DP-specific evidence class; a DP-specific materiality rule (**both considered and declined**, §2.1 there) | An audit finds NEU-887 machinery re-derived rather than referenced. | This task; the register-completeness audit |

## 3. Open filing routes — seeded for the sub-tasks that come next

**This is the skeleton SUB-11 later drives.** Each route names its owner and its procedure **in advance**, so a mapper never has to invent one mid-task. A route that exists is a route that gets used; an unnamed one becomes a silent local decision.

| Route | Who files | When | Procedure | Interim state |
| --- | --- | --- | --- | --- |
| **`D-S1a`** — indeterminate skill type (Convention S) | Any mapper | A skill node's type cannot be confidently determined by the S1→S8 cascade | Assign the **best candidate** (the type is mandatory), state the indeterminacy and name the rival type in `skill_type_rationale`, add a `D-S1a` row here with a **named re-adjudication trigger** | Node `status: "provisional"` |
| **`AR-1`** — anchor request | Any mapper | A real non-DP prerequisite has no registered anchor | File against `D-S3` naming: the anchor, the dependent node, and **why the prerequisite is genuinely non-DP** (why NEU-932's cascade doesn't own it) | Node `status: "provisional"`; dependency recorded in `notes`. **Never** invent an anchor, fake a root edge, declare it cross-cluster, or drop it |
| **`D-F4a` U4 challenge** — cluster misassignment | Any mapper | A technique is believed misassigned across clusters | File against `D-F4a` (or `D-F4`) in **NEU-932's** ledger — not this one. Convention U4: **the existing assignment stands until adjudicated, and the technique stays mapped.** The map never has a hole while an argument is in progress | Existing assignment holds |
| **`D-S1`/`D-S2`/`D-S3` challenge** — the schema itself | Any mapper | The schema cannot express a real node; a chain cannot reach the floor | File here with the **specific node** that cannot be expressed. **Never** locally redesign — five sub-tasks are scoped to this schema | Node `status: "provisional"` |

**Known request already foreseen** (`INC-S1`): SUB-5 is expected to file **AR-1** for Aho–Corasick / string-matching automata, the non-DP prerequisite of automaton DP (CL-3 by `D-F4` §3.2). Named here in advance so it is a **predicted filing, not a surprise**.

### 3.1 `AR-1` requests filed — open, awaiting adjudication against `D-S3`

**Filed by SUB-5 (NEU-936)** while mapping CL-3. Both are **requests, not decisions**: no anchor is minted by a mapper, and `D-S3`'s owner adjudicates. Both dependent nodes carry `status: "provisional"` and record the dependency in `notes` per the AR-1 interim state, so the prerequisite stays **visible** rather than smoothed.

| Req | Anchor requested | Dependent node(s) | Why genuinely non-DP (why `D-F4` §3's cascade doesn't own it) | Status |
| --- | --- | --- | --- | --- |
| **AR-1/a** | **Aho–Corasick automaton construction / string-matching automata** (proposed id `anchor.string-matching-automata`, proposed `kind: "data-structure"`) | `cl-3.formulate-automaton-dp`, `cl-3.implement-aho-corasick-dp` | Automaton construction computes **no recurrence** and makes **no optimal-substructure claim**. Building the trie and wiring suffix links is a structural construction over the pattern set; the DP then **borrows the finished structure** as its state space. Running `D-F4` §3's cascade fires no test — it is general-algorithms machinery above NEU-887's elementary floor, which is exactly the register's admission rule (a)+(b)+(c). | **open** — this is the `INC-S1` filing predicted in §3 above. Not a surprise; not improvised. |
| **AR-1/b** | **Single-source shortest-path relaxation (Dijkstra / Bellman–Ford)** (proposed id `anchor.shortest-path-relaxation`, proposed `kind: "algorithm"`) | `cl-3.implement-steiner-tree-dp` | The Dreyfus–Wagner *grow* transition relaxes the current terminal-mask layer along graph edges to fixpoint; that relaxation is a graph-algorithms primitive above the elementary floor, not a DP the map owns. **Recorded honestly:** Bellman–Ford is arguably itself a DP over edge count — which is precisely why this is filed for **adjudication** rather than asserted. If `D-S3`'s owner decides the DP reading governs, the correct outcome is a **CL-1 cross-cluster declaration, not an anchor**. SUB-5 does not decide this. | **open** |

**Effect on `D-S3`'s revision trigger:** `D-S3` names "**an AR-1 request is filed**" as its trigger, and two now are. `D-S3` remains **settled** at `register_version 1.0.0` — a filed request is not an amendment, and no mapper drew an unregistered anchor. Adjudicating either request MINOR-bumps the register (adding an anchor); neither changes an existing anchor's scope, so no MAJOR bump is implied.

> **Correction filed by NEU-942 (SUB-10), 2026-07-16 — `CV-34`.** The paragraph above is preserved as written and is now **superseded on the count only**: **six** AR-1 requests are filed, not two. The four added in §3.1a below were recorded **in-file only** by their filers because *the ledger was not their file to write* — sole-writer ownership working correctly and producing an incomplete register anyway (`CV-33`). `D-S3` **remains settled at `register_version 1.0.0`**: still no amendment, still no mapper drew an unregistered anchor. Adjudicating any of the six MINOR-bumps the register; **one — `AR-1-b/938` — would MAJOR-bump it if adjudicated by widening `anchor.convex-hull-envelope-geometry`'s scope instead of adding an anchor**, which its filer flags explicitly and reserves to `D-S3`'s owner.

### 3.1a `AR-1` requests filed — **unioned into the ledger by NEU-942 (SUB-10)**, open, awaiting adjudication against `D-S3`

**Provenance of this subsection.** OUT-7's coverage audit found the AR-1 register **provably incomplete**: seven AR-1/`D-S1a`-class claims existed across the map and **only the two in §3.1 were in the ledger**. The rest lived in their authors' node files. **No filer erred** — each was blocked by the schema's own rule 3 (sole writer) and each recorded the request in-file with the dependent node marked `provisional`, exactly as route AR-1's interim state requires. NEU-942 owns this ledger and discharges the backlog. **Nothing in §3.1 was modified**; these are additions.

**⚠ Id collision, recorded not resolved (`CV-32`).** NEU-936 minted **`AR-1/a`**, **`AR-1/b`**; NEU-938 independently minted **`AR-1-a`**, **`AR-1-b`** — four distinct requests under two label pairs differing only by `/` vs `-`. Neither mapper erred: route `AR-1` names no id-minting convention. **Requests are therefore qualified by filer (`/936`, `/938`, `/935`) in this subsection.** Fixing the convention is **SUB-11 (NEU-944)'s** at reconciliation (`INC-C4`).

| Req | Anchor requested | Filed by | Dependent node(s) | Why genuinely non-DP (why `D-F4` §3's cascade doesn't own it) | Status |
| --- | --- | --- | --- | --- | --- |
| **`AR-1-a/938`** | **Lagrangian duality** (proposed id `anchor.lagrangian-duality`, proposed `kind: "mathematics"`). Scope: the Lagrangian and its dual function, weak duality, and the tightness condition for an integer resource constraint. **Not** the DP that exploits it, **not** convex optimization at large, **not** numerical methods. | NEU-938 (SUB-13) | `cl-4.lagrangian-relaxation-for-dp`, `cl-4.aliens-trick-application`, `cl-4.aliens-trick-convexity-proof` | Run the cascade and **no test fires — every test presupposes a recurrence and duality has none.** T1 needs an already-correct recurrence whose cost it reduces; duality is a reformulation theorem applying to linear and continuous programs that are not DPs at all. T2/T3/T4 all presuppose a state; there is none. Condition (b) holds: `cl-4.aliens-trick-application` **is** this theorem specialised to a counted DP constraint. Condition (c): graduate-level optimization theory, well above NEU-887's elementary floor. **Checked against all five registered anchors**; `anchor.convex-hull-envelope-geometry` is the closest and still wrong (envelopes of lines vs. a constrained-program relationship) — the filer cites `D-S3`'s own Li Chao reasoning as precedent. | **open** — recorded in `frontier.yaml` `ar1_requests` and declared as `X-938-3`; transcribed here by NEU-942 |
| **`AR-1-b/938`** | **(min,+) convolution** (proposed id `anchor.min-plus-convolution`, proposed `kind: "mathematics"`). Scope: the infimal-convolution operator on integer-indexed functions, its associativity/commutativity, and **the slope-multiset merge fact** on convex piecewise-linear functions. **Not** any fast general-case algorithm, **not** discrete convex analysis at large. | NEU-938 (SUB-13) | `cl-4.slope-trick-application`, `cl-4.slope-trick-convexity-preservation-proof`, `cl-4.slope-trick-on-trees` | Same structural reason: **it is an OPERATOR, not a technique for evaluating a recurrence** — it is the algebra a recurrence may be *written in*. T1 reduces nothing; T2/T3/T4 presuppose a state it has none of. The merge fact **is** the licence for slope trick's central move. **Counter-argument recorded by the filer rather than omitted:** a knapsack transition *is* a (min,+) convolution, so a reviewer may call it DP content. The filer's answer — that this makes the operator *more general* than the DP, the same relation linear algebra bears to matrix-exponentiation DP (registered as an anchor on exactly that footing) — **is sound but is `D-S3`'s owner's to weigh, not NEU-942's.** | **open** — **⚠ the one request that could imply a MAJOR bump.** The filer warns that folding this into `anchor.convex-hull-envelope-geometry` would change that anchor's scope (a MAJOR bump, shifting every dependent's terminal meaning) and states the clean rejection path instead: rule it CL-4 knowledge, absorbed by `cl-4.slope-trick-convex-function-representation`. |
| **`AR-1-c/935`** | **Topological order / DAG traversal** (id and `kind` **not proposed by the filer** — not invented here either) | NEU-935 (SUB-4) | Recorded in the depending CL-2 node's `notes` with `status: "provisional"` | A genuine non-DP prerequisite of a CL-2 node with no registered anchor. **The filer did not invent an anchor, fake a root edge, or declare it cross-cluster** — all three forbidden by AR-1 — and carried the request in-file because *"the ledger entry is SUB-2's file to write; SUB-4 cannot write it without breaking sole-writer ownership."* **Named as a gap with an owner by its filer, and surfaced in its PR.** | **open** — transcribed by NEU-942. **The cascade rationale is the filer's to state and is not elaborated here**: NEU-942 transcribes a blocked request, it does not author one. `D-S3`'s owner may need the filer's detail. |
| **`AR-1-d/935`** | **Strongly-connected-component condensation** (id and `kind` **not proposed by the filer**) | NEU-935 (SUB-4) | Recorded in the depending CL-2 node's `notes` with `status: "provisional"` | As above — same route, same discipline, same blockage. | **open** — transcribed by NEU-942; same caveat as `AR-1-c/935`. |

**`AR-1/a` and `AR-1/b` in §3.1 are NEU-936's and are unchanged.** For disambiguation they may be cited as `AR-1-a/936` (Aho–Corasick) and `AR-1-b/936` (shortest-path relaxation).

**Cross-file consistency note (`CV-33`), recorded because it is load-bearing:** map nodes reference these requests **by id in their `notes`** (e.g. *"AR-1/a ANCHOR REQUEST IN FLIGHT — THIS IS INC-S1"*). A ledger that dropped or renamed a row would turn those notes into false claims. This subsection is therefore **strictly additive** and preserves every filer's id.

**`INC-S1` status:** the marker names the register's incompleteness over the technique space and route AR-1 as its remedy. **The route is working** — six requests filed, none improvised, no anchor invented locally, no root edge faked. `INC-S1` **remains open** (completeness is unprovable while `INC-D3` stands) but it is **not** un-actioned.

### 3.3 `D-S1a` entries filed — **unioned into the ledger by NEU-942 (SUB-10)**

| Id | Node | Indeterminacy | Rival type | Re-adjudication trigger | Status |
| --- | --- | --- | --- | --- | --- |
| **`D-S1a-1`** | `cl-1.derive-recurrence-routine` (CL-1, NEU-934) | The **S7/S5 boundary is genuinely thin**. The NEU-934 spec names the skill "procedural" and S7's *steps-do-not-vary* test supports it; **S5 fires on any reading where "promote exactly what must be remembered" is a judgment about THIS problem rather than a step of a fixed drill.** Typed **S7** per the spec and the cascade, with the judgment factored out into `cl-1.select-linear-dp-pattern` so the split is structural rather than asserted. | **S5 (strategic)** | **Filer's trigger, transcribed verbatim in substance:** if SUB-9's eight-skill-type audit finds this file's only `procedural` instances are this node, `build-prefix-aggregate`, and `reconstruct-optimal-solution-by-backtracking`, **and judges the drill to be S5**, re-type it — the type stays instantiated via the other two, so **union-completeness does not depend on this node's disposition**. | **open** — node stays `provisional` |

**Why this entry is here and not filed by its author (`CV-35`).** NEU-934's `EXC-11` records that a `D-S1a` entry was **warranted** and states why it was not filed: *"the ledger lives in the NEU-933 package and NEU-934 is sole writer of this file only — writing another sub-task's file is prohibited by the schema's own rule 3, and four sibling mappers are writing concurrently."* SUB-3 did the two things it could — set the node `provisional` and carry the full rationale and trigger in `notes` — and **recorded the omission as an exclusion precisely so the resulting undercount would be visible.**

**The consequence `EXC-11` predicted is now discharged.** It warned: *"if SUB-9 counts `D-S1a` entries against D-S1's '>10 triggers cascade revision' threshold by reading the ledger alone, this one will not be counted."* **It is now counted.** `D-S1a` count = **1**, far below `>10`, so **`D-S1`'s cascade-revision threshold does not fire** and **SUB-9 (NEU-943) may now count from this ledger alone.** `EXC-11` is **resolved**.

### 3.2 Coverage-and-gap adjudication — **NEU-942 (SUB-10) verdicts, OUT-7**

**The full argument is `../../C005-dp-map-coverage/`** (`02_disagreement-adjudication.md` for the per-disagreement verdicts, `04_work-split-seam.md` for the gap class). These rows are the **status record**; that package sets no status.

**30 coverage disagreements adjudicated — 10 genuine gaps (all owned), 11 mapped equivalences, 6 intentional exclusions, 3 unresolved uncertainties. Zero smoothed differences.** 52 residual exclusions consolidated across five mapper files.

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-C1`** | **The CL-4 work-split seam is a systematic gap class — 10 genuine gaps, one defect.** `D-F4`'s cascade assigns a technique to the **CL-4 cluster**; CL-4's two mappers are scoped by **enumerated lists**, so a technique that is **CL-4-by-cascade but in neither enumeration is owned by the cluster and by no mapper**. Instances: SOS DP, LIS O(n log n), bounded-knapsack binary splitting, bitset/word-parallel, prefix-sum acceleration, rolling-array compression, Hirschberg/bit-parallel edit distance, small-to-large/DSU-on-tree, segment-tree-accelerated digit/automaton transitions, profile-hashing acceleration. **Explicitly NOT a partition finding** — Convention U1 already assigns all ten to CL-4, and `D-F4` needs **no amendment**. | **settled** | `../../C005-dp-map-coverage/04_work-split-seam.md`; sweep of all 52 mapper exclusions + 6 taxonomies + 6 corpora; class 1 `[literature]`. **Predicted by three mappers before this audit ran** — mainstream `RX-2`, CL-2 `E3`, frontier `RX-13`, whose triggers all fire as written | ✅ 3 recorded — **B.** widen SUB-6's enumeration and re-run it (*rebuilds the enumeration that caused the seam*); **C.** accept all ten as immaterial residual exclusions (*rejected: five are canon, carried by T1/T2/T3 and exercised in C1/C2/C4 — ruling them immaterial would make the charter's maximalist claim indefensible*); **D.** amend `D-F4` to split CL-4 (*rejected: `D-F4` §2.1 argues no principled boundary exists; the seam is in the work split, not the partition*) | A further material CL-4-by-cascade technique is surfaced that is neither mapped nor listed in `04_…` §4 — a **MINOR** `scope_boundary` bump per `RX-13`, **not** a partition finding | **`INC-C1` — the creator**, to commission a CL-4 completion task. **Recommended: scope it by the cascade, not by an enumeration** |
| **`D-C2`** | **SOS DP is a genuine coverage gap** (`CV-1`) — carried by T4/T5/T6 and C3/C4/C5/C6, **mapped in no cluster**. Four mappers each correctly declined (CL-3 `E1` under U4; mainstream `RX-1` — mapping it would claim a live dispute, asserting it away would smooth it; frontier `RX-3` — canon-adjacent, not research-tier; CL-2 `E8` — no claim either way). **`RX-3`'s and `RX-1`'s own triggers fire exactly as written.** **Consequence:** `cl-3.bitmask-state-encoding` declares `xc.…->cl-4.sos-dp` — **an attachment to a target that does not exist**; SUB-12 will report it unresolvable. **That is a symptom of this gap, not a separate defect — do not close it by deleting the declaration.** | **settled** (the gap) | `../../C005-dp-map-coverage/02_…` §A; `01_coverage-matrix.md` §4 | ✅ recorded — mapping it in this audit (**rejected: NEU-942 may not map or repair nodes; it would also unilaterally resolve `D-F4a`**) | The gap is filled by the cluster `D-F4a` awards it to | **Blocked on `D-C3`.** Then **`INC-C1`**: NEU-937's cluster half (CL-4 mainstream) if CL-4 holds; NEU-936 (CL-3) if CL-3's U4 claim succeeds |
| **`D-C3`** | **The `D-F4a` cluster assignment of SOS DP is UNRESOLVED and is NOT flipped here** (`CV-1a`). **NEU-942 has no standing:** `D-F4a` lives in **NEU-932's** selection-decision ledger, not this one, and U4 forbids local re-decision. **Recommendation offered to `D-F4a`'s owner, not a decision:** `D-F4` §3.1 orders T1 first because *"an optimization is parasitic on a base DP"* that another cluster owns — **but SOS DP is the only T1 member whose accelerated base (the naive 3ⁿ transition) is mapped nowhere and is nobody's technique.** Every other T1 member (CHT, Knuth, D&C, matrix-exp, slope trick) accelerates a mapped sibling-cluster base. **That asymmetry is real evidence for CL-3's live claim** and is offered as such. | **unresolved** | `X-D1` carried undiminished; `D-F4` §4.2; `../../C005-dp-map-coverage/02_…` `CV-1a`, `04_…` §5 | ✅ recorded — flipping `D-F4a` to CL-3 in this ledger (**rejected on standing: a coverage audit re-deciding a partition entry it cannot write is the exact U4 category error, and a worse defect than the gap it closes**) | `D-F4a` is adjudicated either way. **Until then `D-F4`'s assignment to CL-4 stands, per U4.** **`D-C2` does not wait on this** — the coverage verdict is identical under both readings | **`INC-C2` — `D-F4`/`D-F4a`'s owner (NEU-932), a creator decision.** Routable by **SUB-11 (NEU-944)** at reconciliation, or by **NEU-936** filing the U4 challenge `X-D1` has kept live and unfiled |
| **`D-C4`** | **Centroid-decomposition path counting is an intentional exclusion — outside the partition's domain** (`CV-7`). **Settles CL-2's `E9`, the map's one ownerless exclusion, escalated to OUT-7.** SUB-4's not-DP reading **upheld**; the rival T3 reading **rejected on a named ground: centroid decomposition's subproblems are DISJOINT**, which negates **overlapping subproblems** — one of the four DP first principles frozen as roots by `D-S2`. A technique failing a frozen root principle is not a DP technique the map owns. Confirmed by the register's boundary test (*computes no recurrence, makes no optimal-substructure claim*) and consistent with the `RX-5` precedent (segment tree beats, decided the same way by a different mapper). **Nothing is lost:** the corpus composition decomposes into centroid decomposition (not DP) **+** tree DP (CL-2, mapped). **`E9` is no longer ownerless.** | **settled** | `../../C005-dp-map-coverage/02_…` `CV-7`; `D-S2`'s frozen roots; `D-F4` §1 boundary test; `RX-5` precedent; `F-C-4` (composition finding) | ✅ 2 recorded — **routing it to CL-2 on the T3 reading** (*rejected: disjoint subproblems fail the overlapping-subproblems root*); **leaving it unowned and escalating further** (*rejected: OUT-7 forbids shipping a gap without an owner, and this audit is the named escalation endpoint — escalating again would be an abdication*) | A DP dependent appears that genuinely needs centroid-decomposition semantics, **or** a corpus instance is found in which the centroid recursion itself carries overlapping subproblems | **No owning cluster required** (outside the partition). Residual route = **`RX-5`'s**: any mapper acquiring a real dependent files **AR-1** for it as an anchor — with a dependent, as AR-1 requires |

### 3.4 Package assembly — **NEU-944 (SUB-11) rows, OUT-8 + OUT-9**

**The full package is `../../C005-dp-map-package/`.** These rows are the **status record**; that
package sets no status. **This subsection is strictly ADDITIVE** — NEU-944 modified no row above.
`D-S1`…`D-S5`, `D-S1a`, `D-C1`…`D-C4`, §3.1, §3.1a, §3.2, §3.3, §5 and §7.1 are preserved
byte-for-byte, for the reason `CV-33` records: **map nodes cite these ids in `notes`, so a dropped or
renamed row turns those notes into false claims.**

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-P1`** | **The assembled DP map package `1.0.0`** — the per-technique cross-reference view (**generated**, 187 blocks), the OUT-8 authoring-requirements spec, the package-completeness gate, and the cold-context dry-run handoff. Binds NEU-932…NEU-943 through **this** ledger. **The view is a PROJECTION: it mints no node, edge, stage, difficulty value, materiality finding, integrity finding or coverage verdict, and every field names its owning package.** | **settled** | `../../C005-dp-map-package/` `01`…`05`; gate **38/38**, reusing NEU-943's validator (**28/28**) rather than re-deriving it; dry-run `05` | ✅ 3 recorded — **a hand-written index** (*rejected: `D-F3` §3 already records index-drift as an open cost; a hand-maintained 187-node view is drift on day one, and OUT-9's one-hop claim is only worth making if it is MECHANICALLY true*); **a second independent validator** (*rejected: re-deriving NEU-943's result would prove only that two scripts agree — the spec says CONFIRM, so the gate SPAWNS NEU-943's own validator*); **writing the view into `index/00_technique-index.md`** (*rejected: `sole_writer: "generator only"`, and that file's staleness is `INC-S2`'s to close, not this task's to overwrite*) | The map changes (any node, edge, stage, or status) — the view **must be regenerated** and the gate re-run. Gate check `PG-5` fails on drift. | This task; challenges via any consumer |
| **`D-P2`** | **`F-943-1` WAS an OPEN DEFECT IN THE SHIPPED MAP and was bound as such — NOT repaired here.** NEU-940's `progression_stage`/`prerequisite_depth` were computed against the **pre-NEU-939 graph**: **26/179** depths wrong, **6** stage inversions across 5 nodes. **A downstream agent sequencing by stage at NEU-944's ship would have ordered 6 dependencies backwards.** Bound with an owner, a revision trigger, a per-node warning marker on all 26, and a binding consumer rule (**sequence from the graph, not the stage labels**). — **RESOLVED AS TO `F-943-1` ONLY, by `D-R4` (§3.9). This row's named revision trigger HAS FIRED:** the re-run of NEU-940's depth-and-stage computation over the edge-complete (post-NEU-939) graph landed as **`D-R3`** (NEU-954, §3.8), the 26 nodes' values are re-adjudicated there, **the per-node warning markers are gone because nothing computes them**, and **the binding consumer rule is DISCHARGED** — the graph and the stage labels no longer disagree, so sequencing from the labels no longer orders anything backwards. — **The trigger's OTHER clause — *"`F-943-3` closes with it"* — WAS A FORECAST, AND IT DID NOT GOVERN.** It was written before the re-run existed. The substance re-check `D-R4` records, measured against the post-`D-R3` map, found `F-943-3`'s **determinism limb STILL TRUE with 0 exceptions**, so **`F-943-3` stays OPEN (Low)**, owner **NEU-940 / NEU-888**. **The evidence decided, and the forecast was corrected rather than executed** — closing a finding whose substance survives, on the strength of a prediction made in this cell, is exactly the defect the re-check existed to catch. **`D-P3` is untouched: no stage or difficulty value becomes binding by this resolution.** | **resolved** | `../../C005-dp-map-integrity/05_findings-register.md` `F-943-1`; reproduced by NEU-943's validator and by the package gate (`PG-7`). Direction **unanimous — 26 under-report, 0 over-report**; counterfactual reproduces declared depth on **20/26**; isolation: **293 intra-cluster edges → 0 inversions vs 25 cross-cluster → 6** | ✅ 2 recorded — **re-running NEU-940's computation here** (*rejected: producing a progression stage or difficulty value is explicitly OUT of SUB-11's scope; it would be minting difficulty values under an assembly spec — the exact category error file-ownership exists to prevent*); **shipping the stages unflagged** (*rejected: a map whose stage annotations are 26/179 wrong and not flagged would fail OUT-9's own gate — a cold agent could not "tell binding decisions from open ones"*) | **A re-run of NEU-940's depth-and-stage computation over the edge-complete (post-NEU-939) graph.** On that re-run `F-943-1` closes, `F-943-3` closes with it, and the 26 nodes' values are re-adjudicated here. — **FIRED.** The re-run is `D-R3` (§3.8) and the record closure is `D-R4` (§3.9). **The `F-943-3` half of this sentence was a prediction, not a status, and the measured re-check contradicted it** — `F-943-1` closed **alone**. | **NEU-940's owner** — the dimension values (26 nodes, CL-3 + CL-4). **Not SUB-11.** **Discharged by NEU-954 (`D-R3`) for the values and by NEU-956 (`D-R4`) for the record; `F-943-3`'s surviving limb stays with NEU-940 / NEU-888.** |
| **`D-P3`** | **The deferred creator progression-plausibility review ships PROVISIONAL, map-wide.** All **179** mapped nodes carry `creator_review: "deferred-provisional"`. **No stage or difficulty value in this map is binding** — independently of `D-P2`. | **provisional** | Charter **Assumption #11** (creator unavailable in an unattended run); `../../C005-dp-map-package/03_…` §2; per-node field on 179/179 | ✅ recorded — **promoting the values on the strength of the audits** (*rejected: the audits check STRUCTURE; plausibility is the creator's judgment and no audit substitutes for it. Promoting here would be a status flip on incorrectly-classed evidence*) | **The creator reviews the progression assignment for plausibility.** Each node's `creator_review` then flips and these values are re-adjudicated here. | **The creator** |
| **`D-P4`** | **The `AR-1` id convention — `INC-C4` discharged.** Requests are cited **`AR-1-<letter>/<filer>`**; the filer suffix is **mandatory**. **Both original label pairs are PRESERVED AS ALIASES** — `AR-1/a` ≡ `AR-1-a/936`, `AR-1/b` ≡ `AR-1-b/936`. **Resolves `CV-32`.** | **settled** | `CV-32`; §3.1, §3.1a (both unchanged); `../../C005-dp-map-package/03_…` §5.1 | ✅ 2 recorded — **renaming NEU-936's requests to match NEU-938's** (*rejected: map nodes cite `AR-1/a` and `AR-1/b` **verbatim in `notes`** — a rename turns every citing note into a false claim, the exact hazard `CV-33` names*); **leaving the collision unfixed** (*rejected: `CV-32` routed it here explicitly, and two label pairs differing only by `/` vs `-` is a latent mis-citation*) | A seventh request is filed by a filer with no distinct id — the convention needs a tiebreak. | This task; the convention binds any future `AR-1` filer |

**Why `D-P2` is an `unresolved` row and not a repair.** SUB-11 assembles and binds. Producing a
progression stage, difficulty value, node, edge, materiality finding, integrity finding or coverage
verdict is **out of scope by spec** — every one is inherited. **The honest ship for a defect you do not
own is to bind it loudly with a named owner and a named trigger**, which is what `D-P2` does. It is
flagged on all 26 affected nodes in the generated view, headlined in the package README, and stated
first in the open-items register.

### 3.5 `D-S1a` / `AR-1` re-verification by NEU-944 — **nothing re-broken** ✅

NEU-944 **verified rather than re-derived** the two results NEU-942 discharged, because both are
load-bearing for consumers and neither is SUB-11's to re-open:

| Result | NEU-944's check | Outcome |
| --- | --- | --- |
| **`EXC-11` RESOLVED** — `D-S1a` count = **1**, so `D-S1`'s `>10` cascade-revision threshold **does NOT fire** | Gate `PG-9` counts `D-S1a-<n>` rows **in this ledger alone** — which is exactly the capability `EXC-11`'s discharge restored | **Confirmed: 1 row.** `EXC-11` stays **RESOLVED**; SUB-9 and any later consumer may count from this ledger alone |
| **The `AR-1` register was unioned, not replaced** (7 claims existed, 2 were in the ledger, 6 are now) | Gate `PG-10`/`PG-10b` assert every pre-existing id survives **and** that every ledger id **cited by a node's `notes`** still resolves | **Confirmed.** Node-cited ids — `AR-1/a`, `AR-1/b`, `AR-1-a`, `AR-1-b`, `INC-S1`, `D-S1a` — **all resolve. Zero false claims.** |

**`D-S1a-1`'s own trigger is discharged and recorded, not silently dropped.** Its filer wrote that if
the eight-type audit found `procedural`'s only instances to be this node plus two others **and judged
the drill S5**, it should be re-typed. **NEU-943's audit found `procedural` instantiated by 9 non-root
nodes across all four clusters**, so **union-completeness does not depend on this node's disposition** —
exactly as its filer predicted. The entry stays **open** (the S7/S5 typing question is genuinely thin);
the *risk* it named is gone.

### 3.6 Review remediation — **NEU-947 (C008 SUB-2) row, C008 OUT-1**

**This subsection is strictly ADDITIVE.** NEU-947 modified no row above it and no row below it:
`D-S1`…`D-S5`, `D-S1a`, `D-C1`…`D-C4`, `D-P1`…`D-P4`, §3.1, §3.1a, §3.2, §3.3, §3.4, §3.5, §4, §5,
§7.1 and §7.2 are preserved **byte-for-byte**, for the reason `CV-33` records and `AC-4-P` binds:
**map nodes cite these ids verbatim in `notes`, so a dropped or renamed row turns those notes into
false claims.**

**The `-R` (review-remediation) namespace opens here.** `D-S#`, `D-C#` and `D-P#` belong to NEU-933,
NEU-942 and NEU-944; reusing any would collide or imply their authorship. **`D-R2` onward are
reserved** for C008's other serialized ledger writers, in their dependency order — **SUB-4 (NEU-950)
→ SUB-14 (NEU-954) → SUB-15 (NEU-956)**. The next appender takes the next free `D-R#` and appends a
new terminal subsection here, without needing to read this task.

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-R1`** | **`manifest.yaml`'s `index/00_technique-index.md` entry now RECORDS the `INC-S2` hand-edit exception beside the rule it qualifies — the rule itself is unchanged.** A new `hand_edit_exception` block names the marker (**`INC-S2`**), its **live** status, its holder (**`NEU-933 (NEU-889's SUB-2)`** — never a bare `SUB-2`, which would read as C008's SUB-2 and grant this task the very licence the index forbids), the condition that **ends** it (the technique-index generator landing), this ledger as its authority, and the row that recorded it. **`sole_writer: "generator only"` and `hand_edited: false` are byte-for-byte UNCHANGED** — the exception is recorded **alongside** the rule, never by inverting it. **This closes the two unread Copilot findings on merged PR 612.** `3597733838` (*the manifest says generated, the index says hand-maintained; "these two sources should agree"*) — **UPHELD**: that disagreement was the real, one-sided residue, and this row is its fix. `3597733794` (*the header should match current reality* — read broadly as *an agent cannot tell whether it may edit*) — **REJECTED IN PART**: `../../C005-dp-map/index/00_technique-index.md:51-62` is **self-reconciling**, stating that the generator does not exist, that the file is maintained by hand **by its holder only**, that this is *"a deliberate, temporary, and logged exception to `hand_edited: false` — not a licence for mappers to edit it"*, and that the exception ends the moment the generator lands. A reader of the index therefore **can** determine it may not edit; the index's prose is correct and **was not touched**. **No generator was written or commissioned here**: `INC-S2` stays **OPEN**, §4's two `INC-S2` rows stand untouched, and the missing index generator remains **NEU-889's OUT-7's** under `CAP-S5` — the assignment NEU-944 declined to seize (`INC-P2`, *"Superseded for consumers, not seized."*). | **settled** | The amended `../../C005-dp-map/manifest.yaml` entry for `index/00_technique-index.md`; `../../C005-dp-map/index/00_technique-index.md:51-62`; §4's `INC-S2` rows — NEU-933's original and NEU-944's disposition (*"`INC-S2` therefore remains OPEN for the JSON Schema validator and the index regeneration"*), both unchanged; **the tree's generator inventory**, `find docs/research -name "*.mjs"` → exactly `C005-dp-map-integrity/validator/audit-graph-integrity.mjs`, `C005-dp-map-package/generator/package-completeness-gate.mjs`, `C005-dp-map-package/generator/build-cross-reference.mjs` — a graph-integrity validator, a completeness gate and a cross-reference builder, **none a technique-index generator**, which is what makes the exception **live** rather than ended; PR 612 inline comment ids `3597733838` and `3597733794`; `../../C005-dp-map/manifest.yaml:6-11`, which makes this ledger the manifest's only amendment channel | ✅ 3 recorded — **flipping `hand_edited: true`** (*rejected: it dissolves the rule `sole_writer` protects and licenses exactly the mapper edits the index explicitly forbids; the defect was the manifest's SILENCE about an exception, not the rule itself*); **a prose-only extension of the existing `note:`** (*rejected: an audit or agent keying on the `hand_edited` FIELD still reads an unqualified `false`; only a structured block makes the entry self-describing to a field reader as well as a prose reader*); **rewriting `00_technique-index.md`'s header to match the manifest** (*rejected: that is literally what thread `3597733794` asked for, and it would destroy the honest, self-reconciling record of a live logged exception — the file is `sole_writer: "generator only"` and is not this task's to rewrite; the correction belonged on the manifest side*) | **The technique-index generator lands** — the exception **ends**, the `hand_edit_exception` block's `status` stops reading `"live"` (the file becomes generated output), and this row is re-adjudicated. Also revised if `INC-S2` is re-disposed or re-routed by its owner. | **This task — NEU-947 (C008 SUB-2)** — for the manifest record and the two PR-612 verdicts. **`INC-S2` itself, and the missing index generator, remain NEU-889's coverage-audit sub-task's (OUT-7, `CAP-S5`)** — cited here, untouched here. |

### 3.7 Review remediation — **NEU-950 (C008 SUB-4) row, C008 OUT-1 + OUT-4**

**This subsection is strictly ADDITIVE.** NEU-950 modified no row above it and no row below it:
`D-S1`…`D-S5`, `D-S1a`, `D-C1`…`D-C4`, `D-P1`…`D-P4`, `D-R1`, §3.1, §3.1a, §3.2, §3.3, §3.4,
§3.5, §3.6, §4, §5, §7.1 and §7.2 are preserved **byte-for-byte**, for the reason `CV-33` records
and `AC-4-P` binds: **map nodes cite these ids verbatim in `notes`, so a dropped or renamed row
turns those notes into false claims.**

**`D-R3` onward remain reserved** for C008's remaining serialized ledger writers, in their
dependency order — **SUB-14 (NEU-954) → SUB-15 (NEU-956)**. The next appender takes the next free
`D-R#` and appends a new terminal subsection here, without needing to read this task.

**The one thing a cold reader should take from this row**, stated before the table because it is
the row's whole point: **the severity vocabulary contains no multi-effect aggregation rule, and
C008 supplies one here rather than pretending one was always there.** Everything else in this row
is that rule applied, plus one ruling the rule alone could not settle.

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-R2`** | **THE AGGREGATION RULE, stated by C008 to fill a gap the severity vocabulary documents by omission: _a node's severity is the most severe of the severities its individual effects earn under the written tests._** Severity order, most severe first: `blocking` > `correctness-risk` > `idiom-shift` > `performance`. **This is C008's own gap-filling package-level decision, NOT a rule that was already there** — `../../C005-dp-js-materiality/00_method-and-scope.md:119-124` gives four per-effect tests and **no rule for which effect governs when several apply**, which is exactly why the two mismatches below were reachable. It is recorded here so a cold reader can find and **contest** it. **Corollary that fixes where a mitigating criterion may be applied: most-severe-wins is a pure maximum and has NO DOWNGRADE STEP**, so a mitigating criterion enters **when an individual effect is scored against its own written test, or not at all** — applying one after the scores are assigned would subtract from a maximum the rule itself just fixed. — **THE PER-EFFECT SCORES**, so every maximum below is **reconstructable rather than asserted**: **`JS-E8` → `idiom-shift`** (test `:123`, which names *"a different container"* — the heap/ordered-multiset gap is hand-rollable, correct, and asymptotically unchanged); **`JS-E2` → `correctness-risk`** (test `:122` — correct at the sizes the technique usually meets, but a reachable range crosses 2^53 and rounds **silently**); **`JS-E1` → `blocking`** (test `:121`, **both** clauses firing — the direct transcription **crashes** with `RangeError: Maximum call stack size exceeded` at competitive tree sizes, and an explicit stack is ***required, not preferred***). — **THE TWO RE-ADJUDICATIONS**, each a maximum over its own node's effects, and **both shipped `NEU-889 OUT-5` verdicts are CHANGED**: `../../C005-dp-map/nodes/cl-4-optimization/frontier.yaml` `cl-4.slope-trick-heap-implementation` (`effects: ["JS-E8","JS-E2"]`) `idiom-shift` **→ `correctness-risk`**; `cl-4.slope-trick-on-trees` (`effects: ["JS-E8","JS-E1"]`) `idiom-shift` **→ `blocking`**. Each node's `rationale` was rewritten in the same commit so severity and prose state the same verdict and cite this row. — **THE SURMOUNTABILITY RULING (the open judgment C008 did not pre-decide).** `frontier.yaml:636` read *"idiom-shift rather than blocking because both obstacles are surmountable"* — a criterion the vocabulary does not contain. Per the corollary above the question is asked **at per-effect scoring**, never at aggregation, and is exactly: *does surmountability legitimately lower `JS-E1`'s own per-effect score?* **RULED: NO. The `:636` criterion is OVERRIDDEN** — explicitly, on the record, **not silently overwritten**. Four grounds: (1) `:121` asks ***necessity, not difficulty*** — surmountability is a difficulty claim and answers a question the test does not pose; (2) `:123`'s *"feasible and correct"* describes **the direct translation**, not the post-substitution rewrite — read as a set the four tests partition on *what the direct transcription does*, and here it does not run at all; (3) `:123`'s enumerated differences are **representational** (*container, sentinel, encoding, failure signature*), whereas recursion → explicit stack is a control-flow substitution forced by a hard engine limit; (4) **decisive — the contrary reading empties `blocking`**: the effect catalogue names a JavaScript technique that recovers **every** effect, and `:121` itself presupposes a required JavaScript-specific technique **exists**, so if surmountability lowered a score no node could ever earn `blocking`, and a test whose extension is necessarily empty is not a test. **What `:636` was right about is preserved, not deleted**: the obstacles *are* surmountable, which is why this node is a large acquisition rather than a dead end — it simply does not set the severity. | **settled** | `../../C005-dp-js-materiality/00_method-and-scope.md:119-124` (the four tests, and the **absence** of a fifth aggregation row — the gap this row fills); `../../C005-dp-js-materiality/01_effect-catalogue.md` `JS-E1` §`:17-37`, `JS-E2` §`:39-67`, `JS-E8` §`:161-176`; the amended `../../C005-dp-map/nodes/cl-4-optimization/frontier.yaml` nodes `cl-4.slope-trick-heap-implementation` and `cl-4.slope-trick-on-trees`; the single-effect precedents `frontier.yaml:819-820` and `cl-2-combinatorial.yaml:2480-2481`; the **live single-effect `JS-E1` census over the merged tree — eight nodes**, weighed as corroboration and not as authority (`cl-1-foundational.yaml:1244`, `cl-2-combinatorial.yaml:2303`, `:2480`, `:2547`, `:3025`, `:3387`, `:3639`, `:3734`); PR 631 inline comment ids `3598581427`, `3598581455`, `3598581477`, `3598581497`, all four unreplied until this task; `../../C005-dp-map/manifest.yaml:6-11` and `:37-40`, which make this ledger the only channel for a status or severity flip | ✅ 4 recorded — **revising the severity vocabulary itself to add an aggregation row** (*rejected: explicitly OUT of C008's scope — OUT-4 states the missing rule and applies it, it does not reopen the four severity tests, which are `NEU-889 OUT-5`'s*); **applying surmountability as an aggregate downgrade after the per-effect scores were assigned** (*rejected: most-severe-wins has no downgrade step, so this contradicts the rule in the same breath it is recorded — and it is the framing that made OUT-4 undecidable, since it yields opposite results depending on an unstated level*); **silently rewriting `frontier.yaml:636` to match the new severity** (*rejected: the criterion was load-bearing for a shipped verdict, so removing it without a ruling destroys the record of why the verdict was ever `idiom-shift` — OUT-4 requires it upheld or overridden ON THE RECORD*); **correcting the `JS-E1` precedent nodes in place so the file agrees** (*rejected: they lie outside OUT-4's two-node bound; under this ruling they need no correction anyway, and seizing them would be exactly the local re-decision `AC-7-C` forbids*) | **The severity vocabulary gains an aggregation rule of its own** — this row is then superseded rather than merely re-cited. Also revised if a multi-effect node is found whose correct severity the maximum demonstrably mis-scores, or if `00_method-and-scope.md:119-124`'s tests are themselves revised by their owner. | **This task — NEU-950 (C008 SUB-4)** — for the aggregation rule, the per-effect scores, the surmountability ruling, the two re-adjudicated verdicts and the four PR-631 replies. **The severity vocabulary itself remains `NEU-889`'s SUB-8/NEU-941's** — cited here, untouched here. |

#### 3.7.1 Disposition of the single-effect precedents — the routed-finding obligation, discharged

OUT-4 requires **every** single-effect precedent to reach exactly one of three recorded states —
*no longer conflicts*, *explicitly distinguished on the record*, or *the conflict recorded as a
routed finding naming the nodes left at odds and the owner the correction falls to*. **None is
corrected here**; all lie outside OUT-4's two-node bound.

| Precedent | Effect, alone | Its severity | Recorded state |
| --- | --- | --- | --- |
| `frontier.yaml:820` — `cl-4.aliens-trick-application` | `JS-E2` | `correctness-risk` | **No longer conflicts** — identical to `D-R2`'s `JS-E2` score. It scored on *stronger* facts (*"ROUTINELY exceeds `Number.MAX_SAFE_INTEGER`"*), so it bounds the score from above and the agreement is not a coincidence. |
| `cl-2-combinatorial.yaml:2481` — `cl-2.implement-tree-dp-post-order-dfs` | `JS-E1` | `blocking` | **No longer conflicts** — identical to `D-R2`'s `JS-E1` score. Same effect on the same traversal shape; `frontier.yaml:643` cross-references **this very node**. |
| `cl-1-foundational.yaml:1245`; `cl-2-combinatorial.yaml:2304`, `:2548`, `:3026`, `:3388`, `:3640` | `JS-E1` | `blocking` (×6) | **No longer conflict** — same score, same reasoning. |
| `cl-2-combinatorial.yaml:3735` — `cl-2.debug-cyclic-state-dependency` | `JS-E1` | `idiom-shift` | **Explicitly distinguished** — see below. |

**Why `:3735` is distinguished and not a conflict.** It is the one single-effect `JS-E1` node not
marked `blocking`, so it reads as an outlier until its rationale is examined. Its `JS-E1` is **not
scored on a compelled traversal rewrite at all**: it is a **diagnosis** node, and `:3737-3751`
records that JavaScript throws *the same error at the same threshold* for a cyclic state graph as
for a correct-but-deep one, so the symptom stops identifying the cause and an extra disambiguation
step is needed. That is scored on `:123`'s **"failure signature"** clause — which `idiom-shift`'s
test names **verbatim** — not on `:121`'s crash-and-compelled-substitute clause. **Same effect id,
a different aspect of it, a different written test met.**

**No routed finding is required, and that is a result rather than an omission.** The obligation is
discharged by the states above, **not skipped**: because the ruling is *no*, **nothing is lowered**,
so no node falls at odds with the rule. Had the ruling been *yes*, `JS-E1`'s per-effect score would
have dropped to `idiom-shift` and **all seven** `blocking` nodes above would have been left at odds
with `D-R2` and outside OUT-4's bound — mandating a routed finding naming those seven nodes and
their owner (**NEU-935 / NEU-934**, the CL-2 and CL-1 mappers, via a challenge filed against this
row). **This counterfactual is recorded so a cold reader can see the obligation was evaluated, not
overlooked** — and so that anyone who later contests `D-R2`'s ruling inherits the routing it would
have required. **Under either branch the `JS-E1` precedent base is now internally consistent**, the
`:3735` distinction holding on both.

### 3.8 Dimension-value recomputation — **NEU-954 (C008 SUB-14) row, C008 OUT-7 (repair half)**

**Model:** claude-opus-5[1m]

**This subsection is strictly ADDITIVE.** NEU-954 modified no row above it and no row below it:
`D-S1`…`D-S5`, `D-S1a`, `D-C1`…`D-C4`, `D-P1`…`D-P4`, `D-R1`, `D-R2`, §3.1, §3.1a, §3.2, §3.3,
§3.4, §3.5, §3.6, §3.7, §3.7.1, §4, §5, §7.1 and §7.2 are preserved **byte-for-byte**, for the
reason `CV-33` records and `AC-4-P` binds: **map nodes cite these ids verbatim in `notes`, so a
dropped or renamed row turns those notes into false claims.**

**`D-R4` onward remain reserved** for C008's remaining serialized ledger writer — **SUB-15
(NEU-956)**. The ledger is written in strict series: SUB-2 → SUB-4 → SUB-14 → SUB-15.

**The one thing a cold reader should take from this row**, stated before the table: **this row
records CHANGED VALUES ONLY. It does NOT close `D-P2`, does NOT supersede `AC-6-P`, and touches no
register's `Status`.** Those are claims about `F-943-1`'s *state*, and they are **SUB-15
(NEU-956)'s**. A reader who finds this row beside a `D-P2` that still reads `unresolved` is looking
at **the honest intermediate state**, not an inconsistency.

**Every count below was RE-ESTABLISHED by this slice's own run, never copied from the register.**
They happen to agree with `../../C005-dp-map-package/03_open-items-and-provisional-register.md:35-49`
(26 / 6 / 5), and that agreement is a *finding*, not an assumption carried forward.

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-R3`** | **The map's `prerequisite_depth`, `progression_stage` and `entry_gate` are RECOMPUTED over the edge-complete (post-NEU-939) graph and rewritten to agree with it.** NEU-940 computed these against the **pre-NEU-939** graph, before it was edge-complete; the values have disagreed with the rubric ever since (`F-943-1`, HIGH, live, and — until C008 — unowned). **THE RE-ESTABLISHED COUNTS: 26 of 179** dimension-bearing nodes carried a wrong `prerequisite_depth`, **every one of them UNDER-reported, zero over-reported** (unanimity is the tell that one systematic cause, not 26 independent slips, is at work). Split **CL-3 17 · CL-4 9** (`cl-4-optimization/mainstream.yaml` 7, `cl-4-optimization/frontier.yaml` 2). **CL-1 and CL-2 carried ZERO wrong depths** and are not write targets. **16** nodes changed `progression_stage`, **all of them to `PS-4`** (13 CL-3, 3 CL-4-mainstream); **1** node changed `entry_gate`. **43 value lines changed in total (26 + 16 + 1), across exactly three node files.** — **THE STAGE INVERSIONS: 6 before, 0 after**, over the **25** cross-cluster edges the validator checks, concentrated **entirely** in the cross-cluster class (`intra_cluster` 293 checked / 0 inversions, both before and after). The 6 fell across **5** distinct dependent nodes — `cl-3.bitmask-state-encoding`, `cl-3.formulate-digit-dp`, `cl-3.formulate-automaton-dp`, `cl-4.divide-and-conquer-optimization` (**two** of the 6), `cl-4.knuth-yao-optimization`. They are not fixed by hand: **a longest-path depth makes `depth(dependent) >= 1 + depth(prerequisite)` by construction, so a stage that is a monotone function of that depth cannot inverse-order a drawn edge.** The inversions vanish as a *consequence* of the recomputation, which is why none is separately adjudicated. — **`entry_gate` IS RE-DERIVED ON ALL 179, NEVER CARRIED FORWARD.** It is a **documented pure function** of depth, not an independent field: `../../C005-dp-progression/01_progression-stages.md:78-84` and `:150-152` fix **`gate-a` iff PS-1 (depth 1)** and **`gate-c` for PS-2/PS-3/PS-4 (depth >= 2)**. Only **one** node's gate actually moved — `cl-3.bitmask-state-encoding`, `gate-a` → `gate-c`, the live example the charter cites at `../../C005-dp-map/nodes/cl-3-state-compression.yaml:273-276` (it read `PS-1`/`gate-a`/depth 1 against a rubric depth of **4**). **That the other 178 re-derivations reproduced the existing value is a RESULT, not a reason to have skipped them:** recomputing stages while leaving gates alone would have shipped **PS-4 nodes carrying `gate-a`** with every executable green — a new inconsistency introduced by the remediation itself, and the charter's own **High** "second false all-clear" risk. — **THE DISTRIBUTIONS, handed to SUB-15** (`F-943-3` stakes present-tense claims on exactly these, and **all of them moved**): `progression_stage` **PS-1 20 · PS-2 32 · PS-3 36 · PS-4 91 → PS-1 19 · PS-2 26 · PS-3 27 · PS-4 107**; `entry_gate` **`gate-a` 20 · `gate-c` 159 → `gate-a` 19 · `gate-c` 160**. Over the 179, **`gate-b`, `gate-d` and `gate-e` are instantiated by NO node** — **reported mechanically, not ruled on**: whether `F-943-3` closes, and on which limbs, is **SUB-15's** adjudication against this evidence, not this row's. — **THE 6 UNEXPLAINED VALUES — residual cause `unestablished`, and NOT absorbed into the corrected set.** The stated root cause (NEU-940 computing against the pre-939 graph) explains **20 of the 26**: those 20 declared depths *exactly equal* the depth recomputed with the NEU-939 cross-cluster edges removed. **The other 6 match NEITHER graph** — the `cl-4.matrix-exponentiation-dp` family: `cl-4.matrix-exponentiation-dp` (declared 4 \| rubric 5 \| pre-939 2), `cl-4.construct-transfer-matrix` (5 \| 6 \| 3), `cl-4.implement-modular-matrix-power` (6 \| 7 \| 4), `cl-4.recognize-matrix-exponentiable-dp` (5 \| 6 \| 3), `cl-4.smawk-application` (5 \| 6 \| 4), `cl-4.larsch-online-smawk-implementation` (6 \| 7 \| 5). **They are corrected exactly like the other 20 — the recomputation derives from the GRAPH, not from the diagnosis — but they are recorded here as UNEXPLAINED and must not be presented as explained by the stated cause.** (Distinct from the 6 *stage inversions*; a different set, not to be conflated.) The recomputation did **not** turn out to explain them. — **THE AGREEMENT NOW HAS AN EXECUTABLE HOME.** `../../C005-dp-map-integrity/validator/audit-graph-integrity.mjs` gains **§H3**: two `check()`ed, **build-fatal** `entry_gate` assertions over the 179 — **limb 1** (gate vs the node's own **declared** stage) and **limb 2** (gate vs the stage its **rubric-computed** depth implies, reusing the file's existing `depth()`; no second depth engine). **Both limbs were required, and limb 1 alone would have been a fraud:** pre-fix, **zero** nodes disagreed with their own declared stage, so a one-limb check **could not have failed** on the broken tree — reproducing precisely the un-failable weakness `F3.6` caught in the annotation findings. **The 8 frozen roots are bounded OUT** of both limbs: they carry `{}` and no `entry_gate`, sit at depth 0 by construction, and are neither swept into `gate-c` nor reported as violations. | **settled** | **The validator, run four ways, and the ANNOTATION FINDINGS line — never the `STRUCTURAL VERDICT`, which read `PASS` at exit 0 on the broken tree and would have been satisfied by doing nothing (`F3.6`).** (1) **Pre-fix baseline:** `28/28 passed`, `VERDICT: PASS`, **exit 0**, while reporting `26 depth mismatches, 6 stage inversions`. (2) **Repaired tree:** `ANNOTATION FINDINGS … 0 depth mismatches, 0 stage inversions`, `30/30 passed`, **exit 0**, both new limbs PASS, `cross_cluster checked 25 \| inversions 0`. (3) **Negative control A — the extended validator against the PRE-FIX node values:** **limb 2 FAILS**, `29/30`, **exit 1**, naming `cl-3.bitmask-state-encoding rubric depth 4 => PS-4 requires gate-c, declares gate-a` — **the proof the new assertion can actually fail**, on the very tree that used to pass. (4) **Negative control B — a deliberately mismatched gate on the repaired tree:** **both limbs FAIL**, `28/30`, **exit 1**; reverted, and no mismatched gate ships. `../../C005-dp-progression/01_progression-stages.md:78-84`, `:120-123`, `:150-152` (the depth→stage and stage→gate rules this row applies rather than decides); `../../C005-dp-map-package/03_open-items-and-provisional-register.md:35-49` (the register's 26/6/5, re-established and found to agree); `../../C005-dp-map/manifest.yaml:6-11`, which makes this ledger the only channel through which a `settled` value changes | ✅ 4 recorded — **hand-patching the 26 named nodes** (*rejected: the register's list is a symptom inventory, not a derivation; patching it would leave every OTHER node's value unverified and would not re-derive the gates at all — the recomputation covers all 179 and is reproducible by anyone re-running the validator*); **recomputing the depths and stages but leaving `entry_gate` alone** (*rejected: this is the defect the slice came to fix, reproduced — it would ship PS-4 nodes carrying `gate-a`, invisible to every executable in the package, and would be filed as closed by SUB-15. `entry_gate` is a documented function of stage, so leaving it is not "no change", it is a NEW wrong value*); **making the new gate assertion a `console.log` annotation beside H1/H2** (*rejected: that is exactly `F3.6`'s finding — a pass that cannot fail proves nothing. The whole point of §H3 is that it is wired into `check()` and sets the exit code*); **silencing `package-completeness-gate.mjs`, which starts failing the moment this merges** (*rejected: it correctly asserts a state that has just stopped being true. It is a research script under `docs/research/`, not a repo CI check, so nothing in the pipeline goes red. It is NOT edited by this slice — clearing it is SUB-15's, and doing it here would rebuild the oversized slice the F2.13 split exists to avoid*) | **The graph's edges change again** — any edge added, removed or re-targeted moves the longest-path depths, and depth → stage → gate must be recomputed together, in one change, through a new ledger row. §H3's limb 2 is the detector: it fails the build the moment a graph edit outruns a value recomputation. Also revised if the **PS granularity itself** is re-bucketed by its owner (`01_progression-stages.md:100-106`, `PS-GRAN`, deferred to the creator's progression-plausibility review) — that re-buckets stages **without** touching `prerequisite_depth`, which is why the exact depth is stored per node. Also revised if the **residual cause of the 6** is ever established. | **This task — NEU-954 (C008 SUB-14)** — for the recomputed values, the re-derived gates, the §H3 assertion and the counts handed downstream. **The RECORD half of `F-943-1` — `D-P2`, `AC-6-P`, both registers, the PG-7 block, the cross-reference generator and its view, and the package README — is SUB-15 (NEU-956)'s**, untouched here. **The PS-granularity flag stays NEU-940's owner's**, cited here, untouched here. |

**The 26 depth-corrected node ids, enumerated so the count is reconstructable rather than asserted** (`declared → rubric`; the **6 bolded** are the unexplained family, matching neither graph):

- **CL-3 (17)**, all in `../../C005-dp-map/nodes/cl-3-state-compression.yaml`: `cl-3.bitmask-state-encoding` (1→4) · `cl-3.recognize-bitmask-state-applicability` (2→5) · `cl-3.submask-enumeration` (2→5) · `cl-3.implement-bitmask-dp` (3→6) · `cl-3.subset-cover-transition-correctness` (3→6) · `cl-3.diagnose-bitmask-state-blowup` (4→7) · `cl-3.formulate-digit-dp` (2→6) · `cl-3.implement-digit-dp` (3→7) · `cl-3.digit-dp-bound-correctness` (3→7) · `cl-3.recognize-digit-dp-under-unfamiliar-surface` (3→7) · `cl-3.broken-profile-state-encoding` (2→5) · `cl-3.plug-dp-connectivity-encoding` (3→6) · `cl-3.implement-plug-dp` (4→7) · `cl-3.formulate-automaton-dp` (2→4) · `cl-3.implement-aho-corasick-dp` (3→5) · `cl-3.steiner-tree-dp-state-encoding` (4→7) · `cl-3.implement-steiner-tree-dp` (5→8)
- **CL-4 (9)** — `cl-4-optimization/mainstream.yaml` (7): `cl-4.divide-and-conquer-optimization` (2→5) · `cl-4.implement-divide-and-conquer-optimization` (3→6) · `cl-4.knuth-yao-optimization` (3→5) · **`cl-4.matrix-exponentiation-dp` (4→5)** · **`cl-4.construct-transfer-matrix` (5→6)** · **`cl-4.implement-modular-matrix-power` (6→7)** · **`cl-4.recognize-matrix-exponentiable-dp` (5→6)**; `cl-4-optimization/frontier.yaml` (2): **`cl-4.smawk-application` (5→6)** · **`cl-4.larsch-online-smawk-implementation` (6→7)**

**The 16 stage changes** are the 17 CL-3 ids above minus `cl-3.diagnose-bitmask-state-blowup`, `cl-3.implement-plug-dp`, `cl-3.steiner-tree-dp-state-encoding` and `cl-3.implement-steiner-tree-dp` (already `PS-4`), plus `cl-4.divide-and-conquer-optimization`, `cl-4.implement-divide-and-conquer-optimization` and `cl-4.knuth-yao-optimization` — **every one of them to `PS-4`**. **The single gate change** is `cl-3.bitmask-state-encoding`. **The 5 inversion-bearing dependents** were `cl-3.bitmask-state-encoding`, `cl-3.formulate-digit-dp`, `cl-3.formulate-automaton-dp`, `cl-4.divide-and-conquer-optimization` (two edges) and `cl-4.knuth-yao-optimization`.

#### 3.8.1 Two expected intermediate states, and one routed finding discharged by measurement

**Both of the following are correct at this moment and are SUB-15's to clear. Neither is a defect to route around.**

| State | Why it is correct now | Owner |
| --- | --- | --- |
| `../../C005-dp-map-package/generator/package-completeness-gate.mjs` **FAILS** after this merges | It still asserts `F-943-1` is open and that 26 nodes carry a warning marker. **Both have just stopped being true.** The file is **not edited by this slice** — not one byte. It is a research script under `docs/research/`, not a repo CI check, so nothing in the delivery pipeline goes red. | **SUB-15 (NEU-956)** |
| `D-P2` still reads **`unresolved`** beside this row | The values are corrected; the *status claim* has not yet caught up. `D-P2` closes when its own revision trigger is discharged **on the record** — a claim SUB-15 makes deliberately, never a side effect of a value edit. | **SUB-15 (NEU-956)** |

**Routed finding `RR-3` — discharged by measurement, not dropped.** PR 651's finding 5 was rejected on the map (the populated `difficulty_dimensions` **are** NEU-940's discharge of `INC-S3`, and collapsing them to `{}` would delete owner-delivered work), and its residue — **the specific `prerequisite_depth` / `progression_stage` / `entry_gate` VALUES in `../../C005-dp-map/nodes/cl-1-foundational.yaml`** — was routed forward to **NEU-954** as part of `F-943-1`. **It is answered here, by measurement:** this slice's recomputation covers **all 179** dimension-bearing nodes, **including all 46 CL-1 non-root nodes**, and **every one of them already agreed** — **0 depth, 0 stage and 0 gate corrections in CL-1**, which is why `cl-1-foundational.yaml` is **not** a write target of this slice. **"No edit required" here is a verified result, not an omission.** **RR-3's other half — the schema doc `../01_node-and-edge-schema.md:208` still citing the discharged `INC-S3` as live authority — is NOT this slice's** and remains **NEU-947 / NEU-950's**, exactly as PR 651's disposition routed it.

**One prose site is knowingly left stale, and is named rather than silently fixed:**
`../../C005-dp-progression/01_progression-stages.md:108` still reports the **pre-recomputation** observed distribution (*"PS-1 20 · PS-2 32 · PS-3 36 · PS-4 91 … PS-4 holds 51% of the graph"*). The post-recomputation figures are in `D-R3` above. That file is **NEU-940's own progression package** — a package self-description, which the F2.13 split assigns to **SUB-15 (NEU-956)** along with every other record-closure site. **Naming it here is what keeps it from being lost;** correcting it here would be the scope creep the split exists to prevent.

### 3.9 Record closure of `F-943-1` — **NEU-956 (C008 SUB-15) row, C008 OUT-7 (record half)**

**Model:** claude-opus-5[1m]

**This subsection is strictly ADDITIVE.** NEU-956 modified no row above it:
`D-S1`…`D-S5`, `D-S1a`, `D-C1`…`D-C4`, `D-P1`, `D-P3`, `D-P4`, `D-R1`, `D-R2`, `D-R3`, §3.1,
§3.1a, §3.2, §3.3, §3.5, §3.6, §3.7, §3.7.1, §3.8, §3.8.1, §4, §5, §6 and §7.1 are preserved
**byte-for-byte**, as is **every row of §3.4 except `D-P2`**, for the reason `CV-33` records and
`AC-4-P` binds: **map nodes cite these ids verbatim in `notes`, so a dropped or renamed row turns
those notes into false claims.**

**Exactly two pre-existing rows are touched, each in one of the only two ways `AC-4-P` permits, and
both are named here — with the full extent of the edit stated — rather than left for a reader to
discover.** `D-P2` (§3.4) changes in **three** disclosed ways, and in no others: (i) its **`Status`
flips `unresolved` → `resolved`** — a status change is precisely what a ledger row is *for*; (ii)
the clauses inside its Decision, Revision-trigger and Owner cells that were **present-tense claims
now false** are **put into the past tense** (*"is an OPEN DEFECT"* → *"WAS an OPEN DEFECT"*,
*"would order 6 dependencies backwards"* → *"would have ordered"*) — the row is a record of a
decision, and re-tensing a claim that has stopped being true is not the same as rewriting the
decision; (iii) a resolution note is **appended** in-row recording that the named revision trigger
**fired** (`D-R3`) and that the *"`F-943-3` closes with it"* clause was a **forecast that did not
govern**. **The row's substance, its evidence, its rejected alternatives and its account of why it
was ever opened are kept in full, and nothing is deleted.** A reader comparing `D-P2` against
`94ddf22` should expect exactly those three differences. **`AC-6-P`'s cell is preserved VERBATIM and a discharge note is APPENDED** — never rewritten
in place — on the `INC-S3` precedent §4 already carries and `AC-4-P` already blesses (*"its text is
preserved **verbatim** and a discharge note is **appended** to its Owner cell"*). **No row is
deleted, renamed, or rewritten. Every pre-existing id survives; `PG-10` asserts it.**

**The `-R` namespace closes here.** `D-R1`…`D-R4` are C008's four serialized ledger writers —
**SUB-2 (NEU-947) → SUB-4 (NEU-950) → SUB-14 (NEU-954) → SUB-15 (NEU-956)** — and SUB-15 is the
last. A later charter opens its own namespace; it does not continue this one.

**The one thing a cold reader should take from this row**, stated before the table because it is
the row's whole point: **the VALUES were `D-R3`'s, and this row closes the RECORD — against a
committed, re-runnable SEARCH, not against anybody's list of sites.** Three successive charter
enumerations of `F-943-1`'s binding sites each shipped **incomplete**. The search replaces the
list, and with it the *"floor, not the ceiling"* escape clause those enumerations leaned on is
**retired**: a site is in scope because the script returns it, and the check on this closure is
re-running the script.

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-R4`** | **The RECORD half of `F-943-1` is CLOSED, and it is closed everywhere the record is carried.** The corrected **values** are `D-R3`'s (NEU-954) and are not re-decided here; what closes here is the *state claim* — across **both registers** (`../../C005-dp-map-package/03_open-items-and-provisional-register.md`, `../../C005-dp-map-integrity/05_findings-register.md`), **`D-P2`** (which resolves in the same change, **as to `F-943-1` only**), **the `PG-7` block** of `../../C005-dp-map-package/generator/package-completeness-gate.mjs`, **the cross-reference generator** `../../C005-dp-map-package/generator/build-cross-reference.mjs` **and its regenerated view** `../../C005-dp-map-package/01_cross-reference-view.md`, **the package README**, and **NEU-940's own progression package** `../../C005-dp-progression/01_progression-stages.md`. — **THE SCOPE IS A SEARCH, NOT A LIST — and that substitution is the load-bearing decision in this row.** The closure is bounded by `../../../wf-plans/C008__close-the-neu-889-fleets-unread-review-findings/09_f-943-1-discovery.sh`, a **three-limb** grep run whose verbatim output is committed beside it and whose every de-duplicated site is classified one per row in `09_f-943-1-discovery-record.md`. **The site and per-limb counts are stated in that record and deliberately NOT restated here** — the search scans `docs/research/`, which includes this ledger, so a count hard-coded in this row would be a number the search itself perturbs, and it would go stale the moment any wording here changed. **The record, and a re-run of the script, are the counts' single home.** **Limb 1** searches the finding **ids**; **limb 2** the corrected **values** — `prerequisite_depth`, `progression_stage`, `entry_gate`, the `PS-#` and `gate-#` literals; **limb 3** the **derived statistics** — stage distribution, PS-4 share, depth range and histogram, gate counts, affected-node counts, the uninstantiated-gate claim. Every site is classified **live / generated / historical** and treated by its class: a live claim is corrected, a generated one is **regenerated from an edited source**, a historical one is left standing because a narrative of what *was* true is not a false present-tense claim. **Limb 3 reached 10 files limb 1 did not** — canonically `../../C005-dp-progression/01_progression-stages.md`, which carries the stage distribution, the PS-4 share and the depth range while containing **zero** `F-943-` matches (`grep -c 'F-943-'` → **0**). An id-only search returns nothing for that file, which is exactly how it survived three enumerations. **The enumerations were not careless; enumeration is the wrong instrument**, and the *"floor, not the ceiling"* escape clause that made an incomplete list survivable is **retired** — re-running the script is now the check. — **THE `F-943-3` THREE-LIMB SUBSTANCE RE-CHECK, ESTABLISHED BY MEASUREMENT AGAINST THE POST-`D-R3` MAP AND NOT INHERITED FROM ANY REGISTER'S FORECAST.** Measured over all **179** dimension-bearing non-root nodes. **(1) The inheritance limb — DISCHARGED:** `F-943-3` inherited `F-943-1`, and the stages are now correct and `entry_gate` was **re-derived from them** on all 179. **(2) The determinism / redundancy limb — STILL TRUE:** `PS-1` → `gate-a` only and `PS-2`/`PS-3`/`PS-4` → `gate-c` only, **0 exceptions**. `D-R3` re-derived every gate *as* that function, so the redundancy `F-943-3` reports **survives the repair exactly as it was** — which is why **`F-943-3` STAYS OPEN (Low)**. Its stale counts `×20` / `×159` are corrected to **`gate-a` 19 / `gate-c` 160**. **(3) The uninstantiated-gates limb — re-checked, and the registers' own text was INCOMPLETE:** they say *"Gates B and D"*; measured, **`gate-b`, `gate-d` AND `gate-e` are instantiated by no node**, and the corrected claim names all three. The surviving limb is routed to **NEU-940 / NEU-888**. — **`D-P2`'s *"`F-943-3` closes with it"* CLAUSE WAS A FORECAST, AND IT DID NOT GOVERN.** It was written before the re-run existed and predicted a joint close; **the re-check contradicted it, and the evidence decided.** Read as an instruction it would have produced the one defect this slice was hunting — a finding filed closed on a prediction while its substance is still true. **The clause is corrected, not executed**, in `D-P2` and in the register at `03_…:48`, and `F-943-3`'s own status fields are left reading **open** because that is what they truly are. — **THE `PG-7` REWORK — the block now asserts the closure is EVIDENCED, never merely announced.** A closure that erases what it closed is worth nothing, and a deleted block would also have made the gate green, which is why `PG-7` is **reworked rather than removed**: `PG-7a` asserts **no** node carries the per-node `F-943-1` warning marker and `PG-7a2` asserts **no** block names a stage inversion — both hold **because nothing computed them**, the marker being emitted by the generator from its own `depthBad`/`invByNode` sets, so re-breaking one declared depth brings the marker back and fails the gate; `PG-7b0` asserts the register **keeps** a dedicated `F-943-1` section; `PG-7b` that the section records it **closed**; `PG-7c` that the closure **names its discharging ledger entry**; `PG-7d` that the section records **what** discharged it — the edge-complete re-run and its counts. **`PG-7e` is RETIRED — discharged, not inverted, and not silently dropped:** its subject was *the repair's ownership* (*"the `F-943-1` repair is explicitly recorded as OUT of SUB-11's scope"*), a true claim about the package's shipping state that **C008 made false by performing the repair**; a closed finding has no *"the fix is out of scope"* analogue to restate, so the assertion has **no closed-state successor**, and its retirement reason is recorded **in the gate's own comment** because a vanished assertion is indistinguishable from a lost one. **`PG-7f` is NEW** and is the block's anti-false-pass check: it asserts the ledger entry the register **names** as discharging the finding **actually resolves as a ledger row** — matching `^\|\s*\*\*`D-R#`\*\*\s*\|`, so *reservation prose* cannot satisfy it and only a landed row can. That is what keeps the gate from passing on a register that cites an id which does not exist. **The gate's check count is UNCHANGED at 38** — `PG-7f` takes `PG-7e`'s place rather than padding the total. — **THE GENERATOR'S UNGATED LITERALS WERE EDITED AT SOURCE AND THE VIEW WAS REGENERATED — never hand-edited.** `01_cross-reference-view.md` is `sole_writer: "generator only"`; hand-correcting it would produce a view that disagrees with its own emitter and would be overwritten by the next run. **The 26 per-node markers are gone because NOTHING COMPUTED THEM**, not because prose was deleted — the gated `if (depthBad.has(n.id) \|\| invByNode.has(n.id))` branch is **left intact**, since editing it would destroy the machinery that surfaces a *future* mismatch. **`F-943-2`'s and `F-939-2`'s own literals stand** — both are separate, still-open findings, and neither status is touched; the **only** change on that side is `F-939-2`'s stale passing `F-943-1` cross-reference, moved to **past tense**. — **THE 6 RESIDUAL `cl-4` VALUES CARRY CAUSE `unestablished`, and this row RECORDS that without resolving or explaining it.** The `cl-4.matrix-exponentiation-dp` family and the two SMAWK nodes were corrected with the other 20, but their declared values match **neither** the pre-939 nor the post-939 graph. `D-R3` recorded the residual cause as `unestablished`; **this row carries that word forward unchanged.** A closed `F-943-1` does **not** mean every value's history is understood — it means every value now agrees with the graph. | **settled** | **The discovery is committed and re-runnable, and it — not any narrative — is the scope of this closure:** `../../../wf-plans/C008__close-the-neu-889-fleets-unread-review-findings/09_f-943-1-discovery.sh` (the three-limb search), **and it is committed as TWO captures, because a single one could not be honest**: `…/09_f-943-1-discovery-output.txt` is the **post-treatment** capture taken on the shipped tree — **this is the one a reviewer reproduces by simply re-running the script** (byte-identical across repeated runs, sorted, no timestamps and no absolute paths), while `…/09_f-943-1-discovery-baseline-output.txt` is the **pre-treatment** capture at `94ddf22`, the hit set this slice actually classified and treated, reproduced by re-running the same script against that commit. **A capture taken before the edits cannot be reproduced after them** — the treatments change the text the search reads, and the delta is itself reconciled site-by-site in the record, `…/09_f-943-1-discovery-record.md` (**one classification row per de-duplicated site, equal by construction**, with a row-count reconciliation; **the counts live in the record, not in this row** — see above). `D-R3` above (§3.8) for **every value and count used here — taken verbatim, never recomputed**: 26 depth corrections, 16 stage changes all to `PS-4`, 1 `entry_gate` change, 6 inversions → 0, `PS-1 19 · PS-2 26 · PS-3 27 · PS-4 107`, `gate-a 19 · gate-c 160`. **The depth range was independently re-verified against the repaired map and does NOT move** — depth 1 ×19, 2 ×26, 3 ×27, 4 ×30, 5 ×30, 6 ×28, 7 ×14, 8 ×3, 9 ×2 over 179 nodes, **minimum 1, maximum 9**, so `01_progression-stages.md:110`'s *"the depth range is 1–9"* is **still true and is not flipped**; only the distribution and the PS-4 share on the neighbouring lines are stale. **Recorded because a blanket "the statistics all moved" would have introduced a NEW false claim while closing an old one.** The reworked `../../C005-dp-map-package/generator/package-completeness-gate.mjs` `PG-7` block and its **38/38, exit 0** run; the regenerated `../../C005-dp-map-package/01_cross-reference-view.md`; `../../C005-dp-map-package/03_open-items-and-provisional-register.md` §1 (the `F-943-1` section, **kept and stated first**, now reading `closed` and naming `D-R4`) and §1.5 (`F-943-3`, **still `🟡 Low · unresolved`**); `../../C005-dp-map/manifest.yaml:6-11`, which makes this ledger the only channel through which a status changes | ✅ 4 recorded — **closing the record against the charter's enumerated site list** (*rejected on evidence, not on taste: three successive enumerations each shipped incomplete, and limb 3 reached 10 files limb 1 could not see — including NEU-940's own progression package, which names no finding id at all. A fourth hand-written list would have been the fourth incomplete one, and its incompleteness would again have been invisible*); **closing `F-943-3` alongside `F-943-1` on `D-P2`'s and the register's forecast** (*rejected: the forecast is not a finding. Measured against the post-`D-R3` map the determinism limb is STILL TRUE with 0 exceptions, so closing it would file a live defect shut on a prediction — the precise failure this slice exists to prevent, and it would have been executed by simply doing what the two registers said*); **hand-correcting the generated cross-reference view** (*rejected: `sole_writer: "generator only"`. A hand-edited view disagrees with its own emitter, is overwritten by the next run, and would leave the 26 markers being re-emitted from source — the closure would be cosmetic. Editing the emitter makes the markers disappear because nothing computes them*); **deleting the `PG-7` block now that its subject is closed, and rewriting `AC-6-P` in place to match** (*rejected twice over: deleting `PG-7` makes the gate green by removing the assertion, which is the exact outcome the rework exists to prevent — the block is turned around so it still FAILS on a register that claims the closure without evidencing it; and rewriting `AC-6-P` in place would destroy a TRUE pass record and violate `AC-4-P`'s "no row deleted, renamed, or rewritten", so its text is preserved verbatim with a note appended, per the `INC-S3` precedent*) | **The graph's edges change again.** Any edge added, removed or re-targeted moves the longest-path depths, and depth → stage → gate move with them — which can **re-open** this finding, because the closure is a claim about agreement between values and graph, not a claim that the values are permanently right. **`PG-7a`/`PG-7a2` are the detectors:** they hold only because nothing computed a marker, so they fail the moment a per-node `F-943-1` marker or a stage inversion is recomputed — and `D-R3`'s §H3 limb 2 fails the validator in the same instant. Also revised if `F-943-3`'s surviving determinism limb is adjudicated by its owner, which would supersede this row's re-check rather than merely re-cite it. | **This task — NEU-956 (C008 SUB-15)** — for the record closure, the discovery that bounds it, the `F-943-3` substance re-check, the `PG-7` rework, the regenerated view and `D-P2`'s resolution. **`F-943-3` itself remains NEU-940 / NEU-888's** — re-checked here, adjudicated here only as to whether it closes, **not** repaired here. **The residual cause of the 6 `cl-4` values stays `unestablished` and stays NEU-940's owner's.** **The deferred creator plausibility review (`D-P3`) is untouched: no stage or difficulty value becomes binding by this row.** |

**What a closed `F-943-1` does and does not license, stated plainly because the whole point of the
closure is that a cold agent can act on it:**

- **It DOES license sequencing from `progression_stage`.** The stages now agree with the graph on
  every drawn edge, so `D-P2`'s binding consumer rule (*"sequence from the graph, not the stage
  labels"*) is **discharged** — the two no longer disagree.
- **It does NOT make any stage or difficulty value binding.** `D-P3` stands untouched: all 179
  nodes still carry `creator_review: "deferred-provisional"`, and **that limitation was always
  independent of `F-943-1`**, exactly as `D-P3` says.
- **It does NOT close `F-943-3`**, whose determinism limb is still true (Low, open, NEU-940 /
  NEU-888), and it closes **none** of the map's 10 known holes (`INC-C1`), `INC-C2`, `INC-S1`,
  `INC-S2` or `INC-C7`.

### 3.10 `CAP-2` closure proposal — **NEU-959 (C009 SUB-3) row, C009 OUT-3**

**This subsection is strictly ADDITIVE.** NEU-959 modified no row above it: `D-S1`…`D-S5`, `D-S1a`,
`D-C1`…`D-C4`, `D-P1`…`D-P4`, `D-R1`…`D-R4`, §3.1, §3.1a, §3.2, §3.3, §3.4, §3.5, §3.6, §3.7,
§3.7.1, §3.8, §3.8.1, §3.9, §4, §5, §6 and §7 are preserved **byte-for-byte**, for the reason
`CV-33` records and `AC-4-P` binds: **map nodes cite these ids verbatim in `notes`, so a dropped or
renamed row turns those notes into false claims.**

**Namespace note.** The `-R` namespace was opened by C008 (§3.6) and its reservations —
`D-R2` (NEU-950), `D-R3` (NEU-954), `D-R4` (NEU-956) — are **all discharged**. `D-R5` is the next
free id and is taken here by **C009**, a different charter reusing the same namespace rather than
minting a new one, because the namespace's stated rule is *"the next appender takes the next free
`D-R#` and appends a new terminal subsection here, without needing to read this task."*
**`D-R6` onward remain free** for the two C009 ledger writers serialized behind this one —
**NEU-961** and **NEU-963** — each of which appends its own terminal subsection below this one.

**The one thing a cold reader should take from this row**, stated before the table: **`CAP-2` is
NOT closed, and it is not partially closed either.** A sub-task commissioned to close it is
reporting that it could not, on a precondition it was forbidden to re-decide — and is recording the
exact, currently-actionable pass that would unblock it.

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-R5`** | **`CAP-2`'s CLOSURE IS DECLINED. `CAP-2` REMAINS OPEN, and nothing about its substance has changed.** `CAP-2` records that problem-level citations are unverified because an entry-level automated fetch of the Codeforces corpus returned **HTTP 403** on 2026-07-16; `INC-D4` (§4 of NEU-932's ledger) carries the same gap for T4's catalogue entry ids. **C009 SUB-3 was commissioned to close it by evidence and did not.** — **THE REASON IS A RIGHTS PRECONDITION, NOT A METHOD FAILURE AND NOT A SCHEDULE FAILURE.** NEU-957 (C009 SUB-1) recorded a per-source access-permission record dated 2026-08-10 (`../../C009-course-content-quality/01_provenance-and-rights.md` §3) in which **all twelve sources — `T1`–`T6` and `C1`–`C6` — carry access disposition `Restricted`**. C009's charter binds the sanctioned hierarchy with a gate: **a source SUB-1 recorded as restricted is not fetched**, and its scope reduction is *a cap, not a judgment call made here*. Twelve restricted rows over a six-corpus selection means **the hierarchy has no reachable leaf**, so **zero requests were issued to any of the twelve sources**, zero citations were resolved, and the seed set is **empty**. **`CAP-2`'s own source, T4, was gate-blocked too — so the 2026-07-16 403 was neither reproduced nor refuted, and no new information about it exists.** — **WHAT SHIPPED ANYWAY, AND WHY IT IS NOT NOTHING.** `../../C009-course-content-quality/03_problem-citation-verification-and-access-paths.md` §5 supplies the artifact `CAP-2` never had: **an operational, re-executable, transport-invariant definition of "verified"** — seven ordered steps (`V0` access-gate, `V1` pre-selection, `V2` sanctioned transport, `V3` id resolves, `V4` URL resolves and agrees, `V5` title and constraints match, `V6` difficulty signal captured as a dated observation, `V7` rights re-checked independently), each naming its own failure mode, **all-or-nothing**, with only `V2` differing between the API path and the single-targeted-fetch path. §6 supplies **graph-node selection criteria** (`S1`–`S4` inclusion, `X1`–`X5` disqualifiers) with a **residual clause** governing every node not enumerated in a seed set — and `X3` explicitly excludes the **10 `INC-C1` techniques**, which have no nodes to source against. §7 discharges **Branch A**: the citation record is specified for **both** `CH-F5-1` dispositions, the produced shape is `stable_id` + `canonical_url` only, and title/constraints/difficulty live as **dated verification observations** in `../../C009-course-content-quality/traceability/03_access-path-and-verification-record.md` §2 — so a favourable `CH-F5-1` ruling is a **promotion of existing observations, not a re-verification**, and no citation would need re-resolving. — **THE BLOCKER MOVED FROM DIFFUSE TO NAMED, AND THAT IS THIS ROW'S OPERATIONAL CONTRIBUTION.** Before: *"closing it requires corpus access"* — unowned, unbounded, uncosted. After: **exactly one named, dated, currently-actionable pass** — a **NEU-957-owned rights re-verification** that reads each source's own terms, robots directives and rate limits and re-dates its §3 row. `../../C009-course-content-quality/01_provenance-and-rights.md` §11.2 names that as **the only route by which a restricted row becomes permissive**, and SUB-3's own charter puts *deciding a per-source access permission* **out of scope**. — **AND THE TRIGGER FOR THAT PASS HAS NOW FIRED.** `CAP-S1-1` and §11.2 both close on *"network access becomes available to a re-verification pass."* SUB-3 recorded, dated 2026-08-10 and correctly classed **2 `[code-evidence]`**, that **outbound network capability exists in this execution environment** — established against `example.com`, a **neutral endpoint that is none of the twelve sources**, and disclosed in the request-pattern audit rather than quietly excluded from it. **The capability half of that condition is satisfied; the authority half is not.** **Capability is not authority**, and SUB-3 performing the re-verification itself would have been the §3.1 category error twice over — re-deciding an access row *and* self-supplying the precondition it consumes. | **unresolved** — `CAP-2` is known open, with a named owner and a stated closure condition. **A consumer must not invent a value**: no problem-level citation is asserted anywhere in C009's output, and none may be inferred from this row. **Not self-promoted to `settled`** (`A4`) — a producing task may not promote its own artifact, and this row proposes a disposition rather than ratifying one. | **`../../C009-course-content-quality/03_problem-citation-verification-and-access-paths.md`** §0 (the result stated first), §4.1 (the twelve-row gate outcome), §4.2 (the standing argument, recorded so it can be challenged), §4.3 (the network-capability observation), §5 (the seven-step procedure), §6 (the selection criteria and residual clause), §7 (Branch A, both dispositions), §8 (the retention bound, vacuously satisfied), §9 (the request-pattern and retention audits), §11 (cluster coverage 0/4), §12 (the `CAP-2` disposition) · **`../../C009-course-content-quality/traceability/03_access-path-and-verification-record.md`** §1 (**12 rows, one per source, each dated 2026-08-10, each carrying `none — gate` as a real recorded resolved-through value rather than a blank**), §1.1 (the one disclosed non-source request), §2 (**the verification-observation table, empty by construction**), §3 (`RC-7`/`RC-8` re-resolved against SUB-3's own conduct, plus new **`RC-9`** — no access outcome promotes a restricted source — and **`RC-10`** — every asserted problem id has a dated resolution record, **trivially true because zero ids are asserted**), §4 (the re-run scans: **A, B, C all 0 matches**; D confined to the probe's refusal blocks; E **7 matches, every one policy prose naming the endpoint as the subject of a prohibition**) · **`../../C009-course-content-quality/dry-run/03_template-fabrication-probe-run-2.md`** (the second, **admitting** probe run — **10/10, zero invented identifiers, zero invented addresses, zero produced citations, zero citations asserted as verified**) · the consumed preconditions `../../C009-course-content-quality/01_provenance-and-rights.md` §1, §3, §3.1, §4.1, §6, §7.3, §11.2, and `CAP-S1-1` · `CAP-S3-1`…`CAP-S3-7` and `OI-S3-1`…`OI-S3-5` in C009's shared registers | ✅ 4 recorded — **closing `CAP-2` on the strength of the shipped procedure** (*rejected, and it is the tempting one: the procedure is real, re-executable and is the artifact `CAP-2` lacked. But `CAP-2` is a claim about **citations being unverified**, not about a procedure being absent — closing it because a method now exists would file a live gap shut on a **prediction that the method will work**, which is **exactly** the defect `D-R4` (§3.9) was written to prevent in this ledger, one subsection above this one. It is not going to be reintroduced by the next appender*); **partially closing it — declaring the C4/API half closed and the fetch half open** (*rejected: a partial closure needs **at least one verified citation**, and there are zero. **C4 failed the access gate before its API was ever reachable**, so Branch B was never even entered — there is no failed API attempt to fall from. A partial closure here would be a closure of nothing, dressed as progress*); **treating the twelve `Restricted` rows as merely default and issuing one careful probe fetch** (*rejected on **standing**, not on plausibility — and the argument is recorded in full at `03_…` §4.2 rather than buried: (1) §3.1 clause 1 forbids SUB-3 forming its own view of a disposition; (2) §11.2 names a dated rights re-verification as **the only** route that promotes a row, and that pass is **NEU-957's**, not SUB-3's; (3) the failure directions are asymmetric — over-caution costs a cap with a one-line trigger, while a wrong permissive call commits a rights breach against a source whose terms nobody has read, recorded in a package whose entire claim is that it does not do that*); **admitting one unverified id per cluster to ship 4/4 coverage with caveats** (*rejected outright: the acceptance criterion is **zero unverified admitted**, `C1` forbids inventing a problem-level citation **under any pressure, including schedule**, and `EXC-1` is the recorded incident of exactly this. **A cluster row reading 0 with a named owner is worth more than a row reading 1 that nobody can resolve** — and an unverified id is, in `01_provenance-and-rights.md` §8's words, worse than a missing one because it is unfalsifiable at a glance*) | **A NEU-957-owned dated re-verification pass reads each source's own terms, robots directives and stated rate limits and re-dates its `01_provenance-and-rights.md` §3 access-permission row** — the pass `OI-S3-2`'s network-capability observation now makes actionable. **Then** `03_…` §5's procedure is executed against the sources whose re-dated rows permit it. `CAP-2` closes when **at least one citation returns PASS on all seven steps**, and cluster coverage closes when each of CL-1…CL-4 has one. **No fetch outcome, absent `robots.txt`, unenforced rate limit, or `200 OK` closes it** — inherited verbatim from `CAP-S1-1` and deliberately not weakened here. **This row is also revised if `D-F5`'s owner rules that a restricted-by-default row *is* fetchable by a downstream sub-task** (`OI-S3-3`), which would reopen the whole question on better authority than SUB-3 had. | **`CAP-2` itself: the creator / NEU-957 (SUB-1)** for the re-verification half, as residual owner of OUT-7; **SUB-3's successor** for the execution half. **This row: NEU-959 (C009 SUB-3)** — for the declined disposition, the procedure, the selection criteria, the access-path record and the second probe run. **`INC-D4` remains NEU-932's ledger's** — cited here, **untouched here**. **`CH-F5-1` remains NEU-932's** — consumed here, **not re-decided here**. |

**What a declined `CAP-2` does and does not license, stated plainly because the point of declining
is that a cold agent can act on it:**

- **It does NOT license asserting any problem-level citation.** Zero are verified. `01_provenance-and-rights.md` §8's enforcement clause stands: **an artifact carrying a problem id with no corresponding dated resolution record in SUB-3's verification record is a fail**, and that record (`traceability/03_…` §2) is **empty**.
- **It does NOT reopen `D-F2`.** The corpora stand. **A blocked source is a cap, never a reason to swap in an easier corpus** — a different corpus is a `D-F5`/`D-F2` ledger matter, not a local substitution.
- **It does NOT promote, weaken or otherwise touch any access-permission row.** All twelve still read `Restricted`, and only NEU-957's re-verification changes that.
- **It DOES license executing `03_…` §5's procedure the moment a source's row permits it** — the procedure, the criteria and the record shapes are complete, and a successor needs no further design work, only authority.
- **It closes none of** `INC-D4`, `INC-C1`, `INC-C2`, `INC-S1`, `INC-S2`, `INC-C7`, `CAP-S1-1`, or `CH-F5-1`.

### 3.11 Per-cluster non-root `conceptual` obligation — **NEU-961 (C009 SUB-5) row, C009 OUT-6**

**This subsection is strictly ADDITIVE.** NEU-961 modified no row above it: `D-S1`…`D-S5`, `D-S1a`,
`D-C1`…`D-C4`, `D-P1`…`D-P4`, `D-R1`…`D-R5`, §3.1, §3.1a, §3.2, §3.3, §3.4, §3.5, §3.6, §3.7,
§3.7.1, §3.8, §3.8.1, §3.9, §3.10, §4, §5, §6 and §7 are preserved **byte-for-byte**, for the reason
`CV-33` records and `AC-4-P` binds: **map nodes cite these ids verbatim in `notes`, so a dropped or
renamed row turns those notes into false claims.**

**Namespace note.** §3.10 reserved **`D-R6` onward** for the two C009 ledger writers serialized
behind NEU-959. **`D-R6` is taken here by NEU-961.** **`D-R7` onward remain free** for **NEU-963**,
which appends its own terminal subsection **below** this one under the same stated rule — *"the next
appender takes the next free `D-R#` and appends a new terminal subsection here, without needing to
read this task."*

**The one thing a cold reader should take from this row**, stated before the table: **the map is
being handed an obligation, not a repair — and the obligation cannot be discharged by content of any
kind, by anybody, until a map-side decision is made.** Three of the four clusters instantiate
non-root `conceptual` **zero** times, and **retyping an existing node to close the gap is barred**,
because every candidate carries its own committed rationale for why the `settled` `D-S1` cascade
stopped earlier. **No node, edge, stage, difficulty value or status was created or altered by the
filing task.**

| Id | Decision | Status | Evidence | Rejected alternatives recorded | Revision trigger | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **`D-R6`** | **THE PER-CLUSTER NON-ROOT `conceptual` OBLIGATION IS SPECIFIED AND ROUTED TO THE MAP'S OWNER. IT IS NOT DISCHARGED, AND `F-943-2` IS NOT CLOSED.** — **THE BAR CHANGED, AND IT SUPERSEDES UNION-COMPLETENESS FOR THIS ONE TYPE.** OUT-2's criterion is union-completeness **over the graph** — `≥1` node per named type, graph-wide, explicitly **not** per-cluster (`../../C005-dp-map-integrity/02_skill-type-union-completeness.md` §4). `F-943-2` declined to tighten it unilaterally and routed the question to the charter, on the stated grounds that tightening OUT-2 is a **criterion change**. **The charter took that route and closed the decision by requiring spread:** every cluster must carry **non-root** `conceptual` coverage. The two criteria now disagree in the most dangerous available way — **the looser one returns PASS on today's graph while the stricter one fails three of four clusters** — so the supersession is recorded here rather than left to a reader to notice. **A future coverage audit must count non-root instances only, per cluster, and must report the owner named in this row rather than filing a duplicate finding against an already-routed obligation.** — **THE MEASURED STATE, CONSUMED AND NOT RE-DERIVED.** `conceptual` is instantiated by **exactly three** nodes, **all in CL-1**: the two frozen roots `cl-1.root.recognize-optimal-substructure` and `cl-1.root.recognize-overlapping-subproblems` — authored by NEU-933, frozen under `D-S2`, **products of the first-principles floor and not of the mapping phase, and therefore not evidence under a non-root bar** — plus the single non-root technique node `cl-1.judge-dp-applicability`. **CL-2 (31 skill nodes), CL-3 (20) and CL-4 (18 mainstream + 14 frontier) instantiate it ZERO times.** Non-root `conceptual` coverage is therefore **1/4 clusters**, and **0/3** across CL-2, CL-3 and CL-4. — **WHAT IS ROUTED IS THE WHOLE OBLIGATION FOR THREE CLUSTERS, NOT A SHORTFALL.** The C009 package's content forms discharge a conceptual obligation **only** where a non-root node already carries `skill_type: "conceptual"`, and the reason is mechanical: SUB-2's placement matrix (`../../C009-course-content-quality/02_content-and-exercise-forms.md` §6.3) keys the REQUIRED form set off **the node's `skill_type`**, and all ten form templates (§7) require `node_id` to be *"the exact node id from the map — copy it; if you cannot locate it, refuse."* **A cluster with no `conceptual` node offers no attachment point.** Attaching a lesson, reflection or retrieval item to a node typed `strategic` produces coverage of `strategic` — the artifact is valid and is simply not, and cannot be made into, conceptual coverage. **CL-1's half is complete content-side and needs nothing from the map; CL-2's, CL-3's and CL-4's halves are 100% map-side.** — **THE FOUR OBLIGATIONS ARE STATED, BY REFERENCE, SO THE ADJUDICATION IS AGAINST A NAMED ACQUISITION RATHER THAN A DESCRIPTION.** `../../C009-course-content-quality/05_per-cluster-conceptual-obligation.md` §4: **CL-1** — the **joint** admissibility judgment, that optimal substructure and overlapping subproblems hold **together**, with its two failure directions (substructure without overlap is divide-and-conquer; overlap without substructure is a search problem memoization cannot rescue); carrier `cl-1.judge-dp-applicability`. **CL-2** — whether a combinatorial structure **indexes a state space at all**, i.e. whether the objects a statement describes admit a decomposition boundary along which optima compose, versus a collection the problem merely enumerates; failure directions: no decomposition boundary, and a decomposition that double-counts. **CL-3** — whether a state is **compressible**, i.e. whether the information a transition actually consumes is bounded and enumerable independently of the state's apparent size; and what a state **is** in a domain whose semantics redefine it (a probability is not an expectation; a game position's value is defined by who moves). **CL-4** — whether a too-slow recurrence's cost is **structural at all**, i.e. whether the transition possesses an exploitable property before and independently of choosing which optimization to run; the failure direction being the cluster's own invited fallacy, *the recurrence is slow, therefore an optimization exists.* — **RECLASSIFYING AN EXISTING NODE IS BARRED, AND THAT IS THIS ROW'S SHARPEST OPERATIONAL CONTRIBUTION.** The obvious reading — *"three clusters just need a node retyped"* — is wrong. The eight-type assignment is governed by the ordered, first-match-wins cascade `S1→S8`, **`settled` as `D-S1`**, in which `conceptual` sits at **S8** and is reached only as a **confident residual** after proof, optimization, debugging, transfer, strategic, implementation and procedural have each declined. The union-completeness audit already records the consequence — *"the cascade structurally suppresses `conceptual` outside the first-principles layer"*. **Every near-candidate in the three clusters carries its own committed `skill_type_rationale` stating why the cascade stopped earlier** — at **S5** for the selection nodes `cl-2.select-knapsack-variant`, `cl-2.recognize-rerooting-applicability` (whose rationale reads *"choosing the formulation under a cost constraint, which is the definition"*), `cl-3.recognize-bitmask-state-applicability`, `cl-3.formulate-game-dp` and `cl-4.select-mainstream-optimization`; and at **S4** for the `recognize-…-under-an-unfamiliar-surface` family in all three clusters. Retyping one would **contradict a committed node-local rationale under a `settled` decision** and would install a type the cascade does not produce — a green spread row over a map that instantiates `conceptual` by fiat. **Nearby knowledge nodes cannot substitute either:** `cl-3.probability-vs-expectation-dp-semantics` and `cl-3.win-lose-position-semantics` **state** the distinctions the obligation requires a learner to **judge with**, but they are `node_kind: "knowledge"`, carry no `skill_type`, and cannot instantiate one. **If the map's owner nonetheless judges a reclassification correct, the route is a `D-S1` challenge in this ledger — never a local retype.** — **THREE ROUTES REMAIN OPEN AND THE CHOICE AMONG THEM IS THE MAP OWNER'S, NOT C009's.** **(1) Mint a new non-root node per cluster whose acquisition is a genuine S8 residual** — the four obligations above are written in exactly that shape so the route is assessable, but satisfying the cascade for each is an adjudication this package cannot make on the map's behalf, minting being out of scope charter-wide. **(2) Amend the cascade** so `conceptual` is reachable per cluster — `D-S1`'s own revision trigger has two limbs, and **the `>10` `D-S1a` limb has NOT fired (the count is 1)**, so this route rests on the second limb, *"a mapper cannot express a real node in the schema"*, which the owner must decide the spread bar constitutes. **(3) Decline the spread bar** and revert to union-completeness, recording that CL-2/CL-3/CL-4 learners are taught no non-root conceptual skill — a charter-level reversal, named here only so the routed decision is complete rather than leading. — **CL-4 IS SCOPED TO ITS MAPPED MEMBERS AND THE `INC-C1` HOLE IS RESTATED, NOT ABSORBED.** The CL-4 obligation covers the cluster's **mapped** members only. **The 10 `INC-C1` techniques have no nodes**, and no coverage is implied for them; **`F-939-A` (SOS DP) and `F-939-B` (bitset / word-parallel) are left standing exactly as `../../C005-dp-map-integrity/05_findings-register.md` records them** — both *"🔴 GENUINE GAP — confirmed"*, owned by NEU-942 as `INC-C1`, gated on `INC-C2` (`D-F4a`). **No node is minted, no edge is faked, `D-F4a` is not re-decided.** Even a fully discharged CL-4 obligation leaves the unmapped techniques unaddressed, because a technique with no node has nowhere for a conceptual obligation to attach or be checked — filed as `OI-S5-3` and deliberately **not** folded into the CL-4 row. — **THE OBLIGATION RESTS ON NO VERIFIED PROBLEM CITATION, AND THAT IS STATED RATHER THAN ASSUMED.** **Zero problem-level citations are verified anywhere in C009's output** and **cluster citation coverage is `0/4`** (`CAP-S3-1`); `D-R5` (§3.10) declined `CAP-2`'s closure on a rights precondition. **The obligation is nevertheless statable in full, and citation-independently so, by a property of the matrix rather than by convenience:** for a `conceptual` node, `problem-reference` and `solution` are both **O**, and none of the six REQUIRED forms carries a required citation-bearing field (`example`'s `problem_ref` is OPTIONAL and explicitly *"never an inline invented problem"*). **This is not presented as making the obligation dischargeable** — three clusters remain blocked on the map, which no citation would unblock. Where a citation is eventually used the interim field set is **`stable_id` + `canonical_url` only** (`CH-F5-1`, `DR-C09-01`, `CAP-S1-2`) and **an unverifiable value is refused, never invented**. **The `OI-S3-2` network-capability observation is capability, not authority; the rights gate stays shut and nothing in this row licenses a fetch.** — **THE RESIDUAL CLAUSE IS OWNED, NOT ASSUMED.** *"…and any cluster member whose conceptual obligation is not enumerated above"* is **NEU-961's**. The four-cluster enumeration is **the floor, not the boundary**: **cluster-level completeness is not member-level completeness**, and an unenumerated member is a gap this sub-task records (`OI-S5-5`), not one that disappears because 4/4 clusters are addressed. | **unresolved** — known open, with a named owner and a stated closure condition. **A consumer must not invent a value**: **no cluster's conceptual coverage may be reported as achieved on the strength of this row**, and `F-943-2` may not be cited as closed. **Not self-promoted to `settled`** (`A4`) — a producing task may not promote its own artifact, and this row routes an obligation rather than ratifying its discharge. | **`../../C009-course-content-quality/05_per-cluster-conceptual-obligation.md`** §0 (the result stated first), §1 (the measured baseline, consumed from the union-completeness audit), §2 (the bar and its supersession, with the audit consequence), §3 (the discharge mechanism — the placement matrix and the `node_id` refusal instruction), §4.1–§4.4 (the four per-cluster obligations and the per-cluster candidate tables with current types), §5 (the 4/4 check), §6 (the reclassification bar and the three open routes), §8 (the citation position), §9 (`INC-C1`, `F-939-A`, `F-939-B`), §10 (the residual clause) · **`../../C009-course-content-quality/traceability/05_conceptual-obligation-and-routing-matrix.md`** §1 (the 4/4 matrix, one row per cluster), §2 (the REQUIRED form set for a `conceptual` node, verbatim from SUB-2), §3 (evidence class per assertion), §4 (the non-mutation check) · the consumed contracts `../../C009-course-content-quality/02_content-and-exercise-forms.md` §4, §6.3, §7 and `../../C005-dp-map-integrity/02_skill-type-union-completeness.md` §1–§4 · the node files `../../C005-dp-map/nodes/*.yaml` and `../../C005-dp-map/manifest.yaml`, **read and not written** · `OI-S5-1`…`OI-S5-5` and `CAP-S5-1`…`CAP-S5-3` in C009's shared registers | ✅ 4 recorded — **authoring conceptual-flavoured content against existing CL-2/CL-3/CL-4 nodes and reporting the clusters covered** (*rejected, and it is the tempting one: the artifacts would be individually valid, because `reflection`, `retrieval` and `assessment` are **R** for `strategic` and `transfer` too. But the matrix reads the NODE's type, so the coverage produced is of that type. The package would then read green on all four clusters while the map still instantiates `conceptual` exactly once — hiding the very fragility `F-943-2` was filed to keep visible, behind our own output*); **reclassifying a near-candidate — the `strategic` selection nodes or the `transfer` recognition family — to `conceptual`** (*rejected on the cascade, not on taste: each carries a committed `skill_type_rationale` recording why `S1→S8` stopped earlier, and `D-S1` is `settled`. A retype installs a type the cascade does not produce and makes the spread row green by fiat — the same concealment as the first rejected alternative, executed one layer down. The route, if the owner wants it, is a `D-S1` challenge*); **filing four separate ledger rows, one per cluster** (*rejected: the three routed clusters share **one** cause — the cascade suppresses `conceptual` outside the first-principles layer — and **one** decision surface. Four rows would misrepresent one adjudication as four independent problems and would let three be closed while the shared cause stands, exactly as `CAP-S3-1` reasons for its own single-entry treatment of four clusters*); **treating the frozen CL-1 roots as evidence of CL-1's compliance and declaring the bar met there generously** (*rejected: the bar is explicitly **non-root**, and the two roots are `D-S2` floor artifacts that the mapping phase did not produce. Counting them would make CL-1 look doubly-covered while its real compliance rests on **one** node — `F-943-2`'s original point, which survives this row unchanged and is filed as `OI-S5-4`*) | **The map's owner adjudicates among the three routes in §6** — mint per-cluster S8-residual nodes, amend the `D-S1` cascade, or decline the spread bar — **and records the disposition in this ledger.** This row is also revised if: **`D-S1`'s `>10` `D-S1a` threshold fires** (currently 1), which would supply the cascade-revision trigger route (2) presently lacks; **or the `INC-C1` completion task mints CL-4's missing nodes**, which changes what CL-4's obligation attaches to and may exercise `OI-S5-5`'s residual clause; **or `cl-1.judge-dp-applicability` is retyped or removed**, which drops non-root `conceptual` to **zero graph-wide** and converts this row's subject from a spread shortfall into a total absence. **No content authored under C009 closes it** — that is the whole point of the split, and it is deliberately not weakened here. | **The map-side half: the map's owner — the creator, and whichever task next writes `nodes/*.yaml`** (the same routing `INC-C7` and the `INC-S2` residual already carry). **Which party ultimately satisfies a map-side conceptual obligation is a charter open question — this row routes it and does not resolve it on the map's behalf.** **This row, and the specification it routes: NEU-961 (C009 SUB-5)** — for the per-cluster obligations, the discharge split, the reclassification bar and the residual clause. **`F-943-2` itself remains NEU-943's / the integrity audit's** — consumed here, **status untouched here**. **`INC-C1`, `F-939-A` and `F-939-B` remain NEU-942's** — restated here, **untouched here**. **`D-S1` remains NEU-933's** — consumed here, **not re-decided here**. |

**What this row does and does not license, stated plainly because the point of routing is that a cold
agent can act on it:**

- **It does NOT license reporting any cluster's conceptual coverage as achieved.** Three of four are **routed, pending a map-side decision**. The completion condition C009 met is *specified and routed*, never *covered*.
- **It does NOT close `F-943-2`**, which stays **decided (spread is required); closure pending the routed map-side change**. Nor does it close `INC-C1`, `INC-C2`, `INC-D4`, `INC-S1`, `INC-S2`, `INC-C7`, `CAP-2`, `CAP-S1-1`, `CAP-S3-1` or `CH-F5-1`.
- **It does NOT authorize a retype.** Reclassifying a `strategic` or `transfer` node to `conceptual` contradicts a committed rationale under a `settled` `D-S1`; the sanctioned route is a `D-S1` challenge filed here.
- **It does NOT touch the map.** No node, edge, `progression_stage`, `entry_gate`, difficulty value, `manifest.yaml` entry or boundary-register entry was created or altered by the filing task, and that is verifiable by diff rather than asserted.
- **It does NOT license a fetch.** `OI-S3-2` established capability, not authority; all twelve access rows still read `Restricted`, and only NEU-957's re-verification changes that.
- **It DOES license the map's owner to act on a fully specified obligation** — four named acquisitions, a stated discharge split, an evidenced reason why content cannot close three of them, and three enumerated routes with the barred fourth explained. **A successor needs no further specification work, only authority.**
- **It DOES license SUB-9 (NEU-965) to bind the content-form half today** — the REQUIRED form set for a `conceptual` node, and the field names `misconception_or_edge_case` and `separating_distractor_or_boundary_input`, are SUB-2's exact strings and need no translation. **The map-side half is not yet enforceable**, and a gate that fired on it today would fail three clusters for a reason no content author can fix.

## 4. Incomplete-state markers (`INC-S#`)

Namespaced `-S` so they never collide with NEU-887's `INC-1…5`, NEU-888's `INC-I#`, or NEU-932's `INC-D#`. Each names a **missing artifact with an owner** — reported, never invented (NEU-899 rule 4, inherited).

> **⚠ Cross-namespace row, flagged deliberately (NEU-944).** This table is `INC-S#` by construction, but
> the **`INC-C7`** row below is **`-C`-namespaced** — NEU-942's namespace, not NEU-933's. **This is
> intentional and is called out rather than left to confuse a reader who relies on the namespace to
> infer provenance.** `INC-C7` is filed **by NEU-944** and continues **NEU-942's `INC-C#` sequence**
> (`INC-C1`…`INC-C6`) because it is a **coverage-audit** finding — coverage is NEU-942's subject, not
> the schema's — and minting it as `INC-S4` would have implied the schema owns a coverage gap it does
> not. It sits **here** because this is the one table a reader scans for *"what is missing and who owns
> it"*, and a marker filed in a table nobody reads is a marker that does not exist. **The `INC-S2`
> disposition row is likewise NEU-944's, against NEU-933's own `INC-S2`.** **No id is reused across
> namespaces; no collision is created.**

| Marker | Missing artifact | Owner |
| --- | --- | --- |
| **INC-S1** | **A boundary register complete over the technique space.** The register is exactly the spec's sanctioned 5 anchors; the technique space does not exist yet (`INC-D3`), so completeness is unprovable now. Route **AR-1** exists precisely for this. **Foreseen case: Aho–Corasick** (automaton DP's non-DP prerequisite) is not registered — the spec doesn't name it and the register invents nothing. | The five mappers, via AR-1 |
| **INC-S2** | **The JSON Schema validator and the index generator.** NEU-932 `D-F3` §5 assigns both to SUB-2; deferred because a validator over **zero technique nodes** is tested only against the specimens that authored it — the weakest possible evidence. The 18 checks it must enforce are specified (`../01_…` §6) so the deferral loses no content. Index-drift is already an open cost on `D-F3`. | Coverage-audit sub-task (OUT-7) — the first consumer needing mechanical validation and index freshness |
| **INC-S2 — re-routed by NEU-942 (`INC-C6`)** | **NEU-942 did not build the validator or the index generator, and says so rather than letting the deferral lapse silently.** OUT-7's remit is *technique coverage against the references* — a judgment no validator can check. The 18 checks are **structural** (`../01_…` §6), which is the **integrity** audit's subject. Building them under a coverage spec would have been scope NEU-942 was not granted, on a deliverable whose real first consumer is SUB-9. | **Re-routed to SUB-9 (NEU-943)** — the integrity audit is the true first consumer of mechanical validation; or **SUB-11 (NEU-944)** at package assembly. See `../../C005-dp-map-coverage/05_caps-and-incomplete-scope.md` `INC-C6` |
| **INC-S3** | **The difficulty-dimension set.** The schema fixes the *shape* (`difficulty_dimensions: map<string, string\|number\|null>`); the dimension set itself is **unresolved**. Mappers write `{}` and **must not invent dimensions** (NEU-887: a consumer must not invent a value). | SUB-7 (OUT-3) — **DISCHARGED** by NEU-940's `dimension_set_version 1.0.0`, applied to 179/179 with one key-set and zero key drift (NEU-943-verified). **The values are `provisional`** (`D-P3`) and 26 are defective (`D-P2`); the *dimension set* is delivered. |
| **INC-S2 — disposition by NEU-944 (`INC-C6` closed to a partial)** | **NEU-944 built the INDEX-CLASS deliverable and did NOT build the JSON Schema validator, and says which is which rather than letting the deferral lapse silently.** **Built:** `../../C005-dp-map-package/generator/build-cross-reference.mjs` — a **generated** 187-node cross-reference view that supersedes `index/00_technique-index.md`'s stale scaffold content **for consumers**, plus `…/generator/package-completeness-gate.mjs`. **Not built:** the JSON Schema validator over `../01_…` §6's 18 structural checks. **Why not:** NEU-943's `validator/audit-graph-integrity.mjs` already enforces the structural invariants that matter to a consumer (acyclicity, grounding, referential integrity, union-completeness, OUT-6) and **passes 28/28**; a *second* schema-shaped validator would duplicate it under an assembly spec. **`index/00_technique-index.md` is NOT overwritten** — its `sole_writer` is `"generator only"` and its staleness is `INC-S2`'s to close. **`INC-S2` therefore remains OPEN for the JSON Schema validator and the index regeneration.** | **Open** — routed to the `INC-C1` CL-4 completion task or a later map-maintenance charter, whichever next writes the graph. See `../../C005-dp-map-package/06_caps-and-incomplete-scope.md` `INC-P1`. |
| **INC-C7 — filed by NEU-944** | **Node-level `coverage.status` is `"unaudited"` on ALL 179 mapped nodes, and no node-level coverage verdict exists.** **Nobody erred.** NEU-942 adjudicated coverage at the **map** level (30 disagreements, zero smoothed) and **wrote no node file** (its own `AC-6-C`, correctly — NEU-940 was writing those files concurrently), so no verdict was ever written back to a node. **The binding per-technique coverage verdict is therefore "in scope and mapped; the OUT-7 sweep raised no gap against it"** — which the cross-reference view states explicitly rather than leaving `unaudited` to read as an omission. **NEU-944 invented no per-node verdict** — producing a coverage verdict is out of scope, and 179 verdicts minted by an assembler would be exactly the fabrication the status discipline forbids. | A node-level coverage write-back pass — **the creator**, or the `INC-C1` completion task, whichever next writes `nodes/*.yaml`. |

## 5. Carried conflicts (preserved, not smoothed)

| Id | Conflict | Disposition |
| --- | --- | --- |
| **X-S1** | **`D-S4` refines NEU-932's `03_…` §4 rule 4.** Read literally, rule 4 sends every root edge (which spans CL-1) to `edges/cross-cluster.yaml`. `D-S4` rejects that reading and draws root edges directly. | **Carried, not treated as settled-by-silence.** The argument is `../01_…` §4.2. **The honest cost is live**: an audit classifying edges by *endpoint span* will report root edges as false-positive missing cross-cluster edges. Two mitigations shipped: **R5** in `edges/cross-cluster.yaml` warns SUB-12 that re-drawing them **duplicates every floor edge**, and `manifest.yaml`'s `edge_disposition` block lets an audit classify by **field, not span**. If NEU-932's author disagrees, the route is a challenge against `D-S4`. |
| **X-D1** | **SOS DP: CL-4 vs CL-3** (inherited from NEU-932). T1 and T2 both plausibly fire. | **Carried undiminished.** Adjudicated to CL-4 by rule order, logged `D-F4a` **provisional**, CL-3's claim live, U4 route named. NEU-933 **does not touch it** — no cluster assignment is NEU-933's to make. Surfaced in the three places a mapper will actually look: CL-3's file header, CL-4's mainstream file header, and §3's U4 route. |
| **X-D2** | **Naming instability across references** (inherited). The same technique carries different names by tradition. | **Carried.** Not resolved here. The schema's optional `aliases` field gives mappers a place to record synonyms so **OUT-7 does not read a naming difference as a coverage gap** — a mitigation, not a resolution. |
| **X-D3** | **Inherited NEU-887 R1 — the DP-transfer gap.** No selected corpus is ordered by *learning* dependency. Non-downgradable High. | **Carried undiminished.** Directly binding on this schema: **a prerequisite edge is a structural claim, not a validated learning claim.** Nothing in C005 measures DP learning. The map must not import any corpus's ordering as a prerequisite claim, and `coverage.corpus_refs` is a **reference**, never evidence of a prerequisite. |

## 6. Node status discipline (what this ledger means for the map)

**The map's YAML reflects this ledger; it never overrides it.**

- **A mapper may not promote its own node to `"settled"`.** The correct default for a mapped node is **`"provisional"`** — not a weakness, but the map's honesty.
- **`"settled"` today: the 8 roots (`D-S2`), and nothing else.** No technique node exists.
- **`adjudicated_at_map_version`** records the `map_version` at which a node's status was last adjudicated, so a reader tells settled-and-current from settled-long-ago-under-an-older-schema.
- **`"unresolved"` names an owner and invents no value.**

## 7. Self-check

| Check | Passing condition | Result |
| --- | --- | --- |
| **AC-1-S2** | Every material NEU-933 decision has exactly one ledger row. | **Pass** — 6 rows (`D-S1`…`D-S5`, `D-S1a`). |
| **AC-2-S2** | Every settled decision records its rejected alternatives. | **Pass** — 5/5 settled rows. |
| **AC-3-S2** | Every non-settled item names an owner and a trigger. | **Pass** — `D-S1a` (open slot), `INC-S1`…`INC-S3`. |
| **AC-4-S2** | No status is set outside this file. | **Pass** — every topic file's header defers here; `manifest.yaml` carries the legend but adjudicates nothing. |
| **AC-5-S2** | No NEU-887/NEU-888/NEU-932 machinery is re-derived. | **Pass** — `../04_register-extension.md` RE-1…RE-12; only `-S`-namespaced ids added. |
| **AC-6-S2** | Conflicts are carried, not smoothed. | **Pass** — `X-S1`, `X-D1`, `X-D2`, `X-D3`, §5. |
| **AC-7-S2** | No class-1–6 evidence is presented as class 7. | **Pass** — no external-user, expert, or market claim anywhere. |
| **AC-8-S2** | `D-F3a` — assigned to SUB-2 by NEU-932 — is resolved. | **Pass** — resolved by `D-S1`. NEU-932's constraints on it are all honoured: `status` required with three legal values; `adjudicated_at_map_version` required; string enums quoted; no field holds verbatim external content (`D-F5`). |
| **AC-9-S2** | NEU-933 makes no family-mapping and no progression decision. | **Pass** — zero technique nodes authored; root-internal edges minimal and structural, explicitly not a progression (`../02_…` §2.3); `difficulty_dimensions` left `{}` for SUB-7. |
| **AC-10-S2** | The filing routes SUB-11 drives are seeded with owners and procedures. | **Pass** — §3: `D-S1a`, `AR-1`, U4, schema challenge. |

### 7.1 Self-check on the NEU-942 (SUB-10) union — OUT-7

| Check | Passing condition | Result |
| --- | --- | --- |
| **AC-1-C** | Every coverage disagreement carries exactly one of the four verdicts; no difference is smoothed. | **Pass** — 30 adjudicated: 10 GAP, 11 ME, 6 IE, 3 UU. Zero smoothed. `../../C005-dp-map-coverage/02_…` |
| **AC-2-C** | Every genuine gap carries a **named owner**. | **Pass** — 10/10. Nine → `INC-C1` (creator); `CV-19` → SUB-9/SUB-12. `CV-1` additionally gated on `D-C3`/`INC-C2`. |
| **AC-3-C** | Every material residual exclusion carries a documented rationale. | **Pass** — all 52 read; **none found without a rationale, none silently dropped.** `../../C005-dp-map-coverage/03_…` |
| **AC-4-C** | NEU-938's maximalist-frontier drops are adjudicated. | **Pass** — `RX-7`…`RX-13` all upheld; each materiality claim independently checked against C1…C6 with no counter-instance found. |
| **AC-5-C** | The ledger was **unioned, never replaced**. | **Pass** — `D-S1`…`D-S5` and §3.1's `AR-1/a`,`AR-1/b` preserved byte-for-byte. Only additions (§3.1a, §3.2, §3.3, §7.1) plus two **superseded-on-the-count** notes that preserve the original text. **No row deleted or renamed** — map nodes cite these ids in `notes`. |
| **AC-6-C** | No node file or edge file was written. | **Pass** — `nodes/*.yaml` untouched (NEU-940 writing concurrently); `edges/cross-cluster.yaml` untouched (NEU-939's). |
| **AC-7-C** | No status flipped outside a ledger, and none flipped without standing. | **Pass** — `D-F4a` **not** flipped; `D-C3` records a recommendation to its named owner. The coverage package sets no status. |
| **AC-8-C** | No problem-level citation invented; `CAP-2` respected. | **Pass** — no C4 problem id asserted anywhere; the withdrawals in `EXC-1`/`E0` are **upheld, not reversed**. |
| **AC-9-C** | Blocked ledger filings are discharged, not inherited. | **Pass** — `EXC-11`'s `D-S1a` filed (§3.3); four in-file-only AR-1 requests unioned (§3.1a). **7 claims existed, 2 were in the ledger; 6 are now.** |
| **AC-10-C** | Conflicts carried, not smoothed. | **Pass** — `X-D1` untouched and carried; `CV-32`'s AR-1 id collision recorded and routed, not resolved locally; `CV-31`'s citation slip recorded rather than silently corrected. |

### 7.2 Self-check on the NEU-944 (SUB-11) package assembly — OUT-8 + OUT-9

Every row is mechanically re-checked by
`../../C005-dp-map-package/generator/package-completeness-gate.mjs` (**38/38**).

| Check | Passing condition | Result |
| --- | --- | --- |
| **AC-1-P** | Every element of the package is bound through **this** ledger; no element is unlabelled. | **Pass** — `PG-11a`: 187/187 nodes carry a legal status (8 `settled`, 179 `provisional`); `PG-11b`: 187/187 carry `adjudicated_at_map_version`; `PG-11c`: `D-P1`…`D-P4` present. |
| **AC-2-P** | A cold agent recovers a technique's **7 facets in ONE hop** and tells binding from open. | **Pass** — `PG-6c`: **179 blocks × 7 facets = 1253 facet checks, 0 gaps**; `PG-6d`/`PG-6e`: every realized cross-cluster prerequisite and every anchor terminal appears on its dependent's block. Dry-run `../../C005-dp-map-package/05_…`. |
| **AC-3-P** | The **eight-skill-type union result** and **floor termination** are **CONFIRMED, not re-derived**. | **Pass** — `PG-2`/`PG-3` **spawn NEU-943's own validator** and assert on its verdicts. **28/28**, exit 0. No second implementation was written. |
| **AC-4-P** | The ledger was **UNIONED, never replaced**. | **Pass** — `PG-10`: all 29 pre-existing ids survive; `PG-10b`: every ledger id **cited by a node's `notes`** (`AR-1/a`, `AR-1/b`, `AR-1-a`, `AR-1-b`, `INC-S1`, `D-S1a`) still resolves — **zero false claims**. **Added:** §3.4, §3.5, §7.2, and the `INC-S2`-disposition and `INC-C7` marker rows. **The one pre-existing row touched is `INC-S3`** — its text is preserved **verbatim** and a discharge note is **appended to its Owner cell**; the row and its id survive, per NEU-942's own "superseded-on-the-count" precedent. **No row deleted, renamed, or rewritten.** |
| **AC-5-P** | **No new node, edge, progression stage, difficulty value, JS-materiality finding, integrity finding, or coverage verdict.** | **Pass** — `PG-12c`: the package contains **no YAML at all**. No `nodes/*.yaml`, no `edges/*.yaml`, no register touched. The view is a **projection**; its only non-source number is the rubric depth inside an `F-943-1` warning, which is **NEU-943's published finding**, quoted as evidence and never as a value to consume. |
| **AC-6-P** | **`F-943-1` is bound as an explicit, prominent, unresolved element** with an owner and a revision trigger. | **Pass** — `D-P2`; `PG-7a`: **all 26** affected nodes carry the per-node warning marker; `PG-7a2`: all **5** inversion-bearing nodes name their backwards dependency; `PG-7b`/`c`/`d`: unresolved + owner + trigger. Stated **first** in `03_…`, headlined in the README. **Not buried.** **⚠ SUPERSEDED BY APPEND — NEU-956 (C008 SUB-15), `D-R4` (§3.9). The text above is a TRUE PASS RECORD at NEU-944's ship and is preserved VERBATIM, never rewritten in place** (`AC-4-P`: *"No row deleted, renamed, or rewritten"*; the `INC-S3` precedent in §4 is the worked example). **What has changed is the world, not the record: `F-943-1` is CLOSED** — the values by `D-R3` (NEU-954, §3.8), the record by `D-R4`. **The `PG-7a`/`PG-7a2`/`PG-7b`/`PG-7c`/`PG-7d` results this row cites are therefore SUPERSEDED by the reworked post-closure `PG-7` block**, which asserts the mirror image on the same emitted-not-written basis: **no** node carries the per-node warning marker and **no** block names a stage inversion (both holding *because nothing computed them*), the register **keeps** its dedicated `F-943-1` section and records it **closed**, **names** its discharging ledger entry, and records **what** discharged it. **`PG-7e` is RETIRED** — its subject, the repair's ownership, was settled by C008 performing the repair, so it has no closed-state successor — **and `PG-7f` is ADDED**, asserting that the ledger entry the register names **actually resolves as a ledger row**. **The gate's check count is unchanged at 38.** A reader wanting the current assertion set reads `D-R4` and the gate; a reader wanting to know what was true when this package shipped reads the sentence above, unaltered. |
| **AC-7-P** | Every open item ships with a **named owner and revision trigger**; none is smoothed. | **Pass** — `PG-8a`: 27 inherited open items all bound; `PG-8b`: the deferred creator review is listed among the decisions that ship provisional. `INC-C1`, `INC-C2`, `F-943-1/2/3`, `F-939-1/A/B`, six `AR-1`s, `D-S1a-1`, `CAP-2`, `R1`, `JS-U1/2/3/5`, `PS-2/3/4`, `X-S1`, `X-D1`, `X-D2`, `INC-S1`, `INC-C7`. |
| **AC-8-P** | **No lesson, problem, graph editor, or exercise runner** was built. | **Pass** — `PG-12a`/`PG-12b`: the only executables are the view generator and this gate, **both projections**. No lesson, problem, exercise, editor, runner or curriculum artifact exists in the package. |
| **AC-9-P** | The **OUT-8 authoring-requirements spec** is complete. | **Pass** — `PG-13a`…`PG-13f`: required coverage, sequencing/prerequisite constraints, and difficulty-calibration inputs all stated; **both root causes routed into binding rules** — `INC-C1`'s *"one half must be the residual owner"* and `F-943-1`'s *"file-disjointness is not data-disjointness"*. |
| **AC-10-P** | **Conflicts and gaps preserved, never smoothed.** | **Pass** — `INC-C2` **not** decided (no standing: `D-F4a` is NEU-932's, U4 forbids local re-decision); the two dangling declarations **left in place** as the honest face of `INC-C1`; `X-S1`/`X-D1`/`X-D2` carried; `INC-C7` **filed rather than papering over 179 `unaudited` coverage fields**; `INC-S2` closed only to a **partial**, with the unbuilt half named. |
| **AC-11-P** | `EXC-11` / the `AR-1` union are **verified, not re-broken**. | **Pass** — §3.5; `PG-9`: `D-S1a` count = **1**, `D-S1`'s `>10` threshold does **not** fire. |
| **AC-12-P** | `INC-D1` is superseded by a **real** cold-agent handoff. | **Pass** — NEU-932's was a desk-check and said so. `../../C005-dp-map-package/05_cold-context-dry-run.md` is the real one. |

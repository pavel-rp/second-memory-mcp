# Domain Glossary

A single, greppable lookup for the project-local vocabulary of Second Memory MCP.
Grep a term, land on one row, and reach its defining file in **one hop** — without a
second search and without reverting to a pretrained or generic-programming meaning.

Scope note: this file **seeds** the charter-named core terms (NEU-911). Full domain
coverage is a later sweep (NEU-912); a write-time maintenance hook (NEU-913) and a
PR-time audit backstop (NEU-914) build on the shape and inclusion bar defined here.

## How to read a row

Each row resolves a term in one hop:

`Term` — **one-line definition** | **owning module** | **defining file** (repo-relative
path) | **cross-refs** (sibling glossary terms and/or owning ledgers).

- **Owning module** is the hexagonal layer that owns the concept: `transport`, `server`,
  `orchestration`, `domain`, `ports`, `adapters`, `infrastructure`, or `shared`
  (see `CLAUDE.md` → File Organization).
- **Defining file** is the single most authoritative file that defines or centrally uses
  the term. It is where you go to read the real thing; the row only summarizes it.
- **Cross-refs** point at related terms in this file (in `backticks`) and, where a term's
  authority lives elsewhere, at that owning file/ledger.

## Authority discipline

**The glossary re-states; it never re-decides.** A row is a signpost, not a source of
truth.

- The **single source of truth** for any term is its defining file. If a definition here
  and the code disagree, the code wins and the row is stale — fix the row.
- **Mutable statuses and decisions stay in their owning ledgers** (the code, config,
  migrations, Linear issues, research registers) and are **cross-linked, never
  duplicated**. This file records _what a term means_, not _what its current value or
  status is_. Example: `tier` names the audit stages; the blocking thresholds that decide
  outcomes live in `src/domain/config/classifier.ts` and are cited, not copied.
- **Research id-families** from the C005 product-foundation register (`F*` / `MC-*` /
  `BM-*` / `RQ*`) are **linked, not copied in**. The C005 vocabulary exemplar this file is
  modeled on lives at `docs/research/C005-product-foundation/00_vocabulary.md`, which is
  **worktree-only and not yet on `develop`** (charter Assumption 1, [unconfirmed]); C005
  id-family cross-refs are therefore marked **[pending: C005 not on develop]** until that
  file lands. `docs/research/` and `docs/specs/` are gitignored in this repo, so any
  cross-ref into them is **non-durable / local-only** and is marked as such.

## Inclusion predicate — what earns a row

A token earns a row when it names a concept in the project's **domain or system model**
whose meaning is either:

1. **Project-specific / coined** — a term the project invented or gave a bespoke meaning
   (e.g. `chunk`, `leech`, `roadblock`, `context token`, `classifier`, `tier`,
   `remediation`); **or**
2. **Familiar-but-redefined** — an everyday or generic-programming word the project
   narrows or redefines such that an agent could **misresolve it from pretrained or
   generic defaults** (e.g. `port` / `adapter` in the hexagonal sense, `session`,
   `mastery`, `audit pipeline`).

**Excluded** — _not_ every exported symbol earns a row: generic programming identifiers,
framework/library symbols (Zod, Drizzle, LangChain, MCP SDK names), and
implementation-only helpers do **not** get rows. The read path and the maintaining
hook/audit apply this one shared "what counts as a domain term" bar.

---

## Learning & product terms

| Term                   | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Owning module   | Defining file                                                                                      | Cross-refs                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `chunk`                | The atomic unit of learning content — one teachable idea carrying its own spaced-repetition state (`easeFactor`, `repetitions`, `consecutiveFailures`, `nextReviewAt`) and a 1-based `orderIndex` within its topic. **Not** a generic text/byte "chunk".                                                                                                                                                                                                                                                                                                                                                                        | `domain`        | `src/domain/types/entities.ts` (`LearningChunk`)                                                   | `topic`, `session`, `ease-factor`, `leech`; port `src/ports/chunk-repository.ts`                          |
| `topic`                | A named grouping of ordered `chunk`s on one subject, carrying a dependency-graph shape (`dependencyGraphType`) and an optional versioned summary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `domain`        | `src/domain/types/entities.ts` (`LearningTopic`)                                                   | `chunk`; port `src/ports/topic-repository.ts`                                                             |
| `session`              | A bounded learning/review run over a set of `chunk`s in one of five **modes** (`scaffolding` / `learning` / `retrieval` / `review` / `assessment`), tracking per-chunk attempts and questions. **Not** an HTTP/auth session.                                                                                                                                                                                                                                                                                                                                                                                                    | `domain`        | `src/domain/types/session.ts` (`SessionMode`)                                                      | `chunk`, `roadblock`, `mastery`, `stopping advisory`; entity `LearningSession` in `src/domain/types/entities.ts`               |
| `spaced-repetition`    | The SM-2-derived scheduling scheme that sets each `chunk`'s next review date from recall quality; `intervalDays` is always read from the calculator's output, never hardcoded.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `domain`        | `src/domain/algorithms/sr-calculator.ts` (`calculateNextReview`, `calculateNextReviewAdvanced`)    | `ease-factor`, `leech`; I/O types `src/domain/types/sr.ts`                                                |
| `ease-factor`          | The per-`chunk` SM-2 multiplier that stretches or compresses the next review interval as recall quality rises or falls; floored at the configured `minimumEaseFactor`.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `domain`        | `src/domain/algorithms/sr-calculator.ts`                                                           | `spaced-repetition`, `leech`; tunables `src/domain/config/algorithm.ts` (`clampEaseFactor`)               |
| `scheduling snapshot`  | The answering `chunk`'s pre-review scheduling state, captured on every scored attempt and persisted as four nullable `session_question_attempts` columns: `snapshot_band` (`'fresh'` = no usable interval — `null`, non-finite, zero **or negative**; `'established'` = a finite `interval_days > 0`), `snapshot_predicted_recall` (the `classifyChunk` power-law estimate, non-NULL only on the `'established'` band), `snapshot_interval_days` and `snapshot_days_overdue`. A NULL `snapshot_band` means **uncovered** — a pre-cutover row, a multi-chunk assessment attempt, or a failed best-effort read — never "no risk". | `domain`        | `src/domain/algorithms/scheduling-snapshot.ts` (`computeSchedulingSnapshot`, `SchedulingSnapshot`) | `chunk`, `spaced-repetition`, `retrievability`; power law in `src/domain/algorithms/classify-chunk.ts`    |
| `mastery`              | A **derived** progression state, not a stored flag: the server decides when a `chunk` is mastered by gating chunk completion on its mapped questions passing (a `repetitions`-derived `masteryLevel`). Authority: computed at runtime; never persisted as a standalone decision.                                                                                                                                                                                                                                                                                                                                                | `orchestration` | `src/orchestration/teaching-workflows.ts`                                                          | `session`, `roadblock`; prompt context in `src/shared/prompts/prompt-pack.ts`                             |
| `leech`                | A `chunk` repeatedly failed — flagged by `calculateNextReviewAdvanced` when lifetime `totalAttempts >= leechFailureThreshold` **and** `consecutiveFailures >= leechConsecutiveFailures`; incurs an extra ease penalty and routes to `remediation`. **Not** a resource leak.                                                                                                                                                                                                                                                                                                                                                     | `domain`        | `src/domain/algorithms/sr-calculator.ts` (`leech` flag)                                            | `ease-factor`, `remediation`; `LEECH_CHUNK_TYPE = 'remediation'` in `src/domain/types/recommendations.ts` |
| `roadblock`            | A gate that requires N follow-up questions (per the trigger answer's quality) before a learner may progress past a `chunk`, so a low-scored answer cannot be skipped. **Not** a build/CI blocker. **Not** the `stopping advisory` — a roadblock blocks progression; a stopping advisory only suggests a break.                                                                                                                                                                                                                                                                                                                | `domain`        | `src/domain/algorithms/roadblock-gate.ts` (`computeRoadblockState`, `getRequiredFollowups`)        | `session`, `mastery`, `stopping advisory`; response type `RoadblockDetail` in `src/domain/types/teaching.ts`                   |
| `correct-answer block` | Chunk-derived corrective material (`content`, `condensed_summary`, `title`) plus a fixed presentation directive, surfaced on `submit_answer`'s `recorded` response only after a second failed attempt. **Not** a canonical or generated per-question answer — the server holds none; best-effort and fail-open (never blocks recording the attempt).                                                                                                                                                                                                                                                                            | `domain`        | `src/domain/types/teaching.ts` (`CorrectAnswerBlock`)                                              | `roadblock`, `session`, `stopping advisory`; built in `src/orchestration/teaching-workflows.ts` (`buildCorrectAnswerBlock`)    |
| `remediation`          | A recommended recovery plan built after a `session` for weak/`leech` chunks, tagging each with a `ReasonCode` (`WEAK_AFTER_ASSESSMENT` / `LEECH_THRESHOLD` / `PREREQ_LOW_EASE` / `NEW_MATERIAL`).                                                                                                                                                                                                                                                                                                                                                                                                                               | `domain`        | `src/domain/types/remediation.ts` (`RemediationPlan`, `WeakChunk`, `ReasonCode`)                   | `leech`, `session`; workflow `recommendRemediation` in `src/orchestration/remediation-workflows.ts`       |

## System & machinery terms

| Term             | Definition (project-local meaning)                                                                                                                                                                                               | Owning module   | Defining file                                                                   | Cross-refs                                                                                                                            |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `tier`           | A **stage of the content-audit pipeline**, not a subscription/pricing tier: Tier 1 is the deterministic linter (`tier1a` structural-blocking vs `tier1b` heuristic-warning rules), Tier 2 is the post-commit LLM `classifier`.   | `domain`        | `src/domain/services/chunk-linter.ts` (`LinterRuleTier = 'tier1a' \| 'tier1b'`) | `audit pipeline`, `classifier`; Tier 2 threshold in `src/domain/config/classifier.ts` (`BLOCKING_THRESHOLD`)                          |
| `classifier`     | The Tier 2 LLM content-quality judge returning a six-aspect verdict (`renderingClarity`, `vocabularyAppropriate`, `mathNotationRenderingRisk`, `definitionConstructive`, `epistemicConsistency`, `overallFit`), each scored 1–5. | `domain`        | `src/domain/types/classifier.ts` (`VerdictSchema`, `ChunkClassifierVerdict`)    | `tier`, `audit pipeline`; port `src/ports/content-classifier-port.ts`, adapter `src/adapters/langchain/content-classifier-adapter.ts` |
| `port`           | Hexagonal sense: an **interface** the domain/orchestration depends on, decoupling use-cases from I/O. **Not** a network port. Each port has a concrete `adapter`.                                                                | `ports`         | `src/ports/` (e.g. `src/ports/chunk-repository.ts`)                             | `adapter`; wiring in `src/composition-root.ts`                                                                                        |
| `adapter`        | Hexagonal sense: the **concrete implementation** of a `port` — a Postgres/Drizzle repository (`src/adapters/drizzle/`) or an external-service client (e.g. LangChain). Swapped in at composition time.                           | `adapters`      | `src/adapters/drizzle/` (e.g. `src/adapters/drizzle/chunk-repository.ts`)       | `port`; wiring in `src/composition-root.ts`                                                                                           |
| `audit pipeline` | The shared content-audit flow run on chunk create/update: Tier 1 linter suite then Tier 2 `classifier` (post-commit), guarded by a Tier 2 circuit breaker.                                                                       | `orchestration` | `src/orchestration/audit-pipeline.ts`                                           | `tier`, `classifier`; breaker `src/orchestration/tier2-circuit-breaker.ts`                                                            |
| `context token`  | An opaque, TTL-bounded token issued by `init_agent_context` that every stateful tool call must present; created/validated/deleted through the repository and enforced at the transport boundary. **Not** an LLM "token".         | `ports`         | `src/ports/context-token-repository.ts` (`ContextTokenRepository`)              | `session`; enforced in `src/transport/context-token-middleware.ts`, payload built in `src/orchestration/learner-context-workflows.ts` |
| `gated package`  | A `docs/research/` package whose citations **must** all resolve — one non-resolving citation fails CI. Ungated packages are still walked and their counts printed, but never fail the build: gating, not defect count, decides failure. | `scripts` | `scripts/check-citation-paths.ts` (`GATED`) | `non-claim`; failure rule in `scripts/citation-paths/checker.ts` (`isFailing`) |
| `non-claim`      | A citation-shaped token that **is not a citation** — prose quoting a defective path in order to report it. Declared by `(package, file, target, count)` and exempted from the gate; a declaration whose observed count no longer matches is **stale** and fails CI, so the list cannot rot into a blanket amnesty. | `scripts` | `scripts/citation-paths/non-claims.ts` (`NonClaim`, `NON_CLAIMS`) | `gated package`; convention `DR-C10-S12-2`, disposition in the C010 register `90_open-items-and-provisional-register.md` |

<!-- NEU-912: the sections below extend the NEU-911 seed to full domain coverage. Same row shape, same inclusion predicate and authority discipline as above. The falsifiable per-file enumeration, selection rationale, and coverage pass live in docs/GLOSSARY-COVERAGE.md. -->

## Content authoring & structure terms

| Term                            | Definition (project-local meaning)                                                                                                                                                                                                     | Owning module | Defining file                                                                               | Cross-refs                                                                                 |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `knowledge_type`                | Pedagogical class of a chunk's content per Component Display Theory — `fact` / `concept` / `procedure` / `principle`; a teaching taxonomy, **not** a generic `type` field.                                                             | `domain`      | `src/domain/types/entities.ts` (`KnowledgeType`)                                            | `chunk`, `chunk_type`, `difficulty`; tool field in `src/domain/types/persistence-tools.ts` |
| `chunk_type`                    | The role a chunk plays — `new` (novel), `review` (reinforcement), `remediation` (targeted fix for a known gap); orthogonal to `knowledge_type`. **Not** a text/content type.                                                           | `domain`      | `src/domain/types/recommendations.ts` (`ChunkType`)                                         | `chunk`, `remediation`, `leech` (`LEECH_CHUNK_TYPE`), `knowledge_type`                     |
| `dependency_graph_type`         | Shape of a topic's inter-chunk prerequisite graph — `linear_chain` / `convergent` / `divergent` / `single_root`.                                                                                                                       | `domain`      | `src/domain/types/entities.ts` (`DependencyGraphType`, `LearningTopic.dependencyGraphType`) | `topic`, `prerequisites`, `structural dependency`                                          |
| `prerequisites`                 | IDs of **other chunks in the same topic** that must be understood first — a structural "understanding X is impossible without Y" dependency, **not** topical relatedness or cross-topic linkage.                                       | `domain`      | `src/domain/types/persistence-tools.ts` (`TopicChunkShape.prerequisites`)                   | `dependency_graph_type`, `structural dependency`, `orderIndex`                             |
| `orderIndex`                    | 1-based teaching-sequence position of a chunk within its topic (mirrors DB `order_index`); drives teach ordering — **not** an arbitrary 0-based array index.                                                                           | `domain`      | `src/domain/types/entities.ts` (`LearningChunk.orderIndex`)                                 | `chunk`, `topic`, `dependency_graph_type`                                                  |
| `structural dependency`         | The topic-scoping rule: group chunks by "does understanding X require understanding Y?" (the dependency chain), **not** by subject/topical similarity — independently-learnable same-subject chunks go in separate topics.             | `shared`      | `src/shared/domain-rules.ts` (`DOMAIN_RULES.topic_scoping`)                                 | `prerequisites`, `dependency_graph_type`, `probe-first scaffolding`                        |
| `condensed_summary`             | A 2–4 sentence quick-refresher for a chunk (recognition trigger when `retrievability` is low-but-nonzero) — **not** the full teaching `content` (a 300–1500-word script) and **not** the topic-level `topic_summary`.                  | `domain`      | `src/domain/types/persistence-tools.ts` (`TopicChunkShape.condensed_summary`)               | `chunk`, `topic_summary`, `retrievability`                                                 |
| `topic_summary`                 | A topic's embedding-generation text for semantic `search mode` — deliberately packed with technical terms and synonyms; distinct from a learner-facing `topic_description` and from a chunk's `condensed_summary`.                     | `domain`      | `src/domain/types/persistence-tools.ts` (`CreateTopicWithChunksInputShape.topic_summary`)   | `search mode`, `condensed_summary`, `topic`                                                |
| `subject`                       | A broad category label for filtering/organization only — **not** a scoping container: many topics may share one subject (unlike `topic`→`chunk` containment).                                                                          | `domain`      | `src/domain/types/persistence-tools.ts` (`CreateTopicWithChunksInputShape.subject`)         | `topic`, `chunk`                                                                           |
| `difficulty`                    | Integer 1–10 chunk rating on a bespoke rubric (1–3 single fact … 9–10 advanced synthesis), expected to rise with `orderIndex` — **not** a free-form difficulty score.                                                                  | `domain`      | `src/domain/types/persistence-tools.ts` (`TopicChunkShape.difficulty`)                      | `knowledge_type`, `orderIndex`                                                             |
| `content_status`                | Publication gate on a chunk's authored content — `draft` (fill later, just-in-time) vs `final` (established).                                                                                                                          | `domain`      | `src/domain/types/recommendations.ts` (`ContentStatus`)                                     | `just-in-time content fill`, `chunk`                                                       |
| `note`                          | A first-class, typed learner/agent annotation on a chunk/topic/session; its `note_type` taxonomy is `insight` / `confusion` / `connection` / `deeper_exploration` / `gap`. Distinct from chunk content.                                | `domain`      | `src/domain/types/notes-tools.ts` (`AddNoteInputShape`, `NoteType`)                         | `gap`, `chunk`, `revise grade` (writes a `confusion` note)                                 |
| `minimum information principle` | Chunk-authoring rule (Wozniak): one chunk targets one knowledge component; split content that tests two separate ideas; ≤2–3 novel interacting concepts at once.                                                                       | `shared`      | `src/shared/domain-rules.ts` (`DOMAIN_RULES.chunk_definition`)                              | `chunk`, `phantom chapter`, `knowledge_type`                                               |
| `domain rules`                  | The static "ubiquitous language" authoring constraints returned by `init_agent_context` (`chunk_definition`, `topic_scoping`, `content_requirements`, `anti_patterns`, `sizing`) — advisory constants, **not** a business-rule engine. | `shared`      | `src/shared/domain-rules.ts` (`DOMAIN_RULES`)                                               | `minimum information principle`, `structural dependency`, `context token`                  |

## Scheduling & review terms

| Term                          | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Owning module   | Defining file                                                                                                                                                                                                                                                                                                                              | Cross-refs                                                                                                                 |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| `quality`                     | The 0–5 SM-2 recall grade the **server derives deterministically** via the `grade mapper` from a `rubric grading payload` (NEU-928; 0–2 fail/reset, 3 correct-with-effort, 4–5 confident) — drives scheduling; **not** code/content quality, never hardcoded, and never an agent-supplied raw number. `passed` = `quality >= 3`.                                                                                                                                                                                                                                                                                                  | `domain`        | `src/domain/types/session.ts` (`ChunkAttempt.quality`); mapper in `src/domain/algorithms/grade-mapper.ts`                                                                                                                                                                                                                                  | `grade mapper`, `rubric grading payload`, `ease-factor`, `spaced-repetition`, `quality cap`, `content_quality` (unrelated) |
| `quality cap`                 | A server-enforced, **downward-only session-scoped cap** on the mapper-derived `quality` (min prior 0–1 → cap 3, =2 → cap 4), preventing inflated self-assessment after a low score. (The former agent-facing scaffolding ceiling was removed in NEU-928.)                                                                                                                                                                                                                                                                                                                                                                         | `domain`        | `src/domain/algorithms/quality-cap.ts` (`computeQualityCap`)                                                                                                                                                                                                                                                                               | `quality`, `grade mapper`, `teaching approach`                                                                             |
| `grade mapper`                | The pure, non-LLM function that turns a `rubric grading payload` into a 0–5 `quality`: sums per-criterion weights (`correct_recurrence` 2, `correct_base_case`/`correct_iteration_order`/`complexity_stated` 1) only when a criterion is both claimed true **and** carries a non-empty verbatim justifying span; malformed/unevidenced payloads fail closed to 0. Deterministic and rebuttal-invariant (DR-M08 / EXP-03).                                                                                                                                                                                                         | `domain`        | `src/domain/algorithms/grade-mapper.ts` (`mapRubricToQuality`, `RUBRIC_WEIGHTS`)                                                                                                                                                                                                                                                           | `rubric grading payload`, `quality`, `revise grade`                                                                        |
| `rubric grading payload`      | The structured grading input the agent supplies on `submit_answer` / `revise_grade` in place of a raw quality: per-criterion booleans (`criteria`) plus verbatim `justifying_spans` of the learner's own answer. Snake_case MCP schema, passed through to the `grade mapper` unchanged. All four criterion booleans are required (missing → Zod rejection); missing spans are uncredited (fail closed).                                                                                                                                                                                                                           | `domain`        | `src/domain/algorithms/grade-mapper.ts` (`RubricGradingPayload`); schema `src/domain/types/teaching.ts` (`GradingPayloadShape`)                                                                                                                                                                                                            | `grade mapper`, `quality`, `revise grade`                                                                                  |
| `over-validation ceiling`     | Algorithm-config knob (`overValidationCeiling`, NEU-929 / MM-T5): the maximum tolerated fraction of KNOWN-INCORRECT held-out grading cases the `grade mapper` may resolve to a pass (`false-accept rate`). A held-out CI fixture measures the aggregate rate and FAILS the build above it (fail-closed). Provisional 0.10 (documented epistemic band, never a measured optimum); on by default. A check on the held-out rate, **not** a runtime per-grade rate-limiter.                                                                                                                                                           | `domain`        | `src/domain/config/algorithm.ts` (`AlgorithmConfig.overValidationCeiling`); default in `src/domain/config/algorithm-defaults.ts`; guard `src/domain/algorithms/over-validation-guard.ts` (`evaluateOverValidation`); env overlay `SM_OVER_VALIDATION_CEILING`                                                                              | `adversarial grading fixture`, `grade mapper`, `quality`                                                                   |
| `durability gate`             | The prerequisite-unlock gate (NEU-931 / MM-T8 Gate C **posterior sub-gate**): a dependent stays **locked** until the prerequisite's `retrievability posterior` — a Beta-Binomial estimate of recall reliability from persisted multi-observation review history — clears the config bar (`durabilityPosteriorBar`, provisional 0.90). Thin/single-success history fails closed (never unlocks). Replaces the former 0.5 estimated-retrievability reteach rule. Pure predicate; the Gate-B non-massed/separated-session component is C2 (deferred). Emits an auditable gate-decision record on both lock and unlock paths (OUT-7). | `domain`        | `src/domain/algorithms/durability-gate.ts` (`isPrerequisiteSatisfied`, `evaluatePrerequisiteGate`, `computeRetrievabilityPosterior`); consumed in `src/domain/algorithms/resolve-stale-prerequisites.ts`; config in `src/domain/config/algorithm.ts` (`AlgorithmConfig.durabilityPosteriorBar`), env overlay `SM_DURABILITY_POSTERIOR_BAR` | `retrievability posterior`, `retrievability`, `prerequisite`, `over-validation ceiling`                                    |
| `retrievability posterior`    | The monotonic durability signal the `durability gate` evaluates: the posterior mean of a Beta-Binomial model over a prerequisite's recall reliability, updated from persisted graded pass/fail attempt counts (prior Beta(1,1)). Empty history → 0.5; a single success → 2/3; rises toward 1 with accumulating successes and falls with failures. Distinct from FSRS `retrievability` (a point-in-time decay estimate).                                                                                                                                                                                                           | `domain`        | `src/domain/algorithms/durability-gate.ts` (`computeRetrievabilityPosterior`, `ReviewObservationCounts`); observation counts from `src/adapters/drizzle/review-persistence-adapter.ts` (`getReviewObservations`)                                                                                                                           | `durability gate`, `retrievability`, `quality`                                                                             |
| `adversarial grading fixture` | The held-out CI oracle of hard grading cases (`known_incorrect` wrong/unevidenced DP answers + `valid_unusual` correct-but-unconventional ones) as `rubric grading payload`s + expected verdicts, with a fail-closed assertion that the mapper's `false-accept rate` on the known-incorrect subset stays ≤ the `over-validation ceiling`. CI product code (not a throwaway EXP-\* fixture); mirrors the EXP-03 oracle. Rebuttal text is metadata, never fed to the mapper.                                                                                                                                                        | `test`          | `tests/fixtures/adversarial-grading-fixture.ts`; gate in `tests/unit/domain/algorithms/adversarial-grading.test.ts`                                                                                                                                                                                                                        | `over-validation ceiling`, `grade mapper`, `rubric grading payload`                                                        |
| `rebuttal-invariance`         | Property of `revise grade` (NEU-929): a bare learner rebuttal — persuasive `new_feedback` prose with no NEW `rubric grading payload` — produces zero upward grade flips; only a new payload the deterministic `grade mapper` credits higher can lift a grade. The rebuttal text is never an input to the derived `quality`.                                                                                                                                                                                                                                                                                                       | `orchestration` | `src/orchestration/teaching-workflows.ts` (`reviseGrade`); asserted in `tests/unit/orchestration/revise-grade.test.ts` + `tests/integration/workflows/revise-grade.test.ts`                                                                                                                                                                | `revise grade`, `grade mapper`, `over-validation ceiling`                                                                  |
| `priority score`              | A computed review-urgency ranking (from next-review date, `ease-factor`, repetitions, difficulty) used to order due items within a timebox — distinct from the SM-2 interval.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `domain`        | `src/domain/types/spaced-repetition-tools.ts` (`CalculatePriorityScoreInputShape`, `RankCandidatesInputShape`)                                                                                                                                                                                                                             | `spaced-repetition`, `ease-factor`, `cognitive load`                                                                       |
| `cognitive load`              | A per-item load estimate carried on a ranked review candidate, used to shape ordering/selection within a timebox — a project ranking measure, **not** the generic UX notion.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `domain`        | `src/domain/types/sr.ts` (`RankedItem.cognitiveLoad`)                                                                                                                                                                                                                                                                                      | `priority score`, `spaced-repetition`                                                                                      |
| `learning item`               | The generic spaced-repetition-tracked unit the SR calculators operate on (id + `easeFactor` + `repetitions` + interval + difficulty) — the standalone single-item abstraction (`create_learning_item`), effectively a `chunk` without a topic.                                                                                                                                                                                                                                                                                                                                                                                    | `domain`        | `src/shared/chunk-mapping.ts` (`LearningItem`, `mapChunkRowToLearningItem`)                                                                                                                                                                                                                                                                | `chunk`, `ease-factor`, `priority score`                                                                                   |
| `retrievability`              | FSRS power-law estimate of current recall probability `R(t)=(1+(19/81)·t/S)^-0.5` for a chunk, with sibling estimates `storageStrengthEstimate`, `reteachCompression`, `daysOverdue`; selects the `teaching approach`. Forgetting-curve terms redefined.                                                                                                                                                                                                                                                                                                                                                                          | `domain`        | `src/domain/algorithms/classify-chunk.ts` (`classifyChunk`, `TeachingDecision`)                                                                                                                                                                                                                                                            | `teaching approach`, `spaced-repetition`, `condensed_summary`                                                              |
| `search mode`                 | Retrieval strategy for learning content — `keyword` (default text match), `semantic` (embedding cosine), `hybrid` (min-max-normalized weighted blend, falling back to keyword when no embedding provider).                                                                                                                                                                                                                                                                                                                                                                                                                        | `domain`        | `src/domain/types/search-tools.ts` (`SearchLearningContentInputShape.mode`); hybrid merge in `src/orchestration/search-workflows.ts`                                                                                                                                                                                                       | `topic_summary`, `chunk`                                                                                                   |
| `true retention`              | The headline scheduler-health figure (NEU-845): the **first-attempt** pass rate over scored questions whose `scheduling snapshot` recorded `snapshot_band = 'established'`. The `attempt_number = 2` pivot-hint retry **never** contributes — the hint destroys the retrieval event. Membership is decided on the band, never on a raw `snapshot_interval_days > 0` comparison. Overdue attempts count; overdueness is a breakdown axis, never an exclusion. Suppressed to `null` with `below_min_sample: true` under `MIN_SAMPLE_SIZE` (20). Not the `fresh` band, which is reported separately.                                | `domain`        | `src/domain/services/analytics-health.ts` (`computeSchedulerHealth`, `MIN_SAMPLE_SIZE`); exposed by the `analytics_health` MCP tool                                                                                                                                                                                                        | `eventual-pass rate`, `scheduling snapshot`, `retrievability`, `interval band`, `days-overdue band`, `teaching approach`   |
| `eventual-pass rate`          | The secondary figure reported **alongside** `true retention` over the same population and denominator: the question passed on attempt 1 **or** on the attempt-2 pivot-hint retry. Never a replacement for `true retention` — it measures whether the `retry pivot` rescued the item, not whether the schedule worked. Carried on every retention cell, and suppressed by the same min-sample rule.                                                                                                                                                                                                                              | `domain`        | `src/domain/services/analytics-health.ts` (`RetentionCell.eventualPassRate`)                                                                                                                                                                                                                                                               | `true retention`, `retry pivot`, `scheduling snapshot`                                                                     |
| `interval band`               | Half-open `[edge, next_edge)` bucketing of an established observation's `snapshot_interval_days` into `1-6d` / `7-20d` / `21-59d` / `60d+` (edges `[1, 7, 21, 60]`; 21d is Anki's canonical young/mature boundary). One of the three `true retention` breakdown axes. The lowest band absorbs null, non-finite and out-of-domain values so every established row lands in exactly one band and the per-axis sample sizes always sum to the headline sample size.                                                                                                                                                                | `domain`        | `src/domain/services/analytics-health.ts` (`INTERVAL_BAND_EDGES_DAYS`, `INTERVAL_BAND_KEYS`)                                                                                                                                                                                                                                               | `true retention`, `days-overdue band`, `scheduling snapshot`                                                               |
| `days-overdue band`           | Half-open `[edge, next_edge)` bucketing of an established observation's `snapshot_days_overdue` into `on_time` / `1-2d` / `3-6d` / `7d+` (edges `[1, 3, 7]`). Sub-day lateness is same-review-day noise and sits in `on_time`; null, non-finite and negative values fall there too. A `true retention` breakdown axis **only** — being overdue never excludes an attempt from the headline.                                                                                                                                                                                                                                     | `domain`        | `src/domain/services/analytics-health.ts` (`OVERDUE_BAND_EDGES_DAYS`, `OVERDUE_BAND_KEYS`)                                                                                                                                                                                                                                                 | `true retention`, `interval band`, `scheduling snapshot`                                                                   |
| `calibration`                 | Predicted-vs-observed scoring of `classifyChunk`'s `retrievability` estimate (NEU-846): every first-attempt observation whose `scheduling snapshot` stored a `snapshot_predicted_recall` is compared against whether that attempt actually passed. Membership is exactly `snapshot_predicted_recall IS NOT NULL`, so the `fresh` band is **structurally excluded** — there R = 1.0 is definitional, not measured, and NEU-844 deliberately never stores it. Both exclusions (fresh, uncovered) are counted in a coverage block. Always an object on `analytics_health`, never `null`; at zero observations a complete payload with zero counts and suppressed rates. | `domain`        | `src/domain/services/analytics-calibration.ts` (`computeCalibration`, `CalibrationReport`); exposed by the `analytics_health` MCP tool                                                                                                                                                                                                     | `log-loss`, `RMSE-bins`, `calibration bin`, `true retention`, `scheduling snapshot`, `retrievability`                      |
| `log-loss`                    | The **headline** `calibration` figure: mean negative log-likelihood in nats, `−(1/N)·Σ[y·ln(p̂) + (1−y)·ln(1−p̂)]` over first-attempt outcomes `y` and predictions `p`. A proper scoring rule defined per-observation, so it is meaningful at N in the tens where a binned statistic is not. `p̂ = min(max(p, ε), 1−ε)` with `LOG_LOSS_EPSILON = 1e-6`: the clamp is load-bearing, not defensive, because `classifyChunk` returns **exactly** 1.0 whenever `daysOverdue = 0`, making `p = 1.0` the most common stored prediction — unclamped, one first-attempt failure there would be `−Infinity`. The epsilon rides the response so the figure is reproducible. | `domain`        | `src/domain/services/analytics-calibration.ts` (`LOG_LOSS_EPSILON`, `CalibrationOverall.logLoss`)                                                                                                                                                                                                                                         | `calibration`, `RMSE-bins`, `calibration bin`, `retrievability`                                                            |
| `RMSE-bins`                   | The **secondary** `calibration` figure: `sqrt(Σ_b n_b·(meanPredicted_b − observedRate_b)² / N)` over non-empty `calibration bin`s. Emitted because it is the FSRS benchmark's comparison axis and the figure a later FSRS migration must be judged on, so the series exists from cutover — but never the headline, because at small N each bin's observed rate carries binomial noise of order `sqrt(p(1−p)/n_b)` and squaring it biases the statistic **upward**, indicting a perfectly calibrated scheduler. Never reported without per-bin sample sizes.                                                                     | `domain`        | `src/domain/services/analytics-calibration.ts` (`CalibrationOverall.rmseBins`)                                                                                                                                                                                                                                                            | `calibration`, `log-loss`, `calibration bin`, `true retention`                                                             |
| `calibration bin`             | Half-open `[edge, next)` bucketing of a stored prediction over `CALIBRATION_BIN_EDGES = [0, 0.1, … , 0.9]`, with the **top bin closed at 1.0** so the `p = 1.0` mass always lands rather than falling off the end. All ten bins are emitted in fixed order including empty ones, each carrying its own sample size, observed pass count and `below_min_sample` flag; `Σ bin.sampleSize === overall.sampleSize` holds for every input. Uniform edges are an engineering default for the current data volume, emitted on the response as `bin_edges` so a revision is a visible contract change.                                   | `domain`        | `src/domain/services/analytics-calibration.ts` (`CALIBRATION_BIN_EDGES`, `CALIBRATION_BIN_KEYS`, `CalibrationBin`)                                                                                                                                                                                                                        | `calibration`, `log-loss`, `RMSE-bins`, `interval band`, `days-overdue band`                                               |

## Teaching-flow terms

| Term                         | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Owning module   | Defining file                                                                                                                                                                    | Cross-refs                                             |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| `session mode`               | The five session pedagogies (`scaffolding` / `learning` / `retrieval` / `review` / `assessment`), each driving a different teach/grade flow — e.g. `assessment` pre-creates questions, allows one attempt, and fans the SR update out to every mapped chunk (the server derives `quality` 0–5 via the `grade mapper` — full granularity, no binary collapse).                                                                                                                              | `domain`        | `src/domain/types/session.ts` (`SessionMode`, `SessionModeSchema`)                                                                                                               | `session`, `question type`, `roadblock`                |
| `question type`              | Cognitive level of a drill question — `recall` (retrieval), `explain_apply` (understanding+application), `analyze_create` (analysis+synthesis); a bespoke 3-level ladder with accuracy gates, **not** Bloom's six levels.                                                                                                                                                                                                                                                                  | `domain`        | `src/domain/types/teaching.ts` (`QuestionType`)                                                                                                                                  | `session mode`, `quality`, `drill`                     |
| `teaching approach`          | The `retrievability`-selected teaching tier for a chunk — `recall` / `cued_recall` / `reteach` / `scaffold` (thresholds 0.7 / 0.5 / 0.3); reteach & scaffold responses are capped at Level-1 recall questions.                                                                                                                                                                                                                                                                             | `domain`        | `src/domain/algorithms/classify-chunk.ts` (`TeachingApproach`, `mapTier`)                                                                                                        | `retrievability`, `retry pivot`, `question type`       |
| `pacing`                     | A server-issued delivery directive for a chunk — `delivery_mode` (`incremental` / `full`) + `checkpoint_cadence` (`after_each_concept` / `end_of_chunk`) plus a natural-language `directive`; a coined teaching-control object.                                                                                                                                                                                                                                                            | `domain`        | `src/domain/types/teaching.ts` (`Pacing`, `TeachNextTeach.pacing`); computed in `src/domain/algorithms/compute-pacing.ts`                                                        | `teaching approach`, `session mode`                    |
| `retry pivot`                | A teaching-approach-keyed instruction telling the agent exactly what to change after a first failed attempt (scaffold→recognition, reteach→compressed re-explain, cued_recall→graduated hints) — pedagogical remediation, **not** retry/backoff.                                                                                                                                                                                                                                           | `orchestration` | `src/orchestration/teaching-workflows.ts` (`RETRY_PIVOT`)                                                                                                                        | `teaching approach`, `roadblock`                       |
| `fatigue signal`             | The pure, within-`session` **relative** trend `computeFatigueTrend` derives from a session's scored attempts: split (once >= 6 valid attempts exist) into earlier/later halves, `fatigued` is `true` only when the later half's mean latency rises **and** its mean `quality` falls past fixed margins vs. the earlier half. Never an absolute latency cutoff and never compared across sessions (agent pacing makes raw latency meaningless in isolation). Zero I/O, never throws.       | `domain`        | `src/domain/algorithms/fatigue-trend.ts` (`computeFatigueTrend`, `FatigueTrendResult`)                                                                                            | `stopping advisory`, `session`, `quality`               |
| `stopping advisory`          | NEU-848: the single shared producer (`resolveSessionAdvisory`) of within-`session` stopping guidance — at most one `SessionAdvisoryKind` (`fatigue` / `time_ceiling`) with a learner-facing `reason`; a `fatigue signal` takes precedence over the `ceiling backstop` when both apply. Both `session_status` and the teaching responses (`teach_next`/`submit_answer`, as the optional `session_advisory` block) resolve through this one function so the two surfaces can never diverge. Advisory only, recurring while the signal fires — like the `correct-answer block`, a best-effort, fail-open block spread onto a teaching response, but never a blocking gate like `roadblock`. | `domain`        | `src/domain/algorithms/session-advisory.ts` (`resolveSessionAdvisory`, `SessionAdvisory`); block shape `src/domain/types/teaching.ts` (`SessionAdvisoryBlock`)                    | `fatigue signal`, `ceiling backstop`, `session`, `roadblock`, `correct-answer block` |
| `ceiling backstop`           | The `time_ceiling` `stopping advisory`: fires once elapsed `session` time reaches the configured `maxTimeMs` (an engineering-default 2-hour ceiling, not evidence-derived), independent of any `fatigue signal`. Since NEU-848, surfaced both through `session_status` **and** in-band, recurring, as a `session_advisory` block on `teach_next`/`submit_answer`.                                                                                                                          | `domain`        | `src/domain/algorithms/session-advisory.ts` (`resolveSessionAdvisory`, kind `'time_ceiling'`); ceiling config `src/domain/config/algorithm-defaults.ts` (`sessionConfig.maxTimeMs`) | `stopping advisory`, `fatigue signal`, `session`        |
| `drill`                      | A retrieval-practice question generated in one of five `DrillFormat`s (`multiple_choice` / `open_ended` / `coding_problem` / `explanation` / `application`); the learner gets two attempts before answers are revealed. **Not** a generic exercise.                                                                                                                                                                                                                                        | `shared`        | `src/shared/prompts/prompt-pack.ts` (`DrillFormat`, `getRetrievalPrompt`)                                                                                                        | `question type`, `retrievability`                      |
| `revise grade`               | Overwrite a recorded attempt's `quality`/`passed`/feedback when the agent realizes its own grading was wrong. The corrected grade resolves through the same `grade mapper` as `submit_answer` (a fresh `rubric grading payload`, no raw agent number persists, NEU-928); originals are preserved in a revisions table and the `roadblock` gate is recomputed (`roadblock_cancelled` when the fix lifts a gate). Reason kept in a `ReviseGradeReason` enum synced to a DB CHECK constraint. | `orchestration` | `src/orchestration/teaching-workflows.ts` (`reviseGrade`); reasons in `src/domain/types/teaching.ts` (`ReviseGradeReason`)                                                       | `quality`, `roadblock`, `note`                         |
| `rolling session`            | An interactive session flow where the learner picks chunks on the fly (empty session, chunks added/activated one at a time via `create_session_chunk`) instead of pre-declaring all chunks upfront.                                                                                                                                                                                                                                                                                        | `shared`        | `src/shared/prompts/prompt-pack.ts` (`getWorkflowGuidancePrompt`); also `src/shared/instructions.ts`                                                                             | `session`, `session mode`                              |
| `probe-first scaffolding`    | Workflow rule: before creating a topic/prerequisite, search existing content **and** probe the learner with questions — "absence from the DB does not mean the learner lacks knowledge"; create topics only for confirmed `gap`s.                                                                                                                                                                                                                                                          | `shared`        | `src/shared/prompts/prompt-pack.ts` (`getWorkflowGuidancePrompt`); also `src/shared/instructions.ts`                                                                             | `gap`, `structural dependency`, `domain rules`         |
| `just-in-time content fill`  | Bootstrap technique: create chunks with `content_status` `draft`, then fill learner-tailored content during teaching (`update_chunk_content`) based on observed responses, rather than authoring generic content upfront.                                                                                                                                                                                                                                                                  | `shared`        | `src/shared/prompts/prompt-pack.ts` (`getWorkflowGuidancePrompt`, "Just-in-Time Content Fill")                                                                                   | `content_status`, `chunk`                              |
| `stale-prerequisite reteach` | Mid-session redirect: before teaching a chunk, walk its prerequisite graph and insert any prerequisite whose `retrievability` decayed below the cued-recall threshold as a new pending chunk ahead of the dependent one, with a reteach preamble.                                                                                                                                                                                                                                          | `orchestration` | `src/orchestration/teaching-workflows.ts` (step 5b of `getNextTeachingStep`); resolver `src/domain/algorithms/resolve-stale-prerequisites.ts`                                    | `retrievability`, `prerequisites`, `teaching approach` |
| `historical feedback`        | Learner-reported difficulties/successes from prior completed sessions on the same chunk, formatted into later prompts so teaching adapts to past pain points — a cross-session carry-forward, **not** generic user feedback.                                                                                                                                                                                                                                                               | `shared`        | `src/shared/prompts/prompt-pack.ts` (`formatPreviousFeedbackSection`, `PromptFeedbackEntry`)                                                                                     | `session`, `note`                                      |
| `gap`                        | A confirmed **missing prerequisite** (found by probing or a mid-drill struggle), recorded via an `add_note` `gap` note and remediated by teaching the prerequisite first — an upstream dependency gap, **not** a `roadblock` (an in-session progression gate).                                                                                                                                                                                                                             | `shared`        | `src/shared/prompts/prompt-pack.ts` (`getWorkflowGuidancePrompt`, "Reactive Gap Detection"); gap notes written in `src/orchestration/remediation-workflows.ts` (`writeGapNotes`) | `roadblock`, `prerequisites`, `note`, `remediation`    |
| `prompt-pack`                | The central library of parameterized prompt strings the server hands to the client keyed by `PromptName` (scaffolding/learning/retrieval/review/workflow_guidance/chunk_generation/…); pure string authoring, zero I/O.                                                                                                                                                                                                                                                                    | `shared`        | `src/shared/prompts/prompt-pack.ts` (`PromptPack`, `promptPack`)                                                                                                                 | `drill`, `quality cap`, `historical feedback`          |
| `learner context`            | The aggregated whole-system snapshot built for agent priming by `init_agent_context` (totals, due-today/overdue split, overdue topics, recent subjects, `flaggedWeakAreas`, `streakDays`, leech count, active session) — the payload, **not** the `context token` that authorizes the call.                                                                                                                                                                                                | `orchestration` | `src/orchestration/learner-context-workflows.ts` (`LearnerContext`, `buildLearnerContext`)                                                                                       | `context token`, `domain rules`, `leech`, `session`    |

## Content-audit & quality machinery

_Extends the System & machinery seed; same authority discipline — thresholds live in config, this file only names the mechanisms._

| Term                         | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                         | Owning module   | Defining file                                                                                                         | Cross-refs                                               |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- | --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `Tier 2 circuit-breaker`     | In-process, fail-open guard that shrinks the `classifier`'s effective `blocking fields` set when a field's current-week rejection count exceeds its rolling prior-weeks mean + 2σ; trips once per (process, field), read-only against the event log. **Not** an RPC/network breaker — it gates content _blocking_, not request forwarding. | `orchestration` | `src/orchestration/tier2-circuit-breaker.ts` (`createTier2CircuitBreaker`)                                            | `blocking fields`, `classifier`, `audit pipeline`        |
| `blocking fields`            | The allowlist of `classifier` verdict fields that, at/below threshold, escalate a `soft-warn` into a write-rejecting block; the "effective" set is that allowlist after the circuit-breaker optionally shrinks it. Empty ⇒ soft-warn only.                                                                                                 | `orchestration` | `src/orchestration/audit-pipeline.ts` (`effectiveBlocking`); thresholds in `src/domain/config/classifier.ts`          | `Tier 2 circuit-breaker`, `soft-warn`, `classifier`      |
| `soft-warn`                  | The default Tier 2 disposition: a low-scoring verdict field yields a `severity:'warning'` finding surfaced to the caller but does **not** block the write (contrast a blocking hit).                                                                                                                                                       | `orchestration` | `src/orchestration/audit-pipeline.ts` (`classifyChunksSoftWarn`)                                                      | `blocking fields`, `findings`, `classifier`              |
| `validator report`           | The per-chunk JSONB (`learning_chunks.validator_report`) partitioned into `tier1a` (structural-blocking), `tier1b` (heuristic-warning, each entry carrying a `blocking_eligible` snapshot), and `tier2` (`classifier` output), merged section-wise last-write-wins.                                                                        | `domain`        | `src/domain/types/validator-report.ts` (`ValidatorReportSchema`, `mergeReportSections`)                               | `tier`, `classifier`, `blocking-eligible`, `findings`    |
| `content_quality`            | The single `ServiceError`/`ApiErrorType` whose response actually serializes the structured `findings` array — the required channel for surfacing per-item chunk-audit failures (id, field, score, rationale); any other type silently drops them. **Not** a measure of content quality.                                                    | `domain`        | `src/domain/types/api-response.ts` (`ApiErrorType`); serialization gate in `src/server/topic-tools.ts`                | `findings`, `audit pipeline`, `classifier`               |
| `findings`                   | Structured per-item audit diagnostics (`LinterFinding[]`, snake-cased) attached to a response and surfaced **only** on `content_quality` errors; typed `unknown` at the domain boundary.                                                                                                                                                   | `domain`        | `src/domain/types/api-response.ts` (`ApiError.error.findings`)                                                        | `content_quality`, `validator report`, `tier`            |
| `blocking-eligible`          | Flag on a linter rule / validator-report entry meaning the rule met precision/recall thresholds and may block topic creation on a positive finding; `tier1a` rules are eligible by construction, `tier1b` rules earn it only via the validation harness.                                                                                   | `shared`        | `src/shared/linter/rule-intent.ts` (`applyEligibilityToRules`, `RuleLike.blockingEligible`)                           | `rule intent`, `tier`, `validator report`                |
| `rule intent`                | Per-linter-rule declaration of whether maintainers **intend** it to block topic creation; the OOD validation harness fails CI when declared intent diverges from a rule's measured blocking-eligibility. The declared target, **not** current behavior.                                                                                    | `shared`        | `src/shared/linter/rule-intent.ts` (`RULE_INTENT`, `validateRuleIntentParity`)                                        | `blocking-eligible`, `OOD validation harness`, `tier`    |
| `OOD validation harness`     | The out-of-distribution harness (`pnpm lint:validate`) that measures each linter rule's precision/recall against a held-out set and gates promotion of `tier1b` heuristic rules from warning-only to blocking; enforces rule-intent parity in CI.                                                                                          | `shared`        | `src/shared/linter/rule-intent.ts`; thresholds in `src/shared/linter/section-thresholds.ts`                           | `rule intent`, `blocking-eligible`, `tier`               |
| `phantom chapter`            | A chunk that is really a multi-section chapter masquerading as one atomic chunk (multiple H2/H3 sections, summary blocks, exercise lists) — a cognitive-load violation flagged both by a `tier1b` linter rule and by the `classifier`'s `epistemicConsistency` aspect.                                                                     | `shared`        | `src/shared/linter/section-thresholds.ts` (`PHANTOM_CHAPTER_*`); rubric in `src/shared/prompts/classifier-prompts.ts` | `epistemic consistency`, `minimum information principle` |
| `epistemic consistency`      | Coined `classifier` quality aspect / teaching directive: a chunk teaches one internally-consistent concept (not a bundled chapter), and every learner-facing answer is anchored to the chunk's canonical statement rather than improvised.                                                                                                 | `shared`        | `src/shared/prompts/classifier-prompts.ts` (`CLASSIFIER_RUBRIC.epistemicConsistency`)                                 | `phantom chapter`, `classifier`, `condensed_summary`     |
| `significant content change` | A Levenshtein-similarity test (below ~0.5) deciding whether an edit changed a chunk enough to reset spaced-repetition progress and bump the content version; falls back to a length+sampling heuristic above 10k chars.                                                                                                                    | `shared`        | `src/shared/content-similarity.ts` (`hasSignificantContentChange`)                                                    | `spaced-repetition`, `chunk`                             |

## Research-model terms (C005 product foundation)

_These terms are defined in the C005 product-foundation research docs. Those docs are tracked but live under a gitignored tree; per NEU-912 scope their C005 id-family cross-refs (`F*` / `J*` / `P*` / `DEC*` / `EX*`) ship **[pending: C005 not on develop]** and are not re-activated in this release._

| Term                    | Definition (project-local meaning)                                                                                                                                          | Owning module | Defining file                                                                                     | Cross-refs                                                                         |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `schema`                | A transferable mental problem-pattern a learner forms (e.g. a dynamic-programming pattern) — the unit of `transfer`, **not** a database/data schema.                        | `domain`      | `docs/research/C005-product-foundation/product-model/00_learner-and-prerequisite-model.md`        | `transfer`, `durable mastery`; C005 `FM2`/`J2`/`P2` [pending: C005 not on develop] |
| `transfer`              | Applying a learned `schema` to a novel problem — a mechanism kept distinct from retention (the two are never collapsed).                                                    | `domain`      | `docs/research/C005-product-foundation/product-model/01_principles-differentiators-exclusions.md` | `schema`, `durable mastery`; C005 `P1`/`P2` [pending: C005 not on develop]         |
| `durable mastery`       | Retention **and** `transfer` of algorithmic problem-solving skill sustained over months (the product thesis) — a stronger bar than the runtime-derived `mastery` state.     | `domain`      | `docs/research/C005-product-foundation/product-model/00_learner-and-prerequisite-model.md`        | `mastery`, `transfer`, `schema`; C005 `J4`/`P1` [pending: C005 not on develop]     |
| `prerequisite boundary` | The charter-fixed audience competence floor (who is in/out of the target learner set) — an audience gate, **not** the chunk/topic `prerequisites` dependency graph in code. | `domain`      | `docs/research/C005-product-foundation/product-model/00_learner-and-prerequisite-model.md`        | `prerequisites`; C005 `DEC1`/`EX1`/`EX2` [pending: C005 not on develop]            |

## Architecture components (C010 system & repository architecture)

_Product-domain component names introduced by the C010 decision package and **not yet built** — each is placed `required-by-upstream`, so "Owning module" records where it would live rather than where it does. Package-governance vocabulary (`spike`, `cap`, `open item`, `stand-in assumption`) is deliberately **not** listed here; see `docs/research/C010-system-and-repository-architecture/00_method-and-provenance.md` §4.1._

| Term                             | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `authoring-time gate runner`     | The component that executes a **creator-authored** approach over a node's boundary inputs to decide the boundary-confrontation gate, at authoring time only. Runs in a terminable isolate under a host-enforced wall-clock bound — an isolation boundary for **liveness**, not containment. **Never** executes anything a learner submits; there is no in-app judge, sandbox or exercise runner in this product. | _unbuilt — architecture_   | `docs/research/C010-system-and-repository-architecture/03_execution-environment-and-citation-drift-component.md` §3.5 | `citation-drift verdict producer`, `chunk`, `validator report`     |
| `citation-drift verdict producer` | The **out-of-band** component that re-checks one cited external problem and writes one dated drift verdict for it. The only component with egress to a party outside the operator's control; one request per citation, never a corpus walk, and it retains the verdict only — never the source's problem text or its enumerated list.                                                                          | _unbuilt — architecture_   | `docs/research/C010-system-and-repository-architecture/03_execution-environment-and-citation-drift-component.md` §4.2 | `drift-verdict cache`, `authoring-time gate runner`               |
| `drift-verdict cache`            | The internal store the serve path **reads** to learn a cited problem's drift verdict. No egress, no computation, keyed read only — it is on the learner's latency path, which may carry no reviewer, model call or execution. A **stale or absent** verdict quarantines the unit; the learner's request still completes.                                                                                        | _unbuilt — architecture_   | `docs/research/C010-system-and-repository-architecture/03_execution-environment-and-citation-drift-component.md` §4.3 | `citation-drift verdict producer`, `session`                      |
| `authoring pipeline`             | The path a content unit takes from draft to available-to-a-learner. **The only component that admits new content into the store of record**, and the component that runs the `quality-gate battery`. Partly exists today as the chunk and topic creation workflows.                                                                                                                                            | _partly built — architecture_ | `docs/research/C010-system-and-repository-architecture/05_system-context-and-responsibility-boundaries.md` §3.2 (`CMP-S4-13`), §7.1 | `quality-gate battery`, `content serve path`, `chunk`, `topic`     |
| `quality-gate battery`           | The component that evaluates **every content gate except the citation-drift check** against a unit at **authoring time**, producing one verdict record per requirement. Subordinate to it is the `authoring-time gate runner`. Partly exists today as the Tier-1 linter and Tier-2 classifier passes.                                                                                                           | _partly built — architecture_ | `docs/research/C010-system-and-repository-architecture/05_system-context-and-responsibility-boundaries.md` §3.2 (`CMP-S4-14`), §7.2 | `authoring-time gate runner`, `authoring pipeline`, `validator report` |
| `content serve path`             | The component that hands an already-gated content unit to a learner. Carries **no reviewer, no model call and no execution** and has no egress; its entire gate surface is one keyed read of the `drift-verdict cache`, quarantining on a blocked, quarantined, stale or absent verdict. At a revalidation budget of zero, stale-or-absent is its **ordinary** mode.                                            | _partly built — architecture_ | `docs/research/C010-system-and-repository-architecture/05_system-context-and-responsibility-boundaries.md` §3.2 (`CMP-S4-16`), §7.2–§7.3 | `drift-verdict cache`, `quality-gate battery`, `gate-bearing`      |
| `operational-log derived-extract producer` | The component that derives the minimized, allowlisted, **payload-free** `PLA-*` aggregate every log-derived claim must go through, under a named measurement-contract version. It reads the measurement-contract register **in place** and holds no copy of it; only the contract version identifier crosses into this system, on the extract.                                                     | _unbuilt — architecture_   | `docs/research/C010-system-and-repository-architecture/05_system-context-and-responsibility-boundaries.md` §3.2 (`CMP-S4-20`), §8.2 | `mcp_request_log`, `operation_event_log`                           |
| `gate-bearing`                   | A property of a value, not a component: a value is **gate-bearing** if changing it would change whether the system permits something — whether a learner advances, whether a content unit is served, whether a call is authorized, or when a review falls due. The browser holds **nothing** gate-bearing, under every rendering model.                                                                        | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/05_system-context-and-responsibility-boundaries.md` §6.1 | `content serve path`, `mastery`, `context token`                   |
| `isolation invariant`            | A property of a **state category**, not a component: every read and write of the category a request can cause is confined to the instances owned by the request's `authenticated principal`, decided by the server from server-held state, identically on **every** transport. Evaluated as an ordered five-check decision procedure returning exactly one of six verdicts — `not-applicable`, `not-evaluable`, `fails-confinement`, `fails-transport`, `fails-principal`, `holds`. **No category reaches `holds` today.** | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md` §3 | `authenticated principal`, `state category`, `context token`, `gate-bearing` |
| `authenticated principal`        | Whoever the server has decided a request is, as distinct from a **learner**. Resolved at the transport edge as the JWT `sub` claim falling back to `azp`, so under a client_credentials grant the principal is an **OAuth client**, not a person — and the resolved value records no trace of which claim it came from. Per-principal confinement is therefore **not** per-learner confinement. On STDIO no principal is produced at all. | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md` §4.1 | `isolation invariant`, `context token`                             |
| `authority` (of a state category) | The **single** component permitted to **write** a state category — not the only one that may read it, and not necessarily the one that holds the bytes. Exactly one per category, always a component id and never a role or a layer; a component may issue a write it does not have authority over (recorded as the *write path*), and a component may hold a value it has authority over nothing of. Never shared: a category that cannot take one authority is a routed finding, not a split. | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/08_per-state-authority-matrix.md` §3, §8 | `isolation invariant`, `gate-bearing`, `drift-verdict cache`, `quality-gate battery` |
| `target state` (of an isolation evaluation) | The named system state an `isolation invariant` verdict is evaluated **against** — one of three forms: *as it stands* at a cutoff, *assigned* (the category's row plus its named `authority`), or *composed* (either of those plus an **explicitly enumerated** set of outstanding changes assumed landed). The same category returns different verdicts under each, so **a verdict quoted without its target state is not a result**, and a composed state that does not enumerate its assumptions is void. | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/09_authority-matrix-validation.md` §4.2 | `isolation invariant`, `authority`, `authenticated principal` |
| `distribution line` | The boundary between what is published and what is withheld: the general-purpose MCP core is **public MIT**; the DP course application is **private and closed**. Crossing it is a *publication* event in one direction and a *withholding* event in the other, and the two errors are not equal — publication is irreversible, withholding is recoverable. Every classification a capability receives records which side it lands on. | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/12_application-versus-core-rule-and-compatibility-contract.md` §5 | `application-specific capability`, `reusable core capability`, C010 charter assumption 32 |
| `application-specific capability` | A capability that **cannot be stated without naming an artifact of a particular course** — a syllabus, qualification, subject taxonomy, grade scale, content map, or a node within one. Decided by rewriting the capability's statement with every course proper noun replaced by a free variable: if the rewrite is no longer well-formed, the capability is application-specific and lands on the private, closed side. Also the rule's **default** for anything no clause classifies, deliberately asymmetric because publication cannot be undone. | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/12_application-versus-core-rule-and-compatibility-contract.md` §5 (`R8-1`, `R8-5`) | `reusable core capability`, `distribution line`, `DR-C10-S8-1` |
| `reusable core capability` | A capability an operator with **no relationship to any course of ours** would want, **and** which is statable in vocabulary the public surface already publishes. **Both halves must hold** — wanting it without shared vocabulary drags private concepts into the public surface under general-sounding names, which is the quiet form of the publication failure. A capability that decomposes into a course-noun-free mechanism plus a course-specific policy value is **split**, not forced one way: mechanism to core, values to the application, never compiled in. | _cross-cutting — architecture_ | `docs/research/C010-system-and-repository-architecture/12_application-versus-core-rule-and-compatibility-contract.md` §5 (`R8-3`, `R8-4`) | `application-specific capability`, `distribution line`, `DR-C10-S8-1` |
| `regression boundary` | The set of published surface properties this architecture undertakes not to break — tool names, input schemas, the snake_case convention, the `toolData`/`toolError` envelope, `content_quality` as the sole carrier of structured `findings`, prompt names and `argsSchema`, and the exempt-tool list. A change altering one is *contract-changed* and owes a named detection method. Two of the seven are **routing** properties invisible to any structural diff of the tool manifest. | `server` / `transport` | `docs/research/C010-system-and-repository-architecture/12_application-versus-core-rule-and-compatibility-contract.md` §8.1 | `context token`, `gated tool` |
| `token-bound identity` | The obligated placement of the authenticated principal: bound to the **context token at issue time**, server-side, rather than carried as a per-call tool argument. Chosen on **forgeability** — an identity in a tool argument is caller-supplied, whereas the token is server-minted, so binding at issue time leaves the caller nothing to forge. Costs **zero** new schema declarations (all 43 gated tools already declare `context_token`); what changes is the argument's *meaning*, which a schema diff cannot see. **HTTP-qualified**: STDIO produces no principal to bind. | `transport` | `docs/research/C010-system-and-repository-architecture/decision-records/DR-C10-S8-2_token-bound-identity-over-per-call-identity.md` | `context token`, `authenticated principal`, `isolation invariant`, `regression boundary` |
| `state category` | One individuated unit of system state — a table, a column group, a module-level mutable structure, or a read-time aggregate with no backing column — carrying exactly one `authority`. The package's 45 categories are `SC-S3-1` … `SC-S3-45`. Individuated by whether two pieces of state could ever have **different** authorities, lifetimes or learner-scoping answers, so it is neither "one table" nor "one variable" by default. Its **learner-scoping** column records an open *question*, **never** a schema fact: no ownership column exists on any table today. | `infrastructure` / cross-cutting — architecture | `docs/research/C010-system-and-repository-architecture/04_state-category-inventory.md` §2, §7; `decision-records/DR-C10-S3-1_state-category-individuation.md` | `authority`, `isolation invariant`, `target state`, `gate-bearing` |
| `read-projection` | One named, resource-shaped **read** the general web API exposes — a projection over state the API does **not** own, never a query language and never a route. Eleven are enumerated. A read-projection confers no write authority whatever: the web tier holds **0 of 45** authorities, so every projection is a view onto state the MCP core writes. Counterpart of `write-intent`. | _unbuilt — architecture_ | `docs/research/C010-system-and-repository-architecture/11_web-api-scope-and-resource-inventory.md` §9; `decision-records/DR-C10-S15-3_api-protocol-style.md` | `write-intent`, `authority`, `state category` |
| `write-intent` | One named **mutation the web API may request**, expressed as an intent rather than a resource write — the API states *what is wanted*, the owning component decides whether it happens. Five are enumerated. Because the web tier owns no state category, a write-intent is always a request across an authority boundary, never a direct write; the enclosing endpoint path, payload schema and error catalogue are deliberately **not** specified anywhere in the package. Counterpart of `read-projection`. | _unbuilt — architecture_ | `docs/research/C010-system-and-repository-architecture/11_web-api-scope-and-resource-inventory.md` §9; `decision-records/DR-C10-S15-3_api-protocol-style.md` | `read-projection`, `authority`, `gate-bearing` |
| `split-visibility workspace` | The **selected** repository topology (`T2`): **one** repository holding both the public MIT core and the private, closed course application, with visibility split **within** the tree rather than across repositories. Not a public repository with private parts, and not two repositories — the phrase's obvious reading ("some files are hidden from some readers") is wrong. The whole workspace is private today; the `distribution line` runs inside it, and publication happens by **packing and publishing the core**, never by making the repository public. | `repository` / cross-cutting — architecture | `docs/research/C010-system-and-repository-architecture/14_repository-topology-decision.md` §4, §5; `decision-records/DR-C10-S9-1_repository-topology.md` | `distribution line`, `reusable core capability`, `application-specific capability` |
| `unattributed learner content` | One of four `personal-data status` values in the learner-data inventory: content authored by, or describing, a learner with **no** column anywhere in the store that attributes it to one. It exists as a distinct value because **no ownership column exists on any table today**, so no persisted category can be `learner-linked` on the data alone — attribution is a property of the single-tenant *deployment*, not of any stored value. A personal/not-personal binary cannot express this, which is why the vocabulary has four values and not two. | `infrastructure` / cross-cutting — architecture | `docs/research/C011-safe-production-integration-and-learner-isolation/03_learner-data-inventory-and-classification.md` §2; `decision-records/DR-C11-S3-1_learner-data-classification-scheme.md` | `state category`, `authenticated principal`, `isolation invariant`, `sixth copy class` |
| `sixth copy class` | The copies of real learner-derived data that the **C011 research package's own activity** creates — chiefly its captured production evidence — governed by the same erasure duty as any other copy rather than exempted as work product. Membership turns on **derivation, not on a label**: a synthetically generated artifact is not a member however production-shaped it looks. Its terms (owner, retention bound, destruction condition, redaction discipline, quarantine path) were set before any member existed, and **empty membership is not absent terms** — the class is inventoried with zero known members so a data right can still be propagated through it. | _cross-cutting — research package_ | `docs/research/C011-safe-production-integration-and-learner-isolation/01_production-evidence-and-the-access-audit.md` §6 (terms); `03_learner-data-inventory-and-classification.md` §8 (inventory entry `LD-S3-31`) | `unattributed learner content`, `state category` |

## Identity and ownership (C011 safe production integration and learner isolation)

_Vocabulary introduced by the C011 decision package and **not yet built** — no ownership column exists on any table today, so "Owning module" records where these would live rather than where they do. These terms are deliberately distinct from C010's `authenticated principal`, which they refine rather than replace._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `learner key` | The value that **owns** a learner's rows: the OIDC `sub` claim, written **verbatim** (no hash, prefix or normalization) into the `user_id NOT NULL` column `NEU-850`'s `OUT-2` fixes. **Not** the same as C010's `authenticated principal` — a principal may be an OAuth client, which yields **no** learner key at all — and **never** `azp`, which is never a learner key under any principal shape. A token with no usable `sub` produces no learner key. Nothing to do with a `context token`, which authorizes a call rather than owning a row. | _unbuilt — architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/02_identity-the-learner-key-and-principal-kind.md` §3; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S2-1_the-persisted-learner-key.md` | `principal kind`, `authenticated principal`, `isolation invariant`, `token-bound identity` |
| `bound context token` | A `context_tokens` row that carries a principal written **at mint time** — `principal_id`, `principal_kind` and `principal_claim_source` — so the token authorizes **one** principal rather than any caller holding the id. Contrast an **unbound token**: any row as the table stands today, which carries `id`/`created_at`/`expires_at` only and therefore passes the gate for whoever presents it. The identifier is a `learner key` **if and only if** the kind is `user`; reading it without the kind rebuilds the `sub`-or-`azp` collapse one layer below the transport. Unbound tokens are **rejected, never grandfathered**, and are purged rather than backfilled. | _unbuilt — `transport` / `infrastructure`_ | `docs/research/C011-safe-production-integration-and-learner-isolation/04_the-stdio-identity-gate-and-the-bound-context-token.md` §4; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S4-2_what-the-context-token-row-carries.md` | `context token`, `learner key`, `principal kind`, `token-bound identity`, `configured transport principal` |
| `configured transport principal` | The principal a transport that carries **no token** gates on: an identifier plus an **explicitly declared** `principal kind`, read from server-held deployment configuration at start-up and never influenced by the caller. Introduced for STDIO, where no credential reaches the server and — because the production learner path is a manually provisioned static client rather than DCR — no self-registration path exists for a client to obtain one. It satisfies check `I5` because configuration is state the server holds and the kind is declared rather than inferred. **Where none is configured the transport refuses every gated tool and mints nothing** — inert, not open. It is a **per-process singleton**: one process serves one principal, so a shared STDIO process confines two learners to one identity. Like `principal kind = user`, it certifies no human. | _unbuilt — `transport`_ | `docs/research/C011-safe-production-integration-and-learner-isolation/04_the-stdio-identity-gate-and-the-bound-context-token.md` §3, §5; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S4-1_the-stdio-identity-gate.md` | `principal kind`, `bound context token`, `authenticated principal`, `isolation invariant` |
| `principal kind` | The **determined** discriminator recording which claim a resolved identity came from: `user` when the verified token carries a `sub`, `client` when it does not and carries an `azp`. Determined by **`sub`-presence**, never inferred from the audience shape (a rule that would misclassify this deployment's actual learner, who arrives on a static client). Carried alongside the identity rather than re-derived, because the token is gone below the transport edge — it is what makes C010's check `I5` *evaluable*. **It records the claim, not humanity:** `user` does **not** certify a natural person; that question is charter assumption 35 / `A-S2-1` and is still open. A `client`-kind principal is admitted as a **service principal holding no learner state**. | _unbuilt — architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/02_identity-the-learner-key-and-principal-kind.md` §3, §6, §7; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S2-2_principal-kind-and-the-service-principal-disposition.md` | `learner key`, `authenticated principal`, `isolation invariant`, `token-bound identity` |

## Attribution and detection (C011 safe production integration and learner isolation)

_Vocabulary introduced by the C011 attribution-and-detection chapter and **not yet built** — no log table carries a principal column today, and no signal, alert route or completion proof exists anywhere in the deployment. "Owning module" therefore records where these would live. Each term is deliberately distinct from a nearby domain word it would otherwise be confused with, and that contrast is stated in the definition rather than left to a reader._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `attribution carrier` | The **server-derived column pair** that makes a persisted record attributable to a principal: `principal_kind` (`NOT NULL`, exactly `user` \| `client` \| `none`) plus `learner_key` (the OIDC `sub` verbatim, non-null **iff** the kind is `user`). Deliberately **not** `session_id` or `correlation_id` — both are **caller-asserted** (`src/transport/audit-middleware.ts:94`–`:99`; `src/transport/http.ts:154`–`:157`) and a caller-asserted principal fails C010's check `I5`. Three-valued rather than two so a **service principal is a countable state, not a silence**: folding `client` into `none` would make a machine principal wired into a learner path indistinguishable from an unauthenticated request. | `transport` / `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/16_attribution-and-detection.md` §2; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S16-1_the-attribution-carrier.md` | `principal kind`, `learner key`, `authenticated principal`, `token-bound identity` |
| `completion proof` | One record asserting that **one action, on one copy, for one data-right request, is done** — the thing a stalled-propagation signal reads. Fixed at **nine required non-null fields** (`propagation_id`, `request_kind`, `learner_key`, `copy_class`, `action`, `rows_affected`, `emitted_at`, `deadline_at`, `emitter`), in a durable store that is queryable on `propagation_id` alone, is **not** either log table, and is **not** process-local. **`rows_affected = 0` is legal and must be distinguishable from the field being absent**, and a copy with nothing to do emits `action = not-applicable` rather than no proof — because the signal cannot otherwise tell *nothing to do* from *nothing done*. Not a log line, not an audit row, and not an absence of error. | `orchestration` / `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S16-3_the-stalled-propagation-signal-contract.md`; `docs/research/C011-safe-production-integration-and-learner-isolation/16_attribution-and-detection.md` §6 | `signal contract`, `missing emission`, `learner key`, `sixth copy class` |
| `signal contract` | A published statement of **the exact shape of the evidence a detection signal reads** — required fields, where they live, and the timing they are evaluated on — written so a **later** sub-task can conform to it without asking a question. The direction is **forward-only**: the contract's author asserts only that it is conformable; the conforming party's own acceptance asserts the match. Distinct from a **signal**, which is the detection rule itself, and from a **threshold**, which is the value the rule compares against. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S16-3_the-stalled-propagation-signal-contract.md`; `docs/research/C011-safe-production-integration-and-learner-isolation/16_attribution-and-detection.md` §6 | `completion proof`, `missing emission` |
| `missing emission` | A datum a detection signal requires that **the deployment does not emit today**, recorded with a named owner rather than assumed available. It is the honest alternative to a signal specified against imaginary input: seven are named for C011's detection matrix, including the absent principal column, the total absence of any audit record on STDIO, and the fact that audit emission is itself conditional on a database URL (`src/transport/http.ts:177`–`:182`). Distinct from an **open item** (an unanswered question) and from a **cap** (a bound on what published scope may be read to establish) — a missing emission is a known absence with a known owner. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/16_attribution-and-detection.md` §4 | `signal contract`, `completion proof`, `attribution carrier` |

## Enforcement and confinement (C011 safe production integration and learner isolation)

_Vocabulary introduced by the C011 enforcement-point chapter and **not yet built** — no ownership column, no principal-scoped adapter and no confinement predicate exists in `src/` today, so "Owning module" records where these would live. Each term names a **place** or a **rule**, never a component, because the whole point of the outcome is that confinement is decided somewhere specific rather than everywhere generally._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `enforcement point` | The **single place** at which confinement is mechanically implemented for a port: the Drizzle adapter, with the predicate written inside the query the adapter method already issues. Named per port rather than once for the system, because three ports cannot take the uniform mechanism and say so. It is deliberately **not** the transport gate (HTTP-only, and fails open on internal error at `src/transport/context-token-middleware.ts:83`–`:86`), **not** orchestration (above the port boundary, which is what puts the existing single-learner guard outside `A-28`'s envelope) and **not** the tool layer (46 registration sites, each able to forget). Distinct from the `isolation invariant`, which is the property being satisfied, and from a `confinement predicate`, which is the mechanism it uses. | _unbuilt — `adapters`_ | `docs/research/C011-safe-production-integration-and-learner-isolation/05_the-enforcement-point-that-confines-every-read-and-write.md` §2, §3; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S5-1_the-enforcement-point.md` | `confinement predicate`, `indivisible principal pair`, `enumerated access-path set`, `isolation invariant`, `learner key` |
| `confinement predicate` | The SQL condition that restricts a statement to one principal's rows, conjoined **inside** the adapter method to whatever predicate the query already carries — and, on an insert, the value the ownership column is **set** from rather than accepted from the caller. Its presence is decided by `principal kind`: a `user` principal gets the predicate, a `client` or `none` principal gets a **refusal**, never an empty result set. **An aggregate is confined if and only if the predicate applies to the row set *before* aggregation** — a `COUNT(*)` over an unconfined set discloses a cross-learner fact while returning no rows, so "returns no rows" is not a confinement argument. | _unbuilt — `adapters`_ | `docs/research/C011-safe-production-integration-and-learner-isolation/05_the-enforcement-point-that-confines-every-read-and-write.md` §2, §6.3 | `enforcement point`, `principal kind`, `learner key`, `indivisible principal pair` |
| `indivisible principal pair` | The `(principal_id, principal_kind)` tuple taken as **one value** at the port boundary, fixed at adapter construction rather than passed per call. Indivisible because `principal_id` is a `learner key` **iff** the kind is `user`, and nothing in the schema can enforce a rule of the form "this column means something different depending on that column" — so an adapter that could receive the identifier without the kind would silently treat a service principal's `azp` as an owner key. Constructor-bound rather than method-bound because a value orchestration can pass is a value orchestration can vary, and a variable principal is caller-asserted at the point of use, which fails check `I5`. | _unbuilt — `ports` / `adapters`_ | `docs/research/C011-safe-production-integration-and-learner-isolation/05_the-enforcement-point-that-confines-every-read-and-write.md` §2; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S5-1_the-enforcement-point.md` | `principal kind`, `learner key`, `bound context token`, `enforcement point` |
| `enumerated access-path set` | The **complete, closed list of every SQL statement that reaches one state category**, reads and writes alike — the artifact check `I3` requires before a category may return `holds`, and which C010 records that no sub-task owed. Closure is a property of the **module boundary**, not of search effort: for `public.notes` it holds because the table object is imported in exactly one file, no raw SQL names the table, `UnitOfWorkPort` does not compose the port, and the only other reference is a test-only truncate. **Absence of a found unscoped path is not an enumeration** — `I3` may be *failed* from one counter-example but may never be *passed* by failing to find one. Bound to a stated cutoff: a new import breaks it. | _cross-cutting — architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/05_the-enforcement-point-that-confines-every-read-and-write.md` §8.3; `docs/research/C010-system-and-repository-architecture/06_isolation-invariant-and-the-neu-893-split.md` §3.4.1 | `isolation invariant`, `state category`, `target state`, `enforcement point` |
### C011 — consent, export and erasure (SUB-8 / NEU-1002)

_Vocabulary introduced by the C011 consent-and-data-rights chapter and **not yet built** — there is no consent record, no export path and no erasure path anywhere in the product, and `consent`, `gdpr`, `dsar` and `erasure` return zero hits across `src/` and `drizzle/`. "Owning module" therefore records where each would live. Each term is deliberately distinguished from the nearby everyday word a reader would otherwise substitute for it._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `severability test` | The published test that decides whether a processing purpose rests on consent: **a purpose is consent-governed if and only if the service continues to function when that purpose is switched off.** It exists so the consent boundary is falsifiable by a reader rather than a list they must accept — which matters here because **zero** of the 32 inventory categories rested on consent before it was applied, so the boundary is created rather than inherited. Its consequence is the one readers get wrong: a learner's own study material is **contract**, not consent, so withdrawal does not delete it. | `orchestration` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/08_consent-and-what-a-learner-can-export-and-erase.md` §3; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S8-1_the-consent-record-and-the-consent-boundary.md` | `consent purpose`, `retention exception`, `learner key` |
| `consent purpose` | A **named severable processing purpose** that consent governs, identified `CP-S8-<k>` — the unit consent is captured against, withdrawn against, and versioned against. Deliberately **not** a table, a category or a feature: consent attaches to *why* data is processed, so one purpose may span several inventory categories and one category may be touched by none. Exactly three exist (`CP-S8-1` rule-validation corpus, `CP-S8-2` scheduling-algorithm evaluation, `CP-S8-3` third-party model processing), and `CP-S8-3` is severable only **conditionally**, on a deployment fact that is unobserved. | `orchestration` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/08_consent-and-what-a-learner-can-export-and-erase.md` §3 | `severability test`, `consent record`, `withdrawal` |
| `consent record` | The **append-only, versioned** store of a learner's consent decisions — one row per grant and **a new row per withdrawal, never an update**, because an overwritten row cannot prove what was in force when. Carries `learner_key` (the OIDC `sub` verbatim, never `azp`) and a `policy_version`, so a consent given against one statement of purposes is never silently re-read as consent to a later one. It is a **new learner-data category** the product does not have, is the only category that is `learner-identifying` on the data alone, and is **retained after withdrawal** as the evidence that the withdrawal happened. | `orchestration` / `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/08_consent-and-what-a-learner-can-export-and-erase.md` §5 (`LD-S8-1`); `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S8-1_the-consent-record-and-the-consent-boundary.md` | `consent purpose`, `withdrawal`, `learner key`, `retention exception` |
| `withdrawal` | Revoking consent for a **consent purpose** — distinct from **erasure**, and the distinction is the one most likely to mislead. Withdrawal stops three severable secondary uses and leaves **thirty of thirty-three** categories untouched, because they rest on contract or legitimate interests rather than on consent; erasure is a separate act with a separate scope and its own deadline. Its processing switch takes effect on **the next request** — a switch that takes 30 days to flip is not a switch — while already-collected copies are purged within **7 days**. | `orchestration` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/08_consent-and-what-a-learner-can-export-and-erase.md` §4, §6, §9.1 | `consent purpose`, `erasure disposition`, `consent record` |
| `retention exception` | A named departure from a category's erasure obligation, valid **only** when it carries all four of **a justification, a time bound, an owner and a stated basis**. One that cannot be given all four is recorded as a **blocking finding**, never accepted — an unbounded exception is exactly the silent indefinite retention the rule exists to end. Six are audited for C011 and **five pass**; the consent record's own retention-after-withdrawal position is audited as one of them rather than exempted for being the package's own. Distinct from a **cap** (a bound on what a document may be read to establish) and from an **open item** (an unanswered question). | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/08_consent-and-what-a-learner-can-export-and-erase.md` §9 | `erasure disposition`, `consent record`, `missing emission` |
| `erasure disposition` | The per-category verdict on what erasure actually does — one of `delete`, `cascade`, `de-identify`, `not-applicable` or **`unreachable`**. **`unreachable` is a real value and is used**: it names personal data that **no per-learner predicate can select**, most importantly every audit-log row written before attribution existed, which no later process can key because the only structure that ever held the binding is process-local and emptied by every restart. Recording such rows as `delete` would be the *"erasure completes on paper"* failure written into the design. | `orchestration` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/08_consent-and-what-a-learner-can-export-and-erase.md` §8, §8.2 | `retention exception`, `attribution carrier`, `completion proof`, `sixth copy class` |

### C011 — the disposition of unowned rows (SUB-6 / NEU-1000)

_Vocabulary introduced by the C011 migration chapter and **not yet built** — no ownership column exists on any of the 14 production tables, and a grep for `user_id`, `learner_id`, `owner_id` and `tenant_id` across `src/` and `drizzle/` returns zero hits. "Owning module" therefore records where each would live. Each term is deliberately distinguished from the nearby term a reader would otherwise conflate it with — most importantly `migration disposition` from `erasure disposition`, which are different verdicts on the same rows._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `migration disposition` | The per-**table** verdict on what the one-time ownership migration does to a table's *existing* rows — one of `backfill`, `backfill-by-join`, `purge`, `archive` or `no-key-owed`. **Deliberately not the same vocabulary as an `erasure disposition`, and the two must never be collapsed**: an erasure disposition answers *what a learner's request does to a category, on every request thereafter*; a migration disposition answers *what the migration does to a table, once, at cutover*. Assigned by partitioning the 14 tables on **what evidence can attribute their rows** rather than on data class or schema, because two tables holding the same kind of learner free text (`notes` and `mcp_request_log`) take opposite dispositions for reasons that are entirely about attribution. | cross-cutting (`infrastructure` / `adapters`) — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/06_the-disposition-of-every-unowned-row.md` §2, §3; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S6-1_the-migration-disposition-scheme.md` | `erasure disposition`, `pre-cutover population`, `learner key`, `attribution carrier` |
| `pre-cutover population` | The rows written **before the attribution carrier lands** — they carry no `learner key` and **can never be given one**, because the only structure that ever held a session-to-subject binding is process-local and emptied by every restart. The set is addressable **only as a whole**, which is why the two per-learner mechanisms fail on it in *opposite* directions: erasure under-reaches it (a `DELETE … WHERE learner_key = $1` reports success while the whole population survives) and confinement over-reaches it (a per-learner predicate matches none of it, hiding it from everyone, including its author). Its `migration disposition` is `archive` — closing the set at the cutover instant so it becomes finite and countable — which is **not** a discharge of the erasure duty over it. | `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/06_the-disposition-of-every-unowned-row.md` §4; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S6-2_archiving-the-pre-cutover-log-population.md` | `migration disposition`, `erasure disposition`, `attribution carrier`, `confinement predicate` |
| `input-closure argument` | The method by which a generated dataset is proven to contain **no row copied out of production**: enumerate the generator's inputs exhaustively and show that **none has row type** — schema text carries no rows, and `COUNT`/`MIN`/`MAX` return scalars, which cannot carry a row even in principle. Used in place of an empirical row-by-row diff, which is *self-defeating* here because performing it would require extracting the very rows it exists to disprove the presence of. Stronger than the empirical form: it quantifies over **every** dataset the generator can emit rather than the one instance a diff would sample. Its falsifier is stated in terms of input *type* — any row-valued input (a `SELECT *`, a `LIMIT` sample, a `DISTINCT` over a content column) breaks the closure and overturns the exclusion. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/06_the-disposition-of-every-unowned-row.md` §7.2; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S6-3_aggregate-then-generate-and-the-exclusion-evidence.md` | `sixth copy class`, `migration disposition` |

### C011 — the rollout sequence (SUB-7 / NEU-1001)

_Vocabulary introduced by the C011 rollout chapter and **not yet built** — no control named below exists in `src/`, and the sequence has never been executed. "Owning module" therefore records where each would live. `rollout stage` is deliberately distinguished from `tier` (a stage of the content-audit pipeline) and from `migration disposition` (a per-table verdict, not a step); `containment` is deliberately distinguished from `topic`→`chunk` containment, which is a structural relation and not an operational act._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `rollout stage` | One numbered step of the ten-step sequence (`T0` … `T9`) that carries learner isolation into production. **Not a `tier`** (a stage of the content-audit pipeline) and **not a `migration disposition`** (a per-table verdict): a rollout stage is a unit of *scheduling*, and each one carries six things — an entry condition, an exit condition, a measurable isolation signal, a health signal, a named owner, and either a `deploy-independent disable path` or a named exception. A stage maps to **one merge to `develop`** and therefore to one deploy and one container restart, which is why the stage count is an input to the availability budget rather than a presentational choice. | cross-cutting (`infrastructure` / `transport`) — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/07_the-rollout-sequence-and-what-each-stage-cannot-undo.md` §3, §6; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S7-1_the-rollout-stage-order.md` | `deploy-independent disable path`, `containment`, `migration disposition`, `attribution carrier`, `tier` |
| `deploy-independent disable path` | A way to turn a landed behaviour **off without shipping a revert through the deploy pipeline**. On this deployment it is concretely an operator-set environment variable on the off-repo compose stack at `/home/deploy/docker-services/second-memory-mcp`, applied over SSH. It reuses the **shape** of the existing `CLASSIFIER_ENABLE` toggle but **not** its procedure: that toggle's own runbook requires a deploy to apply it (`docs/runbooks/classifier-blocking-activation.md:261`–`:262`), so the deploy-independent application is **specified, not precedented**. **Deploy-independent does not mean free:** every such variable is read at boot (`src/transport/main.ts:42`–`:43`, `src/composition-root.ts:379`), and boot runs the migrator first (`src/transport/main.ts:27`), so applying one costs a container restart that re-executes the migration — which means a pause can only ever land *between* batches, never during one. A stage with no such path is **named as an exception with a reason and an owner**, never left blank. | `shared` / `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S7-2_the-deploy-independent-disable-path.md`; `docs/research/C011-safe-production-integration-and-learner-isolation/07_the-rollout-sequence-and-what-each-stage-cannot-undo.md` §8; pattern precedent at `src/config/resolve-classifier-config.ts` | `rollout stage`, `containment` |
| `containment` | Stopping a landed behaviour **without returning the system to its prior state** — the operational sense, distinct from the structural `topic`→`chunk` containment relation elsewhere in this glossary. Kept deliberately separate from **reversal**, which undoes the change itself: a stage's disable position states which behaviour stops, which persisted state *remains*, and whether the next `rollout stage` can still be entered from it. The distinction is load-bearing on a deployment where a schema change and its deployment are not separable events, because without it there is no step at all between detecting a regression and reverting a deploy. Containment is **not always safe ground**: disabling the enforcement predicate contains a regression by returning the system to unconfined behaviour. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/07_the-rollout-sequence-and-what-each-stage-cannot-undo.md` §9, §10.1 | `deploy-independent disable path`, `rollout stage`, `subject` |
## Client compatibility (C011 safe production integration and learner isolation)

_Vocabulary introduced by the C011 client-compatibility chapter. Two of the three terms name
properties of code that **does** exist in `src/` today and can be checked against it; the third names
an abstraction that deliberately **does not** exist, which is the whole content of the finding that
introduced it._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `gate-exempt tool` | One of the **exactly three** registered MCP tools the context-token gate does not intercept — `init_agent_context`, `get_server_info`, `get_server_workflow`. Membership is **doubly determined and not structurally coupled**: each declares an empty input schema (`z.object({}).shape`) *and* each is named in the middleware's hard-coded `EXCLUDED_TOOLS` literal, and nothing keeps the two in step, so their agreement is a fact about a cutoff rather than an invariant. Exempt means *not gated*, never *not authenticated*: on HTTP the JWT middleware still covers them; on STDIO nothing does. Distinct from the 43 **gated** tools, every one of which already declares `context_token`. | `transport` / `server` | `src/transport/context-token-middleware.ts:5`–`:9`; `docs/research/C011-safe-production-integration-and-learner-isolation/11_the-client-compatibility-contract.md` §1.2, §4.2 | `context token`, `semantics-only change`, `transport-neutral seam` |
| `semantics-only change` | A change that alters what a tool call **means** while leaving its **schema shape** byte-identical, so a tool-manifest diff detects nothing. The canonical instance is `context_token` ceasing to mean *"you called `init_agent_context`"* and beginning to mean *"you are this principal"* — zero of the 43 gated schemas change, because every one already required the argument. Its defining property is that the **obvious** compatibility check returns green, so it is detectable only by a **behavioural probe** asserting on refusals and row visibility, and specifically by probes written in the negative (*a refusal, not an empty result*) because the failures it hides present as success. | `transport` — _rule, not a component_ | `docs/research/C011-safe-production-integration-and-learner-isolation/11_the-client-compatibility-contract.md` §4.1 | `context token`, `gate-exempt tool`, `token-bound identity` |
| `transport-neutral seam` | The interposition point a gate would need in order to run on **both** transports — a request-like carrier holding the JSON-RPC envelope, a response writer that can short-circuit, and an ordered chain with a `next()`. **It does not exist in the tree**, which is the point: every middleware is an Express `RequestHandler`, the STDIO limb is three statements with nothing interposed, and the MCP SDK offers no documented hook between a `tools/call` arriving and its registered handler running. So "mount the gate on STDIO" is a **rewrite, not a mount**, and it forks on whether the seam wraps the server (touching `src/transport/` only) or wraps each handler (touching all 46 registration sites, where one missed site yields a tool believed gated and ungated). | `transport` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/11_the-client-compatibility-contract.md` §6.1, §6.2; `docs/research/C011-safe-production-integration-and-learner-isolation/91_findings-register.md` § `F-S4-4` | `gate-exempt tool`, `semantics-only change`, `enforcement point` |

### C011 — propagation and completion proof (SUB-9 / NEU-1003)

_Vocabulary introduced by the C011 propagation chapter and **not yet built** — no completion-proof store exists, no propagation emits anything, and no disposal has been executed. "Owning module" records where each would live. Both terms below are defined chiefly by what they are **not**: a `propagation action` is the third and last verdict axis over the same rows, and confusing it with the other two is the exact misreading `R-S6-1` was registered to prevent._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `propagation action` | The per-**copy-class**, per-**duty** verdict on what a data right actually does to a copy — the cell of the propagation matrix. It is the **third** verdict axis over the same rows and must not be collapsed into either of the other two: an `erasure disposition` answers *what a request means for a category*, a `migration disposition` answers *what the cutover does to a table once*, and a propagation action answers *what reaches a given copy, by what mechanism, by when, and with what proof*. The distinction is load-bearing because a class can take `archive` on the migration axis and `delete` on the propagation axis without contradiction — relocation is not disposal. Only three of the six classes are reachable by a statement the deployment can issue; the rest propagate by **instruction** (browser-side state), by **citation** (backups, unestablished) or **by hand** (the sixth copy class, behind no port). | `orchestration` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/09_proving-a-data-right-reaches-every-copy.md` §7; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S9-1_the-pre-cutover-population-disposition.md` | `erasure disposition`, `migration disposition`, `pre-cutover population`, `completion proof`, `write-path closure` |
| `write-path closure` | The method by which a **copy set** is proven complete: enumerate every channel by which a byte can leave the process — persistence, disk, network, memory, protocol response, platform backup, and the research package's own capture activity — and show each terminates in an enumerated copy class or in a **named exception**. Used in place of enumerating *stores*, which is unfalsifiable: a store nobody thought of is invisible to a survey of stores somebody thought of, whereas the write set is bounded by the source tree. The mirror of an `input-closure argument` — that one closes over what *enters* a generator, this one over what *leaves* a process. Its falsifier is a single exhibited write of learner-derived data terminating outside the classes and outside the named exception; re-running it costs four greps and needs no production access. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/09_proving-a-data-right-reaches-every-copy.md` §4; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S9-2_the-copy-set-closure-argument.md` | `input-closure argument`, `propagation action`, `sixth copy class`, `completion proof` |

### C011 — the threat model and its gates (SUB-12 / NEU-1005)

_Vocabulary introduced by the C011 threat model and **not yet built** — no gate has been evaluated and no threshold is a measurement. "Owning module" records where each concept would live. `ingress closure` is the deliberate mirror of `write-path closure`, and the pair should be read together: one closes over what leaves a process, the other over what reaches learner state._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `threat path` | One route by which learner state can be read, written or influenced, identified as a pair of an **ingress surface** and a **path class** (read, write, session, retrieval, context-token, analytics, migration, operator, web API, egress, and the research package's own captures). Each carries an explicit **isolation** or **lifecycle** invariant and exactly one of three verdicts: `held-by-design` (satisfied under the C011 mechanism as designed — never a claim about the deployment), `gap` (stated and not satisfied), or `out-of-reach` (stated, with no mechanism on this deployment that could satisfy it). **`out-of-reach` is not an exemption** — the path still carries its invariant, its owner, and its row in the coverage cross-check; that distinction is what keeps the operator path modelled rather than waved through. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/12_threat-model-and-the-gates-that-authorize-implementation.md` §3–§6 | `ingress closure`, `measurable gate`, `actuation channel`, `write-path closure` |
| `ingress closure` | The method by which a **threat set** is proven complete: enumerate every surface by which a request can reach learner state — the process's own entry points (HTTP, STDIO, the boot migrator) **plus the paths that reach the state without entering the process** (a direct database session, the host shell, the delivery pipeline, operator-run scripts) — and show each terminates in an enumerated surface. The mirror of `write-path closure`, and it differs in one load-bearing way: a copy set is bounded by the source tree because a copy exists only where a write put it, whereas **a threat set is not**, because four of the eight surfaces execute no code in this repository. Its falsifier is a single exhibited read or write of learner state entering through none of the surfaces; the procedure is *enumerate the entry points and show each terminates*, and it is **deliberately not stated as a fixed number of greps** — that phrasing is what the write-path enumeration's own chapter repudiates after four passing greps missed an entire channel. | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/12_threat-model-and-the-gates-that-authorize-implementation.md` §2; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S12-1_closing-the-threat-set-over-ingress.md` | `write-path closure`, `threat path`, `input-closure argument` |
| `measurable gate` | A control that gates implementation authorization for one critical gap, carrying four mandatory parts — a **control**, a **threshold**, a named **owner** and an **evidence source** — plus a provenance label and the transport on which it can be evaluated. Distinct from a **package-completeness-gate** row (`G-S<n>-<k>`), which audits whether *this package* did its job; a measurable gate audits whether *the deployment* may proceed, and the two take different id prefixes (`GATE-S12-*` vs `G-S12-*`) for that reason. Because no production credential exists, a threshold's provenance must be one of exactly three: `D` derived from a cited non-production source, `S` a registered stand-in with an owner and a re-validation trigger, or `K` a deferred spike with a method and an expiry. **A gap for which no threshold of any of the three kinds can be written is recorded as a blocking finding, never as a gate with an unsettable number.** | cross-cutting — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/12_threat-model-and-the-gates-that-authorize-implementation.md` §8; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S12-3_gate-thresholds-without-a-production-observation.md` | `threat path`, `blocking finding`, `stand-in assumption` |
| `actuation channel` | A cross-learner failure in which state derived from one learner's activity **alters the controls applied to another**, while carrying no row and creating no copy. Distinguished from *disclosure*, where an unconfined aggregate reveals a true fact about another learner's data: in an actuation channel nobody learns anything, and what changes is the system's behaviour. The distinction is load-bearing because the two are closed by different dispositions — a retention or deletion bound on the underlying table resolves the disclosure half and leaves the actuation half untouched — and because an actuation channel is invisible to a confinement check that asks only which rows a principal may reach. The known instance is the Tier-2 circuit breaker consuming an unkeyed aggregate as its trip input. | `orchestration` — _existing behaviour, unremediated_ | `docs/research/C011-safe-production-integration-and-learner-isolation/12_threat-model-and-the-gates-that-authorize-implementation.md` §7; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S12-2_the-unconfined-aggregate-as-a-control-input.md` | `threat path`, `measurable gate`, `tier` |

### C011 — the DDL, the migration plan and the runbook (SUB-13 / NEU-1006)

_Vocabulary introduced by the C011 DDL-and-runbook chapter and **not yet built** — no schema object below exists, no sweep has been executed, and no control has been implemented. "Owning module" records where each would live. Each term names a mechanism chosen so that a rule a human would otherwise have to remember becomes a property of the artifact._

| Term              | Definition (project-local meaning)                                                                                                                                                                                                                                                                                                                                                                                       | Owning module              | Defining file                                                                                                    | Cross-refs                                                        |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `sweep slice` | The unit of work a migration sweep performs in **one boot**: repeated batch statements until a wall-clock budget expires or the sweep's predicate returns no rows. Bounded by **time**, not by a row target, and that is the whole point — a row-denominated batch has an unknown duration, because rows-per-second on this deployment is unmeasured, whereas a clock-bounded slice is self-limiting whatever the table holds. A row ceiling caps a single statement as a secondary guard, since the clock is only checked *between* statements. **A slice bounds the per-boot cost and says nothing about total completion**, which still scales with row counts nobody has taken — so the unknown moves from outage length to migration length rather than disappearing. | `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/13_the-ddl-the-migration-plan-and-the-runbook.md` §3.3; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S13-2_the-sweep-contract-and-batching-without-a-row-count.md` | `self-cursoring sweep`, `rollout stage`, `deploy-independent disable path`, `migration disposition` |
| `self-cursoring sweep` | A batched migration sweep whose **resume state is its own target predicate** rather than a separate progress ledger — `WHERE user_id IS NULL` for a backfill, `WHERE "timestamp" < :cutover` for an archive move, the table's emptiness for a purge. Correct after a crash, after a kill mid-batch, after two concurrent boot migrators and after an operator runs a statement by hand, because the set still to do is *defined* by the predicate at every instant. Chosen over a ledger because a ledger must be written transactionally with each batch and, when it diverges, either re-processes rows or **silently skips** them — and a skipped range surfaces only as a failed `SET NOT NULL` on a production boot several stages later. Its completion condition and its progress condition are the same expression, so the two cannot disagree. | `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/13_the-ddl-the-migration-plan-and-the-runbook.md` §3.2; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S13-2_the-sweep-contract-and-batching-without-a-row-count.md` | `sweep slice`, `cutover marker`, `migration disposition`, `pre-cutover population` |
| `generated learner key` | The `context_tokens.learner_key` column, computed by the database as `CASE WHEN principal_kind = 'user' THEN principal_id END` and stored. It makes the `bound context token`'s **iff** rule *structural*: a consumer reading it cannot obtain a service principal's identifier, because the database already consulted the kind when it computed the value. Distinct from the log tables' `learner_key`, which is a **stored, written** column constrained by a `CHECK` — the two spell the same concept and are enforced by different mechanisms because only one of the two rows holds the facts a null-ness constraint would need. **Closes `R-S4-1` on the token row and not on the ten owned tables**, where `user_id` sits with no kind beside it and no constraint can check the writer's. | `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/13_the-ddl-the-migration-plan-and-the-runbook.md` §2.2; `docs/research/C011-safe-production-integration-and-learner-isolation/decision-records/DR-C11-S13-1_the-constraint-shape-of-the-ownership-and-attribution-ddl.md` | `learner key`, `bound context token`, `attribution carrier`, `indivisible principal pair`, `principal kind` |
| `boot-time sweep runner` | The mechanism that executes a migration **data** sweep, as distinct from the one-shot Drizzle migration that carries the **schema** change. It exists because a Drizzle migration runs **exactly once** — the migrator applies only files whose journal timestamp exceeds the most recent applied row's (`node_modules/drizzle-orm/pg-core/dialect.cjs:63`) — so a batched sweep shipped as a migration would be marked applied after its first boot and could never resume. The runner instead re-enters on **every** boot, executes one `sweep slice`, and stops when its predicate returns nothing. Both halves still run at boot, so nothing about the deployment's "migrations run on boot, cannot be deferred" constraint changes; what changes is which mechanism carries which half. Not a cron job, not a background worker, and not a migration. | `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/13_the-ddl-the-migration-plan-and-the-runbook.md` §1.1, §3.2 | `sweep slice`, `self-cursoring sweep`, `cutover marker`, `rollout stage` |
| `cutover marker` | The **recorded instant the attribution carrier landed**, written by the same migration that adds the carrier columns so the two cannot drift, and read afterwards as the archive sweep's predicate. It exists because the pre- and post-cutover populations are *"distinguished by the record's timestamp against the cutover, not by the column"*: after the carrier lands a **post**-cutover row can legitimately carry `principal_kind = 'none'`, so a sweep predicated on that column would archive live rows and still leave pre-cutover rows behind. Not a feature flag, not a schema version, and not the migration ledger — it is one durable timestamp with one reader. | `infrastructure` — _unbuilt, architecture_ | `docs/research/C011-safe-production-integration-and-learner-isolation/13_the-ddl-the-migration-plan-and-the-runbook.md` §3.4; `docs/research/C011-safe-production-integration-and-learner-isolation/16_attribution-and-detection.md` §2 | `pre-cutover population`, `attribution carrier`, `self-cursoring sweep`, `migration disposition` |

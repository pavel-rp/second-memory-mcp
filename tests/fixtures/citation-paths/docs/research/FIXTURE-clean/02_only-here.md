# Fixture — a document that exists in exactly one package

Its filename is unique across the fixture corpus, so a bare citation of it from
another package resolves to exactly one candidate and is classified
`C3-bare-upstream` rather than left as a missing target.

# Fixture — decision record with a deliberately seeded defect

**Seeded C2 (missing parent segment).** A file in the decision-records folder
must cite a package-root sibling with one parent segment; this bare form is
wrong: `01_sibling.md`.

**By-design non-claim.** This line quotes a defective bare-upstream form in order
to report it as defective, and the test registry exempts it by (file, target):
`02_only-here.md`.
